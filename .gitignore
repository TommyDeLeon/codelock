node_modules/
dist/
.next/
.env
.env.local
*.log
coverage/
.turbo/
release/
*.tsbuildinfo
.DS_Store
next-env.d.ts
.cache/

# Signing material. Never in the repo — see docs/SIGNING-KEYS.md.
*.pfx
*.p12
*.jks
*.keystore
*.cer
*.crt
*.key

# Expo local state, regenerated on every start.
.expo/

# Local agent tool caches, not project content
.claude/sdd-cache/

# Scratch slug list for `import:corpus --only-file=`. Regenerated per run from
# whichever problems are currently inactive; never the same twice.
apps/api/.slugs-todo.txt
