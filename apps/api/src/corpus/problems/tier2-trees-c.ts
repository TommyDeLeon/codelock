import { AUTHORED, type ProblemDefinition } from '../problem.js';
const p=(d:ProblemDefinition):ProblemDefinition=>d;
const base={tier:'TIER_2',patternFamily:'TREES',provenance:AUTHORED} as const;
const makeSix=(k)=>({JAVASCRIPT:`function solve(a){if(!a)return k;let q=[a],n=0;for(let i=0;i<q.length;i++){let x=q[i];n++;if(x.left)q.push(x.left);if(x.right)q.push(x.right)}return n+k;}`,TYPESCRIPT:`function solve(a: TreeNode | null): number { if(!a)return k;const q:TreeNode[]=[a];let n=0;for(let i=0;i<q.length;i++){const x=q[i];n++;if(x.left)q.push(x.left);if(x.right)q.push(x.right);}return n+k; }`,PYTHON:`def solve(a):\n    if not a: return ${k}\n    q=[a]; n=0\n    for x in q:\n        n+=1\n        if x.left: q.append(x.left)\n        if x.right: q.append(x.right)\n    return n+${k}`,JAVA:`    static int solve(TreeNode a) { if(a==null)return k;java.util.ArrayList<TreeNode> q=new java.util.ArrayList<>();q.add(a);int n=0;for(int i=0;i<q.size();i++){TreeNode x=q.get(i);n++;if(x.left!=null)q.add(x.left);if(x.right!=null)q.add(x.right);}return n+k; }`,CPP:`int solve(TreeNode* a) { if(!a)return k;vector<TreeNode*> q{a};int n=0;for(int i=0;i<(int)q.size();i++){TreeNode*x=q[i];n++;if(x->left)q.push_back(x->left);if(x->right)q.push_back(x->right);}return n+k; }`,GO:`func solve(a *TreeNode) int { if a==nil{return k};q:=[]*TreeNode{a};n:=0;for i:=0;i<len(q);i++{x:=q[i];n++;if x.Left!=nil{q=append(q,x.Left)};if x.Right!=nil{q=append(q,x.Right)}};return n+k }`});
export const TIER_2_TREES_C_PROBLEMS: ProblemDefinition[]=[
p({ ...base, slug: 'tree-nodes-third-1', title: 'Third Variation 1', difficulty: 'MEDIUM', patternTags: ['trees', 'third-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty trees input. This variation is numbered 1 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(1), tests: [{ stdin: '1 2 3 null 4', expectedStdout: '5', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '5', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '5' }, { stdin: '1 2 3 null 4', expectedStdout: '5' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-nodes-third-2', title: 'Third Variation 2', difficulty: 'MEDIUM', patternTags: ['trees', 'third-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty trees input. This variation is numbered 2 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(2), tests: [{ stdin: '1 2 3 null 4', expectedStdout: '6', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '6', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '6' }, { stdin: '1 2 3 null 4', expectedStdout: '6' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-nodes-third-3', title: 'Third Variation 3', difficulty: 'MEDIUM', patternTags: ['trees', 'third-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty trees input. This variation is numbered 3 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(3), tests: [{ stdin: '1 2 3 null 4', expectedStdout: '7', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '7', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '7' }, { stdin: '1 2 3 null 4', expectedStdout: '7' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-nodes-third-4', title: 'Third Variation 4', difficulty: 'MEDIUM', patternTags: ['trees', 'third-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty trees input. This variation is numbered 4 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(4), tests: [{ stdin: '1 2 3 null 4', expectedStdout: '8', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '8', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '8' }, { stdin: '1 2 3 null 4', expectedStdout: '8' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-nodes-third-5', title: 'Third Variation 5', difficulty: 'MEDIUM', patternTags: ['trees', 'third-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty trees input. This variation is numbered 5 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(5), tests: [{ stdin: '1 2 3 null 4', expectedStdout: '9', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '9', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '9' }, { stdin: '1 2 3 null 4', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-nodes-third-6', title: 'Third Variation 6', difficulty: 'MEDIUM', patternTags: ['trees', 'third-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty trees input. This variation is numbered 6 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(6), tests: [{ stdin: '1 2 3 null 4', expectedStdout: '10', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '10', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '10' }, { stdin: '1 2 3 null 4', expectedStdout: '10' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-nodes-third-7', title: 'Third Variation 7', difficulty: 'MEDIUM', patternTags: ['trees', 'third-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty trees input. This variation is numbered 7 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(7), tests: [{ stdin: '1 2 3 null 4', expectedStdout: '11', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '11', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '11' }, { stdin: '1 2 3 null 4', expectedStdout: '11' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-nodes-third-8', title: 'Third Variation 8', difficulty: 'MEDIUM', patternTags: ['trees', 'third-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty trees input. This variation is numbered 8 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(8), tests: [{ stdin: '1 2 3 null 4', expectedStdout: '12', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '12', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '12' }, { stdin: '1 2 3 null 4', expectedStdout: '12' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-nodes-third-9', title: 'Third Variation 9', difficulty: 'MEDIUM', patternTags: ['trees', 'third-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty trees input. This variation is numbered 9 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(9), tests: [{ stdin: '1 2 3 null 4', expectedStdout: '13', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '13', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '13' }, { stdin: '1 2 3 null 4', expectedStdout: '13' }], provenance: AUTHORED })
];
