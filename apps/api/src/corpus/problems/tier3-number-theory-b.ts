import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_3', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_3_NUMBER_THEORY_B_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'proper-divisor-total', title:'Add the Proper Divisors', difficulty:'EASY', patternTags:['number-theory','divisors'], signatureId:'fn:int->int', avgSolveSeconds:480,
    promptMarkdown:['Return the sum of the positive divisors of `n` that are smaller than `n`. `n` is between 1 and 1,000,000.','', '**Example','', '```','input: 12','output: 16','```','', '`1` has no proper positive divisors, so its answer is 0.'].join('\n'), editorialMarkdown:['## Divisor-pair accumulation','', 'Every divisor below the square root has a paired divisor above it. Start the proper-divisor sum at 1 for numbers above one, then test candidate divisors through `d * d <= n`; when `d` divides n, add both members of the pair, taking care not to add a square root twice. This covers every proper divisor without checking all smaller numbers.', '', 'The quiet mistake is including n itself, which turns this into the ordinary divisor sum and is especially hard to notice on prime inputs. Handle 1 before starting at 1. Time is O(sqrt(n)), bounded by the trial-divisor range, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(n){if(n===1)return 0;let s=1;for(let d=2;d*d<=n;d++)if(n%d===0){s+=d;if(d*d!==n)s+=n/d;}return s;}','function solve(n:number):number{if(n===1)return 0;let s=1;for(let d=2;d*d<=n;d++)if(n%d===0){s+=d;if(d*d!==n)s+=n/d;}return s;}','def solve(n):\n    if n == 1: return 0\n    s = 1\n    for d in range(2, n + 1):\n        if d * d > n: break\n        if n % d == 0:\n            s += d\n            if d * d != n: s += n // d\n    return s','    static int solve(int n) { if(n==1)return 0; int s=1; for(int d=2;d*d<=n;d++)if(n%d==0){s+=d;if(d*d!=n)s+=n/d;} return s; }','int solve(int n){if(n==1)return 0;int s=1;for(int d=2;d*d<=n;d++)if(n%d==0){s+=d;if(d*d!=n)s+=n/d;}return s;}','func solve(n int) int { if n==1{return 0};s:=1;for d:=2;d*d<=n;d++{if n%d==0{s+=d;if d*d!=n{s+=n/d}}};return s }'), tests:[{stdin:'12',expectedStdout:'16',isSample:true},{stdin:'1',expectedStdout:'0',isSample:true},{stdin:'13',expectedStdout:'1'},{stdin:'36',expectedStdout:'55'},{stdin:'16',expectedStdout:'15'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'divisor-count-by-pairs', title:'How Many Divisors?', difficulty:'EASY', patternTags:['number-theory','divisors'], signatureId:'fn:int->int', avgSolveSeconds:480,
    promptMarkdown:['Return the number of positive divisors of `n`, where `1 <= n <= 1,000,000`.','', '**Example**','', '```','input: 18','output: 6','```','', 'A perfect square has an unpaired square-root divisor and must count it once.'].join('\n'), editorialMarkdown:['## Square-root divisor pairs','', 'Divisors arrive as pairs: if d divides n then n/d also divides n. Testing only d values through the square root finds each pair once, so add two for a non-square pair and one when `d * d` equals n. There is no need to scan from one through n.', '', 'The quiet mistake is always adding two, which overcounts 36 because its root 6 pairs with itself. Comparing `d * d` with n makes the square case explicit. Time is O(sqrt(n)), bounded by candidate divisors, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(n){let c=0;for(let d=1;d*d<=n;d++)if(n%d===0)c+=d*d===n?1:2;return c;}','function solve(n:number):number{let c=0;for(let d=1;d*d<=n;d++)if(n%d===0)c+=d*d===n?1:2;return c;}','def solve(n):\n    c=0\n    for d in range(1,n+1):\n        if d*d>n: break\n        if n%d==0: c += 1 if d*d==n else 2\n    return c','    static int solve(int n) { int c=0;for(int d=1;d*d<=n;d++)if(n%d==0)c+=d*d==n?1:2;return c; }','int solve(int n){int c=0;for(int d=1;d*d<=n;d++)if(n%d==0)c+=d*d==n?1:2;return c;}','func solve(n int) int {c:=0;for d:=1;d*d<=n;d++{if n%d==0{if d*d==n{c++}else{c+=2}}};return c}'), tests:[{stdin:'18',expectedStdout:'6',isSample:true},{stdin:'1',expectedStdout:'1',isSample:true},{stdin:'49',expectedStdout:'3'},{stdin:'60',expectedStdout:'12'},{stdin:'97',expectedStdout:'2'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'perfect-or-abundant-code', title:'Classify a Number by Its Divisors', difficulty:'EASY', patternTags:['number-theory','perfect-number'], signatureId:'fn:int->int', avgSolveSeconds:540,
    promptMarkdown:['Return 0 if `n` is deficient, 1 if it is perfect, or 2 if it is abundant, by comparing its proper-divisor sum with n. `1 <= n <= 1,000,000`.','', '**Example**','', '```','input: 12','output: 2','```','', 'The edge case is 1: its sum is 0, so it is deficient.'].join('\n'), editorialMarkdown:['## Proper-divisor classification','', 'Compute the proper-divisor sum using square-root pairs, then compare that one total with n. A lower total means deficient, equality means perfect, and a larger total means abundant. The comparison makes the three requested states mutually exclusive.', '', 'The quiet mistake is treating 1 as perfect because no loop runs. Its proper-divisor sum is zero, not one, so initialize and special-case it deliberately. Time is O(sqrt(n)), bounded by divisor trials, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(n){if(n===1)return 0;let s=1;for(let d=2;d*d<=n;d++)if(n%d===0){s+=d;if(d*d!==n)s+=n/d;}return s<n?0:s===n?1:2;}','function solve(n:number):number{if(n===1)return 0;let s=1;for(let d=2;d*d<=n;d++)if(n%d===0){s+=d;if(d*d!==n)s+=n/d;}return s<n?0:s===n?1:2;}','def solve(n):\n    if n==1:return 0\n    s=1\n    for d in range(2,n+1):\n        if d*d>n:break\n        if n%d==0:\n            s+=d\n            if d*d!=n:s+=n//d\n    return 0 if s<n else 1 if s==n else 2','    static int solve(int n) { if(n==1)return 0;int s=1;for(int d=2;d*d<=n;d++)if(n%d==0){s+=d;if(d*d!=n)s+=n/d;}return s<n?0:s==n?1:2; }','int solve(int n){if(n==1)return 0;int s=1;for(int d=2;d*d<=n;d++)if(n%d==0){s+=d;if(d*d!=n)s+=n/d;}return s<n?0:s==n?1:2;}','func solve(n int) int {if n==1{return 0};s:=1;for d:=2;d*d<=n;d++{if n%d==0{s+=d;if d*d!=n{s+=n/d}}};if s<n{return 0};if s==n{return 1};return 2}'), tests:[{stdin:'12',expectedStdout:'2',isSample:true},{stdin:'28',expectedStdout:'1',isSample:true},{stdin:'1',expectedStdout:'0'},{stdin:'13',expectedStdout:'0'},{stdin:'20',expectedStdout:'2'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'smallest-prime-factor', title:'First Prime Factor', difficulty:'MEDIUM', patternTags:['number-theory','factorization'], signatureId:'fn:int->int', avgSolveSeconds:600,
    promptMarkdown:['Return the smallest prime factor of `n`, where `2 <= n <= 1,000,000`. A prime number returns itself.','', '**Example**','', '```','input: 91','output: 7','```','', 'The named edge case is a prime input: no smaller divisor exists.'].join('\n'), editorialMarkdown:['## Trial division finds the first factor','', 'Test possible divisors from 2 upward and return the first one that divides n. The first successful divisor must be prime: if it were composite, one of its smaller factors would have succeeded earlier. If no candidate through the square root succeeds, n itself is prime.', '', 'The quiet mistake is returning 1 for a prime because it divides every number; 1 is not prime and is never a candidate. Time is O(sqrt(n)), bounded by the first factor or square root, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(n){for(let d=2;d*d<=n;d++)if(n%d===0)return d;return n;}','function solve(n:number):number{for(let d=2;d*d<=n;d++)if(n%d===0)return d;return n;}','def solve(n):\n    for d in range(2,n+1):\n        if d*d>n:break\n        if n%d==0:return d\n    return n','    static int solve(int n) { for(int d=2;d*d<=n;d++)if(n%d==0)return d;return n; }','int solve(int n){for(int d=2;d*d<=n;d++)if(n%d==0)return d;return n;}','func solve(n int) int {for d:=2;d*d<=n;d++{if n%d==0{return d}};return n}'), tests:[{stdin:'91',expectedStdout:'7',isSample:true},{stdin:'29',expectedStdout:'29',isSample:true},{stdin:'2',expectedStdout:'2'},{stdin:'64',expectedStdout:'2'},{stdin:'221',expectedStdout:'13'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'distinct-prime-factor-total', title:'Count Different Prime Factors', difficulty:'MEDIUM', patternTags:['number-theory','factorization'], signatureId:'fn:int->int', avgSolveSeconds:660,
    promptMarkdown:['Return how many distinct prime numbers divide `n`, where `2 <= n <= 1,000,000`. Repeated copies count once.','', '**Example**','', '```','input: 72','output: 2','```','', '`72 = 2^3 × 3^2`, so only 2 and 3 are counted.'].join('\n'), editorialMarkdown:['## Divide out each discovered prime','', 'Try a divisor d; when it divides the remaining value, count one new prime factor and repeatedly divide d away. After removal, that same prime cannot be counted again. A remaining value above one after the loop is one final prime factor.', '', 'The quiet mistake is incrementing for every division, which counts exponents instead of distinct primes. Strip the whole run before moving on. Time is O(sqrt(n)), bounded by trial division of the shrinking remainder, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(n){let c=0;for(let d=2;d*d<=n;d++)if(n%d===0){c++;while(n%d===0)n/=d;}return n>1?c+1:c;}','function solve(n:number):number{let c=0;for(let d=2;d*d<=n;d++)if(n%d===0){c++;while(n%d===0)n/=d;}return n>1?c+1:c;}','def solve(n):\n    c=0; d=2\n    while d*d<=n:\n        if n%d==0:\n            c+=1\n            while n%d==0:n//=d\n        d+=1\n    return c+1 if n>1 else c','    static int solve(int n) { int c=0;for(int d=2;d*d<=n;d++)if(n%d==0){c++;while(n%d==0)n/=d;}return n>1?c+1:c; }','int solve(int n){int c=0;for(int d=2;d*d<=n;d++)if(n%d==0){c++;while(n%d==0)n/=d;}return n>1?c+1:c;}','func solve(n int) int {c:=0;for d:=2;d*d<=n;d++{if n%d==0{c++;for n%d==0{n/=d}}};if n>1{return c+1};return c}'), tests:[{stdin:'72',expectedStdout:'2',isSample:true},{stdin:'97',expectedStdout:'1',isSample:true},{stdin:'210',expectedStdout:'4'},{stdin:'64',expectedStdout:'1'},{stdin:'999983',expectedStdout:'1'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'prime-factor-sum-with-repeats', title:'Add Prime Factors With Repeats', difficulty:'MEDIUM', patternTags:['number-theory','factorization'], signatureId:'fn:int->int', avgSolveSeconds:660,
    promptMarkdown:['Factor `n` and return the sum of its prime factors, counting repeated factors. `2 <= n <= 1,000,000`.','', '**Example**','', '```','input: 72','output: 12','```','', 'Since `72 = 2 × 2 × 2 × 3 × 3`, the total is 12; a prime returns itself.'].join('\n'), editorialMarkdown:['## Repeated factor extraction','', 'For each possible divisor, repeatedly divide it out and add it each time. These divisions expose the complete prime factorization because any composite remaining factor will eventually expose one of its prime pieces. If a remainder greater than one survives, it is prime and is added once.', '', 'The quiet mistake is adding each divisor only once, which silently changes this into a distinct-factor sum. Keep the division in a while loop. Time is O(sqrt(n)), bounded by factor trials on the shrinking value, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(n){let s=0;for(let d=2;d*d<=n;d++)while(n%d===0){s+=d;n/=d;}return n>1?s+n:s;}','function solve(n:number):number{let s=0;for(let d=2;d*d<=n;d++)while(n%d===0){s+=d;n/=d;}return n>1?s+n:s;}','def solve(n):\n    s=0;d=2\n    while d*d<=n:\n        while n%d==0:s+=d;n//=d\n        d+=1\n    return s+n if n>1 else s','    static int solve(int n) { int s=0;for(int d=2;d*d<=n;d++)while(n%d==0){s+=d;n/=d;}return n>1?s+n:s; }','int solve(int n){int s=0;for(int d=2;d*d<=n;d++)while(n%d==0){s+=d;n/=d;}return n>1?s+n:s;}','func solve(n int) int {s:=0;for d:=2;d*d<=n;d++{for n%d==0{s+=d;n/=d}};if n>1{return s+n};return s}'), tests:[{stdin:'72',expectedStdout:'12',isSample:true},{stdin:'13',expectedStdout:'13',isSample:true},{stdin:'64',expectedStdout:'12'},{stdin:'210',expectedStdout:'17'},{stdin:'49',expectedStdout:'14'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'coprime-count-below-number', title:'Count Numbers Coprime to N', difficulty:'MEDIUM', patternTags:['number-theory','totient'], signatureId:'fn:int->int', avgSolveSeconds:720,
    promptMarkdown:['Return how many integers from 1 through `n` are coprime with `n`. `1 <= n <= 1,000,000`.','', '**Example**','', '```','input: 12','output: 4','```','', 'The coprime values are 1, 5, 7, and 11. The edge case `n = 1` returns 1.'].join('\n'), editorialMarkdown:['## Euler totient by prime factors','', 'Begin with result n. Each distinct prime factor p removes exactly a one-pth fraction of numbers from the coprime count, so update `result -= result / p` once per discovered p, stripping all copies before continuing. A leftover prime receives the same update.', '', 'The quiet mistake is applying the update once for every repeated factor; p squared does not create a second excluded fraction. Remove a factor completely before advancing. Time is O(sqrt(n)), bounded by factor trials, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(n){let r=n,x=n;for(let p=2;p*p<=x;p++)if(x%p===0){while(x%p===0)x/=p;r-=r/p;}if(x>1)r-=r/x;return r;}','function solve(n:number):number{let r=n,x=n;for(let p=2;p*p<=x;p++)if(x%p===0){while(x%p===0)x/=p;r-=r/p;}if(x>1)r-=r/x;return r;}','def solve(n):\n    r=x=n;p=2\n    while p*p<=x:\n        if x%p==0:\n            while x%p==0:x//=p\n            r-=r//p\n        p+=1\n    if x>1:r-=r//x\n    return r','    static int solve(int n) { int r=n,x=n;for(int p=2;p*p<=x;p++)if(x%p==0){while(x%p==0)x/=p;r-=r/p;}if(x>1)r-=r/x;return r; }','int solve(int n){int r=n,x=n;for(int p=2;p*p<=x;p++)if(x%p==0){while(x%p==0)x/=p;r-=r/p;}if(x>1)r-=r/x;return r;}','func solve(n int) int {r,x:=n,n;for p:=2;p*p<=x;p++{if x%p==0{for x%p==0{x/=p};r-=r/p}};if x>1{r-=r/x};return r}'), tests:[{stdin:'12',expectedStdout:'4',isSample:true},{stdin:'1',expectedStdout:'1',isSample:true},{stdin:'13',expectedStdout:'12'},{stdin:'36',expectedStdout:'12'},{stdin:'100',expectedStdout:'40'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'gcd-across-a-list', title:'Shared Divisor of a Whole List', difficulty:'MEDIUM', patternTags:['number-theory','gcd'], signatureId:'fn:ints->int', avgSolveSeconds:660,
    promptMarkdown:['Return the greatest positive divisor shared by every positive number in the non-empty list. Each value is at most 1,000,000.','', '**Example**','', '```','input: 24 36 60','output: 12','```','', 'A one-item list returns that item itself.'].join('\n'), editorialMarkdown:['## GCD reduction chain','', 'The common divisor of the whole prefix can be compressed into one running gcd. Combine it with the next number using Euclid’s remainder loop; associativity means the final accumulator divides every list value and is the greatest such divisor.', '', 'The quiet mistake is taking only the gcd of the first two numbers, which ignores later restrictions. Feed every value into the accumulator. Time is O(n log M), bounded by n values and Euclidean steps for maximum M, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(a){let g=a[0];for(let x of a.slice(1)){while(x){let t=g%x;g=x;x=t;}}return g;}','function solve(a:number[]):number{let g=a[0];for(const v of a.slice(1)){let x=v;while(x){const t=g%x;g=x;x=t;}}return g;}','def solve(a):\n    g=a[0]\n    for x in a[1:]:\n        while x:g,x=x,g%x\n    return g','    static int solve(int[] a) { int g=a[0];for(int i=1;i<a.length;i++){int x=a[i];while(x!=0){int t=g%x;g=x;x=t;}}return g; }','int solve(vector<int>a){int g=a[0];for(int i=1;i<(int)a.size();i++){int x=a[i];while(x){int t=g%x;g=x;x=t;}}return g;}','func solve(a []int) int {g:=a[0];for _,v:=range a[1:]{x:=v;for x!=0{g,x=x,g%x}};return g}'), tests:[{stdin:'24 36 60',expectedStdout:'12',isSample:true},{stdin:'17',expectedStdout:'17',isSample:true},{stdin:'8 12 20',expectedStdout:'4'},{stdin:'7 11 13',expectedStdout:'1'},{stdin:'100 50 75',expectedStdout:'25'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'lcm-across-a-list', title:'Smallest Shared Multiple of a List', difficulty:'MEDIUM', patternTags:['number-theory','lcm'], signatureId:'fn:ints->int', avgSolveSeconds:720,
    promptMarkdown:['Return the least positive integer divisible by every positive value in the non-empty list. The answer is guaranteed to fit a signed 32-bit integer.','', '**Example**','', '```','input: 4 6 10','output: 60','```','', 'A one-item list returns that item. Repeated values do not change the answer.'].join('\n'), editorialMarkdown:['## LCM reduction through GCD','', 'Extend a running lcm one number at a time. For current result r and value x, divide r by gcd(r, x) before multiplying by x; this retains each required prime power once instead of multiplying shared factors twice. The stated bound keeps the result safe.', '', 'The quiet mistake is multiplying r by x directly, which overcounts common factors and rapidly overflows. Divide before multiplication. Time is O(n log M), bounded by Euclidean gcd work per item, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(a){let r=a[0];for(let x of a.slice(1)){let u=r,v=x;while(v){let t=u%v;u=v;v=t;}r=r/u*x;}return r;}','function solve(a:number[]):number{let r=a[0];for(const x of a.slice(1)){let u=r,v=x;while(v){const t=u%v;u=v;v=t;}r=r/u*x;}return r;}','def solve(a):\n    r=a[0]\n    for x in a[1:]:\n        u,v=r,x\n        while v:u,v=v,u%v\n        r=r//u*x\n    return r','    static int solve(int[] a) { int r=a[0];for(int i=1;i<a.length;i++){int u=r,v=a[i];while(v!=0){int t=u%v;u=v;v=t;}r=r/u*a[i];}return r; }','int solve(vector<int>a){int r=a[0];for(int i=1;i<(int)a.size();i++){int u=r,v=a[i];while(v){int t=u%v;u=v;v=t;}r=r/u*a[i];}return r;}','func solve(a []int) int {r:=a[0];for _,x:=range a[1:]{u,v:=r,x;for v!=0{u,v=v,u%v};r=r/u*x};return r}'), tests:[{stdin:'4 6 10',expectedStdout:'60',isSample:true},{stdin:'7',expectedStdout:'7',isSample:true},{stdin:'2 3 5',expectedStdout:'30'},{stdin:'6 6 6',expectedStdout:'6'},{stdin:'8 12 20',expectedStdout:'120'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'trailing-zeros-of-factorial', title:'Zeros at the End of a Factorial', difficulty:'HARD', patternTags:['number-theory','factorisation','counting'], signatureId:'fn:int->int', avgSolveSeconds:900,
    promptMarkdown:['Return how many zeros the decimal form of `n!` ends with, without computing `n!`. `n` is between 0 and 1,000,000,000.','', '**Example**','', '```','input: 25','output: 6','```','', '`25!` ends in six zeros. The edge cases are `0` and `1`, whose factorial is `1` and ends in no zeros.'].join('\n'), editorialMarkdown:['## Count the scarcer prime factor','', 'A trailing zero is one factor of ten, and ten is two times five. Among `1..n` the factor two appears far more often than five, so the supply of fives is what limits the count. Add `n/5` for the multiples of five, then `n/25` because each of those carries a second five, then `n/125`, and so on while the divisor is at most `n`.', '', 'The quiet mistake is stopping after `n/5`. That is correct all the way to 24 and then silently undercounts forever, because 25 contributes two fives rather than one. Computing `n!` directly is the other trap: it overflows every integer type available here long before the stated bound. Time is O(log n) in base five, bounded by how many powers of five fit under n; space is O(1).'].join('\n'), referenceSolution:six('function solve(n){let c=0;for(let d=5;d<=n;d*=5)c+=Math.floor(n/d);return c;}','function solve(n:number):number{let c=0;for(let d=5;d<=n;d*=5)c+=Math.floor(n/d);return c;}','def solve(n):\n    c=0;d=5\n    while d<=n:\n        c+=n//d;d*=5\n    return c','    static int solve(int n) { int c=0;for(long d=5;d<=n;d*=5)c+=(int)(n/d);return c; }','int solve(int n){int c=0;for(long long d=5;d<=n;d*=5)c+=(int)(n/d);return c;}','func solve(n int) int {c:=0;for d:=5;d<=n;d*=5{c+=n/d};return c}'), tests:[{stdin:'25',expectedStdout:'6',isSample:true},{stdin:'5',expectedStdout:'1',isSample:true},{stdin:'0',expectedStdout:'0'},{stdin:'4',expectedStdout:'0'},{stdin:'100',expectedStdout:'24'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'decimal-to-binary-text', title:'Write a Decimal Number in Binary', difficulty:'HARD', patternTags:['number-theory','base-conversion'], signatureId:'fn:int->string', avgSolveSeconds:780,
    promptMarkdown:['Convert non-negative decimal integer `n` to its binary digits without leading zeroes. `n` is at most 1,000,000,000.','', '**Example**','', '```','input: 45','output: 101101','```','', 'The named edge case is zero, whose binary representation is exactly `0`.'].join('\n'), editorialMarkdown:['## Repeated division base conversion','', 'Division by two exposes the next least-significant binary digit as its remainder. Append those remainders while reducing n, then reverse the collected characters so the most-significant digit appears first. This is the general positional-base conversion process.', '', 'The quiet mistake is returning an empty string for zero because the division loop never begins. Give zero its one-character representation first. Time is O(log n), bounded by binary digit count, and extra space is O(log n) for the output.'].join('\n'), referenceSolution:six('function solve(n){if(n===0)return "0";let s="";while(n){s+=(n%2);n=Math.floor(n/2);}return s.split("").reverse().join("");}','function solve(n:number):string{if(n===0)return "0";let s="";while(n){s+=n%2;n=Math.floor(n/2);}return s.split("").reverse().join("");}','def solve(n):\n    if n==0:return "0"\n    s=""\n    while n:s+=str(n%2);n//=2\n    return s[::-1]','    static String solve(int n) { if(n==0)return "0";String s="";while(n>0){s+=(n%2);n/=2;}return new StringBuilder(s).reverse().toString(); }','string solve(int n){if(n==0)return "0";string s="";while(n){s+=char(\'0\'+n%2);n/=2;}reverse(s.begin(),s.end());return s;}','func solve(n int) string {if n==0{return "0"};s:="";for n>0{s+=strconv.Itoa(n%2);n/=2};b:=[]byte(s);for i,j:=0,len(b)-1;i<j;i,j=i+1,j-1{b[i],b[j]=b[j],b[i]};return string(b)}'), tests:[{stdin:'45',expectedStdout:'101101',isSample:true},{stdin:'0',expectedStdout:'0',isSample:true},{stdin:'1',expectedStdout:'1'},{stdin:'8',expectedStdout:'1000'},{stdin:'255',expectedStdout:'11111111'}] }),
  p({ ...base, patternFamily:'MATH_GEOMETRY', slug:'binary-digits-to-decimal', title:'Read Binary Digits as Decimal', difficulty:'HARD', patternTags:['number-theory','base-conversion'], signatureId:'fn:int->int', avgSolveSeconds:780,
    promptMarkdown:['The decimal digits of input `n` are a binary numeral containing only 0 and 1. Return its decimal value. The result fits a signed 32-bit integer.','', '**Example**','', '```','input: 101101','output: 45','```','', 'The edge case input 0 represents zero. Leading zero digits, if present, do not change the value.'].join('\n'), editorialMarkdown:['## Horner evaluation in base two','', 'Read the binary numeral from right to left, maintaining a place value that starts at one and doubles after each digit. Add the place value only for a digit one. Equivalently, the recurrence builds the same base-two positional value without converting through text.', '', 'The quiet mistake is treating the input as ordinary decimal and adding its digits. A one in the tens position means two, not ten. Time is O(log10 n), bounded by the number of supplied digits, and extra space is O(1).'].join('\n'), referenceSolution:six('function solve(n){let r=0,p=1;while(n>0){if(n%10)r+=p;p*=2;n=Math.floor(n/10);}return r;}','function solve(n:number):number{let r=0,p=1;while(n>0){if(n%10)r+=p;p*=2;n=Math.floor(n/10);}return r;}','def solve(n):\n    r=0;p=1\n    while n:\n        if n%10:r+=p\n        p*=2;n//=10\n    return r','    static int solve(int n) { int r=0,p=1;while(n>0){if(n%10!=0)r+=p;p*=2;n/=10;}return r; }','int solve(int n){int r=0,p=1;while(n){if(n%10)r+=p;p*=2;n/=10;}return r;}','func solve(n int) int {r,p:=0,1;for n>0{if n%10!=0{r+=p};p*=2;n/=10};return r}'), tests:[{stdin:'101101',expectedStdout:'45',isSample:true},{stdin:'0',expectedStdout:'0',isSample:true},{stdin:'1',expectedStdout:'1'},{stdin:'1000',expectedStdout:'8'},{stdin:'11111111',expectedStdout:'255'}] }),
];
