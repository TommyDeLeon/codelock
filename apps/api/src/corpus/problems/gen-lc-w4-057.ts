import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w4-057` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W4_057_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "maximum-probe-energy-level",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Maximum Account Balance",
    patternTags: ["array","prefix-sum","running-maximum"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "You are tracking the balance of a bank account over several days. The account starts with a balance of `0`.\n\nYou are given an integer array `transactions`, where `transactions[i]` represents the deposit or withdrawal amount on the `i`-th day.\n\nReturn the maximum balance the account achieves at any point during these days (including the initial balance of `0`).\n\n**Constraints**\n- `0 <= transactions.length <= 100`\n- `-100 <= transactions[i] <= 100`\n\n**Example 1**\n```\ninput:\n-5 1 5 0 -7\noutput: 1\n```\n*Explanation: The account balances are `0` (start) -> `-5` -> `-4` -> `1` -> `1` -> `-6`. The highest balance reached is `1`.*\n\n**Example 2**\n```\ninput:\n4 -3 2 -1 -2\noutput: 4\n```\n*Explanation: The account balances are `0` (start) -> `4` -> `1` -> `3` -> `2` -> `0`. The highest balance reached is `4`.*\n\n**Example 3**\n```\ninput:\n\noutput: 0\n```\n*Explanation: No transactions occur, so the maximum balance remains `0`.*\n\n**Follow-up**\nCan you compute the maximum balance without storing the daily balances in a new array?",
    editorialMarkdown: "## Maximum Probe Energy Level\n\nThe problem asks for the peak value of a running total that starts at 0. This is a classic application of the **Prefix Sum** pattern, where we accumulate the values sequentially.\n\nWe can initialize a `current` variable to 0 and a `max` variable to 0. As we iterate through the `changes` array, we add each value to `current` and update `max` if `current` exceeds it.\n\n**Trap**: Initializing `max` to negative infinity or the first element's value. The problem states the probe starts at energy level 0, so the maximum energy reached can never be less than 0, even if all changes are negative.\n\n**Complexity:**\n- **Time:** O(N) — We make a single pass through the array.\n- **Space:** O(1) — We only store a few variables for the running sums.",
    referenceSolution: {
      JAVASCRIPT: "function solve(changes) {\n    let max = 0, current = 0;\n    for (let i = 0; i < changes.length; i++) {\n        current += changes[i];\n        if (current > max) max = current;\n    }\n    return max;\n}",
      TYPESCRIPT: "function solve(changes: number[]): number {\n    let max = 0, current = 0;\n    for (let i = 0; i < changes.length; i++) {\n        current += changes[i];\n        if (current > max) max = current;\n    }\n    return max;\n}",
      PYTHON: "def solve(changes):\n    max_val = 0\n    current = 0\n    for c in changes:\n        current += c\n        if current > max_val:\n            max_val = current\n    return max_val",
      JAVA: "    static int solve(int[] changes) {\n        int max = 0, current = 0;\n        for (int c : changes) {\n            current += c;\n            if (current > max) max = current;\n        }\n        return max;\n    }",
      CPP: "int solve(vector<int> changes) {\n    int max_val = 0, current = 0;\n    for (int c : changes) {\n        current += c;\n        if (current > max_val) max_val = current;\n    }\n    return max_val;\n}",
      GO: "func solve(changes []int) int {\n    maxVal := 0\n    current := 0\n    for _, c := range changes {\n        current += c\n        if current > maxVal {\n            maxVal = current\n        }\n    }\n    return maxVal\n}",
    },
    tests: [
      { stdin: "-5 1 5 0 -7", expectedStdout: "1", isSample: true },
      { stdin: "4 -3 2 -1 -2", expectedStdout: "4", isSample: true },
      { stdin: "", expectedStdout: "0" },
      { stdin: "10", expectedStdout: "10" },
      { stdin: "-5", expectedStdout: "0" },
      { stdin: "0 0 0", expectedStdout: "0" },
      { stdin: "2 2 2", expectedStdout: "6" },
      { stdin: "-1 -2 -3", expectedStdout: "0" },
    ],
  }),

  p({
    ...base,
    slug: "critical-subsystem-code",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "BIT_MANIPULATION",
    title: "Essential Product Features",
    patternTags: ["array","bit-manipulation","counting"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 400,
    promptMarkdown: "A software company is deciding which features to include in their new application. They collect survey responses from users, where each response is an integer whose binary representation indicates the requested features (the `i`-th bit is `1` if the `i`-th feature is requested).\n\nA feature is deemed \"essential\" if it is requested by at least `k` users. \n\nThe **essential feature profile** is a single integer formed by taking the bitwise OR of all essential features. Given an integer array `responses` and an integer `k`, return the essential feature profile.\n\n**Constraints**\n- `1 <= responses.length <= 50`\n- `0 <= responses[i] < 2^31`\n- `1 <= k <= 50`\n\n**Example 1**\n```\ninput:\n7 12 9 8 9 15\n4\noutput: 9\n```\n*Explanation: \nBit 0 is 1 in 7, 9, 9, 15 (4 responses >= k=4) -> essential\nBit 1 is 1 in 7, 15 (2 responses < 4) -> not essential\nBit 2 is 1 in 7, 12, 15 (3 responses < 4) -> not essential\nBit 3 is 1 in 12, 9, 8, 9, 15 (5 responses >= 4) -> essential\nThe result has bits 0 and 3 set to 1: 1 + 8 = 9.*\n\n**Example 2**\n```\ninput:\n2 10 14\n1\noutput: 14\n```\n*Explanation: Since k=1, any feature requested by at least one user is essential. The bitwise OR of 2, 10, and 14 is 14.*\n\n**Example 3**\n```\ninput:\n15 15\n3\noutput: 0\n```\n*Explanation: No feature is requested by at least 3 users, so the result is 0.*\n\n**Follow-up**\nCould you optimize this if `k` is very small and the array is extremely large?",
    editorialMarkdown: "## Critical Subsystem Code\n\nThe problem requires us to calculate a resulting integer by setting specific bits. A bit is set in the result if and only if it is set in at least `k` of the input integers.\n\nSince the inputs fit within 32-bit integers, we can iterate over every bit position from `0` to `30`. For each bit position, we traverse the `codes` array and count how many integers have that specific bit set (using `codes[i] & (1 << bit)`). If the count is greater than or equal to `k`, we set that bit in our answer (using `ans |= (1 << bit)`).\n\n**Trap**: Misinterpreting the problem to mean finding the overall bitwise OR of all codes or combinations. You must evaluate each bit position independently across all elements.\n\n**Complexity:**\n- **Time:** O(N) — We iterate through the array of length N exactly 31 times, yielding O(31 * N) which simplifies to O(N).\n- **Space:** O(1) — We use only a few integer variables.",
    referenceSolution: {
      JAVASCRIPT: "function solve(codes, k) {\n    let ans = 0;\n    for (let bit = 0; bit < 31; bit++) {\n        let count = 0;\n        for (let i = 0; i < codes.length; i++) {\n            if ((codes[i] & (1 << bit)) !== 0) count++;\n        }\n        if (count >= k) ans |= (1 << bit);\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(codes: number[], k: number): number {\n    let ans = 0;\n    for (let bit = 0; bit < 31; bit++) {\n        let count = 0;\n        for (let i = 0; i < codes.length; i++) {\n            if ((codes[i] & (1 << bit)) !== 0) count++;\n        }\n        if (count >= k) ans |= (1 << bit);\n    }\n    return ans;\n}",
      PYTHON: "def solve(codes, k):\n    ans = 0\n    for bit in range(31):\n        count = 0\n        for c in codes:\n            if (c & (1 << bit)) != 0:\n                count += 1\n        if count >= k:\n            ans |= (1 << bit)\n    return ans",
      JAVA: "    static int solve(int[] codes, int k) {\n        int ans = 0;\n        for (int bit = 0; bit < 31; bit++) {\n            int count = 0;\n            for (int c : codes) {\n                if ((c & (1 << bit)) != 0) count++;\n            }\n            if (count >= k) ans |= (1 << bit);\n        }\n        return ans;\n    }",
      CPP: "int solve(vector<int> codes, int k) {\n    int ans = 0;\n    for (int bit = 0; bit < 31; bit++) {\n        int count = 0;\n        for (int c : codes) {\n            if ((c & (1 << bit)) != 0) count++;\n        }\n        if (count >= k) ans |= (1 << bit);\n    }\n    return ans;\n}",
      GO: "func solve(codes []int, k int) int {\n    ans := 0\n    for bit := 0; bit < 31; bit++ {\n        count := 0\n        for _, c := range codes {\n            if (c & (1 << bit)) != 0 {\n                count++\n            }\n        }\n        if count >= k {\n            ans |= (1 << bit)\n        }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "7 12 9 8 9 15\n4", expectedStdout: "9", isSample: true },
      { stdin: "2 10 14\n1", expectedStdout: "14", isSample: true },
      { stdin: "15 15\n3", expectedStdout: "0" },
      { stdin: "1\n1", expectedStdout: "1" },
      { stdin: "0 0 0\n1", expectedStdout: "0" },
      { stdin: "2 10 14\n2", expectedStdout: "10" },
      { stdin: "1 2 4 8\n1", expectedStdout: "15" },
      { stdin: "\n1", expectedStdout: "0" },
    ],
  }),

  p({
    ...base,
    slug: "rarest-high-value-cargo",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Most Valuable Unique Stamp",
    patternTags: ["array","hash-map","counting","maximum"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A collector has a collection of vintage stamps, represented by an integer array `stamps`, where `stamps[i]` is both the identifier and the monetary value of a stamp. \n\nA stamp is considered **unique** in the collection if there is exactly one copy of it. Return the value of the unique stamp that has the highest monetary value. If the collection contains no unique stamps, return `-1`.\n\n**Constraints**\n- `0 <= stamps.length <= 100`\n- `0 <= stamps[i] <= 1000`\n\n**Example 1**\n```\ninput:\n5 7 3 9 4 9 8 3 1\noutput: 8\n```\n*Explanation: The frequencies are: 9(2), 3(2), 5(1), 7(1), 4(1), 8(1), 1(1). The unique stamps are 1, 4, 5, 7, and 8. The largest among them is 8.*\n\n**Example 2**\n```\ninput:\n9 9 9\noutput: -1\n```\n*Explanation: Stamp 9 appears 3 times. There are no stamps that appear exactly once.*\n\n**Example 3**\n```\ninput:\n2\noutput: 2\n```\n*Explanation: Stamp 2 is the only item in the collection, appearing exactly once.*\n\n**Follow-up**\nCan you solve this in O(N) time without sorting the array?",
    editorialMarkdown: "## Rarest High-Value Cargo\n\nThe problem asks us to find the largest ID among items that appear exactly once in an array. This is a standard frequency counting problem.\n\nFirst, we iterate through the `cargo` array and use a hash map to count the occurrences of each ID. Then, we iterate through the keys (or entries) of the hash map, checking if the frequency is exactly `1`. Among all such keys, we track and return the maximum value. If no ID has a frequency of `1`, we return `-1`.\n\n**Trap**: Sorting the array and trying to check neighbors can be tricky with edge cases (like the first or last elements) and takes O(N log N) time. A hash map is cleaner and strictly O(N).\n\n**Complexity:**\n- **Time:** O(N) — We do one pass to populate the hash map, and another pass to find the maximum.\n- **Space:** O(N) — We store at most N unique IDs in the hash map.",
    referenceSolution: {
      JAVASCRIPT: "function solve(cargo) {\n    let map = new Map();\n    for (let c of cargo) map.set(c, (map.get(c) || 0) + 1);\n    let max = -1;\n    for (let [k, v] of map.entries()) {\n        if (v === 1 && k > max) max = k;\n    }\n    return max;\n}",
      TYPESCRIPT: "function solve(cargo: number[]): number {\n    let map = new Map<number, number>();\n    for (let c of cargo) map.set(c, (map.get(c) || 0) + 1);\n    let max = -1;\n    for (let [k, v] of map.entries()) {\n        if (v === 1 && k > max) max = k;\n    }\n    return max;\n}",
      PYTHON: "def solve(cargo):\n    freq = {}\n    for c in cargo:\n        freq[c] = freq.get(c, 0) + 1\n    max_val = -1\n    for k, v in freq.items():\n        if v == 1 and k > max_val:\n            max_val = k\n    return max_val",
      JAVA: "    static int solve(int[] cargo) {\n        java.util.HashMap<Integer, Integer> map = new java.util.HashMap<>();\n        for (int c : cargo) map.put(c, map.getOrDefault(c, 0) + 1);\n        int max = -1;\n        for (java.util.Map.Entry<Integer, Integer> entry : map.entrySet()) {\n            if (entry.getValue() == 1 && entry.getKey() > max) {\n                max = entry.getKey();\n            }\n        }\n        return max;\n    }",
      CPP: "int solve(vector<int> cargo) {\n    unordered_map<int, int> map;\n    for (int c : cargo) map[c]++;\n    int max_val = -1;\n    for (auto const& [k, v] : map) {\n        if (v == 1 && k > max_val) max_val = k;\n    }\n    return max_val;\n}",
      GO: "func solve(cargo []int) int {\n    m := make(map[int]int)\n    for _, c := range cargo {\n        m[c]++\n    }\n    maxVal := -1\n    for k, v := range m {\n        if v == 1 && k > maxVal {\n            maxVal = k\n        }\n    }\n    return maxVal\n}",
    },
    tests: [
      { stdin: "5 7 3 9 4 9 8 3 1", expectedStdout: "8", isSample: true },
      { stdin: "9 9 9", expectedStdout: "-1", isSample: true },
      { stdin: "2", expectedStdout: "2" },
      { stdin: "", expectedStdout: "-1" },
      { stdin: "10 10 20 20 30 30", expectedStdout: "-1" },
      { stdin: "1 2 3 4 5", expectedStdout: "5" },
      { stdin: "5 4 3 2 1", expectedStdout: "5" },
      { stdin: "10 2 2 10 5 5 8", expectedStdout: "8" },
    ],
  }),

  p({
    ...base,
    slug: "balance-station-compartments",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Equilibrium Trading Day",
    patternTags: ["array","prefix-sum","math"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 350,
    promptMarkdown: "An investor tracks their daily profit and loss over a period of time. You are given an integer array `pnls`, where `pnls[i]` represents the net profit or loss on the `i`-th day.\n\nYou want to identify an **equilibrium day**. This is a specific day index `i` such that the sum of all profits and losses strictly before day `i` is exactly equal to the sum of all profits and losses strictly after day `i`. \n\nIf there are no days before the chosen day (e.g. index `0`), the sum before is considered `0`. Similarly, if there are no days after the chosen day, the sum after is considered `0`.\n\nReturn the **earliest** equilibrium day index, or `-1` if no such day exists.\n\n**Constraints**\n- `0 <= pnls.length <= 100`\n- `-1000 <= pnls[i] <= 1000`\n\n**Example 1**\n```\ninput:\n2 3 -1 8 4\noutput: 3\n```\n*Explanation: The sum of the values before day 3 is 2 + 3 + (-1) = 4. The sum after day 3 is just 4. They are equal.*\n\n**Example 2**\n```\ninput:\n1 -1 4\noutput: 2\n```\n*Explanation: The sum before day 2 is 1 + (-1) = 0. The sum after day 2 is 0. They are equal.*\n\n**Example 3**\n```\ninput:\n2 5\noutput: -1\n```\n*Explanation: No equilibrium day exists.*\n\n**Follow-up**\nCan you find the index in exactly two passes over the array?",
    editorialMarkdown: "## Balance Station Compartments\n\nThis problem tasks us with finding a partition index such that the sum of elements before it equals the sum of elements after it.\n\nWe first compute the `total` sum of the entire array. Then we keep a running `left` sum as we iterate through the array. For any given index `i`, the sum of the elements to its right is simply `total - left - weights[i]`. If this right sum equals our running `left` sum, we have found our answer! We return `i`. If we finish the loop without finding a match, we return `-1`.\n\n**Trap**: An O(N²) solution that re-sums the left and right partitions for every index will time out or perform poorly. Keeping a running sum avoids this redundant work.\n\n**Complexity:**\n- **Time:** O(N) — One pass to find the total sum, and another pass to find the balancing index.\n- **Space:** O(1) — We only store a few sum variables.",
    referenceSolution: {
      JAVASCRIPT: "function solve(weights) {\n    let total = 0;\n    for (let i = 0; i < weights.length; i++) total += weights[i];\n    let left = 0;\n    for (let i = 0; i < weights.length; i++) {\n        if (left === total - left - weights[i]) return i;\n        left += weights[i];\n    }\n    return -1;\n}",
      TYPESCRIPT: "function solve(weights: number[]): number {\n    let total = 0;\n    for (let i = 0; i < weights.length; i++) total += weights[i];\n    let left = 0;\n    for (let i = 0; i < weights.length; i++) {\n        if (left === total - left - weights[i]) return i;\n        left += weights[i];\n    }\n    return -1;\n}",
      PYTHON: "def solve(weights):\n    total = sum(weights)\n    left = 0\n    for i, w in enumerate(weights):\n        if left == total - left - w:\n            return i\n        left += w\n    return -1",
      JAVA: "    static int solve(int[] weights) {\n        int total = 0;\n        for (int w : weights) total += w;\n        int left = 0;\n        for (int i = 0; i < weights.length; i++) {\n            if (left == total - left - weights[i]) return i;\n            left += weights[i];\n        }\n        return -1;\n    }",
      CPP: "int solve(vector<int> weights) {\n    long long total = 0;\n    for (int w : weights) total += w;\n    long long left = 0;\n    for (int i = 0; i < (int)weights.size(); i++) {\n        if (left == total - left - weights[i]) return i;\n        left += weights[i];\n    }\n    return -1;\n}",
      GO: "func solve(weights []int) int {\n    total := 0\n    for _, w := range weights {\n        total += w\n    }\n    left := 0\n    for i, w := range weights {\n        if left == total - left - w {\n            return i\n        }\n        left += w\n    }\n    return -1\n}",
    },
    tests: [
      { stdin: "2 3 -1 8 4", expectedStdout: "3", isSample: true },
      { stdin: "1 -1 4", expectedStdout: "2", isSample: true },
      { stdin: "2 5", expectedStdout: "-1" },
      { stdin: "0", expectedStdout: "0" },
      { stdin: "", expectedStdout: "-1" },
      { stdin: "0 0 0", expectedStdout: "0" },
      { stdin: "-5 5 0", expectedStdout: "2" },
      { stdin: "1 1 1 1", expectedStdout: "-1" },
    ],
  }),

  p({
    ...base,
    slug: "out-of-order-androids",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Misplaced Books",
    patternTags: ["array","sorting","counting"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A librarian is organizing a shelf of historical books. They are supposed to be arranged in non-decreasing order of their `publicationYears`.\n\nGiven an integer array `publicationYears` representing the current arrangement of the books on the shelf, return the number of books that are *not* in the position they would occupy if the shelf were correctly sorted.\n\n**Constraints**\n- `0 <= publicationYears.length <= 100`\n- `1 <= publicationYears[i] <= 1000`\n\n**Example 1**\n```\ninput:\n1 1 4 2 1 3\noutput: 3\n```\n*Explanation: \nCurrent: 1 1 4 2 1 3\nSorted:  1 1 1 2 3 4\nIndices 2, 4, and 5 are mismatched.*\n\n**Example 2**\n```\ninput:\n5 1 2 3 4\noutput: 5\n```\n*Explanation: Every book is out of order compared to the sorted version (1 2 3 4 5).*\n\n**Example 3**\n```\ninput:\n1 2 3 4 5\noutput: 0\n```\n*Explanation: The books are already perfectly sorted.*\n\n**Follow-up**\nCan you solve this without using your language's built-in sorting functions?",
    editorialMarkdown: "## Out of Order Androids\n\nWe are tasked with determining how many elements in an array are not in their correct sorted positions.\n\nThe most straightforward way is to create a copy of the array and sort it. Then, we iterate through both the original array and the sorted array side-by-side, counting how many indices have mismatched values.\n\n**Trap**: Mutating the original array during sorting! If you sort the array in place, you lose the original order, making it impossible to compare. Always copy the array first.\n\n**Complexity:**\n- **Time:** O(N log N) — The time complexity is dominated by the sorting step.\n- **Space:** O(N) — We need a secondary array to hold the sorted elements.",
    referenceSolution: {
      JAVASCRIPT: "function solve(serialNumbers) {\n    let sorted = [];\n    for (let i = 0; i < serialNumbers.length; i++) sorted.push(serialNumbers[i]);\n    for (let i = 0; i < sorted.length; i++) {\n        for (let j = i + 1; j < sorted.length; j++) {\n            if (sorted[i] > sorted[j]) {\n                let temp = sorted[i];\n                sorted[i] = sorted[j];\n                sorted[j] = temp;\n            }\n        }\n    }\n    let count = 0;\n    for (let i = 0; i < serialNumbers.length; i++) {\n        if (serialNumbers[i] !== sorted[i]) count++;\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(serialNumbers: number[]): number {\n    let sorted: number[] = [];\n    for (let i = 0; i < serialNumbers.length; i++) sorted.push(serialNumbers[i]);\n    for (let i = 0; i < sorted.length; i++) {\n        for (let j = i + 1; j < sorted.length; j++) {\n            if (sorted[i] > sorted[j]) {\n                let temp = sorted[i];\n                sorted[i] = sorted[j];\n                sorted[j] = temp;\n            }\n        }\n    }\n    let count = 0;\n    for (let i = 0; i < serialNumbers.length; i++) {\n        if (serialNumbers[i] !== sorted[i]) count++;\n    }\n    return count;\n}",
      PYTHON: "def solve(serialNumbers):\n    sorted_nums = sorted(serialNumbers)\n    count = 0\n    for a, b in zip(serialNumbers, sorted_nums):\n        if a != b:\n            count += 1\n    return count",
      JAVA: "    static int solve(int[] serialNumbers) {\n        int[] sorted = new int[serialNumbers.length];\n        for (int i = 0; i < serialNumbers.length; i++) sorted[i] = serialNumbers[i];\n        for (int i = 0; i < sorted.length; i++) {\n            for (int j = i + 1; j < sorted.length; j++) {\n                if (sorted[i] > sorted[j]) {\n                    int temp = sorted[i];\n                    sorted[i] = sorted[j];\n                    sorted[j] = temp;\n                }\n            }\n        }\n        int count = 0;\n        for (int i = 0; i < serialNumbers.length; i++) {\n            if (serialNumbers[i] != sorted[i]) count++;\n        }\n        return count;\n    }",
      CPP: "int solve(vector<int> serialNumbers) {\n    vector<int> sorted_nums = serialNumbers;\n    for (size_t i = 0; i < sorted_nums.size(); i++) {\n        for (size_t j = i + 1; j < sorted_nums.size(); j++) {\n            if (sorted_nums[i] > sorted_nums[j]) {\n                int temp = sorted_nums[i];\n                sorted_nums[i] = sorted_nums[j];\n                sorted_nums[j] = temp;\n            }\n        }\n    }\n    int count = 0;\n    for (size_t i = 0; i < serialNumbers.size(); i++) {\n        if (serialNumbers[i] != sorted_nums[i]) count++;\n    }\n    return count;\n}",
      GO: "func solve(serialNumbers []int) int {\n    n := len(serialNumbers)\n    sortedNums := make([]int, n)\n    for i := 0; i < n; i++ {\n        sortedNums[i] = serialNumbers[i]\n    }\n    for i := 0; i < n; i++ {\n        for j := i + 1; j < n; j++ {\n            if sortedNums[i] > sortedNums[j] {\n                temp := sortedNums[i]\n                sortedNums[i] = sortedNums[j]\n                sortedNums[j] = temp\n            }\n        }\n    }\n    count := 0\n    for i := 0; i < n; i++ {\n        if serialNumbers[i] != sortedNums[i] {\n            count++\n        }\n    }\n    return count\n}",
    },
    tests: [
      { stdin: "1 1 4 2 1 3", expectedStdout: "3", isSample: true },
      { stdin: "5 1 2 3 4", expectedStdout: "5", isSample: true },
      { stdin: "1 2 3 4 5", expectedStdout: "0" },
      { stdin: "1", expectedStdout: "0" },
      { stdin: "", expectedStdout: "0" },
      { stdin: "2 2 2 2", expectedStdout: "0" },
      { stdin: "3 2 1", expectedStdout: "2" },
      { stdin: "10 5 20 15", expectedStdout: "4" },
    ],
  }),

  p({
    ...base,
    slug: "maximum-energy-difference",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Maximum Energy Difference",
    patternTags: ["array","maximum","math"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 200,
    promptMarkdown: "The daily energy output from an experimental reactor is logged in an integer array `readings`. \n\nTo assess the stability of the reactor core, scientists need to know the span of its output. Given the `readings`, calculate and return the difference between the absolute highest energy output and the absolute lowest energy output recorded.\n\nIf the `readings` array is empty, return `0`.\n\n**Constraints**\n- `0 <= readings.length <= 1000`\n- `-10000 <= readings[i] <= 10000`\n\n**Example 1**\n```\ninput:\n10 20 30 5 15\noutput: 25\n```\n*Explanation: The highest reading is 30, and the lowest is 5. Their difference is 25.*\n\n**Example 2**\n```\ninput:\n100 100 100\noutput: 0\n```\n*Explanation: The highest and lowest readings are both 100. The difference is 0.*\n\n**Example 3**\n```\ninput:\n-10 5 0 -20 10\noutput: 30\n```\n*Explanation: The highest reading is 10 and the lowest is -20. The difference is 10 - (-20) = 30.*\n\n**Follow-up**\nCan you find the result in a single pass of the array?",
    editorialMarkdown: "## Maximum Energy Difference\n\nFinding the difference between the absolute highest and absolute lowest element in an array is one of the simplest array operations, yet fundamental.\n\nWe initialize a `max` and `min` variable, typically with the first element of the array. Then we iterate through the remaining elements, updating `max` if we find a larger value and `min` if we find a smaller one. Finally, we return their difference. \n\n**Trap**: Initializing `max` to 0 and `min` to 0 will fail if all numbers are strictly negative or strictly positive. Always initialize them to the first element in the array!\n\n**Complexity:**\n- **Time:** O(N) — We inspect each reading once.\n- **Space:** O(1) — We track only two integer values.",
    referenceSolution: {
      JAVASCRIPT: "function solve(readings) {\n    if (readings.length === 0) return 0;\n    let max = readings[0], min = readings[0];\n    for (let i = 1; i < readings.length; i++) {\n        if (readings[i] > max) max = readings[i];\n        if (readings[i] < min) min = readings[i];\n    }\n    return max - min;\n}",
      TYPESCRIPT: "function solve(readings: number[]): number {\n    if (readings.length === 0) return 0;\n    let max = readings[0], min = readings[0];\n    for (let i = 1; i < readings.length; i++) {\n        if (readings[i] > max) max = readings[i];\n        if (readings[i] < min) min = readings[i];\n    }\n    return max - min;\n}",
      PYTHON: "def solve(readings):\n    if not readings: return 0\n    max_val = readings[0]\n    min_val = readings[0]\n    for r in readings[1:]:\n        if r > max_val: max_val = r\n        if r < min_val: min_val = r\n    return max_val - min_val",
      JAVA: "    static int solve(int[] readings) {\n        if (readings.length == 0) return 0;\n        int max = readings[0], min = readings[0];\n        for (int i = 1; i < readings.length; i++) {\n            if (readings[i] > max) max = readings[i];\n            if (readings[i] < min) min = readings[i];\n        }\n        return max - min;\n    }",
      CPP: "int solve(vector<int> readings) {\n    if (readings.empty()) return 0;\n    int max_val = readings[0], min_val = readings[0];\n    for (size_t i = 1; i < readings.size(); i++) {\n        if (readings[i] > max_val) max_val = readings[i];\n        if (readings[i] < min_val) min_val = readings[i];\n    }\n    return max_val - min_val;\n}",
      GO: "func solve(readings []int) int {\n    if len(readings) == 0 {\n        return 0\n    }\n    maxVal := readings[0]\n    minVal := readings[0]\n    for i := 1; i < len(readings); i++ {\n        if readings[i] > maxVal {\n            maxVal = readings[i]\n        }\n        if readings[i] < minVal {\n            minVal = readings[i]\n        }\n    }\n    return maxVal - minVal\n}",
    },
    tests: [
      { stdin: "10 20 30 5 15", expectedStdout: "25", isSample: true },
      { stdin: "100 100 100", expectedStdout: "0", isSample: true },
      { stdin: "-10 5 0 -20 10", expectedStdout: "30" },
      { stdin: "5", expectedStdout: "0" },
      { stdin: "0 -5", expectedStdout: "5" },
      { stdin: "1 2 3 4 5", expectedStdout: "4" },
      { stdin: "10 9 8 7", expectedStdout: "3" },
      { stdin: "-1 -2 -3 -4", expectedStdout: "3" },
    ],
  }),
];
