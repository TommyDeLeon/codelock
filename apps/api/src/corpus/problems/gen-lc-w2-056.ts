import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w2-056` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W2_056_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "faulty-sensor-transmission",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Train Carriage Manifest",
    patternTags: ["string","counting","adjacent-pairs","one-pass"],
    signatureId: "fn:string->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You are reviewing the color code manifest of a newly assembled train, represented by a string `sequence`. Due to a logging error, a single color code might have been recorded multiple times consecutively when it was only supposed to be recorded once.\n\nGiven the logged string `sequence`, return the total number of possible original manifests the train could have had, assuming the recording error happened at most once.\n\n**Constraints**\n- `1 <= sequence.length <= 100`\n- `sequence` consists of lowercase English letters only.\n\n**Example 1**\n```\ninput:\nabbc\noutput: 2\n```\n*Explanation: The original manifest could be \"abc\" (if 'b' was mislogged) or \"abbc\" (if no error occurred).*\n\n**Example 2**\n```\ninput:\nz\noutput: 1\n```\n*Explanation: There is only 1 possible original manifest \"z\".*\n\n**Example 3**\n```\ninput:\naaaa\noutput: 4\n```\n*Explanation: The original manifest could be \"a\", \"aa\", \"aaa\", or \"aaaa\".*\n\n**Follow-up**\nCan you solve this in O(N) time with only a single pass through the string?",
    editorialMarkdown: "## Faulty Sensor Transmission\n\nThe key insight is that the number of possible original strings is exactly equal to `1 + the number of adjacent identical characters`. Each time `signal[i] == signal[i-1]`, we have one additional possibility for the original string (representing an extra stutter of that character).\n\n**Trap**: Trying to simulate removing each character or generating all possible strings using subsets will lead to duplicated effort or overcounting. We only need to count transitions where characters remain the same.\n\n**Complexity:**\n- **Time:** O(N) — one pass through the string to compare adjacent pairs.\n- **Space:** O(1) — no extra memory needed other than a counter variable.",
    referenceSolution: {
      JAVASCRIPT: "function solve(signal) {\n    let ans = 1;\n    for (let i = 1; i < signal.length; i++) {\n        if (signal[i] === signal[i - 1]) ans++;\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(signal: string): number {\n    let ans = 1;\n    for (let i = 1; i < signal.length; i++) {\n        if (signal[i] === signal[i - 1]) ans++;\n    }\n    return ans;\n}",
      PYTHON: "def solve(signal):\n    ans = 1\n    for i in range(1, len(signal)):\n        if signal[i] == signal[i - 1]:\n            ans += 1\n    return ans",
      JAVA: "    static int solve(String signal) {\n        int ans = 1;\n        for (int i = 1; i < signal.length(); i++) {\n            if (signal.charAt(i) == signal.charAt(i - 1)) ans++;\n        }\n        return ans;\n    }",
      CPP: "int solve(string signal) {\n    int ans = 1;\n    for (int i = 1; i < (int)signal.length(); i++) {\n        if (signal[i] == signal[i - 1]) ans++;\n    }\n    return ans;\n}",
      GO: "func solve(signal string) int {\n    ans := 1\n    for i := 1; i < len(signal); i++ {\n        if signal[i] == signal[i-1] {\n            ans++\n        }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "abbc", expectedStdout: "2", isSample: true },
      { stdin: "z", expectedStdout: "1", isSample: true },
      { stdin: "aaaa", expectedStdout: "4" },
      { stdin: "abcdef", expectedStdout: "1" },
      { stdin: "aabbcc", expectedStdout: "4" },
      { stdin: "aabaa", expectedStdout: "3" },
      { stdin: "zzzzzzzzzz", expectedStdout: "10" },
      { stdin: "a", expectedStdout: "1" },
    ],
  }),

  p({
    ...base,
    slug: "format-delivery-log-grid",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Spreadsheet Column Auto-Fit",
    patternTags: ["matrix","string","length","maximum"],
    signatureId: "fn:matrix->ints",
    avgSolveSeconds: 600,
    promptMarkdown: "You are developing a spreadsheet application and need to auto-fit the columns for a given `m x n` integer matrix `data`. The width required for a cell is the number of characters needed to represent its integer value, where a negative sign counts as one character.\n\nReturn an array of length `n`, where each element at index `i` represents the maximum character width required for any value in the `i`-th column.\n\n**Constraints**\n- `1 <= m, n <= 100`\n- `-10^9 <= data[i][j] <= 10^9`\n\n**Example 1**\n```\ninput:\n1 2;3 4\noutput: 1 1\n```\n*Explanation: Each column only contains single-digit positive numbers, so the max width is 1 for both.*\n\n**Example 2**\n```\ninput:\n-10 200;3 4\noutput: 3 3\n```\n*Explanation: In column 0, \"-10\" has length 3 and \"3\" has length 1, so max is 3. In column 1, \"200\" has length 3, max is 3.*\n\n**Example 3**\n```\ninput:\n0\noutput: 1\n```\n*Explanation: The only element is \"0\", which has length 1.*\n\n**Follow-up**\nCan you do this without storing the string representation of every element simultaneously?",
    editorialMarkdown: "## Format Delivery Log Grid\n\nThe most straightforward way is to iterate column by column. For each element in a given column, convert it to a string and measure its length. Track the maximum length observed for that column and add it to your results array.\n\n**Trap**: Make sure to account for negative signs, which add 1 to the string length. Converting an entire column before measuring can waste memory compared to converting on the fly.\n\n**Complexity:**\n- **Time:** O(M * N) — we visit each cell exactly once.\n- **Space:** O(N) — to store the array of column widths.",
    referenceSolution: {
      JAVASCRIPT: "function solve(grid) {\n    if (grid.length === 0) return [];\n    let m = grid.length, n = grid[0].length;\n    let ans = new Array(n).fill(0);\n    for (let j = 0; j < n; j++) {\n        let maxLen = 0;\n        for (let i = 0; i < m; i++) {\n            let len = String(grid[i][j]).length;\n            if (len > maxLen) maxLen = len;\n        }\n        ans[j] = maxLen;\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(grid: number[][]): number[] {\n    if (grid.length === 0) return [];\n    let m = grid.length, n = grid[0].length;\n    let ans = new Array(n).fill(0);\n    for (let j = 0; j < n; j++) {\n        let maxLen = 0;\n        for (let i = 0; i < m; i++) {\n            let len = String(grid[i][j]).length;\n            if (len > maxLen) maxLen = len;\n        }\n        ans[j] = maxLen;\n    }\n    return ans;\n}",
      PYTHON: "def solve(grid):\n    if not grid: return []\n    return [max(len(str(grid[i][j])) for i in range(len(grid))) for j in range(len(grid[0]))]",
      JAVA: "    static int[] solve(int[][] grid) {\n        if (grid.length == 0) return new int[0];\n        int m = grid.length, n = grid[0].length;\n        int[] ans = new int[n];\n        for (int j = 0; j < n; j++) {\n            int maxLen = 0;\n            for (int i = 0; i < m; i++) {\n                int len = String.valueOf(grid[i][j]).length();\n                if (len > maxLen) maxLen = len;\n            }\n            ans[j] = maxLen;\n        }\n        return ans;\n    }",
      CPP: "vector<int> solve(vector<vector<int>> grid) {\n    if (grid.empty()) return {};\n    int m = grid.size(), n = grid[0].size();\n    vector<int> ans(n, 0);\n    for (int j = 0; j < n; j++) {\n        for (int i = 0; i < m; i++) {\n            int len = to_string(grid[i][j]).length();\n            if (len > ans[j]) ans[j] = len;\n        }\n    }\n    return ans;\n}",
      GO: "func solve(grid [][]int) []int {\n    if len(grid) == 0 {\n        return []int{}\n    }\n    m, n := len(grid), len(grid[0])\n    ans := make([]int, n)\n    for j := 0; j < n; j++ {\n        maxLen := 0\n        for i := 0; i < m; i++ {\n            l := len(strconv.Itoa(grid[i][j]))\n            if l > maxLen {\n                maxLen = l\n            }\n        }\n        ans[j] = maxLen\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "1 2;3 4", expectedStdout: "1 1", isSample: true },
      { stdin: "-10 200;3 4", expectedStdout: "3 3", isSample: true },
      { stdin: "0", expectedStdout: "1" },
      { stdin: "-1 -2 -3;-4 -5 -6", expectedStdout: "2 2 2" },
      { stdin: "1000 2;30 4000", expectedStdout: "4 4" },
      { stdin: "9", expectedStdout: "1" },
      { stdin: "1 2 3;4 5 6;7 8 9", expectedStdout: "1 1 1" },
      { stdin: "1000000000 -1000000000", expectedStdout: "10 11" },
    ],
  }),

  p({
    ...base,
    slug: "secure-pin-verification",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Symbiotic Plant Phenotypes",
    patternTags: ["string","counting","frequency-count","adjacent-pairs"],
    signatureId: "fn:string->string",
    avgSolveSeconds: 600,
    promptMarkdown: "You are a botanist analyzing a sequence of plant phenotypes encoded as a string of digits `sequence`. You are looking for a symbiotic pair of plants. A symbiotic pair consists of two adjacent, distinct digits `x` and `y` where:\n1. `x != y`\n2. The total count of digit `x` in the entire sequence is exactly its numeric value.\n3. The total count of digit `y` in the entire sequence is exactly its numeric value.\n\nGiven the string `sequence`, return the first such adjacent pair as a 2-character string. If no such pair exists, return an empty string `\"\"`.\n\n**Constraints**\n- `2 <= sequence.length <= 100`\n- `sequence` consists of digits from `1` to `9` only.\n\n**Example 1**\n```\ninput:\n122\noutput: 12\n```\n*Explanation: '1' occurs 1 time, '2' occurs 2 times. The pair \"12\" satisfies the condition.*\n\n**Example 2**\n```\ninput:\n3331\noutput: 31\n```\n*Explanation: '3' occurs 3 times, '1' occurs 1 time. The adjacent pair \"31\" satisfies the condition.*\n\n**Example 3**\n```\ninput:\n111\noutput: \n```\n*Explanation: '1' occurs 3 times which does not match its numeric value (1), and there are no distinct adjacent pairs anyway.*\n\n**Follow-up**\nCan you solve this with a single integer array for frequency counting?",
    editorialMarkdown: "## Secure Pin Verification\n\nWe first need to count the overall frequency of every digit in the string. Since there are only 9 possible digits, a fixed-size array of length 10 is perfect for this. After counting frequencies, loop through the string looking for the first adjacent pair `pin[i]` and `pin[i+1]` that satisfy the conditions.\n\n**Trap**: Do not stop at checking if `pin[i] != pin[i+1]`; you must verify that the total frequency of each character equals its literal integer value.\n\n**Complexity:**\n- **Time:** O(N) — one pass to populate frequencies, and a second pass to find the valid pair.\n- **Space:** O(1) — the frequency map only requires space for 10 digits.",
    referenceSolution: {
      JAVASCRIPT: "function solve(pin) {\n    let counts = {};\n    for (let char of pin) {\n        counts[char] = (counts[char] || 0) + 1;\n    }\n    for (let i = 0; i < pin.length - 1; i++) {\n        let c1 = pin[i], c2 = pin[i+1];\n        if (c1 !== c2 && counts[c1] === parseInt(c1) && counts[c2] === parseInt(c2)) {\n            return c1 + c2;\n        }\n    }\n    return \"\";\n}",
      TYPESCRIPT: "function solve(pin: string): string {\n    let counts: Record<string, number> = {};\n    for (let char of pin) {\n        counts[char] = (counts[char] || 0) + 1;\n    }\n    for (let i = 0; i < pin.length - 1; i++) {\n        let c1 = pin[i], c2 = pin[i+1];\n        if (c1 !== c2 && counts[c1] === parseInt(c1) && counts[c2] === parseInt(c2)) {\n            return c1 + c2;\n        }\n    }\n    return \"\";\n}",
      PYTHON: "def solve(pin):\n    from collections import Counter\n    counts = Counter(pin)\n    for i in range(len(pin) - 1):\n        c1, c2 = pin[i], pin[i+1]\n        if c1 != c2 and counts[c1] == int(c1) and counts[c2] == int(c2):\n            return c1 + c2\n    return \"\"",
      JAVA: "    static String solve(String pin) {\n        int[] counts = new int[10];\n        for (char c : pin.toCharArray()) counts[c - '0']++;\n        for (int i = 0; i < pin.length() - 1; i++) {\n            char c1 = pin.charAt(i), c2 = pin.charAt(i+1);\n            if (c1 != c2 && counts[c1 - '0'] == (c1 - '0') && counts[c2 - '0'] == (c2 - '0')) {\n                return \"\" + c1 + c2;\n            }\n        }\n        return \"\";\n    }",
      CPP: "string solve(string pin) {\n    vector<int> counts(10, 0);\n    for (char c : pin) counts[c - '0']++;\n    for (int i = 0; i < (int)pin.length() - 1; i++) {\n        char c1 = pin[i], c2 = pin[i+1];\n        if (c1 != c2 && counts[c1 - '0'] == (c1 - '0') && counts[c2 - '0'] == (c2 - '0')) {\n            return string(1, c1) + string(1, c2);\n        }\n    }\n    return \"\";\n}",
      GO: "func solve(pin string) string {\n    counts := make([]int, 10)\n    for _, c := range pin {\n        counts[c - '0']++\n    }\n    for i := 0; i < len(pin)-1; i++ {\n        c1 := pin[i]\n        c2 := pin[i+1]\n        if c1 != c2 && counts[c1 - '0'] == int(c1 - '0') && counts[c2 - '0'] == int(c2 - '0') {\n            return string(c1) + string(c2)\n        }\n    }\n    return \"\"\n}",
    },
    tests: [
      { stdin: "122", expectedStdout: "12", isSample: true },
      { stdin: "3331", expectedStdout: "31", isSample: true },
      { stdin: "111", expectedStdout: "" },
      { stdin: "22333", expectedStdout: "23" },
      { stdin: "4444", expectedStdout: "" },
      { stdin: "1333", expectedStdout: "13" },
      { stdin: "5555522", expectedStdout: "52" },
      { stdin: "221", expectedStdout: "21" },
      { stdin: "22", expectedStdout: "" },
    ],
  }),

  p({
    ...base,
    slug: "assemble-robot-parts",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Assemble Robot Parts",
    patternTags: ["string","hash-map","counting","frequency-count"],
    signatureId: "fn:strings->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You are assembling robots from available parts. You are given an array of strings `blueprints`. \n\nThe first string `blueprints[0]` represents your `supply` of available components (each lowercase letter is a component type). The remaining strings represent the components required to build specific robots. \n\nYou can build a robot if you have enough components in your `supply`. Each component from your `supply` can be used at most once per robot (the supply does not deplete across different robots).\n\nReturn the total length of all robot strings that can be successfully built. If no robots can be built, or if `blueprints` only contains the `supply` string, return `0`.\n\n**Constraints**\n- `1 <= blueprints.length <= 100`\n- `1 <= blueprints[i].length <= 100`\n- `blueprints[i]` consists of lowercase English letters only.\n\n**Example 1**\n```\ninput:\nabc c ab a\noutput: 4\n```\n*Explanation: Supply is \"abc\". Robot \"c\" can be built (length 1). Robot \"ab\" can be built (length 2). Robot \"a\" can be built (length 1). Total length = 1 + 2 + 1 = 4.*\n\n**Example 2**\n```\ninput:\na b c\noutput: 0\n```\n*Explanation: Supply is \"a\". Robot \"b\" and \"c\" cannot be built because the components are missing. Total = 0.*\n\n**Example 3**\n```\ninput:\na\noutput: 0\n```\n*Explanation: There are no robots to build, only the supply string. Total = 0.*\n\n**Follow-up**\nCan you avoid re-counting the supply characters for every robot?",
    editorialMarkdown: "## Assemble Robot Parts\n\nFirst, build a frequency map (or a 26-element array) for the `supply` string (which is the first element of the input array). Then, for each remaining string, build its own frequency map and compare it to the supply map. If for every character, the required count is less than or equal to the supply count, the robot can be built. Add its length to the total.\n\n**Trap**: Do not deduct from the supply globally. The problem specifies that the supply does not deplete across different robots. Treat each robot independently against the full initial supply.\n\n**Complexity:**\n- **Time:** O(N) — where N is the total number of characters across all strings. We process each character a constant number of times.\n- **Space:** O(1) — since we only need arrays of size 26 for the lowercase English letters.",
    referenceSolution: {
      JAVASCRIPT: "function solve(blueprints) {\n    if (blueprints.length <= 1) return 0;\n    let supply = blueprints[0];\n    let supplyCounts = new Array(26).fill(0);\n    for (let i = 0; i < supply.length; i++) {\n        supplyCounts[supply.charCodeAt(i) - 97]++;\n    }\n    let total = 0;\n    for (let i = 1; i < blueprints.length; i++) {\n        let req = new Array(26).fill(0);\n        let possible = true;\n        for (let j = 0; j < blueprints[i].length; j++) {\n            let idx = blueprints[i].charCodeAt(j) - 97;\n            req[idx]++;\n            if (req[idx] > supplyCounts[idx]) {\n                possible = false;\n                break;\n            }\n        }\n        if (possible) total += blueprints[i].length;\n    }\n    return total;\n}",
      TYPESCRIPT: "function solve(blueprints: string[]): number {\n    if (blueprints.length <= 1) return 0;\n    let supply = blueprints[0];\n    let supplyCounts = new Array(26).fill(0);\n    for (let i = 0; i < supply.length; i++) {\n        supplyCounts[supply.charCodeAt(i) - 97]++;\n    }\n    let total = 0;\n    for (let i = 1; i < blueprints.length; i++) {\n        let req = new Array(26).fill(0);\n        let possible = true;\n        for (let j = 0; j < blueprints[i].length; j++) {\n            let idx = blueprints[i].charCodeAt(j) - 97;\n            req[idx]++;\n            if (req[idx] > supplyCounts[idx]) {\n                possible = false;\n                break;\n            }\n        }\n        if (possible) total += blueprints[i].length;\n    }\n    return total;\n}",
      PYTHON: "def solve(blueprints):\n    if len(blueprints) <= 1: return 0\n    from collections import Counter\n    supply = Counter(blueprints[0])\n    total = 0\n    for i in range(1, len(blueprints)):\n        req = Counter(blueprints[i])\n        if all(req[c] <= supply[c] for c in req):\n            total += len(blueprints[i])\n    return total",
      JAVA: "    static int solve(String[] blueprints) {\n        if (blueprints.length <= 1) return 0;\n        int[] supply = new int[26];\n        for (char c : blueprints[0].toCharArray()) supply[c - 'a']++;\n        int total = 0;\n        for (int i = 1; i < blueprints.length; i++) {\n            int[] req = new int[26];\n            boolean possible = true;\n            for (char c : blueprints[i].toCharArray()) {\n                req[c - 'a']++;\n                if (req[c - 'a'] > supply[c - 'a']) {\n                    possible = false;\n                    break;\n                }\n            }\n            if (possible) total += blueprints[i].length();\n        }\n        return total;\n    }",
      CPP: "int solve(vector<string> blueprints) {\n    if (blueprints.size() <= 1) return 0;\n    vector<int> supply(26, 0);\n    for (char c : blueprints[0]) supply[c - 'a']++;\n    int total = 0;\n    for (int i = 1; i < (int)blueprints.size(); i++) {\n        vector<int> req(26, 0);\n        bool possible = true;\n        for (char c : blueprints[i]) {\n            req[c - 'a']++;\n            if (req[c - 'a'] > supply[c - 'a']) {\n                possible = false;\n                break;\n            }\n        }\n        if (possible) total += blueprints[i].size();\n    }\n    return total;\n}",
      GO: "func solve(blueprints []string) int {\n    if len(blueprints) <= 1 {\n        return 0\n    }\n    supply := make([]int, 26)\n    for _, c := range blueprints[0] {\n        supply[c - 'a']++\n    }\n    total := 0\n    for i := 1; i < len(blueprints); i++ {\n        req := make([]int, 26)\n        possible := true\n        for _, c := range blueprints[i] {\n            req[c - 'a']++\n            if req[c - 'a'] > supply[c - 'a'] {\n                possible = false\n                break\n            }\n        }\n        if possible {\n            total += len(blueprints[i])\n        }\n    }\n    return total\n}",
    },
    tests: [
      { stdin: "abc c ab a", expectedStdout: "4", isSample: true },
      { stdin: "a b c", expectedStdout: "0", isSample: true },
      { stdin: "a", expectedStdout: "0" },
      { stdin: "aabbcc aabb aabbc abc", expectedStdout: "12" },
      { stdin: "xyz x y z", expectedStdout: "3" },
      { stdin: "abcdefgh abc xyz abcd", expectedStdout: "7" },
      { stdin: "z z z z", expectedStdout: "3" },
      { stdin: "aaa aa a aaaa", expectedStdout: "3" },
    ],
  }),

  p({
    ...base,
    slug: "identify-rare-isotope",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Unique Even Box Weight",
    patternTags: ["array","hash-map","frequency-count","first-occurrence"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You are processing a shipment of boxes, where the weight of each box is given in an integer array `weights`. You need to identify the first box weight in the sequence that is an even number and occurs exactly once in the entire shipment.\n\nReturn this box weight. If no such weight exists, return `-1`.\n\n**Constraints**\n- `1 <= weights.length <= 10^5`\n- `-10^9 <= weights[i] <= 10^9`\n\n**Example 1**\n```\ninput:\n2 4 6 2\noutput: 4\n```\n*Explanation: The even weights are 2, 4, 6, and 2. 2 appears twice. 4 appears once and is the first unique even element, so we return 4.*\n\n**Example 2**\n```\ninput:\n1 3 5\noutput: -1\n```\n*Explanation: There are no even weights in the array.*\n\n**Example 3**\n```\ninput:\n2 2 4 4\noutput: -1\n```\n*Explanation: The even weights 2 and 4 both appear multiple times. None are unique.*\n\n**Follow-up**\nCan you solve this in O(N) time?",
    editorialMarkdown: "## Identify Rare Isotope\n\nWe can find the solution in two passes. First, iterate through the array and use a hash map to count the frequencies of every integer. In the second pass, check each element in the original order. If an element is even and its frequency is exactly 1, return it immediately.\n\n**Trap**: Make sure to check the original array in order during the second pass, not the hash map's keys, to ensure you return the *first* unique even element according to the original sequence.\n\n**Complexity:**\n- **Time:** O(N) — two passes through the array.\n- **Space:** O(N) — worst case, every element is distinct, requiring O(N) space for the hash map.",
    referenceSolution: {
      JAVASCRIPT: "function solve(isotopes) {\n    let counts = new Map();\n    for (let x of isotopes) {\n        counts.set(x, (counts.get(x) || 0) + 1);\n    }\n    for (let x of isotopes) {\n        if (x % 2 === 0 && counts.get(x) === 1) return x;\n    }\n    return -1;\n}",
      TYPESCRIPT: "function solve(isotopes: number[]): number {\n    let counts = new Map<number, number>();\n    for (let x of isotopes) {\n        counts.set(x, (counts.get(x) || 0) + 1);\n    }\n    for (let x of isotopes) {\n        if (x % 2 === 0 && counts.get(x) === 1) return x;\n    }\n    return -1;\n}",
      PYTHON: "def solve(isotopes):\n    from collections import Counter\n    counts = Counter(isotopes)\n    for x in isotopes:\n        if x % 2 == 0 and counts[x] == 1:\n            return x\n    return -1",
      JAVA: "    static int solve(int[] isotopes) {\n        java.util.HashMap<Integer, Integer> counts = new java.util.HashMap<>();\n        for (int x : isotopes) {\n            counts.put(x, counts.getOrDefault(x, 0) + 1);\n        }\n        for (int x : isotopes) {\n            if (x % 2 == 0 && counts.get(x) == 1) return x;\n        }\n        return -1;\n    }",
      CPP: "int solve(vector<int> isotopes) {\n    unordered_map<int, int> counts;\n    for (int x : isotopes) counts[x]++;\n    for (int x : isotopes) {\n        if (x % 2 == 0 && counts[x] == 1) return x;\n    }\n    return -1;\n}",
      GO: "func solve(isotopes []int) int {\n    counts := make(map[int]int)\n    for _, x := range isotopes {\n        counts[x]++\n    }\n    for _, x := range isotopes {\n        if x % 2 == 0 && counts[x] == 1 {\n            return x\n        }\n    }\n    return -1\n}",
    },
    tests: [
      { stdin: "2 4 6 2", expectedStdout: "4", isSample: true },
      { stdin: "1 3 5", expectedStdout: "-1", isSample: true },
      { stdin: "2 2 4 4", expectedStdout: "-1" },
      { stdin: "8", expectedStdout: "8" },
      { stdin: "3 5 4 4 6 8 8", expectedStdout: "6" },
      { stdin: "-2 4 -2", expectedStdout: "4" },
      { stdin: "-4 -6 -6", expectedStdout: "-4" },
      { stdin: "", expectedStdout: "-1" },
      { stdin: "2 1 2 4 1 6", expectedStdout: "4" },
    ],
  }),

  p({
    ...base,
    slug: "standardize-planetary-names",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Standardize Planetary Names",
    patternTags: ["string","strings","case-conversion","array"],
    signatureId: "fn:strings->strings",
    avgSolveSeconds: 600,
    promptMarkdown: "You have a list of discovered planets `names`, but their entries were recorded with inconsistent capitalization. \n\nFormat each name so that its first letter is uppercase and all subsequent letters are lowercase. Return the corrected list of planet names.\n\n**Constraints**\n- `0 <= names.length <= 100`\n- `1 <= names[i].length <= 100`\n- `names[i]` consists of uppercase and lowercase English letters only.\n\n**Example 1**\n```\ninput:\nmARs vEnUs\noutput: Mars Venus\n```\n*Explanation: \"mARs\" becomes \"Mars\" and \"vEnUs\" becomes \"Venus\".*\n\n**Example 2**\n```\ninput:\nEARTH\noutput: Earth\n```\n*Explanation: \"EARTH\" is corrected to \"Earth\".*\n\n**Example 3**\n```\ninput:\npluto\noutput: Pluto\n```\n*Explanation: \"pluto\" is corrected to \"Pluto\".*\n\n**Follow-up**\nDoes your language provide built-in string methods that simplify case conversion?",
    editorialMarkdown: "## Standardize Planetary Names\n\nThe most direct approach is to take each string and reconstruct it: convert the first character to uppercase and the rest of the substring to lowercase. Most languages have built-in string slicing and case conversion functions that make this a one-liner per element.\n\n**Trap**: Be mindful of empty strings if the constraints allow them. Attempting to access the first character of an empty string can cause an out-of-bounds error.\n\n**Complexity:**\n- **Time:** O(N * M) — where N is the number of strings and M is the maximum length of a string, since we must visit every character to change its case.\n- **Space:** O(N * M) — to store the new list of formatted strings.",
    referenceSolution: {
      JAVASCRIPT: "function solve(names) {\n    return names.map(name => {\n        if (!name) return name;\n        return name.charAt(0).toUpperCase() + name.slice(1).toLowerCase();\n    });\n}",
      TYPESCRIPT: "function solve(names: string[]): string[] {\n    return names.map(name => {\n        if (!name) return name;\n        return name.charAt(0).toUpperCase() + name.slice(1).toLowerCase();\n    });\n}",
      PYTHON: "def solve(names):\n    return [name.capitalize() for name in names]",
      JAVA: "    static String[] solve(String[] names) {\n        String[] ans = new String[names.length];\n        for (int i = 0; i < names.length; i++) {\n            if (names[i].isEmpty()) {\n                ans[i] = \"\";\n            } else {\n                ans[i] = names[i].substring(0, 1).toUpperCase() + names[i].substring(1).toLowerCase();\n            }\n        }\n        return ans;\n    }",
      CPP: "vector<string> solve(vector<string> names) {\n    vector<string> ans(names.size());\n    for (int i = 0; i < (int)names.size(); i++) {\n        if (names[i].empty()) {\n            ans[i] = \"\";\n        } else {\n            string s = names[i];\n            if (s[0] >= 'a' && s[0] <= 'z') s[0] = s[0] - 'a' + 'A';\n            for (int j = 1; j < (int)s.length(); j++) {\n                if (s[j] >= 'A' && s[j] <= 'Z') s[j] = s[j] - 'A' + 'a';\n            }\n            ans[i] = s;\n        }\n    }\n    return ans;\n}",
      GO: "func solve(names []string) []string {\n    ans := make([]string, len(names))\n    for i, name := range names {\n        if len(name) == 0 {\n            ans[i] = \"\"\n            continue\n        }\n        runes := []rune(name)\n        if runes[0] >= 'a' && runes[0] <= 'z' {\n            runes[0] = runes[0] - 'a' + 'A'\n        }\n        for j := 1; j < len(runes); j++ {\n            if runes[j] >= 'A' && runes[j] <= 'Z' {\n                runes[j] = runes[j] - 'A' + 'a'\n            }\n        }\n        ans[i] = string(runes)\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "mARs vEnUs", expectedStdout: "Mars Venus", isSample: true },
      { stdin: "EARTH", expectedStdout: "Earth", isSample: true },
      { stdin: "pluto", expectedStdout: "Pluto" },
      { stdin: "jUPITER sATURN", expectedStdout: "Jupiter Saturn" },
      { stdin: "a b c", expectedStdout: "A B C" },
      { stdin: "xY zW", expectedStdout: "Xy Zw" },
      { stdin: "nePTune URANUS", expectedStdout: "Neptune Uranus" },
      { stdin: "", expectedStdout: "" },
    ],
  }),
];
