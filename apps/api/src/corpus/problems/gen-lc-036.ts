import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-036` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_036_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "valid-sensor-segments",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "SLIDING_WINDOW",
    title: "Harmonious Paint Strokes",
    patternTags: ["string","substring","counting","sliding-window"],
    signatureId: "fn:string,int->int",
    avgSolveSeconds: 420,
    promptMarkdown: "An artist creates a digital painting using two colors, represented by a string `canvas` consisting only of the characters 'x' (teal) and 'y' (magenta). A contiguous sequence of colors is considered a *harmonious stroke* if it is dominated by one of the colors—specifically, if it contains at most `tolerance` 'x' characters OR at most `tolerance` 'y' characters.\n\nGiven the string `canvas` and an integer `tolerance`, return the total number of harmonious strokes that can be found in the string.\n\n**Constraints**\n- `1 <= canvas.length <= 40`\n- `0 <= tolerance <= 40`\n- `canvas` consists only of 'x' and 'y'.\n\n**Example 1**\n```\ninput:\nxxyy\n1\noutput: 9\n```\n*Explanation: The valid substrings are \"x\" (2), \"y\" (2), \"xx\" (1), \"xy\" (1), \"yy\" (1), \"xxy\" (1), \"xyy\" (1). The only invalid substring is \"xxyy\" (contains 2 'x's and 2 'y's).*\n\n**Example 2**\n```\ninput:\nyyy\n1\noutput: 6\n```\n*Explanation: All 6 contiguous substrings are valid because they contain zero 'x's, satisfying the condition of having at most 1 'x'.*\n\n**Example 3**\n```\ninput:\nxyx\n0\noutput: 3\n```\n*Explanation: The valid substrings are the individual characters: \"x\" (twice) and \"y\" (once).*\n\n**Follow-up:** Can you solve this in \\(\\mathcal{O}(N)\\) time using a sliding window approach?",
    editorialMarkdown: "## Valid Sensor Segments\nTo solve this, we can check all possible substrings since the constraints are very small (N \\u2264 40). We use two nested loops to iterate over all start and end points of a substring. We maintain a count of 'x's and 'y's. For each substring, if the count of 'x' \\u2264 limit OR the count of 'y' \\u2264 limit, it's valid. An \\(\\mathcal{O}(N)\\) sliding window approach is also possible by counting substrings with at most `limit` 'x's, then substrings with at most `limit` 'y's, and subtracting the intersection, but brute force is simplest here.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N^2)\\) using nested loops, which easily passes for N \\u2264 40.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\).\n\n**Common Trap:**\nMisunderstanding the \"OR\" condition as an \"AND\" condition. A substring only needs to satisfy one of the two constraints to be counted as valid.",
    referenceSolution: {
      JAVASCRIPT: "function solve(s, k) { let c=0; for(let i=0;i<s.length;i++){ let x=0,y=0; for(let j=i;j<s.length;j++){ if(s[j]==='x') x++; else y++; if(x<=k || y<=k) c++; else break; } } return c; }",
      TYPESCRIPT: "function solve(s: string, k: number): number { let c=0; for(let i=0;i<s.length;i++){ let x=0,y=0; for(let j=i;j<s.length;j++){ if(s[j]==='x') x++; else y++; if(x<=k || y<=k) c++; else break; } } return c; }",
      PYTHON: "def solve(s, k):\n    c = 0\n    for i in range(len(s)):\n        x, y = 0, 0\n        for j in range(i, len(s)):\n            if s[j] == 'x': x += 1\n            else: y += 1\n            if x <= k or y <= k: c += 1\n            else: break\n    return c",
      JAVA: "    static int solve(String s, int k) { int c = 0; for(int i = 0; i < s.length(); i++) { int x = 0, y = 0; for(int j = i; j < s.length(); j++) { if(s.charAt(j) == 'x') x++; else y++; if(x <= k || y <= k) c++; else break; } } return c; }",
      CPP: "int solve(string s, int k) { int c = 0; for(int i = 0; i < s.length(); i++) { int x = 0, y = 0; for(int j = i; j < s.length(); j++) { if(s[j] == 'x') x++; else y++; if(x <= k || y <= k) c++; else break; } } return c; }",
      GO: "func solve(s string, k int) int { c := 0; for i := 0; i < len(s); i++ { x, y := 0, 0; for j := i; j < len(s); j++ { if s[j] == 'x' { x++ } else { y++ }; if x <= k || y <= k { c++ } else { break } } }; return c }",
    },
    tests: [
      { stdin: "xxyy\n1", expectedStdout: "9", isSample: true },
      { stdin: "yyy\n1", expectedStdout: "6", isSample: true },
      { stdin: "xyx\n0", expectedStdout: "3" },
      { stdin: "x\n1", expectedStdout: "1" },
      { stdin: "yyyyy\n2", expectedStdout: "15" },
      { stdin: "xyxyxy\n2", expectedStdout: "20" },
      { stdin: "xxyyxy\n1", expectedStdout: "16" },
      { stdin: "y\n0", expectedStdout: "1" },
    ],
  }),

  p({
    ...base,
    slug: "steady-drone-flight-periods",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Steady Drone Flight Periods",
    patternTags: ["string","counting","substring","math"],
    signatureId: "fn:string->int",
    avgSolveSeconds: 360,
    promptMarkdown: "You are analyzing the flight logs of an exploration drone. The logs are represented as a string `directions` where each character is 'N', 'S', 'E', or 'W', indicating the direction of movement for one unit of time.\n\nA \"steady flight period\" is defined as any contiguous sequence of time (substring) where the drone moves strictly in one direction.\n\nReturn the total number of steady flight periods in the log.\n\n**Constraints**\n- `1 <= directions.length <= 40`\n- `directions` consists only of 'N', 'S', 'E', and 'W'.\n\n**Example 1**\n```\ninput:\nNNSEEE\noutput: 10\n```\n*Explanation: The steady periods are: \"N\" (x2), \"S\" (x1), \"E\" (x3), \"NN\" (x1), \"EE\" (x2), and \"EEE\" (x1). Total = 2+1+3+1+2+1 = 10.*\n\n**Example 2**\n```\ninput:\nNESW\noutput: 4\n```\n*Explanation: Only length 1 periods are steady, as no direction is repeated consecutively.*\n\n**Example 3**\n```\ninput:\nWWWW\noutput: 10\n```\n*Explanation: 4 consecutive 'W's contribute 4 + 3 + 2 + 1 = 10 steady periods.*\n\n**Follow-up:** Can you calculate the total in \\(\\mathcal{O}(N)\\) time and \\(\\mathcal{O}(1)\\) space?",
    editorialMarkdown: "## Steady Drone Flight Periods\nTo solve this, we can group consecutive identical characters. For any contiguous block of identical characters of length `L`, the number of substrings it can form is given by the formula `L * (L + 1) / 2`. By iterating through the string and counting the length of each steady segment, we can add this formula's result to our total count and then reset for the next different direction.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N)\\), where \\(N\\) is the length of the string.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) as we only need to store counters.\n\n**Common Trap:**\nUsing a nested loop approach might be easier to write but it isn't necessary. If done using the formula, a common bug is forgetting to process the final group of characters after the loop terminates.",
    referenceSolution: {
      JAVASCRIPT: "function solve(s) { let ans=0, i=0; while(i<s.length) { let j=i; while(j<s.length && s[j]===s[i]) j++; let l=j-i; ans+=l*(l+1)/2; i=j; } return ans; }",
      TYPESCRIPT: "function solve(s: string): number { let ans=0, i=0; while(i<s.length) { let j=i; while(j<s.length && s[j]===s[i]) j++; let l=j-i; ans+=l*(l+1)/2; i=j; } return ans; }",
      PYTHON: "def solve(s):\n    ans = 0\n    i = 0\n    while i < len(s):\n        j = i\n        while j < len(s) and s[j] == s[i]: j += 1\n        l = j - i\n        ans += l * (l + 1) // 2\n        i = j\n    return ans",
      JAVA: "    static int solve(String s) { int ans=0, i=0; while(i<s.length()){ int j=i; while(j<s.length() && s.charAt(j)==s.charAt(i)) j++; int l=j-i; ans+=l*(l+1)/2; i=j; } return ans; }",
      CPP: "int solve(string s) { int ans=0, i=0; while(i<s.length()){ int j=i; while(j<s.length() && s[j]==s[i]) j++; int l=j-i; ans+=l*(l+1)/2; i=j; } return ans; }",
      GO: "func solve(s string) int { ans, i := 0, 0; for i < len(s) { j := i; for j < len(s) && s[j] == s[i] { j++ }; l := j - i; ans += l * (l + 1) / 2; i = j }; return ans }",
    },
    tests: [
      { stdin: "NNSEEE", expectedStdout: "10", isSample: true },
      { stdin: "NESW", expectedStdout: "4", isSample: true },
      { stdin: "WWWW", expectedStdout: "10" },
      { stdin: "N", expectedStdout: "1" },
      { stdin: "NNNNNN", expectedStdout: "21" },
      { stdin: "SSSSEEEENNNNWWWW", expectedStdout: "40" },
      { stdin: "NSN", expectedStdout: "3" },
      { stdin: "EEEWEE", expectedStdout: "10" },
    ],
  }),

  p({
    ...base,
    slug: "count-balanced-identification-codes",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Lucky Train Tickets",
    patternTags: ["math","strings","counting","digits"],
    signatureId: "fn:int,int->int",
    avgSolveSeconds: 480,
    promptMarkdown: "A conductor on a special train line checks the serial numbers on passengers' tickets. A ticket is considered a \"lucky ticket\" if it has an even number of digits (without leading zeros) and the sum of the digits in its first half is exactly equal to the sum of the digits in its second half.\n\nGiven two integers `start` and `end`, return the number of lucky tickets in the inclusive range `[start, end]`.\n\n**Constraints**\n- `1 <= start <= end <= 10000`\n\n**Example 1**\n```\ninput:\n10\n15\noutput: 1\n```\n*Explanation: The only lucky ticket is 11, because 1 = 1.*\n\n**Example 2**\n```\ninput:\n1\n9\noutput: 0\n```\n*Explanation: All numbers in this range have an odd number of digits, so none are lucky.*\n\n**Example 3**\n```\ninput:\n1000\n1020\noutput: 2\n```\n*Explanation: The lucky tickets are 1001 (1+0 = 0+1) and 1010 (1+0 = 1+0).*\n\n**Follow-up:** Can you optimize the check to avoid string conversions for every number?",
    editorialMarkdown: "## Count Balanced Identification Codes\nTo find all balanced identification codes, we can iterate through every number in the range from `low` to `high`. For each number, we convert it to a string to easily determine its length. If the length is odd, we skip it. If it is even, we slice the string into two halves and compare the sum of the digits in each half. If they match, we increment our counter.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N \\times L)\\), where \\(N\\) is the range `high - low` and \\(L\\) is the maximum number of digits (at most 5). This is well within limits.\n- **Space Complexity:** \\(\\mathcal{O}(L)\\) to store the string representation of the number.\n\n**Common Trap:**\nFailing to convert the character digits into integers before summing them. If you add characters '1' + '0' in some languages, you will concatenate them into \"10\" or sum their ASCII values, leading to incorrect comparisons.",
    referenceSolution: {
      JAVASCRIPT: "function solve(low, high) { let ans=0; for(let i=low; i<=high; i++) { let s = i.toString(); if(s.length % 2 === 0) { let h = s.length/2, sum1=0, sum2=0; for(let j=0; j<h; j++) sum1 += parseInt(s[j]); for(let j=h; j<s.length; j++) sum2 += parseInt(s[j]); if(sum1 === sum2) ans++; } } return ans; }",
      TYPESCRIPT: "function solve(low: number, high: number): number { let ans=0; for(let i=low; i<=high; i++) { let s = i.toString(); if(s.length % 2 === 0) { let h = s.length/2, sum1=0, sum2=0; for(let j=0; j<h; j++) sum1 += parseInt(s[j]); for(let j=h; j<s.length; j++) sum2 += parseInt(s[j]); if(sum1 === sum2) ans++; } } return ans; }",
      PYTHON: "def solve(low, high):\n    ans = 0\n    for i in range(low, high + 1):\n        s = str(i)\n        if len(s) % 2 == 0:\n            half = len(s) // 2\n            if sum(int(x) for x in s[:half]) == sum(int(x) for x in s[half:]):\n                ans += 1\n    return ans",
      JAVA: "    static int solve(int low, int high) { int ans=0; for(int i=low; i<=high; i++) { String s = String.valueOf(i); if(s.length() % 2 == 0) { int h = s.length()/2, sum1=0, sum2=0; for(int j=0; j<h; j++) sum1 += s.charAt(j)-'0'; for(int j=h; j<s.length(); j++) sum2 += s.charAt(j)-'0'; if(sum1 == sum2) ans++; } } return ans; }",
      CPP: "int solve(int low, int high) { int ans=0; for(int i=low; i<=high; i++) { string s = to_string(i); if(s.length() % 2 == 0) { int h = s.length()/2, sum1=0, sum2=0; for(int j=0; j<h; j++) sum1 += s[j]-'0'; for(int j=h; j<s.length(); j++) sum2 += s[j]-'0'; if(sum1 == sum2) ans++; } } return ans; }",
      GO: "func solve(low int, high int) int { ans := 0; for i := low; i <= high; i++ { s := strconv.Itoa(i); if len(s) % 2 == 0 { h := len(s)/2; sum1, sum2 := 0, 0; for j := 0; j < h; j++ { sum1 += int(s[j]-'0') }; for j := h; j < len(s); j++ { sum2 += int(s[j]-'0') }; if sum1 == sum2 { ans++ } } }; return ans }",
    },
    tests: [
      { stdin: "10\n15", expectedStdout: "1", isSample: true },
      { stdin: "1\n9", expectedStdout: "0", isSample: true },
      { stdin: "1000\n1020", expectedStdout: "2" },
      { stdin: "1\n1", expectedStdout: "0" },
      { stdin: "11\n11", expectedStdout: "1" },
      { stdin: "100\n100", expectedStdout: "0" },
      { stdin: "1\n1000", expectedStdout: "9" },
      { stdin: "1990\n2010", expectedStdout: "2" },
    ],
  }),

  p({
    ...base,
    slug: "count-successful-balloon-launches",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Count Successful Balloon Launches",
    patternTags: ["array","counting","simulation","greedy"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A team of meteorologists is launching a sequence of weather balloons. You are given an array of integers `levels`, representing the initial helium level of each balloon in the order they are prepared.\n\nWhen a balloon is prepared, some helium leaks based on the number of balloons *already successfully launched*. Specifically, the current balloon's helium level drops by exactly the number of successful previous launches. If its remaining helium level is strictly greater than 0, it is successfully launched. Otherwise, it fails to launch and does not increment the successful launch count.\n\nReturn the total number of successfully launched balloons.\n\n**Constraints**\n- `1 <= levels.length <= 40`\n- `0 <= levels[i] <= 40`\n\n**Example 1**\n```\ninput:\n1 1 2 1 5\noutput: 3\n```\n*Explanation: \n- Balloon 1: level 1 - 0 = 1 > 0. Launched (count=1).\n- Balloon 2: level 1 - 1 = 0. Fails.\n- Balloon 3: level 2 - 1 = 1 > 0. Launched (count=2).\n- Balloon 4: level 1 - 2 = -1 <= 0. Fails.\n- Balloon 5: level 5 - 2 = 3 > 0. Launched (count=3).\nTotal launched is 3.*\n\n**Example 2**\n```\ninput:\n0 1 2\noutput: 2\n```\n*Explanation:\n- Balloon 1: 0 - 0 = 0. Fails.\n- Balloon 2: 1 - 0 = 1 > 0. Launched (count=1).\n- Balloon 3: 2 - 1 = 1 > 0. Launched (count=2).*\n\n**Example 3**\n```\ninput:\n5 4 3 2 1\noutput: 3\n```\n*Explanation: Balloons 1, 2, and 3 are launched. Balloons 4 and 5 fail because the penalty is 3.*\n\n**Follow-up:** What is the time and space complexity of your solution?",
    editorialMarkdown: "## Count Successful Balloon Launches\nTo solve this, you can simulate the launch process by iterating over the array while keeping a counter for the number of balloons successfully launched. For each balloon, subtract the current successful launch count from its initial helium level. If the result is greater than 0, increment your success count. \n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N)\\), where \\(N\\) is the number of balloons. We process the array in a single pass.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) as we only use a single integer variable to track the launches.\n\n**Common Trap:**\nApplying the penalty based on the index position instead of the number of *successful* prior launches. The problem specifies that only successful launches cause helium leaks.",
    referenceSolution: {
      JAVASCRIPT: "function solve(levels) { let count = 0; for(let i=0; i<levels.length; i++) { if(levels[i] - count > 0) count++; } return count; }",
      TYPESCRIPT: "function solve(levels: number[]): number { let count = 0; for(let i=0; i<levels.length; i++) { if(levels[i] - count > 0) count++; } return count; }",
      PYTHON: "def solve(levels):\n    count = 0\n    for lvl in levels:\n        if lvl - count > 0:\n            count += 1\n    return count",
      JAVA: "    static int solve(int[] levels) { int count = 0; for(int i=0; i<levels.length; i++) { if(levels[i] - count > 0) count++; } return count; }",
      CPP: "int solve(vector<int> levels) { int count = 0; for(int i=0; i<levels.size(); i++) { if(levels[i] - count > 0) count++; } return count; }",
      GO: "func solve(levels []int) int { count := 0; for _, lvl := range levels { if lvl - count > 0 { count++ } }; return count }",
    },
    tests: [
      { stdin: "1 1 2 1 5", expectedStdout: "3", isSample: true },
      { stdin: "0 1 2", expectedStdout: "2", isSample: true },
      { stdin: "5 4 3 2 1", expectedStdout: "3" },
      { stdin: "0", expectedStdout: "0" },
      { stdin: "10", expectedStdout: "1" },
      { stdin: "0 0 0 0", expectedStdout: "0" },
      { stdin: "1 2 3 4 5", expectedStdout: "5" },
      { stdin: "10 0 10 0 10", expectedStdout: "3" },
    ],
  }),

  p({
    ...base,
    slug: "count-harmonic-serial-digits",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Magic Chest Combination",
    patternTags: ["math","digits","modulo","counting"],
    signatureId: "fn:int->int",
    avgSolveSeconds: 360,
    promptMarkdown: "In an adventure game, players encounter locked chests with an engraved integer `code`. The chest's internal locking mechanism reacts to the individual digits of this `code`. A digit helps unlock the chest if the entire `code` is perfectly divisible by that digit (leaving no remainder).\n\nYour task is to count how many of the individual digits in `code` evenly divide the whole `code`. \n\nIt is guaranteed that `code` does not contain the digit `0`.\n\n**Constraints**\n- `1 <= code <= 10^9`\n- `code` consists only of digits from '1' to '9'.\n\n**Example 1**\n```\ninput:\n125\noutput: 2\n```\n*Explanation: 125 is divisible by 1 and 5, but not by 2. Total is 2.*\n\n**Example 2**\n```\ninput:\n333\noutput: 3\n```\n*Explanation: 333 is divisible by 3 three times.*\n\n**Example 3**\n```\ninput:\n13\noutput: 1\n```\n*Explanation: 13 is divisible by 1, but not by 3.*\n\n**Follow-up:** Can you extract the digits mathematically without converting the integer to a string?",
    editorialMarkdown: "## Count Harmonic Serial Digits\nTo solve this problem, we need to extract each digit from the integer and verify if it evenly divides the original integer. We can convert the number to a string to easily iterate over each character, convert it back to an integer, and apply the modulo operator. Alternatively, we can use a mathematical approach: a `while` loop that takes `serial % 10` to get the last digit and then `serial / 10` to strip it off, processing the number right to left.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(\\log_{10} N)\\), which is simply the number of digits in `serial` (at most 10).\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) if done mathematically, or \\(\\mathcal{O}(\\log_{10} N)\\) if using a string conversion.\n\n**Common Trap:**\nIf the problem didn't guarantee the absence of the digit `0`, a division-by-zero error would crash your code when checking modulo zero. Even with the guarantee, mutating the original `serial` variable during extraction is a common mistake; you need a preserved copy of the original number for the modulo check.",
    referenceSolution: {
      JAVASCRIPT: "function solve(serial) { let count = 0; let s = serial.toString(); for(let i=0; i<s.length; i++) { if(serial % parseInt(s[i]) === 0) count++; } return count; }",
      TYPESCRIPT: "function solve(serial: number): number { let count = 0; let s = serial.toString(); for(let i=0; i<s.length; i++) { if(serial % parseInt(s[i]) === 0) count++; } return count; }",
      PYTHON: "def solve(serial):\n    return sum(1 for d in str(serial) if serial % int(d) == 0)",
      JAVA: "    static int solve(int serial) { int count = 0; int temp = serial; while(temp > 0) { int d = temp % 10; if(serial % d == 0) count++; temp /= 10; } return count; }",
      CPP: "int solve(int serial) { int count = 0; int temp = serial; while(temp > 0) { int d = temp % 10; if(serial % d == 0) count++; temp /= 10; } return count; }",
      GO: "func solve(serial int) int { count := 0; temp := serial; for temp > 0 { d := temp % 10; if serial % d == 0 { count++ }; temp /= 10 }; return count }",
    },
    tests: [
      { stdin: "125", expectedStdout: "2", isSample: true },
      { stdin: "333", expectedStdout: "3", isSample: true },
      { stdin: "13", expectedStdout: "1" },
      { stdin: "1", expectedStdout: "1" },
      { stdin: "7", expectedStdout: "1" },
      { stdin: "111", expectedStdout: "3" },
      { stdin: "124", expectedStdout: "3" },
      { stdin: "825", expectedStdout: "1" },
    ],
  }),

  p({
    ...base,
    slug: "count-valid-pantry-recipes",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Count Valid Pantry Recipes",
    patternTags: ["hash-set","string","counting","arrays"],
    signatureId: "fn:strings->int",
    avgSolveSeconds: 420,
    promptMarkdown: "A chef is deciding what to cook based on the ingredients available in their pantry. You are given an array of strings `data`. The first element, `data[0]`, is a string of distinct characters representing the ingredients currently in the pantry. All subsequent strings in the array represent recipes, where each character is a required ingredient.\n\nCount and return the number of recipes that can be made using *only* the ingredients available in the pantry. A recipe can use an available ingredient multiple times.\n\n**Constraints**\n- `2 <= data.length <= 40`\n- `1 <= data[i].length <= 20`\n- All strings consist of lowercase English letters.\n- `data[0]` contains distinct characters.\n\n**Example 1**\n```\ninput:\nab a b ab c abc\noutput: 3\n```\n*Explanation: The pantry has 'a' and 'b'. The recipes \"a\", \"b\", and \"ab\" can be made. \"c\" and \"abc\" require 'c', which is not in the pantry.*\n\n**Example 2**\n```\ninput:\nxyz x xyx z xyzzy\noutput: 4\n```\n*Explanation: The pantry has 'x', 'y', and 'z'. All 4 recipes can be made.*\n\n**Example 3**\n```\ninput:\na b c\noutput: 0\n```\n*Explanation: The pantry only has 'a'. The recipes \"b\" and \"c\" cannot be made.*\n\n**Follow-up:** Can you solve this using a boolean array instead of a hash set?",
    editorialMarkdown: "## Count Valid Pantry Recipes\nTo check if a recipe can be made, every character in the recipe string must exist in the pantry string. We can speed up this lookup by putting all characters from the pantry string into a Set or a boolean array (of size 26, for lowercase letters). Then, for each recipe string, we iterate through its characters; if we find any character not in our Set, the recipe is invalid. If we check all characters without failing, we increment our counter.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(P + R \\times M)\\), where \\(P\\) is the length of the pantry string, \\(R\\) is the number of recipes, and \\(M\\) is the max length of a recipe.\n- **Space Complexity:** \\(\\mathcal{O}(P)\\) or \\(\\mathcal{O}(1)\\) if using a fixed-size boolean array of 26 letters.\n\n**Common Trap:**\nCounting the pantry string `data[0]` as a recipe! The outer loop needs to start at index 1 to avoid evaluating the pantry itself.",
    referenceSolution: {
      JAVASCRIPT: "function solve(data) { let p = new Set(data[0]); let c = 0; for(let i=1; i<data.length; i++) { let v = true; for(let char of data[i]) { if(!p.has(char)) { v = false; break; } } if(v) c++; } return c; }",
      TYPESCRIPT: "function solve(data: string[]): number { let p = new Set(data[0]); let c = 0; for(let i=1; i<data.length; i++) { let v = true; for(let char of data[i]) { if(!p.has(char)) { v = false; break; } } if(v) c++; } return c; }",
      PYTHON: "def solve(data):\n    pantry = set(data[0])\n    c = 0\n    for i in range(1, len(data)):\n        if all(char in pantry for char in data[i]):\n            c += 1\n    return c",
      JAVA: "    static int solve(String[] data) { boolean[] p = new boolean[26]; for(char c : data[0].toCharArray()) p[c-'a'] = true; int count = 0; for(int i=1; i<data.length; i++) { boolean v = true; for(char c : data[i].toCharArray()) { if(!p[c-'a']) { v = false; break; } } if(v) count++; } return count; }",
      CPP: "int solve(vector<string> data) { bool p[26] = {false}; for(char c : data[0]) p[c-'a'] = true; int count = 0; for(int i=1; i<data.size(); i++) { bool v = true; for(char c : data[i]) { if(!p[c-'a']) { v = false; break; } } if(v) count++; } return count; }",
      GO: "func solve(data []string) int { p := make([]bool, 26); for _, c := range data[0] { p[c-'a'] = true }; count := 0; for i := 1; i < len(data); i++ { v := true; for _, c := range data[i] { if !p[c-'a'] { v = false; break } }; if v { count++ } }; return count }",
    },
    tests: [
      { stdin: "ab a b ab c abc", expectedStdout: "3", isSample: true },
      { stdin: "xyz x xyx z xyzzy", expectedStdout: "4", isSample: true },
      { stdin: "a b c", expectedStdout: "0" },
      { stdin: "a a", expectedStdout: "1" },
      { stdin: "abcdefghijklmnopqrstuvwxyz hello world", expectedStdout: "2" },
      { stdin: "z z z z z", expectedStdout: "4" },
      { stdin: "abcdef aa bb cc dd abc defg", expectedStdout: "5" },
      { stdin: "qwertyuiop asdf ghjk l", expectedStdout: "0" },
    ],
  }),
];
