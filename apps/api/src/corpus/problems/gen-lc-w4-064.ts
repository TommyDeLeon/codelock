import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w4-064` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-17 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W4_064_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "drone-repair-minimum-replacements",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "SLIDING_WINDOW",
    title: "Drone Repair Minimum Replacements",
    patternTags: ["sliding-window","string","counting"],
    signatureId: "fn:string,int->int",
    avgSolveSeconds: 450,
    promptMarkdown: "You are given a string `drones` where each character is either 'H' (Healthy) or 'D' (Damaged), and an integer `k` representing the required number of consecutive healthy drones for a patrol mission. In one operation, you can repair a damaged drone, changing it from 'D' to 'H'. Return the minimum number of operations required to get at least `k` consecutive 'H' drones.\n\n**Constraints**\n- `k` will be between 1 and the length of `drones`.\n- The length of `drones` will be between 1 and 10^4.\n- `drones` consists only of 'H' and 'D'.\n\n**Example 1**\n```\ninput:\nDHDHD\n2\noutput:\n1\n```\n*Explanation: Repairing the first 'D' gives \"HHDHD\", which contains 2 consecutive 'H's.*\n\n**Example 2**\n```\ninput:\nDDDD\n4\noutput:\n4\n```\n*Explanation: All 4 drones must be repaired to get 4 consecutive 'H's.*\n\n**Example 3**\n```\ninput:\nHHHH\n3\noutput:\n0\n```\n*Explanation: There are already 3 consecutive 'H's, so no repairs are needed.*\n\n**Follow-up**\nCan you solve this with O(N) time complexity and O(1) space complexity?",
    editorialMarkdown: "## Drone Repair Minimum Replacements\n\nYou need to find a window of size `k` in the string that contains the minimum number of 'D' characters.\n\nThe most efficient approach is to use a sliding window of size `k`. First, count the number of 'D's in the first `k` characters. This is your initial minimum operations. Then, slide the window one character at a time to the right. When moving the window, you only need to check the character entering the window and the character leaving the window, adjusting your count accordingly.\n\n**Trap**: Don't recalculate the number of 'D's from scratch for every window, as that would result in O(N*K) time complexity and likely time out.\n\n**Complexity:**\n- **Time:** O(N), where N is the length of the string.\n- **Space:** O(1), as we only need variables to keep track of counts.",
    referenceSolution: {
      JAVASCRIPT: "function solve(drones, k) {\n    let currD = 0;\n    for (let i = 0; i < k; i++) {\n        if (drones[i] === 'D') currD++;\n    }\n    let minD = currD;\n    for (let i = k; i < drones.length; i++) {\n        if (drones[i] === 'D') currD++;\n        if (drones[i - k] === 'D') currD--;\n        if (currD < minD) minD = currD;\n    }\n    return minD;\n}",
      TYPESCRIPT: "function solve(drones: string, k: number): number {\n    let currD = 0;\n    for (let i = 0; i < k; i++) {\n        if (drones[i] === 'D') currD++;\n    }\n    let minD = currD;\n    for (let i = k; i < drones.length; i++) {\n        if (drones[i] === 'D') currD++;\n        if (drones[i - k] === 'D') currD--;\n        if (currD < minD) minD = currD;\n    }\n    return minD;\n}",
      PYTHON: "def solve(drones, k):\n    currD = sum(1 for c in drones[:k] if c == 'D')\n    minD = currD\n    for i in range(k, len(drones)):\n        if drones[i] == 'D': currD += 1\n        if drones[i-k] == 'D': currD -= 1\n        if currD < minD: minD = currD\n    return minD",
      JAVA: "    static int solve(String drones, int k) {\n        int currD = 0;\n        for (int i = 0; i < k; i++) {\n            if (drones.charAt(i) == 'D') currD++;\n        }\n        int minD = currD;\n        for (int i = k; i < drones.length(); i++) {\n            if (drones.charAt(i) == 'D') currD++;\n            if (drones.charAt(i - k) == 'D') currD--;\n            if (currD < minD) minD = currD;\n        }\n        return minD;\n    }",
      CPP: "int solve(string drones, int k) {\n    int currD = 0;\n    for (int i = 0; i < k; i++) {\n        if (drones[i] == 'D') currD++;\n    }\n    int minD = currD;\n    for (int i = k; i < drones.length(); i++) {\n        if (drones[i] == 'D') currD++;\n        if (drones[i - k] == 'D') currD--;\n        if (currD < minD) minD = currD;\n    }\n    return minD;\n}",
      GO: "func solve(drones string, k int) int {\n    minD := k\n    currD := 0\n    for i := 0; i < k; i++ {\n        if drones[i] == 'D' {\n            currD++\n        }\n    }\n    minD = currD\n    for i := k; i < len(drones); i++ {\n        if drones[i] == 'D' {\n            currD++\n        }\n        if drones[i-k] == 'D' {\n            currD--\n        }\n        if currD < minD {\n            minD = currD\n        }\n    }\n    return minD\n}",
    },
    tests: [
      { stdin: "DHDHD\n2", expectedStdout: "1", isSample: true },
      { stdin: "DDDD\n4", expectedStdout: "4", isSample: true },
      { stdin: "HHHH\n3", expectedStdout: "0" },
      { stdin: "D\n1", expectedStdout: "1" },
      { stdin: "H\n1", expectedStdout: "0" },
      { stdin: "HDHDHDHDHD\n3", expectedStdout: "1" },
      { stdin: "DDDHHHDDD\n3", expectedStdout: "0" },
      { stdin: "HD\n2", expectedStdout: "1" },
    ],
  }),

  p({
    ...base,
    slug: "reactor-temperature-stabilization",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "GREEDY",
    title: "Reactor Temperature Stabilization",
    patternTags: ["greedy","arrays","simulation"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 500,
    promptMarkdown: "You are given an array of integers `temps` representing the temperature readings of a reactor over time. For the reactor to be stable, the temperature readings must be strictly increasing. In one operation, you can increase any temperature reading by 1. Return the minimum total number of operations required to make the array strictly increasing.\n\n**Constraints**\n- The array `temps` will contain between 1 and 10^4 elements.\n- Each element in `temps` will be between -10^4 and 10^4.\n\n**Example 1**\n```\ninput:\n1 5 2 4 1\noutput:\n14\n```\n*Explanation: The optimal stabilized temperatures are [1, 5, 6, 7, 8]. The operations are 0 + 0 + 4 + 3 + 7 = 14.*\n\n**Example 2**\n```\ninput:\n1 1 1\noutput:\n3\n```\n*Explanation: The array can be made strictly increasing as [1, 2, 3], which takes 0 + 1 + 2 = 3 operations.*\n\n**Example 3**\n```\ninput:\n1\noutput:\n0\n```\n*Explanation: An array with a single element is already strictly increasing.*\n\n**Follow-up**\nCan you achieve this in O(N) time with only O(1) additional space?",
    editorialMarkdown: "## Reactor Temperature Stabilization\n\nTo make the array strictly increasing with the minimum number of operations, we should process the array from left to right using a greedy approach.\n\nFor each temperature reading, it must be at least one greater than the previous stabilized temperature. If it is already strictly greater, no operations are needed. If it is less than or equal to the previous reading, we must increase it exactly to `previous + 1`. The number of operations added is `(previous + 1) - current`.\n\n**Trap**: Do not actually modify the array if you don't need to; you only need to keep track of the \"previous\" value to compute the operations, which saves memory.\n\n**Complexity:**\n- **Time:** O(N), where N is the length of the array.\n- **Space:** O(1), since we only keep track of the operations and the previous value.",
    referenceSolution: {
      JAVASCRIPT: "function solve(temps) {\n    if (temps.length === 0) return 0;\n    let ops = 0;\n    let prev = temps[0];\n    for (let i = 1; i < temps.length; i++) {\n        if (temps[i] <= prev) {\n            ops += (prev + 1 - temps[i]);\n            prev = prev + 1;\n        } else {\n            prev = temps[i];\n        }\n    }\n    return ops;\n}",
      TYPESCRIPT: "function solve(temps: number[]): number {\n    if (temps.length === 0) return 0;\n    let ops = 0;\n    let prev = temps[0];\n    for (let i = 1; i < temps.length; i++) {\n        if (temps[i] <= prev) {\n            ops += (prev + 1 - temps[i]);\n            prev = prev + 1;\n        } else {\n            prev = temps[i];\n        }\n    }\n    return ops;\n}",
      PYTHON: "def solve(temps):\n    if not temps: return 0\n    ops = 0\n    prev = temps[0]\n    for i in range(1, len(temps)):\n        if temps[i] <= prev:\n            ops += prev + 1 - temps[i]\n            prev = prev + 1\n        else:\n            prev = temps[i]\n    return ops",
      JAVA: "    static int solve(int[] temps) {\n        if (temps.length == 0) return 0;\n        int ops = 0;\n        int prev = temps[0];\n        for (int i = 1; i < temps.length; i++) {\n            if (temps[i] <= prev) {\n                ops += prev + 1 - temps[i];\n                prev = prev + 1;\n            } else {\n                prev = temps[i];\n            }\n        }\n        return ops;\n    }",
      CPP: "int solve(vector<int> temps) {\n    if (temps.empty()) return 0;\n    int ops = 0;\n    int prev = temps[0];\n    for (int i = 1; i < temps.size(); i++) {\n        if (temps[i] <= prev) {\n            ops += prev + 1 - temps[i];\n            prev = prev + 1;\n        } else {\n            prev = temps[i];\n        }\n    }\n    return ops;\n}",
      GO: "func solve(temps []int) int {\n    if len(temps) == 0 { return 0 }\n    ops := 0\n    prev := temps[0]\n    for i := 1; i < len(temps); i++ {\n        if temps[i] <= prev {\n            ops += prev + 1 - temps[i]\n            prev = prev + 1\n        } else {\n            prev = temps[i]\n        }\n    }\n    return ops\n}",
    },
    tests: [
      { stdin: "1 5 2 4 1", expectedStdout: "14", isSample: true },
      { stdin: "1 1 1", expectedStdout: "3", isSample: true },
      { stdin: "1", expectedStdout: "0" },
      { stdin: "-5 -3 -2", expectedStdout: "0" },
      { stdin: "1 2 3 4 5", expectedStdout: "0" },
      { stdin: "5 4 3 2 1", expectedStdout: "20" },
      { stdin: "10 10 10 10", expectedStdout: "6" },
      { stdin: "-10 -10 -10", expectedStdout: "3" },
    ],
  }),

  p({
    ...base,
    slug: "minimum-shield-startup-power",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Bank Account Initial Deposit",
    patternTags: ["prefix-sum","arrays","simulation"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 400,
    promptMarkdown: "You are given a sequence of integers representing daily transactions in a bank account. Positive numbers indicate deposits, and negative numbers indicate withdrawals. The account begins with an initial positive balance `B`. Each transaction is applied sequentially, updating the current balance. Determine the minimum initial positive balance `B` (such that `B >= 1`) required to ensure that the account balance never falls below 1 after any transaction.\n\n**Constraints**\n- The sequence will contain between 1 and 10^4 elements.\n- Each element will be between -10^4 and 10^4.\n\n**Example 1**\n```\ninput:\n-3 2 -3 4 2\noutput:\n5\n```\n*Explanation: Starting with B = 5, the daily balances will be 5 -> 2 -> 4 -> 1 -> 5 -> 7. The balance never falls below 1.*\n\n**Example 2**\n```\ninput:\n1 -2 -3\noutput:\n5\n```\n*Explanation: The balance goes up by 1, then down by 2, then down by 3. This is a net decrease of 4 from the maximum balance (or -4 total). An initial balance of at least B = 5 is required to remain at 1 or higher.*\n\n**Example 3**\n```\ninput:\n10\noutput:\n1\n```\n*Explanation: Since there are only deposits, the minimum positive starting balance of 1 is enough.*\n\n**Follow-up**\nCan you solve this with O(1) auxiliary space?",
    editorialMarkdown: "## Minimum Shield Startup Power\n\nWe need to find the lowest point the power level reaches relative to a starting value of 0.\n\nWe can simulate the fluctuations starting from 0 and keep track of the running sum. At each step, we update the running sum and record the minimum sum encountered so far. If the minimum sum is negative, we need to offset it by starting with `1 - minSum`. If the minimum sum is positive or zero, we only need the base minimum starting power of 1.\n\n**Trap**: Be careful with the requirement that the initial power must be at least 1. If the array contains only positive fluctuations, the minimum sum will be 0 (or positive), and `1 - minSum` could be less than 1 if calculated incorrectly. Simply setting initial power to `1 + abs(minSum)` works if `minSum` is negative.\n\n**Complexity:**\n- **Time:** O(N), where N is the number of fluctuations.\n- **Space:** O(1), as we only track a few integers.",
    referenceSolution: {
      JAVASCRIPT: "function solve(fluctuations) {\n    let minSum = 0;\n    let currentSum = 0;\n    for (let f of fluctuations) {\n        currentSum += f;\n        if (currentSum < minSum) minSum = currentSum;\n    }\n    return 1 - minSum;\n}",
      TYPESCRIPT: "function solve(fluctuations: number[]): number {\n    let minSum = 0;\n    let currentSum = 0;\n    for (let f of fluctuations) {\n        currentSum += f;\n        if (currentSum < minSum) minSum = currentSum;\n    }\n    return 1 - minSum;\n}",
      PYTHON: "def solve(fluctuations):\n    minSum = 0\n    currentSum = 0\n    for f in fluctuations:\n        currentSum += f\n        if currentSum < minSum:\n            minSum = currentSum\n    return 1 - minSum",
      JAVA: "    static int solve(int[] fluctuations) {\n        int minSum = 0;\n        int currentSum = 0;\n        for (int f : fluctuations) {\n            currentSum += f;\n            if (currentSum < minSum) minSum = currentSum;\n        }\n        return 1 - minSum;\n    }",
      CPP: "int solve(vector<int> fluctuations) {\n    int minSum = 0;\n    int currentSum = 0;\n    for (int f : fluctuations) {\n        currentSum += f;\n        if (currentSum < minSum) minSum = currentSum;\n    }\n    return 1 - minSum;\n}",
      GO: "func solve(fluctuations []int) int {\n    minSum := 0\n    currentSum := 0\n    for _, f := range fluctuations {\n        currentSum += f\n        if currentSum < minSum {\n            minSum = currentSum\n        }\n    }\n    return 1 - minSum\n}",
    },
    tests: [
      { stdin: "-3 2 -3 4 2", expectedStdout: "5", isSample: true },
      { stdin: "1 -2 -3", expectedStdout: "5", isSample: true },
      { stdin: "10", expectedStdout: "1" },
      { stdin: "-10", expectedStdout: "11" },
      { stdin: "0", expectedStdout: "1" },
      { stdin: "10 -20 5", expectedStdout: "11" },
      { stdin: "-1 -1 -1", expectedStdout: "4" },
      { stdin: "5 5 5 5", expectedStdout: "1" },
    ],
  }),

  p({
    ...base,
    slug: "teleportation-frequency-error",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Teleportation Frequency Error",
    patternTags: ["math","base-conversion","absolute-value"],
    signatureId: "fn:int->int",
    avgSolveSeconds: 400,
    promptMarkdown: "A teleportation pad is calibrated using a target frequency integer `n`. However, due to quantum interference, the receiver often perceives the \"mirror\" of this frequency. The mirror of a positive integer is formed by reversing its digits (ignoring any leading zeros in the reversed number). The error of the system is the absolute difference between `n` and its mirror. Return this error.\n\n**Constraints**\n- `n` will be between 1 and 10^9.\n\n**Example 1**\n```\ninput:\n123\noutput:\n198\n```\n*Explanation: The mirror of 123 is 321. The absolute difference is |123 - 321| = 198.*\n\n**Example 2**\n```\ninput:\n10\noutput:\n9\n```\n*Explanation: The mirror of 10 is 01, which evaluates to 1. |10 - 1| = 9.*\n\n**Example 3**\n```\ninput:\n5\noutput:\n0\n```\n*Explanation: The mirror of 5 is 5. The absolute difference is 0.*\n\n**Follow-up**\nCan you solve this mathematically without converting the integer to a string?",
    editorialMarkdown: "## Teleportation Frequency Error\n\nThe problem requires calculating the absolute difference between a number and its reversed version.\n\nYou can construct the reversed number mathematically. Start with a variable `reversed = 0` and a temporary variable `temp = n`. While `temp` is greater than 0, multiply `reversed` by 10 and add the last digit of `temp` (`temp % 10`). Then divide `temp` by 10 (integer division). Finally, return the absolute difference between `n` and `reversed`.\n\n**Trap**: Be careful with string conversions in some languages as leading zeros on the reversed string can sometimes cause parsing issues, though standard library integer parsers handle them fine. The mathematical approach avoids strings entirely.\n\n**Complexity:**\n- **Time:** O(log10(N)), where N is the given number. This is proportional to the number of digits.\n- **Space:** O(1), as only a few variables are used.",
    referenceSolution: {
      JAVASCRIPT: "function solve(n) {\n    let temp = n;\n    let r = 0;\n    while (temp > 0) {\n        r = r * 10 + (temp % 10);\n        temp = Math.floor(temp / 10);\n    }\n    return Math.abs(n - r);\n}",
      TYPESCRIPT: "function solve(n: number): number {\n    let temp = n;\n    let r = 0;\n    while (temp > 0) {\n        r = r * 10 + (temp % 10);\n        temp = Math.floor(temp / 10);\n    }\n    return Math.abs(n - r);\n}",
      PYTHON: "def solve(n):\n    r = int(str(n)[::-1])\n    return abs(n - r)",
      JAVA: "    static int solve(int n) {\n        int temp = n;\n        int r = 0;\n        while (temp > 0) {\n            r = r * 10 + (temp % 10);\n            temp /= 10;\n        }\n        return Math.abs(n - r);\n    }",
      CPP: "int solve(int n) {\n    int temp = n;\n    int r = 0;\n    while (temp > 0) {\n        r = r * 10 + (temp % 10);\n        temp /= 10;\n    }\n    return abs(n - r);\n}",
      GO: "func solve(n int) int {\n    temp := n\n    r := 0\n    for temp > 0 {\n        r = r * 10 + (temp % 10)\n        temp /= 10\n    }\n    diff := n - r\n    if diff < 0 {\n        return -diff\n    }\n    return diff\n}",
    },
    tests: [
      { stdin: "123", expectedStdout: "198", isSample: true },
      { stdin: "10", expectedStdout: "9", isSample: true },
      { stdin: "5", expectedStdout: "0" },
      { stdin: "100", expectedStdout: "99" },
      { stdin: "11", expectedStdout: "0" },
      { stdin: "987", expectedStdout: "198" },
      { stdin: "1", expectedStdout: "0" },
      { stdin: "999", expectedStdout: "0" },
    ],
  }),

  p({
    ...base,
    slug: "unmapped-planetary-sectors",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Missing Manuscript Pages",
    patternTags: ["arrays","intervals","two-pointers"],
    signatureId: "fn:ints,int->ints",
    avgSolveSeconds: 600,
    promptMarkdown: "You are given a strictly increasing sequence of unique integers representing the page numbers of a manuscript that have been successfully recovered, followed by an integer `N` which is the highest possible page number in the complete manuscript. The valid page numbers range from `0` to `N` inclusive. Your task is to find all the missing page ranges. The missing ranges must be output in strictly increasing order based on the starting page of each range. Return a 1-dimensional sequence of integers where each consecutive pair of numbers `[start, end]` denotes a missing, inclusive range of page numbers.\n\n**Constraints**\n- The number of recovered pages will be between 0 and 10^4.\n- `N` will be between 0 and 10^9.\n- Each recovered page number will be between 0 and `N`.\n- The sequence of recovered pages is strictly increasing.\n\n**Example 1**\n```\ninput:\n0 1 3 50 75\n99\noutput:\n2 2 4 49 51 74 76 99\n```\n*Explanation: The unrecovered page ranges are 2 to 2, 4 to 49, 51 to 74, and 76 to 99, output in strictly increasing order.*\n\n**Example 2**\n```\ninput:\n\n5\noutput:\n0 5\n```\n*Explanation: No pages have been recovered, meaning the full range from 0 to 5 is missing.*\n\n**Example 3**\n```\ninput:\n2\n2\noutput:\n0 1\n```\n*Explanation: Only page 2 was recovered, so pages 0 to 1 are missing.*\n\n**Follow-up**\nCan you find the missing ranges in a single pass?",
    editorialMarkdown: "## Unmapped Planetary Sectors\n\nThe goal is to find missing intervals in a range `[0, N]` given a sorted list of present elements.\n\nWe can iterate through the array and keep track of the `nextExpected` sector ID, initially 0. For each sector in the array, if it is strictly greater than `nextExpected`, we have found a missing range from `nextExpected` to `sector - 1`. We add this pair to our results. We then update `nextExpected` to `sector + 1`. After iterating through the array, we must also check if `nextExpected` is less than or equal to `N`. If so, we add a final missing range from `nextExpected` to `N`.\n\n**Trap**: The most common mistake is forgetting to check for a missing range after the last element in the array up to `N`.\n\n**Complexity:**\n- **Time:** O(K), where K is the number of elements in the `sectors` array.\n- **Space:** O(1) auxiliary space, ignoring the space used for the output array.",
    referenceSolution: {
      JAVASCRIPT: "function solve(sectors, N) {\n    let res = [];\n    let nextExpected = 0;\n    for (let s of sectors) {\n        if (s > nextExpected) {\n            res.push(nextExpected, s - 1);\n        }\n        nextExpected = s + 1;\n    }\n    if (nextExpected <= N) {\n        res.push(nextExpected, N);\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(sectors: number[], N: number): number[] {\n    let res: number[] = [];\n    let nextExpected = 0;\n    for (let s of sectors) {\n        if (s > nextExpected) {\n            res.push(nextExpected, s - 1);\n        }\n        nextExpected = s + 1;\n    }\n    if (nextExpected <= N) {\n        res.push(nextExpected, N);\n    }\n    return res;\n}",
      PYTHON: "def solve(sectors, N):\n    res = []\n    nextExpected = 0\n    for s in sectors:\n        if s > nextExpected:\n            res.extend([nextExpected, s - 1])\n        nextExpected = s + 1\n    if nextExpected <= N:\n        res.extend([nextExpected, N])\n    return res",
      JAVA: "    static int[] solve(int[] sectors, int N) {\n        java.util.List<Integer> list = new java.util.ArrayList<>();\n        int nextExpected = 0;\n        for (int s : sectors) {\n            if (s > nextExpected) {\n                list.add(nextExpected);\n                list.add(s - 1);\n            }\n            nextExpected = s + 1;\n        }\n        if (nextExpected <= N) {\n            list.add(nextExpected);\n            list.add(N);\n        }\n        int[] res = new int[list.size()];\n        for (int i = 0; i < list.size(); i++) res[i] = list.get(i);\n        return res;\n    }",
      CPP: "vector<int> solve(vector<int> sectors, int N) {\n    vector<int> res;\n    int nextExpected = 0;\n    for (int s : sectors) {\n        if (s > nextExpected) {\n            res.push_back(nextExpected);\n            res.push_back(s - 1);\n        }\n        nextExpected = s + 1;\n    }\n    if (nextExpected <= N) {\n        res.push_back(nextExpected);\n        res.push_back(N);\n    }\n    return res;\n}",
      GO: "func solve(sectors []int, N int) []int {\n    res := []int{}\n    nextExpected := 0\n    for _, s := range sectors {\n        if s > nextExpected {\n            res = append(res, nextExpected, s - 1)\n        }\n        nextExpected = s + 1\n    }\n    if nextExpected <= N {\n        res = append(res, nextExpected, N)\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "0 1 3 50 75\n99", expectedStdout: "2 2 4 49 51 74 76 99", isSample: true },
      { stdin: "\n5", expectedStdout: "0 5", isSample: true },
      { stdin: "2\n2", expectedStdout: "0 1" },
      { stdin: "0 1 2 3\n3", expectedStdout: "" },
      { stdin: "0\n2", expectedStdout: "1 2" },
      { stdin: "1 2\n3", expectedStdout: "0 0 3 3" },
      { stdin: "1 3 5\n6", expectedStdout: "0 0 2 2 4 4 6 6" },
      { stdin: "0 5\n5", expectedStdout: "1 4" },
    ],
  }),

  p({
    ...base,
    slug: "identify-cloned-android-model",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Identify Cloned Android Model",
    patternTags: ["hash-map","arrays","counting"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A batch of androids has been delivered to a space station. The batch is represented by an array `models` of size `2 * N`, where each element is an integer representing an android model ID. Due to a manufacturing quirk, exactly one model ID is cloned and appears exactly `N` times in the batch. All other `N` androids in the batch have completely unique model IDs (they appear exactly once). Find and return the model ID of the cloned android.\n\n**Constraints**\n- The length of `models` will be `2 * N` where `N` is between 1 and 10^4.\n- Each element in `models` will be between 0 and 10^9.\n- Exactly one element appears `N` times, and all other elements appear exactly once.\n\n**Example 1**\n```\ninput:\n1 2 3 3\noutput:\n3\n```\n*Explanation: The array size is 4 (N = 2). The model ID 3 appears 2 times.*\n\n**Example 2**\n```\ninput:\n2 1 2 5 3 2\noutput:\n2\n```\n*Explanation: The array size is 6 (N = 3). The model ID 2 appears 3 times.*\n\n**Example 3**\n```\ninput:\n5 1 5 2 5 3 5 4\noutput:\n5\n```\n*Explanation: The model ID 5 appears 4 times in an array of size 8.*\n\n**Follow-up**\nCan you solve this in O(N) time and O(1) auxiliary space?",
    editorialMarkdown: "## Identify Cloned Android Model\n\nYou need to find the element that appears `N` times in an array of size `2 * N`.\n\nThe simplest approach is to use a hash set to keep track of the model IDs you have seen so far. Since all other elements appear exactly once, the first element you encounter that is already in the set must be the cloned model ID.\n\nAlternatively, for O(1) space, since the target element appears `N` times in an array of size `2 * N`, it must appear adjacent or separated by at most two elements. You can check `models[i] == models[i+1]` and `models[i] == models[i+2]`.\n\n**Trap**: Be careful with assumptions about array sorting; the array is unsorted and sorting it would take O(N log N) time, which is less optimal than O(N).\n\n**Complexity:**\n- **Time:** O(N), as we might need to scan through part of the array.\n- **Space:** O(N) for the hash set, or O(1) if using the adjacent checking method.",
    referenceSolution: {
      JAVASCRIPT: "function solve(models) {\n    let seen = new Set();\n    for (let m of models) {\n        if (seen.has(m)) return m;\n        seen.add(m);\n    }\n    return -1;\n}",
      TYPESCRIPT: "function solve(models: number[]): number {\n    let seen = new Set<number>();\n    for (let m of models) {\n        if (seen.has(m)) return m;\n        seen.add(m);\n    }\n    return -1;\n}",
      PYTHON: "def solve(models):\n    seen = set()\n    for m in models:\n        if m in seen:\n            return m\n        seen.add(m)\n    return -1",
      JAVA: "    static int solve(int[] models) {\n        java.util.HashSet<Integer> seen = new java.util.HashSet<>();\n        for (int m : models) {\n            if (seen.contains(m)) return m;\n            seen.add(m);\n        }\n        return -1;\n    }",
      CPP: "int solve(vector<int> models) {\n    unordered_set<int> seen;\n    for (int m : models) {\n        if (seen.count(m)) return m;\n        seen.insert(m);\n    }\n    return -1;\n}",
      GO: "func solve(models []int) int {\n    seen := make(map[int]bool)\n    for _, m := range models {\n        if seen[m] {\n            return m\n        }\n        seen[m] = true\n    }\n    return -1\n}",
    },
    tests: [
      { stdin: "1 2 3 3", expectedStdout: "3", isSample: true },
      { stdin: "2 1 2 5 3 2", expectedStdout: "2", isSample: true },
      { stdin: "5 1 5 2 5 3 5 4", expectedStdout: "5" },
      { stdin: "9 9", expectedStdout: "9" },
      { stdin: "100 200 100 300 100 400", expectedStdout: "100" },
      { stdin: "10 20 30 20", expectedStdout: "20" },
      { stdin: "7 7 8 9", expectedStdout: "7" },
      { stdin: "10 11 12 10 13 10 14 10 15 10", expectedStdout: "10" },
    ],
  }),
];
