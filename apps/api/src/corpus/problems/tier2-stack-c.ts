import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'STACK', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_2_STACK_C_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'quoted-bracket-check', title:'Brackets Outside Quoted Text', difficulty:'EASY', patternTags:['stack','strings','parsing'], signatureId:'fn:string->bool', avgSolveSeconds:600,
    promptMarkdown:['Return whether every `(`, `)`, `[`, `]`, `{`, and `}` is properly nested, but ignore brackets between double quotes. A quote is never escaped.','', '**Example','','```','input: ("[not a bracket]")','output: true','```','', 'The input may be empty. The named edge case is an unmatched bracket inside quotes: it does not affect the answer.'].join('\n'),
    editorialMarkdown:['## A delimiter stack with quote mode','', 'A stack records only opening delimiters that are active code. Toggle a quote flag at every double quote; while that flag is set, all brackets are ordinary characters. Outside quotes, push an opening delimiter and require each closing delimiter to match the current top, because the newest unclosed opening must close first.', '', 'The quiet mistake is treating quoted brackets as syntax. That passes plain examples but rejects text such as `"["`. The scan is O(n) time and the stack is O(n) space, both bounded by the string length.'].join('\n'),
    referenceSolution:six('function solve(a){let s=[],q=false,m={")":"(","]":"[","}":"{"};for(let c of a){if(c==="\\\""){q=!q;continue}if(q)continue;if("([{".includes(c))s.push(c);else if(m[c]&&(s.pop()!==m[c]))return false}return !q&&s.length===0;}','function solve(a:string):boolean{const s:string[]=[];let q=false;const m:any={")":"(","]":"[","}":"{"};for(const c of a){if(c===`"`){q=!q;continue;}if(q)continue;if("([{".includes(c))s.push(c);else if(m[c]&&s.pop()!==m[c])return false;}return !q&&!s.length;}','def solve(a):\n    s=[]; quoted=False; match={")":"(","]":"[","}":"{"}\n    for c in a:\n        if c=="\\\"": quoted=not quoted\n        elif not quoted and c in "([{": s.append(c)\n        elif not quoted and c in match and (not s or s.pop()!=match[c]): return False\n    return not quoted and not s','    static boolean solve(String a) { java.util.ArrayList<Character>s=new java.util.ArrayList<>();boolean q=false;for(char c:a.toCharArray()){if(c==34){q=!q;continue;}if(q)continue;if("([{".indexOf(c)>=0)s.add(c);else if(")] }".replace(" ","").indexOf(c)>=0){char e=c==\')\'?\'(\':c==\']\'?\'[\':\'{\';if(s.isEmpty()||s.remove(s.size()-1)!=e)return false;}}return !q&&s.isEmpty(); }','bool solve(string a){vector<char>s;bool q=false;for(char c:a){if(c==34){q=!q;continue;}if(q)continue;if(c==\'(\'||c==\'[\'||c==\'{\')s.push_back(c);else if(c==\')\'||c==\']\'||c==\'}\'){char e=c==\')\'?\'(\':c==\']\'?\'[\':\'{\';if(s.empty()||s.back()!=e)return false;s.pop_back();}}return !q&&s.empty();}','func solve(a string) bool {s:=[]rune{};q:=false;for _,c:=range a{if c==34{q=!q;continue};if q{continue};if strings.ContainsRune("([{",c){s=append(s,c)}else if strings.ContainsRune(")]}",c){e:=rune(\'(\');if c==\']\'{e=\'[\'};if c==\'}\'{e=\'{\'};if len(s)==0||s[len(s)-1]!=e{return false};s=s[:len(s)-1]}};return !q&&len(s)==0}'),
    tests:[{stdin:'("[not a bracket]")',expectedStdout:'true',isSample:true},{stdin:'([)]',expectedStdout:'false',isSample:true},{stdin:'"}"',expectedStdout:'true'},{stdin:'(',expectedStdout:'false'},{stdin:'',expectedStdout:'true'}] }),
  p({ ...base, slug:'circular-next-larger-values', title:'Next Larger Value Around the Ring', difficulty:'EASY', patternTags:['stack','monotonic-stack','circular-array'], signatureId:'fn:ints->ints', avgSolveSeconds:660,
    promptMarkdown:['For each number, return the first strictly larger value encountered while moving right around the circular list. Return `-1` if a full lap finds none.','', '**Example','','```','input: 2 5 1 3','output: 5 -1 3 5','```','', 'The list has at least one number. Equal values are not larger, and wrapping from the last position to the first is the important edge case.'].join('\n'),
    editorialMarkdown:['## Two laps over a decreasing stack','', 'Walk two conceptual copies of the list while keeping indices whose answers are unresolved in decreasing value order. When a larger current value arrives, it resolves every smaller value at the top. Only push indices during the first lap so each output position is created once; the second lap exists solely to supply wrapped candidates.', '', 'The quiet mistake is pushing again during lap two, which can make an index resolve against itself. Pop only on strict inequality because equal values are not answers. Each index enters and leaves once: O(n) time and O(n) stack space.'].join('\n'),
    referenceSolution:six('function solve(a){let r=Array(a.length).fill(-1),s=[];for(let i=0;i<2*a.length;i++){let j=i%a.length;while(s.length&&a[s.at(-1)]<a[j])r[s.pop()]=a[j];if(i<a.length)s.push(j)}return r;}','function solve(a:number[]):number[]{const r=Array(a.length).fill(-1),s:number[]=[];for(let i=0;i<2*a.length;i++){const j=i%a.length;while(s.length&&a[s[s.length-1]]<a[j])r[s.pop()!]=a[j];if(i<a.length)s.push(j);}return r;}','def solve(a):\n    out=[-1]*len(a); s=[]\n    for i in range(2*len(a)):\n        j=i%len(a)\n        while s and a[s[-1]]<a[j]: out[s.pop()]=a[j]\n        if i<len(a): s.append(j)\n    return out','    static int[] solve(int[] a) { int[]r=new int[a.length];java.util.Arrays.fill(r,-1);java.util.ArrayList<Integer>s=new java.util.ArrayList<>();for(int i=0;i<2*a.length;i++){int j=i%a.length;while(!s.isEmpty()&&a[s.get(s.size()-1)]<a[j])r[s.remove(s.size()-1)]=a[j];if(i<a.length)s.add(j);}return r; }','vector<int> solve(vector<int>a){vector<int>r(a.size(),-1),s;for(int i=0;i<2*(int)a.size();i++){int j=i%a.size();while(!s.empty()&&a[s.back()]<a[j]){r[s.back()]=a[j];s.pop_back();}if(i<(int)a.size())s.push_back(j);}return r;}','func solve(a []int) []int {r:=make([]int,len(a));for i:=range r{r[i]=-1};s:=[]int{};for i:=0;i<2*len(a);i++{j:=i%len(a);for len(s)>0&&a[s[len(s)-1]]<a[j]{r[s[len(s)-1]]=a[j];s=s[:len(s)-1]};if i<len(a){s=append(s,j)}};return r}'),
    tests:[{stdin:'2 5 1 3',expectedStdout:'5 -1 3 5',isSample:true},{stdin:'4 4',expectedStdout:'-1 -1',isSample:true},{stdin:'1',expectedStdout:'-1'},{stdin:'5 1 2',expectedStdout:'-1 2 5'}] }),
  p({ ...base, slug:'postfix-signed-total', title:'Evaluate a Signed Postfix Line', difficulty:'EASY', patternTags:['stack','expression','parsing'], signatureId:'fn:string->int', avgSolveSeconds:660,
    promptMarkdown:['Evaluate a well-formed space-separated postfix expression of integers and `+`, `-`, and `*`. Every operator consumes its left operand first, then its right operand.','', '**Example','','```','input: 8 -3 - 2 *','output: 22','```','', 'Numbers may be negative. A single number is valid; the named edge case is subtraction order, where `8 -3 -` means `8 - (-3)`.'].join('\n'),
    editorialMarkdown:['## Operand stack for postfix evaluation','', 'Read tokens from left to right. A number waits on the stack until an operator arrives; then pop the right operand followed by the left operand, calculate, and push the result. The postfix order guarantees those two popped values are exactly the operands belonging to that operator.', '', 'The quiet mistake is popping into left then right, which silently reverses subtraction while addition and multiplication still pass. Recognize operators as exact one-character tokens so negative numbers remain numbers. Time is O(n) and stack space is O(n), bounded by token count.'].join('\n'),
    referenceSolution:six('function solve(a){let s=[];for(let x of a.split(" ")){if("+-*".includes(x)&&x.length===1){let r=s.pop(),l=s.pop();s.push(x==="+"?l+r:x==="-"?l-r:l*r)}else s.push(+x)}return s[0];}','function solve(a:string):number{const s:number[]=[];for(const x of a.split(" ")){if(x.length===1&&"+-*".includes(x)){const r=s.pop()!,l=s.pop()!;s.push(x==="+"?l+r:x==="-"?l-r:l*r);}else s.push(Number(x));}return s[0];}','def solve(a):\n    s=[]\n    for x in a.split():\n        if x in ("+","-","*"):\n            r=s.pop(); l=s.pop(); s.append(l+r if x=="+" else l-r if x=="-" else l*r)\n        else: s.append(int(x))\n    return s[0]','    static int solve(String a) { java.util.ArrayList<Integer>s=new java.util.ArrayList<>();for(String x:a.split(" ")){if(x.length()==1&&"+-*".contains(x)){int r=s.remove(s.size()-1),l=s.remove(s.size()-1);s.add(x.charAt(0)==\'+\'?l+r:x.charAt(0)==\'-\'?l-r:l*r);}else s.add(Integer.parseInt(x));}return s.get(0); }','int solve(string a){stringstream z(a);string x;vector<int>s;while(z>>x){if(x.size()==1&&(x=="+"||x=="-"||x=="*")){int r=s.back();s.pop_back();int l=s.back();s.pop_back();s.push_back(x=="+"?l+r:x=="-"?l-r:l*r);}else s.push_back(stoi(x));}return s[0];}','func solve(a string) int {s:=[]int{};for _,x:=range strings.Fields(a){if len(x)==1&&strings.Contains("+-*",x){r:=s[len(s)-1];l:=s[len(s)-2];s=s[:len(s)-2];if x=="+"{s=append(s,l+r)}else if x=="-"{s=append(s,l-r)}else{s=append(s,l*r)}}else{v,_:=strconv.Atoi(x);s=append(s,v)}};return s[0]}'),
    tests:[{stdin:'8 -3 - 2 *',expectedStdout:'22',isSample:true},{stdin:'4 5 + 3 *',expectedStdout:'27',isSample:true},{stdin:'-7',expectedStdout:'-7'},{stdin:'9 2 - 4 *',expectedStdout:'28'}] }),
  p({ ...base, slug:'wrapped-next-larger-distance', title:'Steps to a Larger Ring Neighbor', difficulty:'MEDIUM', patternTags:['stack','monotonic-stack','circular-array'], signatureId:'fn:ints->ints', avgSolveSeconds:780,
    promptMarkdown:['For each position, return how many rightward steps around the circular list reach its first strictly larger value, or `-1` if none exists.','', '**Example','','```','input: 3 1 4 2','output: 2 1 -1 1','```','', 'The list is non-empty. A wrapped answer counts real steps, so the final `2` needs one step to reach the first `3`.'].join('\n'),
    editorialMarkdown:['## Circular monotonic stack with original indices','', 'Use the same decreasing unresolved stack as circular next-greater, but store each original index. When index `j` resolves index `i`, the distance is `j - i` if it lies rightward in the first lap, otherwise `j + n - i`. A second lap supplies only potential wrapped values.', '', 'The quiet mistake is using modulo subtraction, which reports zero or a negative distance at the wrap. Do not push in the second lap, or a position can be matched to its own duplicate. The work is O(n) time and O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){let r=Array(a.length).fill(-1),s=[],n=a.length;for(let x=0;x<2*n;x++){let j=x%n;while(s.length&&a[s.at(-1)]<a[j]){let i=s.pop();r[i]=j>i?j-i:j+n-i}if(x<n)s.push(j)}return r;}','function solve(a:number[]):number[]{const n=a.length,r=Array(n).fill(-1),s:number[]=[];for(let x=0;x<2*n;x++){const j=x%n;while(s.length&&a[s[s.length-1]]<a[j]){const i=s.pop()!;r[i]=j>i?j-i:j+n-i;}if(x<n)s.push(j);}return r;}','def solve(a):\n    n=len(a); out=[-1]*n; s=[]\n    for x in range(2*n):\n        j=x%n\n        while s and a[s[-1]]<a[j]:\n            i=s.pop(); out[i]=j-i if j>i else j+n-i\n        if x<n: s.append(j)\n    return out','    static int[] solve(int[]a){int n=a.length;int[]r=new int[n];java.util.Arrays.fill(r,-1);java.util.ArrayList<Integer>s=new java.util.ArrayList<>();for(int x=0;x<2*n;x++){int j=x%n;while(!s.isEmpty()&&a[s.get(s.size()-1)]<a[j]){int i=s.remove(s.size()-1);r[i]=j>i?j-i:j+n-i;}if(x<n)s.add(j);}return r;}','vector<int> solve(vector<int>a){int n=a.size();vector<int>r(n,-1),s;for(int x=0;x<2*n;x++){int j=x%n;while(!s.empty()&&a[s.back()]<a[j]){int i=s.back();s.pop_back();r[i]=j>i?j-i:j+n-i;}if(x<n)s.push_back(j);}return r;}','func solve(a []int) []int {n:=len(a);r:=make([]int,n);for i:=range r{r[i]=-1};s:=[]int{};for x:=0;x<2*n;x++{j:=x%n;for len(s)>0&&a[s[len(s)-1]]<a[j]{i:=s[len(s)-1];s=s[:len(s)-1];if j>i{r[i]=j-i}else{r[i]=j+n-i}};if x<n{s=append(s,j)}};return r}'),
    tests:[{stdin:'3 1 4 2',expectedStdout:'2 1 -1 1',isSample:true},{stdin:'2 2',expectedStdout:'-1 -1',isSample:true},{stdin:'1',expectedStdout:'-1'},{stdin:'5 1 2',expectedStdout:'-1 1 1'}] }),
  p({ ...base, slug:'decode-layered-repeat-note', title:'Decode Layered Repeat Notes', difficulty:'MEDIUM', patternTags:['stack','strings','decoding'], signatureId:'fn:string->string', avgSolveSeconds:780,
    promptMarkdown:['Decode a non-empty note where `number[letters]` repeats its bracketed letters, and groups may nest. Letters are lowercase and every repeat count is positive.','', '**Example','','```','input: 2[a3[b]]','output: abbbabbb','```','', 'A number can have several digits. The named edge case is nesting: an inner group is fully expanded before its outer repetition.'].join('\n'),
    editorialMarkdown:['## Stack of unfinished decoded prefixes','', 'Build the current text until an opening bracket appears. Save that prefix and its parsed count, then start a fresh inner buffer. At a closing bracket, repeat the completed inner buffer and append it to the saved prefix; this restores exactly the enclosing context, so nesting naturally follows last-in-first-out order.', '', 'The quiet mistake is repeating characters as they are read, which breaks multi-character inner groups and nested counts. Reset the number after consuming an opening bracket. If L is the expanded output length, time and space are O(L), bounded by the required decoded string.'].join('\n'),
    referenceSolution:six('function solve(a){let st=[],cur="",num=0;for(let c of a){if(c>="0"&&c<="9")num=num*10+(c.charCodeAt()-48);else if(c==="["){st.push([cur,num]);cur="";num=0}else if(c==="]"){let [p,k]=st.pop();cur=p+cur.repeat(k)}else cur+=c}return cur;}','function solve(a:string):string{let st:[string,number][]=[],cur="",num=0;for(const c of a){if(c>="0"&&c<="9")num=num*10+c.charCodeAt(0)-48;else if(c==="["){st.push([cur,num]);cur="";num=0;}else if(c==="]"){const [p,k]=st.pop()!;cur=p+cur.repeat(k);}else cur+=c;}return cur;}','def solve(a):\n    st=[]; cur=""; num=0\n    for c in a:\n        if c.isdigit(): num=num*10+int(c)\n        elif c=="[": st.append((cur,num));cur="";num=0\n        elif c=="]":\n            p,k=st.pop();cur=p+cur*k\n        else: cur+=c\n    return cur','    static String solve(String a){java.util.ArrayList<String>p=new java.util.ArrayList<>();java.util.ArrayList<Integer>k=new java.util.ArrayList<>();String cur="";int num=0;for(char c:a.toCharArray()){if(Character.isDigit(c))num=num*10+c-\'0\';else if(c==\'[\'){p.add(cur);k.add(num);cur="";num=0;}else if(c==\']\'){int z=k.remove(k.size()-1);String q=p.remove(p.size()-1),x="";for(int i=0;i<z;i++)x+=cur;cur=q+x;}else cur+=c;}return cur;}','string solve(string a){vector<pair<string,int>>s;string cur;int num=0;for(char c:a){if(c>=\'0\'&&c<=\'9\')num=num*10+c-\'0\';else if(c==\'[\'){s.push_back({cur,num});cur="";num=0;}else if(c==\']\'){auto z=s.back();s.pop_back();string x;for(int i=0;i<z.second;i++)x+=cur;cur=z.first+x;}else cur+=c;}return cur;}','func solve(a string) string {p:=[]string{};k:=[]int{};cur:="";num:=0;for _,c:=range a{if c>=\'0\'&&c<=\'9\'{num=num*10+int(c-\'0\')}else if c==\'[\'{p=append(p,cur);k=append(k,num);cur="";num=0}else if c==\']\'{z:=k[len(k)-1];k=k[:len(k)-1];pre:=p[len(p)-1];p=p[:len(p)-1];x:="";for i:=0;i<z;i++{x+=cur};cur=pre+x}else{cur+=string(c)}};return cur}'),
    tests:[{stdin:'2[a3[b]]',expectedStdout:'abbbabbb',isSample:true},{stdin:'3[xy]',expectedStdout:'xyxyxy',isSample:true},{stdin:'10[z]',expectedStdout:'zzzzzzzzzz'},{stdin:'2[a2[c]]',expectedStdout:'accacc'}] }),
  p({ ...base, slug:'parenthesized-plus-minus-total', title:'Add and Subtract Through Parentheses', difficulty:'MEDIUM', patternTags:['stack','expression','parsing'], signatureId:'fn:string->int', avgSolveSeconds:780,
    promptMarkdown:['Evaluate a non-empty expression containing nonnegative integers, `+`, `-`, parentheses, and optional spaces. Parentheses may nest.','', '**Example','','```','input: 10 - (3 + (2 - 5))','output: 10','```','', 'Whitespace has no meaning. The edge case is a minus before a parenthesized group, which negates that whole group.'].join('\n'),
    editorialMarkdown:['## Sign-context stack for nested sums','', 'Maintain the sign contributed by all open parentheses. On an opening parenthesis, save the current context multiplied by the pending sign; every number then contributes its local sign times that context. Closing a parenthesis restores the prior context, so a minus outside a group automatically reaches every term inside it.', '', 'The quiet mistake is applying a minus only to the first number after `(`. It passes flat expressions but fails nested groups. Parse all digits of each number before adding it. The scan is O(n) time and the context stack is O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){let s=[1],ctx=1,sg=1,sum=0;for(let i=0;i<a.length;){let c=a[i];if(c===" ")i++;else if(c==="+"){sg=1;i++}else if(c==="-"){sg=-1;i++}else if(c==="("){ctx*=sg;s.push(ctx);sg=1;i++}else if(c===")"){s.pop();ctx=s.at(-1);i++}else{let v=0;while(i<a.length&&a[i]>="0"&&a[i]<="9")v=v*10+a.charCodeAt(i++)-48;sum+=ctx*sg*v;sg=1}}return sum;}','function solve(a:string):number{const s:number[]=[1];let ctx=1,sg=1,sum=0;for(let i=0;i<a.length;){const c=a[i];if(c===" ")i++;else if(c==="+"){sg=1;i++;}else if(c==="-"){sg=-1;i++;}else if(c==="("){ctx*=sg;s.push(ctx);sg=1;i++;}else if(c===")"){s.pop();ctx=s[s.length-1];i++;}else{let v=0;while(i<a.length&&a[i]>="0"&&a[i]<="9")v=v*10+a.charCodeAt(i++)-48;sum+=ctx*sg*v;sg=1;}}return sum;}','def solve(a):\n    st=[1]; ctx=1; sign=1; total=0; i=0\n    while i<len(a):\n        c=a[i]\n        if c==" ": i+=1\n        elif c=="+": sign=1;i+=1\n        elif c=="-": sign=-1;i+=1\n        elif c=="(": ctx*=sign;st.append(ctx);sign=1;i+=1\n        elif c==")": st.pop();ctx=st[-1];i+=1\n        else:\n            v=0\n            while i<len(a) and a[i].isdigit():v=v*10+int(a[i]);i+=1\n            total+=ctx*sign*v;sign=1\n    return total','    static int solve(String a){java.util.ArrayList<Integer>s=new java.util.ArrayList<>();s.add(1);int ctx=1,sg=1,sum=0;for(int i=0;i<a.length();){char c=a.charAt(i);if(c==\' \')i++;else if(c==\'+\'){sg=1;i++;}else if(c==\'-\'){sg=-1;i++;}else if(c==\'(\'){ctx*=sg;s.add(ctx);sg=1;i++;}else if(c==\')\'){s.remove(s.size()-1);ctx=s.get(s.size()-1);i++;}else{int v=0;while(i<a.length()&&Character.isDigit(a.charAt(i)))v=v*10+a.charAt(i++)-\'0\';sum+=ctx*sg*v;sg=1;}}return sum;}','int solve(string a){vector<int>s(1,1);int ctx=1,sg=1,sum=0;for(int i=0;i<(int)a.size();){char c=a[i];if(c==\' \')i++;else if(c==\'+\'){sg=1;i++;}else if(c==\'-\'){sg=-1;i++;}else if(c==\'(\'){ctx*=sg;s.push_back(ctx);sg=1;i++;}else if(c==\')\'){s.pop_back();ctx=s.back();i++;}else{int v=0;while(i<(int)a.size()&&isdigit(a[i]))v=v*10+a[i++]-\'0\';sum+=ctx*sg*v;sg=1;}}return sum;}','func solve(a string) int {s:=[]int{1};ctx,sg,sum:=1,1,0;for i:=0;i<len(a);{c:=a[i];if c==\' \'{i++}else if c==\'+\'{sg=1;i++}else if c==\'-\'{sg=-1;i++}else if c==\'(\'{ctx*=sg;s=append(s,ctx);sg=1;i++}else if c==\')\'{s=s[:len(s)-1];ctx=s[len(s)-1];i++}else{v:=0;for i<len(a)&&a[i]>=\'0\'&&a[i]<=\'9\'{v=v*10+int(a[i]-\'0\');i++};sum+=ctx*sg*v;sg=1}};return sum}'),
    tests:[{stdin:'10 - (3 + (2 - 5))',expectedStdout:'10',isSample:true},{stdin:'1-(2+3)',expectedStdout:'-4',isSample:true},{stdin:'7',expectedStdout:'7'},{stdin:'20-(1-(2+3))',expectedStdout:'24'}] }),
  p({ ...base, slug:'largest-rectangle-uneven-shelves', title:'Largest Uneven Shelf Block', difficulty:'MEDIUM', patternTags:['stack','monotonic-stack','histogram'], signatureId:'fn:ints->int', avgSolveSeconds:840,
    promptMarkdown:['Each positive number is a shelf height of width one. Return the largest rectangular area made from consecutive shelves, using the shortest shelf in that block as its height.','', '**Example','','```','input: 2 1 5 6 2 3','output: 10','```','', 'The list has at least one positive height. The named edge case is a rectangle that continues to the final shelf.'].join('\n'),
    editorialMarkdown:['## Increasing-stack histogram boundaries','', 'Keep indices with increasing heights. A new shorter height ends the possible right boundary for every taller index at the top. When popping one, the new top is the first shorter shelf on its left, so the popped height owns exactly the width between those two shorter boundaries. Append a zero-height sentinel to flush final rectangles.', '', 'The quiet mistake is forgetting that sentinel, leaving a rising suffix unmeasured. Use the width after popping, not before, so the left boundary is excluded. Every index pushes and pops once: O(n) time and O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){let s=[],best=0;for(let i=0;i<=a.length;i++){let h=i===a.length?0:a[i];while(s.length&&a[s.at(-1)]>h){let x=a[s.pop()],l=s.length?s.at(-1):-1;best=Math.max(best,x*(i-l-1))}s.push(i)}return best;}','function solve(a:number[]):number{const s:number[]=[];let best=0;for(let i=0;i<=a.length;i++){const h=i===a.length?0:a[i];while(s.length&&a[s[s.length-1]]>h){const x=a[s.pop()!],l=s.length?s[s.length-1]:-1;best=Math.max(best,x*(i-l-1));}s.push(i);}return best;}','def solve(a):\n    s=[]; best=0\n    for i in range(len(a)+1):\n        h=0 if i==len(a) else a[i]\n        while s and a[s[-1]]>h:\n            x=a[s.pop()];left=s[-1] if s else -1;best=max(best,x*(i-left-1))\n        s.append(i)\n    return best','    static int solve(int[]a){java.util.ArrayList<Integer>s=new java.util.ArrayList<>();int best=0;for(int i=0;i<=a.length;i++){int h=i==a.length?0:a[i];while(!s.isEmpty()&&a[s.get(s.size()-1)]>h){int x=a[s.remove(s.size()-1)],l=s.isEmpty()?-1:s.get(s.size()-1);best=Math.max(best,x*(i-l-1));}s.add(i);}return best;}','int solve(vector<int>a){vector<int>s;int best=0;for(int i=0;i<=(int)a.size();i++){int h=i==(int)a.size()?0:a[i];while(!s.empty()&&a[s.back()]>h){int x=a[s.back()];s.pop_back();int l=s.empty()?-1:s.back();best=max(best,x*(i-l-1));}s.push_back(i);}return best;}','func solve(a []int) int {s:=[]int{};best:=0;for i:=0;i<=len(a);i++{h:=0;if i<len(a){h=a[i]};for len(s)>0&&a[s[len(s)-1]]>h{x:=a[s[len(s)-1]];s=s[:len(s)-1];l:=-1;if len(s)>0{l=s[len(s)-1]};v:=x*(i-l-1);if v>best{best=v}};s=append(s,i)};return best}'),
    tests:[{stdin:'2 1 5 6 2 3',expectedStdout:'10',isSample:true},{stdin:'2 4',expectedStdout:'4',isSample:true},{stdin:'3 3 3',expectedStdout:'9'},{stdin:'5',expectedStdout:'5'}] }),
  p({ ...base, slug:'total-strict-stock-span', title:'Add Every Strict Price Span', difficulty:'MEDIUM', patternTags:['stack','monotonic-stack','spans'], signatureId:'fn:ints->int', avgSolveSeconds:720,
    promptMarkdown:['For each daily price, find the number of consecutive days ending today whose prices are strictly smaller than today. Return the sum of those spans.','', '**Example','','```','input: 100 80 60 70 60 75 85','output: 16','```','', 'Every list has at least one price. Equal prices stop a span; this strict rule is the named edge case.'].join('\n'),
    editorialMarkdown:['## Previous greater-or-equal boundary stack','', 'For day i, the span begins after the closest earlier price that is at least today’s price. Maintain indices with nonincreasing prices, removing only prices strictly smaller than today. The remaining top is that boundary, so add `i - boundary`; when none remains, add `i + 1`.', '', 'The quiet mistake is popping an equal price, accidentally making the span non-strict. Keeping indices rather than values supplies the distance immediately. Each index is pushed and popped once, so time is O(n) and stack space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let s=[],z=0;for(let i=0;i<a.length;i++){while(s.length&&a[s.at(-1)]<a[i])s.pop();z+=i-(s.length?s.at(-1):-1);s.push(i)}return z;}','function solve(a:number[]):number{const s:number[]=[];let z=0;for(let i=0;i<a.length;i++){while(s.length&&a[s[s.length-1]]<a[i])s.pop();z+=i-(s.length?s[s.length-1]:-1);s.push(i);}return z;}','def solve(a):\n    s=[]; total=0\n    for i,x in enumerate(a):\n        while s and a[s[-1]]<x:s.pop()\n        total+=i-(s[-1] if s else -1);s.append(i)\n    return total','    static int solve(int[]a){java.util.ArrayList<Integer>s=new java.util.ArrayList<>();int z=0;for(int i=0;i<a.length;i++){while(!s.isEmpty()&&a[s.get(s.size()-1)]<a[i])s.remove(s.size()-1);z+=i-(s.isEmpty()?-1:s.get(s.size()-1));s.add(i);}return z;}','int solve(vector<int>a){vector<int>s;int z=0;for(int i=0;i<(int)a.size();i++){while(!s.empty()&&a[s.back()]<a[i])s.pop_back();z+=i-(s.empty()?-1:s.back());s.push_back(i);}return z;}','func solve(a []int) int {s:=[]int{};z:=0;for i,x:=range a{for len(s)>0&&a[s[len(s)-1]]<x{s=s[:len(s)-1]};b:=-1;if len(s)>0{b=s[len(s)-1]};z+=i-b;s=append(s,i)};return z}'),
    tests:[{stdin:'100 80 60 70 60 75 85',expectedStdout:'16',isSample:true},{stdin:'5 5',expectedStdout:'2',isSample:true},{stdin:'1 2 3',expectedStdout:'6'},{stdin:'9',expectedStdout:'1'}] }),
  p({ ...base, slug:'minimum-after-one-digit-removal', title:'Smallest Number After One Digit Vanishes', difficulty:'MEDIUM', patternTags:['stack','greedy','digits'], signatureId:'fn:string->string', avgSolveSeconds:720,
    promptMarkdown:['Given a non-empty decimal number with at least two digits, remove exactly one digit and return the smallest possible remaining decimal string. Keep leading zeroes if they remain.','', '**Example','','```','input: 143221','output: 13221','```','', 'The input contains only digits. The edge case is digits already nondecreasing: remove the final digit.'].join('\n'),
    editorialMarkdown:['## Monotonic digit stack for one deletion','', 'A larger digit immediately before a smaller following digit should be removed: keeping it makes the number larger at the earliest possible position. Scan left to right with an increasing stack and delete the first such larger digit. If no descent appears, all earlier digits are as small as possible and deleting the last digit is optimal.', '', 'The quiet mistake is removing the numerically largest digit anywhere; position matters more than magnitude in a decimal string. Do not trim leading zeroes because the requested result preserves the remaining digits. Time and space are O(n), bounded by digit count.'].join('\n'),
    referenceSolution:six('function solve(a){let s=[];for(let c of a){if(s.length&&s.at(-1)>c){s.pop();return s.join("")+a.slice(s.length+1)}s.push(c)}return a.slice(0,-1);}','function solve(a:string):string{const s:string[]=[];for(let i=0;i<a.length;i++){const c=a[i];if(s.length&&s[s.length-1]>c){s.pop();return s.join("")+a.slice(i);}s.push(c);}return a.slice(0,-1);}','def solve(a):\n    s=[]\n    for i,c in enumerate(a):\n        if s and s[-1]>c:\n            s.pop();return "".join(s)+a[i:]\n        s.append(c)\n    return a[:-1]','    static String solve(String a){StringBuilder s=new StringBuilder();for(int i=0;i<a.length();i++){char c=a.charAt(i);if(s.length()>0&&s.charAt(s.length()-1)>c){s.deleteCharAt(s.length()-1);return s.toString()+a.substring(i);}s.append(c);}return a.substring(0,a.length()-1);}','string solve(string a){string s;for(int i=0;i<(int)a.size();i++){if(!s.empty()&&s.back()>a[i]){s.pop_back();return s+a.substr(i);}s+=a[i];}return a.substr(0,a.size()-1);}','func solve(a string) string {s:="";for i:=0;i<len(a);i++{if len(s)>0&&s[len(s)-1]>a[i]{return s[:len(s)-1]+a[i:]};s+=string(a[i])};return a[:len(a)-1]}'),
    tests:[{stdin:'143221',expectedStdout:'13221',isSample:true},{stdin:'1234',expectedStdout:'123',isSample:true},{stdin:'1000',expectedStdout:'000'},{stdin:'909',expectedStdout:'09'}] }),
  p({ ...base, slug:'sum-of-subarray-minimums', title:'Add the Minimum of Every Slice', difficulty:'HARD', patternTags:['stack','monotonic-stack','contribution'], signatureId:'fn:ints->int', avgSolveSeconds:1020,
    promptMarkdown:['Return the sum of the minimum value of every non-empty contiguous slice. Values are positive and the answer fits a signed 32-bit integer.','', '**Example','','```','input: 3 1 2 4','output: 17','```','', 'The list is non-empty. Equal values must not be counted twice as the minimum of the same slice.'].join('\n'),
    editorialMarkdown:['## Monotonic boundaries and element contribution','', 'Count slices by assigning each slice to one occurrence of its minimum. For each index, find the nearest strictly smaller value on the left and the nearest smaller-or-equal value on the right. Its value is minimum for `(i-left) * (right-i)` slices, and the asymmetric comparisons make equal values belong to exactly one index.', '', 'The quiet mistake is using strict comparisons on both sides, which double-counts slices containing equal minima. The two monotonic scans each push and pop an index once. Time is O(n), and the boundary arrays plus stack use O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){let n=a.length,l=Array(n),r=Array(n),s=[];for(let i=0;i<n;i++){while(s.length&&a[s.at(-1)]>=a[i])s.pop();l[i]=s.length?s.at(-1):-1;s.push(i)}s=[];for(let i=n-1;i>=0;i--){while(s.length&&a[s.at(-1)]>a[i])s.pop();r[i]=s.length?s.at(-1):n;s.push(i)}return a.reduce((z,x,i)=>z+x*(i-l[i])*(r[i]-i),0);}','function solve(a:number[]):number{const n=a.length,l:number[]=Array(n),r:number[]=Array(n),s:number[]=[];for(let i=0;i<n;i++){while(s.length&&a[s[s.length-1]]>=a[i])s.pop();l[i]=s.length?s[s.length-1]:-1;s.push(i);}s.length=0;for(let i=n-1;i>=0;i--){while(s.length&&a[s[s.length-1]]>a[i])s.pop();r[i]=s.length?s[s.length-1]:n;s.push(i);}let z=0;for(let i=0;i<n;i++)z+=a[i]*(i-l[i])*(r[i]-i);return z;}','def solve(a):\n    n=len(a);left=[0]*n;right=[0]*n;s=[]\n    for i in range(n):\n        while s and a[s[-1]]>=a[i]:s.pop()\n        left[i]=s[-1] if s else -1;s.append(i)\n    s=[]\n    for i in range(n-1,-1,-1):\n        while s and a[s[-1]]>a[i]:s.pop()\n        right[i]=s[-1] if s else n;s.append(i)\n    return sum(a[i]*(i-left[i])*(right[i]-i) for i in range(n))','    static int solve(int[]a){int n=a.length;int[]l=new int[n],r=new int[n];java.util.ArrayList<Integer>s=new java.util.ArrayList<>();for(int i=0;i<n;i++){while(!s.isEmpty()&&a[s.get(s.size()-1)]>=a[i])s.remove(s.size()-1);l[i]=s.isEmpty()?-1:s.get(s.size()-1);s.add(i);}s.clear();for(int i=n-1;i>=0;i--){while(!s.isEmpty()&&a[s.get(s.size()-1)]>a[i])s.remove(s.size()-1);r[i]=s.isEmpty()?n:s.get(s.size()-1);s.add(i);}int z=0;for(int i=0;i<n;i++)z+=a[i]*(i-l[i])*(r[i]-i);return z;}','int solve(vector<int>a){int n=a.size(),z=0;vector<int>l(n),r(n),s;for(int i=0;i<n;i++){while(!s.empty()&&a[s.back()]>=a[i])s.pop_back();l[i]=s.empty()?-1:s.back();s.push_back(i);}s.clear();for(int i=n-1;i>=0;i--){while(!s.empty()&&a[s.back()]>a[i])s.pop_back();r[i]=s.empty()?n:s.back();s.push_back(i);}for(int i=0;i<n;i++)z+=a[i]*(i-l[i])*(r[i]-i);return z;}','func solve(a []int) int {n:=len(a);l:=make([]int,n);r:=make([]int,n);s:=[]int{};for i:=0;i<n;i++{for len(s)>0&&a[s[len(s)-1]]>=a[i]{s=s[:len(s)-1]};if len(s)==0{l[i]=-1}else{l[i]=s[len(s)-1]};s=append(s,i)};s=[]int{};for i:=n-1;i>=0;i--{for len(s)>0&&a[s[len(s)-1]]>a[i]{s=s[:len(s)-1]};if len(s)==0{r[i]=n}else{r[i]=s[len(s)-1]};s=append(s,i)};z:=0;for i:=0;i<n;i++{z+=a[i]*(i-l[i])*(r[i]-i)};return z}'),
    tests:[{stdin:'3 1 2 4',expectedStdout:'17',isSample:true},{stdin:'11 11',expectedStdout:'33',isSample:true},{stdin:'5',expectedStdout:'5'},{stdin:'3 2 1',expectedStdout:'10'}] }),
  p({ ...base, slug:'lexicographically-smallest-unique-note', title:'Smallest Note With One of Each Letter', difficulty:'HARD', patternTags:['stack','greedy','strings'], signatureId:'fn:string->string', avgSolveSeconds:960,
    promptMarkdown:['Return the lexicographically smallest string that contains each distinct lowercase letter from the input exactly once, while preserving the order of kept letters.','', '**Example','','```','input: cbacdcbc','output: acdb','```','', 'The input is non-empty lowercase letters. Repeated letters are the named edge case: only one copy may remain.'].join('\n'),
    editorialMarkdown:['## Greedy monotonic stack with last positions','', 'Record each letter’s final occurrence. While scanning, a larger chosen letter can be removed when the current letter is smaller and that larger letter appears again later; replacing it improves the earliest differing position without losing the letter. A membership set prevents taking a letter twice, and the stack preserves subsequence order.', '', 'The quiet mistake is popping a larger letter without checking it will reappear, which loses a required character. Update or inspect last positions before the scan. Each character enters and leaves the stack at most once: O(n) time and O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){let last={},s=[],used={};for(let i=0;i<a.length;i++)last[a[i]]=i;for(let i=0;i<a.length;i++){let c=a[i];if(used[c])continue;while(s.length&&s.at(-1)>c&&last[s.at(-1)]>i)delete used[s.pop()];s.push(c);used[c]=1}return s.join("");}','function solve(a:string):string{const last:any={},used:any={},s:string[]=[];for(let i=0;i<a.length;i++)last[a[i]]=i;for(let i=0;i<a.length;i++){const c=a[i];if(used[c])continue;while(s.length&&s[s.length-1]>c&&last[s[s.length-1]]>i)delete used[s.pop()!];s.push(c);used[c]=true;}return s.join("");}','def solve(a):\n    last={c:i for i,c in enumerate(a)};s=[];used=set()\n    for i,c in enumerate(a):\n        if c in used:continue\n        while s and s[-1]>c and last[s[-1]]>i:used.remove(s.pop())\n        s.append(c);used.add(c)\n    return "".join(s)','    static String solve(String a){int[]last=new int[26];for(int i=0;i<a.length();i++)last[a.charAt(i)-\'a\']=i;boolean[]used=new boolean[26];StringBuilder s=new StringBuilder();for(int i=0;i<a.length();i++){char c=a.charAt(i);if(used[c-\'a\'])continue;while(s.length()>0&&s.charAt(s.length()-1)>c&&last[s.charAt(s.length()-1)-\'a\']>i){used[s.charAt(s.length()-1)-\'a\']=false;s.deleteCharAt(s.length()-1);}s.append(c);used[c-\'a\']=true;}return s.toString();}','string solve(string a){vector<int>last(26),used(26);string s;for(int i=0;i<(int)a.size();i++)last[a[i]-\'a\']=i;for(int i=0;i<(int)a.size();i++){char c=a[i];if(used[c-\'a\'])continue;while(!s.empty()&&s.back()>c&&last[s.back()-\'a\']>i){used[s.back()-\'a\']=0;s.pop_back();}s+=c;used[c-\'a\']=1;}return s;}','func solve(a string) string {last:=make([]int,26);for i:=0;i<len(a);i++{last[a[i]-\'a\']=i};used:=make([]bool,26);s:=[]byte{};for i:=0;i<len(a);i++{c:=a[i];if used[c-\'a\']{continue};for len(s)>0&&s[len(s)-1]>c&&last[s[len(s)-1]-\'a\']>i{used[s[len(s)-1]-\'a\']=false;s=s[:len(s)-1]};s=append(s,c);used[c-\'a\']=true};return string(s)}'),
    tests:[{stdin:'cbacdcbc',expectedStdout:'acdb',isSample:true},{stdin:'bcabc',expectedStdout:'abc',isSample:true},{stdin:'aaaa',expectedStdout:'a'},{stdin:'dcba',expectedStdout:'dcba'}] }),
  p({ ...base, slug:'typed-text-with-undo-redo', title:'Tiny Editor Undo and Redo', difficulty:'HARD', patternTags:['stack','simulation','undo-redo'], signatureId:'fn:string->string', avgSolveSeconds:960,
    promptMarkdown:['Process a command line: lowercase letters type themselves, `-` undoes the most recent typed letter, and `+` redoes the most recently undone letter. A new typed letter clears redo history. Return the final text.','', '**Example','','```','input: abc--+d+','output: abd','```','', 'Undo or redo with no available letter does nothing. The named edge case is typing after undo, which makes an old redo unavailable.'].join('\n'),
    editorialMarkdown:['## Two stacks model reversible edits','', 'The current text acts as an undo stack and a second stack holds characters removed by undo. Undo moves one character from text to redo; redo moves one back. Typing appends a fresh character and clears redo because the old branch no longer follows the current editing history.', '', 'The quiet mistake is retaining redo after a new letter, which lets the editor resurrect text from a discarded future. Treat empty undo and redo stacks as no-ops. Each command performs O(1) work, so time is O(n) and both stacks together use O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){let u=[],r=[];for(let c of a){if(c==="-"){if(u.length)r.push(u.pop())}else if(c==="+"){if(r.length)u.push(r.pop())}else{u.push(c);r=[]}}return u.join("");}','function solve(a:string):string{const u:string[]=[],r:string[]=[];for(const c of a){if(c==="-"){if(u.length)r.push(u.pop()!);}else if(c==="+"){if(r.length)u.push(r.pop()!);}else{u.push(c);r.length=0;}}return u.join("");}','def solve(a):\n    undo=[];redo=[]\n    for c in a:\n        if c=="-":\n            if undo:redo.append(undo.pop())\n        elif c=="+":\n            if redo:undo.append(redo.pop())\n        else:undo.append(c);redo=[]\n    return "".join(undo)','    static String solve(String a){StringBuilder u=new StringBuilder();java.util.ArrayList<Character>r=new java.util.ArrayList<>();for(char c:a.toCharArray()){if(c==\'-\'){if(u.length()>0){r.add(u.charAt(u.length()-1));u.deleteCharAt(u.length()-1);}}else if(c==\'+\'){if(!r.isEmpty())u.append(r.remove(r.size()-1));}else{u.append(c);r.clear();}}return u.toString();}','string solve(string a){string u,r;for(char c:a){if(c==\'-\'){if(!u.empty()){r+=u.back();u.pop_back();}}else if(c==\'+\'){if(!r.empty()){u+=r.back();r.pop_back();}}else{u+=c;r="";}}return u;}','func solve(a string) string {u:=[]byte{};r:=[]byte{};for i:=0;i<len(a);i++{c:=a[i];if c==\'-\'{if len(u)>0{r=append(r,u[len(u)-1]);u=u[:len(u)-1]}}else if c==\'+\'{if len(r)>0{u=append(u,r[len(r)-1]);r=r[:len(r)-1]}}else{u=append(u,c);r=[]byte{}}};return string(u)}'),
    tests:[{stdin:'abc--+d+',expectedStdout:'abd',isSample:true},{stdin:'a-+',expectedStdout:'a',isSample:true},{stdin:'ab-c+',expectedStdout:'ac'},{stdin:'--++',expectedStdout:''}] }),
];
