import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-035` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_035_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "match-scanned-prefixes",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Match Scanned Prefixes",
    patternTags: ["string","prefix","counting","arrays"],
    signatureId: "fn:strings->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A security system logs a master code followed by a series of scanned fragments. You are given an array of strings `records`, where the first element `records[0]` is the master code, and the remaining elements are scanned fragments.\n\nYour task is to count how many of the scanned fragments are exact prefixes of the master code. A string `A` is a prefix of string `B` if `B` starts with `A`.\n\n**Constraints**\n- `2 <= records.length <= 40`\n- `1 <= records[i].length <= 20`\n- All strings consist of lowercase English letters.\n\n**Example 1**\n```\ninput:\nsecurity sec curity s secu\noutput: 3\n```\n*Explanation: The master code is \"security\". The fragments \"sec\", \"s\", and \"secu\" are prefixes. \"curity\" is not.*\n\n**Example 2**\n```\ninput:\nrobot robot rob r\noutput: 3\n```\n*Explanation: \"robot\", \"rob\", and \"r\" are all prefixes of \"robot\".*\n\n**Example 3**\n```\ninput:\nalfa beta gamma\noutput: 0\n```\n*Explanation: Neither \"beta\" nor \"gamma\" is a prefix of \"alfa\".*\n\n**Follow-up:** Can you solve this with zero additional allocations per fragment?",
    editorialMarkdown: "## Match Scanned Prefixes\nTo solve this, we can isolate the first element of the array as our target master code. We then loop through the rest of the array, checking if each string is a prefix of the master code. In most languages, there is a built-in method like `startsWith()` or we can manually check character by character. We maintain a counter for every successful check.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N \\times M)\\), where \\(N\\) is the number of fragments and \\(M\\) is the maximum length of a fragment. \n- **Space Complexity:** \\(\\mathcal{O}(1)\\) assuming the prefix check operates on the strings in-place.\n\n**Common Trap:**\nForgetting that the master code itself is part of the array, so the loop for fragments must start at index 1 instead of index 0. If you start at index 0, you will incorrectly count the master code as a prefix of itself!",
    referenceSolution: {
      JAVASCRIPT: "function solve(records) { let c = 0; for(let i=1; i<records.length; i++) if(records[0].startsWith(records[i])) c++; return c; }",
      TYPESCRIPT: "function solve(records: string[]): number { let c = 0; for(let i=1; i<records.length; i++) if(records[0].startsWith(records[i])) c++; return c; }",
      PYTHON: "def solve(records):\n    return sum(1 for i in range(1, len(records)) if records[0].startswith(records[i]))",
      JAVA: "    static int solve(String[] records) { int c = 0; for (int i = 1; i < records.length; i++) { if (records[0].startsWith(records[i])) c++; } return c; }",
      CPP: "int solve(vector<string> records) { int c = 0; for (int i = 1; i < records.size(); i++) { if (records[0].find(records[i]) == 0) c++; } return c; }",
      GO: "func solve(records []string) int { c := 0; for i := 1; i < len(records); i++ { if strings.HasPrefix(records[0], records[i]) { c++ } }; return c }",
    },
    tests: [
      { stdin: "security sec curity s secu", expectedStdout: "3", isSample: true },
      { stdin: "robot robot rob r", expectedStdout: "3", isSample: true },
      { stdin: "alfa beta gamma", expectedStdout: "0" },
      { stdin: "a a a", expectedStdout: "2" },
      { stdin: "hello h he hel hell hello hellop", expectedStdout: "5" },
      { stdin: "x x y z", expectedStdout: "1" },
      { stdin: "abcde a ab abc abcd abcde abcdef", expectedStdout: "5" },
      { stdin: "foo bar", expectedStdout: "0" },
    ],
  }),

  p({
    ...base,
    slug: "surveyor-triangle-plots",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Quantum Resonance Configurations",
    patternTags: ["math","brute-force","counting","geometry"],
    signatureId: "fn:int->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A scientist is calibrating a quantum resonance chamber using three distinct crystal oscillators. Each crystal operates at a specific frequency, but no frequency can exceed a specified `limit`.\n\nThe chamber achieves resonance if a combination of three frequencies `(f1, f2, f3)` satisfies the relationship `f1^2 + f2^2 = f3^2`, where `1 <= f1, f2, f3 <= limit`.\n\nSince the first two crystals are placed in distinct left and right slots, the order matters. For example, a configuration of `(f1, f2, f3)` is considered different from `(f2, f1, f3)` as long as `f1 != f2`.\n\nCalculate the total number of valid frequency combinations that produce resonance.\n\n**Constraints**\n- `1 <= limit <= 100`\n\n**Example 1**\n```\ninput:\n5\noutput: 2\n```\n*Explanation: The valid resonating configurations are (3, 4, 5) and (4, 3, 5).*\n\n**Example 2**\n```\ninput:\n10\noutput: 4\n```\n*Explanation: The configurations achieving resonance are (3, 4, 5), (4, 3, 5), (6, 8, 10), and (8, 6, 10).*\n\n**Example 3**\n```\ninput:\n4\noutput: 0\n```\n*Explanation: No valid configurations can be formed with frequencies bounded by 4.*\n\n**Follow-up:** Can you reduce the runtime by avoiding the innermost third loop entirely?",
    editorialMarkdown: "## Surveyor Triangle Plots\nThe problem requires us to find triples `(x, y, z)` that satisfy the Pythagorean theorem `x^2 + y^2 = z^2` within a specified `limit`. With `limit <= 100`, a straightforward triple-nested loop generating all `(x, y, z)` combinations checking the condition is perfectly acceptable and runs well within execution time.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(L^3)\\) where \\(L\\) is the `limit`, if implemented naively. This could be optimized to \\(\\mathcal{O}(L^2)\\) by computing \\(z\\) from \\(x\\) and \\(y\\) and checking if it's an integer within bounds.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) as we only keep a running count.\n\n**Common Trap:**\nA common mistake is deduplicating `(x, y, z)` and `(y, x, z)`. The problem explicitly states they should be counted as distinct plots, which matches what a naive nested loop implementation naturally does!",
    referenceSolution: {
      JAVASCRIPT: "function solve(limit) { let c = 0; for(let x=1; x<=limit; x++) for(let y=1; y<=limit; y++) for(let z=1; z<=limit; z++) if(x*x + y*y === z*z) c++; return c; }",
      TYPESCRIPT: "function solve(limit: number): number { let c = 0; for(let x=1; x<=limit; x++) for(let y=1; y<=limit; y++) for(let z=1; z<=limit; z++) if(x*x + y*y === z*z) c++; return c; }",
      PYTHON: "def solve(limit):\n    c = 0\n    for x in range(1, limit+1):\n        for y in range(1, limit+1):\n            for z in range(1, limit+1):\n                if x*x + y*y == z*z:\n                    c += 1\n    return c",
      JAVA: "    static int solve(int limit) { int c = 0; for(int x=1; x<=limit; x++) for(int y=1; y<=limit; y++) for(int z=1; z<=limit; z++) if(x*x + y*y == z*z) c++; return c; }",
      CPP: "int solve(int limit) { int c = 0; for(int x=1; x<=limit; x++) for(int y=1; y<=limit; y++) for(int z=1; z<=limit; z++) if(x*x + y*y == z*z) c++; return c; }",
      GO: "func solve(limit int) int { c := 0; for x:=1; x<=limit; x++ { for y:=1; y<=limit; y++ { for z:=1; z<=limit; z++ { if x*x + y*y == z*z { c++ } } } }; return c }",
    },
    tests: [
      { stdin: "5", expectedStdout: "2", isSample: true },
      { stdin: "10", expectedStdout: "4", isSample: true },
      { stdin: "4", expectedStdout: "0" },
      { stdin: "1", expectedStdout: "0" },
      { stdin: "13", expectedStdout: "6" },
      { stdin: "15", expectedStdout: "8" },
      { stdin: "20", expectedStdout: "12" },
      { stdin: "100", expectedStdout: "104" },
    ],
  }),

  p({
    ...base,
    slug: "balanced-terrain-segments",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "SLIDING_WINDOW",
    title: "Balanced Terrain Segments",
    patternTags: ["array","sliding-window","counting","math"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A geological survey team has recorded elevation data in an array `elevations`. They are searching for specific local terrain formations known as \"balanced segments\".\n\nA balanced segment is any contiguous sub-sequence of exactly three elevations where the sum of the first and third elevations equals exactly twice the second elevation (i.e. they form an arithmetic progression).\n\nCount the total number of balanced segments in the array.\n\n**Constraints**\n- `3 <= elevations.length <= 1000`\n- `1 <= elevations[i] <= 1000`\n\n**Example 1**\n```\ninput:\n2 4 6 5 4\noutput: 2\n```\n*Explanation: The subarrays of length 3 are:*\n- *[2, 4, 6]: 2 + 6 = 8, which is 2 * 4. (Balanced)*\n- *[4, 6, 5]: 4 + 5 = 9, which is not 2 * 6. (Not balanced)*\n- *[6, 5, 4]: 6 + 4 = 10, which is 2 * 5. (Balanced)*\n*Total balanced segments: 2.*\n\n**Example 2**\n```\ninput:\n1 1 1\noutput: 1\n```\n*Explanation: [1, 1, 1] is balanced because 1 + 1 = 2 * 1.*\n\n**Example 3**\n```\ninput:\n1 2 4 8\noutput: 0\n```\n*Explanation: None of the length-3 subarrays ([1,2,4] and [2,4,8]) meet the condition.*\n\n**Follow-up:** Can you solve this with a single pass through the array?",
    editorialMarkdown: "## Balanced Terrain Segments\nWe are tasked with counting subarrays of length 3 that satisfy a specific arithmetic condition. This is a fixed-window problem where the window size is always 3. We can iterate over the array with a loop variable `i` acting as the start of our window, going up to `len - 2`. For each window, we evaluate the condition `elevations[i] + elevations[i+2] == 2 * elevations[i+1]` and increment a counter if it holds.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N)\\), where \\(N\\) is the length of the array, since we check each window of size 3 in a single pass.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) as we only store a counter.\n\n**Common Trap:**\nWatch out for index out of bounds errors. The loop condition must stop at `length - 3` (inclusive), or equivalently, `i < length - 2`. Additionally, beware of modifying the loop step; you must shift the window one element at a time (e.g., `i++`), not by 3.",
    referenceSolution: {
      JAVASCRIPT: "function solve(e) { let c = 0; for(let i=0; i<e.length-2; i++) { if(e[i] + e[i+2] === 2 * e[i+1]) c++; } return c; }",
      TYPESCRIPT: "function solve(e: number[]): number { let c = 0; for(let i=0; i<e.length-2; i++) { if(e[i] + e[i+2] === 2 * e[i+1]) c++; } return c; }",
      PYTHON: "def solve(e):\n    c = 0\n    for i in range(len(e) - 2):\n        if e[i] + e[i+2] == 2 * e[i+1]:\n            c += 1\n    return c",
      JAVA: "    static int solve(int[] e) { int c = 0; for(int i=0; i<e.length-2; i++) { if(e[i] + e[i+2] == 2 * e[i+1]) c++; } return c; }",
      CPP: "int solve(vector<int> e) { int c = 0; int n = e.size(); for(int i=0; i<n-2; i++) { if(e[i] + e[i+2] == 2 * e[i+1]) c++; } return c; }",
      GO: "func solve(e []int) int { c := 0; n := len(e); for i:=0; i<n-2; i++ { if e[i] + e[i+2] == 2 * e[i+1] { c++ } }; return c }",
    },
    tests: [
      { stdin: "2 4 6 5 4", expectedStdout: "2", isSample: true },
      { stdin: "1 1 1", expectedStdout: "1", isSample: true },
      { stdin: "1 2 4 8", expectedStdout: "0" },
      { stdin: "10 20 30 40 50", expectedStdout: "3" },
      { stdin: "5 5 5 5 5", expectedStdout: "3" },
      { stdin: "1 3 5 4 3", expectedStdout: "2" },
      { stdin: "100 200 300", expectedStdout: "1" },
      { stdin: "9 6 3 0", expectedStdout: "2" },
    ],
  }),

  p({
    ...base,
    slug: "stable-cargo-loads",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Stable Cargo Loads",
    patternTags: ["array","prefix-sum","counting","modulo"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A cargo ship is sequentially loaded with containers. The array `weights` represents the weight of each container being placed on the ship, in order. The ship has a stability factor represented by an integer `m`.\n\nThe ship is considered \"stable\" at a given moment if the total weight of all containers loaded so far is a perfect multiple of `m`. \n\nCount how many times the ship is stable during the loading process. In other words, return the number of prefixes of the `weights` array whose sum is evenly divisible by `m`.\n\n**Constraints**\n- `1 <= weights.length <= 100`\n- `1 <= weights[i] <= 1000`\n- `1 <= m <= 10000`\n\n**Example 1**\n```\ninput:\n1 2 3 4\n3\noutput: 2\n```\n*Explanation: The running sums are:*\n- *1 (not divisible by 3)*\n- *1 + 2 = 3 (divisible by 3)*\n- *3 + 3 = 6 (divisible by 3)*\n- *6 + 4 = 10 (not divisible by 3)*\n*Total stable moments: 2.*\n\n**Example 2**\n```\ninput:\n5 5 5\n5\noutput: 3\n```\n*Explanation: Sums are 5, 10, 15. All 3 are divisible by 5.*\n\n**Example 3**\n```\ninput:\n2 4 6\n10\noutput: 0\n```\n*Explanation: Sums are 2, 6, 12. None are divisible by 10.*\n\n**Follow-up:** Can you accomplish this efficiently in a single pass?",
    editorialMarkdown: "## Stable Cargo Loads\nThis problem asks us to evaluate the running sum of an array and count how many times that sum is a multiple of `m`. We can iterate through the array, maintaining a variable for the total weight accumulated so far. After adding the current element's weight to our running sum, we check if `sum % m == 0`. If it is, we increment our counter.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N)\\) where \\(N\\) is the number of containers. We process each weight exactly once.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) as we only need two variables: one for the running sum and one for the counter.\n\n**Common Trap:**\nA common inefficiency is recalculating the sum from the beginning of the array for every prefix (an \\(\\mathcal{O}(N^2)\\) approach). While this passes small constraints, using a running accumulator is the optimal and intended pattern.",
    referenceSolution: {
      JAVASCRIPT: "function solve(weights, m) { let c=0, sum=0; for(let w of weights) { sum += w; if(sum % m === 0) c++; } return c; }",
      TYPESCRIPT: "function solve(weights: number[], m: number): number { let c=0, sum=0; for(let w of weights) { sum += w; if(sum % m === 0) c++; } return c; }",
      PYTHON: "def solve(weights, m):\n    c = 0\n    s = 0\n    for w in weights:\n        s += w\n        if s % m == 0:\n            c += 1\n    return c",
      JAVA: "    static int solve(int[] weights, int m) { int c = 0, sum = 0; for(int w : weights) { sum += w; if(sum % m == 0) c++; } return c; }",
      CPP: "int solve(vector<int> weights, int m) { int c = 0, sum = 0; for(int w : weights) { sum += w; if(sum % m == 0) c++; } return c; }",
      GO: "func solve(weights []int, m int) int { c, sum := 0, 0; for _, w := range weights { sum += w; if sum % m == 0 { c++ } }; return c }",
    },
    tests: [
      { stdin: "1 2 3 4\n3", expectedStdout: "2", isSample: true },
      { stdin: "5 5 5\n5", expectedStdout: "3", isSample: true },
      { stdin: "2 4 6\n10", expectedStdout: "0" },
      { stdin: "10\n2", expectedStdout: "1" },
      { stdin: "3 3 3 3\n4", expectedStdout: "1" },
      { stdin: "10 20 30 40 50\n10", expectedStdout: "5" },
      { stdin: "1\n1", expectedStdout: "1" },
      { stdin: "100 200 300\n150", expectedStdout: "2" },
    ],
  }),

  p({
    ...base,
    slug: "count-suitable-light-rotations",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Count Suitable Light Rotations",
    patternTags: ["string","rotation","counting","simulation"],
    signatureId: "fn:string,int->int",
    avgSolveSeconds: 400,
    promptMarkdown: "A sequence of decorative lights is represented by a string `s`, where each character denotes a light's color. You can rotate the string by taking characters from the front and moving them to the back. A rotation is defined by the number of characters moved, from `0` to `s.length - 1`.\n\nYou want to find a layout where exactly `k` pairs of *adjacent* lights have the same color. (Do not wrap around to check the first and last lights; only check indices `i` and `i+1`).\n\nCount how many of the possible rotations of `s` contain exactly `k` adjacent equal pairs.\n\n**Constraints**\n- `2 <= s.length <= 100`\n- `0 <= k <= s.length`\n- `s` consists of lowercase English letters.\n\n**Example 1**\n```\ninput:\naaba\n1\noutput: 2\n```\n*Explanation: The 4 rotations are:*\n- *\"aaba\" (1 pair: \"aa\" at index 0)*\n- *\"abaa\" (1 pair: \"aa\" at index 2)*\n- *\"baaa\" (2 pairs: \"aa\" at index 1 and index 2)*\n- *\"aaab\" (2 pairs: \"aa\" at index 0 and index 1)*\n*Exactly 2 rotations have 1 pair.*\n\n**Example 2**\n```\ninput:\nabc\n0\noutput: 3\n```\n*Explanation: Rotations \"abc\", \"bca\", \"cab\" all have 0 equal adjacent pairs.*\n\n**Example 3**\n```\ninput:\nzzzz\n2\noutput: 0\n```\n*Explanation: All 4 rotations are \"zzzz\", which has 3 equal adjacent pairs. None have exactly 2.*\n\n**Follow-up:** Can you optimize this to avoid creating a new string for each rotation?",
    editorialMarkdown: "## Count Suitable Light Rotations\nWe need to evaluate all possible rotations of the given string and count how many have exactly `k` identical adjacent characters. Since the string length is small (up to 100), we can literally simulate the process: generate each of the `N` rotations using string slicing, and then perform a linear scan on the rotated string to count matching pairs `s[i] == s[i+1]`.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N^2)\\) to generate \\(N\\) rotations and scan each of length \\(N\\). \n- **Space Complexity:** \\(\\mathcal{O}(N)\\) to store the currently rotated string during the iteration.\n\n**Common Trap:**\nAssuming that \"adjacent\" wraps around the ends of the string. The problem specifies checking only strict adjacency (`i` and `i+1`). If you wrap around, the number of adjacent pairs becomes an invariant across all rotations!",
    referenceSolution: {
      JAVASCRIPT: "function solve(s, k) { let c = 0, n = s.length; for(let i=0; i<n; i++) { let rot = s.slice(i) + s.slice(0, i); let p = 0; for(let j=0; j<n-1; j++) { if(rot[j] === rot[j+1]) p++; } if(p === k) c++; } return c; }",
      TYPESCRIPT: "function solve(s: string, k: number): number { let c = 0, n = s.length; for(let i=0; i<n; i++) { let rot = s.slice(i) + s.slice(0, i); let p = 0; for(let j=0; j<n-1; j++) { if(rot[j] === rot[j+1]) p++; } if(p === k) c++; } return c; }",
      PYTHON: "def solve(s, k):\n    c = 0\n    n = len(s)\n    for i in range(n):\n        rot = s[i:] + s[:i]\n        p = 0\n        for j in range(n-1):\n            if rot[j] == rot[j+1]:\n                p += 1\n        if p == k:\n            c += 1\n    return c",
      JAVA: "    static int solve(String s, int k) { int c = 0, n = s.length(); for(int i=0; i<n; i++) { String rot = s.substring(i) + s.substring(0, i); int p = 0; for(int j=0; j<n-1; j++) { if(rot.charAt(j) == rot.charAt(j+1)) p++; } if(p == k) c++; } return c; }",
      CPP: "int solve(string s, int k) { int c = 0, n = s.length(); for(int i=0; i<n; i++) { string rot = s.substr(i) + s.substr(0, i); int p = 0; for(int j=0; j<n-1; j++) { if(rot[j] == rot[j+1]) p++; } if(p == k) c++; } return c; }",
      GO: "func solve(s string, k int) int { c := 0; n := len(s); for i := 0; i < n; i++ { rot := s[i:] + s[:i]; p := 0; for j := 0; j < n-1; j++ { if rot[j] == rot[j+1] { p++ } }; if p == k { c++ } }; return c }",
    },
    tests: [
      { stdin: "aaba\n1", expectedStdout: "2", isSample: true },
      { stdin: "abc\n0", expectedStdout: "3", isSample: true },
      { stdin: "zzzz\n2", expectedStdout: "0" },
      { stdin: "xx\n1", expectedStdout: "2" },
      { stdin: "ababa\n0", expectedStdout: "1" },
      { stdin: "aabbaa\n2", expectedStdout: "0" },
      { stdin: "aa\n0", expectedStdout: "0" },
      { stdin: "xxyyxy\n2", expectedStdout: "4" },
    ],
  }),

  p({
    ...base,
    slug: "ancient-pillar-alignments",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Thermal Anomaly Detection",
    patternTags: ["array","brute-force","counting","loops"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 360,
    promptMarkdown: "A meteorologist is analyzing atmospheric pressure readings taken at sequentially higher altitudes, provided as an array of integers `readings`.\n\nA \"thermal anomaly\" is detected when four readings from lower to higher altitudes (at indices `a < b < c < d`) have the property that the sum of the first three pressure values exactly equals the fourth pressure value.\n\nDetermine the total number of such anomalies present in the sequence.\n\n**Constraints**\n- `4 <= readings.length <= 50`\n- `1 <= readings[i] <= 100`\n\n**Example 1**\n```\ninput:\n1 2 3 6\noutput: 1\n```\n*Explanation: A single anomaly exists at indices (0, 1, 2, 3) because 1 + 2 + 3 = 6.*\n\n**Example 2**\n```\ninput:\n3 3 6 4 5\noutput: 0\n```\n*Explanation: There are no combinations where the first three pressures sum to the fourth.*\n\n**Example 3**\n```\ninput:\n1 1 1 3 5\noutput: 4\n```\n*Explanation: The anomalies are located at indices (0, 1, 2, 3) since 1+1+1=3, and at (0, 1, 3, 4), (0, 2, 3, 4), and (1, 2, 3, 4) since 1+1+3=5.*\n\n**Follow-up:** Can you optimize the counting process to be faster than \\(\\mathcal{O}(N^4)\\)?",
    editorialMarkdown: "## Ancient Pillar Alignments\nWe are looking for combinations of four indices `a < b < c < d` such that `pillars[a] + pillars[b] + pillars[c] == pillars[d]`. Because the maximum array length is 50, a naive brute-force approach using four nested loops will execute at most \\( \\approx 2.3 \\times 10^5 \\) times, which comfortably runs well within the time limit. \n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N^4)\\) for the four nested loops, where \\(N\\) is the number of pillars.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) since we only track a single integer counter.\n\n**Common Trap:**\nA common error is to iterate combinations without respecting the strict index ordering `a < b < c < d`, or by reusing the same index twice. Ensure the loops depend on each other (e.g. `b` starts at `a + 1`).",
    referenceSolution: {
      JAVASCRIPT: "function solve(p) { let c = 0, n = p.length; for(let i=0; i<n; i++) for(let j=i+1; j<n; j++) for(let k=j+1; k<n; k++) for(let l=k+1; l<n; l++) if(p[i]+p[j]+p[k]===p[l]) c++; return c; }",
      TYPESCRIPT: "function solve(p: number[]): number { let c = 0, n = p.length; for(let i=0; i<n; i++) for(let j=i+1; j<n; j++) for(let k=j+1; k<n; k++) for(let l=k+1; l<n; l++) if(p[i]+p[j]+p[k]===p[l]) c++; return c; }",
      PYTHON: "def solve(p):\n    c = 0\n    n = len(p)\n    for i in range(n):\n        for j in range(i+1, n):\n            for k in range(j+1, n):\n                for l in range(k+1, n):\n                    if p[i]+p[j]+p[k] == p[l]:\n                        c += 1\n    return c",
      JAVA: "    static int solve(int[] p) { int c = 0, n = p.length; for(int i=0; i<n; i++) for(int j=i+1; j<n; j++) for(int k=j+1; k<n; k++) for(int l=k+1; l<n; l++) if(p[i]+p[j]+p[k]==p[l]) c++; return c; }",
      CPP: "int solve(vector<int> p) { int c = 0, n = p.size(); for(int i=0; i<n; i++) for(int j=i+1; j<n; j++) for(int k=j+1; k<n; k++) for(int l=k+1; l<n; l++) if(p[i]+p[j]+p[k]==p[l]) c++; return c; }",
      GO: "func solve(p []int) int { c := 0; n := len(p); for i:=0; i<n; i++ { for j:=i+1; j<n; j++ { for k:=j+1; k<n; k++ { for l:=k+1; l<n; l++ { if p[i]+p[j]+p[k]==p[l] { c++ } } } } }; return c }",
    },
    tests: [
      { stdin: "1 2 3 6", expectedStdout: "1", isSample: true },
      { stdin: "3 3 6 4 5", expectedStdout: "0", isSample: true },
      { stdin: "1 1 1 3 5", expectedStdout: "4", isSample: true },
      { stdin: "1 1 1 3 3", expectedStdout: "2" },
      { stdin: "1 2 3 6 6", expectedStdout: "2" },
      { stdin: "10 20 30 60", expectedStdout: "1" },
      { stdin: "1 1 1 1", expectedStdout: "0" },
      { stdin: "2 3 4 9 10", expectedStdout: "1" },
    ],
  }),
];
