import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-023` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_023_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "verify-parallel-schematics",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Rune Forging",
    patternTags: ["string","parity","grouping"],
    signatureId: "fn:string,string->bool",
    avgSolveSeconds: 600,
    promptMarkdown: "You are a blacksmith embedding magic runes into a sword. The sword has exactly 4 rune slots, represented by two strings `a` and `b` containing lowercase English letters.\n\nYou can swap any rune at position `i` with the rune at position `i + 2` in string `a` an unlimited number of times.\n\nReturn `true` if you can transform the sequence of runes `a` to exactly match the target sequence `b`, and `false` otherwise.\n\n**Constraints**\n- `a.length == b.length == 4`\n- `a` and `b` consist only of lowercase English letters.\n\n**Example 1**\n```\ninput:\nabcd\ncdab\noutput: true\n```\nExplanation: We can swap `a[0]` ('a') with `a[2]` ('c') to get \"cbad\". Then we swap `a[1]` ('b') with `a[3]` ('d') to obtain \"cdab\", which matches `b`.\n\n**Example 2**\n```\ninput:\nwxyz\nxwzy\noutput: false\n```\nExplanation: We cannot swap 'w' and 'x' because they are at indices 0 and 1, meaning their distance is 1 instead of 2. We cannot transform `a` to match `b`.\n\n**Example 3**\n```\ninput:\nffff\nffff\noutput: true\n```\nExplanation: The sequences are already identical.\n\n**Follow-up:** How would your approach scale if the strings were of length N?",
    editorialMarkdown: "## Checking Independent Multisets\n\nSince the strings are exactly 4 characters long and we can only swap characters that are separated by a distance of 2, the characters at even indices (0 and 2) can only be swapped with each other, and the characters at odd indices (1 and 3) can only be swapped with each other.\n\nThis means we can consider the problem as checking two independent sets of characters. The characters at even indices in `a` must match the characters at even indices in `b` (in any order). The same applies to the odd indices. We can directly check all valid configurations since there are only 2 elements in each set.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(1) since the strings are always of length 4.\n- **Space Complexity:** mathcal{O}(1) as no extra scaling space is needed.\n\n**Common Trap:**\nTrying to simulate the swaps instead of recognizing that indices 0 and 2 form an independent multiset from indices 1 and 3.",
    referenceSolution: {
      JAVASCRIPT: "function solve(a, b) {\n    let evenMatch = (a[0] === b[0] && a[2] === b[2]) || (a[0] === b[2] && a[2] === b[0]);\n    let oddMatch = (a[1] === b[1] && a[3] === b[3]) || (a[1] === b[3] && a[3] === b[1]);\n    return evenMatch && oddMatch;\n}",
      TYPESCRIPT: "function solve(a: string, b: string): boolean {\n    let evenMatch = (a[0] === b[0] && a[2] === b[2]) || (a[0] === b[2] && a[2] === b[0]);\n    let oddMatch = (a[1] === b[1] && a[3] === b[3]) || (a[1] === b[3] && a[3] === b[1]);\n    return evenMatch && oddMatch;\n}",
      PYTHON: "def solve(a, b):\n    even_match = (a[0] == b[0] and a[2] == b[2]) or (a[0] == b[2] and a[2] == b[0])\n    odd_match = (a[1] == b[1] and a[3] == b[3]) or (a[1] == b[3] and a[3] == b[1])\n    return even_match and odd_match",
      JAVA: "    static boolean solve(String a, String b) {\n        boolean evenMatch = (a.charAt(0) == b.charAt(0) && a.charAt(2) == b.charAt(2)) || (a.charAt(0) == b.charAt(2) && a.charAt(2) == b.charAt(0));\n        boolean oddMatch = (a.charAt(1) == b.charAt(1) && a.charAt(3) == b.charAt(3)) || (a.charAt(1) == b.charAt(3) && a.charAt(3) == b.charAt(1));\n        return evenMatch && oddMatch;\n    }",
      CPP: "#include <string>\n\nbool solve(std::string a, std::string b) {\n    bool evenMatch = (a[0] == b[0] && a[2] == b[2]) || (a[0] == b[2] && a[2] == b[0]);\n    bool oddMatch = (a[1] == b[1] && a[3] == b[3]) || (a[1] == b[3] && a[3] == b[1]);\n    return evenMatch && oddMatch;\n}",
      GO: "func solve(a string, b string) bool {\n    evenMatch := (a[0] == b[0] && a[2] == b[2]) || (a[0] == b[2] && a[2] == b[0])\n    oddMatch := (a[1] == b[1] && a[3] == b[3]) || (a[1] == b[3] && a[3] == b[1])\n    return evenMatch && oddMatch\n}",
    },
    tests: [
      { stdin: "abcd\ncdab", expectedStdout: "true", isSample: true },
      { stdin: "wxyz\nxwzy", expectedStdout: "false", isSample: true },
      { stdin: "ffff\nffff", expectedStdout: "true" },
      { stdin: "abcd\nbcda", expectedStdout: "false" },
      { stdin: "zzzz\nzzzz", expectedStdout: "true" },
      { stdin: "qwer\nreqw", expectedStdout: "false" },
      { stdin: "qwer\newqr", expectedStdout: "true" },
      { stdin: "mnop\nonmp", expectedStdout: "true" },
    ],
  }),

  p({
    ...base,
    slug: "all-frequencies-active",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Activate All Beacons",
    patternTags: ["hash-set","string","counting"],
    signatureId: "fn:string->bool",
    avgSolveSeconds: 480,
    promptMarkdown: "You are given a string `signals` representing light emissions from different planetary beacons. Each lowercase English letter corresponds to a unique beacon color.\n\nA successful galaxy-wide transmission requires all 26 beacon colors ('a' through 'z') to flash at least once during the sequence.\n\nReturn `true` if `signals` contains all 26 beacon colors, meaning the transmission is successful. Otherwise, return `false`.\n\n**Constraints**\n- `1 <= signals.length <= 1000`\n- `signals` consists of lowercase English letters.\n\n**Example 1**\n```\ninput:\nthequickbrownfoxjumpsoverthelazydog\noutput: true\n```\nExplanation: Every lowercase English letter from 'a' to 'z' appears in the sequence at least once.\n\n**Example 2**\n```\ninput:\nhelloworld\noutput: false\n```\nExplanation: The sequence is missing several colors, including 'a', 'b', and 'c'.\n\n**Example 3**\n```\ninput:\nabcdefghijklmnopqrstuvwxyz\noutput: true\n```\nExplanation: All 26 beacon colors flash exactly once.\n\n**Follow-up:** Can you optimize this to stop iterating as soon as all 26 bands are found?",
    editorialMarkdown: "## Frequency Array or Hash Set\n\nThe core technique is to verify whether all 26 lowercase English letters appear at least once in the string. We can iterate over every character in the string and mark it as seen in a boolean array, hash set, or integer bitmask. After fully processing the string, if the number of unique characters found is exactly 26, we return true.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the length of `signals`, as we examine each character exactly once.\n- **Space Complexity:** mathcal{O}(1) because the maximum number of unique lowercase letters is capped at 26, so our set or array uses constant space.\n\n**Common Trap:**\nA common trap is creating a frequency map and iterating over the string instead of short-circuiting. An optimal approach could break early as soon as 26 unique characters are seen.",
    referenceSolution: {
      JAVASCRIPT: "function solve(signals) {\n    let seen = new Array(26).fill(false);\n    let uniqueCount = 0;\n    for (let i = 0; i < signals.length; i++) {\n        let idx = signals.charCodeAt(i) - 97;\n        if (!seen[idx]) {\n            seen[idx] = true;\n            uniqueCount++;\n            if (uniqueCount === 26) return true;\n        }\n    }\n    return uniqueCount === 26;\n}",
      TYPESCRIPT: "function solve(signals: string): boolean {\n    let seen = new Array(26).fill(false);\n    let uniqueCount = 0;\n    for (let i = 0; i < signals.length; i++) {\n        let idx = signals.charCodeAt(i) - 97;\n        if (!seen[idx]) {\n            seen[idx] = true;\n            uniqueCount++;\n            if (uniqueCount === 26) return true;\n        }\n    }\n    return uniqueCount === 26;\n}",
      PYTHON: "def solve(signals):\n    seen = set()\n    for c in signals:\n        seen.add(c)\n        if len(seen) == 26:\n            return True\n    return len(seen) == 26",
      JAVA: "    static boolean solve(String signals) {\n        boolean[] seen = new boolean[26];\n        int uniqueCount = 0;\n        for (int i = 0; i < signals.length(); i++) {\n            int idx = signals.charAt(i) - 'a';\n            if (!seen[idx]) {\n                seen[idx] = true;\n                uniqueCount++;\n                if (uniqueCount == 26) return true;\n            }\n        }\n        return uniqueCount == 26;\n    }",
      CPP: "#include <string>\n#include <vector>\n\nbool solve(std::string signals) {\n    std::vector<bool> seen(26, false);\n    int uniqueCount = 0;\n    for (char c : signals) {\n        if (!seen[c - 'a']) {\n            seen[c - 'a'] = true;\n            uniqueCount++;\n            if (uniqueCount == 26) return true;\n        }\n    }\n    return uniqueCount == 26;\n}",
      GO: "func solve(signals string) bool {\n    seen := make([]bool, 26)\n    uniqueCount := 0\n    for _, c := range signals {\n        if !seen[c-'a'] {\n            seen[c-'a'] = true\n            uniqueCount++\n            if uniqueCount == 26 {\n                return true\n            }\n        }\n    }\n    return uniqueCount == 26\n}",
    },
    tests: [
      { stdin: "thequickbrownfoxjumpsoverthelazydog", expectedStdout: "true", isSample: true },
      { stdin: "helloworld", expectedStdout: "false", isSample: true },
      { stdin: "abcdefghijklmnopqrstuvwxyz", expectedStdout: "true" },
      { stdin: "a", expectedStdout: "false" },
      { stdin: "abcdefghijklmnopqrstuvwxy", expectedStdout: "false" },
      { stdin: "qwertyuioplkjhgfdsazxcvbnm", expectedStdout: "true" },
      { stdin: "aaaaabbbbbcccccdddddeeeeefffffggggghhhhhiiiiijjjjjkkkkklllllmmmmmnnnnnooooopppppqqqqqrrrrrssssstttttuuuuuvvvvvwwwwwxxxxyy", expectedStdout: "false" },
      { stdin: "bcdefghijklmnopqrstuvwxyza", expectedStdout: "true" },
    ],
  }),

  p({
    ...base,
    slug: "farm-grid-tractors",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Dance Floor Patterns",
    patternTags: ["math","parity","string"],
    signatureId: "fn:string,string->bool",
    avgSolveSeconds: 420,
    promptMarkdown: "You are designing an 8x8 dance floor with columns labeled 'a' to 'h' and rows labeled '1' to '8'.\n\nThe floor tiles are illuminated in an alternating checkerboard pattern. The tile at 'a1' glows red, 'b1' glows blue, 'c1' glows red, and this alternating sequence continues across all rows and columns.\n\nGiven two strings, `cell1` and `cell2`, representing tile coordinates, return `true` if they glow with the same color, and `false` otherwise.\n\n**Constraints**\n- `cell1.length == 2`\n- `cell2.length == 2`\n- `cell1[0]` and `cell2[0]` are between 'a' and 'h'.\n- `cell1[1]` and `cell2[1]` are between '1' and '8'.\n\n**Example 1**\n```\ninput:\na1\nc3\noutput: true\n```\nExplanation: Both 'a1' and 'c3' glow red.\n\n**Example 2**\n```\ninput:\na1\nh3\noutput: false\n```\nExplanation: 'a1' glows red, while 'h3' glows blue.\n\n**Example 3**\n```\ninput:\nc2\nc2\noutput: true\n```\nExplanation: The coordinates point to the exact same tile, which naturally shares the same color.\n\n**Follow-up:** Can you solve this in constant time using modular arithmetic?",
    editorialMarkdown: "## Parity of Coordinates\n\nA standard grid colored with alternating colors functions exactly like a checkerboard. The color of any cell depends on the sum of its 0-indexed column and row coordinates. If the sum is even, it's one color; if the sum is odd, it's the other color. \n\nBy converting the string representations into column and row integer offsets and summing them, we can determine the color purely based on the parity (even/odd state). Two cells have the same color if and only if their parity matches.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(1) since string manipulation and arithmetic operations on fixed length 2 strings take constant time.\n- **Space Complexity:** mathcal{O}(1) as no dynamic structures are used.\n\n**Common Trap:**\nA common trap is trying to hardcode or simulate the grid as an actual 2D array, which takes more code and introduces off-by-one errors. Another is failing to zero-index properly when performing character arithmetic.",
    referenceSolution: {
      JAVASCRIPT: "function solve(cell1, cell2) {\n    let c1 = (cell1.charCodeAt(0) - 97) + (cell1.charCodeAt(1) - 49);\n    let c2 = (cell2.charCodeAt(0) - 97) + (cell2.charCodeAt(1) - 49);\n    return c1 % 2 === c2 % 2;\n}",
      TYPESCRIPT: "function solve(cell1: string, cell2: string): boolean {\n    let c1 = (cell1.charCodeAt(0) - 97) + (cell1.charCodeAt(1) - 49);\n    let c2 = (cell2.charCodeAt(0) - 97) + (cell2.charCodeAt(1) - 49);\n    return c1 % 2 === c2 % 2;\n}",
      PYTHON: "def solve(cell1, cell2):\n    c1 = (ord(cell1[0]) - ord('a')) + (ord(cell1[1]) - ord('1'))\n    c2 = (ord(cell2[0]) - ord('a')) + (ord(cell2[1]) - ord('1'))\n    return c1 % 2 == c2 % 2",
      JAVA: "    static boolean solve(String cell1, String cell2) {\n        int c1 = (cell1.charAt(0) - 'a') + (cell1.charAt(1) - '1');\n        int c2 = (cell2.charAt(0) - 'a') + (cell2.charAt(1) - '1');\n        return c1 % 2 == c2 % 2;\n    }",
      CPP: "#include <string>\n\nbool solve(std::string cell1, std::string cell2) {\n    int c1 = (cell1[0] - 'a') + (cell1[1] - '1');\n    int c2 = (cell2[0] - 'a') + (cell2[1] - '1');\n    return c1 % 2 == c2 % 2;\n}",
      GO: "func solve(cell1 string, cell2 string) bool {\n    c1 := (cell1[0] - 'a') + (cell1[1] - '1')\n    c2 := (cell2[0] - 'a') + (cell2[1] - '1')\n    return c1%2 == c2%2\n}",
    },
    tests: [
      { stdin: "a1\nc3", expectedStdout: "true", isSample: true },
      { stdin: "a1\nh3", expectedStdout: "false", isSample: true },
      { stdin: "c2\nc2", expectedStdout: "true" },
      { stdin: "h8\na1", expectedStdout: "true" },
      { stdin: "a1\nb1", expectedStdout: "false" },
      { stdin: "h7\nh8", expectedStdout: "false" },
      { stdin: "b2\nd4", expectedStdout: "true" },
      { stdin: "e5\ng7", expectedStdout: "true" },
    ],
  }),

  p({
    ...base,
    slug: "valid-drone-formations",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Valid Drone Formations",
    patternTags: ["string","counting","run-length"],
    signatureId: "fn:string->bool",
    avgSolveSeconds: 900,
    promptMarkdown: "You are given a `s` containing uppercase English letters, representing different types of drones in a linear formation.\n\nA formation is considered a \"Valid Drone Formation\" if it can be divided into contiguous blocks of identical drones such that:\n1. Every block's size is a multiple of 5, EXCEPT for exactly one block.\n2. That single exceptional block must have a size such that when divided by 5, the remainder is exactly 3.\n\nIn other words, every contiguous run of identical characters must have a length L where L pmod 5 == 0, except exactly one run must have L pmod 5 == 3.\n\nReturn `true` if the given string represents a Valid Drone Formation, and `false` otherwise.\n\n**Constraints**\n- `1 <= s.length <= 1000`\n- `s` consists only of uppercase English letters.\n\n**Example 1**\n```\ninput:\nAAAAABBB\noutput: true\n```\nExplanation: There are two runs: 'A' of length 5 (5 pmod 5 = 0) and 'B' of length 3 (3 pmod 5 = 3). Exactly one run has remainder 3.\n\n**Example 2**\n```\ninput:\nCCCCC\noutput: false\n```\nExplanation: There is only one run of 'C' with length 5 (5 pmod 5 = 0). There is no run with remainder 3.\n\n**Example 3**\n```\ninput:\nXXXYYY\noutput: false\n```\nExplanation: There are two runs ('X' and 'Y'), both of length 3. This violates the rule that EXACTLY ONE run can have remainder 3.\n\n**Follow-up:** Can you solve this with a single pass and constant extra space?",
    editorialMarkdown: "## Grouping by Character Runs\n\nThe problem asks us to group identical consecutive characters and check their lengths. We can do this in a single pass by keeping track of the current character and counting how many times it appears in a row.\n\nFor each run of identical characters of length L, we check L pmod 5. It must be either 0 or 3. Furthermore, across the entire string, the number of runs with L pmod 5 == 3 must be exactly 1. \n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the length of the string, as we do a single pass over the string.\n- **Space Complexity:** mathcal{O}(1) since we only need a few variables for counting.\n\n**Common Trap:**\nA common mistake is forgetting to process the final run of characters after the loop finishes, or failing to check if the total number of groups with length modulo 5 equal to 3 is exactly one.",
    referenceSolution: {
      JAVASCRIPT: "function solve(s) {\n    let n = s.length;\n    let count3 = 0;\n    let i = 0;\n    while (i < n) {\n        let j = i;\n        while (j < n && s[j] === s[i]) {\n            j++;\n        }\n        let length = j - i;\n        let rem = length % 5;\n        if (rem === 3) {\n            count3++;\n        } else if (rem !== 0) {\n            return false;\n        }\n        i = j;\n    }\n    return count3 === 1;\n}",
      TYPESCRIPT: "function solve(s: string): boolean {\n    let n = s.length;\n    let count3 = 0;\n    let i = 0;\n    while (i < n) {\n        let j = i;\n        while (j < n && s[j] === s[i]) {\n            j++;\n        }\n        let length = j - i;\n        let rem = length % 5;\n        if (rem === 3) {\n            count3++;\n        } else if (rem !== 0) {\n            return false;\n        }\n        i = j;\n    }\n    return count3 === 1;\n}",
      PYTHON: "def solve(s):\n    n = len(s)\n    count3 = 0\n    i = 0\n    while i < n:\n        j = i\n        while j < n and s[j] == s[i]:\n            j += 1\n        length = j - i\n        rem = length % 5\n        if rem == 3:\n            count3 += 1\n        elif rem != 0:\n            return False\n        i = j\n    return count3 == 1",
      JAVA: "    static boolean solve(String s) {\n        int n = s.length();\n        int count3 = 0;\n        int i = 0;\n        while (i < n) {\n            int j = i;\n            while (j < n && s.charAt(j) == s.charAt(i)) {\n                j++;\n            }\n            int length = j - i;\n            int rem = length % 5;\n            if (rem == 3) {\n                count3++;\n            } else if (rem != 0) {\n                return false;\n            }\n            i = j;\n        }\n        return count3 == 1;\n    }",
      CPP: "bool solve(std::string s) {\n    int n = s.length();\n    int count3 = 0;\n    int i = 0;\n    while (i < n) {\n        int j = i;\n        while (j < n && s[j] == s[i]) {\n            j++;\n        }\n        int length = j - i;\n        int rem = length % 5;\n        if (rem == 3) {\n            count3++;\n        } else if (rem != 0) {\n            return false;\n        }\n        i = j;\n    }\n    return count3 == 1;\n}",
      GO: "func solve(s string) bool {\n    n := len(s)\n    count3 := 0\n    i := 0\n    for i < n {\n        j := i\n        for j < n && s[j] == s[i] {\n            j++\n        }\n        length := j - i\n        rem := length % 5\n        if rem == 3 {\n            count3++\n        } else if rem != 0 {\n            return false\n        }\n        i = j\n    }\n    return count3 == 1\n}",
    },
    tests: [
      { stdin: "AAAAABBB", expectedStdout: "true", isSample: true },
      { stdin: "CCCCC", expectedStdout: "false", isSample: true },
      { stdin: "XXXYYY", expectedStdout: "false" },
      { stdin: "A", expectedStdout: "false" },
      { stdin: "AAAA", expectedStdout: "false" },
      { stdin: "AAA", expectedStdout: "true" },
      { stdin: "BBBBBAAAAAAAABBBBB", expectedStdout: "true" },
      { stdin: "BBBBBAAAAAAAAA", expectedStdout: "false" },
    ],
  }),

  p({
    ...base,
    slug: "verify-tripartite-code",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Ancient Vault Key",
    patternTags: ["math","string","counting"],
    signatureId: "fn:int->bool",
    avgSolveSeconds: 600,
    promptMarkdown: "You are given an integer `code` to unlock an ancient vault.\n\nThe vault requires a \"Master Key Sequence\". To generate this sequence, you concatenate the string representations of `code`, `code * 2`, and `code * 3`. The resulting sequence is valid if it contains every digit from '1' to '9' exactly once, and does not contain the digit '0'.\n\nReturn `true` if `code` forms a valid master key sequence, and `false` otherwise.\n\n**Constraints**\n- `100 <= code <= 999`\n\n**Example 1**\n```\ninput:\n192\noutput: true\n```\nExplanation: Concatenating 192, 192 * 2 (384), and 192 * 3 (576) gives \"192384576\". This contains all digits from '1' to '9' exactly once.\n\n**Example 2**\n```\ninput:\n100\noutput: false\n```\nExplanation: Concatenating 100, 200, and 300 gives \"100200300\". This sequence is missing digits and contains '0'.\n\n**Example 3**\n```\ninput:\n273\noutput: true\n```\nExplanation: Concatenating 273, 546, and 819 gives \"273546819\", which contains all digits from '1' to '9' exactly once.\n\n**Follow-up:** Can you solve this without using a hash map for character frequencies?",
    editorialMarkdown: "## String Concatenation and Frequency Counting\n\nThe problem requires us to multiply the given code by 1, 2, and 3, concatenate the results as strings, and verify that the combined string contains exactly the characters '1' through '9' without repetition (and no '0'). \n\nWe can solve this by converting the numbers to strings, concatenating them, and storing the frequency of each digit. Since the string must contain exactly the digits 1 through 9 once each, the length of the concatenated string must be exactly 9.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(1) because the input code can only produce a bounded length string (max length is 9 for it to be valid).\n- **Space Complexity:** mathcal{O}(1) as the concatenated string size and digit frequency array are both very small and strictly bounded.\n\n**Common Trap:**\nForgetting to explicitly check that the concatenated string doesn't contain any '0's, or neglecting to verify that the length of the concatenated string is exactly 9 before checking character counts.",
    referenceSolution: {
      JAVASCRIPT: "function solve(code) {\n    let s = code.toString() + (code * 2).toString() + (code * 3).toString();\n    if (s.length !== 9) return false;\n    let counts = new Array(10).fill(0);\n    for (let i = 0; i < s.length; i++) {\n        counts[s.charCodeAt(i) - 48]++;\n    }\n    if (counts[0] !== 0) return false;\n    for (let i = 1; i <= 9; i++) {\n        if (counts[i] !== 1) return false;\n    }\n    return true;\n}",
      TYPESCRIPT: "function solve(code: number): boolean {\n    let s = code.toString() + (code * 2).toString() + (code * 3).toString();\n    if (s.length !== 9) return false;\n    let counts = new Array(10).fill(0);\n    for (let i = 0; i < s.length; i++) {\n        counts[s.charCodeAt(i) - 48]++;\n    }\n    if (counts[0] !== 0) return false;\n    for (let i = 1; i <= 9; i++) {\n        if (counts[i] !== 1) return false;\n    }\n    return true;\n}",
      PYTHON: "def solve(code):\n    s = str(code) + str(code * 2) + str(code * 3)\n    if len(s) != 9:\n        return False\n    counts = [0] * 10\n    for char in s:\n        counts[int(char)] += 1\n    if counts[0] != 0:\n        return False\n    for i in range(1, 10):\n        if counts[i] != 1:\n            return False\n    return True",
      JAVA: "    static boolean solve(int code) {\n        String s = Integer.toString(code) + Integer.toString(code * 2) + Integer.toString(code * 3);\n        if (s.length() != 9) return false;\n        int[] counts = new int[10];\n        for (int i = 0; i < s.length(); i++) {\n            counts[s.charAt(i) - '0']++;\n        }\n        if (counts[0] != 0) return false;\n        for (int i = 1; i <= 9; i++) {\n            if (counts[i] != 1) return false;\n        }\n        return true;\n    }",
      CPP: "bool solve(int code) {\n    std::string s = std::to_string(code) + std::to_string(code * 2) + std::to_string(code * 3);\n    if (s.length() != 9) return false;\n    std::vector<int> counts(10, 0);\n    for (char c : s) {\n        counts[c - '0']++;\n    }\n    if (counts[0] != 0) return false;\n    for (int i = 1; i <= 9; i++) {\n        if (counts[i] != 1) return false;\n    }\n    return true;\n}",
      GO: "func solve(code int) bool {\n    s := strconv.Itoa(code) + strconv.Itoa(code*2) + strconv.Itoa(code*3)\n    if len(s) != 9 {\n        return false\n    }\n    counts := make([]int, 10)\n    for i := 0; i < len(s); i++ {\n        counts[s[i]-'0']++\n    }\n    if counts[0] != 0 {\n        return false\n    }\n    for i := 1; i <= 9; i++ {\n        if counts[i] != 1 {\n            return false\n        }\n    }\n    return true\n}",
    },
    tests: [
      { stdin: "192", expectedStdout: "true", isSample: true },
      { stdin: "100", expectedStdout: "false", isSample: true },
      { stdin: "273", expectedStdout: "true" },
      { stdin: "327", expectedStdout: "true" },
      { stdin: "999", expectedStdout: "false" },
      { stdin: "500", expectedStdout: "false" },
      { stdin: "219", expectedStdout: "true" },
      { stdin: "888", expectedStdout: "false" },
    ],
  }),

  p({
    ...base,
    slug: "verify-fragmented-logs",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Decipher Ancient Scrolls",
    patternTags: ["string","simulation"],
    signatureId: "fn:string,string->bool",
    avgSolveSeconds: 420,
    promptMarkdown: "You are an archaeologist examining two strings, `log1` and `log2`, which represent text from torn scrolls. The torn edges are marked by the pipe character (`|`).\n\nYour goal is to determine if the original text of both scrolls matches when ignoring the tears.\n\nReturn `true` if deleting all `|` symbols from both `log1` and `log2` produces the exact same text, and `false` otherwise.\n\n**Constraints**\n- `1 <= log1.length, log2.length <= 1000`\n- `log1` and `log2` consist of lowercase English letters and the `|` character.\n\n**Example 1**\n```\ninput:\nab|c\na|bc\noutput: true\n```\nExplanation: Removing the `|` from both strings gives \"abc\", meaning they match.\n\n**Example 2**\n```\ninput:\na|cb\nab|c\noutput: false\n```\nExplanation: The first scroll yields \"acb\", while the second yields \"abc\". They do not match.\n\n**Example 3**\n```\ninput:\nabc|d|e|f|def\nabcdef|def\noutput: true\n```\nExplanation: Both strings evaluate to \"abcdefdef\".\n\n**Follow-up:** Can you solve this efficiently without creating entirely new string copies in memory?",
    editorialMarkdown: "## String Normalization\n\nThe problem essentially requires comparing the underlying data when all delimiter characters ('|') are removed. The simplest way to achieve this is to iterate through each string, building a new string (or using a string builder) containing only the non-delimiter characters. Finally, check if the two resultant strings are equal.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N + M) where N and M are the lengths of `log1` and `log2` respectively, as we must inspect every character to construct the stripped strings.\n- **Space Complexity:** mathcal{O}(N + M) to store the newly created concatenated strings. In languages with mutable strings, it might be possible to modify the strings in place to mathcal{O}(1) space, though in many implementations strings are immutable.\n\n**Common Trap:**\nA common trap is attempting to parse the string character by character with complex indices instead of just utilizing built-in string replace or filter functions, which is more error-prone.",
    referenceSolution: {
      JAVASCRIPT: "function solve(log1, log2) {\n    return log1.split('|').join('') === log2.split('|').join('');\n}",
      TYPESCRIPT: "function solve(log1: string, log2: string): boolean {\n    return log1.split('|').join('') === log2.split('|').join('');\n}",
      PYTHON: "def solve(log1, log2):\n    return log1.replace('|', '') == log2.replace('|', '')",
      JAVA: "    static boolean solve(String log1, String log2) {\n        return log1.replace(\"|\", \"\").equals(log2.replace(\"|\", \"\"));\n    }",
      CPP: "bool solve(std::string log1, std::string log2) {\n    std::string s1 = \"\";\n    std::string s2 = \"\";\n    for (char c : log1) {\n        if (c != '|') s1 += c;\n    }\n    for (char c : log2) {\n        if (c != '|') s2 += c;\n    }\n    return s1 == s2;\n}",
      GO: "func solve(log1 string, log2 string) bool {\n    return strings.ReplaceAll(log1, \"|\", \"\") == strings.ReplaceAll(log2, \"|\", \"\")\n}",
    },
    tests: [
      { stdin: "ab|c\na|bc", expectedStdout: "true", isSample: true },
      { stdin: "a|cb\nab|c", expectedStdout: "false", isSample: true },
      { stdin: "abc|d|e|f|def\nabcdef|def", expectedStdout: "true" },
      { stdin: "a|b|c\na|b|c", expectedStdout: "true" },
      { stdin: "a|b|c\na|b", expectedStdout: "false" },
      { stdin: "|a|b|c|\nabc", expectedStdout: "true" },
      { stdin: "a|||b\na|||c", expectedStdout: "false" },
      { stdin: "||||\n||", expectedStdout: "true" },
    ],
  }),
];
