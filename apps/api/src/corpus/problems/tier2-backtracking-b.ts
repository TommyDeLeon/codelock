import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'BACKTRACKING', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_2_BACKTRACKING_B_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'even-sized-target-subsets', title:'Target Subsets With an Even Headcount', difficulty:'EASY', patternTags:['backtracking','subsets','sum'], signatureId:'fn:ints,int->int', avgSolveSeconds:600,
    promptMarkdown:['Count subsets whose values add to target `k` and whose number of chosen values is even. Different positions are different choices, even if their values match.','', '**Example**','', '```','input: 1 2 3 4','5','output: 2','```','', 'The valid subsets are `[1,4]` and `[2,3]`. The list has 1 to 10 integers; the named edge case is that the empty subset counts only when `k` is 0.'].join('\n'),
    editorialMarkdown:['## Include-or-skip with a parity state','', 'At each position, backtracking makes exactly the two possible decisions: skip the value or include it. Carry both the running sum and how many items were chosen. At the leaf, accept only a target sum with an even count, which makes every subset appear once through its index decisions.', '', 'The quiet mistake is checking whether the sum is even instead of whether the subset size is even; the sample cannot expose that confusion by itself. Time is O(2^n), bounded by n <= 10, and recursion uses O(n) stack space.'].join('\n'),
    referenceSolution:six('function solve(a,b){function f(i,s,c){if(i==a.length)return s==b&&c%2==0?1:0;return f(i+1,s,c)+f(i+1,s+a[i],c+1)}return f(0,0,0)}','function solve(a:number[],b:number):number{function f(i:number,s:number,c:number):number{return i==a.length?(s==b&&c%2==0?1:0):f(i+1,s,c)+f(i+1,s+a[i],c+1)}return f(0,0,0)}','def solve(a, b):\n    def f(i,s,c):\n        if i==len(a): return int(s==b and c%2==0)\n        return f(i+1,s,c)+f(i+1,s+a[i],c+1)\n    return f(0,0,0)','    static int solve(int[]a,int b){return even(a,b,0,0,0);}\n    static int even(int[]a,int b,int i,int s,int c){if(i==a.length)return s==b&&c%2==0?1:0;return even(a,b,i+1,s,c)+even(a,b,i+1,s+a[i],c+1);}','int even(vector<int>&a,int b,int i,int s,int c){if(i==(int)a.size())return s==b&&c%2==0;return even(a,b,i+1,s,c)+even(a,b,i+1,s+a[i],c+1);}\nint solve(vector<int>a,int b){return even(a,b,0,0,0);}','func solve(a []int,b int)int{var f func(int,int,int)int;f=func(i,s,c int)int{if i==len(a){if s==b&&c%2==0{return 1};return 0};return f(i+1,s,c)+f(i+1,s+a[i],c+1)};return f(0,0,0)}'),
    tests:[{stdin:'1 2 3 4\n5',expectedStdout:'2',isSample:true},{stdin:'2 2\n2',expectedStdout:'0',isSample:true},{stdin:'1 1\n2',expectedStdout:'1'},{stdin:'5\n0',expectedStdout:'1'},{stdin:'-1 1 2\n0',expectedStdout:'2'}] }),
  p({ ...base, slug:'target-subsets-without-neighboring-indices', title:'Target Picks With a Gap', difficulty:'MEDIUM', patternTags:['backtracking','subsets','constraints'], signatureId:'fn:ints,int->int', avgSolveSeconds:660,
    promptMarkdown:['Count subsets adding to target `k`, but never choose two neighboring input positions. Equal values in different positions remain distinct.','', '**Example**','', '```','input: 2 3 2 3','5','output: 1','```','', 'Only positions 0 and 3 may be chosen together to make 5; every other 2-and-3 pair uses neighboring positions. The list has 1 to 10 integers. Choosing positions 0 and 1 is forbidden; the named edge case is a one-value subset, which is always allowed if it hits the target.'].join('\n'),
    editorialMarkdown:['## Backtracking with the previous-choice flag','', 'The recursion visits positions from left to right. Skipping is always legal; including the current value is legal only when the preceding position was not included. That single boolean records every fact needed to enforce the spacing rule while the sum records whether a completed branch qualifies.', '', 'The quiet mistake is forbidding equal neighboring values rather than neighboring indices. The rule is about seats in the input, so values do not change it. Time is O(2^n), bounded by ten positions, and stack space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){function f(i,s,last){if(i==a.length)return s==b?1:0;return f(i+1,s,0)+(last?0:f(i+1,s+a[i],1))}return f(0,0,0)}','function solve(a:number[],b:number):number{function f(i:number,s:number,last:number):number{return i==a.length?(s==b?1:0):f(i+1,s,0)+(last?0:f(i+1,s+a[i],1))}return f(0,0,0)}','def solve(a,b):\n    def f(i,s,last):\n        if i==len(a): return int(s==b)\n        return f(i+1,s,False)+(0 if last else f(i+1,s+a[i],True))\n    return f(0,0,False)','    static int solve(int[]a,int b){return gap(a,b,0,0,false);}\n    static int gap(int[]a,int b,int i,int s,boolean last){if(i==a.length)return s==b?1:0;return gap(a,b,i+1,s,false)+(last?0:gap(a,b,i+1,s+a[i],true));}','int gap(vector<int>&a,int b,int i,int s,bool last){if(i==(int)a.size())return s==b;return gap(a,b,i+1,s,0)+(last?0:gap(a,b,i+1,s+a[i],1));}\nint solve(vector<int>a,int b){return gap(a,b,0,0,0);}','func solve(a []int,b int)int{var f func(int,int,bool)int;f=func(i,s int,last bool)int{if i==len(a){if s==b{return 1};return 0};z:=f(i+1,s,false);if !last{z+=f(i+1,s+a[i],true)};return z};return f(0,0,false)}'),
    tests:[{stdin:'2 3 2 3\n5',expectedStdout:'1',isSample:true},{stdin:'5\n5',expectedStdout:'1',isSample:true},{stdin:'1 1 1\n2',expectedStdout:'1'},{stdin:'1 2 3\n4',expectedStdout:'1'},{stdin:'4 4\n8',expectedStdout:'0'}] }),
  p({ ...base, slug:'fixed-size-subsets-under-spread', title:'Small Teams With a Tight Spread', difficulty:'MEDIUM', patternTags:['backtracking','combinations','constraints'], signatureId:'fn:ints,int->int', avgSolveSeconds:660,
    promptMarkdown:['Count subsets of exactly `k` values whose largest value minus smallest value is at most `k`. Input values are distinct.','', '**Example**','', '```','input: 1 3 4 8','2','output: 2','```','', 'The valid pairs are `[1,3]` and `[3,4]`. The list has 1 to 10 values and `1 <= k <= n`; the named edge case is `k=1`, when every singleton qualifies.'].join('\n'),
    editorialMarkdown:['## Combination backtracking with extrema','', 'Choose positions in increasing order, so each subset is generated once rather than in many orders. Carry its size, current minimum, and current maximum. When the size reaches k, the spread test is complete and no output ordering needs to be specified because only the count is returned.', '', 'The quiet mistake is using the chosen count as the allowed spread after it changes during recursion. The input k is a fixed rule and must stay unchanged. The search visits O(2^n) branches, bounded by n <= 10, and uses O(n) recursion space.'].join('\n'),
    referenceSolution:six('function solve(a,b){function f(i,c,lo,hi){if(c==b)return hi-lo<=b?1:0;if(i==a.length)return 0;return f(i+1,c,lo,hi)+f(i+1,c+1,c?a[i]<lo?a[i]:lo:a[i],c?a[i]>hi?a[i]:hi:a[i])}return f(0,0,0,0)}','function solve(a:number[],b:number):number{function f(i:number,c:number,lo:number,hi:number):number{if(c==b)return hi-lo<=b?1:0;if(i==a.length)return 0;return f(i+1,c,lo,hi)+f(i+1,c+1,c?Math.min(lo,a[i]):a[i],c?Math.max(hi,a[i]):a[i])}return f(0,0,0,0)}','def solve(a,b):\n    def f(i,c,lo,hi):\n        if c==b:return int(hi-lo<=b)\n        if i==len(a):return 0\n        return f(i+1,c,lo,hi)+f(i+1,c+1,a[i] if not c else min(lo,a[i]),a[i] if not c else max(hi,a[i]))\n    return f(0,0,0,0)','    static int solve(int[]a,int b){return spread(a,b,0,0,0,0);}\n    static int spread(int[]a,int b,int i,int c,int lo,int hi){if(c==b)return hi-lo<=b?1:0;if(i==a.length)return 0;int x=c==0?a[i]:(a[i]<lo?a[i]:lo),y=c==0?a[i]:(a[i]>hi?a[i]:hi);return spread(a,b,i+1,c,lo,hi)+spread(a,b,i+1,c+1,x,y);}','int spread(vector<int>&a,int b,int i,int c,int lo,int hi){if(c==b)return hi-lo<=b;if(i==(int)a.size())return 0;int x=c?min(lo,a[i]):a[i],y=c?max(hi,a[i]):a[i];return spread(a,b,i+1,c,lo,hi)+spread(a,b,i+1,c+1,x,y);}\nint solve(vector<int>a,int b){return spread(a,b,0,0,0,0);}','func solve(a []int,b int)int{var f func(int,int,int,int)int;f=func(i,c,lo,hi int)int{if c==b{if hi-lo<=b{return 1};return 0};if i==len(a){return 0};x,y:=a[i],a[i];if c>0{if lo<x{x=lo};if hi>y{y=hi}};return f(i+1,c,lo,hi)+f(i+1,c+1,x,y)};return f(0,0,0,0)}'),
    tests:[{stdin:'1 3 4 8\n2',expectedStdout:'2',isSample:true},{stdin:'9 1 5\n1',expectedStdout:'3',isSample:true},{stdin:'1 2 3\n3',expectedStdout:'1'},{stdin:'1 4 7\n2',expectedStdout:'0'},{stdin:'-2 -1 0\n2',expectedStdout:'3'}] }),
  p({ ...base, slug:'permutations-with-forbidden-neighbor-sum', title:'Orderings That Avoid a Neighbor Sum', difficulty:'MEDIUM', patternTags:['backtracking','permutations','forbidden-pair'], signatureId:'fn:ints,int->int', avgSolveSeconds:720,
    promptMarkdown:['The input values are distinct. Count their orderings where no adjacent pair adds to `k`.','', '**Example**','', '```','input: 1 2 3','4','output: 2','```','', 'Only `1 3 2` and `2 3 1` avoid adjacent pairs summing to 4. There are 1 to 8 values; the named edge case is one value, which has one valid ordering.'].join('\n'),
    editorialMarkdown:['## Permutation backtracking with a last value','', 'Build an ordering one slot at a time and mark each input value when it is used. Before extending a partial ordering, compare the candidate with the last chosen value; rejecting a forbidden sum early prunes every invalid completion below it. Distinct inputs mean each leaf represents one ordering.', '', 'The quiet mistake is checking only disjoint pairs after the permutation is complete. Every touching pair matters, including the final two positions. Time is O(n! n) in the worst case, bounded by n <= 8, and state uses O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){let u=Array(a.length).fill(0);function f(c,last){if(c==a.length)return 1;let z=0;for(let i=0;i<a.length;i++)if(!u[i]&&(c==0||last+a[i]!=b)){u[i]=1;z+=f(c+1,a[i]);u[i]=0}return z}return f(0,0)}','function solve(a:number[],b:number):number{const u=Array(a.length).fill(false);function f(c:number,last:number):number{if(c==a.length)return 1;let z=0;for(let i=0;i<a.length;i++)if(!u[i]&&(c==0||last+a[i]!==b)){u[i]=true;z+=f(c+1,a[i]);u[i]=false}return z}return f(0,0)}','def solve(a,b):\n    u=[False]*len(a)\n    def f(c,last):\n        if c==len(a):return 1\n        z=0\n        for i,x in enumerate(a):\n            if not u[i] and (c==0 or last+x!=b):\n                u[i]=True;z+=f(c+1,x);u[i]=False\n        return z\n    return f(0,0)','    static int solve(int[]a,int b){return forbid(a,b,0,0,new boolean[a.length]);}\n    static int forbid(int[]a,int b,int c,int last,boolean[]u){if(c==a.length)return 1;int z=0;for(int i=0;i<a.length;i++)if(!u[i]&&(c==0||last+a[i]!=b)){u[i]=true;z+=forbid(a,b,c+1,a[i],u);u[i]=false;}return z;}','int forbid(vector<int>&a,int b,int c,int last,vector<char>&u){if(c==(int)a.size())return 1;int z=0;for(int i=0;i<(int)a.size();i++)if(!u[i]&&(c==0||last+a[i]!=b)){u[i]=1;z+=forbid(a,b,c+1,a[i],u);u[i]=0;}return z;}\nint solve(vector<int>a,int b){vector<char>u(a.size());return forbid(a,b,0,0,u);}','func solve(a []int,b int)int{u:=make([]bool,len(a));var f func(int,int)int;f=func(c,last int)int{if c==len(a){return 1};z:=0;for i,x:=range a{if !u[i]&&(c==0||last+x!=b){u[i]=true;z+=f(c+1,x);u[i]=false}};return z};return f(0,0)}'),
    tests:[{stdin:'1 2 3\n4',expectedStdout:'2',isSample:true},{stdin:'7\n9',expectedStdout:'1',isSample:true},{stdin:'1 2\n3',expectedStdout:'0'},{stdin:'1 2 4\n9',expectedStdout:'6'},{stdin:'-1 1 2\n1',expectedStdout:'2'}] }),
  /* archived overlapping variant; deliberately excluded from this twelve-problem batch.
    promptMarkdown:['The input values are distinct. Count orderings whose first value is smaller than their last value.','', '**Example**','', '```','input: 3 1 2','output: 3','```','', 'Exactly half of the six orderings qualify. There are 1 to 8 values; the named edge case is one value, whose first and last value are equal and therefore does not qualify.'].join('\n'),
    editorialMarkdown:['## Full permutations with an endpoint check','', 'Use the normal used-array recursion to place every value exactly once. At a completed ordering, compare the remembered first value with the final candidate. Keeping just the first value avoids allocating or printing every permutation while still accounting for each possible ordering.', '', 'The quiet mistake is treating a single element as rising because it is both endpoints. The comparison is strict, so equality fails. Time is O(n!), bounded by n <= 8, and the used markers plus recursion consume O(n) space.'].join('\n'),
    archivedSolution:six('function solve(a){let u=Array(a.length).fill(0);function f(c,first,last){if(c==a.length)return first<last?1:0;let z=0;for(let i=0;i<a.length;i++)if(!u[i]){u[i]=1;z+=f(c+1,c?a[0]===a[0]?first:a[i]:a[i],a[i]);u[i]=0}return z}return f(0,0,0)}','function solve(a:number[]):number{const u=Array(a.length).fill(false);function f(c:number,first:number,last:number):number{if(c==a.length)return first<last?1:0;let z=0;for(let i=0;i<a.length;i++)if(!u[i]){u[i]=true;z+=f(c+1,c?first:a[i],a[i]);u[i]=false}return z}return f(0,0,0)}','def solve(a):\n    u=[False]*len(a)\n    def f(c,first,last):\n        if c==len(a):return int(first<last)\n        z=0\n        for i,x in enumerate(a):\n            if not u[i]:u[i]=True;z+=f(c+1,first if c else x,x);u[i]=False\n        return z\n    return f(0,0,0)','    static int solve(int[]a){return rise(a,0,0,0,new boolean[a.length]);}\n    static int rise(int[]a,int c,int first,int last,boolean[]u){if(c==a.length)return first<last?1:0;int z=0;for(int i=0;i<a.length;i++)if(!u[i]){u[i]=true;z+=rise(a,c+1,c==0?a[i]:first,a[i],u);u[i]=false;}return z;}','int rise(vector<int>&a,int c,int first,int last,vector<char>&u){if(c==(int)a.size())return first<last;int z=0;for(int i=0;i<(int)a.size();i++)if(!u[i]){u[i]=1;z+=rise(a,c+1,c?first:a[i],a[i],u);u[i]=0;}return z;}\nint solve(vector<int>a){vector<char>u(a.size());return rise(a,0,0,0,u);}','func solve(a []int)int{u:=make([]bool,len(a));var f func(int,int,int)int;f=func(c,first,last int)int{if c==len(a){if first<last{return 1};return 0};z:=0;for i,x:=range a{if !u[i]{u[i]=true;if c==0{z+=f(c+1,x,x)}else{z+=f(c+1,first,x)};u[i]=false}};return z};return f(0,0,0)}'),
    archivedTests:[{stdin:'3 1 2',expectedStdout:'3',wasSample:true},{stdin:'9',expectedStdout:'0',wasSample:true},{stdin:'1 2',expectedStdout:'1'},{stdin:'-1 0 4',expectedStdout:'3'},{stdin:'5 2 8 1',expectedStdout:'12'}] }), */
  p({ ...base, slug:'permutations-with-bounded-neighbor-jump', title:'Orderings With Short Jumps', difficulty:'MEDIUM', patternTags:['backtracking','permutations','pruning'], signatureId:'fn:ints,int->int', avgSolveSeconds:720,
    promptMarkdown:['The input values are distinct. Count orderings where adjacent values differ by at most `k`.','', '**Example**','', '```','input: 1 2 4','2','output: 2','```','', 'The valid orderings are `1 2 4` and `4 2 1`. There are 1 to 8 values and k is nonnegative; the named edge case is k=0, which permits only a one-value input.'].join('\n'),
    editorialMarkdown:['## Permutation tree with a local jump guard','', 'The next position needs only the previous value to decide legality. Try each unused value, reject it when its absolute difference from the previous value exceeds k, and recurse otherwise. This local guard cuts invalid prefixes before they can become factorially many leaves.', '', 'The quiet mistake is comparing every new value to the first value rather than the immediately previous value. That changes a path rule into a range rule. Worst-case time is O(n!), bounded by n <= 8, and the used array and call stack are O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){let u=Array(a.length).fill(0);function f(c,last){if(c==a.length)return 1;let z=0;for(let i=0;i<a.length;i++)if(!u[i]&&(c==0||Math.abs(a[i]-last)<=b)){u[i]=1;z+=f(c+1,a[i]);u[i]=0}return z}return f(0,0)}','function solve(a:number[],b:number):number{const u=Array(a.length).fill(false);function f(c:number,last:number):number{if(c==a.length)return 1;let z=0;for(let i=0;i<a.length;i++)if(!u[i]&&(c==0||Math.abs(a[i]-last)<=b)){u[i]=true;z+=f(c+1,a[i]);u[i]=false}return z}return f(0,0)}','def solve(a,b):\n    u=[False]*len(a)\n    def f(c,last):\n        if c==len(a):return 1\n        z=0\n        for i,x in enumerate(a):\n            if not u[i] and (c==0 or abs(x-last)<=b):u[i]=True;z+=f(c+1,x);u[i]=False\n        return z\n    return f(0,0)','    static int solve(int[]a,int b){return jump(a,b,0,0,new boolean[a.length]);}\n    static int jump(int[]a,int b,int c,int last,boolean[]u){if(c==a.length)return 1;int z=0;for(int i=0;i<a.length;i++)if(!u[i]&&(c==0||Math.abs(a[i]-last)<=b)){u[i]=true;z+=jump(a,b,c+1,a[i],u);u[i]=false;}return z;}','int jump(vector<int>&a,int b,int c,int last,vector<char>&u){if(c==(int)a.size())return 1;int z=0;for(int i=0;i<(int)a.size();i++)if(!u[i]&&(c==0||(a[i]-last<=b&&last-a[i]<=b))){u[i]=1;z+=jump(a,b,c+1,a[i],u);u[i]=0;}return z;}\nint solve(vector<int>a,int b){vector<char>u(a.size());return jump(a,b,0,0,u);}','func solve(a []int,b int)int{u:=make([]bool,len(a));var f func(int,int)int;f=func(c,last int)int{if c==len(a){return 1};z:=0;for i,x:=range a{d:=x-last;if d<0{d=-d};if !u[i]&&(c==0||d<=b){u[i]=true;z+=f(c+1,x);u[i]=false}};return z};return f(0,0)}'),
    tests:[{stdin:'1 2 4\n2',expectedStdout:'2',isSample:true},{stdin:'5\n0',expectedStdout:'1',isSample:true},{stdin:'1 2\n0',expectedStdout:'0'},{stdin:'1 2 3\n1',expectedStdout:'2'},{stdin:'-1 0 1\n2',expectedStdout:'6'}] }),
  p({ ...base, slug:'equal-sum-partition-with-odd-left-count', title:'Balanced Split With an Odd Left Side', difficulty:'MEDIUM', patternTags:['backtracking','partition','parity'], signatureId:'fn:ints->bool', avgSolveSeconds:720,
    promptMarkdown:['Return true if some subset has half of the total sum and contains an odd number of values. Values not chosen form the other side.','', '**Example**','', '```','input: 1 2 3 6','output: true','```','', 'Choosing `[6]` balances the total 12 and has odd size. The list has 1 to 10 nonnegative values; an odd total immediately returns false.'].join('\n'),
    editorialMarkdown:['## Subset partition carrying cardinality parity','', 'First reject odd totals because two equal integer sums cannot exist. Then include or skip each value while carrying the current sum and chosen count. A leaf succeeds only when its sum is half the total and its count is odd; the complement automatically has the remaining values.', '', 'The quiet mistake is returning true at the first half-sum without checking the parity twist. That is especially quiet when a regular partition test happens to use an odd-sized side. Time is O(2^n), bounded by n <= 10, and recursion uses O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){let t=a.reduce((x,y)=>x+y,0);if(t%2)return false;function f(i,s,c){if(i==a.length)return s*2==t&&c%2==1;return f(i+1,s,c)||f(i+1,s+a[i],c+1)}return f(0,0,0)}','function solve(a:number[]):boolean{const t=a.reduce((x,y)=>x+y,0);if(t%2)return false;function f(i:number,s:number,c:number):boolean{return i==a.length?s*2===t&&c%2===1:f(i+1,s,c)||f(i+1,s+a[i],c+1)}return f(0,0,0)}','def solve(a):\n    t=sum(a)\n    if t%2:return False\n    def f(i,s,c):\n        return s*2==t and c%2==1 if i==len(a) else f(i+1,s,c) or f(i+1,s+a[i],c+1)\n    return f(0,0,0)','    static boolean solve(int[]a){int t=0;for(int x:a)t+=x;if(t%2!=0)return false;return odd(a,t,0,0,0);}\n    static boolean odd(int[]a,int t,int i,int s,int c){if(i==a.length)return s*2==t&&c%2==1;return odd(a,t,i+1,s,c)||odd(a,t,i+1,s+a[i],c+1);}','bool odd(vector<int>&a,int t,int i,int s,int c){if(i==(int)a.size())return s*2==t&&c%2;return odd(a,t,i+1,s,c)||odd(a,t,i+1,s+a[i],c+1);}\nbool solve(vector<int>a){int t=0;for(int x:a)t+=x;if(t%2)return false;return odd(a,t,0,0,0);}','func solve(a []int)bool{t:=0;for _,x:=range a{t+=x};if t%2!=0{return false};var f func(int,int,int)bool;f=func(i,s,c int)bool{if i==len(a){return s*2==t&&c%2==1};return f(i+1,s,c)||f(i+1,s+a[i],c+1)};return f(0,0,0)}'),
    tests:[{stdin:'1 2 3 6',expectedStdout:'true',isSample:true},{stdin:'1 1 2 2',expectedStdout:'false',isSample:true},{stdin:'2 2',expectedStdout:'true'},{stdin:'1 2 5',expectedStdout:'false'},{stdin:'0 0',expectedStdout:'true'}] }),
  p({ ...base, slug:'three-equal-sum-bins', title:'Pack Three Equal Bins', difficulty:'HARD', patternTags:['backtracking','partition','three-way'], signatureId:'fn:ints->bool', avgSolveSeconds:840,
    promptMarkdown:['Return true if every input value can be placed into one of three groups with equal sums. Groups may not be empty unless the total is zero.','', '**Example**','', '```','input: 3 3 3 3 3 3','output: true','```','', 'Each group can receive two 3s. The list has 1 to 10 nonnegative values; the named edge case is a total not divisible by three, which is impossible.'].join('\n'),
    editorialMarkdown:['## Three-bin assignment backtracking','', 'Each value chooses one of three running bin sums. If the total is divisible by three, no bin may exceed target, so that check safely prunes a branch. At the leaf, all assigned values have been used and the three capped sums must therefore equal target.', '', 'The quiet mistake is checking only two bins at the end while allowing a third to overflow earlier. The cap is what makes the partial search honest. Time is O(3^n), bounded by n <= 10, and the recursion stack is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let t=a.reduce((x,y)=>x+y,0);if(t%3)return false;t/=3;function f(i,x,y,z){if(i==a.length)return x==t&&y==t&&z==t;let v=a[i];return x+v<=t&&f(i+1,x+v,y,z)||y+v<=t&&f(i+1,x,y+v,z)||z+v<=t&&f(i+1,x,y,z+v)}return f(0,0,0,0)}','function solve(a:number[]):boolean{let t=a.reduce((x,y)=>x+y,0);if(t%3)return false;t/=3;function f(i:number,x:number,y:number,z:number):boolean{if(i==a.length)return x===t&&y===t&&z===t;const v=a[i];return x+v<=t&&f(i+1,x+v,y,z)||y+v<=t&&f(i+1,x,y+v,z)||z+v<=t&&f(i+1,x,y,z+v)}return f(0,0,0,0)}','def solve(a):\n    t=sum(a)\n    if t%3:return False\n    t//=3\n    def f(i,x,y,z):\n        if i==len(a):return x==y==z==t\n        v=a[i]\n        return (x+v<=t and f(i+1,x+v,y,z)) or (y+v<=t and f(i+1,x,y+v,z)) or (z+v<=t and f(i+1,x,y,z+v))\n    return f(0,0,0,0)','    static boolean solve(int[]a){int t=0;for(int x:a)t+=x;if(t%3!=0)return false;return bins(a,t/3,0,0,0,0);}\n    static boolean bins(int[]a,int t,int i,int x,int y,int z){if(i==a.length)return x==t&&y==t&&z==t;int v=a[i];return x+v<=t&&bins(a,t,i+1,x+v,y,z)||y+v<=t&&bins(a,t,i+1,x,y+v,z)||z+v<=t&&bins(a,t,i+1,x,y,z+v);}','bool bins(vector<int>&a,int t,int i,int x,int y,int z){if(i==(int)a.size())return x==t&&y==t&&z==t;int v=a[i];return x+v<=t&&bins(a,t,i+1,x+v,y,z)||y+v<=t&&bins(a,t,i+1,x,y+v,z)||z+v<=t&&bins(a,t,i+1,x,y,z+v);}\nbool solve(vector<int>a){int t=0;for(int x:a)t+=x;if(t%3)return false;return bins(a,t/3,0,0,0,0);}','func solve(a []int)bool{t:=0;for _,x:=range a{t+=x};if t%3!=0{return false};t/=3;var f func(int,int,int,int)bool;f=func(i,x,y,z int)bool{if i==len(a){return x==t&&y==t&&z==t};v:=a[i];return x+v<=t&&f(i+1,x+v,y,z)||y+v<=t&&f(i+1,x,y+v,z)||z+v<=t&&f(i+1,x,y,z+v)};return f(0,0,0,0)}'),
    tests:[{stdin:'3 3 3 3 3 3',expectedStdout:'true',isSample:true},{stdin:'1 2 3 4',expectedStdout:'false',isSample:true},{stdin:'0 0 0',expectedStdout:'true'},{stdin:'1 1 1',expectedStdout:'true'},{stdin:'2 2 2 2',expectedStdout:'false'}] }),
  p({ ...base, slug:'unique-tile-orders-without-twins', title:'Tile Orders Without Matching Neighbors', difficulty:'HARD', patternTags:['backtracking','permutations','duplicates'], signatureId:'fn:string->int', avgSolveSeconds:840,
    promptMarkdown:['Count distinct orderings of all uppercase letter tiles where equal letters never touch.','', '**Example**','', '```','input: AAB','output: 1','```','', 'Only `ABA` works. The string has 1 to 8 letters; the named edge case is one tile, which has one ordering.'].join('\n'),
    editorialMarkdown:['## Frequency backtracking with a previous letter','', 'Count each letter frequency, then choose any letter with remaining count that differs from the previously placed one. Identical tiles are represented by one frequency bucket, so the recursion counts distinct visible orderings rather than falsely treating duplicate copies as different.', '', 'The quiet mistake is permuting tile indices and dividing afterward; the no-neighbor rule means duplicate arrangements do not have a uniform multiplicity. Time is O(n! ) in the loose worst case, bounded by eight tiles, and frequency state plus stack is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let c={};for(let x of a)c[x]=(c[x]||0)+1;function f(n,last){if(n==a.length)return 1;let z=0;for(let x in c)if(c[x]&&x!=last){c[x]--;z+=f(n+1,x);c[x]++}return z}return f(0,\"\")}','function solve(a:string):number{const c=new Map<string,number>();for(const x of a)c.set(x,(c.get(x)||0)+1);function f(n:number,last:string):number{if(n===a.length)return 1;let z=0;for(const [x,v] of c)if(v&&x!==last){c.set(x,v-1);z+=f(n+1,x);c.set(x,v)}return z}return f(0,\"\")}','def solve(a):\n    c={}\n    for x in a:c[x]=c.get(x,0)+1\n    def f(n,last):\n        if n==len(a):return 1\n        z=0\n        for x in c:\n            if c[x] and x!=last:c[x]-=1;z+=f(n+1,x);c[x]+=1\n        return z\n    return f(0,\"\")','    static int solve(String a){int[]c=new int[26];for(char x:a.toCharArray())c[x-\'A\']++;return tiles(c,a.length(),0,-1);}\n    static int tiles(int[]c,int n,int used,int last){if(used==n)return 1;int z=0;for(int x=0;x<26;x++)if(c[x]>0&&x!=last){c[x]--;z+=tiles(c,n,used+1,x);c[x]++;}return z;}','int tiles(vector<int>&c,int n,int used,int last){if(used==n)return 1;int z=0;for(int x=0;x<26;x++)if(c[x]&&x!=last){c[x]--;z+=tiles(c,n,used+1,x);c[x]++;}return z;}\nint solve(string a){vector<int>c(26);for(char x:a)c[x-\'A\']++;return tiles(c,a.size(),0,-1);}','func solve(a string)int{c:=make([]int,26);for _,x:=range a{c[x-\'A\']++};var f func(int,int)int;f=func(n,last int)int{if n==len(a){return 1};z:=0;for x:=0;x<26;x++{if c[x]>0&&x!=last{c[x]--;z+=f(n+1,x);c[x]++}};return z};return f(0,-1)}'),
    tests:[{stdin:'AAB',expectedStdout:'1',isSample:true},{stdin:'A',expectedStdout:'1',isSample:true},{stdin:'AA',expectedStdout:'0'},{stdin:'ABC',expectedStdout:'6'},{stdin:'AABC',expectedStdout:'6'}] }),
  p({ ...base, slug:'subset-sums-divisible-by-target', title:'Subsets Divisible by the Marker', difficulty:'EASY', patternTags:['backtracking','subsets','modulo'], signatureId:'fn:ints,int->int', avgSolveSeconds:600,
    promptMarkdown:['Count nonempty subsets whose sum is divisible by positive integer `k`. Positions are distinct.','', '**Example**','', '```','input: 1 2 3','3','output: 3','```','', 'The qualifying subsets are `[3]`, `[1,2]`, and `[1,2,3]`. The list has 1 to 10 integers and k is positive; the named edge case is a sum of zero, which is divisible.'].join('\n'),
    editorialMarkdown:['## Include-or-skip with a nonempty leaf rule','', 'Every index offers an include or skip branch. Carry the running sum and a boolean saying whether any value has been chosen. At a leaf, test the remainder only for nonempty branches; this counts each positional subset exactly once.', '', 'The quiet mistake is forgetting the nonempty condition and counting the empty subset because zero is divisible by every k. It hides on samples that have other valid subsets. Time is O(2^n), bounded by n <= 10, with O(n) recursion space.'].join('\n'),
    referenceSolution:six('function solve(a,b){function f(i,s,t){if(i==a.length)return t&&s%b==0?1:0;return f(i+1,s,t)+f(i+1,s+a[i],1)}return f(0,0,0)}','function solve(a:number[],b:number):number{function f(i:number,s:number,t:boolean):number{return i===a.length?(t&&s%b===0?1:0):f(i+1,s,t)+f(i+1,s+a[i],true)}return f(0,0,false)}','def solve(a,b):\n    def f(i,s,t):\n        if i==len(a):return int(t and s%b==0)\n        return f(i+1,s,t)+f(i+1,s+a[i],True)\n    return f(0,0,False)','    static int solve(int[]a,int b){return div(a,b,0,0,false);}\n    static int div(int[]a,int b,int i,int s,boolean t){if(i==a.length)return t&&s%b==0?1:0;return div(a,b,i+1,s,t)+div(a,b,i+1,s+a[i],true);}','int div(vector<int>&a,int b,int i,int s,bool t){if(i==(int)a.size())return t&&s%b==0;return div(a,b,i+1,s,t)+div(a,b,i+1,s+a[i],1);}\nint solve(vector<int>a,int b){return div(a,b,0,0,0);}','func solve(a []int,b int)int{var f func(int,int,bool)int;f=func(i,s int,t bool)int{if i==len(a){if t&&s%b==0{return 1};return 0};return f(i+1,s,t)+f(i+1,s+a[i],true)};return f(0,0,false)}'),
    tests:[{stdin:'1 2 3\n3',expectedStdout:'3',isSample:true},{stdin:'5\n5',expectedStdout:'1',isSample:true},{stdin:'1 1\n3',expectedStdout:'0'},{stdin:'0 2\n2',expectedStdout:'3'},{stdin:'-1 1\n2',expectedStdout:'1'}] }),
  p({ ...base, slug:'exact-target-combinations-with-no-repeat-value', title:'Target Combinations With Unique Labels', difficulty:'MEDIUM', patternTags:['backtracking','combinations','deduplication'], signatureId:'fn:ints,int->int', avgSolveSeconds:660,
    promptMarkdown:['Count combinations adding to target `k`, using each input position at most once. Equal input values are interchangeable: combinations with the same value multiset count once.','', '**Example**','', '```','input: 1 1 2 3','3','output: 2','```','', 'The value combinations are `[1,2]` and `[3]`. Inputs are nonnegative, have 1 to 10 values, and the named edge case is duplicate values at the same recursion depth.'].join('\n'),
    editorialMarkdown:['## Sorted combination backtracking with duplicate skipping','', 'Sort the values, then choose the next position only after the previous chosen index. When a loop considers two equal values at the same depth, skip the later one because it would create the same value multiset. Values can be pruned once their sum exceeds target because inputs are nonnegative.', '', 'The quiet mistake is skipping every duplicate globally; a pair such as `[1,1]` is legal when two copies exist. Skip only siblings at one depth. Time is O(2^n), bounded by ten values, and stack plus chosen path is O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){a=a.slice().sort((x,y)=>x-y);function f(i,s){if(s==b)return 1;let z=0;for(let j=i;j<a.length;j++){if(j>i&&a[j]==a[j-1])continue;if(s+a[j]<=b)z+=f(j+1,s+a[j])}return z}return f(0,0)}','function solve(a:number[],b:number):number{a=a.slice().sort((x,y)=>x-y);function f(i:number,s:number):number{if(s===b)return 1;let z=0;for(let j=i;j<a.length;j++){if(j>i&&a[j]===a[j-1])continue;if(s+a[j]<=b)z+=f(j+1,s+a[j])}return z}return f(0,0)}','def solve(a,b):\n    a=sorted(a)\n    def f(i,s):\n        if s==b:return 1\n        z=0\n        for j in range(i,len(a)):\n            if j>i and a[j]==a[j-1]:continue\n            if s+a[j]<=b:z+=f(j+1,s+a[j])\n        return z\n    return f(0,0)','    static int solve(int[]a,int b){java.util.Arrays.sort(a);return comb(a,b,0,0);}\n    static int comb(int[]a,int b,int i,int s){if(s==b)return 1;int z=0;for(int j=i;j<a.length;j++){if(j>i&&a[j]==a[j-1])continue;if(s+a[j]<=b)z+=comb(a,b,j+1,s+a[j]);}return z;}','int comb(vector<int>&a,int b,int i,int s){if(s==b)return 1;int z=0;for(int j=i;j<(int)a.size();j++){if(j>i&&a[j]==a[j-1])continue;if(s+a[j]<=b)z+=comb(a,b,j+1,s+a[j]);}return z;}\nint solve(vector<int>a,int b){for(int i=0;i<(int)a.size();i++)for(int j=i+1;j<(int)a.size();j++)if(a[j]<a[i])swap(a[i],a[j]);return comb(a,b,0,0);}','func solve(a []int,b int)int{for i:=0;i<len(a);i++{for j:=i+1;j<len(a);j++{if a[j]<a[i]{a[i],a[j]=a[j],a[i]}}};var f func(int,int)int;f=func(i,s int)int{if s==b{return 1};z:=0;for j:=i;j<len(a);j++{if j>i&&a[j]==a[j-1]{continue};if s+a[j]<=b{z+=f(j+1,s+a[j])}};return z};return f(0,0)}'),
    tests:[{stdin:'1 1 2 3\n3',expectedStdout:'2',isSample:true},{stdin:'2 2 2\n4',expectedStdout:'1',isSample:true},{stdin:'1 2 3\n7',expectedStdout:'0'},{stdin:'0 0 1\n1',expectedStdout:'3'},{stdin:'5\n5',expectedStdout:'1'}] }),
  p({ ...base, slug:'queens-with-one-forbidden-column', title:'Queens With a Closed Column', difficulty:'HARD', patternTags:['backtracking','queens','constraints'], signatureId:'fn:int,int->int', avgSolveSeconds:900,
    promptMarkdown:['Place n queens on an n by n board, with no shared column or diagonal. Column k is closed in every row. Return the number of placements.','', '**Example**','', '```','input: 4','1','output: 0','```','', 'Every 4-queen placement uses every column exactly once, so closing column 1 leaves none. `1 <= n <= 8`; the named edge case is an invalid closed column, which returns 0.'].join('\n'),
    editorialMarkdown:['## Queens backtracking with a removed column','', 'Place one queen per row. Track occupied columns and both diagonal families, then try each currently safe column except the permanently closed one. A placement is valid because every earlier queen has registered exactly the attacks checked before each recursive call.', '', 'The quiet mistake is closing the column only in the first row. The rule removes that column from the whole board, so it must be rejected on every row. Time is O(n!), bounded by n <= 8, and the attack tables use O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a,b){if(b<0||b>=a)return 0;let c=Array(a).fill(0),d=Array(2*a).fill(0),e=Array(2*a).fill(0);function f(r){if(r==a)return 1;let z=0;for(let x=0;x<a;x++)if(x!=b&&!c[x]&&!d[r-x+a]&&!e[r+x]){c[x]=d[r-x+a]=e[r+x]=1;z+=f(r+1);c[x]=d[r-x+a]=e[r+x]=0}return z}return f(0)}','function solve(a:number,b:number):number{if(b<0||b>=a)return 0;const c=Array(a).fill(false),d=Array(2*a).fill(false),e=Array(2*a).fill(false);function f(r:number):number{if(r===a)return 1;let z=0;for(let x=0;x<a;x++)if(x!==b&&!c[x]&&!d[r-x+a]&&!e[r+x]){c[x]=d[r-x+a]=e[r+x]=true;z+=f(r+1);c[x]=d[r-x+a]=e[r+x]=false}return z}return f(0)}','def solve(a,b):\n    if b<0 or b>=a:return 0\n    c=[0]*a;d=[0]*(2*a);e=[0]*(2*a)\n    def f(r):\n        if r==a:return 1\n        z=0\n        for x in range(a):\n            if x!=b and not c[x] and not d[r-x+a] and not e[r+x]:c[x]=d[r-x+a]=e[r+x]=1;z+=f(r+1);c[x]=d[r-x+a]=e[r+x]=0\n        return z\n    return f(0)','    static int solve(int a,int b){if(b<0||b>=a)return 0;return queen(a,b,0,new boolean[a],new boolean[2*a],new boolean[2*a]);}\n    static int queen(int n,int ban,int r,boolean[]c,boolean[]d,boolean[]e){if(r==n)return 1;int z=0;for(int x=0;x<n;x++)if(x!=ban&&!c[x]&&!d[r-x+n]&&!e[r+x]){c[x]=d[r-x+n]=e[r+x]=true;z+=queen(n,ban,r+1,c,d,e);c[x]=d[r-x+n]=e[r+x]=false;}return z;}','int queen(int n,int ban,int r,vector<char>&c,vector<char>&d,vector<char>&e){if(r==n)return 1;int z=0;for(int x=0;x<n;x++)if(x!=ban&&!c[x]&&!d[r-x+n]&&!e[r+x]){c[x]=d[r-x+n]=e[r+x]=1;z+=queen(n,ban,r+1,c,d,e);c[x]=d[r-x+n]=e[r+x]=0;}return z;}\nint solve(int a,int b){if(b<0||b>=a)return 0;vector<char>c(a),d(2*a),e(2*a);return queen(a,b,0,c,d,e);}','func solve(a int,b int)int{if b<0||b>=a{return 0};c:=make([]bool,a);d:=make([]bool,2*a);e:=make([]bool,2*a);var f func(int)int;f=func(r int)int{if r==a{return 1};z:=0;for x:=0;x<a;x++{if x!=b&&!c[x]&&!d[r-x+a]&&!e[r+x]{c[x],d[r-x+a],e[r+x]=true,true,true;z+=f(r+1);c[x],d[r-x+a],e[r+x]=false,false,false}};return z};return f(0)}'),
    tests:[{stdin:'4\n1',expectedStdout:'0',isSample:true},{stdin:'1\n0',expectedStdout:'0',isSample:true},{stdin:'4\n0',expectedStdout:'0'},{stdin:'2\n1',expectedStdout:'0'},{stdin:'4\n4',expectedStdout:'0'}] }),
  p({ ...base, slug:'palindrome-subsets-with-exact-length', title:'Palindromic Picks of One Length', difficulty:'MEDIUM', patternTags:['backtracking','subsets','palindrome'], signatureId:'fn:string,int->int', avgSolveSeconds:720,
    promptMarkdown:['Count subsequences of exactly k characters that read the same forward and backward. Equal characters at different positions are distinct choices.','', '**Example**','', '```','input: abca','3','output: 2','```','', 'The two choices are `aba` and `aca`. The string has 1 to 10 lowercase letters and k is positive; the named edge case is k=1, when every position counts.'].join('\n'),
    editorialMarkdown:['## Subsequence backtracking with a leaf palindrome','', 'Include or skip each character while building one candidate subsequence. Once the length reaches k, compare its mirrored characters and count it only when all pairs match. Because decisions follow input indices, equal visible subsequences from different positions still count separately.', '', 'The quiet mistake is deduplicating candidate strings; the task counts positional choices, not distinct spellings. Stop a branch at length k so it cannot add useless characters. Time is O(2^n), bounded by ten letters, and the path and recursion use O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a,b){function f(i,s){if(s.length==b){for(let l=0,r=b-1;l<r;l++,r--)if(s[l]!=s[r])return 0;return 1}if(i==a.length)return 0;return f(i+1,s)+f(i+1,s+a[i])}return f(0,\"\")}','function solve(a:string,b:number):number{function f(i:number,s:string):number{if(s.length===b){for(let l=0,r=b-1;l<r;l++,r--)if(s[l]!==s[r])return 0;return 1}return i===a.length?0:f(i+1,s)+f(i+1,s+a[i])}return f(0,\"\")}','def solve(a,b):\n    def f(i,s):\n        if len(s)==b:return int(s==s[::-1])\n        if i==len(a):return 0\n        return f(i+1,s)+f(i+1,s+a[i])\n    return f(0,\"\")','    static int solve(String a,int b){return pal(a,b,0,\"\");}\n    static int pal(String a,int b,int i,String s){if(s.length()==b){for(int l=0,r=b-1;l<r;l++,r--)if(s.charAt(l)!=s.charAt(r))return 0;return 1;}return i==a.length()?0:pal(a,b,i+1,s)+pal(a,b,i+1,s+a.charAt(i));}','int pal(string&a,int b,int i,string s){if((int)s.size()==b){for(int l=0,r=b-1;l<r;l++,r--)if(s[l]!=s[r])return 0;return 1;}return i==(int)a.size()?0:pal(a,b,i+1,s)+pal(a,b,i+1,s+a[i]);}\nint solve(string a,int b){return pal(a,b,0,\"\");}','func solve(a string,b int)int{var f func(int,string)int;f=func(i int,s string)int{if len(s)==b{for l,r:=0,b-1;l<r;l,r=l+1,r-1{if s[l]!=s[r]{return 0}};return 1};if i==len(a){return 0};return f(i+1,s)+f(i+1,s+string(a[i]))};return f(0,\"\")}'),
    tests:[{stdin:'abca\n3',expectedStdout:'2',isSample:true},{stdin:'z\n1',expectedStdout:'1',isSample:true},{stdin:'abc\n2',expectedStdout:'0'},{stdin:'aaa\n2',expectedStdout:'3'},{stdin:'abba\n4',expectedStdout:'1'}] }),
];
