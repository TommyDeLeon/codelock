import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-024` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_024_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "sum-of-runic-values",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Legacy Satellite Signals",
    patternTags: ["string","math","base-conversion"],
    signatureId: "fn:string,string->int",
    avgSolveSeconds: 400,
    promptMarkdown: "You are analyzing transmissions from a legacy satellite. You receive two signals `a` and `b` made of lowercase English letters from 'a' to 'j'.\n\nEach letter maps to a numeric code from 0 to 9, such that 'a' = 0, 'b' = 1, ..., 'j' = 9.\n\nDetermine the full numerical value of each signal by converting its letters to digits, and return the sum of these two values.\n\n**Constraints**\n- `1 <= a.length, b.length <= 8`\n- `a` and `b` consist only of characters from 'a' to 'j'.\n\n**Example 1**\n```\ninput:\nacb\ncba\noutput: 231\n```\nExplanation: Signal `a` converts to \"021\", which is 21. Signal `b` converts to \"210\". Their total sum is 231.\n\n**Example 2**\n```\ninput:\na\na\noutput: 0\n```\nExplanation: Both signals convert to 0, resulting in a sum of 0.\n\n**Example 3**\n```\ninput:\nj\nj\noutput: 18\n```\nExplanation: Both signals evaluate to 9, so the sum is 18.\n\n**Follow-up:** How would you handle very large strings that exceed the maximum integer limit?",
    editorialMarkdown: "## Sum of Runic Values\n\nWe need to convert both strings into numbers where 'a' corresponds to 0, 'b' to 1, and so on. We can iterate over the characters of each string, compute the numeric value by subtracting the ASCII value of 'a', and construct the number using base-10 positional arithmetic. After converting both strings, we return their sum.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N + M) where N and M are the lengths of the two strings.\n- **Space Complexity:** mathcal{O}(1) since we are accumulating the values into integer variables.\n\n**Common Trap:**\nA common mistake is parsing the strings individually into large strings of digits and using built-in number conversion functions, which might lead to integer overflow if the string lengths are large (though constraints here keep them within 32-bit limits).",
    referenceSolution: {
      JAVASCRIPT: "function solve(a, b) {\n    const val = (s) => parseInt(s.split('').map(c => c.charCodeAt(0) - 97).join(''), 10) || 0;\n    return val(a) + val(b);\n}",
      TYPESCRIPT: "function solve(a: string, b: string): number {\n    const val = (s: string) => parseInt(s.split('').map(c => String(c.charCodeAt(0) - 97)).join(''), 10) || 0;\n    return val(a) + val(b);\n}",
      PYTHON: "def solve(a, b):\n    def val(s):\n        return int(\"\".join(str(ord(c) - 97) for c in s)) if s else 0\n    return val(a) + val(b)",
      JAVA: "    static int solve(String a, String b) {\n        long v1 = 0, v2 = 0;\n        for (char c : a.toCharArray()) v1 = v1 * 10 + (c - 'a');\n        for (char c : b.toCharArray()) v2 = v2 * 10 + (c - 'a');\n        return (int)(v1 + v2);\n    }",
      CPP: "#include <string>\nint solve(std::string a, std::string b) {\n    long long v1 = 0, v2 = 0;\n    for (char c : a) v1 = v1 * 10 + (c - 'a');\n    for (char c : b) v2 = v2 * 10 + (c - 'a');\n    return v1 + v2;\n}",
      GO: "func solve(a string, b string) int {\n    v1, v2 := 0, 0\n    for _, c := range a {\n        v1 = v1 * 10 + int(c - 'a')\n    }\n    for _, c := range b {\n        v2 = v2 * 10 + int(c - 'a')\n    }\n    return v1 + v2\n}",
    },
    tests: [
      { stdin: "acb\ncba", expectedStdout: "231", isSample: true },
      { stdin: "a\na", expectedStdout: "0", isSample: true },
      { stdin: "j\nj", expectedStdout: "18" },
      { stdin: "ba\nba", expectedStdout: "20" },
      { stdin: "ab\nba", expectedStdout: "11" },
      { stdin: "jj\na", expectedStdout: "99" },
      { stdin: "a\njj", expectedStdout: "99" },
      { stdin: "abcdefgh\nhgfedcba", expectedStdout: "77777777" },
    ],
  }),

  p({
    ...base,
    slug: "compare-flora-samples",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Material Composition Check",
    patternTags: ["string","hash-map","counting"],
    signatureId: "fn:string,string->bool",
    avgSolveSeconds: 400,
    promptMarkdown: "You are a quality assurance engineer comparing the material makeup of two fabrics. You are provided with two strings `a` and `b` representing the materials, consisting of lowercase English letters.\n\nTwo fabrics are deemed \"similar\" if the count of every material type (from 'a' to 'z') in fabric `a` and fabric `b` differs by at most 3.\n\nReturn `true` if the two fabrics are similar, and `false` otherwise.\n\n**Constraints**\n- `1 <= a.length, b.length <= 100`\n- `a` and `b` consist only of lowercase English letters.\n\n**Example 1**\n```\ninput:\naaaaa\na\noutput: false\n```\nExplanation: The material 'a' appears 5 times in fabric `a` and 1 time in fabric `b`. The difference is 4, which is strictly greater than 3, making them not similar.\n\n**Example 2**\n```\ninput:\na\na\noutput: true\n```\nExplanation: The fabrics are identical, so the difference for all materials is 0.\n\n**Example 3**\n```\ninput:\nabc\ndef\noutput: true\n```\nExplanation: The frequencies of 'a', 'b', 'c', 'd', 'e', and 'f' all differ by 1, which is at most 3.\n\n**Follow-up:** How would you implement this if the characters could be any Unicode character?",
    editorialMarkdown: "## Comparing Frequencies\n\nWe are looking to see if the frequency of every letter differs by at most 3. We can use an array of size 26 to keep track of the counts. As we iterate over the first string, we increment the count for each character. As we iterate over the second string, we decrement the count for each character. Finally, we iterate over the count array and return false if the absolute value of any count is strictly greater than 3.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N + M) where N and M are the lengths of the two strings.\n- **Space Complexity:** mathcal{O}(1) since the alphabet size is constant (26).\n\n**Common Trap:**\nChecking only the characters present in the first string without ensuring the characters present only in the second string are also verified. Using an array of size 26 neatly handles all letters.",
    referenceSolution: {
      JAVASCRIPT: "function solve(a, b) {\n    let counts = new Array(26).fill(0);\n    for (let c of a) counts[c.charCodeAt(0) - 97]++;\n    for (let c of b) counts[c.charCodeAt(0) - 97]--;\n    return counts.every(x => Math.abs(x) <= 3);\n}",
      TYPESCRIPT: "function solve(a: string, b: string): boolean {\n    let counts = new Array(26).fill(0);\n    for (let i = 0; i < a.length; i++) counts[a.charCodeAt(i) - 97]++;\n    for (let i = 0; i < b.length; i++) counts[b.charCodeAt(i) - 97]--;\n    return counts.every(x => Math.abs(x) <= 3);\n}",
      PYTHON: "def solve(a, b):\n    counts = [0] * 26\n    for c in a: counts[ord(c) - 97] += 1\n    for c in b: counts[ord(c) - 97] -= 1\n    return all(abs(x) <= 3 for x in counts)",
      JAVA: "    static boolean solve(String a, String b) {\n        int[] counts = new int[26];\n        for (char c : a.toCharArray()) counts[c - 'a']++;\n        for (char c : b.toCharArray()) counts[c - 'a']--;\n        for (int x : counts) if (Math.abs(x) > 3) return false;\n        return true;\n    }",
      CPP: "#include <string>\n#include <vector>\n#include <cmath>\nbool solve(std::string a, std::string b) {\n    std::vector<int> counts(26, 0);\n    for (char c : a) counts[c - 'a']++;\n    for (char c : b) counts[c - 'a']--;\n    for (int x : counts) if (std::abs(x) > 3) return false;\n    return true;\n}",
      GO: "func solve(a string, b string) bool {\n    counts := make([]int, 26)\n    for _, c := range a { counts[c - 'a']++ }\n    for _, c := range b { counts[c - 'a']-- }\n    for _, x := range counts {\n        if x < -3 || x > 3 { return false }\n    }\n    return true\n}",
    },
    tests: [
      { stdin: "aaaaa\na", expectedStdout: "false", isSample: true },
      { stdin: "a\na", expectedStdout: "true", isSample: true },
      { stdin: "abc\ndef", expectedStdout: "true" },
      { stdin: "aaaa\na", expectedStdout: "true" },
      { stdin: "abcde\nabcde", expectedStdout: "true" },
      { stdin: "zzzz\na", expectedStdout: "false" },
      { stdin: "bbbb\nb", expectedStdout: "true" },
      { stdin: "aabb\nbbcc", expectedStdout: "true" },
    ],
  }),

  p({
    ...base,
    slug: "batch-data-sums",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Batch Data Sums",
    patternTags: ["array","grouping","simulation"],
    signatureId: "fn:ints,int->ints",
    avgSolveSeconds: 400,
    promptMarkdown: "You are processing batch data from a set of sensors. You are given an array of integers `a` representing the data points, and an integer `b` representing the batch size.\n\nYou need to partition the data points into contiguous batches of size `b`, and calculate the total sum of each batch. If the final batch has fewer than `b` elements, simply sum the remaining elements.\n\nReturn an array containing the sums of each batch in order.\n\n**Constraints**\n- `0 <= a.length <= 100`\n- `1 <= b <= 100`\n- `-100 <= a[i] <= 100`\n\n**Example 1**\n```\ninput:\n1 2 3 4\n2\noutput: 3 7\n```\nExplanation: The batches are [1, 2] and [3, 4]. Their sums are 3 and 7 respectively.\n\n**Example 2**\n```\ninput:\n1 2 3\n3\noutput: 6\n```\nExplanation: There is only one batch [1, 2, 3], which sums to 6.\n\n**Example 3**\n```\ninput:\n1 2 3\n2\noutput: 3 3\n```\nExplanation: The first batch is [1, 2] summing to 3. The second batch is [3] summing to 3.\n\n**Follow-up:** Can you solve this elegantly using slicing operations in your language if they are supported?",
    editorialMarkdown: "## Batch Data Sums\n\nWe are tasked with grouping elements of an array into consecutive chunks of size `b`, and finding the sum of each chunk. We can achieve this by iterating over the array with a step size of `b`. In each step, we calculate the sum of the elements in the current chunk boundary. \n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the number of elements in the array, as each element is visited once and added to a sum.\n- **Space Complexity:** mathcal{O}(N/B) where B is the batch size, to store the resulting sums.\n\n**Common Trap:**\nA common pitfall is forgetting to handle the final chunk when the array's length is not perfectly divisible by `b`, which can result in an out-of-bounds error if not properly bounded.",
    referenceSolution: {
      JAVASCRIPT: "function solve(a, b) {\n    let res = [];\n    for (let i = 0; i < a.length; i += b) {\n        let sum = 0;\n        for (let j = 0; j < b && i + j < a.length; j++) sum += a[i + j];\n        res.push(sum);\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(a: number[], b: number): number[] {\n    let res: number[] = [];\n    for (let i = 0; i < a.length; i += b) {\n        let sum = 0;\n        for (let j = 0; j < b && i + j < a.length; j++) sum += a[i + j];\n        res.push(sum);\n    }\n    return res;\n}",
      PYTHON: "def solve(a, b):\n    return [sum(a[i:i+b]) for i in range(0, len(a), b)]",
      JAVA: "    static int[] solve(int[] a, int b) {\n        int n = a.length;\n        if (n == 0) return new int[0];\n        int chunks = (n + b - 1) / b;\n        int[] res = new int[chunks];\n        for (int i = 0; i < n; i++) res[i / b] += a[i];\n        return res;\n    }",
      CPP: "#include <vector>\nstd::vector<int> solve(std::vector<int> a, int b) {\n    std::vector<int> res;\n    if (a.empty()) return res;\n    for (size_t i = 0; i < a.size(); i += b) {\n        int sum = 0;\n        for (size_t j = 0; j < (size_t)b && i + j < a.size(); j++) {\n            sum += a[i + j];\n        }\n        res.push_back(sum);\n    }\n    return res;\n}",
      GO: "func solve(a []int, b int) []int {\n    var res []int\n    for i := 0; i < len(a); i += b {\n        sum := 0\n        for j := 0; j < b && i+j < len(a); j++ {\n            sum += a[i+j]\n        }\n        res = append(res, sum)\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "1 2 3 4\n2", expectedStdout: "3 7", isSample: true },
      { stdin: "1 2 3\n3", expectedStdout: "6", isSample: true },
      { stdin: "1 2 3\n2", expectedStdout: "3 3" },
      { stdin: "\n2", expectedStdout: "" },
      { stdin: "10\n1", expectedStdout: "10" },
      { stdin: "1 1 1 1 1\n1", expectedStdout: "1 1 1 1 1" },
      { stdin: "-1 2 -3 4\n2", expectedStdout: "1 1" },
      { stdin: "1 2 3 4 5\n10", expectedStdout: "15" },
    ],
  }),

  p({
    ...base,
    slug: "verify-trade-route",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Seamless Server Chain",
    patternTags: ["string","simulation","circular"],
    signatureId: "fn:string->bool",
    avgSolveSeconds: 400,
    promptMarkdown: "You are a network engineer verifying a daisy-chain connection of servers. The chain is provided as a single string `a` containing space-separated server names made up of lowercase English letters.\n\nA connection chain is considered \"seamless\" if:\n1. The final letter of every server name matches the starting letter of the subsequent server name.\n2. The final letter of the final server name matches the starting letter of the initial server name.\n\nReturn `true` if the given string forms a seamless chain, and `false` otherwise.\n\n**Constraints**\n- `1 <= a.length <= 500`\n- `a` consists of lowercase English letters and single spaces.\n- `a` does not begin or end with a space.\n\n**Example 1**\n```\ninput:\nhello oval lisp\noutput: false\n```\nExplanation: \"lisp\" ends with 'p', which does not match the 'h' at the beginning of \"hello\".\n\n**Example 2**\n```\ninput:\nhello oval lisp path\noutput: true\n```\nExplanation: Every adjacent boundary matches ('o'=='o', 'l'=='l', 'p'=='p'), and the final letter of \"path\" ('h') matches the starting letter of \"hello\" ('h').\n\n**Example 3**\n```\ninput:\na\noutput: true\n```\nExplanation: A single server chain is seamless if its first and last characters match. Here, 'a' == 'a'.\n\n**Follow-up:** Can you perform this check in exactly one pass without splitting the string into an array of words?",
    editorialMarkdown: "## Verify Trade Route\n\nTo check if the string represents a valid continuous loop of words, we only need to inspect the boundaries between words and the ends of the string. A string meets the condition if the first and last characters are equal, and for every space character, the character immediately before it is equal to the character immediately after it.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the length of the string, since we iterate over the characters exactly once.\n- **Space Complexity:** mathcal{O}(1) because we only need to maintain loop indices and do not allocate additional arrays.\n\n**Common Trap:**\nSplitting the string into an array of words instead of checking characters in-place. While splitting works, it takes extra mathcal{O}(N) memory, which is unnecessary and slightly slower.",
    referenceSolution: {
      JAVASCRIPT: "function solve(a) {\n    if (!a) return true;\n    if (a[0] !== a[a.length - 1]) return false;\n    for (let i = 0; i < a.length; i++) {\n        if (a[i] === ' ' && a[i-1] !== a[i+1]) return false;\n    }\n    return true;\n}",
      TYPESCRIPT: "function solve(a: string): boolean {\n    if (!a) return true;\n    if (a[0] !== a[a.length - 1]) return false;\n    for (let i = 0; i < a.length; i++) {\n        if (a[i] === ' ' && a[i-1] !== a[i+1]) return false;\n    }\n    return true;\n}",
      PYTHON: "def solve(a):\n    if not a: return True\n    if a[0] != a[-1]: return False\n    for i in range(len(a)):\n        if a[i] == ' ' and a[i-1] != a[i+1]:\n            return False\n    return True",
      JAVA: "    static boolean solve(String a) {\n        if (a.isEmpty()) return true;\n        if (a.charAt(0) != a.charAt(a.length() - 1)) return false;\n        for (int i = 0; i < a.length(); i++) {\n            if (a.charAt(i) == ' ' && a.charAt(i-1) != a.charAt(i+1)) return false;\n        }\n        return true;\n    }",
      CPP: "#include <string>\nbool solve(std::string a) {\n    if (a.empty()) return true;\n    if (a.front() != a.back()) return false;\n    for (size_t i = 0; i < a.size(); i++) {\n        if (a[i] == ' ' && a[i-1] != a[i+1]) return false;\n    }\n    return true;\n}",
      GO: "func solve(a string) bool {\n    if len(a) == 0 { return true }\n    if a[0] != a[len(a)-1] { return false }\n    for i := 0; i < len(a); i++ {\n        if a[i] == ' ' && a[i-1] != a[i+1] {\n            return false\n        }\n    }\n    return true\n}",
    },
    tests: [
      { stdin: "hello oval lisp", expectedStdout: "false", isSample: true },
      { stdin: "hello oval lisp path", expectedStdout: "true", isSample: true },
      { stdin: "a", expectedStdout: "true" },
      { stdin: "ab", expectedStdout: "false" },
      { stdin: "ab ba", expectedStdout: "true" },
      { stdin: "abc cde efa", expectedStdout: "true" },
      { stdin: "abc cde efg", expectedStdout: "false" },
      { stdin: "a a a", expectedStdout: "true" },
    ],
  }),

  p({
    ...base,
    slug: "active-guilds",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Active Guilds",
    patternTags: ["array","hash-map","counting"],
    signatureId: "fn:strings->strings",
    avgSolveSeconds: 400,
    promptMarkdown: "In a massive multiplayer game, players are registering for different adventure guilds. You are given an array of strings `a` where each element represents the guild chosen by a newly registered player. \n\nA guild is considered \"active\" if it has at least 5 registered players.\n\nReturn an array containing the names of all active guilds. The returned array must be sorted alphabetically.\n\n**Constraints**\n- `0 <= a.length <= 1000`\n- `a[i]` consists of uppercase English letters.\n- `a[i].length <= 20`\n\n**Example 1**\n```\ninput:\nA A A A A\noutput: A\n```\nExplanation: Guild \"A\" has exactly 5 members, so it is active.\n\n**Example 2**\n```\ninput:\nA A A A\noutput: \n```\nExplanation: Guild \"A\" only has 4 members, so there are no active guilds.\n\n**Example 3**\n```\ninput:\nB B B B B A A A A A\noutput: A B\n```\nExplanation: Both \"A\" and \"B\" have 5 members. The result must be sorted alphabetically, so \"A\" comes before \"B\".\n\n**Follow-up:** What data structure would you use if you needed to maintain the active guilds in real-time as millions of players register continuously?",
    editorialMarkdown: "## Active Guilds\n\nTo determine which guilds are highly active, we can count the frequency of each string in the input array. A hash map is perfectly suited for this. We iterate through the array, updating the counts in our map. Finally, we iterate through the map, select the guilds with a frequency of at least 5, and sort the result lexicographically before returning.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N log N) where N is the number of elements in the array. The dominant factor is sorting the resulting array, though hashing takes mathcal{O}(N).\n- **Space Complexity:** mathcal{O}(N) to store the frequencies in the hash map and the resulting array.\n\n**Common Trap:**\nA very common issue is returning the list without sorting it. Dictionaries and hash maps often lack guaranteed ordering, meaning tests will fail if lexicographical sorting is not explicitly applied.",
    referenceSolution: {
      JAVASCRIPT: "function solve(a) {\n    let counts = {};\n    for (let g of a) counts[g] = (counts[g] || 0) + 1;\n    let res = Object.keys(counts).filter(g => counts[g] >= 5);\n    res.sort();\n    return res;\n}",
      TYPESCRIPT: "function solve(a: string[]): string[] {\n    let counts: Record<string, number> = {};\n    for (let g of a) counts[g] = (counts[g] || 0) + 1;\n    let res = Object.keys(counts).filter(g => counts[g] >= 5);\n    res.sort();\n    return res;\n}",
      PYTHON: "def solve(a):\n    counts = {}\n    for g in a:\n        counts[g] = counts.get(g, 0) + 1\n    res = [g for g, c in counts.items() if c >= 5]\n    return sorted(res)",
      JAVA: "    static String[] solve(String[] a) {\n        java.util.Map<String, Integer> counts = new java.util.HashMap<>();\n        for (String g : a) counts.put(g, counts.getOrDefault(g, 0) + 1);\n        java.util.List<String> res = new java.util.ArrayList<>();\n        for (java.util.Map.Entry<String, Integer> e : counts.entrySet()) {\n            if (e.getValue() >= 5) res.add(e.getKey());\n        }\n        java.util.Collections.sort(res);\n        return res.toArray(new String[0]);\n    }",
      CPP: "#include <vector>\n#include <string>\n#include <map>\n#include <algorithm>\nstd::vector<std::string> solve(std::vector<std::string> a) {\n    std::map<std::string, int> counts;\n    for (const auto& g : a) counts[g]++;\n    std::vector<std::string> res;\n    for (const auto& kv : counts) {\n        if (kv.second >= 5) res.push_back(kv.first);\n    }\n    return res;\n}",
      GO: "func solve(a []string) []string {\n    counts := make(map[string]int)\n    for _, g := range a { counts[g]++ }\n    var res []string\n    for g, c := range counts {\n        if c >= 5 {\n            res = append(res, g)\n        }\n    }\n    for i := 0; i < len(res); i++ {\n        for j := i + 1; j < len(res); j++ {\n            if res[i] > res[j] {\n                res[i], res[j] = res[j], res[i]\n            }\n        }\n    }\n    if res == nil { return []string{} }\n    return res\n}",
    },
    tests: [
      { stdin: "A A A A A", expectedStdout: "A", isSample: true },
      { stdin: "A A A A", expectedStdout: "", isSample: true },
      { stdin: "A A A A A B B B B B", expectedStdout: "A B" },
      { stdin: "B B B B B A A A A A", expectedStdout: "A B" },
      { stdin: "X X X X X X", expectedStdout: "X" },
      { stdin: "A B C D E", expectedStdout: "" },
      { stdin: "", expectedStdout: "" },
      { stdin: "A A A A A Z Z Z Z Z M M M M M", expectedStdout: "A M Z" },
    ],
  }),

  p({
    ...base,
    slug: "identify-star-systems",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Identify Star Systems",
    patternTags: ["math","geometry","array"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 400,
    promptMarkdown: "You are plotting hyperspace jump points between star systems. You are given an array of exactly 3 integers, representing the distances between three interconnected planets.\n\nYou must categorize the geometric shape formed by these three planets:\n- Return `1` if the three planets form an **equilateral** triangle (all three distances are equal).\n- Return `2` if they form an **isosceles** triangle (exactly two distances are equal).\n- Return `3` if they form a **scalene** triangle (all three distances are different).\n- Return `0` if the distances do not form a valid triangle (the sum of any two distances is less than or equal to the third distance).\n\n**Constraints**\n- `a.length == 3`\n- `1 <= a[i] <= 1000`\n\n**Example 1**\n```\ninput:\n3 3 3\noutput: 1\n```\nExplanation: All distances are 3, forming an equilateral triangle.\n\n**Example 2**\n```\ninput:\n3 3 4\noutput: 2\n```\nExplanation: Two distances are 3, and one is 4. This forms an isosceles triangle.\n\n**Example 3**\n```\ninput:\n1 2 3\noutput: 0\n```\nExplanation: The sum of the first two distances (1 + 2) is equal to the third distance (3). This is a straight line, not a valid triangle.\n\n**Follow-up:** Can you write a succinct condition to determine if any three lengths form a valid triangle without sorting?",
    editorialMarkdown: "## Identify Star Systems\n\nWe are given the pairwise distances between 3 planets and need to categorize the triangle they form. First, we must verify that they actually form a valid triangle using the triangle inequality theorem: the sum of the lengths of any two sides must be strictly greater than the length of the remaining side. \n\nIf it's valid, we check how many sides are equal to determine whether it is equilateral (all 3 sides equal), isosceles (exactly 2 sides equal, or at least 2), or scalene (all sides different).\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(1) as there are exactly 3 sides to check every time.\n- **Space Complexity:** mathcal{O}(1) since no additional memory scaling with input is required.\n\n**Common Trap:**\nA very common trap is to check for isosceles or equilateral equality before ensuring that the side lengths actually form a valid triangle (e.g., sides [1, 1, 3] are equal in two spots but do not form a triangle since 1+1 <= 3). Always check the triangle inequality first.",
    referenceSolution: {
      JAVASCRIPT: "function solve(a) {\n    if (a.length !== 3) return 0;\n    let x = a[0], y = a[1], z = a[2];\n    if (x + y <= z || x + z <= y || y + z <= x) return 0;\n    if (x === y && y === z) return 1;\n    if (x === y || y === z || x === z) return 2;\n    return 3;\n}",
      TYPESCRIPT: "function solve(a: number[]): number {\n    if (a.length !== 3) return 0;\n    let x = a[0], y = a[1], z = a[2];\n    if (x + y <= z || x + z <= y || y + z <= x) return 0;\n    if (x === y && y === z) return 1;\n    if (x === y || y === z || x === z) return 2;\n    return 3;\n}",
      PYTHON: "def solve(a):\n    if len(a) != 3: return 0\n    x, y, z = a\n    if x + y <= z or x + z <= y or y + z <= x: return 0\n    if x == y == z: return 1\n    if x == y or y == z or x == z: return 2\n    return 3",
      JAVA: "    static int solve(int[] a) {\n        if (a.length != 3) return 0;\n        int x = a[0], y = a[1], z = a[2];\n        if (x + y <= z || x + z <= y || y + z <= x) return 0;\n        if (x == y && y == z) return 1;\n        if (x == y || y == z || x == z) return 2;\n        return 3;\n    }",
      CPP: "#include <vector>\nint solve(std::vector<int> a) {\n    if (a.size() != 3) return 0;\n    long long x = a[0], y = a[1], z = a[2];\n    if (x + y <= z || x + z <= y || y + z <= x) return 0;\n    if (x == y && y == z) return 1;\n    if (x == y || y == z || x == z) return 2;\n    return 3;\n}",
      GO: "func solve(a []int) int {\n    if len(a) != 3 { return 0 }\n    x, y, z := a[0], a[1], a[2]\n    if x + y <= z || x + z <= y || y + z <= x { return 0 }\n    if x == y && y == z { return 1 }\n    if x == y || y == z || x == z { return 2 }\n    return 3\n}",
    },
    tests: [
      { stdin: "3 3 3", expectedStdout: "1", isSample: true },
      { stdin: "3 3 4", expectedStdout: "2", isSample: true },
      { stdin: "1 2 3", expectedStdout: "0", isSample: true },
      { stdin: "3 4 5", expectedStdout: "3" },
      { stdin: "1 1 3", expectedStdout: "0" },
      { stdin: "10 10 10", expectedStdout: "1" },
      { stdin: "10 12 10", expectedStdout: "2" },
      { stdin: "5 12 13", expectedStdout: "3" },
    ],
  }),
];
