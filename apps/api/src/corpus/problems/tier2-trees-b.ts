import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'TREES', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_2_TREES_B_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'tree-even-depth-value-sum', title:'Add the Even Floors', difficulty:'EASY', patternTags:['trees','dfs','depth'], signatureId:'fn:tree->int', avgSolveSeconds:540,
    promptMarkdown:['Return the sum of values stored at even depths, where the root has depth 0.','', '**Example**','', '```','input: 4 2 7 1 3 6 9','output: 23','```','', 'Depths 0 and 2 contribute `4 + 1 + 3 + 6 + 9`. The tree may be empty, which returns 0; negative values are added normally.'].join('\n'),
    editorialMarkdown:['## Depth-carrying depth-first search','', 'Pass the current depth alongside each recursive visit. A node contributes exactly when its depth has remainder zero after division by two, while both children are visited at the next depth. This directly mirrors the level definition without needing to build level arrays.', '', 'The quiet mistake is calling the children with the same depth, which makes every node look like it belongs to the root level. Increment before descending. Time is O(n), bounded by the number of nodes visited once; recursion space is O(h), bounded by tree height.'].join('\n'),
    referenceSolution:six('function solve(a){function f(n,d){return !n?0:(d%2===0?n.val:0)+f(n.left,d+1)+f(n.right,d+1)}return f(a,0)}','function solve(a:TreeNode|null):number{function f(n:TreeNode|null,d:number):number{return !n?0:(d%2===0?n.val:0)+f(n.left,d+1)+f(n.right,d+1)}return f(a,0)}','def solve(a):\n    def f(n, d): return 0 if n is None else (n.val if d % 2 == 0 else 0) + f(n.left, d + 1) + f(n.right, d + 1)\n    return f(a, 0)','    static int solve(TreeNode a){return evenSum(a,0);}\n    static int evenSum(TreeNode n,int d){return n==null?0:(d%2==0?n.val:0)+evenSum(n.left,d+1)+evenSum(n.right,d+1);}','int evenSum(TreeNode*n,int d){return !n?0:(d%2==0?n->val:0)+evenSum(n->left,d+1)+evenSum(n->right,d+1);}\nint solve(TreeNode*a){return evenSum(a,0);}','func evenSum(n *TreeNode,d int) int {if n==nil{return 0};v:=0;if d%2==0{v=n.Val};return v+evenSum(n.Left,d+1)+evenSum(n.Right,d+1)}\nfunc solve(a *TreeNode) int{return evenSum(a,0)}'),
    tests:[{stdin:'4 2 7 1 3 6 9',expectedStdout:'23',isSample:true},{stdin:'5',expectedStdout:'5',isSample:true},{stdin:'1 2 3 4 null null 5',expectedStdout:'10'},{stdin:'',expectedStdout:'0'}] }),
  p({ ...base, slug:'tree-count-leaves-divisible-by-bound', title:'Leaves Divisible by the Bound', difficulty:'EASY', patternTags:['trees','dfs','leaves'], signatureId:'fn:tree,int->int', avgSolveSeconds:540,
    promptMarkdown:['Return how many leaf values are divisible by k. A leaf has no left or right child.','', '**Example**','', '```','input: 8 3 10 6 7 null 15','3','output: 2','```','', 'Only leaves `6`, `7`, and `15` are checked, so `6` and `15` count. k is positive; an empty tree returns 0.'].join('\n'),
    editorialMarkdown:['## Leaf test during a tree walk','', 'Visit every node, but perform the divisibility test only after proving that both child pointers are absent. Internal values are deliberately irrelevant even if they are divisible by k. This one traversal sees every possible leaf and counts each exactly once.', '', 'The quiet mistake is testing every divisible node, which is invisible on a tree whose divisible values happen to be leaves. Check the leaf condition first. Time is O(n), bounded by all tree nodes; recursion space is O(h), bounded by its height.'].join('\n'),
    referenceSolution:six('function solve(a,b){function f(n){if(!n)return 0;if(!n.left&&!n.right)return n.val%b===0?1:0;return f(n.left)+f(n.right)}return f(a)}','function solve(a:TreeNode|null,b:number):number{function f(n:TreeNode|null):number{if(!n)return 0;if(!n.left&&!n.right)return n.val%b===0?1:0;return f(n.left)+f(n.right)}return f(a)}','def solve(a, b):\n    def f(n):\n        if n is None: return 0\n        if n.left is None and n.right is None: return 1 if n.val % b == 0 else 0\n        return f(n.left) + f(n.right)\n    return f(a)','    static int solve(TreeNode a,int b){return leafDiv(a,b);}\n    static int leafDiv(TreeNode n,int b){if(n==null)return 0;if(n.left==null&&n.right==null)return n.val%b==0?1:0;return leafDiv(n.left,b)+leafDiv(n.right,b);}','int leafDiv(TreeNode*n,int b){if(!n)return 0;if(!n->left&&!n->right)return n->val%b==0;return leafDiv(n->left,b)+leafDiv(n->right,b);}\nint solve(TreeNode*a,int b){return leafDiv(a,b);}','func leafDiv(n *TreeNode,b int) int {if n==nil{return 0};if n.Left==nil&&n.Right==nil{if n.Val%b==0{return 1};return 0};return leafDiv(n.Left,b)+leafDiv(n.Right,b)}\nfunc solve(a *TreeNode,b int) int{return leafDiv(a,b)}'),
    tests:[{stdin:'8 3 10 6 7 null 15\n3',expectedStdout:'2',isSample:true},{stdin:'6\n2',expectedStdout:'1',isSample:true},{stdin:'4 2 8\n3',expectedStdout:'0'},{stdin:'\n5',expectedStdout:'0'}] }),
  p({ ...base, slug:'tree-root-leaf-target-count', title:'Count Target Root Paths', difficulty:'EASY', patternTags:['trees','dfs','path-sum'], signatureId:'fn:tree,int->int', avgSolveSeconds:600,
    promptMarkdown:['Return the number of root-to-leaf paths whose values add to target k.','', '**Example**','', '```','input: 5 4 8 11 null 13 4 7 2 null null 5 1','22','output: 2','```','', 'The paths `5 4 11 2` and `5 8 4 5` count. A path ending at an internal node never counts; an empty tree returns 0.'].join('\n'),
    editorialMarkdown:['## Remaining-sum depth-first search','', 'Subtract each visited value from the remaining target. At a leaf, the path is valid precisely when the remaining amount becomes zero, because there is nowhere further to extend that path. Branching recursively enumerates every root-to-leaf route without storing whole routes.', '', 'The quiet mistake is returning success as soon as an internal prefix reaches zero. A target path must end at a leaf, so later values still matter. Time is O(n), bounded by one visit per node; stack space is O(h), bounded by tree height.'].join('\n'),
    referenceSolution:six('function solve(a,b){function f(n,s){if(!n)return 0;s-=n.val;if(!n.left&&!n.right)return s===0?1:0;return f(n.left,s)+f(n.right,s)}return f(a,b)}','function solve(a:TreeNode|null,b:number):number{function f(n:TreeNode|null,s:number):number{if(!n)return 0;s-=n.val;if(!n.left&&!n.right)return s===0?1:0;return f(n.left,s)+f(n.right,s)}return f(a,b)}','def solve(a, b):\n    def f(n, s):\n        if n is None: return 0\n        s -= n.val\n        if n.left is None and n.right is None: return 1 if s == 0 else 0\n        return f(n.left, s) + f(n.right, s)\n    return f(a, b)','    static int solve(TreeNode a,int b){return paths(a,b);}\n    static int paths(TreeNode n,int s){if(n==null)return 0;s-=n.val;if(n.left==null&&n.right==null)return s==0?1:0;return paths(n.left,s)+paths(n.right,s);}','int paths(TreeNode*n,int s){if(!n)return 0;s-=n->val;if(!n->left&&!n->right)return s==0;return paths(n->left,s)+paths(n->right,s);}\nint solve(TreeNode*a,int b){return paths(a,b);}','func paths(n *TreeNode,s int) int {if n==nil{return 0};s-=n.Val;if n.Left==nil&&n.Right==nil{if s==0{return 1};return 0};return paths(n.Left,s)+paths(n.Right,s)}\nfunc solve(a *TreeNode,b int) int{return paths(a,b)}'),
    tests:[{stdin:'5 4 8 11 null 13 4 7 2 null null 5 1\n22',expectedStdout:'2',isSample:true},{stdin:'1 2 3\n3',expectedStdout:'1',isSample:true},{stdin:'1 2 3\n1',expectedStdout:'0'},{stdin:'\n0',expectedStdout:'0'}] }),
  p({ ...base, slug:'tree-diameter-through-even-values', title:'Longest Even-Only Route', difficulty:'MEDIUM', patternTags:['trees','dfs','diameter'], signatureId:'fn:tree->int', avgSolveSeconds:720,
    promptMarkdown:['Return the number of nodes in the longest path made only of even-valued nodes. Adjacent nodes on the path must be parent and child.','', '**Example**','', '```','input: 2 4 7 6 8 10 12','output: 3','```','', 'One best route is `6 4 8`; odd-valued nodes break a route. An empty tree returns 0.'].join('\n'),
    editorialMarkdown:['## Post-order diameter with blocked nodes','', 'Each post-order call returns the longest downward even-only chain starting at its node. An odd node returns zero, cutting any path that tries to cross it. At an even node, the two child chains can join through that node, so their sum plus one is a candidate global route.', '', 'The quiet mistake is allowing a child chain to pass through an odd parent. That accidentally joins two separate valid pieces. Return zero immediately for an odd value. Time is O(n), bounded by visiting every node once; recursion space is O(h), bounded by height.'].join('\n'),
    referenceSolution:six('function solve(a){let best=0;function f(n){if(!n)return 0;let l=f(n.left),r=f(n.right);if(n.val%2)return 0;best=Math.max(best,1+l+r);return 1+Math.max(l,r)}f(a);return best}','function solve(a:TreeNode|null):number{let best=0;function f(n:TreeNode|null):number{if(!n)return 0;const l=f(n.left),r=f(n.right);if(n.val%2)return 0;best=Math.max(best,1+l+r);return 1+Math.max(l,r)}f(a);return best}','def solve(a):\n    best = 0\n    def f(n):\n        nonlocal best\n        if n is None: return 0\n        left, right = f(n.left), f(n.right)\n        if n.val % 2: return 0\n        best = max(best, 1 + left + right)\n        return 1 + max(left, right)\n    f(a); return best','    static int best;\n    static int solve(TreeNode a){best=0;evenChain(a);return best;}\n    static int evenChain(TreeNode n){if(n==null)return 0;int l=evenChain(n.left),r=evenChain(n.right);if(n.val%2!=0)return 0;best=Math.max(best,1+l+r);return 1+Math.max(l,r);}','int best;\nint evenChain(TreeNode*n){if(!n)return 0;int l=evenChain(n->left),r=evenChain(n->right);if(n->val%2)return 0;best=max(best,1+l+r);return 1+max(l,r);}\nint solve(TreeNode*a){best=0;evenChain(a);return best;}','func solve(a *TreeNode) int {best:=0;var f func(*TreeNode)int;f=func(n *TreeNode)int{if n==nil{return 0};l,r:=f(n.Left),f(n.Right);if n.Val%2!=0{return 0};if 1+l+r>best{best=1+l+r};if l>r{return 1+l};return 1+r};f(a);return best}'),
    tests:[{stdin:'2 4 7 6 8 10 12',expectedStdout:'3',isSample:true},{stdin:'1 2 4',expectedStdout:'1',isSample:true},{stdin:'2 3 4',expectedStdout:'2'},{stdin:'',expectedStdout:'0'}] }),
  p({ ...base, slug:'tree-odd-level-rightmost-values', title:'Last Value on Odd Floors', difficulty:'MEDIUM', patternTags:['trees','bfs','level-order'], signatureId:'fn:tree->ints', avgSolveSeconds:660,
    promptMarkdown:['Return the rightmost value from each odd-depth level, in increasing depth order. The root has depth 0.','', '**Example**','', '```','input: 1 2 3 4 5 null 6','output: 3 6','```','', 'Depth 1 ends at 3 and depth 3 is absent. An empty tree returns an empty output line; missing children do not create placeholders.'].join('\n'),
    editorialMarkdown:['## Breadth-first level boundaries','', 'A queue exposes one whole depth at a time when its current size is saved before processing. The final node removed from an odd-depth batch is that level’s rightmost real node, because children were enqueued left before right. Append only those final values.', '', 'The quiet mistake is appending the first item of a level, which quietly returns the left view instead. Preserve left-to-right enqueue order and test the last index. Time is O(n), bounded by all nodes; queue space is O(w), bounded by maximum width.'].join('\n'),
    referenceSolution:six('function solve(a){if(!a)return [];let q=[a],d=0,o=[];while(q.length){let n=q.length,last=0;for(let i=0;i<n;i++){let x=q.shift();last=x.val;if(x.left)q.push(x.left);if(x.right)q.push(x.right)}if(d%2)o.push(last);d++}return o;}','function solve(a:TreeNode|null):number[]{if(!a)return [];let q:TreeNode[]=[a],d=0,o:number[]=[];while(q.length){const n=q.length;let last=0;for(let i=0;i<n;i++){const x=q.shift() as TreeNode;last=x.val;if(x.left)q.push(x.left);if(x.right)q.push(x.right)}if(d%2)o.push(last);d++}return o;}','def solve(a):\n    if a is None: return []\n    q, depth, out = [a], 0, []\n    while q:\n        size = len(q)\n        for i in range(size):\n            node = q.pop(0)\n            if node.left: q.append(node.left)\n            if node.right: q.append(node.right)\n        if depth % 2: out.append(node.val)\n        depth += 1\n    return out','    static int[] solve(TreeNode a){if(a==null)return new int[0];java.util.ArrayDeque<TreeNode>q=new java.util.ArrayDeque<>();java.util.ArrayList<Integer>o=new java.util.ArrayList<>();q.add(a);int d=0;while(!q.isEmpty()){int z=q.size(),last=0;for(int i=0;i<z;i++){TreeNode n=q.remove();last=n.val;if(n.left!=null)q.add(n.left);if(n.right!=null)q.add(n.right);}if(d%2==1)o.add(last);d++;}int[]r=new int[o.size()];for(int i=0;i<r.length;i++)r[i]=o.get(i);return r;}','#include <queue>\nvector<int> solve(TreeNode*a){if(!a)return {};queue<TreeNode*>q;q.push(a);vector<int>o;int d=0;while(!q.empty()){int z=q.size(),last=0;while(z--){TreeNode*n=q.front();q.pop();last=n->val;if(n->left)q.push(n->left);if(n->right)q.push(n->right);}if(d%2)o.push_back(last);d++;}return o;}','func solve(a *TreeNode) []int {if a==nil{return []int{}};q:=[]*TreeNode{a};d:=0;o:=[]int{};for len(q)>0{z:=len(q);last:=0;for i:=0;i<z;i++{n:=q[0];q=q[1:];last=n.Val;if n.Left!=nil{q=append(q,n.Left)};if n.Right!=nil{q=append(q,n.Right)}};if d%2==1{o=append(o,last)};d++};return o}'),
    tests:[{stdin:'1 2 3 4 5 null 6',expectedStdout:'3',isSample:true},{stdin:'1 2 3 4 5 6 7 8',expectedStdout:'3 8',isSample:true},{stdin:'7',expectedStdout:''},{stdin:'',expectedStdout:''}] }),
  p({ ...base, slug:'tree-count-downward-target-paths', title:'Count Paths That End at Target', difficulty:'MEDIUM', patternTags:['trees','dfs','path-sum'], signatureId:'fn:tree,int->int', avgSolveSeconds:780,
    promptMarkdown:['Return the number of downward paths whose values total k. A path may start at any node, but every next node must be a child.','', '**Example**','', '```','input: 10 5 -3 3 2 null 11 3 -2 null 1','8','output: 3','```','', 'The three paths are `5 3`, `5 2 1`, and `-3 11`. A one-node path is allowed; an empty tree returns 0.'].join('\n'),
    editorialMarkdown:['## Prefix sums on a root walk','', 'Keep counts of prefix sums on the current root-to-node route. At a node with running sum S, every earlier prefix S-k begins one downward path ending here, so add its stored count. Add S before visiting children and remove it afterward so sibling routes never mix.', '', 'The quiet mistake is leaving a prefix count behind after returning from a subtree. That invents paths that jump between siblings. Backtrack the frequency table. Time is O(n), bounded by nodes with constant average map work; space is O(h) active prefixes.'].join('\n'),
    referenceSolution:six('function solve(a,b){let m=new Map([[0,1]]);function f(n,s){if(!n)return 0;s+=n.val;let r=m.get(s-b)||0;m.set(s,(m.get(s)||0)+1);r+=f(n.left,s)+f(n.right,s);m.set(s,m.get(s)-1);return r}return f(a,0)}','function solve(a:TreeNode|null,b:number):number{const m=new Map<number,number>([[0,1]]);function f(n:TreeNode|null,s:number):number{if(!n)return 0;s+=n.val;let r=m.get(s-b)||0;m.set(s,(m.get(s)||0)+1);r+=f(n.left,s)+f(n.right,s);m.set(s,(m.get(s) as number)-1);return r}return f(a,0)}','def solve(a, b):\n    seen = {0: 1}\n    def f(n, s):\n        if n is None: return 0\n        s += n.val; out = seen.get(s - b, 0); seen[s] = seen.get(s, 0) + 1\n        out += f(n.left, s) + f(n.right, s); seen[s] -= 1\n        return out\n    return f(a, 0)','    static int solve(TreeNode a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();m.put(0,1);return countPath(a,0,b,m);}\n    static int countPath(TreeNode n,int s,int k,java.util.HashMap<Integer,Integer>m){if(n==null)return 0;s+=n.val;int r=m.getOrDefault(s-k,0);m.put(s,m.getOrDefault(s,0)+1);r+=countPath(n.left,s,k,m)+countPath(n.right,s,k,m);m.put(s,m.get(s)-1);return r;}','#include <unordered_map>\nint pathCount(TreeNode*n,int s,int k,unordered_map<int,int>&m){if(!n)return 0;s+=n->val;int r=m[s-k];m[s]++;r+=pathCount(n->left,s,k,m)+pathCount(n->right,s,k,m);m[s]--;return r;}\nint solve(TreeNode*a,int b){unordered_map<int,int>m;m[0]=1;return pathCount(a,0,b,m);}','func solve(a *TreeNode,b int) int {m:=map[int]int{0:1};var f func(*TreeNode,int)int;f=func(n *TreeNode,s int)int{if n==nil{return 0};s+=n.Val;r:=m[s-b];m[s]++;r+=f(n.Left,s)+f(n.Right,s);m[s]--;return r};return f(a,0)}'),
    tests:[{stdin:'10 5 -3 3 2 null 11 3 -2 null 1\n8',expectedStdout:'3',isSample:true},{stdin:'1 1 1\n1',expectedStdout:'3',isSample:true},{stdin:'1 -1 1\n0',expectedStdout:'1'},{stdin:'\n4',expectedStdout:'0'}] }),
  p({ ...base, slug:'tree-bst-next-greater-than-bound', title:'First Search Value Above the Bound', difficulty:'MEDIUM', patternTags:['trees','bst','in-order'], signatureId:'fn:tree,int->int', avgSolveSeconds:660,
    promptMarkdown:['Given a valid BST and k, return its smallest value strictly greater than k, or -1 when none exists.','', '**Example**','', '```','input: 8 3 10 1 6 null 14 null null 4 7 13','6','output: 7','```','', 'Values equal to k do not qualify. The tree may be empty, and all BST values are distinct.'].join('\n'),
    editorialMarkdown:['## BST successor search','', 'Whenever a node is greater than k, it is a possible answer, but its left subtree may contain a smaller qualifying value, so record it and turn left. A node at most k cannot qualify and neither can its left subtree, so turn right. The last recorded candidate is the successor.', '', 'The quiet mistake is accepting equality, which returns k rather than the strictly next value. Use greater-than, not greater-or-equal. Time is O(h), bounded by BST height; iterative traversal uses O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let ans=-1;while(a){if(a.val>b){ans=a.val;a=a.left}else a=a.right}return ans}','function solve(a:TreeNode|null,b:number):number{let ans=-1;while(a){if(a.val>b){ans=a.val;a=a.left}else a=a.right}return ans}','def solve(a, b):\n    ans = -1\n    while a is not None:\n        if a.val > b: ans = a.val; a = a.left\n        else: a = a.right\n    return ans','    static int solve(TreeNode a,int b){int ans=-1;while(a!=null){if(a.val>b){ans=a.val;a=a.left;}else a=a.right;}return ans;}','int solve(TreeNode*a,int b){int ans=-1;while(a){if(a->val>b){ans=a->val;a=a->left;}else a=a->right;}return ans;}','func solve(a *TreeNode,b int) int {ans:=-1;for a!=nil{if a.Val>b{ans=a.Val;a=a.Left}else{a=a.Right}};return ans}'),
    tests:[{stdin:'8 3 10 1 6 null 14 null null 4 7 13\n6',expectedStdout:'7',isSample:true},{stdin:'2 1 3\n2',expectedStdout:'3',isSample:true},{stdin:'2 1 3\n3',expectedStdout:'-1'},{stdin:'\n5',expectedStdout:'-1'}] }),
  p({ ...base, slug:'tree-bst-child-gap-within-bound', title:'Search Tree With Short Steps', difficulty:'MEDIUM', patternTags:['trees','bst','validation'], signatureId:'fn:tree,int->bool', avgSolveSeconds:720,
    promptMarkdown:['Return true only when the tree is a strict BST and every parent-child value difference is at most k.','', '**Example**','', '```','input: 8 5 11 3 7 10 13','3','output: true','```','', 'Both rules apply: a close child in the wrong BST position still fails. k is nonnegative; an empty tree is valid.'].join('\n'),
    editorialMarkdown:['## Range validation plus local edge checks','', 'Carry an allowed open interval down the tree to validate every descendant against all ancestors, not just its parent. At the same visit, compare the current node with each direct child against k. Combining the independent checks ensures both the ordering and step-size contracts hold.', '', 'The quiet mistake is checking only parent-child ordering, which misses a deep value that crosses the root boundary. Keep inherited lower and upper bounds. Time is O(n), bounded by all nodes; recursion space is O(h), bounded by height.'].join('\n'),
    referenceSolution:six('function solve(a,b){function f(n,lo,hi){if(!n)return true;if(n.val<=lo||n.val>=hi)return false;if(n.left&&n.val-n.left.val>b)return false;if(n.right&&n.right.val-n.val>b)return false;return f(n.left,lo,n.val)&&f(n.right,n.val,hi)}return f(a,-Infinity,Infinity)}','function solve(a:TreeNode|null,b:number):boolean{function f(n:TreeNode|null,lo:number,hi:number):boolean{if(!n)return true;if(n.val<=lo||n.val>=hi)return false;if(n.left&&n.val-n.left.val>b)return false;if(n.right&&n.right.val-n.val>b)return false;return f(n.left,lo,n.val)&&f(n.right,n.val,hi)}return f(a,-Infinity,Infinity)}','def solve(a, b):\n    def f(n, lo, hi):\n        if n is None: return True\n        if n.val <= lo or n.val >= hi: return False\n        if n.left and n.val - n.left.val > b: return False\n        if n.right and n.right.val - n.val > b: return False\n        return f(n.left, lo, n.val) and f(n.right, n.val, hi)\n    return f(a, float("-inf"), float("inf"))','    static boolean solve(TreeNode a,int b){return validGap(a,Long.MIN_VALUE,Long.MAX_VALUE,b);}\n    static boolean validGap(TreeNode n,long lo,long hi,int b){if(n==null)return true;if(n.val<=lo||n.val>=hi)return false;if(n.left!=null&&n.val-n.left.val>b)return false;if(n.right!=null&&n.right.val-n.val>b)return false;return validGap(n.left,lo,n.val,b)&&validGap(n.right,n.val,hi,b);}','bool validGap(TreeNode*n,long long lo,long long hi,int b){if(!n)return true;if(n->val<=lo||n->val>=hi)return false;if(n->left&&n->val-n->left->val>b)return false;if(n->right&&n->right->val-n->val>b)return false;return validGap(n->left,lo,n->val,b)&&validGap(n->right,n->val,hi,b);}\nbool solve(TreeNode*a,int b){return validGap(a,-4000000000LL,4000000000LL,b);}','func solve(a *TreeNode,b int) bool {var f func(*TreeNode,int64,int64)bool;f=func(n *TreeNode,lo,hi int64)bool{if n==nil{return true};v:=int64(n.Val);if v<=lo||v>=hi{return false};if n.Left!=nil&&n.Val-n.Left.Val>b{return false};if n.Right!=nil&&n.Right.Val-n.Val>b{return false};return f(n.Left,lo,v)&&f(n.Right,v,hi)};return f(a,-1000000000000,1000000000000)}'),
    tests:[{stdin:'8 5 11 3 7 10 13\n3',expectedStdout:'true',isSample:true},{stdin:'8 5 11 3 7 10 13\n2',expectedStdout:'false',isSample:true},{stdin:'5 1 7 null null 4 8\n10',expectedStdout:'false'},{stdin:'\n0',expectedStdout:'true'}] }),
  p({ ...base, slug:'tree-bst-prune-below-bound', title:'Cut the Low Search Values', difficulty:'MEDIUM', patternTags:['trees','bst','pruning'], signatureId:'fn:tree,int->tree', avgSolveSeconds:720,
    promptMarkdown:['Given a valid BST and k, remove every node with value below k and return the remaining tree. Keep its original child links where they remain valid.','', '**Example**','', '```','input: 5 3 8 2 4 7 9','4','output: 5 4 8 null null 7 9','```','', 'When a node is below k, its whole left subtree is also below k. An empty tree remains empty.'].join('\n'),
    editorialMarkdown:['## BST pruning by ordered subtrees','', 'If a node is below k, discard it and its left subtree at once because every value there is even smaller; only its pruned right subtree can survive. Otherwise preserve the node and recursively prune its left side. This reuses BST order to avoid treating deletion as arbitrary tree surgery.', '', 'The quiet mistake is returning null for every removed node, which throws away qualifying descendants on its right. Return the pruned right child instead. Time is O(n) in the worst case, bounded by visited nodes; recursion space is O(h).'].join('\n'),
    referenceSolution:six('function solve(a,b){if(!a)return null;if(a.val<b)return solve(a.right,b);a.left=solve(a.left,b);return a}','function solve(a:TreeNode|null,b:number):TreeNode|null{if(!a)return null;if(a.val<b)return solve(a.right,b);a.left=solve(a.left,b);return a}','def solve(a, b):\n    if a is None: return None\n    if a.val < b: return solve(a.right, b)\n    a.left = solve(a.left, b)\n    return a','    static TreeNode solve(TreeNode a,int b){if(a==null)return null;if(a.val<b)return solve(a.right,b);a.left=solve(a.left,b);return a;}','TreeNode* solve(TreeNode*a,int b){if(!a)return nullptr;if(a->val<b)return solve(a->right,b);a->left=solve(a->left,b);return a;}','func solve(a *TreeNode,b int) *TreeNode {if a==nil{return nil};if a.Val<b{return solve(a.Right,b)};a.Left=solve(a.Left,b);return a}'),
    tests:[{stdin:'5 3 8 2 4 7 9\n4',expectedStdout:'5 4 8 null null 7 9',isSample:true},{stdin:'2 1 3\n4',expectedStdout:'',isSample:true},{stdin:'2 1 3\n2',expectedStdout:'2 null 3'},{stdin:'\n1',expectedStdout:''}] }),
  p({ ...base, slug:'tree-longest-alternating-parity-path', title:'Longest Parity-Switching Route', difficulty:'HARD', patternTags:['trees','dfs','diameter','parity'], signatureId:'fn:tree->int', avgSolveSeconds:900,
    promptMarkdown:['Return the number of nodes in the longest parent-child path whose neighboring values always have opposite parity. The path may start and end anywhere.','', '**Example**','', '```','input: 2 5 8 4 7 3 6','output: 3','```','', 'A route such as `4 5 2` alternates even and odd at every edge. An empty tree returns 0; one node gives 1.'].join('\n'),
    editorialMarkdown:['## Post-order diameter with compatible arms','', 'A post-order call returns its longest downward alternating arm. A child arm can connect through the current node only if the two endpoint values have opposite parity. The two best compatible child arms can join through the node, giving a candidate path whose endpoints lie in different subtrees.', '', 'The quiet mistake is choosing the longest child arms before checking their first edge; that lets same-parity neighbors sneak into a route. Filter each arm at its parent. Time is O(n), bounded by one post-order visit per node; recursion space is O(h).'].join('\n'),
    referenceSolution:six('function solve(a){let best=0;function f(n){if(!n)return 0;let l=f(n.left),r=f(n.right);if(n.left&&n.left.val%2===n.val%2)l=0;if(n.right&&n.right.val%2===n.val%2)r=0;best=Math.max(best,1+l+r);return 1+Math.max(l,r)}f(a);return best}','function solve(a:TreeNode|null):number{let best=0;function f(n:TreeNode|null):number{if(!n)return 0;let l=f(n.left),r=f(n.right);if(n.left&&n.left.val%2===n.val%2)l=0;if(n.right&&n.right.val%2===n.val%2)r=0;best=Math.max(best,1+l+r);return 1+Math.max(l,r)}f(a);return best}','def solve(a):\n    best = 0\n    def f(n):\n        nonlocal best\n        if n is None: return 0\n        left, right = f(n.left), f(n.right)\n        if n.left and n.left.val % 2 == n.val % 2: left = 0\n        if n.right and n.right.val % 2 == n.val % 2: right = 0\n        best = max(best, 1 + left + right)\n        return 1 + max(left, right)\n    f(a); return best','    static int altBest;\n    static int solve(TreeNode a){altBest=0;altArm(a);return altBest;}\n    static int altArm(TreeNode n){if(n==null)return 0;int l=altArm(n.left),r=altArm(n.right);if(n.left!=null&&n.left.val%2==n.val%2)l=0;if(n.right!=null&&n.right.val%2==n.val%2)r=0;altBest=Math.max(altBest,1+l+r);return 1+Math.max(l,r);}','int altBest;\nint altArm(TreeNode*n){if(!n)return 0;int l=altArm(n->left),r=altArm(n->right);if(n->left&&n->left->val%2==n->val%2)l=0;if(n->right&&n->right->val%2==n->val%2)r=0;altBest=max(altBest,1+l+r);return 1+max(l,r);}\nint solve(TreeNode*a){altBest=0;altArm(a);return altBest;}','func solve(a *TreeNode) int {best:=0;var f func(*TreeNode)int;f=func(n *TreeNode)int{if n==nil{return 0};l,r:=f(n.Left),f(n.Right);if n.Left!=nil&&n.Left.Val%2==n.Val%2{l=0};if n.Right!=nil&&n.Right.Val%2==n.Val%2{r=0};if 1+l+r>best{best=1+l+r};if l>r{return 1+l};return 1+r};f(a);return best}'),
    tests:[{stdin:'2 5 8 4 7 3 6',expectedStdout:'3',isSample:true},{stdin:'2 4 6',expectedStdout:'1',isSample:true},{stdin:'1 2 3 4 null null 5',expectedStdout:'2'},{stdin:'',expectedStdout:'0'}] }),
  p({ ...base, slug:'tree-smallest-covering-high-values', title:'Deepest Home of High Values', difficulty:'HARD', patternTags:['trees','dfs','lowest-common-ancestor'], signatureId:'fn:tree,int->int', avgSolveSeconds:900,
    promptMarkdown:['Return the value of the deepest node whose subtree contains every node with value at least k. Return -1 if no value reaches k.','', '**Example**','', '```','input: 5 9 4 7 10 3 8','8','output: 5','```','', 'The qualifying values are 9, 10, and 8, spread across both root branches. If exactly one node qualifies, return that node.'].join('\n'),
    editorialMarkdown:['## Post-order target coverage','', 'Each subtree reports how many qualifying nodes it contains and, when all qualifying nodes live in one child, that child’s deepest covering node. First count qualifying nodes globally. During post-order, if both children contain targets, the current node is the first join; otherwise pass the nonempty side upward, or the current qualifying node.', '', 'The quiet mistake is returning the root whenever two qualifying values exist, even when both lie deep in one branch. Propagate the single occupied child. Time is O(n), bounded by two full tree walks; recursion space is O(h), bounded by height.'].join('\n'),
    referenceSolution:six('function solve(a,b){function c(n){return !n?0:(n.val>=b)+c(n.left)+c(n.right)}let all=c(a);if(!all)return -1;function f(n){if(!n)return [0,null];let l=f(n.left),r=f(n.right),x=n.val>=b?1:0,tot=l[0]+r[0]+x;if(l[0]&&r[0])return [tot,n];if(x)return [tot,n];return [tot,l[0]?l[1]:r[1]]}return f(a)[1].val}','function solve(a:TreeNode|null,b:number):number{function c(n:TreeNode|null):number{return !n?0:(n.val>=b?1:0)+c(n.left)+c(n.right)}if(!c(a))return -1;function f(n:TreeNode|null):[number,TreeNode|null]{if(!n)return [0,null];const l=f(n.left),r=f(n.right),x=n.val>=b?1:0,tot=l[0]+r[0]+x;if(l[0]&&r[0])return [tot,n];if(x)return [tot,n];return [tot,l[0]?l[1]:r[1]]}return (f(a)[1] as TreeNode).val}','def solve(a, b):\n    def count(n): return 0 if n is None else (1 if n.val >= b else 0) + count(n.left) + count(n.right)\n    if count(a) == 0: return -1\n    def f(n):\n        if n is None: return 0, None\n        lc, ln = f(n.left); rc, rn = f(n.right); own = 1 if n.val >= b else 0\n        if lc and rc: return lc + rc + own, n\n        if own: return lc + rc + own, n\n        return lc + rc, ln if lc else rn\n    return f(a)[1].val','    static int highK;\n    static int solve(TreeNode a,int b){highK=b;if(highCount(a)==0)return -1;return cover(a)[1].val;}\n    static int highCount(TreeNode n){return n==null?0:(n.val>=highK?1:0)+highCount(n.left)+highCount(n.right);}\n    static TreeNode[] cover(TreeNode n){if(n==null)return new TreeNode[]{null,null};TreeNode[]l=cover(n.left),r=cover(n.right);int lc=l[0]==null?0:l[0].val,rc=r[0]==null?0:r[0].val,own=n.val>=highK?1:0,total=lc+rc+own;TreeNode ans=lc>0&&rc>0?n:(own>0?n:(lc>0?l[1]:r[1]));return new TreeNode[]{new TreeNode(total),ans};}','int highK;\nint highCount(TreeNode*n){return !n?0:(n->val>=highK)+highCount(n->left)+highCount(n->right);}\npair<int,TreeNode*> cover(TreeNode*n){if(!n)return {0,nullptr};auto l=cover(n->left),r=cover(n->right);int own=n->val>=highK,tot=l.first+r.first+own;if(l.first&&r.first)return {tot,n};if(own)return {tot,n};return {tot,l.first?l.second:r.second};}\nint solve(TreeNode*a,int b){highK=b;if(!highCount(a))return -1;return cover(a).second->val;}','func solve(a *TreeNode,b int) int {var count func(*TreeNode)int;count=func(n *TreeNode)int{if n==nil{return 0};x:=0;if n.Val>=b{x=1};return x+count(n.Left)+count(n.Right)};if count(a)==0{return -1};var f func(*TreeNode)(int,*TreeNode);f=func(n *TreeNode)(int,*TreeNode){if n==nil{return 0,nil};lc,ln:=f(n.Left);rc,rn:=f(n.Right);own:=0;if n.Val>=b{own=1};if lc>0&&rc>0{return lc+rc+own,n};if own>0{return lc+rc+own,n};if lc>0{return lc+rc,ln};return lc+rc,rn};_,n:=f(a);return n.Val}'),
    tests:[{stdin:'5 9 4 7 10 3 8\n8',expectedStdout:'5',isSample:true},{stdin:'1 2 3 9 null null 8\n8',expectedStdout:'1',isSample:true},{stdin:'1 2 3\n5',expectedStdout:'-1'},{stdin:'7\n7',expectedStdout:'7'}] }),
  p({ ...base, slug:'tree-count-balanced-target-subtrees', title:'Target Roots With Balanced Branches', difficulty:'HARD', patternTags:['trees','dfs','post-order','height'], signatureId:'fn:tree,int->int', avgSolveSeconds:900,
    promptMarkdown:['Return how many nodes with value k root a height-balanced subtree. A subtree is balanced when each node inside it has child heights differing by at most one.','', '**Example**','', '```','input: 5 2 2 1 3 null 4','2','output: 2','```','', 'Both nodes with value 2 root balanced subtrees: their child heights differ by at most one. An empty tree returns 0.'].join('\n'),
    editorialMarkdown:['## Post-order height certificates','', 'A post-order call returns -1 for an unbalanced subtree, otherwise its height. This compact certificate lets a parent reject a bad child without recomputing heights. When the certificate is valid and the current value equals k, increment the answer; each candidate subtree has already been fully verified.', '', 'The quiet mistake is testing only the two heights at a matching node, while ignoring an imbalance hidden below one child. Propagate failure upward as well as height. Time is O(n), bounded by one visit per node; recursion space is O(h), bounded by height.'].join('\n'),
    referenceSolution:six('function solve(a,b){let ans=0;function f(n){if(!n)return 0;let l=f(n.left),r=f(n.right);if(l<0||r<0||Math.abs(l-r)>1)return -1;if(n.val===b)ans++;return 1+Math.max(l,r)}f(a);return ans}','function solve(a:TreeNode|null,b:number):number{let ans=0;function f(n:TreeNode|null):number{if(!n)return 0;const l=f(n.left),r=f(n.right);if(l<0||r<0||Math.abs(l-r)>1)return -1;if(n.val===b)ans++;return 1+Math.max(l,r)}f(a);return ans}','def solve(a, b):\n    ans = 0\n    def f(n):\n        nonlocal ans\n        if n is None: return 0\n        left, right = f(n.left), f(n.right)\n        if left < 0 or right < 0 or abs(left - right) > 1: return -1\n        if n.val == b: ans += 1\n        return 1 + max(left, right)\n    f(a); return ans','    static int target,balancedCount;\n    static int solve(TreeNode a,int b){target=b;balancedCount=0;balanceMark(a);return balancedCount;}\n    static int balanceMark(TreeNode n){if(n==null)return 0;int l=balanceMark(n.left),r=balanceMark(n.right);if(l<0||r<0||Math.abs(l-r)>1)return -1;if(n.val==target)balancedCount++;return 1+Math.max(l,r);}','int target,balancedCount;\nint balanceMark(TreeNode*n){if(!n)return 0;int l=balanceMark(n->left),r=balanceMark(n->right);if(l<0||r<0||abs(l-r)>1)return -1;if(n->val==target)balancedCount++;return 1+max(l,r);}\nint solve(TreeNode*a,int b){target=b;balancedCount=0;balanceMark(a);return balancedCount;}','func solve(a *TreeNode,b int) int {ans:=0;var f func(*TreeNode)int;f=func(n *TreeNode)int{if n==nil{return 0};l,r:=f(n.Left),f(n.Right);d:=l-r;if d<0{d=-d};if l<0||r<0||d>1{return -1};if n.Val==b{ans++};if l>r{return 1+l};return 1+r};f(a);return ans}'),
    tests:[{stdin:'5 2 2 1 3 null 4\n2',expectedStdout:'2',isSample:true},{stdin:'2 2 2\n2',expectedStdout:'3',isSample:true},{stdin:'1 2 null 3 null 4\n2',expectedStdout:'0'},{stdin:'\n1',expectedStdout:'0'}] }),
];
