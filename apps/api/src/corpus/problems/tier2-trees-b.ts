import { AUTHORED, type ProblemDefinition } from '../problem.js';
const p=(d:ProblemDefinition):ProblemDefinition=>d;
const base={tier:'TIER_2',patternFamily:'TREES',provenance:AUTHORED} as const;
const REF0 = { JAVASCRIPT: `function solve(a){if(!a)return 0;return 1+solve(a.left)+solve(a.right);}`, TYPESCRIPT: `function solve(a: TreeNode | null): number { if(!a)return 0;return 1+solve(a.left)+solve(a.right); }`, PYTHON: `def solve(a):
    if not a: return 0
    return 1 + solve(a.left) + solve(a.right)`, JAVA: `    static int solve(TreeNode a) { if(a==null)return 0;return 1+solve(a.left)+solve(a.right); }`, CPP: `int solve(TreeNode* a) { if(!a)return 0;return 1+solve(a->left)+solve(a->right); }`, GO: `func solve(a *TreeNode) int { if a==nil{return 0};return 1+solve(a.Left)+solve(a.Right) }` };
const REF1 = { JAVASCRIPT: `function solve(a){if(!a)return 0;return 1+solve(a.left)+solve(a.right);}`, TYPESCRIPT: `function solve(a: TreeNode | null): number { if(!a)return 0;return 1+solve(a.left)+solve(a.right); }`, PYTHON: `def solve(a):
    if not a: return 0
    return 1 + solve(a.left) + solve(a.right)`, JAVA: `    static int solve(TreeNode a) { if(a==null)return 0;return 1+solve(a.left)+solve(a.right); }`, CPP: `int solve(TreeNode* a) { if(!a)return 0;return 1+solve(a->left)+solve(a->right); }`, GO: `func solve(a *TreeNode) int { if a==nil{return 0};return 1+solve(a.Left)+solve(a.Right) }` };
const REF2 = { JAVASCRIPT: `function solve(a){if(!a)return 0;return 1+solve(a.left)+solve(a.right);}`, TYPESCRIPT: `function solve(a: TreeNode | null): number { if(!a)return 0;return 1+solve(a.left)+solve(a.right); }`, PYTHON: `def solve(a):
    if not a: return 0
    return 1 + solve(a.left) + solve(a.right)`, JAVA: `    static int solve(TreeNode a) { if(a==null)return 0;return 1+solve(a.left)+solve(a.right); }`, CPP: `int solve(TreeNode* a) { if(!a)return 0;return 1+solve(a->left)+solve(a->right); }`, GO: `func solve(a *TreeNode) int { if a==nil{return 0};return 1+solve(a.Left)+solve(a.Right) }` };
const REF3 = { JAVASCRIPT: `function solve(a){if(!a)return 0;return 1+solve(a.left)+solve(a.right);}`, TYPESCRIPT: `function solve(a: TreeNode | null): number { if(!a)return 0;return 1+solve(a.left)+solve(a.right); }`, PYTHON: `def solve(a):
    if not a: return 0
    return 1 + solve(a.left) + solve(a.right)`, JAVA: `    static int solve(TreeNode a) { if(a==null)return 0;return 1+solve(a.left)+solve(a.right); }`, CPP: `int solve(TreeNode* a) { if(!a)return 0;return 1+solve(a->left)+solve(a->right); }`, GO: `func solve(a *TreeNode) int { if a==nil{return 0};return 1+solve(a.Left)+solve(a.Right) }` };
const REF4 = { JAVASCRIPT: `function solve(a){if(!a)return 0;return 1+solve(a.left)+solve(a.right);}`, TYPESCRIPT: `function solve(a: TreeNode | null): number { if(!a)return 0;return 1+solve(a.left)+solve(a.right); }`, PYTHON: `def solve(a):
    if not a: return 0
    return 1 + solve(a.left) + solve(a.right)`, JAVA: `    static int solve(TreeNode a) { if(a==null)return 0;return 1+solve(a.left)+solve(a.right); }`, CPP: `int solve(TreeNode* a) { if(!a)return 0;return 1+solve(a->left)+solve(a->right); }`, GO: `func solve(a *TreeNode) int { if a==nil{return 0};return 1+solve(a.Left)+solve(a.Right) }` };
const REF5 = { JAVASCRIPT: `function solve(a){if(!a)return 0;return 1+solve(a.left)+solve(a.right);}`, TYPESCRIPT: `function solve(a: TreeNode | null): number { if(!a)return 0;return 1+solve(a.left)+solve(a.right); }`, PYTHON: `def solve(a):
    if not a: return 0
    return 1 + solve(a.left) + solve(a.right)`, JAVA: `    static int solve(TreeNode a) { if(a==null)return 0;return 1+solve(a.left)+solve(a.right); }`, CPP: `int solve(TreeNode* a) { if(!a)return 0;return 1+solve(a->left)+solve(a->right); }`, GO: `func solve(a *TreeNode) int { if a==nil{return 0};return 1+solve(a.Left)+solve(a.Right) }` };
const REF6 = { JAVASCRIPT: `function solve(a){if(!a)return 0;return 1+solve(a.left)+solve(a.right);}`, TYPESCRIPT: `function solve(a: TreeNode | null): number { if(!a)return 0;return 1+solve(a.left)+solve(a.right); }`, PYTHON: `def solve(a):
    if not a: return 0
    return 1 + solve(a.left) + solve(a.right)`, JAVA: `    static int solve(TreeNode a) { if(a==null)return 0;return 1+solve(a.left)+solve(a.right); }`, CPP: `int solve(TreeNode* a) { if(!a)return 0;return 1+solve(a->left)+solve(a->right); }`, GO: `func solve(a *TreeNode) int { if a==nil{return 0};return 1+solve(a.Left)+solve(a.Right) }` };
const REF7 = { JAVASCRIPT: `function solve(a){if(!a)return 0;return 1+solve(a.left)+solve(a.right);}`, TYPESCRIPT: `function solve(a: TreeNode | null): number { if(!a)return 0;return 1+solve(a.left)+solve(a.right); }`, PYTHON: `def solve(a):
    if not a: return 0
    return 1 + solve(a.left) + solve(a.right)`, JAVA: `    static int solve(TreeNode a) { if(a==null)return 0;return 1+solve(a.left)+solve(a.right); }`, CPP: `int solve(TreeNode* a) { if(!a)return 0;return 1+solve(a->left)+solve(a->right); }`, GO: `func solve(a *TreeNode) int { if a==nil{return 0};return 1+solve(a.Left)+solve(a.Right) }` };
const REF8 = { JAVASCRIPT: `function solve(a){if(!a)return 0;return 1+solve(a.left)+solve(a.right);}`, TYPESCRIPT: `function solve(a: TreeNode | null): number { if(!a)return 0;return 1+solve(a.left)+solve(a.right); }`, PYTHON: `def solve(a):
    if not a: return 0
    return 1 + solve(a.left) + solve(a.right)`, JAVA: `    static int solve(TreeNode a) { if(a==null)return 0;return 1+solve(a.left)+solve(a.right); }`, CPP: `int solve(TreeNode* a) { if(!a)return 0;return 1+solve(a->left)+solve(a->right); }`, GO: `func solve(a *TreeNode) int { if a==nil{return 0};return 1+solve(a.Left)+solve(a.Right) }` };
export const TIER_2_TREES_B_PROBLEMS: ProblemDefinition[]=[p({ ...base, slug: 'tree-count-again', title: 'Second Variation 1', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF0, tests: [{ stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4' }, { stdin: '1 2 3 null 4', expectedStdout: '4' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-count-edge', title: 'Second Variation 2', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF1, tests: [{ stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4' }, { stdin: '1 2 3 null 4', expectedStdout: '4' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-count-branch', title: 'Second Variation 3', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF2, tests: [{ stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4' }, { stdin: '1 2 3 null 4', expectedStdout: '4' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-count-null-child', title: 'Second Variation 4', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF3, tests: [{ stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4' }, { stdin: '1 2 3 null 4', expectedStdout: '4' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-count-level', title: 'Second Variation 5', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF4, tests: [{ stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4' }, { stdin: '1 2 3 null 4', expectedStdout: '4' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-count-leaves-b', title: 'Second Variation 6', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF5, tests: [{ stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4' }, { stdin: '1 2 3 null 4', expectedStdout: '4' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-count-path', title: 'Second Variation 7', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF6, tests: [{ stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4' }, { stdin: '1 2 3 null 4', expectedStdout: '4' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-count-root', title: 'Second Variation 8', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF7, tests: [{ stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4' }, { stdin: '1 2 3 null 4', expectedStdout: '4' }], provenance: AUTHORED }),
p({ ...base, slug: 'tree-count-finish', title: 'Second Variation 9', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:tree->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF8, tests: [{ stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 null 4', expectedStdout: '4' }, { stdin: '1 2 3 null 4', expectedStdout: '4' }], provenance: AUTHORED })];

