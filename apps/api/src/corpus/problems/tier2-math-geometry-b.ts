import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'MATH_GEOMETRY', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_2_MATH_GEOMETRY_B_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'gcd-of-a-number-chain', title:'Shared Divisor of a Chain', difficulty:'EASY', patternTags:['math','gcd','euclidean-algorithm'], signatureId:'fn:ints->int', avgSolveSeconds:510,
    promptMarkdown:['Return the greatest positive integer dividing every number in the non-empty list. Values are positive and at most 1,000,000.','','**Example**','','```','input: 24 36 60','output: 12','```','','The named edge case is a one-number list: its answer is that number.'].join('\n'),
    editorialMarkdown:['## Fold the Euclidean algorithm','', 'The gcd of a whole list can be built left to right: after processing a prefix, keep its shared divisor, then replace it with the gcd of that value and the next number. This is valid because a divisor shared by the prefix and the next value is exactly a divisor shared by the enlarged prefix. Euclid reduces each pair by remainders.', '', 'The quiet mistake is taking the smallest input value; it need not divide the others. Do not stop after finding a common divisor for only the first two values. Time is O(n log M), bounded by n values and the largest magnitude M; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let g=a[0];for(let x of a){let y=x;while(y){let r=g%y;g=y;y=r;}}return g;}','function solve(a:number[]):number{let g=a[0];for(const x of a){let y=x;while(y){const r=g%y;g=y;y=r;}}return g;}','def solve(a):\n    g=a[0]\n    for x in a:\n        while x: g,x=x,g%x\n    return g','    static int solve(int[] a) { int g=a[0]; for(int x:a){ while(x!=0){int r=g%x;g=x;x=r;} } return g; }','int solve(vector<int>a){int g=a[0];for(int x:a)while(x){int r=g%x;g=x;x=r;}return g;}','func solve(a []int) int { g:=a[0];for _,x:=range a{for x!=0{g,x=x,g%x}};return g }'),
    tests:[{stdin:'24 36 60',expectedStdout:'12',isSample:true},{stdin:'7',expectedStdout:'7',isSample:true},{stdin:'17 19',expectedStdout:'1'},{stdin:'1000000 500000 250000',expectedStdout:'250000'}] }),
  p({ ...base, slug:'least-common-multiple-with-cap', title:'Bounded Least Common Multiple', difficulty:'EASY', patternTags:['math','lcm','gcd'], signatureId:'fn:int,int->int', avgSolveSeconds:540,
    promptMarkdown:['Return the least positive common multiple of two positive integers, or `-1` if it exceeds 1,000,000,000.','','**Example**','','```','input: 12','18','output: 36','```','','Both inputs are at most 1,000,000. The edge case is a product that would exceed the limit.'].join('\n'),
    editorialMarkdown:['## GCD before multiplication','', 'Compute the gcd first. Dividing one factor by that gcd removes the overlap, so `(a / gcd(a,b)) * b` is the least common multiple without counting shared prime factors twice. Use a wide intermediate in Java and C++ before testing the stated cap.', '', 'The quiet mistake is multiplying `a * b` first: it can overflow a 32-bit integer even when the final quotient is small. Another is returning the capped value instead of the required sentinel. Time is O(log min(a,b)), bounded by Euclidean steps, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let x=a,y=b;while(y){let r=x%y;x=y;y=r;}let z=a/x*b;return z>1e9?-1:z;}','function solve(a:number,b:number):number{let x=a,y=b;while(y){const r=x%y;x=y;y=r;}const z=a/x*b;return z>1e9?-1:z;}','def solve(a,b):\n    x,y=a,b\n    while y:x,y=y,x%y\n    z=a//x*b\n    return z if z<=1000000000 else -1','    static int solve(int a,int b) { int x=a,y=b; while(y!=0){int r=x%y;x=y;y=r;} long z=(long)(a/x)*b; return z>1000000000L?-1:(int)z; }','int solve(int a,int b){int x=a,y=b;while(y){int r=x%y;x=y;y=r;}long long z=(long long)(a/x)*b;return z>1000000000LL?-1:(int)z;}','func solve(a int,b int) int { x,y:=a,b;for y!=0{x,y=y,x%y};z:=int64(a/x)*int64(b);if z>1000000000{return -1};return int(z) }'),
    tests:[{stdin:'12\n18',expectedStdout:'36',isSample:true},{stdin:'7\n13',expectedStdout:'91',isSample:true},{stdin:'1000000\n999983',expectedStdout:'-1'},{stdin:'1\n1',expectedStdout:'1'}] }),
  p({ ...base, slug:'prime-total-below-limit', title:'How Many Primes Before the Limit', difficulty:'MEDIUM', patternTags:['math','sieve','prime'], signatureId:'fn:int->int', avgSolveSeconds:660,
    promptMarkdown:['Return how many prime numbers are strictly smaller than `n`. `n` is between 0 and 1,000,000.','','**Example**','','```','input: 10','output: 4','```','','The primes below 10 are 2, 3, 5, and 7. The edge case is `n <= 2`, which has no answer to count.'].join('\n'),
    editorialMarkdown:['## Sieve of Eratosthenes','', 'Make a mark array for candidate integers. When an unmarked number p is reached, it is prime; mark its multiples beginning at p squared, because smaller multiples already had a smaller factor. Counting unmarked entries then gives every prime below the limit in one coordinated pass.', '', 'The quiet mistake is including n itself even though the boundary is strict. Starting at 2p is correct but does needless work; starting at p squared is the sieve insight. Time is O(n log log n), bounded by the limit, and extra space is O(n) for marks.'].join('\n'),
    referenceSolution:six('function solve(n){let a=Array(n).fill(true),c=0;for(let p=2;p<n;p++)if(a[p]){c++;for(let q=p*p;q<n;q+=p)a[q]=false;}return c;}','function solve(n:number):number{const a=Array(n).fill(true);let c=0;for(let p=2;p<n;p++)if(a[p]){c++;for(let q=p*p;q<n;q+=p)a[q]=false;}return c;}','def solve(n):\n    a=[True]*n;c=0\n    for p in range(2,n):\n        if a[p]:\n            c+=1\n            for q in range(p*p,n,p): a[q]=False\n    return c','    static int solve(int n) { boolean[] a=new boolean[n]; java.util.Arrays.fill(a,true); int c=0; for(int p=2;p<n;p++)if(a[p]){c++;for(long q=(long)p*p;q<n;q+=p)a[(int)q]=false;}return c; }','int solve(int n){vector<bool>a(n,true);int c=0;for(int p=2;p<n;p++)if(a[p]){c++;for(long long q=(long long)p*p;q<n;q+=p)a[(int)q]=false;}return c;}','func solve(n int) int { a:=make([]bool,n);for i:=range a{a[i]=true};c:=0;for p:=2;p<n;p++{if a[p]{c++;for q:=p*p;q<n;q+=p{a[q]=false}}};return c }'),
    tests:[{stdin:'10',expectedStdout:'4',isSample:true},{stdin:'2',expectedStdout:'0',isSample:true},{stdin:'3',expectedStdout:'1'},{stdin:'20',expectedStdout:'8'}] }),
  p({ ...base, slug:'modular-power-seven', title:'Power Modulo Seven', difficulty:'MEDIUM', patternTags:['math','modular-arithmetic','binary-exponentiation'], signatureId:'fn:int,int->int', avgSolveSeconds:630,
    promptMarkdown:['Return `a` raised to the non-negative power `b`, modulo 1,000,000,007. Both inputs fit in a signed 32-bit integer.','','**Example**','','```','input: 2','10','output: 1024','```','','For exponent zero, return 1, including when the base is zero.'].join('\n'),
    editorialMarkdown:['## Binary exponentiation under a modulus','', 'Repeated squaring writes the exponent in binary. When its current low bit is one, multiply the answer by the current base; square the base each step and halve the exponent. Reducing after every multiplication preserves the final remainder while keeping every intermediate controlled.', '', 'The quiet mistake is looping b times, which is too slow for a large exponent. In Java and C++, multiplying two int residues before widening overflows, so use long arithmetic. Time is O(log b), bounded by exponent bits, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let m=1000000007n,r=1n,x=(BigInt(a)%m+m)%m;while(b){if(b%2)r=r*x%m;x=x*x%m;b=Math.floor(b/2);}return Number(r);}','function solve(a:number,b:number):number{const m=1000000007n;let r=1n,x=(BigInt(a)%m+m)%m;while(b){if(b%2)r=r*x%m;x=x*x%m;b=Math.floor(b/2);}return Number(r);}','def solve(a,b):\n    m=1000000007;r=1;a%=m\n    while b:\n        if b%2:r=r*a%m\n        a=a*a%m;b//=2\n    return r','    static int solve(int a,int b) { long m=1000000007L,r=1,x=((a%m)+m)%m; while(b>0){if((b&1)==1)r=r*x%m;x=x*x%m;b>>=1;}return (int)r; }','int solve(int a,int b){long long m=1000000007LL,r=1,x=((a%m)+m)%m;while(b){if(b&1)r=r*x%m;x=x*x%m;b>>=1;}return (int)r;}','func solve(a int,b int) int { const m int64=1000000007; x:=int64(a)%m;if x<0{x+=m};r:=int64(1);for b>0{if b%2==1{r=r*x%m};x=x*x%m;b/=2};return int(r) }'),
    tests:[{stdin:'2\n10',expectedStdout:'1024',isSample:true},{stdin:'0\n0',expectedStdout:'1',isSample:true},{stdin:'7\n1',expectedStdout:'7'},{stdin:'1000000006\n2',expectedStdout:'1'}] }),
  p({ ...base, slug:'integer-fourth-root-floor', title:'Floor of a Fourth Root', difficulty:'MEDIUM', patternTags:['math','binary-search','integer-root'], signatureId:'fn:int->int', avgSolveSeconds:690,
    promptMarkdown:['Return the largest non-negative integer r with `r^4 <= n`. `n` is between 0 and 2,000,000,000.','','**Example**','','```','input: 80','output: 2','```','','The named edge case is a perfect fourth power: 81 returns 3. Zero returns zero.'].join('\n'),
    editorialMarkdown:['## Binary search with safe powers','', 'Whether a candidate fourth power is at most n is monotone: once it fails, every larger candidate fails. Binary search for the last successful value. Use division checks or a wide integer for repeated multiplication so the comparison itself cannot overflow.', '', 'The quiet mistake is using `mid*mid*mid*mid` in a 32-bit expression. It can wrap and falsely look small near the upper bound. Floating roots can also round across the boundary. Time is O(log n), bounded by the candidate interval, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(n){let l=0,h=50000,z=0;while(l<=h){let m=(l+h)>>1;if(m*m*m*m<=n){z=m;l=m+1}else h=m-1}return z;}','function solve(n:number):number{let l=0,h=50000,z=0;while(l<=h){const m=Math.floor((l+h)/2);if(m*m*m*m<=n){z=m;l=m+1}else h=m-1}return z;}','def solve(n):\n    l,h,z=0,50000,0\n    while l<=h:\n        m=(l+h)//2\n        if m**4<=n:z=m;l=m+1\n        else:h=m-1\n    return z','    static int solve(int n) { int l=0,h=50000,z=0; while(l<=h){int m=l+(h-l)/2;long q=(long)m*m*m*m;if(q<=n){z=m;l=m+1;}else h=m-1;}return z; }','int solve(int n){int l=0,h=50000,z=0;while(l<=h){int m=l+(h-l)/2;long long q=1LL*m*m*m*m;if(q<=n){z=m;l=m+1;}else h=m-1;}return z;}','func solve(n int) int { l,h,z:=0,50000,0;for l<=h{m:=(l+h)/2;q:=int64(m)*int64(m)*int64(m)*int64(m);if q<=int64(n){z=m;l=m+1}else{h=m-1}};return z }'),
    tests:[{stdin:'80',expectedStdout:'2',isSample:true},{stdin:'81',expectedStdout:'3',isSample:true},{stdin:'0',expectedStdout:'0'},{stdin:'2000000000',expectedStdout:'211'}] }),
  p({ ...base, slug:'digit-sum-after-bounded-reversal', title:'Digit Sum After a Safe Reverse', difficulty:'EASY', patternTags:['math','digits','bounds'], signatureId:'fn:int->int', avgSolveSeconds:510,
    promptMarkdown:['Reverse the digits of a non-negative integer, but use zero if that reversed number exceeds 100,000,000. Return the digit sum of the resulting number.','','**Example**','','```','input: 1203','output: 6','```','','Zero has digit sum zero. The edge case is an oversized reversal, which contributes zero rather than its digits.'].join('\n'),
    editorialMarkdown:['## Digit extraction with a guarded accumulator','', 'Peel the input with remainder and division, appending each digit to a reversed accumulator. Before multiplying the accumulator by ten, compare against the cap boundary; this proves the next append is safe. If it would exceed the cap, the specified result is immediately zero; otherwise sum the completed digits.', '', 'The quiet mistake is summing the original digits after an overflow rejection. The task changes the value to zero first. Checking only after overflow also defeats the purpose of the bound. Time is O(d), bounded by decimal digits, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let r=0;while(a){let d=a%10;if(r>10000000||(r==10000000&&d>0))return 0;r=r*10+d;a=Math.floor(a/10)}let s=0;while(r){s+=r%10;r=Math.floor(r/10)}return s;}','function solve(a:number):number{let r=0;while(a){const d=a%10;if(r>10000000||(r===10000000&&d>0))return 0;r=r*10+d;a=Math.floor(a/10)}let s=0;while(r){s+=r%10;r=Math.floor(r/10)}return s;}','def solve(a):\n    r=0\n    while a:\n        d=a%10\n        if r>10000000 or r==10000000 and d:return 0\n        r=r*10+d;a//=10\n    return sum(map(int,str(r)))','    static int solve(int a) { int r=0;while(a>0){int d=a%10;if(r>10000000||(r==10000000&&d>0))return 0;r=r*10+d;a/=10;}int s=0;while(r>0){s+=r%10;r/=10;}return s; }','int solve(int a){int r=0;while(a){int d=a%10;if(r>10000000||(r==10000000&&d))return 0;r=r*10+d;a/=10;}int s=0;while(r){s+=r%10;r/=10;}return s;}','func solve(a int) int { r:=0;for a>0{d:=a%10;if r>10000000||(r==10000000&&d>0){return 0};r=r*10+d;a/=10};s:=0;for r>0{s+=r%10;r/=10};return s }'),
    tests:[{stdin:'1203',expectedStdout:'6',isSample:true},{stdin:'0',expectedStdout:'0',isSample:true},{stdin:'100000001',expectedStdout:'0'},{stdin:'99',expectedStdout:'18'}] }),
  p({ ...base, slug:'closed-rectangle-overlap-check', title:'Closed Rectangle Overlap', difficulty:'EASY', patternTags:['geometry','rectangles','intervals'], signatureId:'fn:matrix->bool', avgSolveSeconds:570,
    promptMarkdown:['The matrix has two rows, each `x1 y1 x2 y2` for an axis-aligned rectangle with `x1 < x2` and `y1 < y2`. Return whether their closed areas overlap or touch.','','**Example**','','```','input: 0 0 2 2;2 1 4 3','output: true','```','','Touching along an edge or at one corner counts as overlap; separated rectangles return false.'].join('\n'),
    editorialMarkdown:['## Rectangle overlap as interval intersection','', 'Two axis-aligned rectangles share a closed point exactly when their x intervals intersect and their y intervals intersect. Each one-dimensional test is `max(lefts) <= min(rights)`. Requiring both axes removes any need to enumerate points or compute area.', '', 'The quiet mistake is using strict comparisons and rejecting edge contact, despite the closed-rectangle rule. Another is checking just x overlap, which accepts rectangles stacked apart. Time is O(1), bounded by the eight coordinates, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let x=a[0],y=a[1];return Math.max(x[0],y[0])<=Math.min(x[2],y[2])&&Math.max(x[1],y[1])<=Math.min(x[3],y[3]);}','function solve(a:number[][]):boolean{const x=a[0],y=a[1];return Math.max(x[0],y[0])<=Math.min(x[2],y[2])&&Math.max(x[1],y[1])<=Math.min(x[3],y[3]);}','def solve(a):\n x,y=a\n return max(x[0],y[0])<=min(x[2],y[2]) and max(x[1],y[1])<=min(x[3],y[3])','    static boolean solve(int[][] a) { int[] x=a[0],y=a[1];return Math.max(x[0],y[0])<=Math.min(x[2],y[2])&&Math.max(x[1],y[1])<=Math.min(x[3],y[3]); }','bool solve(vector<vector<int>>a){auto x=a[0],y=a[1];return max(x[0],y[0])<=min(x[2],y[2])&&max(x[1],y[1])<=min(x[3],y[3]);}','func solve(a [][]int) bool { x,y:=a[0],a[1];return maxi(x[0],y[0])<=mini(x[2],y[2])&&maxi(x[1],y[1])<=mini(x[3],y[3]) }\nfunc maxi(a,b int)int{if a>b{return a};return b}\nfunc mini(a,b int)int{if a<b{return a};return b}'),
    tests:[{stdin:'0 0 2 2;2 1 4 3',expectedStdout:'true',isSample:true},{stdin:'0 0 1 1;2 2 3 3',expectedStdout:'false',isSample:true},{stdin:'0 0 2 2;2 2 3 3',expectedStdout:'true'},{stdin:'-3 -1 -1 1;-2 0 0 2',expectedStdout:'true'}] }),
  p({ ...base, slug:'point-inside-closed-rectangle', title:'Point in a Closed Rectangle', difficulty:'EASY', patternTags:['geometry','coordinates','bounds'], signatureId:'fn:matrix->bool', avgSolveSeconds:480,
    promptMarkdown:['The first row is `x1 y1 x2 y2` for an axis-aligned rectangle; the second is point `x y`. Return whether the point is inside or on the boundary.','','**Example**','','```','input: 0 0 5 3;5 1','output: true','```','','Rectangle corners are ordered low then high. A point on any edge is included.'].join('\n'),
    editorialMarkdown:['## Coordinate bounds test','', 'A point lies in an axis-aligned closed rectangle precisely when its x coordinate lies between the two x bounds and its y coordinate lies between the y bounds. The two independent comparisons directly represent the rectangle as the Cartesian product of its intervals.', '', 'The quiet mistake is using `<` instead of `<=`, which silently loses every boundary point. Do not compare distances from the center: that describes a different shape. Time is O(1), bounded by six coordinates, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let r=a[0],p=a[1];return r[0]<=p[0]&&p[0]<=r[2]&&r[1]<=p[1]&&p[1]<=r[3];}','function solve(a:number[][]):boolean{const r=a[0],p=a[1];return r[0]<=p[0]&&p[0]<=r[2]&&r[1]<=p[1]&&p[1]<=r[3];}','def solve(a):\n r,p=a\n return r[0]<=p[0]<=r[2] and r[1]<=p[1]<=r[3]','    static boolean solve(int[][] a) { int[] r=a[0],p=a[1];return r[0]<=p[0]&&p[0]<=r[2]&&r[1]<=p[1]&&p[1]<=r[3]; }','bool solve(vector<vector<int>>a){auto r=a[0],p=a[1];return r[0]<=p[0]&&p[0]<=r[2]&&r[1]<=p[1]&&p[1]<=r[3];}','func solve(a [][]int) bool { r,p:=a[0],a[1];return r[0]<=p[0]&&p[0]<=r[2]&&r[1]<=p[1]&&p[1]<=r[3] }'),
    tests:[{stdin:'0 0 5 3;5 1',expectedStdout:'true',isSample:true},{stdin:'0 0 5 3;6 1',expectedStdout:'false',isSample:true},{stdin:'-2 -2 2 2;0 0',expectedStdout:'true'},{stdin:'-2 -2 2 2;-2 -2',expectedStdout:'true'}] }),
  p({ ...base, slug:'smallest-manhattan-pair', title:'Closest Manhattan Pair', difficulty:'HARD', patternTags:['geometry','manhattan-distance','brute-force'], signatureId:'fn:matrix->int', avgSolveSeconds:840,
    promptMarkdown:['Each matrix row is a point `x y`; there are at least two points. Return the smallest Manhattan distance between two different points.','','**Example**','','```','input: 0 0;2 1;5 5','output: 3','```','','Duplicate points are allowed and have distance zero.'].join('\n'),
    editorialMarkdown:['## Pairwise Manhattan comparison','', 'For each unordered pair, the Manhattan distance is `abs(x1-x2)+abs(y1-y2)`. Every valid answer belongs to one such pair, so retaining the smallest value while scanning all pairs proves the result. This deliberately favors a clear exhaustive geometry baseline over an unnecessary sort.', '', 'The quiet mistake is comparing a point with itself, which makes every answer zero. In Java and C++, subtract in a wide type before absolute value because coordinate differences can exceed signed 32-bit range. Time is O(n²), bounded by point pairs, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let z=Infinity;for(let i=0;i<a.length;i++)for(let j=i+1;j<a.length;j++)z=Math.min(z,Math.abs(a[i][0]-a[j][0])+Math.abs(a[i][1]-a[j][1]));return z;}','function solve(a:number[][]):number{let z=Infinity;for(let i=0;i<a.length;i++)for(let j=i+1;j<a.length;j++)z=Math.min(z,Math.abs(a[i][0]-a[j][0])+Math.abs(a[i][1]-a[j][1]));return z;}','def solve(a):\n z=10**30\n for i in range(len(a)):\n  for j in range(i+1,len(a)):z=min(z,abs(a[i][0]-a[j][0])+abs(a[i][1]-a[j][1]))\n return z','    static int solve(int[][] a) { long z=Long.MAX_VALUE;for(int i=0;i<a.length;i++)for(int j=i+1;j<a.length;j++){long d=Math.abs((long)a[i][0]-a[j][0])+Math.abs((long)a[i][1]-a[j][1]);if(d<z)z=d;}return (int)z; }','int solve(vector<vector<int>>a){long long z=LLONG_MAX;for(int i=0;i<(int)a.size();i++)for(int j=i+1;j<(int)a.size();j++){long long d=llabs((long long)a[i][0]-a[j][0])+llabs((long long)a[i][1]-a[j][1]);if(d<z)z=d;}return(int)z;}','func solve(a [][]int) int { z:=int64(1<<62);for i:=0;i<len(a);i++{for j:=i+1;j<len(a);j++{x:=int64(a[i][0])-int64(a[j][0]);if x<0{x=-x};y:=int64(a[i][1])-int64(a[j][1]);if y<0{y=-y};if x+y<z{z=x+y}}};return int(z) }'),
    tests:[{stdin:'0 0;2 1;5 5',expectedStdout:'3',isSample:true},{stdin:'1 1;1 1',expectedStdout:'0',isSample:true},{stdin:'-1 -1;2 3;10 0',expectedStdout:'7'},{stdin:'0 0;0 5;5 0',expectedStdout:'5'}] }),
  p({ ...base, slug:'nearest-manhattan-point-low-index', title:'Nearest Point, Earlier Row Wins', difficulty:'MEDIUM', patternTags:['geometry','manhattan-distance','tie-break'], signatureId:'fn:matrix->int', avgSolveSeconds:690,
    promptMarkdown:['Each row is a point `x y`. Return the zero-based row index closest to the origin by Manhattan distance. If distances tie, return the smaller row index.','','**Example**','','```','input: 2 1;-1 -2;3 0','output: 0','```','','The matrix has at least one point. The stated tie-break makes the answer deterministic.'].join('\n'),
    editorialMarkdown:['## Stable scan by Manhattan key','', 'Scan the points once and compute `abs(x)+abs(y)`. Keep the first row with a strictly smaller distance; refusing to replace on equality preserves the earlier index automatically. This is a stable minimum selection, and the row order supplies the required tie-break without sorting.', '', 'The quiet mistake is using `<=` while updating, which turns the tie rule around and returns the last tied point. Do not use Euclidean distance; it ranks a different metric. Time is O(n), bounded by point rows, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let k=0,b=Math.abs(a[0][0])+Math.abs(a[0][1]);for(let i=1;i<a.length;i++){let d=Math.abs(a[i][0])+Math.abs(a[i][1]);if(d<b){b=d;k=i}}return k;}','function solve(a:number[][]):number{let k=0,b=Math.abs(a[0][0])+Math.abs(a[0][1]);for(let i=1;i<a.length;i++){const d=Math.abs(a[i][0])+Math.abs(a[i][1]);if(d<b){b=d;k=i}}return k;}','def solve(a):\n k=0;b=abs(a[0][0])+abs(a[0][1])\n for i in range(1,len(a)):\n  d=abs(a[i][0])+abs(a[i][1])\n  if d<b:b,k=d,i\n return k','    static int solve(int[][] a) { int k=0;long b=Math.abs((long)a[0][0])+Math.abs((long)a[0][1]);for(int i=1;i<a.length;i++){long d=Math.abs((long)a[i][0])+Math.abs((long)a[i][1]);if(d<b){b=d;k=i;}}return k; }','int solve(vector<vector<int>>a){int k=0;long long b=llabs((long long)a[0][0])+llabs((long long)a[0][1]);for(int i=1;i<(int)a.size();i++){long long d=llabs((long long)a[i][0])+llabs((long long)a[i][1]);if(d<b){b=d;k=i;}}return k;}','func solve(a [][]int) int { k:=0;x:=int64(a[0][0]);if x<0{x=-x};y:=int64(a[0][1]);if y<0{y=-y};b:=x+y;for i:=1;i<len(a);i++{x=int64(a[i][0]);if x<0{x=-x};y=int64(a[i][1]);if y<0{y=-y};if x+y<b{b=x+y;k=i}};return k }'),
    tests:[{stdin:'2 1;-1 -2;3 0',expectedStdout:'0',isSample:true},{stdin:'4 0;1 3',expectedStdout:'0',isSample:true},{stdin:'0 0',expectedStdout:'0'},{stdin:'-5 1;1 1;2 2',expectedStdout:'1'}] }),
  p({ ...base, slug:'three-points-on-one-line', title:'Three Points, One Line', difficulty:'MEDIUM', patternTags:['geometry','cross-product','coordinates'], signatureId:'fn:matrix->bool', avgSolveSeconds:720,
    promptMarkdown:['The matrix has exactly three rows, each a point `x y`. Return whether all three points are collinear. Coordinates fit in signed 32-bit integers.','','**Example**','','```','input: 0 0;2 2;5 5','output: true','```','','Repeated points count as collinear because a line can contain the same location more than once.'].join('\n'),
    editorialMarkdown:['## Cross product area test','', 'Three points are collinear when the signed area of their triangle is zero. Compare `(x2-x1)*(y3-y1)` with `(y2-y1)*(x3-x1)`; equal products mean the two direction vectors have zero cross product. This avoids division, so vertical lines need no special case.', '', 'The quiet mistake is comparing slopes with division, which fails on vertical lines and can lose exactness. The products need wide arithmetic in Java and C++ because coordinate differences multiply. Time is O(1), bounded by three points, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){return (a[1][0]-a[0][0])*(a[2][1]-a[0][1])===(a[1][1]-a[0][1])*(a[2][0]-a[0][0]);}','function solve(a:number[][]):boolean{return (a[1][0]-a[0][0])*(a[2][1]-a[0][1])===(a[1][1]-a[0][1])*(a[2][0]-a[0][0]);}','def solve(a):\n return (a[1][0]-a[0][0])*(a[2][1]-a[0][1])==(a[1][1]-a[0][1])*(a[2][0]-a[0][0])','    static boolean solve(int[][] a) { return ((long)a[1][0]-a[0][0])*((long)a[2][1]-a[0][1])==((long)a[1][1]-a[0][1])*((long)a[2][0]-a[0][0]); }','bool solve(vector<vector<int>>a){return((long long)a[1][0]-a[0][0])*((long long)a[2][1]-a[0][1])==((long long)a[1][1]-a[0][1])*((long long)a[2][0]-a[0][0]);}','func solve(a [][]int) bool { return (int64(a[1][0])-int64(a[0][0]))*(int64(a[2][1])-int64(a[0][1]))==(int64(a[1][1])-int64(a[0][1]))*(int64(a[2][0])-int64(a[0][0])) }'),
    tests:[{stdin:'0 0;2 2;5 5',expectedStdout:'true',isSample:true},{stdin:'0 0;0 2;1 2',expectedStdout:'false',isSample:true},{stdin:'1 1;1 1;2 2',expectedStdout:'true'},{stdin:'3 0;3 2;3 -5',expectedStdout:'true'}] }),
  p({ ...base, slug:'rectangle-union-area-two', title:'Area Covered by Two Rectangles', difficulty:'HARD', patternTags:['geometry','rectangles','inclusion-exclusion'], signatureId:'fn:matrix->int', avgSolveSeconds:870,
    promptMarkdown:['The matrix has two rows, each `x1 y1 x2 y2` for a non-empty axis-aligned rectangle. Return the area covered by either rectangle.','','**Example**','','```','input: 0 0 3 2;2 1 5 4','output: 14','```','','Touching edges have zero overlap area. Coordinates fit signed 32-bit integers, and the final area fits an int.'].join('\n'),
    editorialMarkdown:['## Inclusion-exclusion for rectangles','', 'Add the two rectangle areas, then subtract their shared area once because it was counted twice. The intersection width is `max(0, min(rights)-max(lefts))`, with the same formula for height. The clamp naturally makes disjoint and edge-touching rectangles contribute zero overlap.', '', 'The quiet mistake is treating a negative intersection width as area, which adds nonsense for separated rectangles. Products and coordinate subtraction require wide arithmetic in Java and C++ before narrowing the guaranteed final result. Time is O(1), bounded by two rectangles, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let x=a[0],y=a[1],w=Math.max(0,Math.min(x[2],y[2])-Math.max(x[0],y[0])),h=Math.max(0,Math.min(x[3],y[3])-Math.max(x[1],y[1]));return (x[2]-x[0])*(x[3]-x[1])+(y[2]-y[0])*(y[3]-y[1])-w*h;}','function solve(a:number[][]):number{const x=a[0],y=a[1],w=Math.max(0,Math.min(x[2],y[2])-Math.max(x[0],y[0])),h=Math.max(0,Math.min(x[3],y[3])-Math.max(x[1],y[1]));return (x[2]-x[0])*(x[3]-x[1])+(y[2]-y[0])*(y[3]-y[1])-w*h;}','def solve(a):\n x,y=a;w=max(0,min(x[2],y[2])-max(x[0],y[0]));h=max(0,min(x[3],y[3])-max(x[1],y[1]))\n return (x[2]-x[0])*(x[3]-x[1])+(y[2]-y[0])*(y[3]-y[1])-w*h','    static int solve(int[][] a) { long[] x={a[0][0],a[0][1],a[0][2],a[0][3]},y={a[1][0],a[1][1],a[1][2],a[1][3]};long w=Math.max(0,Math.min(x[2],y[2])-Math.max(x[0],y[0])),h=Math.max(0,Math.min(x[3],y[3])-Math.max(x[1],y[1]));return (int)((x[2]-x[0])*(x[3]-x[1])+(y[2]-y[0])*(y[3]-y[1])-w*h); }','int solve(vector<vector<int>>a){auto x=a[0],y=a[1];long long w=max(0,min(x[2],y[2])-max(x[0],y[0])),h=max(0,min(x[3],y[3])-max(x[1],y[1]));return(int)((long long)(x[2]-x[0])*(x[3]-x[1])+(long long)(y[2]-y[0])*(y[3]-y[1])-w*h);}','func solve(a [][]int) int { x,y:=a[0],a[1];w:=mini(x[2],y[2])-maxi(x[0],y[0]);if w<0{w=0};h:=mini(x[3],y[3])-maxi(x[1],y[1]);if h<0{h=0};return (x[2]-x[0])*(x[3]-x[1])+(y[2]-y[0])*(y[3]-y[1])-w*h }\nfunc maxi(a,b int)int{if a>b{return a};return b}\nfunc mini(a,b int)int{if a<b{return a};return b}'),
    tests:[{stdin:'0 0 3 2;2 1 5 4',expectedStdout:'14',isSample:true},{stdin:'0 0 1 1;1 0 2 1',expectedStdout:'2',isSample:true},{stdin:'0 0 2 2;3 3 4 4',expectedStdout:'5'},{stdin:'-2 -2 2 2;-1 -1 1 1',expectedStdout:'16'}] }),
];
