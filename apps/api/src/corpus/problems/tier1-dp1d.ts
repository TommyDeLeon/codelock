import { AUTHORED, type ProblemDefinition } from '../problem.js';

/** Tier 1 — 1-D dynamic programming: one answer per prefix, index, or total. */
const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = {
  tier: 'TIER_1',
  patternFamily: 'DP_1D',
  provenance: AUTHORED,
} as const;

export const TIER_1_DP_1D_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base, slug: 'climbing-steps-count', title: 'Ways Up the Staircase', difficulty: 'EASY',
    patternTags: ['dynamic-programming', 'fibonacci', 'state-transition'], signatureId: 'fn:int->int', avgSolveSeconds: 360,
    promptMarkdown: ['A staircase has `n` steps. On each move, climb either one step or two steps. Return how many different sequences of moves reach exactly the top.', '', '**Example**', '', '```', 'input:  4', 'output: 5', '```', '', 'The sequences are `1+1+1+1`, `1+1+2`, `1+2+1`, `2+1+1`, and `2+2`. `n` is between 0 and 45. For **zero steps**, there is one answer: take no moves.'].join('\n'),
    editorialMarkdown: ['## 1-D DP: remember the two previous totals', '', 'Let `ways[i]` mean the number of ways to stand on step `i`. The final move onto that step was either a one-step move from `i - 1`, or a two-step move from `i - 2`. Those groups cannot overlap, so `ways[i] = ways[i - 1] + ways[i - 2]`. The base cases are `ways[0] = 1` and `ways[1] = 1`.', '', 'The quiet mistake is choosing `ways[0] = 0` because there are no moves. That makes every later answer too small: the empty sequence is one valid way to already be at the top. Only the previous two values are needed, so keep two variables rather than an array.', '', 'Time is O(n), bounded by the number of steps; extra space is O(1).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a) {\n  let prev = 1, cur = 1;\n  for (let i = 2; i <= a; i++) { const next = prev + cur; prev = cur; cur = next; }\n  return cur;\n}',
      TYPESCRIPT: 'function solve(a: number): number {\n  let prev = 1, cur = 1;\n  for (let i = 2; i <= a; i++) { const next = prev + cur; prev = cur; cur = next; }\n  return cur;\n}',
      PYTHON: 'def solve(a):\n    prev, cur = 1, 1\n    for _ in range(2, a + 1):\n        prev, cur = cur, prev + cur\n    return cur',
      JAVA: '    static int solve(int a) {\n        int prev = 1, cur = 1;\n        for (int i = 2; i <= a; i++) {\n            int next = prev + cur;\n            prev = cur;\n            cur = next;\n        }\n        return cur;\n    }',
      CPP: 'int solve(int a) {\n    int prev = 1, cur = 1;\n    for (int i = 2; i <= a; i++) {\n        int next = prev + cur;\n        prev = cur;\n        cur = next;\n    }\n    return cur;\n}',
      GO: 'func solve(a int) int {\n\tprev, cur := 1, 1\n\tfor i := 2; i <= a; i++ {\n\t\tprev, cur = cur, prev+cur\n\t}\n\treturn cur\n}',
    },
    tests: [{ stdin: '4', expectedStdout: '5', isSample: true }, { stdin: '2', expectedStdout: '2', isSample: true }, { stdin: '0', expectedStdout: '1' }, { stdin: '10', expectedStdout: '89' }, { stdin: '45', expectedStdout: '1836311903' }],
  }),
  p({
    ...base, slug: 'largest-non-adjacent-sum', title: 'Take Without Neighbours', difficulty: 'MEDIUM',
    patternTags: ['dynamic-programming', 'house-robber', 'optimization'], signatureId: 'fn:ints->int', avgSolveSeconds: 540,
    promptMarkdown: ['Choose numbers from the list to maximize their sum, but never choose two neighbouring positions. Return the largest possible sum. You may choose no numbers.', '', '**Example**', '', '```', 'input:  2 7 9 3 1', 'output: 12', '```', '', 'Take `2`, `9`, and `1`. Values may be negative. For an **empty list** or a list of only negative values, return `0`.'].join('\n'),
    editorialMarkdown: ['## 1-D DP: take this position or skip it', '', 'After reading a number, there are exactly two useful outcomes: the best sum that skips it, and the best sum that takes it. Taking it must extend the best answer from two positions earlier; skipping it keeps the best answer from the previous position. Thus `best[i] = max(best[i - 1], best[i - 2] + a[i])`.', '', 'The quiet mistake is adding adjacent values because each seems helpful. That violates the rule and can look convincing on positive examples. Starting both remembered answers at zero also handles the named empty and all-negative cases: choosing nothing remains available. The recurrence needs only two prior totals.', '', 'Time is O(n), bounded by list length, and extra space is O(1).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a) {\n  let two = 0, one = 0;\n  for (const x of a) { const cur = Math.max(one, two + x); two = one; one = cur; }\n  return one;\n}', TYPESCRIPT: 'function solve(a: number[]): number {\n  let two = 0, one = 0;\n  for (const x of a) { const cur = Math.max(one, two + x); two = one; one = cur; }\n  return one;\n}',
      PYTHON: 'def solve(a):\n    two = one = 0\n    for x in a:\n        two, one = one, max(one, two + x)\n    return one', JAVA: '    static int solve(int[] a) {\n        int two = 0, one = 0;\n        for (int x : a) {\n            int cur = Math.max(one, two + x);\n            two = one;\n            one = cur;\n        }\n        return one;\n    }',
      CPP: 'int solve(vector<int> a) {\n    int two = 0, one = 0;\n    for (int x : a) {\n        int cur = max(one, two + x);\n        two = one;\n        one = cur;\n    }\n    return one;\n}', GO: 'func solve(a []int) int {\n\ttwo, one := 0, 0\n\tfor _, x := range a {\n\t\tcur := one\n\t\tif two+x > cur { cur = two + x }\n\t\ttwo, one = one, cur\n\t}\n\treturn one\n}',
    }, tests: [{ stdin: '2 7 9 3 1', expectedStdout: '12', isSample: true }, { stdin: '2 1 1 2', expectedStdout: '4', isSample: true }, { stdin: '', expectedStdout: '0' }, { stdin: '-3 -1 -2', expectedStdout: '0' }, { stdin: '5', expectedStdout: '5' }],
  }),
  p({
    ...base, slug: 'largest-contiguous-sum', title: 'Best Unbroken Run', difficulty: 'MEDIUM',
    patternTags: ['dynamic-programming', 'kadane', 'subarray'], signatureId: 'fn:ints->int', avgSolveSeconds: 540,
    promptMarkdown: ['Return the largest sum of a non-empty contiguous run of numbers.', '', '**Example**', '', '```', 'input:  -2 1 -3 4 -1 2 1 -5 4', 'output: 6', '```', '', 'The best run is `4 -1 2 1`. The list has at least one number and may contain negatives. If every number is negative, return the least negative single number.'].join('\n'),
    editorialMarkdown: ['## 1-D DP: the best run ending here', '', 'Let `ending` be the largest sum of a run that must end at the current number. Either extend yesterday’s run, or start fresh at this number: `ending = max(x, ending + x)`. The answer is the largest `ending` seen anywhere. This is dynamic programming because each position reuses the best result for the immediately previous position.', '', 'The quiet mistake is initializing the answer to zero. It silently returns zero for all-negative input, even though the run must be non-empty. Initialize from the first number instead. Resetting when an earlier sum is harmful is not a greedy shortcut; it is exactly the recurrence choosing the new run.', '', 'Time is O(n), bounded by the list length, and extra space is O(1).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a) {\n  let ending = a[0], best = a[0];\n  for (let i = 1; i < a.length; i++) { ending = Math.max(a[i], ending + a[i]); best = Math.max(best, ending); }\n  return best;\n}', TYPESCRIPT: 'function solve(a: number[]): number {\n  let ending = a[0], best = a[0];\n  for (let i = 1; i < a.length; i++) { ending = Math.max(a[i], ending + a[i]); best = Math.max(best, ending); }\n  return best;\n}', PYTHON: 'def solve(a):\n    ending = best = a[0]\n    for x in a[1:]:\n        ending = max(x, ending + x)\n        best = max(best, ending)\n    return best', JAVA: '    static int solve(int[] a) {\n        int ending = a[0], best = a[0];\n        for (int i = 1; i < a.length; i++) {\n            ending = Math.max(a[i], ending + a[i]);\n            best = Math.max(best, ending);\n        }\n        return best;\n    }', CPP: 'int solve(vector<int> a) {\n    int ending = a[0], best = a[0];\n    for (int i = 1; i < (int)a.size(); i++) {\n        ending = max(a[i], ending + a[i]);\n        best = max(best, ending);\n    }\n    return best;\n}', GO: 'func solve(a []int) int {\n\tending, best := a[0], a[0]\n\tfor _, x := range a[1:] {\n\t\tif ending+x > x { ending += x } else { ending = x }\n\t\tif ending > best { best = ending }\n\t}\n\treturn best\n}',
    }, tests: [{ stdin: '-2 1 -3 4 -1 2 1 -5 4', expectedStdout: '6', isSample: true }, { stdin: '1', expectedStdout: '1', isSample: true }, { stdin: '-3 -2 -5', expectedStdout: '-2' }, { stdin: '5 -1 5', expectedStdout: '9' }, { stdin: '0 0', expectedStdout: '0' }],
  }),
  p({
    ...base, slug: 'fewest-coins-for-total', title: 'Fewest Coins to Make a Total', difficulty: 'MEDIUM',
    patternTags: ['dynamic-programming', 'unbounded-knapsack', 'minimization'], signatureId: 'fn:ints,int->int', avgSolveSeconds: 660,
    promptMarkdown: ['The first line lists positive coin values; the second line is a target. Return the fewest coins whose values add to the target. Each value may be used any number of times. Return `-1` if the target cannot be made.', '', '**Example**', '', '```', 'input:  1 2 5', '        11', 'output: 3', '```', '', 'Use `5 + 5 + 1`. The target is between 0 and 10,000. For target `0`, return `0`; an empty coin list can make no positive target.'].join('\n'),
    editorialMarkdown: ['## 1-D DP: build every smaller total first', '', 'Let `dp[t]` be the fewest coins needed for total `t`. Start with `dp[0] = 0` and mark every other total as impossible. For each reachable earlier total `t - coin`, one more coin makes `t`, so try `dp[t - coin] + 1` and retain the minimum. Processing totals upward permits a denomination to be used repeatedly.', '', 'The quiet mistake is treating an impossible state as zero. Then a target can appear to be made with one coin after an unreachable predecessor. Use a sentinel larger than any possible answer and convert it to `-1` only at the end. Greedily taking the largest coin also fails for values such as `1 3 4` and target `6`.', '', 'Time is O(target × number of coins); the array of target states uses O(target) space.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a, b) {\n  const dp = new Array(b + 1).fill(b + 1); dp[0] = 0;\n  for (let t = 1; t <= b; t++) for (const c of a) if (c <= t) dp[t] = Math.min(dp[t], dp[t - c] + 1);\n  return dp[b] > b ? -1 : dp[b];\n}', TYPESCRIPT: 'function solve(a: number[], b: number): number {\n  const dp: number[] = new Array(b + 1).fill(b + 1); dp[0] = 0;\n  for (let t = 1; t <= b; t++) for (const c of a) if (c <= t) dp[t] = Math.min(dp[t], dp[t - c] + 1);\n  return dp[b] > b ? -1 : dp[b];\n}', PYTHON: 'def solve(a, b):\n    dp = [b + 1] * (b + 1)\n    dp[0] = 0\n    for t in range(1, b + 1):\n        for c in a:\n            if c <= t:\n                dp[t] = min(dp[t], dp[t - c] + 1)\n    return -1 if dp[b] > b else dp[b]', JAVA: '    static int solve(int[] a, int b) {\n        int[] dp = new int[b + 1];\n        Arrays.fill(dp, b + 1);\n        dp[0] = 0;\n        for (int t = 1; t <= b; t++) for (int c : a) if (c <= t) dp[t] = Math.min(dp[t], dp[t - c] + 1);\n        return dp[b] > b ? -1 : dp[b];\n    }', CPP: 'int solve(vector<int> a, int b) {\n    vector<int> dp(b + 1, b + 1);\n    dp[0] = 0;\n    for (int t = 1; t <= b; t++) for (int c : a) if (c <= t) dp[t] = min(dp[t], dp[t-c] + 1);\n    return dp[b] > b ? -1 : dp[b];\n}', GO: 'func solve(a []int, b int) int {\n\tdp := make([]int, b+1)\n\tfor i := 1; i <= b; i++ { dp[i] = b + 1 }\n\tfor t := 1; t <= b; t++ {\n\t\tfor _, c := range a { if c <= t && dp[t-c]+1 < dp[t] { dp[t] = dp[t-c] + 1 } }\n\t}\n\tif dp[b] > b { return -1 }; return dp[b]\n}',
    }, tests: [{ stdin: '1 2 5\n11', expectedStdout: '3', isSample: true }, { stdin: '2\n3', expectedStdout: '-1', isSample: true }, { stdin: '\n7', expectedStdout: '-1' }, { stdin: '2 4\n0', expectedStdout: '0' }, { stdin: '1 3 4\n6', expectedStdout: '2' }],
  }),
  p({
    ...base, slug: 'decode-digit-message-count', title: 'Read the Digit Message', difficulty: 'MEDIUM',
    patternTags: ['dynamic-programming', 'string', 'counting'], signatureId: 'fn:string->int', avgSolveSeconds: 660,
    promptMarkdown: ['A digit string encodes letters: `1` means A through `26` means Z. A decoding splits the entire string into valid one- or two-digit letter codes. Return the number of decodings.', '', '**Example**', '', '```', 'input:  226', 'output: 3', '```', '', 'The splits are `2 2 6`, `22 6`, and `2 26`. The string contains only digits and has at most 45 characters. `0` cannot be a letter on its own, so a string beginning with `0` has zero decodings.'].join('\n'),
    editorialMarkdown: ['## 1-D DP: count valid final chunks', '', 'Let `dp[i]` count decodings of the first `i` characters. To finish such a prefix, take a valid final one-digit code, or take a valid final two-digit code from 10 through 26. Add the counts of the prefixes that remain. Two variables store the counts for the previous two prefix lengths.', '', 'The quiet mistake is allowing `06` as six. A two-character slice is valid only from 10 to 26, and a zero can contribute only as the second digit of 10 or 20. Checking both conditions separately prevents an invalid prefix from leaking into later totals. An empty string has zero decodings here because the input represents a message, not an already-complete split.', '', 'Time is O(n), bounded by string length, and extra space is O(1).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a) {\n  if (a.length === 0 || a[0] === \'0\') return 0;\n  let two = 1, one = 1;\n  for (let i = 1; i < a.length; i++) { const pair = Number(a.slice(i - 1, i + 1)); const cur = (a[i] !== \'0\' ? one : 0) + (pair >= 10 && pair <= 26 ? two : 0); two = one; one = cur; }\n  return one;\n}', TYPESCRIPT: 'function solve(a: string): number {\n  if (a.length === 0 || a[0] === \'0\') return 0;\n  let two = 1, one = 1;\n  for (let i = 1; i < a.length; i++) { const pair = Number(a.slice(i - 1, i + 1)); const cur = (a[i] !== \'0\' ? one : 0) + (pair >= 10 && pair <= 26 ? two : 0); two = one; one = cur; }\n  return one;\n}', PYTHON: "def solve(a):\n    if not a or a[0] == '0':\n        return 0\n    two = one = 1\n    for i in range(1, len(a)):\n        pair = int(a[i-1:i+1])\n        cur = (one if a[i] != '0' else 0) + (two if 10 <= pair <= 26 else 0)\n        two, one = one, cur\n    return one", JAVA: "    static int solve(String a) {\n        if (a.length() == 0 || a.charAt(0) == '0') return 0;\n        int two = 1, one = 1;\n        for (int i = 1; i < a.length(); i++) {\n            int pair = (a.charAt(i - 1) - '0') * 10 + a.charAt(i) - '0';\n            int cur = (a.charAt(i) != '0' ? one : 0) + (pair >= 10 && pair <= 26 ? two : 0);\n            two = one;\n            one = cur;\n        }\n        return one;\n    }", CPP: "int solve(string a) {\n    if (a.empty() || a[0] == '0') return 0;\n    int two = 1, one = 1;\n    for (int i = 1; i < (int)a.size(); i++) {\n        int pair = (a[i-1] - '0') * 10 + a[i] - '0';\n        int cur = (a[i] != '0' ? one : 0) + (pair >= 10 && pair <= 26 ? two : 0);\n        two = one;\n        one = cur;\n    }\n    return one;\n}", GO: "func solve(a string) int {\n\tif len(a) == 0 || a[0] == '0' { return 0 }\n\ttwo, one := 1, 1\n\tfor i := 1; i < len(a); i++ {\n\t\tpair := int(a[i-1]-'0')*10 + int(a[i]-'0')\n\t\tcur := 0\n\t\tif a[i] != '0' { cur += one }; if pair >= 10 && pair <= 26 { cur += two }\n\t\ttwo, one = one, cur\n\t}\n\treturn one\n}",
    }, tests: [{ stdin: '226', expectedStdout: '3', isSample: true }, { stdin: '06', expectedStdout: '0', isSample: true }, { stdin: '10', expectedStdout: '1' }, { stdin: '100', expectedStdout: '0' }, { stdin: '11106', expectedStdout: '2' }],
  }),
  p({
    ...base, slug: 'longest-rising-subsequence', title: 'Longest Strictly Rising Pick', difficulty: 'MEDIUM',
    patternTags: ['dynamic-programming', 'subsequence', 'lis'], signatureId: 'fn:ints->int', avgSolveSeconds: 720,
    promptMarkdown: ['Return the length of the longest subsequence whose values are strictly increasing. A subsequence keeps original order but may skip positions.', '', '**Example**', '', '```', 'input:  10 9 2 5 3 7 101 18', 'output: 4', '```', '', 'One longest pick is `2 3 7 101`. Equal values cannot both appear in a strictly rising pick. For an **empty list**, return `0`.'].join('\n'),
    editorialMarkdown: ['## 1-D DP: best sequence ending at each index', '', 'Let `dp[i]` be the length of the best strictly rising subsequence that ends at index `i`. It can extend any earlier index `j` whose value is smaller, so start every `dp[i]` at one and try `dp[j] + 1` for every qualifying `j`. The answer is the maximum state, not necessarily the final state.', '', 'The quiet mistake is using `<=` and accidentally accepting equal values. Another is returning only `dp[n - 1]`; the longest subsequence may end much earlier. This O(n²) version is intentional: it makes the state and transition visible before the faster tails technique.', '', 'Time is O(n²), from comparing every earlier position for every ending position; space is O(n).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a) {\n  const dp = new Array(a.length).fill(1); let best = 0;\n  for (let i = 0; i < a.length; i++) { for (let j = 0; j < i; j++) if (a[j] < a[i]) dp[i] = Math.max(dp[i], dp[j] + 1); best = Math.max(best, dp[i]); }\n  return best;\n}', TYPESCRIPT: 'function solve(a: number[]): number {\n  const dp: number[] = new Array(a.length).fill(1); let best = 0;\n  for (let i = 0; i < a.length; i++) { for (let j = 0; j < i; j++) if (a[j] < a[i]) dp[i] = Math.max(dp[i], dp[j] + 1); best = Math.max(best, dp[i]); }\n  return best;\n}', PYTHON: 'def solve(a):\n    dp = [1] * len(a)\n    best = 0\n    for i in range(len(a)):\n        for j in range(i):\n            if a[j] < a[i]:\n                dp[i] = max(dp[i], dp[j] + 1)\n        best = max(best, dp[i])\n    return best', JAVA: '    static int solve(int[] a) {\n        int[] dp = new int[a.length];\n        int best = 0;\n        for (int i = 0; i < a.length; i++) {\n            dp[i] = 1;\n            for (int j = 0; j < i; j++) if (a[j] < a[i]) dp[i] = Math.max(dp[i], dp[j] + 1);\n            best = Math.max(best, dp[i]);\n        }\n        return best;\n    }', CPP: 'int solve(vector<int> a) {\n    vector<int> dp(a.size(), 1);\n    int best = 0;\n    for (int i = 0; i < (int)a.size(); i++) {\n        for (int j = 0; j < i; j++) if (a[j] < a[i]) dp[i] = max(dp[i], dp[j] + 1);\n        best = max(best, dp[i]);\n    }\n    return best;\n}', GO: 'func solve(a []int) int {\n\tdp := make([]int, len(a)); best := 0\n\tfor i := range a {\n\t\tdp[i] = 1\n\t\tfor j := 0; j < i; j++ { if a[j] < a[i] && dp[j]+1 > dp[i] { dp[i] = dp[j] + 1 } }\n\t\tif dp[i] > best { best = dp[i] }\n\t}\n\treturn best\n}',
    }, tests: [{ stdin: '10 9 2 5 3 7 101 18', expectedStdout: '4', isSample: true }, { stdin: '0 1 0 3 2 3', expectedStdout: '4', isSample: true }, { stdin: '', expectedStdout: '0' }, { stdin: '7 7 7', expectedStdout: '1' }, { stdin: '5 4 3 2 1', expectedStdout: '1' }],
  }),
  p({
    ...base, slug: 'equal-half-sum', title: 'Split Into Equal Totals', difficulty: 'MEDIUM',
    patternTags: ['dynamic-programming', 'subset-sum', 'boolean-dp'], signatureId: 'fn:ints->bool', avgSolveSeconds: 720,
    promptMarkdown: ['Decide whether the list can be split into two groups with equal sums. Every number must go in exactly one group.', '', '**Example**', '', '```', 'input:  1 5 11 5', 'output: true', '```', '', 'One group is `11`; the other is `1 5 5`. Values are non-negative and the total is at most 20,000. An **empty list** can be split into two empty groups, so return `true`.'].join('\n'),
    editorialMarkdown: ['## 1-D DP: which partial sums are reachable?', '', 'If the total is odd, equal groups are impossible. Otherwise it is enough to decide whether some subset reaches half the total. Let `reachable[s]` mean that chosen numbers so far can sum to `s`; initially only zero is reachable. For each number, walk sums downward and mark `s` reachable when `s - x` was reachable before this number.', '', 'The downward direction is the important detail. Walking upward lets the current number update another state and then be reused again in the same pass, which changes this into an unlimited-use coin problem. The named empty case works because half is zero and `reachable[0]` begins true.', '', 'Time is O(n × total), where total is the sum of the values; space is O(total).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a) {\n  const sum = a.reduce((x, y) => x + y, 0); if (sum % 2) return false;\n  const target = sum / 2, ok = new Array(target + 1).fill(false); ok[0] = true;\n  for (const x of a) for (let s = target; s >= x; s--) ok[s] = ok[s] || ok[s - x];\n  return ok[target];\n}', TYPESCRIPT: 'function solve(a: number[]): boolean {\n  const sum = a.reduce((x, y) => x + y, 0); if (sum % 2) return false;\n  const target = sum / 2, ok: boolean[] = new Array(target + 1).fill(false); ok[0] = true;\n  for (const x of a) for (let s = target; s >= x; s--) ok[s] = ok[s] || ok[s - x];\n  return ok[target];\n}', PYTHON: 'def solve(a):\n    total = sum(a)\n    if total % 2:\n        return False\n    target = total // 2\n    ok = [False] * (target + 1)\n    ok[0] = True\n    for x in a:\n        for s in range(target, x - 1, -1):\n            ok[s] = ok[s] or ok[s - x]\n    return ok[target]', JAVA: '    static boolean solve(int[] a) {\n        int sum = 0;\n        for (int x : a) sum += x;\n        if (sum % 2 != 0) return false;\n        int target = sum / 2;\n        boolean[] ok = new boolean[target + 1];\n        ok[0] = true;\n        for (int x : a) for (int s = target; s >= x; s--) ok[s] = ok[s] || ok[s - x];\n        return ok[target];\n    }', CPP: 'bool solve(vector<int> a) {\n    int sum = 0; for (int x : a) sum += x;\n    if (sum % 2) return false;\n    int target = sum / 2; vector<char> ok(target + 1, 0); ok[0] = 1;\n    for (int x : a) for (int s = target; s >= x; s--) ok[s] = ok[s] || ok[s-x];\n    return ok[target];\n}', GO: 'func solve(a []int) bool {\n\ttotal := 0; for _, x := range a { total += x }; if total%2 != 0 { return false }\n\ttarget := total / 2; ok := make([]bool, target+1); ok[0] = true\n\tfor _, x := range a { for s := target; s >= x; s-- { ok[s] = ok[s] || ok[s-x] } }\n\treturn ok[target]\n}',
    }, tests: [{ stdin: '1 5 11 5', expectedStdout: 'true', isSample: true }, { stdin: '1 2 3 5', expectedStdout: 'false', isSample: true }, { stdin: '', expectedStdout: 'true' }, { stdin: '0 0 0', expectedStdout: 'true' }, { stdin: '2 2 3 5', expectedStdout: 'false' }],
  }),
  p({
    ...base, slug: 'fewest-perfect-square-sum', title: 'Build a Number From Squares', difficulty: 'MEDIUM',
    patternTags: ['dynamic-programming', 'minimization', 'perfect-squares'], signatureId: 'fn:int->int', avgSolveSeconds: 660,
    promptMarkdown: ['Return the fewest perfect-square numbers that add to `n`. A square may be used more than once.', '', '**Example**', '', '```', 'input:  12', 'output: 3', '```', '', 'Use `4 + 4 + 4`. `n` is between 0 and 10,000. For **zero**, return `0`; for a square such as `16`, return `1`.'].join('\n'),
    editorialMarkdown: ['## 1-D DP: choose the final square', '', 'Let `dp[t]` be the fewest squares that make total `t`. Every solution for `t` ends with some square `s²` no larger than `t`, following a solution for `t - s²`. Try every possible final square and take the smallest `dp[t - s²] + 1`. Start with `dp[0] = 0` and fill totals upward.', '', 'The quiet mistake is only trying squares near the square root. A smaller final square can complete a much better earlier total, as `12` demonstrates. Initializing each state to its own value is a safe upper bound because `t` copies of `1²` always work. That also avoids confusing an unfilled zero with a real answer.', '', 'Time is O(n √n), from checking all squares for each total; space is O(n).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a) {\n  const dp = new Array(a + 1); dp[0] = 0;\n  for (let t = 1; t <= a; t++) { dp[t] = t; for (let s = 1; s * s <= t; s++) dp[t] = Math.min(dp[t], dp[t - s * s] + 1); }\n  return dp[a];\n}', TYPESCRIPT: 'function solve(a: number): number {\n  const dp: number[] = new Array(a + 1); dp[0] = 0;\n  for (let t = 1; t <= a; t++) { dp[t] = t; for (let s = 1; s * s <= t; s++) dp[t] = Math.min(dp[t], dp[t - s * s] + 1); }\n  return dp[a];\n}', PYTHON: 'def solve(a):\n    dp = [0] * (a + 1)\n    for t in range(1, a + 1):\n        dp[t] = t\n        for s in range(1, int(t ** 0.5) + 1):\n            dp[t] = min(dp[t], dp[t - s * s] + 1)\n    return dp[a]', JAVA: '    static int solve(int a) {\n        int[] dp = new int[a + 1];\n        for (int t = 1; t <= a; t++) {\n            dp[t] = t;\n            for (int s = 1; s * s <= t; s++) dp[t] = Math.min(dp[t], dp[t - s * s] + 1);\n        }\n        return dp[a];\n    }', CPP: 'int solve(int a) {\n    vector<int> dp(a + 1, 0);\n    for (int t = 1; t <= a; t++) {\n        dp[t] = t;\n        for (int s = 1; s * s <= t; s++) dp[t] = min(dp[t], dp[t-s*s] + 1);\n    }\n    return dp[a];\n}', GO: 'func solve(a int) int {\n\tdp := make([]int, a+1)\n\tfor t := 1; t <= a; t++ {\n\t\tdp[t] = t\n\t\tfor s := 1; s*s <= t; s++ { if dp[t-s*s]+1 < dp[t] { dp[t] = dp[t-s*s] + 1 } }\n\t}\n\treturn dp[a]\n}',
    }, tests: [{ stdin: '12', expectedStdout: '3', isSample: true }, { stdin: '13', expectedStdout: '2', isSample: true }, { stdin: '0', expectedStdout: '0' }, { stdin: '16', expectedStdout: '1' }, { stdin: '43', expectedStdout: '3' }],
  }),
];
