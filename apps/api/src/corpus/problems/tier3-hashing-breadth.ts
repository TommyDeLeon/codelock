import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_3', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_3_HASHING_BREADTH_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'largest-remainder-bucket', title:'The Busiest Remainder', difficulty:'EASY', patternTags:['hash-map','derived-key','counting'], signatureId:'fn:ints,int->int', avgSolveSeconds:540,
    promptMarkdown:['Put every number into the bucket given by its non-negative remainder after division by k. Return the largest bucket size.','', '**Example**','', '```','input: 1 5 8 2 11','3','output: 4','```','', 'The remainders are 1, 2, 2, 2, 2, so the largest bucket has four values. The list is non-empty and k is positive; negative numbers use a remainder from 0 through k - 1.'].join('\n'),
    editorialMarkdown:['## Count by a normalized derived key','','A map turns each number into its remainder bucket, then increments that bucket once. Normalizing a negative remainder by adding k keeps equivalent values together: -1 and 2 belong together when k is 3. The answer is simply the greatest frequency stored.','','The quiet mistake is using a language remainder directly for negatives, which can create a separate negative bucket. The normalized key is the actual mathematical remainder. Time is O(n), bounded by the input length, and the map uses O(min(n, k)) space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let m=new Map(),z=0;for(let x of a){let r=((x%b)+b)%b,mv=(m.get(r)||0)+1;m.set(r,mv);if(mv>z)z=mv;}return z;}','function solve(a:number[],b:number):number{const m=new Map<number,number>();let z=0;for(const x of a){const r=((x%b)+b)%b,v=(m.get(r)||0)+1;m.set(r,v);if(v>z)z=v;}return z;}','def solve(a,b):\n    m={};best=0\n    for x in a:\n        r=x%b;m[r]=m.get(r,0)+1;best=max(best,m[r])\n    return best','    static int solve(int[] a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();int z=0;for(int x:a){int r=((x%b)+b)%b,v=m.getOrDefault(r,0)+1;m.put(r,v);if(v>z)z=v;}return z;}','int solve(vector<int>a,int b){unordered_map<int,int>m;int z=0;for(int x:a){int r=((x%b)+b)%b,v=++m[r];if(v>z)z=v;}return z;}','func solve(a []int,b int) int {m:=map[int]int{};z:=0;for _,x:=range a{r:=((x%b)+b)%b;m[r]++;if m[r]>z{z=m[r]}};return z}'),
    tests:[{stdin:'1 5 8 2 11\n3',expectedStdout:'4',isSample:true},{stdin:'-1 2 5\n3',expectedStdout:'3',isSample:true},{stdin:'7\n4',expectedStdout:'1'},{stdin:'0 4 1 5\n2',expectedStdout:'2'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'last-singleton-in-input-order', title:'Last Value Seen Once', difficulty:'EASY', patternTags:['hash-map','frequency','order'], signatureId:'fn:ints->int', avgSolveSeconds:540,
    promptMarkdown:['Return the last value in input order whose total frequency is exactly one. Return -1 when every value repeats.','', '**Example**','', '```','input: 4 7 4 9 2 7','output: 2','```','', 'Both 9 and 2 occur once, and 2 appears later. The list is non-empty; no singleton is the named edge case.'].join('\n'),
    editorialMarkdown:['## Count first, scan from the end','','Whether a value is single cannot be known at its first appearance, so first count all values. Then walk the original list backwards and stop at the first count of one. That backward scan preserves the requested last-in-input order without relying on map iteration.','','The quiet mistake is returning the last key inserted into a map; map key order is not the task order and repeated values complicate it. Time is O(n), bounded by list length, with O(n) frequency storage.'].join('\n'),
    referenceSolution:six('function solve(a){let m={};for(let x of a)m[x]=(m[x]||0)+1;for(let i=a.length-1;i>=0;i--)if(m[a[i]]===1)return a[i];return -1;}','function solve(a:number[]):number{const m=new Map<number,number>();for(const x of a)m.set(x,(m.get(x)||0)+1);for(let i=a.length-1;i>=0;i--)if(m.get(a[i])===1)return a[i];return -1;}','def solve(a):\n    m={}\n    for x in a:m[x]=m.get(x,0)+1\n    for x in a[::-1]:\n        if m[x]==1:return x\n    return -1','    static int solve(int[]a){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();for(int x:a)m.put(x,m.getOrDefault(x,0)+1);for(int i=a.length-1;i>=0;i--)if(m.get(a[i])==1)return a[i];return -1;}','int solve(vector<int>a){unordered_map<int,int>m;for(int x:a)m[x]++;for(int i=(int)a.size()-1;i>=0;i--)if(m[a[i]]==1)return a[i];return -1;}','func solve(a []int) int {m:=map[int]int{};for _,x:=range a{m[x]++};for i:=len(a)-1;i>=0;i--{if m[a[i]]==1{return a[i]}};return -1}'),
    tests:[{stdin:'4 7 4 9 2 7',expectedStdout:'2',isSample:true},{stdin:'1 2 1',expectedStdout:'2',isSample:true},{stdin:'5 5',expectedStdout:'-1'},{stdin:'-3',expectedStdout:'-3'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'distinct-common-values-ascending', title:'Shared Values, Once, Ascending', difficulty:'EASY', patternTags:['hash-set','intersection','ordering'], signatureId:'fn:ints,ints->ints', avgSolveSeconds:600,
    promptMarkdown:['Return values that occur in both lists, once each, in ascending numeric order.','', '**Example**','', '```','input: 4 1 2 2','2 4 2 5','output: 2 4','```','', 'Duplicates do not repeat in the answer. Either input may be empty; then the output is empty.'].join('\n'),
    editorialMarkdown:['## Set intersection followed by a stated order','','Store the first list in a set, scan the second, and add a matching value to an answer set. The second set removes duplicate encounters. Finally sort the distinct matches because the required order is numeric, not an accidental hash-table order.','','The quiet mistake is emitting while scanning the second list and claiming that order is acceptable. It is deterministic but not ascending. Time is O(n+m+d log d), bounded by both list lengths and d shared values; space is O(n+m).'].join('\n'),
    referenceSolution:six('function solve(a,b){let s=new Set(a),r=[...new Set(b.filter(x=>s.has(x)))];return r.sort((x,y)=>x-y);}','function solve(a:number[],b:number[]):number[]{const s=new Set(a),r=[...new Set(b.filter(x=>s.has(x)))];return r.sort((x,y)=>x-y);}','def solve(a,b):\n    return sorted(set(a)&set(b))','    static int[] solve(int[]a,int[]b){java.util.HashSet<Integer>s=new java.util.HashSet<>(),r=new java.util.HashSet<>();for(int x:a)s.add(x);for(int x:b)if(s.contains(x))r.add(x);int[]z=new int[r.size()];int i=0;for(int x:r)z[i++]=x;java.util.Arrays.sort(z);return z;}','vector<int> solve(vector<int>a,vector<int>b){unordered_set<int>s(a.begin(),a.end()),u;for(int x:b)if(s.count(x))u.insert(x);vector<int>r(u.begin(),u.end());sort(r.begin(),r.end());return r;}','func solve(a []int,b []int) []int {s:=map[int]bool{};u:=map[int]bool{};for _,x:=range a{s[x]=true};for _,x:=range b{if s[x]{u[x]=true}};r:=[]int{};for x:=range u{r=append(r,x)};for i:=0;i<len(r);i++{for j:=i+1;j<len(r);j++{if r[j]<r[i]{r[i],r[j]=r[j],r[i]}}};return r}'),
    tests:[{stdin:'4 1 2 2\n2 4 2 5',expectedStdout:'2 4',isSample:true},{stdin:'-1 0\n0 -1 -1',expectedStdout:'-1 0',isSample:true},{stdin:'1\n2',expectedStdout:''},{stdin:'3 3\n3',expectedStdout:'3'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'first-list-only-values-ascending', title:'Values Missing From the Second List', difficulty:'MEDIUM', patternTags:['hash-set','difference','ordering'], signatureId:'fn:ints,ints->ints', avgSolveSeconds:660,
    promptMarkdown:['Return distinct values appearing in the first list but never in the second, ordered ascending.','', '**Example**','', '```','input: 5 1 5 3 2','2 4 3','output: 1 5','```','', 'Repeated first-list values appear once. If all first-list values occur in the second list, return an empty list.'].join('\n'),
    editorialMarkdown:['## Set difference with explicit output order','','A membership set for the second list answers whether each first-list value is excluded. Put surviving values in another set so repeated first values do not leak into output, then sort it ascending. That sequence separates membership, deduplication, and presentation.','','The quiet mistake is subtracting frequencies: this task asks whether a value exists in the second list at all, not for multiset leftovers. Time is O(n+m+d log d), bounded by input sizes and d answers; space is O(n+m).'].join('\n'),
    referenceSolution:six('function solve(a,b){let s=new Set(b),r=[...new Set(a.filter(x=>!s.has(x)))];return r.sort((x,y)=>x-y);}','function solve(a:number[],b:number[]):number[]{const s=new Set(b),r=[...new Set(a.filter(x=>!s.has(x)))];return r.sort((x,y)=>x-y);}','def solve(a,b):\n    return sorted(set(a)-set(b))','    static int[] solve(int[]a,int[]b){java.util.HashSet<Integer>s=new java.util.HashSet<>(),u=new java.util.HashSet<>();for(int x:b)s.add(x);for(int x:a)if(!s.contains(x))u.add(x);int[]r=new int[u.size()];int i=0;for(int x:u)r[i++]=x;java.util.Arrays.sort(r);return r;}','vector<int> solve(vector<int>a,vector<int>b){unordered_set<int>s(b.begin(),b.end()),u;for(int x:a)if(!s.count(x))u.insert(x);vector<int>r(u.begin(),u.end());sort(r.begin(),r.end());return r;}','func solve(a []int,b []int) []int {s:=map[int]bool{};u:=map[int]bool{};for _,x:=range b{s[x]=true};for _,x:=range a{if !s[x]{u[x]=true}};r:=[]int{};for x:=range u{r=append(r,x)};for i:=0;i<len(r);i++{for j:=i+1;j<len(r);j++{if r[j]<r[i]{r[i],r[j]=r[j],r[i]}}};return r}'),
    tests:[{stdin:'5 1 5 3 2\n2 4 3',expectedStdout:'1 5',isSample:true},{stdin:'1 2\n1 2 3',expectedStdout:'',isSample:true},{stdin:'-2 -1 0\n-1',expectedStdout:'-2 0'},{stdin:'7 7\n8',expectedStdout:'7'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'count-xor-complement-pairs', title:'Pairs That Reach an XOR Target', difficulty:'MEDIUM', patternTags:['hash-map','complement','counting'], signatureId:'fn:ints,int->int', avgSolveSeconds:720,
    promptMarkdown:['Count pairs of different positions whose bitwise XOR equals target. Each pair of positions counts once.','', '**Example**','', '```','input: 1 2 3 1','3','output: 2','```','', 'The pairs are the first 1 with 2, and 2 with the last 1. Return 0 when no pair qualifies; repeated values may form a pair when target is 0.'].join('\n'),
    editorialMarkdown:['## Earlier complements under XOR','','For a current x, the only earlier partner that reaches target is x XOR target. Store frequencies of earlier values and add that partner frequency before recording x. Looking only backward gives each position pair exactly one chance to count.','','The quiet mistake is treating XOR like addition and searching target minus x. It passes coincidental tests but has no algebraic justification. Time is O(n) expected from the map, bounded by the list length, and space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){let m=new Map(),c=0;for(let x of a){c+=m.get(x^b)||0;m.set(x,(m.get(x)||0)+1);}return c;}','function solve(a:number[],b:number):number{const m=new Map<number,number>();let c=0;for(const x of a){c+=m.get(x^b)||0;m.set(x,(m.get(x)||0)+1);}return c;}','def solve(a,b):\n    m={};c=0\n    for x in a:\n        c+=m.get(x^b,0);m[x]=m.get(x,0)+1\n    return c','    static int solve(int[]a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();int c=0;for(int x:a){c+=m.getOrDefault(x^b,0);m.put(x,m.getOrDefault(x,0)+1);}return c;}','int solve(vector<int>a,int b){unordered_map<int,int>m;int c=0;for(int x:a){c+=m[x^b];m[x]++;}return c;}','func solve(a []int,b int) int {m:=map[int]int{};c:=0;for _,x:=range a{c+=m[x^b];m[x]++};return c}'),
    tests:[{stdin:'1 2 3 1\n3',expectedStdout:'2',isSample:true},{stdin:'5 5 5\n0',expectedStdout:'3',isSample:true},{stdin:'1 2\n0',expectedStdout:'0'},{stdin:'0 4 4\n4',expectedStdout:'2'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'repeat-within-position-gap', title:'A Duplicate Close Enough', difficulty:'MEDIUM', patternTags:['hash-map','last-index','window'], signatureId:'fn:ints,int->bool', avgSolveSeconds:660,
    promptMarkdown:['Return true if equal values occur at two positions whose index difference is at most k. Otherwise return false.','', '**Example**','', '```','input: 1 2 3 1 2','3','output: true','```','', 'The two 1s are three positions apart. k is non-negative; k equal to 0 can never use two different positions.'].join('\n'),
    editorialMarkdown:['## Remember the latest index','','The closest earlier copy of a value is always its latest earlier index, so a map needs only that one index. On each value, compare its current index with the stored one before replacing it. A successful comparison proves the requested pair exists.','','The quiet mistake is retaining the first index forever. A later pair can be close even when the first and current copies are too far apart. Time is O(n) expected, bounded by the list length, and the last-index map uses O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let m=new Map();for(let i=0;i<a.length;i++){if(m.has(a[i])&&i-m.get(a[i])<=b)return true;m.set(a[i],i);}return false;}','function solve(a:number[],b:number):boolean{const m=new Map<number,number>();for(let i=0;i<a.length;i++){const j=m.get(a[i]);if(j!==undefined&&i-j<=b)return true;m.set(a[i],i);}return false;}','def solve(a,b):\n    m={}\n    for i,x in enumerate(a):\n        if x in m and i-m[x]<=b:return True\n        m[x]=i\n    return False','    static boolean solve(int[]a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();for(int i=0;i<a.length;i++){if(m.containsKey(a[i])&&i-m.get(a[i])<=b)return true;m.put(a[i],i);}return false;}','bool solve(vector<int>a,int b){unordered_map<int,int>m;for(int i=0;i<(int)a.size();i++){if(m.count(a[i])&&i-m[a[i]]<=b)return true;m[a[i]]=i;}return false;}','func solve(a []int,b int) bool {m:=map[int]int{};for i,x:=range a{if j,ok:=m[x];ok&&i-j<=b{return true};m[x]=i};return false}'),
    tests:[{stdin:'1 2 3 1 2\n3',expectedStdout:'true',isSample:true},{stdin:'1 2 3 1\n2',expectedStdout:'false',isSample:true},{stdin:'7 7\n0',expectedStdout:'false'},{stdin:'-1 4 -1\n2',expectedStdout:'true'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'rarest-value-smallest-tie', title:'Rarest Value, Smallest Wins', difficulty:'MEDIUM', patternTags:['hash-map','frequency','tie-breaking'], signatureId:'fn:ints->int', avgSolveSeconds:600,
    promptMarkdown:['Return the value that occurs **fewest** times. If several values tie on that lowest count, return the smallest of them.','', '**Example**','', '```','input: 4 1 4 1 7','output: 7','```','', '7 occurs once while 1 and 4 occur twice, so 7 is rarest. The list is non-empty and values may be negative.'].join('\n'),
    editorialMarkdown:['## Frequency table read from the bottom','','Count every value first, then scan the finished table for the smallest count, breaking ties toward the smaller value. The count has to be complete before any comparison: a value that looks rare early can be overtaken by a later occurrence, so deciding during the counting pass is wrong.','','The quiet mistake is seeding the answer with `a[0]` and never tracking its count, which returns the first value whenever every count is equal. Track the running best count alongside the best value. Time is O(n) expected, bounded by input length; the table uses O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){const m=new Map();for(const x of a)m.set(x,(m.get(x)||0)+1);let z=null,bc=0;for(const [x,c] of m)if(z===null||c<bc||(c===bc&&x<z)){z=x;bc=c;}return z;}','function solve(a:number[]):number{const m=new Map<number,number>();for(const x of a)m.set(x,(m.get(x)||0)+1);let z:number|null=null,bc=0;for(const [x,c] of m)if(z===null||c<bc||(c===bc&&x<z)){z=x;bc=c;}return z as number;}','def solve(a):\n    m={}\n    for x in a:m[x]=m.get(x,0)+1\n    return min(m,key=lambda x:(m[x],x))','    static int solve(int[]a){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();for(int x:a)m.put(x,m.getOrDefault(x,0)+1);int z=a[0],bc=Integer.MAX_VALUE;for(int x:m.keySet()){int c=m.get(x);if(c<bc||(c==bc&&x<z)){z=x;bc=c;}}return z;}','#include <unordered_map>\nusing namespace std;int solve(vector<int>a){unordered_map<int,int>m;for(int x:a)m[x]++;int z=a[0],bc=2147483647;for(auto q:m)if(q.second<bc||(q.second==bc&&q.first<z)){z=q.first;bc=q.second;}return z;}','func solve(a []int) int {m:=map[int]int{};for _,x:=range a{m[x]++};z:=a[0];bc:=len(a)+1;for x,c:=range m{if c<bc||(c==bc&&x<z){z=x;bc=c}};return z}'),
    tests:[{stdin:'4 1 4 1 7',expectedStdout:'7',isSample:true},{stdin:'-2 -2 -1 -1',expectedStdout:'-2',isSample:true},{stdin:'9',expectedStdout:'9'},{stdin:'3 3 2 2 2',expectedStdout:'3'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'values-by-pivot-distance', title:'Group Around a Pivot', difficulty:'MEDIUM', patternTags:['hash-map','derived-key','ordering'], signatureId:'fn:ints,int->ints', avgSolveSeconds:720,
    promptMarkdown:['Return all input values ordered by increasing absolute distance from pivot. Equal distances are ordered by numeric value; duplicates stay.','', '**Example**','', '```','input: 7 3 5 9 1','5','output: 5 3 7 1 9','```','', 'The distances are 2, 2, 0, 4, 4. The exact tie order is part of the output rule.'].join('\n'), editorialMarkdown:['## Buckets keyed by distance','','Use a map from absolute distance to the values at that distance, then visit distance keys in ascending order and sort each bucket numerically. Grouping makes the derived key explicit while preserving duplicate values in the bucket.','','The quiet mistake is emitting equal-distance values in their arrival order, which is stable but violates the numeric tie rule. Time is O(n log n) in the worst case from ordering buckets, bounded by n values; space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){return a.slice().sort((x,y)=>Math.abs(x-b)-Math.abs(y-b)||x-y);}','function solve(a:number[],b:number):number[]{return a.slice().sort((x,y)=>Math.abs(x-b)-Math.abs(y-b)||x-y);}','def solve(a,b):\n    return sorted(a,key=lambda x:(abs(x-b),x))','    static int[] solve(int[]a,int b){Integer[]v=new Integer[a.length];for(int i=0;i<a.length;i++)v[i]=a[i];java.util.Arrays.sort(v,(x,y)->{int d=Math.abs(x-b)-Math.abs(y-b);return d!=0?d:x-y;});for(int i=0;i<a.length;i++)a[i]=v[i];return a;}','vector<int> solve(vector<int>a,int b){sort(a.begin(),a.end(),[&](int x,int y){int u=x-b;if(u<0)u=-u;int v=y-b;if(v<0)v=-v;return u!=v?u<v:x<y;});return a;}','func solve(a []int,b int) []int {for i:=0;i<len(a);i++{for j:=i+1;j<len(a);j++{x:=a[i]-b;if x<0{x=-x};y:=a[j]-b;if y<0{y=-y};if y<x||(y==x&&a[j]<a[i]){a[i],a[j]=a[j],a[i]}}};return a}'),
    tests:[{stdin:'7 3 5 9 1\n5',expectedStdout:'5 3 7 1 9',isSample:true},{stdin:'-1 1 -2 2\n0',expectedStdout:'-1 1 -2 2',isSample:true},{stdin:'4\n4',expectedStdout:'4'},{stdin:'3 3 1\n2',expectedStdout:'1 3 3'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'first-value-occurring-thrice', title:'First Value Occurring Exactly Three Times', difficulty:'MEDIUM', patternTags:['hash-map','frequency','order'], signatureId:'fn:ints->int', avgSolveSeconds:660,
    promptMarkdown:['Return the first value in input order whose total frequency is exactly three. Return -1 if there is none.','', '**Example**','', '```','input: 8 4 8 4 8 4 2','output: 8','```','', 'Both 8 and 4 occur three times, and 8 first appears earlier. A value occurring four times does not qualify.'].join('\n'), editorialMarkdown:['## Complete frequencies before choosing order','','Count first because a running third occurrence can later become a fourth. Once all totals are known, scan the original list from left to right and return the first key whose count is three. This separates the global frequency fact from the positional ordering rule.','','The quiet mistake is returning when a counter reaches three, which incorrectly accepts values with later copies. Time is O(n) expected, bounded by list length, and the frequency map uses O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){let m={};for(let x of a)m[x]=(m[x]||0)+1;for(let x of a)if(m[x]===3)return x;return -1;}','function solve(a:number[]):number{const m=new Map<number,number>();for(const x of a)m.set(x,(m.get(x)||0)+1);for(const x of a)if(m.get(x)===3)return x;return -1;}','def solve(a):\n m={}\n for x in a:m[x]=m.get(x,0)+1\n for x in a:\n  if m[x]==3:return x\n return -1','    static int solve(int[]a){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();for(int x:a)m.put(x,m.getOrDefault(x,0)+1);for(int x:a)if(m.get(x)==3)return x;return -1;}','int solve(vector<int>a){unordered_map<int,int>m;for(int x:a)m[x]++;for(int x:a)if(m[x]==3)return x;return -1;}','func solve(a []int) int {m:=map[int]int{};for _,x:=range a{m[x]++};for _,x:=range a{if m[x]==3{return x}};return -1}'),
    tests:[{stdin:'8 4 8 4 8 4 2',expectedStdout:'8',isSample:true},{stdin:'1 1 1 2 2 2',expectedStdout:'1',isSample:true},{stdin:'5 5 5 5',expectedStdout:'-1'},{stdin:'-2 -2 -2',expectedStdout:'-2'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'count-equal-frequency-shared-values', title:'Shared Values With Matching Counts', difficulty:'MEDIUM', patternTags:['hash-map','frequency','intersection'], signatureId:'fn:matrix->int', avgSolveSeconds:720,
    promptMarkdown:['The matrix has exactly two rows. Count distinct values that occur in both rows the same positive number of times.','', '**Example**','', '```','input: 1 1 2 3;1 2 2 4','output: 0','```','', 'No value has matching positive frequency. Either row may be empty and contributes zero matches.'].join('\n'), editorialMarkdown:['## Compare two frequency maps','','Build a frequency map for each matrix row, then inspect keys from the first map. A key contributes only if the other map has the same positive count. Counting keys rather than occurrences keeps the requested unit distinct.','','The quiet mistake is adding the common frequency, which answers a multiset-overlap question instead. A missing key must not match a zero count because matches must appear in both rows. Time is O(n+m), bounded by both row lengths, and space is O(n+m).'].join('\n'),
    referenceSolution:six('function solve(a){let x={},y={},c=0;for(let v of a[0])x[v]=(x[v]||0)+1;for(let v of a[1])y[v]=(y[v]||0)+1;for(let v in x)if(x[v]===y[v])c++;return c;}','function solve(a:number[][]):number{const x=new Map<number,number>(),y=new Map<number,number>();for(const v of a[0])x.set(v,(x.get(v)||0)+1);for(const v of a[1])y.set(v,(y.get(v)||0)+1);let c=0;for(const [v,n] of x)if(y.get(v)===n)c++;return c;}','def solve(a):\n x={};y={}\n for v in a[0]:x[v]=x.get(v,0)+1\n for v in a[1]:y[v]=y.get(v,0)+1\n return sum(x[v]==y.get(v,0) for v in x)','    static int solve(int[][]a){java.util.HashMap<Integer,Integer>x=new java.util.HashMap<>(),y=new java.util.HashMap<>();for(int v:a[0])x.put(v,x.getOrDefault(v,0)+1);for(int v:a[1])y.put(v,y.getOrDefault(v,0)+1);int c=0;for(int v:x.keySet())if(x.get(v).equals(y.get(v)))c++;return c;}','int solve(vector<vector<int>>a){unordered_map<int,int>x,y;for(int v:a[0])x[v]++;for(int v:a[1])y[v]++;int c=0;for(auto q:x)if(q.second==y[q.first])c++;return c;}','func solve(a [][]int) int {x:=map[int]int{};y:=map[int]int{};for _,v:=range a[0]{x[v]++};for _,v:=range a[1]{y[v]++};c:=0;for v,n:=range x{if n==y[v]{c++}};return c}'),
    tests:[{stdin:'1 1 2 3;1 2 2 4',expectedStdout:'0',isSample:true},{stdin:'5 5;5 5',expectedStdout:'1',isSample:true},{stdin:';1',expectedStdout:'0'},{stdin:'1 2 2;1 1 2 2',expectedStdout:'1'}] }),
  p({ ...base, patternFamily:'SLIDING_WINDOW', slug:'longest-window-with-k-values', title:'Longest Window With Limited Variety', difficulty:'HARD', patternTags:['hash-map','sliding-window','frequency'], signatureId:'fn:ints,int->int', avgSolveSeconds:900,
    promptMarkdown:['Return the greatest length of a contiguous block containing at most k distinct values.','','**Example**','','```','input: 1 2 1 3 2 2','2','output: 3','```','','The list is non-empty and k is positive. A window with fewer than k values is still valid.'].join('\n'), editorialMarkdown:['## Shrinking frequency window','','Expand the right edge while a map counts the current values. If more than k keys remain, advance the left edge and delete keys whose count becomes zero. Once repaired, the current window is the longest valid one ending at its right edge.','','The quiet mistake is retaining a zero-count key, making the distinct count permanently too high. Each pointer moves forward only, so time is O(n) expected, bounded by list length, and map space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){let m=new Map(),l=0,z=0;for(let r=0;r<a.length;r++){m.set(a[r],(m.get(a[r])||0)+1);while(m.size>b){let x=a[l++],v=m.get(x)-1;if(v)m.set(x,v);else m.delete(x);}z=Math.max(z,r-l+1)}return z}','function solve(a:number[],b:number):number{const m=new Map<number,number>();let l=0,z=0;for(let r=0;r<a.length;r++){m.set(a[r],(m.get(a[r])||0)+1);while(m.size>b){const x=a[l++],v=m.get(x)!-1;if(v)m.set(x,v);else m.delete(x)}z=Math.max(z,r-l+1)}return z}','def solve(a,b):\n m={};l=z=0\n for r,x in enumerate(a):\n  m[x]=m.get(x,0)+1\n  while len(m)>b:\n   y=a[l];m[y]-=1;l+=1\n   if m[y]==0:del m[y]\n  z=max(z,r-l+1)\n return z','    static int solve(int[]a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();int l=0,z=0;for(int r=0;r<a.length;r++){m.put(a[r],m.getOrDefault(a[r],0)+1);while(m.size()>b){int x=a[l++],v=m.get(x)-1;if(v==0)m.remove(x);else m.put(x,v);}z=Math.max(z,r-l+1);}return z;}','int solve(vector<int>a,int b){unordered_map<int,int>m;int l=0,z=0;for(int r=0;r<(int)a.size();r++){m[a[r]]++;while((int)m.size()>b){if(--m[a[l]]==0)m.erase(a[l]);l++;}z=max(z,r-l+1);}return z;}','func solve(a []int,b int) int {m:=map[int]int{};l,z:=0,0;for r,x:=range a{m[x]++;for len(m)>b{y:=a[l];m[y]--;l++;if m[y]==0{delete(m,y)}};if r-l+1>z{z=r-l+1}};return z}'),
    tests:[{stdin:'1 2 1 3 2 2\n2',expectedStdout:'3',isSample:true},{stdin:'4 4 4\n1',expectedStdout:'3',isSample:true},{stdin:'1 2 3\n2',expectedStdout:'2'},{stdin:'-1 0 -1 0 2\n2',expectedStdout:'4'}] }),
  p({ ...base, patternFamily:'SLIDING_WINDOW', slug:'shortest-window-covering-distinct-values', title:'Smallest Window Covering Every Value', difficulty:'HARD', patternTags:['hash-set','hash-map','sliding-window'], signatureId:'fn:ints->int', avgSolveSeconds:960,
    promptMarkdown:['Return the shortest contiguous block containing every distinct value found in the entire list.','','**Example**','','```','input: 1 2 2 3 1 2 3','output: 3','```','','The list is non-empty. If it has only one distinct value, the answer is 1.'].join('\n'), editorialMarkdown:['## Cover all global keys, then tighten','','Collect the required distinct values, then expand a counted window until it contains all of them. At that moment contract its left edge as far as coverage permits, recording each valid width. Every optimum appears during one such contraction.','','The quiet mistake is counting occurrences rather than map keys: duplicates never replace a missing type. Zero-count keys must be removed. Time is O(n) expected, bounded by list length, with O(n) map and set space.'].join('\n'),
    referenceSolution:six('function solve(a){let n=new Set(a).size,m=new Map(),l=0,z=a.length;for(let r=0;r<a.length;r++){m.set(a[r],(m.get(a[r])||0)+1);while(m.size===n){z=Math.min(z,r-l+1);let x=a[l++],v=m.get(x)-1;if(v)m.set(x,v);else m.delete(x)}}return z}','function solve(a:number[]):number{const n=new Set(a).size,m=new Map<number,number>();let l=0,z=a.length;for(let r=0;r<a.length;r++){m.set(a[r],(m.get(a[r])||0)+1);while(m.size===n){z=Math.min(z,r-l+1);const x=a[l++],v=m.get(x)!-1;if(v)m.set(x,v);else m.delete(x)}}return z}','def solve(a):\n n=len(set(a));m={};l=0;z=len(a)\n for r,x in enumerate(a):\n  m[x]=m.get(x,0)+1\n  while len(m)==n:\n   z=min(z,r-l+1);y=a[l];m[y]-=1;l+=1\n   if m[y]==0:del m[y]\n return z','    static int solve(int[]a){java.util.HashSet<Integer>s=new java.util.HashSet<>();for(int x:a)s.add(x);java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();int l=0,z=a.length;for(int r=0;r<a.length;r++){m.put(a[r],m.getOrDefault(a[r],0)+1);while(m.size()==s.size()){z=Math.min(z,r-l+1);int x=a[l++],v=m.get(x)-1;if(v==0)m.remove(x);else m.put(x,v);}}return z;}','int solve(vector<int>a){unordered_set<int>s(a.begin(),a.end());unordered_map<int,int>m;int l=0,z=a.size();for(int r=0;r<(int)a.size();r++){m[a[r]]++;while(m.size()==s.size()){z=min(z,r-l+1);if(--m[a[l]]==0)m.erase(a[l]);l++;}}return z;}','func solve(a []int) int {s:=map[int]bool{};for _,x:=range a{s[x]=true};m:=map[int]int{};l,z:=0,len(a);for r,x:=range a{m[x]++;for len(m)==len(s){if r-l+1<z{z=r-l+1};y:=a[l];m[y]--;l++;if m[y]==0{delete(m,y)}}};return z}'),
    tests:[{stdin:'1 2 2 3 1 2 3',expectedStdout:'3',isSample:true},{stdin:'5 5 5',expectedStdout:'1',isSample:true},{stdin:'1 2 3',expectedStdout:'3'},{stdin:'1 2 1 3 2',expectedStdout:'3'}] }),
];
