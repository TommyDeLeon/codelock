import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-010` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_010_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "maximum-spice-profit",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Maximum Spice Profit",
    patternTags: ["array","running-minimum","maximum","one-pass"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 400,
    promptMarkdown: "You are an interstellar merchant trading rare Spices. You are given an array of integers `prices` representing the value of one unit of Spice on successive days at the local space station. \n\nYou want to maximize your profit by choosing exactly one day to buy one unit of Spice, and a different, strictly later day to sell it. If no profit is possible, you should choose not to trade, resulting in a profit of 0.\n\nReturn the maximum profit you can achieve from this transaction.\n\n**Constraints**\n- `0 <= prices.length <= 40`\n- `0 <= prices[i] <= 10000`\n\n**Example 1**\n```\ninput:\n7 1 5 3 6 4\noutput: 5\n```\nExplanation: Buy on day 2 (price 1) and sell on day 5 (price 6). Profit = 6 - 1 = 5.\n\n**Example 2**\n```\ninput:\n7 6 4 3 1\noutput: 0\n```\nExplanation: Prices are strictly decreasing, so no profitable trade can be made. Output is 0.\n\n**Example 3**\n```\ninput:\n3 3\noutput: 0\n```\nExplanation: You cannot make a profit.\n\n**Follow-up:** Can you solve this in a single pass with O(1) space complexity?",
    editorialMarkdown: "## Running Minimum Tracking\n\nThe naive approach is to check every possible pair of buy and sell days, which takes O(N^2) time. We can optimize this to O(N) by keeping track of the minimum price seen so far as we iterate through the array. At each step, we calculate the potential profit if we sold at the current price and bought at the running minimum. We update the maximum profit accordingly.\n\nTime complexity is O(N) where N is the number of prices, and space complexity is O(1).\n\nThe one trap most solvers hit is initializing the maximum profit to the first element or a negative number, forgetting that the profit can be 0 if we choose not to trade.",
    referenceSolution: {
      JAVASCRIPT: "function solve(prices) {\n    if (!prices || prices.length === 0) return 0;\n    let minPrice = prices[0];\n    let maxProfit = 0;\n    for (let i = 0; i < prices.length; i++) {\n        if (prices[i] < minPrice) {\n            minPrice = prices[i];\n        } else if (prices[i] - minPrice > maxProfit) {\n            maxProfit = prices[i] - minPrice;\n        }\n    }\n    return maxProfit;\n}",
      TYPESCRIPT: "function solve(prices: number[]): number {\n    if (!prices || prices.length === 0) return 0;\n    let minPrice = prices[0];\n    let maxProfit = 0;\n    for (let i = 0; i < prices.length; i++) {\n        if (prices[i] < minPrice) {\n            minPrice = prices[i];\n        } else if (prices[i] - minPrice > maxProfit) {\n            maxProfit = prices[i] - minPrice;\n        }\n    }\n    return maxProfit;\n}",
      PYTHON: "def solve(prices):\n    if not prices: return 0\n    min_price = prices[0]\n    max_profit = 0\n    for p in prices:\n        if p < min_price:\n            min_price = p\n        elif p - min_price > max_profit:\n            max_profit = p - min_price\n    return max_profit",
      JAVA: "    static int solve(int[] prices) {\n        if (prices == null || prices.length == 0) return 0;\n        int minPrice = prices[0];\n        int maxProfit = 0;\n        for (int p : prices) {\n            if (p < minPrice) {\n                minPrice = p;\n            } else if (p - minPrice > maxProfit) {\n                maxProfit = p - minPrice;\n            }\n        }\n        return maxProfit;\n    }",
      CPP: "int solve(vector<int> prices) {\n    if (prices.empty()) return 0;\n    int min_price = prices[0];\n    int max_profit = 0;\n    for (int p : prices) {\n        if (p < min_price) {\n            min_price = p;\n        } else if (p - min_price > max_profit) {\n            max_profit = p - min_price;\n        }\n    }\n    return max_profit;\n}",
      GO: "func solve(prices []int) int {\n    if len(prices) == 0 {\n        return 0\n    }\n    minPrice := prices[0]\n    maxProfit := 0\n    for _, p := range prices {\n        if p < minPrice {\n            minPrice = p\n        } else if p - minPrice > maxProfit {\n            maxProfit = p - minPrice\n        }\n    }\n    return maxProfit\n}",
    },
    tests: [
      { stdin: "7 1 5 3 6 4", expectedStdout: "5", isSample: true },
      { stdin: "7 6 4 3 1", expectedStdout: "0", isSample: true },
      { stdin: "3 3", expectedStdout: "0" },
      { stdin: "", expectedStdout: "0" },
      { stdin: "10", expectedStdout: "0" },
      { stdin: "2 10", expectedStdout: "8" },
      { stdin: "2 4 1", expectedStdout: "2" },
      { stdin: "1 2 3 4 5", expectedStdout: "4" },
    ],
  }),

  p({
    ...base,
    slug: "heavy-duty-vehicles",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Heavy Duty Vehicles",
    patternTags: ["matrix","filtering","array","linear-scan"],
    signatureId: "fn:matrix->ints",
    avgSolveSeconds: 300,
    promptMarkdown: "A transport company tracks its fleet of cargo vehicles. You are given a 2D integer array `vehicles` where each element is `[id, weight, volume]`. \n\nA vehicle is considered \"Heavy Duty\" if its `weight` is at least `10000` OR its `volume` is at least `500`. \n\nReturn an array containing the `id` of all Heavy Duty vehicles, in the order they appear in the input matrix.\n\n**Constraints**\n- `0 <= vehicles.length <= 40`\n- `vehicles[i].length == 3`\n- `1 <= id <= 100`\n- `1000 <= weight <= 50000`\n- `100 <= volume <= 1000`\n\n**Example 1**\n```\ninput:\n1 9000 400;2 12000 300;3 8000 600\noutput: 2 3\n```\nExplanation: Vehicle 2 is Heavy Duty because weight 12000 >= 10000. Vehicle 3 is Heavy Duty because volume 600 >= 500.\n\n**Example 2**\n```\ninput:\n5 5000 200;7 15000 800\noutput: 7\n```\nExplanation: Vehicle 7 meets both conditions (only one is required). Vehicle 5 meets neither.\n\n**Example 3**\n```\ninput:\n8 9999 499\noutput: \n```\nExplanation: No vehicles are Heavy Duty.\n\n**Follow-up:** Does your approach preserve the original order of the vehicles?",
    editorialMarkdown: "## Matrix Filtering\n\nThe problem asks us to extract the IDs of entities that meet at least one of two conditions. We can iterate over each row in the 2D array, check if `weight >= 10000` or `volume >= 500`, and if so, append the `id` to our result list.\n\nTime complexity is O(N) where N is the number of vehicles, as we visit each vehicle once. Space complexity is O(N) to store the result array (or O(1) auxiliary space).\n\nThe one trap most solvers hit is using logical AND instead of logical OR, or mishandling rows that might not have exactly 3 columns (if the input format guarantees it, this is less of an issue).",
    referenceSolution: {
      JAVASCRIPT: "function solve(vehicles) {\n    let res = [];\n    for (let v of vehicles) {\n        if (v && v.length >= 3 && (v[1] >= 10000 || v[2] >= 500)) {\n            res.push(v[0]);\n        }\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(vehicles: number[][]): number[] {\n    let res: number[] = [];\n    for (let v of vehicles) {\n        if (v && v.length >= 3 && (v[1] >= 10000 || v[2] >= 500)) {\n            res.push(v[0]);\n        }\n    }\n    return res;\n}",
      PYTHON: "def solve(vehicles):\n    res = []\n    for v in vehicles:\n        if v and len(v) >= 3 and (v[1] >= 10000 or v[2] >= 500):\n            res.append(v[0])\n    return res",
      JAVA: "    static int[] solve(int[][] vehicles) {\n        if (vehicles == null) return new int[0];\n        int count = 0;\n        for (int[] v : vehicles) {\n            if (v.length >= 3 && (v[1] >= 10000 || v[2] >= 500)) count++;\n        }\n        int[] res = new int[count];\n        int idx = 0;\n        for (int[] v : vehicles) {\n            if (v.length >= 3 && (v[1] >= 10000 || v[2] >= 500)) {\n                res[idx++] = v[0];\n            }\n        }\n        return res;\n    }",
      CPP: "vector<int> solve(vector<vector<int>> vehicles) {\n    vector<int> res;\n    for (auto& v : vehicles) {\n        if (v.size() >= 3 && (v[1] >= 10000 || v[2] >= 500)) {\n            res.push_back(v[0]);\n        }\n    }\n    return res;\n}",
      GO: "func solve(vehicles [][]int) []int {\n    var res []int\n    for _, v := range vehicles {\n        if len(v) >= 3 && (v[1] >= 10000 || v[2] >= 500) {\n            res = append(res, v[0])\n        }\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "1 9000 400;2 12000 300;3 8000 600", expectedStdout: "2 3", isSample: true },
      { stdin: "5 5000 200;7 15000 800", expectedStdout: "7", isSample: true },
      { stdin: "8 9999 499", expectedStdout: "" },
      { stdin: "", expectedStdout: "" },
      { stdin: "1 10000 100", expectedStdout: "1" },
      { stdin: "2 1000 500", expectedStdout: "2" },
      { stdin: "1 10000 500;2 12000 100;3 1000 600", expectedStdout: "1 2 3" },
      { stdin: "1 9999 499;2 1000 499", expectedStdout: "" },
    ],
  }),

  p({
    ...base,
    slug: "longest-silent-period",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "BIT_MANIPULATION",
    title: "Longest Silent Period",
    patternTags: ["bit-manipulation","counting","maximum","binary"],
    signatureId: "fn:int->int",
    avgSolveSeconds: 400,
    promptMarkdown: "A communication system sends information via an integer `pulse`. In the binary representation of this integer, the bits `1` represent an active signal, while `0` represents silence. \n\nThe distance between two adjacent `1`s is the number of bit positions between them. For instance, in the binary representation `1001`, the distance between the two `1`s is `3`.\n\nReturn the maximum distance between any two adjacent `1`s in the binary representation of `pulse`. If there are fewer than two `1`s, return `0`.\n\n**Constraints**\n- `0 <= pulse <= 10^9`\n\n**Example 1**\n```\ninput:\n22\noutput: 2\n```\nExplanation: 22 in binary is 10110. The first adjacent pair of 1s is at positions 1 and 2 (distance 1). The second adjacent pair of 1s is at positions 2 and 4 (distance 2). The maximum is 2.\n\n**Example 2**\n```\ninput:\n8\noutput: 0\n```\nExplanation: 8 in binary is 1000. There is only one 1, so the distance is 0.\n\n**Example 3**\n```\ninput:\n5\noutput: 2\n```\nExplanation: 5 in binary is 101. The distance between the 1s is 2.\n\n**Follow-up:** Try to solve this without converting the integer into a string.",
    editorialMarkdown: "## Bitwise Right Shift\n\nWe can iterate through the bits of `pulse` by repeatedly checking the least significant bit (`pulse & 1`) and then shifting the number to the right (`pulse >>= 1`). When we encounter a `1`, if we have seen a previous `1`, we update our maximum gap with the difference in their positions.\n\nTime complexity is O(log N) where N is the value of `pulse`, as the number of loop iterations is bounded by the number of bits required to represent N. Space complexity is O(1).\n\nThe one trap most solvers hit is not handling the case where `pulse` contains fewer than two `1`s. We must track whether a previous `1` has been seen to avoid calculating a distance against an uninitialized position.",
    referenceSolution: {
      JAVASCRIPT: "function solve(pulse) {\n    let maxGap = 0;\n    let lastPos = -1;\n    let pos = 0;\n    while (pulse > 0) {\n        if ((pulse & 1) === 1) {\n            if (lastPos !== -1) {\n                if (pos - lastPos > maxGap) {\n                    maxGap = pos - lastPos;\n                }\n            }\n            lastPos = pos;\n        }\n        pulse >>= 1;\n        pos++;\n    }\n    return maxGap;\n}",
      TYPESCRIPT: "function solve(pulse: number): number {\n    let maxGap = 0;\n    let lastPos = -1;\n    let pos = 0;\n    while (pulse > 0) {\n        if ((pulse & 1) === 1) {\n            if (lastPos !== -1) {\n                if (pos - lastPos > maxGap) {\n                    maxGap = pos - lastPos;\n                }\n            }\n            lastPos = pos;\n        }\n        pulse >>= 1;\n        pos++;\n    }\n    return maxGap;\n}",
      PYTHON: "def solve(pulse):\n    max_gap = 0\n    last_pos = -1\n    pos = 0\n    while pulse > 0:\n        if (pulse & 1) == 1:\n            if last_pos != -1:\n                if pos - last_pos > max_gap:\n                    max_gap = pos - last_pos\n            last_pos = pos\n        pulse >>= 1\n        pos += 1\n    return max_gap",
      JAVA: "    static int solve(int pulse) {\n        int maxGap = 0;\n        int lastPos = -1;\n        int pos = 0;\n        while (pulse > 0) {\n            if ((pulse & 1) == 1) {\n                if (lastPos != -1) {\n                    if (pos - lastPos > maxGap) {\n                        maxGap = pos - lastPos;\n                    }\n                }\n                lastPos = pos;\n            }\n            pulse >>= 1;\n            pos++;\n        }\n        return maxGap;\n    }",
      CPP: "int solve(int pulse) {\n    int max_gap = 0;\n    int last_pos = -1;\n    int pos = 0;\n    while (pulse > 0) {\n        if ((pulse & 1) == 1) {\n            if (last_pos != -1) {\n                if (pos - last_pos > max_gap) {\n                    max_gap = pos - last_pos;\n                }\n            }\n            last_pos = pos;\n        }\n        pulse >>= 1;\n        pos++;\n    }\n    return max_gap;\n}",
      GO: "func solve(pulse int) int {\n    maxGap := 0\n    lastPos := -1\n    pos := 0\n    for pulse > 0 {\n        if pulse&1 == 1 {\n            if lastPos != -1 {\n                if pos-lastPos > maxGap {\n                    maxGap = pos - lastPos\n                }\n            }\n            lastPos = pos\n        }\n        pulse >>= 1\n        pos++\n    }\n    return maxGap\n}",
    },
    tests: [
      { stdin: "22", expectedStdout: "2", isSample: true },
      { stdin: "8", expectedStdout: "0", isSample: true },
      { stdin: "5", expectedStdout: "2" },
      { stdin: "0", expectedStdout: "0" },
      { stdin: "1", expectedStdout: "0" },
      { stdin: "3", expectedStdout: "1" },
      { stdin: "1073741825", expectedStdout: "30" },
      { stdin: "257", expectedStdout: "8" },
    ],
  }),

  p({
    ...base,
    slug: "zebra-barcode-check",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "BIT_MANIPULATION",
    title: "Zebra Barcode Check",
    patternTags: ["bit-manipulation","parity","validation","binary"],
    signatureId: "fn:int->bool",
    avgSolveSeconds: 300,
    promptMarkdown: "A barcode scanner uses integers to represent black and white stripes. An integer is considered a valid \"Zebra Stripe\" pattern if its binary representation has alternating `0`s and `1`s (meaning no two adjacent bits have the same value).\n\nGiven a non-negative integer `pattern`, return `true` if it represents a valid Zebra Stripe, and `false` otherwise.\n\n**Constraints**\n- `0 <= pattern <= 10^9`\n\n**Example 1**\n```\ninput:\n5\noutput: true\n```\nExplanation: 5 in binary is 101, which has alternating bits.\n\n**Example 2**\n```\ninput:\n7\noutput: false\n```\nExplanation: 7 in binary is 111, which has adjacent 1s.\n\n**Example 3**\n```\ninput:\n10\noutput: true\n```\nExplanation: 10 in binary is 1010, which alternates.\n\n**Follow-up:** Can you determine this without a loop using only bitwise operations?",
    editorialMarkdown: "## Fast Bit Verification\n\nWe can verify the alternating property by maintaining the last seen bit and iterating through the integer using bitwise operations (`n & 1` and `n >>= 1`). If we ever encounter two adjacent bits that are identical, we immediately return `false`.\n\nAlternatively, a trickier O(1) bitwise solution exists: Let `x = n ^ (n >> 1)`. If `n` has alternating bits, `x` will be a number with all 1s in its binary representation. We can check this property using `(x & (x + 1)) == 0`.\n\nTime complexity is O(log N) for the loop approach, or O(1) for the clever bit trick. Space complexity is O(1).\n\nThe one trap most solvers hit is accidentally shifting a negative number, but since the input is non-negative, this is a very straightforward application of bit shifting.",
    referenceSolution: {
      JAVASCRIPT: "function solve(pattern) {\n    if (pattern === 0) return true;\n    let prev = pattern & 1;\n    pattern >>= 1;\n    while (pattern > 0) {\n        let curr = pattern & 1;\n        if (curr === prev) return false;\n        prev = curr;\n        pattern >>= 1;\n    }\n    return true;\n}",
      TYPESCRIPT: "function solve(pattern: number): boolean {\n    if (pattern === 0) return true;\n    let prev = pattern & 1;\n    pattern >>= 1;\n    while (pattern > 0) {\n        let curr = pattern & 1;\n        if (curr === prev) return false;\n        prev = curr;\n        pattern >>= 1;\n    }\n    return true;\n}",
      PYTHON: "def solve(pattern):\n    if pattern == 0: return True\n    x = pattern ^ (pattern >> 1)\n    return (x & (x + 1)) == 0",
      JAVA: "    static boolean solve(int pattern) {\n        if (pattern == 0) return true;\n        long x = (long)pattern ^ (pattern >> 1);\n        return (x & (x + 1)) == 0;\n    }",
      CPP: "bool solve(int pattern) {\n    if (pattern == 0) return true;\n    long long x = pattern ^ (pattern >> 1);\n    return (x & (x + 1)) == 0;\n}",
      GO: "func solve(pattern int) bool {\n    if pattern == 0 {\n        return true\n    }\n    x := int64(pattern) ^ (int64(pattern) >> 1)\n    return (x & (x + 1)) == 0\n}",
    },
    tests: [
      { stdin: "5", expectedStdout: "true", isSample: true },
      { stdin: "7", expectedStdout: "false", isSample: true },
      { stdin: "10", expectedStdout: "true" },
      { stdin: "0", expectedStdout: "true" },
      { stdin: "1", expectedStdout: "true" },
      { stdin: "3", expectedStdout: "false" },
      { stdin: "682", expectedStdout: "true" },
      { stdin: "11", expectedStdout: "false" },
    ],
  }),

  p({
    ...base,
    slug: "highest-unique-bid",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Highest Unique Bid",
    patternTags: ["hash-map","counting","maximum","frequency-count"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 400,
    promptMarkdown: "In a silent auction, participants write their bids on slips of paper. A special prize is given to the person who submitted the highest unique bid — that is, a bid amount that no one else submitted.\n\nYou are given an array of integers `bids`. Return the value of the highest unique bid. If there is no unique bid, return `-1`.\n\n**Constraints**\n- `0 <= bids.length <= 40`\n- `0 <= bids[i] <= 1000`\n\n**Example 1**\n```\ninput:\n8 3 8 9 4 4 7\noutput: 9\n```\nExplanation: The unique bids are 3, 9, and 7. The highest of these is 9.\n\n**Example 2**\n```\ninput:\n2 2 5 5 8 8\noutput: -1\n```\nExplanation: There are no unique bids, so we return -1.\n\n**Example 3**\n```\ninput:\n15\noutput: 15\n```\nExplanation: There is only one bid, and it is unique.\n\n**Follow-up:** Can you do this in one pass if the data is already sorted?",
    editorialMarkdown: "## Frequency Map and Maximum Search\n\nWe can count the occurrences of each bid using a hash map or an array if the bounds are small. Since bids can be up to 1000, an array or map works well. After populating the frequency map, we iterate through its keys. For every key that has a count of exactly 1, we update our maximum unique bid. If no such bid is found, we return `-1`.\n\nTime complexity is O(N) where N is the length of `bids`, as we iterate through it once to populate the map, and once through the unique keys (which is at most N) to find the maximum. Space complexity is O(N) for storing the frequencies.\n\nThe one trap most solvers hit is sorting the array and checking adjacent elements, which works but takes O(N log N) time, or they forget to return `-1` when the input is empty or all elements are duplicated.",
    referenceSolution: {
      JAVASCRIPT: "function solve(bids) {\n    let counts = new Map();\n    for (let b of bids) counts.set(b, (counts.get(b) || 0) + 1);\n    let maxUnique = -1;\n    for (let [k, v] of counts.entries()) {\n        if (v === 1 && k > maxUnique) maxUnique = k;\n    }\n    return maxUnique;\n}",
      TYPESCRIPT: "function solve(bids: number[]): number {\n    let counts = new Map<number, number>();\n    for (let b of bids) counts.set(b, (counts.get(b) || 0) + 1);\n    let maxUnique = -1;\n    for (let [k, v] of counts.entries()) {\n        if (v === 1 && k > maxUnique) maxUnique = k;\n    }\n    return maxUnique;\n}",
      PYTHON: "def solve(bids):\n    counts = {}\n    for b in bids:\n        counts[b] = counts.get(b, 0) + 1\n    max_unique = -1\n    for k, v in counts.items():\n        if v == 1 and k > max_unique:\n            max_unique = k\n    return max_unique",
      JAVA: "    static int solve(int[] bids) {\n        java.util.Map<Integer, Integer> counts = new java.util.HashMap<>();\n        for (int b : bids) counts.put(b, counts.getOrDefault(b, 0) + 1);\n        int maxUnique = -1;\n        for (java.util.Map.Entry<Integer, Integer> entry : counts.entrySet()) {\n            if (entry.getValue() == 1 && entry.getKey() > maxUnique) {\n                maxUnique = entry.getKey();\n            }\n        }\n        return maxUnique;\n    }",
      CPP: "int solve(vector<int> bids) {\n    unordered_map<int, int> counts;\n    for (int b : bids) counts[b]++;\n    int max_unique = -1;\n    for (auto& p : counts) {\n        if (p.second == 1 && p.first > max_unique) {\n            max_unique = p.first;\n        }\n    }\n    return max_unique;\n}",
      GO: "func solve(bids []int) int {\n    counts := make(map[int]int)\n    for _, b := range bids {\n        counts[b]++\n    }\n    maxUnique := -1\n    for k, v := range counts {\n        if v == 1 && k > maxUnique {\n            maxUnique = k\n        }\n    }\n    return maxUnique\n}",
    },
    tests: [
      { stdin: "8 3 8 9 4 4 7", expectedStdout: "9", isSample: true },
      { stdin: "2 2 5 5 8 8", expectedStdout: "-1", isSample: true },
      { stdin: "15", expectedStdout: "15" },
      { stdin: "", expectedStdout: "-1" },
      { stdin: "0", expectedStdout: "0" },
      { stdin: "10 9 10 9 10 9 10 10", expectedStdout: "-1" },
      { stdin: "10 20 999 10 20", expectedStdout: "999" },
      { stdin: "1000 0 1000 0 1000 1000 1000 1000 0 5 1000 6 5 6 1000 1000 999 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000", expectedStdout: "999" },
    ],
  }),

  p({
    ...base,
    slug: "latest-sensor-readings",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Latest Sensor Readings",
    patternTags: ["matrix","aggregation","array","buckets"],
    signatureId: "fn:matrix->matrix",
    avgSolveSeconds: 400,
    promptMarkdown: "A weather station receives continuous updates from its distributed sensors. You are given a 2D integer array `readings` where each element is `[sensorId, timestamp]`. \n\nYour task is to find the latest (maximum) `timestamp` for each unique `sensorId` that sent a reading.\n\nReturn a 2D integer array where each element is `[sensorId, maxTimestamp]`. The result must be sorted in strictly increasing order of `sensorId`.\n\n**Constraints**\n- `0 <= readings.length <= 40`\n- `readings[i].length == 2`\n- `0 <= sensorId <= 100`\n- `0 <= timestamp <= 100000`\n\n**Example 1**\n```\ninput:\n5 100;2 300;5 250\noutput: 2 300;5 250\n```\nExplanation: Sensor 2 has only one reading at 300. Sensor 5 has two readings, 100 and 250, so its max timestamp is 250.\n\n**Example 2**\n```\ninput:\n1 50;1 40;1 60\noutput: 1 60\n```\nExplanation: Sensor 1's latest reading is at 60.\n\n**Example 3**\n```\ninput:\n10 500\noutput: 10 500\n```\nExplanation: Only one reading exists.\n\n**Follow-up:** Can you solve this efficiently without using a built-in sorting function?",
    editorialMarkdown: "## Direct Array Indexing\n\nSince the `sensorId` values are small (between 0 and 100), we can avoid hash maps and simply use an integer array of size 101 to keep track of the maximum timestamp seen for each ID. We initialize the array with a special value like `-1`. As we iterate through the `readings` array, we update the entry for each `sensorId` to be the maximum of its current value and the new timestamp. \n\nAfter completing the pass, we do a second pass over our size 101 array in index order (which naturally sorts the output by `sensorId`) and collect any non-negative values into our 2D result array.\n\nTime complexity is O(N) where N is the number of readings, and space complexity is O(M) where M is the maximum `sensorId` (in this case, bounded by 101).\n\nThe one trap most solvers hit is writing their own sorting algorithm for the 2D array, which is error-prone and unnecessary when the IDs are bounded and can be bucket-sorted.",
    referenceSolution: {
      JAVASCRIPT: "function solve(readings) {\n    let maxTime = new Array(101).fill(-1);\n    for (let r of readings) {\n        if (r && r.length >= 2) {\n            let id = r[0];\n            let ts = r[1];\n            if (ts > maxTime[id]) maxTime[id] = ts;\n        }\n    }\n    let res = [];\n    for (let i = 0; i <= 100; i++) {\n        if (maxTime[i] !== -1) {\n            res.push([i, maxTime[i]]);\n        }\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(readings: number[][]): number[][] {\n    let maxTime = new Array(101).fill(-1);\n    for (let r of readings) {\n        if (r && r.length >= 2) {\n            let id = r[0];\n            let ts = r[1];\n            if (ts > maxTime[id]) maxTime[id] = ts;\n        }\n    }\n    let res: number[][] = [];\n    for (let i = 0; i <= 100; i++) {\n        if (maxTime[i] !== -1) {\n            res.push([i, maxTime[i]]);\n        }\n    }\n    return res;\n}",
      PYTHON: "def solve(readings):\n    max_time = [-1] * 101\n    for r in readings:\n        if r and len(r) >= 2:\n            sid, ts = r[0], r[1]\n            if ts > max_time[sid]:\n                max_time[sid] = ts\n    return [[i, max_time[i]] for i in range(101) if max_time[i] != -1]",
      JAVA: "    static int[][] solve(int[][] readings) {\n        if (readings == null) return new int[0][0];\n        int[] maxTime = new int[101];\n        for (int i = 0; i <= 100; i++) maxTime[i] = -1;\n        for (int[] r : readings) {\n            if (r.length >= 2) {\n                int id = r[0];\n                int ts = r[1];\n                if (ts > maxTime[id]) maxTime[id] = ts;\n            }\n        }\n        int count = 0;\n        for (int i = 0; i <= 100; i++) {\n            if (maxTime[i] != -1) count++;\n        }\n        int[][] res = new int[count][2];\n        int idx = 0;\n        for (int i = 0; i <= 100; i++) {\n            if (maxTime[i] != -1) {\n                res[idx][0] = i;\n                res[idx][1] = maxTime[i];\n                idx++;\n            }\n        }\n        return res;\n    }",
      CPP: "vector<vector<int>> solve(vector<vector<int>> readings) {\n    vector<int> max_time(101, -1);\n    for (auto& r : readings) {\n        if (r.size() < 2) continue;\n        int id = r[0];\n        int ts = r[1];\n        if (ts > max_time[id]) {\n            max_time[id] = ts;\n        }\n    }\n    vector<vector<int>> res;\n    for (int i = 0; i <= 100; i++) {\n        if (max_time[i] != -1) {\n            res.push_back({i, max_time[i]});\n        }\n    }\n    return res;\n}",
      GO: "func solve(readings [][]int) [][]int {\n    maxTime := make([]int, 101)\n    for i := range maxTime {\n        maxTime[i] = -1\n    }\n    for _, r := range readings {\n        if len(r) >= 2 {\n            id := r[0]\n            ts := r[1]\n            if ts > maxTime[id] {\n                maxTime[id] = ts\n            }\n        }\n    }\n    var res [][]int\n    for i := 0; i <= 100; i++ {\n        if maxTime[i] != -1 {\n            res = append(res, []int{i, maxTime[i]})\n        }\n    }\n    if res == nil {\n        return [][]int{}\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "5 100;2 300;5 250", expectedStdout: "2 300;5 250", isSample: true },
      { stdin: "1 50;1 40;1 60", expectedStdout: "1 60", isSample: true },
      { stdin: "10 500", expectedStdout: "10 500" },
      { stdin: "", expectedStdout: "" },
      { stdin: "0 0", expectedStdout: "0 0" },
      { stdin: "2 20;0 10;1 15", expectedStdout: "0 10;1 15;2 20" },
      { stdin: "100 0;100 100000;100 99999", expectedStdout: "100 100000" },
      { stdin: "100 5;30 6;20 7;10 8;0 9", expectedStdout: "0 9;10 8;20 7;30 6;100 5" },
    ],
  }),
];
