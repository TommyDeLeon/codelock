import { AUTHORED, type ProblemDefinition } from '../problem.js';

/**
 * Tier 1 — Arrays & Hashing.
 *
 * The first pattern family, and the one the canonical interview lists agree on
 * most: nine problems, each the canonical representative of one idea. Every
 * statement is written from the task, not from anyone's prose — see the
 * non-negotiable at the top of docs/AUTHORING.md.
 *
 * Tier 1 assumes Tier 0.5: the progression gate will not serve these until the
 * user has built a dynamic array, a hash map and a hash set. That is the point
 * of the ordering — "use a hash set for O(1) membership" means something
 * different to someone who has written one.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = {
  tier: 'TIER_1',
  patternFamily: 'ARRAYS_HASHING',
  provenance: AUTHORED,
} as const;

export const TIER_1_ARRAYS_HASHING_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: 'pair-with-target-sum',
    title: 'Pair With a Target Sum',
    difficulty: 'EASY',
    patternTags: ['hash-map', 'complement', 'one-pass'],
    signatureId: 'fn:ints,int->ints',
    avgSolveSeconds: 480,
    promptMarkdown: [
      'Find two numbers that add up to a target.',
      '',
      'The first line is the list, the second the target. Return the **positions**',
      'of the two numbers, smaller position first.',
      '',
      '**Example**',
      '',
      '```',
      'input:  2 7 11 15',
      '        9',
      'output: 0 1',
      '```',
      '',
      '`2 + 7 = 9`, and they sit at positions 0 and 1.',
      '',
      'Exactly one pair works. You may not use the same position twice.',
    ].join('\n'),
    editorialMarkdown: [
      '## Store what you have seen, look for what you need',
      '',
      'The obvious solution checks every pair: two nested loops, O(n^2). It is',
      'correct, and it is the version to beat.',
      '',
      'The insight is to stop thinking about *pairs* and start thinking about',
      '**complements**. Standing on a number `n`, you do not need to search for a',
      'partner — you already know exactly what the partner must be: `target - n`.',
      'The only question is whether you have seen it.',
      '',
      '"Have I seen this value, and where?" is a hash map from value to position:',
      '',
      '```',
      'for i, n in list:',
      '    need = target - n',
      '    if need in seen: return [seen[need], i]',
      '    seen[n] = i',
      '```',
      '',
      'One pass, O(n) time, O(n) space. You traded memory for a loop, which is the',
      'trade behind most of this family.',
      '',
      'The ordering detail that catches people: check for the complement **before**',
      'inserting the current number. Insert first and a target of `8` with a `4` in',
      'the list finds the `4` as its own partner — the same position used twice,',
      'which the statement forbids.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  const seen = new Map();\n  for (let i = 0; i < a.length; i++) {\n    const need = b - a[i];\n    if (seen.has(need)) return [seen.get(need), i];\n    seen.set(a[i], i);\n  }\n  return [];\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number): number[] {\n  const seen = new Map<number, number>();\n  for (let i = 0; i < a.length; i++) {\n    const need = b - a[i];\n    if (seen.has(need)) return [seen.get(need) as number, i];\n    seen.set(a[i], i);\n  }\n  return [];\n}',
      PYTHON:
        'def solve(a, b):\n    seen = {}\n    for i, n in enumerate(a):\n        need = b - n\n        if need in seen:\n            return [seen[need], i]\n        seen[n] = i\n    return []',
      JAVA: '    static int[] solve(int[] a, int b) {\n        Map<Integer, Integer> seen = new HashMap<>();\n        for (int i = 0; i < a.length; i++) {\n            int need = b - a[i];\n            if (seen.containsKey(need)) return new int[] { seen.get(need), i };\n            seen.put(a[i], i);\n        }\n        return new int[0];\n    }',
      CPP: 'vector<int> solve(vector<int> a, int b) {\n    unordered_map<int, int> seen;\n    for (int i = 0; i < (int) a.size(); i++) {\n        int need = b - a[i];\n        auto it = seen.find(need);\n        if (it != seen.end()) return { it->second, i };\n        seen[a[i]] = i;\n    }\n    return {};\n}',
      GO: 'func solve(a []int, b int) []int {\n\tseen := map[int]int{}\n\tfor i, n := range a {\n\t\tif j, ok := seen[b-n]; ok {\n\t\t\treturn []int{j, i}\n\t\t}\n\t\tseen[n] = i\n\t}\n\treturn []int{}\n}',
    },
    tests: [
      { stdin: '2 7 11 15\n9', expectedStdout: '0 1', isSample: true },
      { stdin: '3 2 4\n6', expectedStdout: '1 2', isSample: true },
      { stdin: '3 3\n6', expectedStdout: '0 1' },
      { stdin: '-1 -2 -3\n-5', expectedStdout: '1 2' },
      { stdin: '0 4 3 0\n0', expectedStdout: '0 3' },
      { stdin: '1 5 2 8\n10', expectedStdout: '2 3' },
    ],
  }),

  p({
    ...base,
    slug: 'has-duplicate',
    title: 'Does It Repeat?',
    difficulty: 'EASY',
    patternTags: ['hash-set', 'membership', 'early-exit'],
    signatureId: 'fn:ints->bool',
    avgSolveSeconds: 300,
    promptMarkdown: [
      'Decide whether any number appears more than once.',
      '',
      'Return `true` if some number repeats, `false` if every number is different.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 3 1',
      'output: true',
      '```',
      '',
      'An empty list and a one-number list both have no repeats.',
    ].join('\n'),
    editorialMarkdown: [
      '## The cheapest use of a hash set there is',
      '',
      'Add each number to a set; if it was already there, you have your answer.',
      'Return immediately — no reason to look at the rest.',
      '',
      'There is an even shorter version: build a set from the whole list and compare',
      'sizes. If the set is smaller, something collapsed, so something repeated. It',
      'is elegant, and it is slightly worse — it always reads the entire list, where',
      'the loop stops at the first repeat. On a list whose first two entries are',
      'equal, that is the difference between two steps and a million.',
      '',
      'Both are O(n) time and O(n) space. Sorting first and checking neighbours is',
      'O(n log n) time but O(1) extra space, which is the better answer when memory',
      'is the scarce resource — worth saying out loud in an interview, because the',
      'follow-up is usually "what if you could not allocate?".',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const seen = new Set();\n  for (const n of a) {\n    if (seen.has(n)) return true;\n    seen.add(n);\n  }\n  return false;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): boolean {\n  const seen = new Set<number>();\n  for (const n of a) {\n    if (seen.has(n)) return true;\n    seen.add(n);\n  }\n  return false;\n}',
      PYTHON:
        'def solve(a):\n    seen = set()\n    for n in a:\n        if n in seen:\n            return True\n        seen.add(n)\n    return False',
      JAVA: '    static boolean solve(int[] a) {\n        Set<Integer> seen = new HashSet<>();\n        for (int n : a) {\n            if (!seen.add(n)) return true;\n        }\n        return false;\n    }',
      CPP: 'bool solve(vector<int> a) {\n    unordered_set<int> seen;\n    for (int n : a) {\n        if (!seen.insert(n).second) return true;\n    }\n    return false;\n}',
      GO: 'func solve(a []int) bool {\n\tseen := map[int]bool{}\n\tfor _, n := range a {\n\t\tif seen[n] {\n\t\t\treturn true\n\t\t}\n\t\tseen[n] = true\n\t}\n\treturn false\n}',
    },
    tests: [
      { stdin: '1 2 3 1', expectedStdout: 'true', isSample: true },
      { stdin: '1 2 3', expectedStdout: 'false', isSample: true },
      { stdin: '', expectedStdout: 'false' },
      { stdin: '7', expectedStdout: 'false' },
      { stdin: '-1 -1', expectedStdout: 'true' },
      { stdin: '0 1 2 3 4 0', expectedStdout: 'true' },
    ],
  }),

  p({
    ...base,
    slug: 'majority-element',
    title: 'The Majority Element',
    difficulty: 'MEDIUM',
    patternTags: ['hash-map', 'boyer-moore', 'counting'],
    signatureId: 'fn:ints->int',
    avgSolveSeconds: 600,
    promptMarkdown: [
      'Find the number that appears more than half the time.',
      '',
      'The list always has at least one number, and one value is guaranteed to',
      'appear in **more than half** the positions.',
      '',
      '**Example**',
      '',
      '```',
      'input:  3 2 3',
      'output: 3',
      '```',
      '',
      '`3` appears twice out of three positions, which is more than half.',
    ].join('\n'),
    editorialMarkdown: [
      '## Counting works; cancelling is better',
      '',
      'Count every value in a hash map and return the one whose count exceeds half',
      'the length. O(n) time, O(n) space, and there is nothing wrong with it.',
      '',
      'The reason this problem is famous is that it can be done in **O(1) space**.',
      'The Boyer-Moore vote works like this: keep a candidate and a counter. When',
      'the counter hits zero, adopt the current number as the new candidate. Then',
      'increment if the current number matches the candidate, decrement if not.',
      '',
      '```',
      'count = 0',
      'for n in list:',
      '    if count == 0: candidate = n',
      '    count += 1 if n == candidate else -1',
      'return candidate',
      '```',
      '',
      'Why it works: think of each non-matching element as cancelling one matching',
      'element. A value occupying more than half the positions has more copies than',
      'everything else combined, so it cannot be fully cancelled — whatever else',
      'happens, it is what survives.',
      '',
      'The guarantee in the statement is doing real work. Without a true majority',
      'the algorithm still returns *something*, and that something is meaningless. A',
      'correct implementation for unguaranteed input needs a second pass to verify',
      'the candidate — which is exactly the follow-up question.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  let candidate = a[0];\n  let count = 0;\n  for (const n of a) {\n    if (count === 0) candidate = n;\n    count += n === candidate ? 1 : -1;\n  }\n  return candidate;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): number {\n  let candidate = a[0];\n  let count = 0;\n  for (const n of a) {\n    if (count === 0) candidate = n;\n    count += n === candidate ? 1 : -1;\n  }\n  return candidate;\n}',
      PYTHON:
        'def solve(a):\n    candidate = a[0]\n    count = 0\n    for n in a:\n        if count == 0:\n            candidate = n\n        count += 1 if n == candidate else -1\n    return candidate',
      JAVA: '    static int solve(int[] a) {\n        int candidate = a[0], count = 0;\n        for (int n : a) {\n            if (count == 0) candidate = n;\n            count += (n == candidate) ? 1 : -1;\n        }\n        return candidate;\n    }',
      CPP: 'int solve(vector<int> a) {\n    int candidate = a[0], count = 0;\n    for (int n : a) {\n        if (count == 0) candidate = n;\n        count += (n == candidate) ? 1 : -1;\n    }\n    return candidate;\n}',
      GO: 'func solve(a []int) int {\n\tcandidate := a[0]\n\tcount := 0\n\tfor _, n := range a {\n\t\tif count == 0 {\n\t\t\tcandidate = n\n\t\t}\n\t\tif n == candidate {\n\t\t\tcount++\n\t\t} else {\n\t\t\tcount--\n\t\t}\n\t}\n\treturn candidate\n}',
    },
    tests: [
      { stdin: '3 2 3', expectedStdout: '3', isSample: true },
      { stdin: '2 2 1 1 1 2 2', expectedStdout: '2', isSample: true },
      { stdin: '5', expectedStdout: '5' },
      { stdin: '1 1', expectedStdout: '1' },
      { stdin: '-3 -3 -3 1 2', expectedStdout: '-3' },
      { stdin: '4 4 4 4 1 2 3', expectedStdout: '4' },
    ],
  }),

  p({
    ...base,
    slug: 'product-except-self',
    title: 'Product of Everything Else',
    difficulty: 'MEDIUM',
    patternTags: ['prefix-product', 'arrays', 'two-pass'],
    signatureId: 'fn:ints->ints',
    avgSolveSeconds: 780,
    promptMarkdown: [
      'For each position, give the product of every **other** number.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 3 4',
      'output: 24 12 8 6',
      '```',
      '',
      'At position 0 the answer is `2 x 3 x 4 = 24`, at position 1 it is',
      '`1 x 3 x 4 = 12`, and so on.',
      '',
      '**You may not use division.** The list has at least two numbers, and it may',
      'contain zeros and negatives.',
    ].join('\n'),
    editorialMarkdown: [
      '## Everything to the left, times everything to the right',
      '',
      'Multiply everything and divide by each element — and the statement forbids',
      'it. That ban is not arbitrary: division breaks on zero, and a list with two',
      'zeros breaks it twice. Handling that needs special cases for "one zero" and',
      '"more than one zero", and the resulting code is longer than the intended',
      'solution.',
      '',
      'The intended solution splits the answer in two. The product of everything',
      'except position `i` is:',
      '',
      '```',
      '(everything to the left of i) x (everything to the right of i)',
      '```',
      '',
      'So make one pass left to right carrying a running product, writing it into',
      'the output *before* including the current element. Then one pass right to',
      'left doing the same, multiplying into what is already there.',
      '',
      'Both running products start at **1**, the identity for multiplication — which',
      'is exactly right for the ends, where there is nothing on one side.',
      '',
      'O(n) time, and O(1) extra space if you do not count the output array. Zeros',
      'need no special handling at all: they simply propagate, which is the elegant',
      'part.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const out = new Array(a.length).fill(1);\n  let running = 1;\n  for (let i = 0; i < a.length; i++) {\n    out[i] = running;\n    running *= a[i];\n  }\n  running = 1;\n  for (let i = a.length - 1; i >= 0; i--) {\n    out[i] *= running;\n    running *= a[i];\n  }\n  return out;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): number[] {\n  const out: number[] = new Array(a.length).fill(1);\n  let running = 1;\n  for (let i = 0; i < a.length; i++) {\n    out[i] = running;\n    running *= a[i];\n  }\n  running = 1;\n  for (let i = a.length - 1; i >= 0; i--) {\n    out[i] *= running;\n    running *= a[i];\n  }\n  return out;\n}',
      PYTHON:
        'def solve(a):\n    out = [1] * len(a)\n    running = 1\n    for i in range(len(a)):\n        out[i] = running\n        running *= a[i]\n    running = 1\n    for i in range(len(a) - 1, -1, -1):\n        out[i] *= running\n        running *= a[i]\n    return out',
      JAVA: '    static int[] solve(int[] a) {\n        int[] out = new int[a.length];\n        int running = 1;\n        for (int i = 0; i < a.length; i++) {\n            out[i] = running;\n            running *= a[i];\n        }\n        running = 1;\n        for (int i = a.length - 1; i >= 0; i--) {\n            out[i] *= running;\n            running *= a[i];\n        }\n        return out;\n    }',
      CPP: 'vector<int> solve(vector<int> a) {\n    vector<int> out(a.size(), 1);\n    int running = 1;\n    for (size_t i = 0; i < a.size(); i++) {\n        out[i] = running;\n        running *= a[i];\n    }\n    running = 1;\n    for (int i = (int) a.size() - 1; i >= 0; i--) {\n        out[i] *= running;\n        running *= a[i];\n    }\n    return out;\n}',
      GO: 'func solve(a []int) []int {\n\tout := make([]int, len(a))\n\trunning := 1\n\tfor i := range a {\n\t\tout[i] = running\n\t\trunning *= a[i]\n\t}\n\trunning = 1\n\tfor i := len(a) - 1; i >= 0; i-- {\n\t\tout[i] *= running\n\t\trunning *= a[i]\n\t}\n\treturn out\n}',
    },
    tests: [
      { stdin: '1 2 3 4', expectedStdout: '24 12 8 6', isSample: true },
      { stdin: '2 3', expectedStdout: '3 2', isSample: true },
      { stdin: '1 0 3', expectedStdout: '0 3 0' },
      { stdin: '0 0 5', expectedStdout: '0 0 0' },
      { stdin: '-1 2 -3', expectedStdout: '-6 3 -2' },
      { stdin: '5 1 1', expectedStdout: '1 5 5' },
    ],
  }),

  p({
    ...base,
    slug: 'longest-consecutive-run',
    title: 'Longest Consecutive Run',
    difficulty: 'MEDIUM',
    patternTags: ['hash-set', 'sequence', 'start-detection'],
    signatureId: 'fn:ints->int',
    avgSolveSeconds: 840,
    promptMarkdown: [
      'Find the length of the longest run of consecutive numbers.',
      '',
      'The numbers may appear in any order, and the run does not have to be',
      'contiguous in the list — only the *values* must be consecutive.',
      '',
      '**Example**',
      '',
      '```',
      'input:  100 4 200 1 3 2',
      'output: 4',
      '```',
      '',
      '`1 2 3 4` are all present, so the longest run has length 4.',
      '',
      'An empty list has length `0`. Duplicates count once.',
    ].join('\n'),
    editorialMarkdown: [
      '## Only start counting from the start of a run',
      '',
      'Sorting makes this easy and costs O(n log n). The O(n) solution is the reason',
      'the problem is asked, and it turns on one small idea.',
      '',
      'Put every value in a hash set. Now, for each value, you *could* walk upward',
      'counting `n+1`, `n+2`, ... — but doing that for every value re-walks the same',
      'run once per member, which is O(n^2) on a long run.',
      '',
      'The fix: only walk from a value that **starts** a run. And a value starts a',
      'run exactly when `n - 1` is not in the set:',
      '',
      '```',
      'for n in set:',
      '    if (n - 1) not in set:        # n is the start of its run',
      '        length = 1',
      '        while (n + length) in set: length += 1',
      '        best = max(best, length)',
      '```',
      '',
      'Each value is now visited at most twice — once by the outer loop, once by the',
      'inner walk of its own run — so the whole thing is O(n) despite the nested',
      'loop. That is worth internalising: a nested loop is not automatically',
      'quadratic; what matters is how many times each element is touched.',
      '',
      'Iterate the **set**, not the list, or duplicates make you redo work.',
      '',
      'O(n) time, O(n) space.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const set = new Set(a);\n  let best = 0;\n  for (const n of set) {\n    if (set.has(n - 1)) continue;\n    let length = 1;\n    while (set.has(n + length)) length++;\n    if (length > best) best = length;\n  }\n  return best;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): number {\n  const set = new Set<number>(a);\n  let best = 0;\n  for (const n of set) {\n    if (set.has(n - 1)) continue;\n    let length = 1;\n    while (set.has(n + length)) length++;\n    if (length > best) best = length;\n  }\n  return best;\n}',
      PYTHON:
        'def solve(a):\n    s = set(a)\n    best = 0\n    for n in s:\n        if n - 1 in s:\n            continue\n        length = 1\n        while n + length in s:\n            length += 1\n        if length > best:\n            best = length\n    return best',
      JAVA: '    static int solve(int[] a) {\n        Set<Integer> s = new HashSet<>();\n        for (int n : a) s.add(n);\n        int best = 0;\n        for (int n : s) {\n            if (s.contains(n - 1)) continue;\n            int length = 1;\n            while (s.contains(n + length)) length++;\n            if (length > best) best = length;\n        }\n        return best;\n    }',
      CPP: 'int solve(vector<int> a) {\n    unordered_set<int> s(a.begin(), a.end());\n    int best = 0;\n    for (int n : s) {\n        if (s.count(n - 1)) continue;\n        int length = 1;\n        while (s.count(n + length)) length++;\n        if (length > best) best = length;\n    }\n    return best;\n}',
      GO: 'func solve(a []int) int {\n\ts := map[int]bool{}\n\tfor _, n := range a {\n\t\ts[n] = true\n\t}\n\tbest := 0\n\tfor n := range s {\n\t\tif s[n-1] {\n\t\t\tcontinue\n\t\t}\n\t\tlength := 1\n\t\tfor s[n+length] {\n\t\t\tlength++\n\t\t}\n\t\tif length > best {\n\t\t\tbest = length\n\t\t}\n\t}\n\treturn best\n}',
    },
    tests: [
      { stdin: '100 4 200 1 3 2', expectedStdout: '4', isSample: true },
      { stdin: '', expectedStdout: '0', isSample: true },
      { stdin: '5', expectedStdout: '1' },
      { stdin: '1 1 1', expectedStdout: '1' },
      { stdin: '0 -1 1 2', expectedStdout: '4' },
      { stdin: '10 30 20', expectedStdout: '1' },
    ],
  }),

  p({
    ...base,
    slug: 'subarray-sum-equals-target',
    title: 'Slices That Add Up',
    difficulty: 'HARD',
    patternTags: ['prefix-sum', 'hash-map', 'counting'],
    signatureId: 'fn:ints,int->int',
    avgSolveSeconds: 1080,
    promptMarkdown: [
      'Count the runs of neighbouring numbers that add up to a target.',
      '',
      'A run is any stretch of one or more positions next to each other. The first',
      'line is the list, the second the target.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 1 1',
      '        2',
      'output: 2',
      '```',
      '',
      'Positions 0-1 and positions 1-2 both add to `2`, so the answer is `2`.',
      '',
      'Runs at different positions count separately even if they contain the same',
      'values. Numbers may be negative, and the target may be zero.',
    ].join('\n'),
    editorialMarkdown: [
      '## Prefix sums, and the difference that vanishes',
      '',
      'Checking every run is two nested loops, O(n^2). To do better, notice that the',
      'sum of the run from `i` to `j` is a subtraction of two prefix sums:',
      '',
      '```',
      'sum(i..j) = prefix(j) - prefix(i-1)',
      '```',
      '',
      'So a run ending at `j` hits the target exactly when some earlier prefix',
      'equals `prefix(j) - target`. That is a membership question again — and this',
      'time you want the *count*, because several earlier positions may give the',
      'same prefix.',
      '',
      'So: walk once, carrying the running prefix, and keep a hash map from prefix',
      'value to how many times it has occurred.',
      '',
      '```',
      'counts = {0: 1}',
      'for n in list:',
      '    running += n',
      '    total += counts.get(running - target, 0)',
      '    counts[running] += 1',
      '```',
      '',
      'The `{0: 1}` seed is the part everyone forgets, and it is not a trick: it says',
      '"before you start, the empty prefix has sum zero, once". Without it, every run',
      'that begins at position 0 is missed, and the bug is quiet — the answer is',
      'merely too small.',
      '',
      'Note the sliding-window approach does **not** work here, because negative',
      'numbers mean growing the window can decrease the sum. That precondition is why',
      'the statement mentions negatives.',
      '',
      'O(n) time, O(n) space.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  const counts = new Map([[0, 1]]);\n  let running = 0;\n  let total = 0;\n  for (const n of a) {\n    running += n;\n    total += counts.get(running - b) || 0;\n    counts.set(running, (counts.get(running) || 0) + 1);\n  }\n  return total;\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number): number {\n  const counts = new Map<number, number>([[0, 1]]);\n  let running = 0;\n  let total = 0;\n  for (const n of a) {\n    running += n;\n    total += counts.get(running - b) || 0;\n    counts.set(running, (counts.get(running) || 0) + 1);\n  }\n  return total;\n}',
      PYTHON:
        'def solve(a, b):\n    counts = {0: 1}\n    running = 0\n    total = 0\n    for n in a:\n        running += n\n        total += counts.get(running - b, 0)\n        counts[running] = counts.get(running, 0) + 1\n    return total',
      JAVA: '    static int solve(int[] a, int b) {\n        Map<Integer, Integer> counts = new HashMap<>();\n        counts.put(0, 1);\n        int running = 0, total = 0;\n        for (int n : a) {\n            running += n;\n            total += counts.getOrDefault(running - b, 0);\n            counts.merge(running, 1, Integer::sum);\n        }\n        return total;\n    }',
      CPP: 'int solve(vector<int> a, int b) {\n    unordered_map<int, int> counts;\n    counts[0] = 1;\n    int running = 0, total = 0;\n    for (int n : a) {\n        running += n;\n        auto it = counts.find(running - b);\n        if (it != counts.end()) total += it->second;\n        counts[running]++;\n    }\n    return total;\n}',
      GO: 'func solve(a []int, b int) int {\n\tcounts := map[int]int{0: 1}\n\trunning, total := 0, 0\n\tfor _, n := range a {\n\t\trunning += n\n\t\ttotal += counts[running-b]\n\t\tcounts[running]++\n\t}\n\treturn total\n}',
    },
    tests: [
      { stdin: '1 1 1\n2', expectedStdout: '2', isSample: true },
      { stdin: '1 2 3\n3', expectedStdout: '2', isSample: true },
      { stdin: '\n0', expectedStdout: '0' },
      { stdin: '1 -1 0\n0', expectedStdout: '3' },
      { stdin: '3\n3', expectedStdout: '1' },
      { stdin: '1 2 3\n7', expectedStdout: '0' },
    ],
  }),

  p({
    ...base,
    slug: 'intersection-of-two-lists',
    title: 'What Both Lists Contain',
    difficulty: 'EASY',
    patternTags: ['hash-set', 'membership', 'deduplication'],
    signatureId: 'fn:ints,ints->ints',
    avgSolveSeconds: 420,
    promptMarkdown: [
      'Find the values that appear in both lists.',
      '',
      'Each value appears **once** in the answer, in the order it first appears in',
      'the first list.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 2 1',
      '        2 2',
      'output: 2',
      '```',
      '',
      'If nothing is shared, the answer is empty.',
    ].join('\n'),
    editorialMarkdown: [
      '## One set to ask, one set to remember',
      '',
      'Two jobs again, and they need two containers.',
      '',
      'Build a set from the **second** list — that answers "is this value shared?"',
      'in constant time. Then walk the **first** list in order, emitting a value when',
      'it is in that set and has not been emitted yet, which needs a second set to',
      'track what you already output.',
      '',
      'Walking the first list rather than iterating either set is what satisfies the',
      'ordering requirement. Sets have no order, so any solution that iterates one',
      'produces an order that varies by language and by run — which passes your local',
      'test and fails the grader.',
      '',
      'The deduplication set is the other half. Without it, `1 2 2 1` against `2 2`',
      'emits `2` twice.',
      '',
      'O(n + m) time, O(n + m) space. Sorting both and walking them together is the',
      'O(1)-extra-space alternative, at O(n log n).',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  const inB = new Set(b);\n  const emitted = new Set();\n  const out = [];\n  for (const n of a) {\n    if (inB.has(n) && !emitted.has(n)) {\n      emitted.add(n);\n      out.push(n);\n    }\n  }\n  return out;\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number[]): number[] {\n  const inB = new Set<number>(b);\n  const emitted = new Set<number>();\n  const out: number[] = [];\n  for (const n of a) {\n    if (inB.has(n) && !emitted.has(n)) {\n      emitted.add(n);\n      out.push(n);\n    }\n  }\n  return out;\n}',
      PYTHON:
        'def solve(a, b):\n    in_b = set(b)\n    emitted = set()\n    out = []\n    for n in a:\n        if n in in_b and n not in emitted:\n            emitted.add(n)\n            out.append(n)\n    return out',
      JAVA: '    static int[] solve(int[] a, int[] b) {\n        Set<Integer> inB = new HashSet<>();\n        for (int n : b) inB.add(n);\n        Set<Integer> emitted = new HashSet<>();\n        List<Integer> out = new ArrayList<>();\n        for (int n : a) {\n            if (inB.contains(n) && emitted.add(n)) out.add(n);\n        }\n        int[] r = new int[out.size()];\n        for (int i = 0; i < r.length; i++) r[i] = out.get(i);\n        return r;\n    }',
      CPP: 'vector<int> solve(vector<int> a, vector<int> b) {\n    unordered_set<int> inB(b.begin(), b.end());\n    unordered_set<int> emitted;\n    vector<int> out;\n    for (int n : a) {\n        if (inB.count(n) && emitted.insert(n).second) out.push_back(n);\n    }\n    return out;\n}',
      GO: 'func solve(a []int, b []int) []int {\n\tinB := map[int]bool{}\n\tfor _, n := range b {\n\t\tinB[n] = true\n\t}\n\temitted := map[int]bool{}\n\tout := []int{}\n\tfor _, n := range a {\n\t\tif inB[n] && !emitted[n] {\n\t\t\temitted[n] = true\n\t\t\tout = append(out, n)\n\t\t}\n\t}\n\treturn out\n}',
    },
    tests: [
      { stdin: '1 2 2 1\n2 2', expectedStdout: '2', isSample: true },
      { stdin: '4 9 5\n9 4 9 8 4', expectedStdout: '4 9', isSample: true },
      { stdin: '1 2\n3 4', expectedStdout: '' },
      { stdin: '\n1 2', expectedStdout: '' },
      { stdin: '3 1 2\n2 1 3', expectedStdout: '3 1 2' },
      { stdin: '-1 0 1\n0 -1', expectedStdout: '-1 0' },
    ],
  }),

  p({
    ...base,
    slug: 'first-unique-number',
    title: 'The First Number That Appears Once',
    difficulty: 'EASY',
    patternTags: ['hash-map', 'frequency-count', 'two-pass'],
    signatureId: 'fn:ints->int',
    avgSolveSeconds: 420,
    promptMarkdown: [
      'Find the first number that appears exactly once.',
      '',
      '"First" means earliest in the list, not smallest.',
      '',
      '**Example**',
      '',
      '```',
      'input:  2 3 2 4 3 5',
      'output: 4',
      '```',
      '',
      '`2` and `3` both repeat; `4` is the first that does not.',
      '',
      'If every number repeats, return `-1`.',
    ].join('\n'),
    editorialMarkdown: [
      '## Count first, then scan in order',
      '',
      'The same two-pass shape as counting unique characters, and the same reason for',
      'it: you cannot know whether a number is unique until you have seen the whole',
      'list, because its twin might be the last element.',
      '',
      'Pass one builds a frequency map. Pass two walks the **original list**, in',
      'order, and returns the first value whose count is 1.',
      '',
      'That second pass has to iterate the list, not the map. A hash map has no',
      'order, so scanning it gives you *a* unique number rather than the *first* one',
      '— and on many inputs it will even give the right answer, which is what makes',
      'the bug so easy to ship. The failure only shows up when the map happens to',
      'enumerate in a different order, which can vary between runs.',
      '',
      'This is the general lesson of the family: hash structures answer questions',
      'about membership and counts, and they answer nothing at all about order. When',
      'order matters, keep the ordered thing around.',
      '',
      'O(n) time, O(n) space.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const counts = new Map();\n  for (const n of a) counts.set(n, (counts.get(n) || 0) + 1);\n  for (const n of a) if (counts.get(n) === 1) return n;\n  return -1;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): number {\n  const counts = new Map<number, number>();\n  for (const n of a) counts.set(n, (counts.get(n) || 0) + 1);\n  for (const n of a) if (counts.get(n) === 1) return n;\n  return -1;\n}',
      PYTHON:
        'def solve(a):\n    counts = {}\n    for n in a:\n        counts[n] = counts.get(n, 0) + 1\n    for n in a:\n        if counts[n] == 1:\n            return n\n    return -1',
      JAVA: '    static int solve(int[] a) {\n        Map<Integer, Integer> counts = new HashMap<>();\n        for (int n : a) counts.merge(n, 1, Integer::sum);\n        for (int n : a) if (counts.get(n) == 1) return n;\n        return -1;\n    }',
      CPP: 'int solve(vector<int> a) {\n    unordered_map<int, int> counts;\n    for (int n : a) counts[n]++;\n    for (int n : a) if (counts[n] == 1) return n;\n    return -1;\n}',
      GO: 'func solve(a []int) int {\n\tcounts := map[int]int{}\n\tfor _, n := range a {\n\t\tcounts[n]++\n\t}\n\tfor _, n := range a {\n\t\tif counts[n] == 1 {\n\t\t\treturn n\n\t\t}\n\t}\n\treturn -1\n}',
    },
    tests: [
      { stdin: '2 3 2 4 3 5', expectedStdout: '4', isSample: true },
      { stdin: '1 1 2 2', expectedStdout: '-1', isSample: true },
      { stdin: '', expectedStdout: '-1' },
      { stdin: '9', expectedStdout: '9' },
      { stdin: '5 5 5 7', expectedStdout: '7' },
      { stdin: '-1 -1 -2', expectedStdout: '-2' },
    ],
  }),

  p({
    ...base,
    slug: 'count-pairs-with-sum',
    title: 'How Many Pairs Add Up',
    difficulty: 'MEDIUM',
    patternTags: ['hash-map', 'complement', 'counting'],
    signatureId: 'fn:ints,int->int',
    avgSolveSeconds: 660,
    promptMarkdown: [
      'Count how many pairs of positions add up to a target.',
      '',
      'A pair is two **different** positions. Two pairs are the same only if they',
      'use the same two positions, so repeated values can form several pairs.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 1 1',
      '        2',
      'output: 3',
      '```',
      '',
      'Positions (0,1), (0,2) and (1,2) each add to `2`.',
      '',
      'If no pair works, the answer is `0`.',
    ].join('\n'),
    editorialMarkdown: [
      '## Count the complements you have already passed',
      '',
      'The same complement idea as finding a single pair, but now you want *how',
      'many* rather than *which*, and that changes the bookkeeping.',
      '',
      'Walk the list once. Before recording the current number, ask how many earlier',
      'numbers equal `target - n` — every one of them forms a pair with the current',
      'position:',
      '',
      '```',
      'for n in list:',
      '    total += counts.get(target - n, 0)',
      '    counts[n] += 1',
      '```',
      '',
      'Counting *backwards* like this — only against elements already seen — is what',
      'stops each pair being counted twice. If you instead counted every match in the',
      'whole list for every element, you would get exactly double, and halving it is',
      'a fix that breaks the moment a number pairs with itself.',
      '',
      'The `1 1 1` case is the one to check by hand. At the second `1` there is one',
      'earlier `1`, giving one pair; at the third there are two, giving two more.',
      'Three in total, which is what naive doubling would get wrong.',
      '',
      'O(n) time, O(n) space.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  const counts = new Map();\n  let total = 0;\n  for (const n of a) {\n    total += counts.get(b - n) || 0;\n    counts.set(n, (counts.get(n) || 0) + 1);\n  }\n  return total;\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number): number {\n  const counts = new Map<number, number>();\n  let total = 0;\n  for (const n of a) {\n    total += counts.get(b - n) || 0;\n    counts.set(n, (counts.get(n) || 0) + 1);\n  }\n  return total;\n}',
      PYTHON:
        'def solve(a, b):\n    counts = {}\n    total = 0\n    for n in a:\n        total += counts.get(b - n, 0)\n        counts[n] = counts.get(n, 0) + 1\n    return total',
      JAVA: '    static int solve(int[] a, int b) {\n        Map<Integer, Integer> counts = new HashMap<>();\n        int total = 0;\n        for (int n : a) {\n            total += counts.getOrDefault(b - n, 0);\n            counts.merge(n, 1, Integer::sum);\n        }\n        return total;\n    }',
      CPP: 'int solve(vector<int> a, int b) {\n    unordered_map<int, int> counts;\n    int total = 0;\n    for (int n : a) {\n        auto it = counts.find(b - n);\n        if (it != counts.end()) total += it->second;\n        counts[n]++;\n    }\n    return total;\n}',
      GO: 'func solve(a []int, b int) int {\n\tcounts := map[int]int{}\n\ttotal := 0\n\tfor _, n := range a {\n\t\ttotal += counts[b-n]\n\t\tcounts[n]++\n\t}\n\treturn total\n}',
    },
    tests: [
      { stdin: '1 1 1\n2', expectedStdout: '3', isSample: true },
      { stdin: '1 2 3 4\n5', expectedStdout: '2', isSample: true },
      // Two parameters means two lines. An empty first line is an empty list,
      // but the target line still has to be there.
      { stdin: '\n0', expectedStdout: '0' },
      { stdin: '1 2 3\n99', expectedStdout: '0' },
      { stdin: '0 0\n0', expectedStdout: '1' },
      { stdin: '-1 1 0 0\n0', expectedStdout: '2' },
    ],
  }),
];
