import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w2-142` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-17 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W2_142_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "signal-frequency-triplets",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Checkpoint Distances",
    patternTags: ["array","hash-set","two-pointers"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 400,
    promptMarkdown: "You are designing an autonomous vehicle course along a long, straight road. You have a list of checkpoint locations represented as a strictly increasing array of integers, `locations`, and a required spacing integer, `diff`.\n\nYour task is to find the number of perfectly spaced checkpoint triplets. A perfectly spaced triplet is defined by three indices `(i, j, k)` satisfying the following conditions:\n- `i < j < k`\n- `locations[j] - locations[i] == diff`\n- `locations[k] - locations[j] == diff`\n\nReturn the total number of such triplets in the course.\n\n**Constraints**\n- `3 <= locations.length <= 100`\n- `0 <= locations[i] <= 1000`\n- The `locations` array is strictly increasing.\n- `1 <= diff <= 100`\n\n**Example 1**\n```\ninput:\n1 3 5 7\n2\noutput:\n2\n```\n*Explanation: The valid triplets are indices (0, 1, 2) where values are 1, 3, 5 and indices (1, 2, 3) where values are 3, 5, 7. Both have a strict step difference of 2.*\n\n**Example 2**\n```\ninput:\n0 1 4 6 7 10\n3\noutput:\n2\n```\n*Explanation: The perfectly spaced triplets are indices (1, 2, 5) with values 1, 4, 10 and indices (0, 2, 4) with values 0, 4, 7.*\n\n**Example 3**\n```\ninput:\n2 4 8\n1\noutput:\n0\n```\n*Explanation: No three locations are separated by exactly 1.*\n\n**Follow-up**\nCan you find a solution that operates in O(N) time complexity using a Hash Set?",
    editorialMarkdown: "## Signal Frequency Triplets\n\nThe goal is to count the number of triplets (i, j, k) with `i < j < k` such that the difference between the j-th and i-th signal is `diff`, and the difference between the k-th and j-th signal is also `diff`. \n\nBecause the input array of signals is strictly increasing, if we fix an element `signals[i]`, we are simply looking for whether `signals[i] + diff` and `signals[i] + 2 * diff` also exist in the array. \nFor an O(N^3) brute-force solution, we can use three nested loops to verify the condition for every possible combination of indices. A much more optimal approach is to store all values in a hash set, and then for each element, check if the required two subsequent values are also present in the set.\n\n**Trap**: Assuming the elements are unsorted or can contain duplicates, which would complicate the triplet counting. However, the constraints guarantee that the elements are strictly increasing.\n\n**Complexity:**\n- **Time:** O(N) using a hash set, or O(N^3) with three nested loops, where N is the number of signals.\n- **Space:** O(N) for the hash set, or O(1) for the nested loops approach.",
    referenceSolution: {
      JAVASCRIPT: "function solve(signals, diff) {\n    let ans = 0;\n    let n = signals.length;\n    for (let i = 0; i < n; i++) {\n        for (let j = i + 1; j < n; j++) {\n            if (signals[j] - signals[i] === diff) {\n                for (let k = j + 1; k < n; k++) {\n                    if (signals[k] - signals[j] === diff) {\n                        ans++;\n                    }\n                }\n            }\n        }\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(signals: number[], diff: number): number {\n    let ans = 0;\n    let n = signals.length;\n    for (let i = 0; i < n; i++) {\n        for (let j = i + 1; j < n; j++) {\n            if (signals[j] - signals[i] === diff) {\n                for (let k = j + 1; k < n; k++) {\n                    if (signals[k] - signals[j] === diff) {\n                        ans++;\n                    }\n                }\n            }\n        }\n    }\n    return ans;\n}",
      PYTHON: "def solve(signals, diff):\n    ans = 0\n    n = len(signals)\n    for i in range(n):\n        for j in range(i + 1, n):\n            if signals[j] - signals[i] == diff:\n                for k in range(j + 1, n):\n                    if signals[k] - signals[j] == diff:\n                        ans += 1\n    return ans",
      JAVA: "    static int solve(int[] signals, int diff) {\n        int ans = 0;\n        int n = signals.length;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                if (signals[j] - signals[i] == diff) {\n                    for (int k = j + 1; k < n; k++) {\n                        if (signals[k] - signals[j] == diff) {\n                            ans++;\n                        }\n                    }\n                }\n            }\n        }\n        return ans;\n    }",
      CPP: "int solve(vector<int> signals, int diff) {\n    int ans = 0;\n    int n = signals.size();\n    for (int i = 0; i < n; i++) {\n        for (int j = i + 1; j < n; j++) {\n            if (signals[j] - signals[i] == diff) {\n                for (int k = j + 1; k < n; k++) {\n                    if (signals[k] - signals[j] == diff) {\n                        ans++;\n                    }\n                }\n            }\n        }\n    }\n    return ans;\n}",
      GO: "func solve(signals []int, diff int) int {\n    ans := 0\n    n := len(signals)\n    for i := 0; i < n; i++ {\n        for j := i + 1; j < n; j++ {\n            if signals[j] - signals[i] == diff {\n                for k := j + 1; k < n; k++ {\n                    if signals[k] - signals[j] == diff {\n                        ans++\n                    }\n                }\n            }\n        }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "1 3 5 7\n2", expectedStdout: "2", isSample: true },
      { stdin: "0 1 4 6 7 10\n3", expectedStdout: "2", isSample: true },
      { stdin: "2 4 8\n1", expectedStdout: "0" },
      { stdin: "1 2 3\n2", expectedStdout: "0" },
      { stdin: "10 20 30\n10", expectedStdout: "1" },
      { stdin: "0 5 10 15 20\n5", expectedStdout: "3" },
      { stdin: "1 10 100 1000\n9", expectedStdout: "0" },
      { stdin: "2 5 7 8 10 12\n3", expectedStdout: "1" },
    ],
  }),

  p({
    ...base,
    slug: "forum-interaction-counts",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Forum Interaction Counts",
    patternTags: ["hash-map","hash-set","sorting"],
    signatureId: "fn:matrix->matrix",
    avgSolveSeconds: 600,
    promptMarkdown: "You have a log of interactions on a community forum. Each interaction is recorded as a pair of integers `[interactionId, parentId]`, provided as a 2D array `interactions`. \n\nIf `parentId` is `0`, the interaction is a new main post. \nIf `parentId` is greater than `0`, the interaction is a comment on the post with ID `parentId`. \n\nYour task is to count the number of **unique** comments on each main post. The same user might trigger the same interaction log multiple times, meaning there can be duplicate comments with the exact same `interactionId` on the same post. You should count the number of distinct `interactionId`s that are comments for a given post. \n\nReturn a 2D array where each inner array is `[postId, numberOfUniqueComments]`. The result must contain all posts, even those with zero comments, and it must be sorted by `postId` in ascending order.\n\n**Constraints**\n- `1 <= interactions.length <= 100`\n- `interactions[i].length == 2`\n- `1 <= interactions[i][0] <= 1000`\n- `0 <= interactions[i][1] <= 1000`\n- It is guaranteed that any `parentId > 0` corresponds to a valid post's `interactionId` that also appears in the logs with `parentId == 0`.\n\n**Example 1**\n```\ninput:\n1 0;2 0;3 1;4 1;5 2\noutput:\n1 2;2 1\n```\n*Explanation: Interaction 1 and 2 are posts. Interaction 3 and 4 are comments on post 1. Interaction 5 is a comment on post 2.*\n\n**Example 2**\n```\ninput:\n10 0;11 10;11 10;12 0\noutput:\n10 1;12 0\n```\n*Explanation: Interaction 10 and 12 are posts. Interaction 11 is a comment on post 10, appearing twice, so it's counted only once. Post 12 has no comments.*\n\n**Example 3**\n```\ninput:\n100 0\noutput:\n100 0\n```\n*Explanation: There is only one post and zero comments.*\n\n**Follow-up**\nCan you optimize this by using hash maps so that counting unique comments takes O(N) time?",
    editorialMarkdown: "## Forum Interaction Counts\n\nThe problem requires us to count the number of unique comments for each main post in a forum. A post is an interaction with `parentId == 0`, and a comment is an interaction where `parentId` corresponds to a post's `interactionId`.\n\nFirst, we can scan the input to identify all unique post IDs (those with `parentId == 0`). We store these in a list and sort them in ascending order. \nNext, for each post ID in our sorted list, we scan the input again to find all comments that have this post ID as their `parentId`. We need to ensure we only count unique `interactionId`s for comments. A hash set is a natural choice to track which comments we have already seen for a given post. Finally, we build the result array where each entry is `[postId, uniqueCommentCount]`.\n\n**Trap**: A post may exist without any comments, in which case the count should be `0`. Also, duplicate comments might be logged, so uniqueness is essential.\n\n**Complexity:**\n- **Time:** O(N^2) using loops and sorting, or O(N log N) using hash maps to group and count efficiently.\n- **Space:** O(N) to store the posts and the unique comments for counting.",
    referenceSolution: {
      JAVASCRIPT: "function solve(interactions) {\n    let posts = [];\n    for (let inter of interactions) {\n        if (inter[1] === 0) {\n            let found = false;\n            for (let p of posts) {\n                if (p === inter[0]) found = true;\n            }\n            if (!found) posts.push(inter[0]);\n        }\n    }\n    \n    posts.sort((a, b) => a - b);\n    \n    let ans = [];\n    for (let p of posts) {\n        let cmts = [];\n        for (let inter of interactions) {\n            if (inter[1] === p) {\n                let c_found = false;\n                for (let c of cmts) {\n                    if (c === inter[0]) c_found = true;\n                }\n                if (!c_found) cmts.push(inter[0]);\n            }\n        }\n        ans.push([p, cmts.length]);\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(interactions: number[][]): number[][] {\n    let posts: number[] = [];\n    for (let inter of interactions) {\n        if (inter[1] === 0) {\n            let found = false;\n            for (let p of posts) {\n                if (p === inter[0]) found = true;\n            }\n            if (!found) posts.push(inter[0]);\n        }\n    }\n    \n    posts.sort((a, b) => a - b);\n    \n    let ans: number[][] = [];\n    for (let p of posts) {\n        let cmts: number[] = [];\n        for (let inter of interactions) {\n            if (inter[1] === p) {\n                let c_found = false;\n                for (let c of cmts) {\n                    if (c === inter[0]) c_found = true;\n                }\n                if (!c_found) cmts.push(inter[0]);\n            }\n        }\n        ans.push([p, cmts.length]);\n    }\n    return ans;\n}",
      PYTHON: "def solve(interactions):\n    posts = []\n    for inter in interactions:\n        if inter[1] == 0:\n            found = False\n            for p in posts:\n                if p == inter[0]: found = True\n            if not found: posts.append(inter[0])\n            \n    posts.sort()\n    \n    ans = []\n    for p in posts:\n        cmts = []\n        for inter in interactions:\n            if inter[1] == p:\n                c_found = False\n                for c in cmts:\n                    if c == inter[0]: c_found = True\n                if not c_found: cmts.append(inter[0])\n        ans.append([p, len(cmts)])\n    return ans",
      JAVA: "    static int[][] solve(int[][] interactions) {\n        java.util.List<Integer> posts = new java.util.ArrayList<>();\n        for (int[] inter : interactions) {\n            if (inter[1] == 0) {\n                boolean found = false;\n                for (int p : posts) {\n                    if (p == inter[0]) found = true;\n                }\n                if (!found) posts.add(inter[0]);\n            }\n        }\n        \n        for (int i = 0; i < posts.size(); i++) {\n            for (int j = i + 1; j < posts.size(); j++) {\n                if (posts.get(i) > posts.get(j)) {\n                    int temp = posts.get(i);\n                    posts.set(i, posts.get(j));\n                    posts.set(j, temp);\n                }\n            }\n        }\n        \n        int[][] ans = new int[posts.size()][2];\n        for (int i = 0; i < posts.size(); i++) {\n            int p = posts.get(i);\n            java.util.List<Integer> cmts = new java.util.ArrayList<>();\n            for (int[] inter : interactions) {\n                if (inter[1] == p) {\n                    boolean c_found = false;\n                    for (int c : cmts) {\n                        if (c == inter[0]) c_found = true;\n                    }\n                    if (!c_found) cmts.add(inter[0]);\n                }\n            }\n            ans[i][0] = p;\n            ans[i][1] = cmts.size();\n        }\n        return ans;\n    }",
      CPP: "vector<vector<int>> solve(vector<vector<int>> interactions) {\n    vector<int> posts;\n    for (auto& inter : interactions) {\n        if (inter[1] == 0) {\n            bool found = false;\n            for (int p : posts) {\n                if (p == inter[0]) found = true;\n            }\n            if (!found) posts.push_back(inter[0]);\n        }\n    }\n    \n    for (size_t i = 0; i < posts.size(); i++) {\n        for (size_t j = i + 1; j < posts.size(); j++) {\n            if (posts[i] > posts[j]) {\n                int temp = posts[i];\n                posts[i] = posts[j];\n                posts[j] = temp;\n            }\n        }\n    }\n    \n    vector<vector<int>> ans;\n    for (int p : posts) {\n        vector<int> cmts;\n        for (auto& inter : interactions) {\n            if (inter[1] == p) {\n                bool c_found = false;\n                for (int c : cmts) {\n                    if (c == inter[0]) c_found = true;\n                }\n                if (!c_found) cmts.push_back(inter[0]);\n            }\n        }\n        ans.push_back({p, (int)cmts.size()});\n    }\n    return ans;\n}",
      GO: "func solve(interactions [][]int) [][]int {\n    posts := []int{}\n    for _, inter := range interactions {\n        if inter[1] == 0 {\n            found := false\n            for _, p := range posts {\n                if p == inter[0] {\n                    found = true\n                }\n            }\n            if !found {\n                posts = append(posts, inter[0])\n            }\n        }\n    }\n    \n    for i := 0; i < len(posts); i++ {\n        for j := i + 1; j < len(posts); j++ {\n            if posts[i] > posts[j] {\n                posts[i], posts[j] = posts[j], posts[i]\n            }\n        }\n    }\n    \n    ans := [][]int{}\n    for _, p := range posts {\n        cmts := []int{}\n        for _, inter := range interactions {\n            if inter[1] == p {\n                c_found := false\n                for _, c := range cmts {\n                    if c == inter[0] {\n                        c_found = true\n                    }\n                }\n                if !c_found {\n                    cmts = append(cmts, inter[0])\n                }\n            }\n        }\n        ans = append(ans, []int{p, len(cmts)})\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "1 0;2 0;3 1;4 1;5 2", expectedStdout: "1 2;2 1", isSample: true },
      { stdin: "10 0;11 10;11 10;12 0", expectedStdout: "10 1;12 0", isSample: true },
      { stdin: "100 0", expectedStdout: "100 0" },
      { stdin: "9 0;5 0;6 5", expectedStdout: "5 1;9 0" },
      { stdin: "1 0;2 1;2 1;2 1", expectedStdout: "1 1" },
      { stdin: "1 0;2 1;3 1;1 0", expectedStdout: "1 2" },
      { stdin: "10 0;15 10;16 10;17 10", expectedStdout: "10 3" },
      { stdin: "1 0;4 1;2 0;5 2;3 0;6 3", expectedStdout: "1 1;2 1;3 1" },
    ],
  }),

  p({
    ...base,
    slug: "next-higher-stock-price",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "STACK",
    title: "Taller Trees Ahead",
    patternTags: ["array","hash-map","monotonic-stack","simulation"],
    signatureId: "fn:ints,ints->ints",
    avgSolveSeconds: 600,
    promptMarkdown: "You are surveying a row of trees in a forest. You want to know the height of the first tree *after* a specific queried tree that is strictly taller than the queried tree.\n\nYou are given an integer array `heights`, representing the sequence of tree heights from left to right, and another integer array `queries`, which is a subset of `heights`. For each value in `queries`, find the first height in `heights` that appears to its right and is strictly greater than it.\n\nReturn an array of the same length as `queries` containing these taller tree heights. If no taller tree exists to the right of the queried tree height, the result for that query should be `-1`.\n\n**Constraints**\n- `1 <= queries.length <= heights.length <= 100`\n- `1 <= heights[i], queries[i] <= 1000`\n- All integers in `heights` are distinct.\n- All integers in `queries` appear in `heights`.\n\n**Example 1**\n```\ninput:\n4 1 2\n1 3 4 2\noutput:\n-1 3 -1\n```\n*Explanation: For query 4, there is no tree to its right taller than 4. For query 1, the first taller tree to its right has height 3. For query 2, there are no trees to its right.*\n\n**Example 2**\n```\ninput:\n2 4\n1 2 3 4\noutput:\n3 -1\n```\n*Explanation: For query 2, the next taller tree is 3. For query 4, there is no taller tree to its right.*\n\n**Example 3**\n```\ninput:\n5\n5\noutput:\n-1\n```\n*Explanation: There is only one tree and no subsequent trees.*\n\n**Follow-up**\nCan you solve this with a time complexity of O(heights.length + queries.length)?",
    editorialMarkdown: "## Next Higher Stock Price\n\nThe problem requires us to find the first day after a queried day where the stock price was strictly higher. We are given the overall sequence of stock `prices` and a subset of `queries`.\n\nFor each query, we can scan the `prices` array to find the location of the queried price. Then, we continue scanning from that point to the right to find the first price that is strictly greater. If we reach the end of the array without finding such a price, we append -1 for that query. Since the queries are a subset of prices, the initial scan will always find a match.\n\n**Trap**: A common mistake is to try searching for the queried value from the beginning of `prices` every time, which works here but might fail if there are duplicates (though constraints guarantee distinct elements). A monotonic stack could be used to optimize to O(N + Q), but O(N*Q) is well within limits for EASY constraints.\n\n**Complexity:**\n- **Time:** O(N * Q), where N is the length of prices and Q is the length of queries, as we might scan the entire prices array for each query.\n- **Space:** O(1) auxiliary space (excluding the output array).",
    referenceSolution: {
      JAVASCRIPT: "function solve(queries, prices) {\n    let ans = [];\n    for (let q of queries) {\n        let found = false;\n        let res = -1;\n        for (let p of prices) {\n            if (p === q) {\n                found = true;\n            } else if (found && p > q) {\n                res = p;\n                break;\n            }\n        }\n        ans.push(res);\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(queries: number[], prices: number[]): number[] {\n    let ans: number[] = [];\n    for (let q of queries) {\n        let found = false;\n        let res = -1;\n        for (let p of prices) {\n            if (p === q) {\n                found = true;\n            } else if (found && p > q) {\n                res = p;\n                break;\n            }\n        }\n        ans.push(res);\n    }\n    return ans;\n}",
      PYTHON: "def solve(queries, prices):\n    ans = []\n    for q in queries:\n        found = False\n        res = -1\n        for p in prices:\n            if p == q:\n                found = True\n            elif found and p > q:\n                res = p\n                break\n        ans.append(res)\n    return ans",
      JAVA: "    static int[] solve(int[] queries, int[] prices) {\n        int[] ans = new int[queries.length];\n        for (int i = 0; i < queries.length; i++) {\n            int q = queries[i];\n            boolean found = false;\n            int res = -1;\n            for (int p : prices) {\n                if (p == q) {\n                    found = true;\n                } else if (found && p > q) {\n                    res = p;\n                    break;\n                }\n            }\n            ans[i] = res;\n        }\n        return ans;\n    }",
      CPP: "vector<int> solve(vector<int> queries, vector<int> prices) {\n    vector<int> ans;\n    for (int q : queries) {\n        bool found = false;\n        int res = -1;\n        for (int p : prices) {\n            if (p == q) {\n                found = true;\n            } else if (found && p > q) {\n                res = p;\n                break;\n            }\n        }\n        ans.push_back(res);\n    }\n    return ans;\n}",
      GO: "func solve(queries []int, prices []int) []int {\n    ans := make([]int, len(queries))\n    for i, q := range queries {\n        found := false\n        res := -1\n        for _, p := range prices {\n            if p == q {\n                found = true\n            } else if found && p > q {\n                res = p\n                break\n            }\n        }\n        ans[i] = res\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "4 1 2\n1 3 4 2", expectedStdout: "-1 3 -1", isSample: true },
      { stdin: "2 4\n1 2 3 4", expectedStdout: "3 -1", isSample: true },
      { stdin: "5\n5", expectedStdout: "-1" },
      { stdin: "5 10\n5 10", expectedStdout: "10 -1" },
      { stdin: "5\n5 10 6", expectedStdout: "10" },
      { stdin: "3 4 5 1\n3 4 5 1", expectedStdout: "4 5 -1 -1" },
      { stdin: "1 2 3\n1 2 3 9", expectedStdout: "2 3 9" },
      { stdin: "5 1 2 3\n5 1 4 2 3", expectedStdout: "-1 4 3 -1" },
    ],
  }),
];
