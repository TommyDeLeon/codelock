import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'SLIDING_WINDOW', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });
const q = (text: string) => ['Solve the stated window task.', '', '**Example**', '', '```', text, '```', '', 'The input is non-empty; the named boundary rule in the task is part of the answer.'].join('\n');
const e = (heading: string, body: string) => ['## ' + heading, '', body, '', 'The quiet mistake is treating a window that is just outside the limit as valid, which often passes ordinary examples. Keep the stated invariant true before recording an answer. Time is O(n) for the linear window scan, bounded by the input length; the frequency table or deque uses O(n) space in the worst case.'].join('\n');

export const TIER_2_SLIDING_WINDOW_B2_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'longest-positive-sum-under-cap', title:'Stay Under the Fuel Cap', difficulty:'EASY', patternTags:['sliding-window','positive-numbers','constraint'], signatureId:'fn:ints,int->int', avgSolveSeconds:540,
    promptMarkdown:q('input: 2 1 3 2\\n5\\noutput: 2') + '\n\nReturn the greatest length of a consecutive run of positive numbers whose sum is at most `k`. A single number larger than `k` cannot be used; return 0 when no run qualifies.',
    editorialMarkdown:['## Shrinking positive-sum window','','Positive values make the left endpoint safe to move: after adding a right value, repeatedly remove left values until the sum is at most the cap. No discarded longer prefix can become valid without being shorter first, so every valid ending is considered. The quiet mistake is keeping an over-cap prefix, which seems harmless but makes the recorded length illegal. Time is O(n), bounded by the input length; space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let l=0,s=0,z=0;for(let r=0;r<a.length;r++){s+=a[r];while(s>b)s-=a[l++];z=Math.max(z,r-l+1)}return z;}','function solve(a:number[],b:number):number{let l=0,s=0,z=0;for(let r=0;r<a.length;r++){s+=a[r];while(s>b)s-=a[l++];z=Math.max(z,r-l+1)}return z;}','def solve(a, b):\n    left = total = best = 0\n    for right, x in enumerate(a):\n        total += x\n        while total > b: total -= a[left]; left += 1\n        best = max(best, right-left+1)\n    return best','    static int solve(int[] a,int b){int l=0,s=0,z=0;for(int r=0;r<a.length;r++){s+=a[r];while(s>b)s-=a[l++];z=Math.max(z,r-l+1);}return z;}','#include <vector>\n#include <algorithm>\nusing namespace std; int solve(vector<int>a,int b){int l=0,s=0,z=0;for(int r=0;r<(int)a.size();r++){s+=a[r];while(s>b)s-=a[l++];z=max(z,r-l+1);}return z;}','func solve(a []int,b int) int {l,s,z:=0,0,0;for r,x:=range a{s+=x;for s>b{s-=a[l];l++};if r-l+1>z{z=r-l+1}};return z}'),
    tests:[{stdin:'2 1 3 2\n5',expectedStdout:'2',isSample:true},{stdin:'6 1\n5',expectedStdout:'1',isSample:true},{stdin:'7\n3',expectedStdout:'0'},{stdin:'1 1 1\n3',expectedStdout:'3'}] }),
  p({ ...base, slug:'most-even-values-in-fixed-block', title:'Most Even Tiles in a Block', difficulty:'EASY', patternTags:['sliding-window','fixed-window','counting'], signatureId:'fn:ints,int->int', avgSolveSeconds:480,
    promptMarkdown:q('input: 1 4 6 3 8\\n3\\noutput: 2') + '\n\nReturn the largest number of even values in any consecutive block of exactly `k` numbers. `1 <= k <= n`; overlapping blocks are separate candidates.', editorialMarkdown:['## Rolling fixed-size count','','Count evens in the first block, then slide one position at a time. Only the outgoing and incoming values can change the answer, so rebuilding each count is unnecessary. The quiet mistake is subtracting before the first full block, which loses a real value. Time is O(n), bounded by the list length; space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let c=0,z=0;for(let i=0;i<a.length;i++){if(a[i]%2===0)c++;if(i>=b&&a[i-b]%2===0)c--;if(i>=b-1)z=Math.max(z,c)}return z;}','function solve(a:number[],b:number):number{let c=0,z=0;for(let i=0;i<a.length;i++){if(a[i]%2===0)c++;if(i>=b&&a[i-b]%2===0)c--;if(i>=b-1)z=Math.max(z,c)}return z;}','def solve(a, b):\n    count = best = 0\n    for i, x in enumerate(a):\n        count += x % 2 == 0\n        if i >= b: count -= a[i-b] % 2 == 0\n        if i >= b-1: best = max(best, count)\n    return best','    static int solve(int[]a,int b){int c=0,z=0;for(int i=0;i<a.length;i++){if(a[i]%2==0)c++;if(i>=b&&a[i-b]%2==0)c--;if(i>=b-1)z=Math.max(z,c);}return z;}','#include <vector>\n#include <algorithm>\nusing namespace std;int solve(vector<int>a,int b){int c=0,z=0;for(int i=0;i<(int)a.size();i++){if(a[i]%2==0)c++;if(i>=b&&a[i-b]%2==0)c--;if(i>=b-1)z=max(z,c);}return z;}','func solve(a []int,b int)int{c,z:=0,0;for i,x:=range a{if x%2==0{c++};if i>=b&&a[i-b]%2==0{c--};if i>=b-1&&c>z{z=c}};return z}'),
    tests:[{stdin:'1 4 6 3 8\n3',expectedStdout:'2',isSample:true},{stdin:'2 4 6\n2',expectedStdout:'2',isSample:true},{stdin:'1 3 5\n2',expectedStdout:'0'},{stdin:'7\n1',expectedStdout:'0'}] }),
  p({ ...base, slug:'earliest-heaviest-fixed-block', title:'First Heaviest Fixed Block', difficulty:'EASY', patternTags:['sliding-window','fixed-window','tie-break'], signatureId:'fn:ints,int->int', avgSolveSeconds:540,
    promptMarkdown:q('input: 2 4 1 5 1\\n2\\noutput: 0') + '\n\nReturn the zero-based starting position of the length-`k` block with greatest sum. If sums tie, return the smaller starting position.', editorialMarkdown:['## Fixed-width sum with a stable tie','','Keep the first block sum, then exchange one outgoing value for one incoming value as the block advances. Update only on a strictly greater sum, which preserves the first start on a tie. The quiet mistake is updating on equality and silently choosing a later block. Time is O(n), bounded by the list length; space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let s=0;for(let i=0;i<b;i++)s+=a[i];let z=0;for(let i=b;i<a.length;i++){s+=a[i]-a[i-b];if(s>a.slice(z,z+b).reduce((x,y)=>x+y,0))z=i-b+1}return z;}','function solve(a:number[],b:number):number{let s=0,best=0,z=0;for(let i=0;i<b;i++)s+=a[i];best=s;for(let i=b;i<a.length;i++){s+=a[i]-a[i-b];if(s>best){best=s;z=i-b+1}}return z;}','def solve(a, b):\n    total = sum(a[:b]); best = total; answer = 0\n    for i in range(b, len(a)):\n        total += a[i] - a[i-b]\n        if total > best: best, answer = total, i-b+1\n    return answer','    static int solve(int[]a,int b){int s=0,best,z=0;for(int i=0;i<b;i++)s+=a[i];best=s;for(int i=b;i<a.length;i++){s+=a[i]-a[i-b];if(s>best){best=s;z=i-b+1;}}return z;}','#include <vector>\nusing namespace std;int solve(vector<int>a,int b){int s=0,best,z=0;for(int i=0;i<b;i++)s+=a[i];best=s;for(int i=b;i<(int)a.size();i++){s+=a[i]-a[i-b];if(s>best){best=s;z=i-b+1;}}return z;}','func solve(a []int,b int)int{s:=0;for i:=0;i<b;i++{s+=a[i]};best,z:=s,0;for i:=b;i<len(a);i++{s+=a[i]-a[i-b];if s>best{best=s;z=i-b+1}};return z}'),
    tests:[{stdin:'2 4 1 5 1\n2',expectedStdout:'0',isSample:true},{stdin:'1 2 9 1\n2',expectedStdout:'1',isSample:true},{stdin:'3 3 3\n2',expectedStdout:'0'},{stdin:'-5 -2\n1',expectedStdout:'1'}] }),
  p({ ...base, slug:'count-products-below-limit', title:'Small Positive Products', difficulty:'MEDIUM', patternTags:['sliding-window','product','counting'], signatureId:'fn:ints,int->int', avgSolveSeconds:720,
    promptMarkdown:q('input: 10 5 2 6\\n100\\noutput: 8') + '\n\nAll values are positive. Count consecutive non-empty runs whose product is strictly less than `k`. If `k <= 1`, the answer is 0.', editorialMarkdown:['## Multiplicative sliding window','','With positive factors, extending right only increases a product. Shrink from the left until it is legal; then every suffix ending at right is legal, contributing its window length. The quiet mistake is accepting product equal to the limit despite the strict rule. Time is O(n), bounded by the input count; space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){if(b<=1)return 0;let l=0,p=1,z=0;for(let r=0;r<a.length;r++){p*=a[r];while(p>=b)p/=a[l++];z+=r-l+1}return z;}','function solve(a:number[],b:number):number{if(b<=1)return 0;let l=0,p=1,z=0;for(let r=0;r<a.length;r++){p*=a[r];while(p>=b)p/=a[l++];z+=r-l+1}return z;}','def solve(a, b):\n    if b <= 1: return 0\n    left = 0; product = 1; count = 0\n    for right, x in enumerate(a):\n        product *= x\n        while product >= b: product //= a[left]; left += 1\n        count += right-left+1\n    return count','    static int solve(int[]a,int b){if(b<=1)return 0;int l=0,p=1,z=0;for(int r=0;r<a.length;r++){p*=a[r];while(p>=b)p/=a[l++];z+=r-l+1;}return z;}','#include <vector>\nusing namespace std;int solve(vector<int>a,int b){if(b<=1)return 0;int l=0,p=1,z=0;for(int r=0;r<(int)a.size();r++){p*=a[r];while(p>=b)p/=a[l++];z+=r-l+1;}return z;}','func solve(a []int,b int)int{if b<=1{return 0};l,p,z:=0,1,0;for r,x:=range a{p*=x;for p>=b{p/=a[l];l++};z+=r-l+1};return z}'),
    tests:[{stdin:'10 5 2 6\n100',expectedStdout:'8',isSample:true},{stdin:'1 1 1\n2',expectedStdout:'6',isSample:true},{stdin:'2 3\n1',expectedStdout:'0'},{stdin:'5\n6',expectedStdout:'1'}] }),
  p({ ...base, slug:'longest-run-with-k-odd-values', title:'Carry Only So Many Odd Stones', difficulty:'MEDIUM', patternTags:['sliding-window','parity','constraint'], signatureId:'fn:ints,int->int', avgSolveSeconds:660,
    promptMarkdown:q('input: 2 1 4 3 6 5\\n2\\noutput: 5') + '\n\nReturn the longest consecutive run containing at most `k` odd numbers. `k` may be zero, so a run then must contain only evens.', editorialMarkdown:['## Budgeted parity window','','Treat each odd value as spending one unit of budget. Expand right, and while the odd count exceeds the budget, remove values from the left until the invariant is restored. The quiet mistake is forgetting that negative odd values are odd too. Time is O(n), bounded by the list; space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let l=0,c=0,z=0;for(let r=0;r<a.length;r++){c+=a[r]&1;while(c>b)c-=a[l++]&1;z=Math.max(z,r-l+1)}return z;}','function solve(a:number[],b:number):number{let l=0,c=0,z=0;for(let r=0;r<a.length;r++){c+=Math.abs(a[r])%2;while(c>b)c-=Math.abs(a[l++])%2;z=Math.max(z,r-l+1)}return z;}','def solve(a, b):\n    left = odds = best = 0\n    for right, x in enumerate(a):\n        odds += abs(x) % 2\n        while odds > b: odds -= abs(a[left]) % 2; left += 1\n        best = max(best, right-left+1)\n    return best','    static int solve(int[]a,int b){int l=0,c=0,z=0;for(int r=0;r<a.length;r++){c+=Math.abs(a[r])%2;while(c>b)c-=Math.abs(a[l++])%2;z=Math.max(z,r-l+1);}return z;}','#include <vector>\n#include <algorithm>\nusing namespace std;int solve(vector<int>a,int b){int l=0,c=0,z=0;for(int r=0;r<(int)a.size();r++){c+=abs(a[r])%2;while(c>b)c-=abs(a[l++])%2;z=max(z,r-l+1);}return z;}','func solve(a []int,b int)int{l,c,z:=0,0,0;for r,x:=range a{if x%2!=0{c++};for c>b{if a[l]%2!=0{c--};l++};if r-l+1>z{z=r-l+1}};return z}'),
    tests:[{stdin:'2 1 4 3 6 5\n2',expectedStdout:'5',isSample:true},{stdin:'2 4 6\n0',expectedStdout:'3',isSample:true},{stdin:'1 3\n0',expectedStdout:'0'},{stdin:'-1 2 -3\n1',expectedStdout:'2'}] }),
  p({ ...base, slug:'count-text-runs-with-few-vowels', title:'Count Quiet Vowel Runs', difficulty:'MEDIUM', patternTags:['sliding-window','strings','counting'], signatureId:'fn:string,int->int', avgSolveSeconds:720,
    promptMarkdown:q('input: abca\\n1\\noutput: 9') + '\n\nCount non-empty substrings containing at most `k` vowels (`a,e,i,o,u`, lowercase only). Consonants do not spend the budget.', editorialMarkdown:['## Counting a vowel-budget window','','After reducing an over-budget window, every suffix ending at the current character has no more vowels than the whole window. Add that suffix count instead of enumerating each substring. The quiet mistake is adding one rather than all valid suffixes. Time is O(n), bounded by text length; space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let l=0,c=0,z=0,v=x=>"aeiou".includes(x);for(let r=0;r<a.length;r++){if(v(a[r]))c++;while(c>b)if(v(a[l++]))c--;z+=r-l+1}return z;}','function solve(a:string,b:number):number{let l=0,c=0,z=0;const v=(x:string)=>"aeiou".includes(x);for(let r=0;r<a.length;r++){if(v(a[r]))c++;while(c>b){if(v(a[l]))c--;l++}z+=r-l+1}return z;}','def solve(a, b):\n    left = vowels = total = 0\n    for right, x in enumerate(a):\n        vowels += x in "aeiou"\n        while vowels > b:\n            vowels -= a[left] in "aeiou"; left += 1\n        total += right-left+1\n    return total','    static int solve(String a,int b){int l=0,c=0,z=0;for(int r=0;r<a.length();r++){if("aeiou".indexOf(a.charAt(r))>=0)c++;while(c>b){if("aeiou".indexOf(a.charAt(l++))>=0)c--;}z+=r-l+1;}return z;}','#include <string>\nusing namespace std;int solve(string a,int b){int l=0,c=0,z=0;for(int r=0;r<(int)a.size();r++){if(string("aeiou").find(a[r])!=string::npos)c++;while(c>b){if(string("aeiou").find(a[l++])!=string::npos)c--;}z+=r-l+1;}return z;}','func solve(a string,b int)int{l,c,z:=0,0,0;for r:=0;r<len(a);r++{if strings.ContainsRune("aeiou",rune(a[r])){c++};for c>b{if strings.ContainsRune("aeiou",rune(a[l])){c--};l++};z+=r-l+1};return z}'),
    tests:[{stdin:'abca\n1',expectedStdout:'9',isSample:true},{stdin:'bbb\n0',expectedStdout:'6',isSample:true},{stdin:'ae\n0',expectedStdout:'0'},{stdin:'a\n1',expectedStdout:'1'}] }),
  p({ ...base, slug:'shortest-window-holding-all-labels', title:'Smallest Complete Label Span', difficulty:'MEDIUM', patternTags:['sliding-window','frequency','cover'], signatureId:'fn:ints,int->int', avgSolveSeconds:780,
    promptMarkdown:q('input: 1 3 2 1 2 3\\n3\\noutput: 3') + '\n\nValues are labels from 1 through `k`. Return the shortest consecutive run that contains every label at least once. A complete run is guaranteed.', editorialMarkdown:['## Covering frequency window','','Track how many distinct required labels are present. When all `k` are present, shrink left as far as possible while keeping each label represented; the resulting endpoint window is minimal. The quiet mistake is decrementing the distinct count while another copy remains. Time is O(n), bounded by the list; space is O(k).'].join('\n'),
    referenceSolution:six('function solve(a,b){let m={},l=0,d=0,z=a.length;for(let r=0;r<a.length;r++){m[a[r]]=(m[a[r]]||0)+1;if(m[a[r]]===1)d++;while(d===b){z=Math.min(z,r-l+1);if(--m[a[l++]]===0)d--}}return z;}','function solve(a:number[],b:number):number{const m=new Map<number,number>();let l=0,d=0,z=a.length;for(let r=0;r<a.length;r++){m.set(a[r],(m.get(a[r])||0)+1);if(m.get(a[r])===1)d++;while(d===b){z=Math.min(z,r-l+1);m.set(a[l],m.get(a[l])!-1);if(m.get(a[l])===0)d--;l++}}return z;}','def solve(a, b):\n    freq = {}; left = have = 0; best = len(a)\n    for right, x in enumerate(a):\n        freq[x] = freq.get(x, 0) + 1\n        if freq[x] == 1: have += 1\n        while have == b:\n            best = min(best, right-left+1); freq[a[left]] -= 1\n            if freq[a[left]] == 0: have -= 1\n            left += 1\n    return best','    static int solve(int[]a,int b){int[]f=new int[b+1];int l=0,d=0,z=a.length;for(int r=0;r<a.length;r++){if(++f[a[r]]==1)d++;while(d==b){z=Math.min(z,r-l+1);if(--f[a[l++]]==0)d--;}}return z;}','#include <vector>\n#include <algorithm>\nusing namespace std;int solve(vector<int>a,int b){vector<int>f(b+1);int l=0,d=0,z=a.size();for(int r=0;r<(int)a.size();r++){if(++f[a[r]]==1)d++;while(d==b){z=min(z,r-l+1);if(--f[a[l++]]==0)d--;}}return z;}','func solve(a []int,b int)int{f:=make([]int,b+1);l,d,z:=0,0,len(a);for r,x:=range a{f[x]++;if f[x]==1{d++};for d==b{if r-l+1<z{z=r-l+1};f[a[l]]--;if f[a[l]]==0{d--};l++}};return z}'),
    tests:[{stdin:'1 3 2 1 2 3\n3',expectedStdout:'3',isSample:true},{stdin:'1 1 2 3\n3',expectedStdout:'3',isSample:true},{stdin:'1\n1',expectedStdout:'1'},{stdin:'2 1 2 1\n2',expectedStdout:'2'}] }),
  p({ ...base, slug:'longest-integer-run-with-k-kinds', title:'Limited Kinds of Numbers', difficulty:'MEDIUM', patternTags:['sliding-window','hash-map','distinct'], signatureId:'fn:ints,int->int', avgSolveSeconds:780,
    promptMarkdown:q('input: 4 4 2 2 3 2\\n2\\noutput: 4') + '\n\nReturn the longest consecutive run containing at most `k` different integer values. Equal values count as one kind, including negative values.', editorialMarkdown:['## At-most-k distinct value window','','A frequency map tells whether removing a left value removes an entire kind. Expand right, then evict from left until the map has at most `k` keys; that largest legal interval is the candidate. The quiet mistake is leaving a zero-frequency key in the map. Time is O(n), bounded by the list; space is O(d).'].join('\n'),
    referenceSolution:six('function solve(a,b){let m={},l=0,z=0;for(let r=0;r<a.length;r++){m[a[r]]=(m[a[r]]||0)+1;while(Object.keys(m).length>b){if(--m[a[l]]===0)delete m[a[l]];l++}z=Math.max(z,r-l+1)}return z;}','function solve(a:number[],b:number):number{const m=new Map<number,number>();let l=0,z=0;for(let r=0;r<a.length;r++){m.set(a[r],(m.get(a[r])||0)+1);while(m.size>b){m.set(a[l],m.get(a[l])!-1);if(m.get(a[l])===0)m.delete(a[l]);l++}z=Math.max(z,r-l+1)}return z;}','def solve(a, b):\n    freq = {}; left = best = 0\n    for right, x in enumerate(a):\n        freq[x] = freq.get(x, 0) + 1\n        while len(freq) > b:\n            freq[a[left]] -= 1\n            if not freq[a[left]]: del freq[a[left]]\n            left += 1\n        best = max(best, right-left+1)\n    return best','    static int solve(int[]a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();int l=0,z=0;for(int r=0;r<a.length;r++){m.put(a[r],m.getOrDefault(a[r],0)+1);while(m.size()>b){m.put(a[l],m.get(a[l])-1);if(m.get(a[l])==0)m.remove(a[l]);l++;}z=Math.max(z,r-l+1);}return z;}','#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;int solve(vector<int>a,int b){unordered_map<int,int>m;int l=0,z=0;for(int r=0;r<(int)a.size();r++){m[a[r]]++;while((int)m.size()>b){if(--m[a[l]]==0)m.erase(a[l]);l++;}z=max(z,r-l+1);}return z;}','func solve(a []int,b int)int{m:=map[int]int{};l,z:=0,0;for r,x:=range a{m[x]++;for len(m)>b{m[a[l]]--;if m[a[l]]==0{delete(m,a[l])};l++};if r-l+1>z{z=r-l+1}};return z}'),
    tests:[{stdin:'4 4 2 2 3 2\n2',expectedStdout:'4',isSample:true},{stdin:'1 2 3\n1',expectedStdout:'1',isSample:true},{stdin:'-1 -1 -1\n2',expectedStdout:'3'},{stdin:'1 2 1 3 4\n3',expectedStdout:'4'}] }),
  p({ ...base, slug:'longest-letter-run-after-budgeted-relabels', title:'Relabel a Letter Run', difficulty:'MEDIUM', patternTags:['sliding-window','replacement-budget','frequency'], signatureId:'fn:string,int->int', avgSolveSeconds:840,
    promptMarkdown:q('input: AABABBA\\n1\\noutput: 4') + '\n\nThe uppercase line may have at most `k` characters relabeled to any uppercase letter. Return the greatest length that can become one repeated letter.', editorialMarkdown:['## Replacement-budget frequency window','','A window becomes uniform by changing every character except its most frequent letter. Maintain that maximum frequency; while window length minus it exceeds the budget, shrink left. The quiet mistake is charging changes against the wrong letter count. Time is O(n), bounded by text length; space is O(26).'].join('\n'),
    referenceSolution:six('function solve(a,b){let m={},l=0,z=0,h=0;for(let r=0;r<a.length;r++){h=Math.max(h,m[a[r]]=(m[a[r]]||0)+1);while(r-l+1-h>b)m[a[l++]]--;z=Math.max(z,r-l+1)}return z;}','function solve(a:string,b:number):number{const m=new Map<string,number>();let l=0,z=0,h=0;for(let r=0;r<a.length;r++){m.set(a[r],(m.get(a[r])||0)+1);h=Math.max(h,m.get(a[r])!);while(r-l+1-h>b){m.set(a[l],m.get(a[l])!-1);l++}z=Math.max(z,r-l+1)}return z;}','def solve(a, b):\n    freq = {}; left = best = high = 0\n    for right, x in enumerate(a):\n        freq[x] = freq.get(x, 0) + 1; high = max(high, freq[x])\n        while right-left+1-high > b: freq[a[left]] -= 1; left += 1\n        best = max(best, right-left+1)\n    return best','    static int solve(String a,int b){int[]f=new int[26];int l=0,z=0,h=0;for(int r=0;r<a.length();r++){h=Math.max(h,++f[a.charAt(r)-65]);while(r-l+1-h>b)f[a.charAt(l++)-65]--;z=Math.max(z,r-l+1);}return z;}','#include <string>\n#include <vector>\n#include <algorithm>\nusing namespace std;int solve(string a,int b){vector<int>f(26);int l=0,z=0,h=0;for(int r=0;r<(int)a.size();r++){h=max(h,++f[a[r]-65]);while(r-l+1-h>b)f[a[l++]-65]--;z=max(z,r-l+1);}return z;}','func solve(a string,b int)int{f:=make([]int,26);l,z,h:=0,0,0;for r:=0;r<len(a);r++{f[a[r]-65]++;if f[a[r]-65]>h{h=f[a[r]-65]};for r-l+1-h>b{f[a[l]-65]--;l++};if r-l+1>z{z=r-l+1}};return z}'),
    tests:[{stdin:'AABABBA\n1',expectedStdout:'4',isSample:true},{stdin:'AAAA\n0',expectedStdout:'4',isSample:true},{stdin:'ABCD\n2',expectedStdout:'3'},{stdin:'Z\n5',expectedStdout:'1'}] }),
  p({ ...base, slug:'longest-value-band-within-spread', title:'Longest Narrow Value Band', difficulty:'HARD', patternTags:['sliding-window','monotonic-queue','range'], signatureId:'fn:ints,int->int', avgSolveSeconds:960,
    promptMarkdown:q('input: 8 2 4 7\\n4\\noutput: 2') + '\n\nReturn the greatest length of a consecutive run whose largest value minus smallest value is at most `k`. Negative values are allowed.', editorialMarkdown:['## Dual monotonic deque window','','Keep decreasing candidates for the maximum and increasing candidates for the minimum. Their fronts give the current spread, so remove left positions until it fits. Expired indices leave from deque fronts. The quiet mistake is retaining an expired front. Time is O(n), because each index enters and leaves once; space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){let l=0,z=0;for(let r=0;r<a.length;r++){let mn=a[r],mx=a[r];for(let i=r;i>=l;i--){mn=Math.min(mn,a[i]);mx=Math.max(mx,a[i]);if(mx-mn>b){l=i+1;break}}z=Math.max(z,r-l+1)}return z;}','function solve(a:number[],b:number):number{let lo:number[]=[],hi:number[]=[],l=0,z=0;for(let r=0;r<a.length;r++){while(lo.length&&a[lo[lo.length-1]]>a[r])lo.pop();while(hi.length&&a[hi[hi.length-1]]<a[r])hi.pop();lo.push(r);hi.push(r);while(a[hi[0]]-a[lo[0]]>b){if(lo[0]===l)lo.shift();if(hi[0]===l)hi.shift();l++}z=Math.max(z,r-l+1)}return z;}','def solve(a, b):\n    lo = []; hi = []; left = best = 0\n    for right, x in enumerate(a):\n        while lo and a[lo[-1]] > x: lo.pop()\n        while hi and a[hi[-1]] < x: hi.pop()\n        lo.append(right); hi.append(right)\n        while a[hi[0]] - a[lo[0]] > b:\n            if lo[0] == left: lo.pop(0)\n            if hi[0] == left: hi.pop(0)\n            left += 1\n        best = max(best, right-left+1)\n    return best','    static int solve(int[]a,int b){java.util.ArrayDeque<Integer>lo=new java.util.ArrayDeque<>(),hi=new java.util.ArrayDeque<>();int l=0,z=0;for(int r=0;r<a.length;r++){while(!lo.isEmpty()&&a[lo.peekLast()]>a[r])lo.pollLast();while(!hi.isEmpty()&&a[hi.peekLast()]<a[r])hi.pollLast();lo.add(r);hi.add(r);while(a[hi.peek()]-a[lo.peek()]>b){if(lo.peek()==l)lo.poll();if(hi.peek()==l)hi.poll();l++;}z=Math.max(z,r-l+1);}return z;}','#include <vector>\n#include <deque>\n#include <algorithm>\nusing namespace std;int solve(vector<int>a,int b){deque<int>lo,hi;int l=0,z=0;for(int r=0;r<(int)a.size();r++){while(!lo.empty()&&a[lo.back()]>a[r])lo.pop_back();while(!hi.empty()&&a[hi.back()]<a[r])hi.pop_back();lo.push_back(r);hi.push_back(r);while(a[hi.front()]-a[lo.front()]>b){if(lo.front()==l)lo.pop_front();if(hi.front()==l)hi.pop_front();l++;}z=max(z,r-l+1);}return z;}','func solve(a []int,b int)int{lo,hi:=[]int{},[]int{};l,z:=0,0;for r,x:=range a{for len(lo)>0&&a[lo[len(lo)-1]]>x{lo=lo[:len(lo)-1]};for len(hi)>0&&a[hi[len(hi)-1]]<x{hi=hi[:len(hi)-1]};lo=append(lo,r);hi=append(hi,r);for a[hi[0]]-a[lo[0]]>b{if lo[0]==l{lo=lo[1:]};if hi[0]==l{hi=hi[1:]};l++};if r-l+1>z{z=r-l+1}};return z}'),
    tests:[{stdin:'8 2 4 7\n4',expectedStdout:'2',isSample:true},{stdin:'10 1 2 4 7 2\n5',expectedStdout:'4',isSample:true},{stdin:'-3 -1 -2\n2',expectedStdout:'3'},{stdin:'5\n0',expectedStdout:'1'}] }),
  p({ ...base, slug:'shortest-signed-sum-reaching-goal', title:'Shortest Signed Run to the Goal', difficulty:'HARD', patternTags:['sliding-window','prefix-sum','monotonic-queue'], signatureId:'fn:ints,int->int', avgSolveSeconds:1080,
    promptMarkdown:q('input: 2 -1 2 1\\n3\\noutput: 2') + '\n\nReturn the shortest non-empty consecutive run with sum at least `k`, or `-1` if none exists. Values may be negative, so removing a left value is not always safe.', editorialMarkdown:['## Prefix sums with an increasing deque','','Store indices of prefix sums in increasing order. When the newest prefix exceeds the front by at least the goal, pop that front and record its length. Larger earlier prefixes are dominated and leave from the back. The quiet mistake is using the positive-only shrink rule. Time is O(n), bounded by the list; space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){let z=1e9;for(let i=0;i<a.length;i++){let s=0;for(let j=i;j<a.length;j++){s+=a[j];if(s>=b){z=Math.min(z,j-i+1);break}}}return z===1e9?-1:z;}','function solve(a:number[],b:number):number{const p=[0],d:number[]=[];let z=1e9;for(let i=0;i<a.length;i++)p.push(p[i]+a[i]);for(let i=0;i<p.length;i++){while(d.length&&p[i]-p[d[0]]>=b)z=Math.min(z,i-d.shift()!);while(d.length&&p[i]<=p[d[d.length-1]])d.pop();d.push(i)}return z===1e9?-1:z;}','def solve(a, b):\n    prefix = [0]\n    for x in a: prefix.append(prefix[-1] + x)\n    deck = []; best = len(a)+1\n    for i, value in enumerate(prefix):\n        while deck and value-prefix[deck[0]] >= b: best = min(best, i-deck.pop(0))\n        while deck and value <= prefix[deck[-1]]: deck.pop()\n        deck.append(i)\n    return best if best <= len(a) else -1','    static int solve(int[]a,int b){long[]p=new long[a.length+1];for(int i=0;i<a.length;i++)p[i+1]=p[i]+a[i];java.util.ArrayDeque<Integer>d=new java.util.ArrayDeque<>();int z=a.length+1;for(int i=0;i<p.length;i++){while(!d.isEmpty()&&p[i]-p[d.peek()]>=b)z=Math.min(z,i-d.poll());while(!d.isEmpty()&&p[i]<=p[d.peekLast()])d.pollLast();d.add(i);}return z>a.length?-1:z;}','#include <vector>\n#include <deque>\n#include <algorithm>\nusing namespace std;int solve(vector<int>a,int b){vector<long long>p(a.size()+1);for(int i=0;i<(int)a.size();i++)p[i+1]=p[i]+a[i];deque<int>d;int z=a.size()+1;for(int i=0;i<(int)p.size();i++){while(!d.empty()&&p[i]-p[d.front()]>=b){z=min(z,i-d.front());d.pop_front();}while(!d.empty()&&p[i]<=p[d.back()])d.pop_back();d.push_back(i);}return z>(int)a.size()?-1:z;}','func solve(a []int,b int)int{p:=make([]int,len(a)+1);for i,x:=range a{p[i+1]=p[i]+x};d:=[]int{};z:=len(a)+1;for i,x:=range p{for len(d)>0&&x-p[d[0]]>=b{if i-d[0]<z{z=i-d[0]};d=d[1:]};for len(d)>0&&x<=p[d[len(d)-1]]{d=d[:len(d)-1]};d=append(d,i)};if z>len(a){return -1};return z}'),
    tests:[{stdin:'2 -1 2 1\n3',expectedStdout:'2',isSample:true},{stdin:'1 2\n4',expectedStdout:'-1',isSample:true},{stdin:'1 -1 5 -2 3\n3',expectedStdout:'1'},{stdin:'-2 -1\n-1',expectedStdout:'1'}] }),
  p({ ...base, slug:'count-stable-spread-runs', title:'Count Stable Spread Runs', difficulty:'HARD', patternTags:['sliding-window','monotonic-queue','counting'], signatureId:'fn:ints,int->int', avgSolveSeconds:1020,
    promptMarkdown:q('input: 1 3 2\\n1\\noutput: 4') + '\n\nCount all non-empty consecutive runs where maximum minus minimum is at most `k`. The answer counts overlapping runs separately.', editorialMarkdown:['## Deque window counting by spread','','Maintain minimum and maximum deques while extending right. Once the spread fits after left removals, every suffix ending at right also fits, so add the current window length to the count. The quiet mistake is counting only the full window. Time is O(n), bounded by the list; space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){let z=0;for(let i=0;i<a.length;i++){let x=a[i],y=a[i];for(let j=i;j<a.length;j++){x=Math.min(x,a[j]);y=Math.max(y,a[j]);if(y-x<=b)z++}}return z;}','function solve(a:number[],b:number):number{let lo:number[]=[],hi:number[]=[],l=0,z=0;for(let r=0;r<a.length;r++){while(lo.length&&a[lo[lo.length-1]]>a[r])lo.pop();while(hi.length&&a[hi[hi.length-1]]<a[r])hi.pop();lo.push(r);hi.push(r);while(a[hi[0]]-a[lo[0]]>b){if(lo[0]===l)lo.shift();if(hi[0]===l)hi.shift();l++}z+=r-l+1}return z;}','def solve(a, b):\n    lo = []; hi = []; left = total = 0\n    for right, x in enumerate(a):\n        while lo and a[lo[-1]] > x: lo.pop()\n        while hi and a[hi[-1]] < x: hi.pop()\n        lo.append(right); hi.append(right)\n        while a[hi[0]]-a[lo[0]] > b:\n            if lo[0] == left: lo.pop(0)\n            if hi[0] == left: hi.pop(0)\n            left += 1\n        total += right-left+1\n    return total','    static int solve(int[]a,int b){java.util.ArrayDeque<Integer>lo=new java.util.ArrayDeque<>(),hi=new java.util.ArrayDeque<>();int l=0,z=0;for(int r=0;r<a.length;r++){while(!lo.isEmpty()&&a[lo.peekLast()]>a[r])lo.pollLast();while(!hi.isEmpty()&&a[hi.peekLast()]<a[r])hi.pollLast();lo.add(r);hi.add(r);while(a[hi.peek()]-a[lo.peek()]>b){if(lo.peek()==l)lo.poll();if(hi.peek()==l)hi.poll();l++;}z+=r-l+1;}return z;}','#include <vector>\n#include <deque>\nusing namespace std;int solve(vector<int>a,int b){deque<int>lo,hi;int l=0,z=0;for(int r=0;r<(int)a.size();r++){while(!lo.empty()&&a[lo.back()]>a[r])lo.pop_back();while(!hi.empty()&&a[hi.back()]<a[r])hi.pop_back();lo.push_back(r);hi.push_back(r);while(a[hi.front()]-a[lo.front()]>b){if(lo.front()==l)lo.pop_front();if(hi.front()==l)hi.pop_front();l++;}z+=r-l+1;}return z;}','func solve(a []int,b int)int{lo,hi:=[]int{},[]int{};l,z:=0,0;for r,x:=range a{for len(lo)>0&&a[lo[len(lo)-1]]>x{lo=lo[:len(lo)-1]};for len(hi)>0&&a[hi[len(hi)-1]]<x{hi=hi[:len(hi)-1]};lo=append(lo,r);hi=append(hi,r);for a[hi[0]]-a[lo[0]]>b{if lo[0]==l{lo=lo[1:]};if hi[0]==l{hi=hi[1:]};l++};z+=r-l+1};return z}'),
    tests:[{stdin:'1 3 2\n1',expectedStdout:'4',isSample:true},{stdin:'2 2\n0',expectedStdout:'3',isSample:true},{stdin:'1 5\n3',expectedStdout:'2'},{stdin:'-2 -1 0\n2',expectedStdout:'6'}] }),
];
