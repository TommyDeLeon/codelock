import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_3', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_3_PREFIX_SUMS_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'count-positive-prefixes', title:'Positive Totals So Far', difficulty:'EASY', patternTags:['prefix-sum','counting'], signatureId:'fn:ints->int', avgSolveSeconds:540,
    promptMarkdown:['Count positions whose prefix sum, including that position, is strictly positive.','', '**Example**','', '```','input: 2 -3 4 1','output: 3','```','', 'The prefix totals are 2, -1, 3, and 4. The list has at least one value; a prefix total of exactly zero does not count.'].join('\n'),
    editorialMarkdown:['## Prefix sign counter','','Carry one running sum. After adding each value, that sum is precisely the prefix ending at the current position, so its sign answers the question without rebuilding the prefix. Count it only when it is greater than zero.','','The quiet mistake is using a nonnegative comparison, which incorrectly includes zero-total prefixes. Negative values can pull a formerly positive total below zero, so testing each prefix independently matters. Time is O(n), bounded by list length, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let s=0,c=0;for(const x of a){s+=x;if(s>0)c++;}return c;}','function solve(a:number[]):number{let s=0,c=0;for(const x of a){s+=x;if(s>0)c++;}return c;}','def solve(a):\n    s=c=0\n    for x in a:\n        s+=x\n        if s>0:c+=1\n    return c','    static int solve(int[] a) { int s=0,c=0;for(int x:a){s+=x;if(s>0)c++;}return c; }','int solve(vector<int>a){int s=0,c=0;for(int x:a){s+=x;if(s>0)c++;}return c;}','func solve(a []int) int {s,c:=0,0;for _,x:=range a{s+=x;if s>0{c++}};return c}'),
    tests:[{stdin:'2 -3 4 1',expectedStdout:'3',isSample:true},{stdin:'-1 1 0',expectedStdout:'0',isSample:true},{stdin:'5',expectedStdout:'1'},{stdin:'0 1 -1 2',expectedStdout:'2'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'first-prefix-at-least-target', title:'First Total That Reaches the Mark', difficulty:'EASY', patternTags:['prefix-sum','search'], signatureId:'fn:ints,int->int', avgSolveSeconds:540,
    promptMarkdown:['Return the first index where the running sum is at least target. Return -1 when no prefix reaches it.','', '**Example**','', '```','input: 1 2 -1 4','4','output: 3','```','', 'The list has at least one value and may contain negatives. The named edge case is a target that is never reached.'].join('\n'),
    editorialMarkdown:['## One-pass threshold check','','The running sum represents each prefix in order. Test it immediately after adding the current value; the first passing position is automatically the smallest index required by the contract. A later negative value cannot change that already-earned answer.','','The quiet mistake is assuming values are positive and stopping logic before adding the current value. That shifts the returned index and fails when one value crosses the threshold. Time is O(n), bounded by the input length, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let s=0;for(let i=0;i<a.length;i++){s+=a[i];if(s>=b)return i;}return -1;}','function solve(a:number[],b:number):number{let s=0;for(let i=0;i<a.length;i++){s+=a[i];if(s>=b)return i;}return -1;}','def solve(a,b):\n    s=0\n    for i,x in enumerate(a):\n        s+=x\n        if s>=b:return i\n    return -1','    static int solve(int[] a,int b) { int s=0;for(int i=0;i<a.length;i++){s+=a[i];if(s>=b)return i;}return -1; }','int solve(vector<int>a,int b){int s=0;for(int i=0;i<(int)a.size();i++){s+=a[i];if(s>=b)return i;}return -1;}','func solve(a []int,b int) int {s:=0;for i,x:=range a{s+=x;if s>=b{return i}};return -1}'),
    tests:[{stdin:'1 2 -1 4\n4',expectedStdout:'3',isSample:true},{stdin:'-2 5\n3',expectedStdout:'1',isSample:true},{stdin:'1 1\n5',expectedStdout:'-1'},{stdin:'7\n7',expectedStdout:'0'}] }),
  p({ ...base, patternFamily:'GREEDY', slug:'largest-rise-from-earlier-total', title:'Largest Rise Between Prefix Totals', difficulty:'EASY', patternTags:['prefix-sum','running-minimum'], signatureId:'fn:ints->int', avgSolveSeconds:600,
    promptMarkdown:['Build prefix totals with an initial total of zero. Return the largest increase from an earlier prefix total to a later prefix total; returning zero is allowed when totals never rise.','', '**Example**','', '```','input: -2 5 -1 4','output: 8','```','', 'The rise is from prefix -2 to prefix 6. The list is non-empty; all falling totals return 0.'].join('\n'),
    editorialMarkdown:['## Running minimum before each total','','A later prefix makes its best possible rise by subtracting the smallest prefix seen before it. Keep that minimum alongside the running total and update the answer before allowing the new total to become the minimum. The initial zero is a real earlier prefix.','','The quiet mistake is omitting the initial zero, which misses rises beginning at the first value. Updating the minimum first also allows a prefix to subtract itself. Time is O(n), bounded by list length, with O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a){let s=0,m=0,b=0;for(const x of a){s+=x;b=Math.max(b,s-m);m=Math.min(m,s);}return b;}','function solve(a:number[]):number{let s=0,m=0,b=0;for(const x of a){s+=x;b=Math.max(b,s-m);m=Math.min(m,s);}return b;}','def solve(a):\n    s=m=best=0\n    for x in a:\n        s+=x; best=max(best,s-m); m=min(m,s)\n    return best','    static int solve(int[] a) { int s=0,m=0,b=0;for(int x:a){s+=x;b=Math.max(b,s-m);m=Math.min(m,s);}return b; }','int solve(vector<int>a){int s=0,m=0,b=0;for(int x:a){s+=x;b=max(b,s-m);m=min(m,s);}return b;}','func solve(a []int) int {s,m,b:=0,0,0;for _,x:=range a{s+=x;if s-m>b{b=s-m};if s<m{m=s}};return b}'),
    tests:[{stdin:'-2 5 -1 4',expectedStdout:'8',isSample:true},{stdin:'3 -1 2',expectedStdout:'4',isSample:true},{stdin:'-3 -2',expectedStdout:'0'},{stdin:'5',expectedStdout:'5'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'longest-zero-total-block', title:'Longest Block With Total Zero', difficulty:'MEDIUM', patternTags:['prefix-sum','hash-map'], signatureId:'fn:ints->int', avgSolveSeconds:780,
    promptMarkdown:['Return the greatest length of a contiguous non-empty block whose values sum to zero. Return 0 if none exists.','', '**Example**','', '```','input: 1 2 -3 3 -1 -2','output: 6','```','', 'The whole list sums to zero. The list is non-empty and may contain negative values.'].join('\n'),
    editorialMarkdown:['## First occurrence of each prefix','','Equal prefix totals at indices i and j mean the values between them cancel to zero. Store only the first index for each total, because it gives every later repeat the longest possible block. Seed total zero at virtual index -1 for blocks beginning at index zero.','','The quiet mistake is overwriting the first index on a repeat, which silently shortens future candidates. Forgetting the virtual prefix misses a zero-total beginning. Time is O(n) expected from hashing, bounded by list length, and space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let m=new Map([[0,-1]]),s=0,b=0;for(let i=0;i<a.length;i++){s+=a[i];if(m.has(s))b=Math.max(b,i-m.get(s));else m.set(s,i);}return b;}','function solve(a:number[]):number{const m=new Map<number,number>([[0,-1]]);let s=0,b=0;for(let i=0;i<a.length;i++){s+=a[i];if(m.has(s))b=Math.max(b,i-m.get(s)!);else m.set(s,i);}return b;}','def solve(a):\n    m={0:-1};s=best=0\n    for i,x in enumerate(a):\n        s+=x\n        if s in m:best=max(best,i-m[s])\n        else:m[s]=i\n    return best','    static int solve(int[] a) { java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();m.put(0,-1);int s=0,b=0;for(int i=0;i<a.length;i++){s+=a[i];if(m.containsKey(s))b=Math.max(b,i-m.get(s));else m.put(s,i);}return b; }','int solve(vector<int>a){unordered_map<int,int>m;m[0]=-1;int s=0,b=0;for(int i=0;i<(int)a.size();i++){s+=a[i];if(m.count(s))b=max(b,i-m[s]);else m[s]=i;}return b;}','func solve(a []int) int {m:=map[int]int{0:-1};s,b:=0,0;for i,x:=range a{s+=x;if j,ok:=m[s];ok{if i-j>b{b=i-j}}else{m[s]=i}};return b}'),
    tests:[{stdin:'1 2 -3 3 -1 -2',expectedStdout:'6',isSample:true},{stdin:'1 -1 2',expectedStdout:'2',isSample:true},{stdin:'1 2',expectedStdout:'0'},{stdin:'0',expectedStdout:'1'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'count-zero-total-blocks', title:'How Many Blocks Cancel?', difficulty:'MEDIUM', patternTags:['prefix-sum','frequency'], signatureId:'fn:ints->int', avgSolveSeconds:780,
    promptMarkdown:['Count contiguous non-empty blocks whose sum is zero. Blocks with different endpoints count separately.','', '**Example**','', '```','input: 1 -1 1 -1','output: 4','```','', 'The list is non-empty and may contain negatives. A single zero is one qualifying block.'].join('\n'),
    editorialMarkdown:['## Prefix frequency counting','','A zero-total block appears whenever the current prefix total has appeared before. Each earlier occurrence contributes one distinct start position, so add its stored frequency and then record this prefix. The one empty prefix with total zero enables blocks starting at position zero.','','The quiet mistake is storing only whether a total was seen; repeated prefixes create multiple valid blocks and need their full count. Recording before counting also creates an invalid empty block. Time is O(n) expected, bounded by list length, and extra space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let m=new Map([[0,1]]),s=0,c=0;for(const x of a){s+=x;c+=m.get(s)||0;m.set(s,(m.get(s)||0)+1);}return c;}','function solve(a:number[]):number{const m=new Map<number,number>([[0,1]]);let s=0,c=0;for(const x of a){s+=x;c+=m.get(s)||0;m.set(s,(m.get(s)||0)+1);}return c;}','def solve(a):\n    m={0:1};s=c=0\n    for x in a:\n        s+=x;c+=m.get(s,0);m[s]=m.get(s,0)+1\n    return c','    static int solve(int[] a) { java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();m.put(0,1);int s=0,c=0;for(int x:a){s+=x;c+=m.getOrDefault(s,0);m.put(s,m.getOrDefault(s,0)+1);}return c; }','int solve(vector<int>a){unordered_map<int,int>m;m[0]=1;int s=0,c=0;for(int x:a){s+=x;c+=m[s];m[s]++;}return c;}','func solve(a []int) int {m:=map[int]int{0:1};s,c:=0,0;for _,x:=range a{s+=x;c+=m[s];m[s]++};return c}'),
    tests:[{stdin:'1 -1 1 -1',expectedStdout:'4',isSample:true},{stdin:'0 0',expectedStdout:'3',isSample:true},{stdin:'1 2',expectedStdout:'0'},{stdin:'2 -2 3 -3',expectedStdout:'3'}] }),
  p({ ...base, patternFamily:'GREEDY', slug:'smallest-prefix-total', title:'Lowest Running Balance', difficulty:'MEDIUM', patternTags:['prefix-sum','running-minimum'], signatureId:'fn:ints->int', avgSolveSeconds:600,
    promptMarkdown:['Return the smallest prefix sum after reading one or more values. The empty prefix is not considered.','', '**Example**','', '```','input: 4 -7 2 -1','output: -3','```','', 'The totals are 4, -3, -1, and -2. The non-empty list may contain negative values.'].join('\n'),
    editorialMarkdown:['## Running aggregate minimum','','Every requested candidate is a running sum, so update that sum once per value and compare it to the best minimum so far. No prefix needs to be saved because only its smallest value is requested. Initializing from the first computed prefix keeps the empty prefix out.','','The quiet mistake is initializing the answer to zero, which returns zero incorrectly when every prefix is positive. Another is comparing before adding, which includes the forbidden empty prefix. Time is O(n), bounded by the list length, with O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a){let s=0,m=a[0];for(const x of a){s+=x;m=Math.min(m,s);}return m;}','function solve(a:number[]):number{let s=0,m=a[0];for(const x of a){s+=x;m=Math.min(m,s);}return m;}','def solve(a):\n    s=0;m=a[0]\n    for x in a:s+=x;m=min(m,s)\n    return m','    static int solve(int[] a) { int s=0,m=a[0];for(int x:a){s+=x;m=Math.min(m,s);}return m; }','int solve(vector<int>a){int s=0,m=a[0];for(int x:a){s+=x;m=min(m,s);}return m;}','func solve(a []int) int {s,m:=0,a[0];for _,x:=range a{s+=x;if s<m{m=s}};return m}'),
    tests:[{stdin:'4 -7 2 -1',expectedStdout:'-3',isSample:true},{stdin:'2 3',expectedStdout:'2',isSample:true},{stdin:'-5',expectedStdout:'-5'},{stdin:'-1 2 -4',expectedStdout:'-3'}] }),
  p({ ...base, patternFamily:'GREEDY', slug:'count-new-prefix-highs', title:'New Record Totals', difficulty:'MEDIUM', patternTags:['prefix-sum','running-maximum'], signatureId:'fn:ints->int', avgSolveSeconds:600,
    promptMarkdown:['Count prefixes whose sum is strictly greater than every earlier non-empty prefix sum.','', '**Example**','', '```','input: 2 -1 3 -4 5','output: 3','```','', 'The record totals are 2, 4, and 5. The list has at least one value; equal totals are not new records.'].join('\n'),
    editorialMarkdown:['## Running maximum of prefix totals','','Maintain the current prefix total and the largest earlier prefix total. A total is a record exactly when it exceeds that maximum, after which it becomes the new maximum. The first prefix always counts because it has no earlier non-empty competitor.','','The quiet mistake is using greater-than-or-equal, which turns tied totals into false records. Starting the maximum at zero also fails when the first total is negative. Time is O(n), bounded by list length, and auxiliary space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let s=0,m=-Infinity,c=0;for(const x of a){s+=x;if(s>m){c++;m=s;}}return c;}','function solve(a:number[]):number{let s=0,m=-Infinity,c=0;for(const x of a){s+=x;if(s>m){c++;m=s;}}return c;}','def solve(a):\n    s=0;m=None;c=0\n    for x in a:\n        s+=x\n        if m is None or s>m:c+=1;m=s\n    return c','    static int solve(int[] a) { int s=0,m=Integer.MIN_VALUE,c=0;for(int x:a){s+=x;if(s>m){c++;m=s;}}return c; }','int solve(vector<int>a){int s=0,m=-2147483647,c=0;for(int x:a){s+=x;if(s>m){c++;m=s;}}return c;}','func solve(a []int) int {s,m,c:=0,a[0]-1,0;for _,x:=range a{s+=x;if s>m{c++;m=s}};return c}'),
    tests:[{stdin:'2 -1 3 -4 5',expectedStdout:'3',isSample:true},{stdin:'-2 -1',expectedStdout:'1',isSample:true},{stdin:'1 -1 1',expectedStdout:'1'},{stdin:'7',expectedStdout:'1'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'count-balanced-parity-blocks', title:'Even and Odd in Balance', difficulty:'MEDIUM', patternTags:['prefix-sum','parity','frequency'], signatureId:'fn:ints->int', avgSolveSeconds:780,
    promptMarkdown:['Count contiguous non-empty blocks containing equally many even and odd values. Negative values use ordinary parity.','', '**Example**','', '```','input: 2 1 4 3','output: 4','```','', 'The list is non-empty. Different endpoint pairs are different blocks, and a block of one value is never balanced.'].join('\n'),
    editorialMarkdown:['## Prefix balance frequencies','','Treat an even value as +1 and an odd value as -1. A block has equal counts exactly when its two boundary prefix balances are equal. Count earlier occurrences of each balance just as with zero-sum blocks, beginning with balance zero once.','','The quiet mistake is testing numeric sums instead of parity counts; equal numeric totals say nothing about the number of evens and odds. In languages where negative remainder is awkward, use x modulo 2 only to test equality with zero. Time is O(n) expected and space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let m=new Map([[0,1]]),s=0,c=0;for(const x of a){s+=x%2===0?1:-1;c+=m.get(s)||0;m.set(s,(m.get(s)||0)+1);}return c;}','function solve(a:number[]):number{const m=new Map<number,number>([[0,1]]);let s=0,c=0;for(const x of a){s+=x%2===0?1:-1;c+=m.get(s)||0;m.set(s,(m.get(s)||0)+1);}return c;}','def solve(a):\n    m={0:1};s=c=0\n    for x in a:\n        s+=1 if x%2==0 else -1;c+=m.get(s,0);m[s]=m.get(s,0)+1\n    return c','    static int solve(int[] a) { java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();m.put(0,1);int s=0,c=0;for(int x:a){s+=x%2==0?1:-1;c+=m.getOrDefault(s,0);m.put(s,m.getOrDefault(s,0)+1);}return c; }','int solve(vector<int>a){unordered_map<int,int>m;m[0]=1;int s=0,c=0;for(int x:a){s+=x%2==0?1:-1;c+=m[s];m[s]++;}return c;}','func solve(a []int) int {m:=map[int]int{0:1};s,c:=0,0;for _,x:=range a{if x%2==0{s++}else{s--};c+=m[s];m[s]++};return c}'),
    tests:[{stdin:'2 1 4 3',expectedStdout:'4',isSample:true},{stdin:'1 2',expectedStdout:'1',isSample:true},{stdin:'2 4 6',expectedStdout:'0'},{stdin:'-1 -2 3 4',expectedStdout:'4'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'shortest-block-reaching-target', title:'Shortest Exact Total Block', difficulty:'HARD', patternTags:['prefix-sum','hash-map','optimization'], signatureId:'fn:ints,int->int', avgSolveSeconds:960,
    promptMarkdown:['Return the shortest length of a contiguous non-empty block whose sum equals target. Return -1 when no such block exists.','', '**Example**','', '```','input: 2 -1 2 3 -2','3','output: 1','```','', 'The one-value block 3 is shortest. The list is non-empty and may contain negatives, so a sliding window is not reliable.'].join('\n'),
    editorialMarkdown:['## Latest prefix index for a shortest match','','For a block ending at i, its left boundary needs a prior prefix equal to current minus target. To make that block shortest, retain the latest index for each prefix total, unlike the longest-block variant. Check before storing the current prefix, then replace its index.','','The quiet mistake is retaining the first index out of habit; that returns the longest qualifying block instead. Forgetting the virtual zero prefix loses blocks starting at zero. Time is O(n) expected from hashing, bounded by list length, and space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a,b){let m=new Map([[0,-1]]),s=0,z=1e9;for(let i=0;i<a.length;i++){s+=a[i];if(m.has(s-b))z=Math.min(z,i-m.get(s-b));m.set(s,i);}return z===1e9?-1:z;}','function solve(a:number[],b:number):number{const m=new Map<number,number>([[0,-1]]);let s=0,z=1e9;for(let i=0;i<a.length;i++){s+=a[i];if(m.has(s-b))z=Math.min(z,i-m.get(s-b)!);m.set(s,i);}return z===1e9?-1:z;}','def solve(a,b):\n    m={0:-1};s=0;best=10**9\n    for i,x in enumerate(a):\n        s+=x\n        if s-b in m:best=min(best,i-m[s-b])\n        m[s]=i\n    return -1 if best==10**9 else best','    static int solve(int[] a,int b) { java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();m.put(0,-1);int s=0,z=1000000000;for(int i=0;i<a.length;i++){s+=a[i];if(m.containsKey(s-b))z=Math.min(z,i-m.get(s-b));m.put(s,i);}return z==1000000000?-1:z; }','int solve(vector<int>a,int b){unordered_map<int,int>m;m[0]=-1;int s=0,z=1000000000;for(int i=0;i<(int)a.size();i++){s+=a[i];if(m.count(s-b))z=min(z,i-m[s-b]);m[s]=i;}return z==1000000000?-1:z;}','func solve(a []int,b int) int {m:=map[int]int{0:-1};s,z:=0,1000000000;for i,x:=range a{s+=x;if j,ok:=m[s-b];ok&&i-j<z{z=i-j};m[s]=i};if z==1000000000{return -1};return z}'),
    tests:[{stdin:'2 -1 2 3 -2\n3',expectedStdout:'1',isSample:true},{stdin:'1 2 3\n3',expectedStdout:'1',isSample:true},{stdin:'1 -1\n5',expectedStdout:'-1'},{stdin:'2 2 -1\n4',expectedStdout:'2'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'largest-prefix-gap', title:'Widest Gap Between Prefix Totals', difficulty:'HARD', patternTags:['prefix-sum','running-extremes'], signatureId:'fn:ints->int', avgSolveSeconds:900,
    promptMarkdown:['Using the initial prefix total zero and every later prefix total, return the largest absolute difference between any two prefix totals.','', '**Example**','', '```','input: 3 -8 2','output: 8','```','', 'The prefix totals are 0, 3, -5, and -3; the widest gap is from 3 to -5. The list is non-empty.'].join('\n'),
    editorialMarkdown:['## Prefix extremes describe every pair','','The largest absolute difference among numbers is simply maximum minus minimum. Generate every prefix total once, including the initial zero, and maintain both extremes. Their final difference equals the best pair without enumerating quadratic many prefix pairs.','','The quiet mistake is comparing only adjacent prefixes, which measures individual values rather than distant aggregate levels. Excluding zero also loses gaps that begin before the first value. Time is O(n), bounded by list length, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let s=0,lo=0,hi=0;for(const x of a){s+=x;lo=Math.min(lo,s);hi=Math.max(hi,s);}return hi-lo;}','function solve(a:number[]):number{let s=0,lo=0,hi=0;for(const x of a){s+=x;lo=Math.min(lo,s);hi=Math.max(hi,s);}return hi-lo;}','def solve(a):\n    s=lo=hi=0\n    for x in a:\n        s+=x;lo=min(lo,s);hi=max(hi,s)\n    return hi-lo','    static int solve(int[] a) { int s=0,lo=0,hi=0;for(int x:a){s+=x;lo=Math.min(lo,s);hi=Math.max(hi,s);}return hi-lo; }','int solve(vector<int>a){int s=0,lo=0,hi=0;for(int x:a){s+=x;lo=min(lo,s);hi=max(hi,s);}return hi-lo;}','func solve(a []int) int {s,lo,hi:=0,0,0;for _,x:=range a{s+=x;if s<lo{lo=s};if s>hi{hi=s}};return hi-lo}'),
    tests:[{stdin:'3 -8 2',expectedStdout:'8',isSample:true},{stdin:'1 2 3',expectedStdout:'6',isSample:true},{stdin:'-4',expectedStdout:'4'},{stdin:'2 -1 2 -1',expectedStdout:'3'}] }),
  p({ ...base, patternFamily:'INTERVALS', slug:'apply-closed-range-additions', title:'Many Closed Range Additions', difficulty:'HARD', patternTags:['difference-array','range-update'], signatureId:'fn:matrix->ints', avgSolveSeconds:960,
    promptMarkdown:['The first matrix row is the initial list. Every later row is `[left, right, add]`: add `add` to every index from left through right, inclusive. Return the final list.','', '**Example**','', '```','input: 1 2 3 4;1 3 5;0 1 -2','output: -1 5 8 9','```','', 'Indices are zero-based, every update is valid, and the matrix has at least the initial row. The edge case is no update rows, which returns the initial list unchanged.'].join('\n'),
    editorialMarkdown:['## Difference array and one reconstruction','','Instead of touching every covered cell for each update, place +add at left and -add just after right when that position exists. A final running difference sum tells how much to add at each index. This records each range boundary once, then reconstructs the result in one pass.','','The quiet mistake is subtracting at right rather than right plus one, which makes the right endpoint miss its update. Guard the position after the last cell. Time is O(n + q), bounded by list length n and update count q; space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let v=a[0].slice(),d=Array(v.length+1).fill(0);for(let i=1;i<a.length;i++){let [l,r,x]=a[i];d[l]+=x;if(r+1<v.length)d[r+1]-=x;}let s=0;for(let i=0;i<v.length;i++){s+=d[i];v[i]+=s;}return v;}','function solve(a:number[][]):number[]{const v=a[0].slice(),d=Array(v.length+1).fill(0);for(let i=1;i<a.length;i++){const[l,r,x]=a[i];d[l]+=x;if(r+1<v.length)d[r+1]-=x;}let s=0;for(let i=0;i<v.length;i++){s+=d[i];v[i]+=s;}return v;}','def solve(a):\n    v=a[0][:];d=[0]*(len(v)+1)\n    for l,r,x in a[1:]:\n        d[l]+=x\n        if r+1<len(v):d[r+1]-=x\n    s=0\n    for i in range(len(v)):s+=d[i];v[i]+=s\n    return v','    static int[] solve(int[][] a) { int[]v=a[0].clone(),d=new int[v.length+1];for(int i=1;i<a.length;i++){int l=a[i][0],r=a[i][1],x=a[i][2];d[l]+=x;if(r+1<v.length)d[r+1]-=x;}int s=0;for(int i=0;i<v.length;i++){s+=d[i];v[i]+=s;}return v; }','vector<int> solve(vector<vector<int>>a){vector<int>v=a[0],d(v.size()+1);for(int i=1;i<(int)a.size();i++){int l=a[i][0],r=a[i][1],x=a[i][2];d[l]+=x;if(r+1<(int)v.size())d[r+1]-=x;}int s=0;for(int i=0;i<(int)v.size();i++){s+=d[i];v[i]+=s;}return v;}','func solve(a [][]int) []int {v:=append([]int{},a[0]...);d:=make([]int,len(v)+1);for i:=1;i<len(a);i++{l,r,x:=a[i][0],a[i][1],a[i][2];d[l]+=x;if r+1<len(v){d[r+1]-=x}};s:=0;for i:=range v{s+=d[i];v[i]+=s};return v}'),
    tests:[{stdin:'1 2 3 4;1 3 5;0 1 -2',expectedStdout:'-1 5 8 9',isSample:true},{stdin:'4 5',expectedStdout:'4 5',isSample:true},{stdin:'0 0 0;0 2 1',expectedStdout:'1 1 1'},{stdin:'1 1 1;1 1 -3;0 0 2',expectedStdout:'3 -2 1'}] }),
  p({ ...base, patternFamily:'ARRAYS_HASHING', slug:'count-prefix-product-one', title:'Prefixes With Product One', difficulty:'MEDIUM', patternTags:['prefix-product','counting'], signatureId:'fn:ints->int', avgSolveSeconds:720,
    promptMarkdown:['All values are either -1 or 1. Count non-empty prefixes whose product is exactly 1.','', '**Example**','', '```','input: -1 -1 1 -1','output: 2','```','', 'The list has at least one value. Product one means an even number of -1 values; a prefix containing only ones also qualifies.'].join('\n'),
    editorialMarkdown:['## Running product sign','','With inputs restricted to -1 and 1, the full product is only one of those two signs. Start at one and multiply each arriving value; count every time the sign returns to one. This is a prefix product rather than a sum, but it has the same one-pass aggregate structure.','','The quiet mistake is counting negative prefixes instead, or resetting the product after a match and losing later prefixes. No zero exists to erase history under the stated guarantee. Time is O(n), bounded by list length, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let p=1,c=0;for(const x of a){p*=x;if(p===1)c++;}return c;}','function solve(a:number[]):number{let p=1,c=0;for(const x of a){p*=x;if(p===1)c++;}return c;}','def solve(a):\n    p=1;c=0\n    for x in a:\n        p*=x\n        if p==1:c+=1\n    return c','    static int solve(int[] a) { int p=1,c=0;for(int x:a){p*=x;if(p==1)c++;}return c; }','int solve(vector<int>a){int p=1,c=0;for(int x:a){p*=x;if(p==1)c++;}return c;}','func solve(a []int) int {p,c:=1,0;for _,x:=range a{p*=x;if p==1{c++}};return c}'),
    tests:[{stdin:'-1 -1 1 -1',expectedStdout:'2',isSample:true},{stdin:'1 1',expectedStdout:'2',isSample:true},{stdin:'-1',expectedStdout:'0'},{stdin:'-1 -1 -1 -1',expectedStdout:'2'}] }),
];
