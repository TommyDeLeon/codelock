import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-026` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_026_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "binary-signal-to-decimal",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "BIT_MANIPULATION",
    title: "Decode Binary Signal",
    patternTags: ["array","bit-manipulation","math"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "You receive a binary signal represented as an array of `0`s and `1`s, where the first element is the most significant bit. Convert this signal into its decimal equivalent.\n\n**Constraints**\n- `1 <= signal.length <= 30`\n- `signal[i]` is either `0` or `1`.\n\n**Example 1**\n```\ninput:\n1 0 1\noutput: 5\n```\nExplanation: `101` in binary is `5` in decimal.\n\n**Example 2**\n```\ninput:\n1 1\noutput: 3\n```\nExplanation: `11` in binary is `3` in decimal.\n\n**Example 3**\n```\ninput:\n0\noutput: 0\n```\nExplanation: `0` in binary is `0` in decimal.\n\n**Follow-up:** Can you solve this in a single pass?",
    editorialMarkdown: "## Decode Binary Signal\n\nThis problem requires converting an array of binary digits into its integer representation. The most efficient way is to use a running total, multiplying it by 2 (or left-shifting by 1) for each new digit, and then adding the new digit.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the number of bits.\n- **Space Complexity:** mathcal{O}(1) as we only need an integer to store the result.\n\n**Common Trap:**\nAttempting to calculate powers of 2 using indices might lead to index out of bounds or off-by-one errors. It is cleaner to accumulate the value iteratively from left to right.",
    referenceSolution: {
      JAVASCRIPT: "function solve(signal) {\n    let res = 0;\n    for (let b of signal) {\n        res = (res << 1) | b;\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(signal: number[]): number {\n    let res = 0;\n    for (let b of signal) {\n        res = (res << 1) | b;\n    }\n    return res;\n}",
      PYTHON: "def solve(signal):\n    res = 0\n    for b in signal:\n        res = (res << 1) | b\n    return res",
      JAVA: "    static int solve(int[] signal) {\n        int res = 0;\n        for (int b : signal) {\n            res = (res << 1) | b;\n        }\n        return res;\n    }",
      CPP: "int solve(vector<int> signal) {\n    int res = 0;\n    for (int b : signal) {\n        res = (res << 1) | b;\n    }\n    return res;\n}",
      GO: "func solve(signal []int) int {\n    res := 0\n    for _, b := range signal {\n        res = (res << 1) | b\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "1 0 1", expectedStdout: "5", isSample: true },
      { stdin: "1 1", expectedStdout: "3", isSample: true },
      { stdin: "0", expectedStdout: "0", isSample: true },
      { stdin: "1", expectedStdout: "1" },
      { stdin: "1 0 0 0", expectedStdout: "8" },
      { stdin: "0 0 0 0", expectedStdout: "0" },
      { stdin: "1 1 1 1 1", expectedStdout: "31" },
      { stdin: "1 0 1 0 1 0 1 0", expectedStdout: "170" },
    ],
  }),

  p({
    ...base,
    slug: "format-id-badge",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Format ID Badge",
    patternTags: ["strings","concatenation"],
    signatureId: "fn:string,string->string",
    avgSolveSeconds: 200,
    promptMarkdown: "Given a person's `name` and their `title`, return a single string in the exact format `\"name (title)\"`.\n\n**Constraints**\n- `1 <= name.length, title.length <= 50`\n- Both strings contain only English letters and spaces.\n\n**Example 1**\n```\ninput:\nAlice\nEngineer\noutput: Alice (Engineer)\n```\nExplanation: The name and title are formatted correctly.\n\n**Example 2**\n```\ninput:\nBob\nDoctor\noutput: Bob (Doctor)\n```\nExplanation: The output matches the requested format.\n\n**Example 3**\n```\ninput:\nZoe\nCEO\noutput: Zoe (CEO)\n```\nExplanation: Correct format applied.\n\n**Follow-up:** Does string concatenation take linear time with respect to the input lengths?",
    editorialMarkdown: "## Format ID Badge\n\nThis problem is a basic string manipulation task. We simply need to concatenate the given `name`, the string `\" (\"`, the given `title`, and `\")\"`.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N + M) where N and M are the lengths of the two strings.\n- **Space Complexity:** mathcal{O}(N + M) for the resulting string.\n\n**Common Trap:**\nForgetting the space before the open parenthesis or failing to include both parentheses.",
    referenceSolution: {
      JAVASCRIPT: "function solve(name, title) {\n    return `${name} (${title})`;\n}",
      TYPESCRIPT: "function solve(name: string, title: string): string {\n    return `${name} (${title})`;\n}",
      PYTHON: "def solve(name, title):\n    return f\"{name} ({title})\"",
      JAVA: "    static String solve(String name, String title) {\n        return name + \" (\" + title + \")\";\n    }",
      CPP: "string solve(string name, string title) {\n    return name + \" (\" + title + \")\";\n}",
      GO: "func solve(name string, title string) string {\n    return name + \" (\" + title + \")\"\n}",
    },
    tests: [
      { stdin: "Alice\nEngineer", expectedStdout: "Alice (Engineer)", isSample: true },
      { stdin: "Bob\nDoctor", expectedStdout: "Bob (Doctor)", isSample: true },
      { stdin: "Zoe\nCEO", expectedStdout: "Zoe (CEO)", isSample: true },
      { stdin: "A\nB", expectedStdout: "A (B)" },
      { stdin: "Long Name\nLong Title", expectedStdout: "Long Name (Long Title)" },
      { stdin: "x\ny", expectedStdout: "x (y)" },
      { stdin: "First Last\nSome Cool Title", expectedStdout: "First Last (Some Cool Title)" },
      { stdin: "A B C\nD E F", expectedStdout: "A B C (D E F)" },
    ],
  }),

  p({
    ...base,
    slug: "double-transmission-packet",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Double Transmission",
    patternTags: ["array","concatenation"],
    signatureId: "fn:ints->ints",
    avgSolveSeconds: 250,
    promptMarkdown: "Given an array `packet`, return a new array consisting of `packet` repeated twice consecutively.\n\n**Constraints**\n- `0 <= packet.length <= 20`\n- `-100 <= packet[i] <= 100`\n\n**Example 1**\n```\ninput:\n1 2 3\noutput: 1 2 3 1 2 3\n```\nExplanation: The output is the `packet` concatenated with itself.\n\n**Example 2**\n```\ninput:\n5\noutput: 5 5\n```\nExplanation: Repeating the single element twice.\n\n**Example 3**\n```\ninput:\n\noutput: \n```\nExplanation: Repeating an empty packet yields an empty packet.\n\n**Follow-up:** Can you initialize the output array with the correct exact size beforehand to avoid dynamic resizing overhead?",
    editorialMarkdown: "## Double Transmission\n\nThis is an array manipulation problem where we return a new array consisting of the elements of the original array repeated twice in the same order.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the length of the array.\n- **Space Complexity:** mathcal{O}(N) for the new array that is double the size of the input.\n\n**Common Trap:**\nTrying to modify the original array during iteration, which can cause an infinite loop or index out-of-bounds errors. Creating a new array is safer and recommended.",
    referenceSolution: {
      JAVASCRIPT: "function solve(packet) {\n    return packet.concat(packet);\n}",
      TYPESCRIPT: "function solve(packet: number[]): number[] {\n    return packet.concat(packet);\n}",
      PYTHON: "def solve(packet):\n    return packet * 2",
      JAVA: "    static int[] solve(int[] packet) {\n        int[] res = new int[packet.length * 2];\n        for (int i = 0; i < packet.length; i++) {\n            res[i] = packet[i];\n            res[i + packet.length] = packet[i];\n        }\n        return res;\n    }",
      CPP: "vector<int> solve(vector<int> packet) {\n    vector<int> res;\n    res.reserve(packet.size() * 2);\n    for (int x : packet) res.push_back(x);\n    for (int x : packet) res.push_back(x);\n    return res;\n}",
      GO: "func solve(packet []int) []int {\n    res := make([]int, len(packet)*2)\n    for i, x := range packet {\n        res[i] = x\n        res[i+len(packet)] = x\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "1 2 3", expectedStdout: "1 2 3 1 2 3", isSample: true },
      { stdin: "5", expectedStdout: "5 5", isSample: true },
      { stdin: "", expectedStdout: "", isSample: true },
      { stdin: "0", expectedStdout: "0 0" },
      { stdin: "-1 -2 -3", expectedStdout: "-1 -2 -3 -1 -2 -3" },
      { stdin: "10 20", expectedStdout: "10 20 10 20" },
      { stdin: "1 1 1", expectedStdout: "1 1 1 1 1 1" },
      { stdin: "42", expectedStdout: "42 42" },
    ],
  }),

  p({
    ...base,
    slug: "inverted-display-valid",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Reflective Cipher Validation",
    patternTags: ["math","digits","simulation"],
    signatureId: "fn:int->bool",
    avgSolveSeconds: 400,
    promptMarkdown: "A cryptographer is analyzing a substitution cipher based on numerical streams. In this cipher, an operation called \"Reflection\" is applied to an integer by reversing the order of its digits and substituting each digit according to a specific rule:\n- `0`, `1`, and `8` translate to themselves.\n- `6` translates to `9`, and `9` translates to `6`.\n- `2`, `3`, `4`, `5`, and `7` translate to invalid characters.\n\nAn integer `n` is considered \"Reflective\" if applying this operation produces a valid integer (no invalid characters) that is strictly different from the original integer `n`. \n\nGiven a non-negative integer `n`, return `true` if it is Reflective, and `false` otherwise.\n\n**Constraints**\n- `0 <= n <= 1000000000`\n\n**Example 1**\n```\ninput:\n6\noutput: true\n```\nExplanation: Reversing the single digit gives `6`. Substituting yields `9`. Since `9 != 6`, the integer is Reflective.\n\n**Example 2**\n```\ninput:\n89\noutput: true\n```\nExplanation: Reversing the digits of `89` gives `98`. Substituting these digits yields `68`. Since `68 != 89`, the integer is Reflective.\n\n**Example 3**\n```\ninput:\n11\noutput: false\n```\nExplanation: Reversing `11` gives `11`. Substituting yields `11`. The result is the same as the original, so it is not Reflective.\n\n**Follow-up:** Try to solve it mathematically without converting the number to a string.",
    editorialMarkdown: "## Inverted Display Valid\n\nThis problem asks us to simulate reading an integer upside down. The digits `0`, `1`, `8` map to themselves, `6` maps to `9`, and `9` maps to `6`. All other digits are invalid and immediately mean the number is not readable. Additionally, the inverted number must be numerically different from the original number.\n\nWe can solve this by continuously extracting the last digit (using modulo 10), checking if it is invertible, getting its inverted counterpart, and appending it to a new number (using multiplication by 10 and addition).\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(log N), which is the number of digits in N.\n- **Space Complexity:** mathcal{O}(1) as we only need a few integer variables.\n\n**Common Trap:**\nForgetting that numbers that read the same upside down (like `11`, `69`, `88`) should return `false`. The problem explicitly states it must be strictly different.",
    referenceSolution: {
      JAVASCRIPT: "function solve(n) {\n    if (n === 0) return false;\n    let original = n;\n    let inverted = 0;\n    while (n > 0) {\n        let d = n % 10;\n        let inv = -1;\n        if (d === 0 || d === 1 || d === 8) inv = d;\n        else if (d === 6) inv = 9;\n        else if (d === 9) inv = 6;\n        else return false;\n        inverted = inverted * 10 + inv;\n        n = Math.floor(n / 10);\n    }\n    return inverted !== original;\n}",
      TYPESCRIPT: "function solve(n: number): boolean {\n    if (n === 0) return false;\n    let original = n;\n    let inverted = 0;\n    while (n > 0) {\n        let d = n % 10;\n        let inv = -1;\n        if (d === 0 || d === 1 || d === 8) inv = d;\n        else if (d === 6) inv = 9;\n        else if (d === 9) inv = 6;\n        else return false;\n        inverted = inverted * 10 + inv;\n        n = Math.floor(n / 10);\n    }\n    return inverted !== original;\n}",
      PYTHON: "def solve(n):\n    if n == 0: return False\n    original = n\n    inverted = 0\n    while n > 0:\n        d = n % 10\n        if d in (0, 1, 8): inv = d\n        elif d == 6: inv = 9\n        elif d == 9: inv = 6\n        else: return False\n        inverted = inverted * 10 + inv\n        n //= 10\n    return inverted != original",
      JAVA: "    static boolean solve(int n) {\n        if (n == 0) return false;\n        int original = n;\n        long inverted = 0;\n        while (n > 0) {\n            int d = n % 10;\n            int inv = -1;\n            if (d == 0 || d == 1 || d == 8) inv = d;\n            else if (d == 6) inv = 9;\n            else if (d == 9) inv = 6;\n            else return false;\n            inverted = inverted * 10 + inv;\n            n /= 10;\n        }\n        return inverted != original;\n    }",
      CPP: "bool solve(int n) {\n    int original = n;\n    long long inverted = 0;\n    while (n > 0 || (n == 0 && inverted == 0)) {\n        int d = n % 10;\n        int inv = -1;\n        if (d == 0 || d == 1 || d == 8) inv = d;\n        else if (d == 6) inv = 9;\n        else if (d == 9) inv = 6;\n        else return false;\n        inverted = inverted * 10 + inv;\n        if (n == 0) break;\n        n /= 10;\n    }\n    return inverted != original;\n}",
      GO: "func solve(n int) bool {\n    if n == 0 {\n        return false\n    }\n    original := n\n    inverted := 0\n    for n > 0 {\n        d := n % 10\n        inv := -1\n        if d == 0 || d == 1 || d == 8 {\n            inv = d\n        } else if d == 6 {\n            inv = 9\n        } else if d == 9 {\n            inv = 6\n        } else {\n            return false\n        }\n        inverted = inverted*10 + inv\n        n /= 10\n    }\n    return inverted != original\n}",
    },
    tests: [
      { stdin: "6", expectedStdout: "true", isSample: true },
      { stdin: "89", expectedStdout: "true", isSample: true },
      { stdin: "11", expectedStdout: "false", isSample: true },
      { stdin: "25", expectedStdout: "false" },
      { stdin: "0", expectedStdout: "false" },
      { stdin: "916", expectedStdout: "false" },
      { stdin: "69", expectedStdout: "false" },
      { stdin: "68", expectedStdout: "true" },
    ],
  }),

  p({
    ...base,
    slug: "echo-chamber-waves",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Echo Chamber Waves",
    patternTags: ["array","concatenation","reversal"],
    signatureId: "fn:ints->ints",
    avgSolveSeconds: 300,
    promptMarkdown: "Given an array of integers representing sound `waves`, return a new array where the original `waves` are immediately followed by the `waves` in reverse order.\n\n**Constraints**\n- `0 <= waves.length <= 40`\n- `-1000 <= waves[i] <= 1000`\n\n**Example 1**\n```\ninput:\n1 2 3\noutput: 1 2 3 3 2 1\n```\nExplanation: The original waves `1 2 3` are followed by their reverse `3 2 1`.\n\n**Example 2**\n```\ninput:\n5\noutput: 5 5\n```\nExplanation: The reverse of a single element is the element itself.\n\n**Example 3**\n```\ninput:\n\noutput: \n```\nExplanation: An empty input produces an empty output.\n\n**Follow-up:** Can you do this without calling built-in reverse functions?",
    editorialMarkdown: "## Echo Chamber Waves\n\nThis problem simply requires returning a new array that contains the elements of the original array followed by its elements in reverse order. This can be achieved by iterating through the original array twice or by taking advantage of language-specific array concatenation and reversal features.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) to traverse and copy the elements.\n- **Space Complexity:** mathcal{O}(N) for the new array.\n\n**Common Trap:**\nModifying the original array in place instead of creating a new one might lead to infinite loops or incorrect results when appending the reversed elements.",
    referenceSolution: {
      JAVASCRIPT: "function solve(waves) {\n    let res = waves.slice();\n    for (let i = waves.length - 1; i >= 0; i--) {\n        res.push(waves[i]);\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(waves: number[]): number[] {\n    let res = waves.slice();\n    for (let i = waves.length - 1; i >= 0; i--) {\n        res.push(waves[i]);\n    }\n    return res;\n}",
      PYTHON: "def solve(waves):\n    return waves + waves[::-1]",
      JAVA: "    static int[] solve(int[] waves) {\n        int[] res = new int[waves.length * 2];\n        for (int i = 0; i < waves.length; i++) {\n            res[i] = waves[i];\n            res[waves.length * 2 - 1 - i] = waves[i];\n        }\n        return res;\n    }",
      CPP: "vector<int> solve(vector<int> waves) {\n    vector<int> res;\n    for (int w : waves) res.push_back(w);\n    int n = waves.size();\n    for (int i = n - 1; i >= 0; i--) res.push_back(waves[i]);\n    return res;\n}",
      GO: "func solve(waves []int) []int {\n    res := make([]int, 0, len(waves)*2)\n    res = append(res, waves...)\n    for i := len(waves) - 1; i >= 0; i-- {\n        res = append(res, waves[i])\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "1 2 3", expectedStdout: "1 2 3 3 2 1", isSample: true },
      { stdin: "5", expectedStdout: "5 5", isSample: true },
      { stdin: "", expectedStdout: "", isSample: true },
      { stdin: "10 20 30 40", expectedStdout: "10 20 30 40 40 30 20 10" },
      { stdin: "-1 -2 -3", expectedStdout: "-1 -2 -3 -3 -2 -1" },
      { stdin: "0 0", expectedStdout: "0 0 0 0" },
      { stdin: "1 -1 1 -1", expectedStdout: "1 -1 1 -1 -1 1 -1 1" },
      { stdin: "42", expectedStdout: "42 42" },
    ],
  }),

  p({
    ...base,
    slug: "checksum-multiplier",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Transmission Validation Key",
    patternTags: ["array","digits","math"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 400,
    promptMarkdown: "A spacecraft receives communication packets, each represented by an array of single-digit integers `codes`. To verify the integrity of the transmission, the onboard computer computes a validation key based on two properties:\n\n1. The \"Base Sequence\": Formed by extracting all non-zero integers from the array in their original order and concatenating them to form a single integer. If the array contains no non-zero integers, this sequence evaluates to `0`.\n2. The \"Total Energy\": The sum of all integers in the original array.\n\nThe final validation key is the product of the Base Sequence and the Total Energy. Given the array `codes`, return this final key.\n\n**Constraints**\n- `0 <= codes.length <= 8`\n- `0 <= codes[i] <= 9`\n\n**Example 1**\n```\ninput:\n1 0 2\noutput: 36\n```\nExplanation: The Base Sequence is `12` (combining `1` and `2`). The Total Energy is `1 + 0 + 2 = 3`. The final key is `12 * 3 = 36`.\n\n**Example 2**\n```\ninput:\n0 0 0\noutput: 0\n```\nExplanation: The Base Sequence is `0`. The Total Energy is `0 + 0 + 0 = 0`. The final key is `0 * 0 = 0`.\n\n**Example 3**\n```\ninput:\n5\noutput: 25\n```\nExplanation: The Base Sequence is `5`. The Total Energy is `5`. The final key is `5 * 5 = 25`.\n\n**Follow-up:** Can you solve it in a single pass?",
    editorialMarkdown: "## Checksum Multiplier\n\nTo solve this problem, we need to iterate through the array once or twice to calculate two values: the integer formed by concatenating all non-zero digits, and the sum of all digits. We can maintain a running string or integer for the concatenation and a running sum for the sum of digits.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the number of digits in the array.\n- **Space Complexity:** mathcal{O}(1) auxiliary space if we build the number mathematically.\n\n**Common Trap:**\nHandling the case where there are no non-zero digits. The concatenated number should be `0` in this case. Also, beware of integer overflow if the input array is very large, but constraints prevent this here.",
    referenceSolution: {
      JAVASCRIPT: "function solve(codes) {\n    let a = 0;\n    let b = 0;\n    for (let x of codes) {\n        if (x !== 0) {\n            a = a * 10 + x;\n        }\n        b += x;\n    }\n    return a * b;\n}",
      TYPESCRIPT: "function solve(codes: number[]): number {\n    let a = 0;\n    let b = 0;\n    for (let x of codes) {\n        if (x !== 0) {\n            a = a * 10 + x;\n        }\n        b += x;\n    }\n    return a * b;\n}",
      PYTHON: "def solve(codes):\n    a = 0\n    b = 0\n    for x in codes:\n        if x != 0:\n            a = a * 10 + x\n        b += x\n    return a * b",
      JAVA: "    static int solve(int[] codes) {\n        long a = 0;\n        int b = 0;\n        for (int x : codes) {\n            if (x != 0) {\n                a = a * 10 + x;\n            }\n            b += x;\n        }\n        return (int)(a * b);\n    }",
      CPP: "int solve(vector<int> codes) {\n    long long a = 0;\n    int b = 0;\n    for (int x : codes) {\n        if (x != 0) {\n            a = a * 10 + x;\n        }\n        b += x;\n    }\n    return a * b;\n}",
      GO: "func solve(codes []int) int {\n    a := 0\n    b := 0\n    for _, x := range codes {\n        if x != 0 {\n            a = a*10 + x\n        }\n        b += x\n    }\n    return a * b\n}",
    },
    tests: [
      { stdin: "1 0 2", expectedStdout: "36", isSample: true },
      { stdin: "0 0 0", expectedStdout: "0", isSample: true },
      { stdin: "5", expectedStdout: "25", isSample: true },
      { stdin: "", expectedStdout: "0" },
      { stdin: "1 2 3 4 5", expectedStdout: "185175" },
      { stdin: "9 0 9 0 0 9 0", expectedStdout: "26973" },
      { stdin: "0 0 0 0 0 0 0 0", expectedStdout: "0" },
      { stdin: "9", expectedStdout: "81" },
    ],
  }),
];
