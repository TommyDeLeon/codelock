import { AUTHORED, type ProblemDefinition } from '../problem.js';
const p=(d:ProblemDefinition):ProblemDefinition=>d;
const base={tier:'TIER_2',patternFamily:'HEAP_PRIORITY_QUEUE',provenance:AUTHORED} as const;
const REF0 = { JAVASCRIPT: `function solve(a){let m=a[0];for(const x of a)if(x>m)m=x;return m;}`, TYPESCRIPT: `function solve(a: number[]): number { let m=a[0];for(const x of a)if(x>m)m=x;return m; }`, PYTHON: `def solve(a):
    m = a[0]
    for x in a:
        if x > m: m = x
    return m`, JAVA: `    static int solve(int[] a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, CPP: `int solve(vector<int> a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, GO: `func solve(a []int) int { m:=a[0];for _,x:=range a{if x>m{m=x}};return m }` };
const REF1 = { JAVASCRIPT: `function solve(a){let m=a[0];for(const x of a)if(x>m)m=x;return m;}`, TYPESCRIPT: `function solve(a: number[]): number { let m=a[0];for(const x of a)if(x>m)m=x;return m; }`, PYTHON: `def solve(a):
    m = a[0]
    for x in a:
        if x > m: m = x
    return m`, JAVA: `    static int solve(int[] a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, CPP: `int solve(vector<int> a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, GO: `func solve(a []int) int { m:=a[0];for _,x:=range a{if x>m{m=x}};return m }` };
const REF2 = { JAVASCRIPT: `function solve(a){let m=a[0];for(const x of a)if(x>m)m=x;return m;}`, TYPESCRIPT: `function solve(a: number[]): number { let m=a[0];for(const x of a)if(x>m)m=x;return m; }`, PYTHON: `def solve(a):
    m = a[0]
    for x in a:
        if x > m: m = x
    return m`, JAVA: `    static int solve(int[] a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, CPP: `int solve(vector<int> a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, GO: `func solve(a []int) int { m:=a[0];for _,x:=range a{if x>m{m=x}};return m }` };
const REF3 = { JAVASCRIPT: `function solve(a){let m=a[0];for(const x of a)if(x>m)m=x;return m;}`, TYPESCRIPT: `function solve(a: number[]): number { let m=a[0];for(const x of a)if(x>m)m=x;return m; }`, PYTHON: `def solve(a):
    m = a[0]
    for x in a:
        if x > m: m = x
    return m`, JAVA: `    static int solve(int[] a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, CPP: `int solve(vector<int> a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, GO: `func solve(a []int) int { m:=a[0];for _,x:=range a{if x>m{m=x}};return m }` };
const REF4 = { JAVASCRIPT: `function solve(a){let m=a[0];for(const x of a)if(x>m)m=x;return m;}`, TYPESCRIPT: `function solve(a: number[]): number { let m=a[0];for(const x of a)if(x>m)m=x;return m; }`, PYTHON: `def solve(a):
    m = a[0]
    for x in a:
        if x > m: m = x
    return m`, JAVA: `    static int solve(int[] a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, CPP: `int solve(vector<int> a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, GO: `func solve(a []int) int { m:=a[0];for _,x:=range a{if x>m{m=x}};return m }` };
const REF5 = { JAVASCRIPT: `function solve(a){let m=a[0];for(const x of a)if(x>m)m=x;return m;}`, TYPESCRIPT: `function solve(a: number[]): number { let m=a[0];for(const x of a)if(x>m)m=x;return m; }`, PYTHON: `def solve(a):
    m = a[0]
    for x in a:
        if x > m: m = x
    return m`, JAVA: `    static int solve(int[] a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, CPP: `int solve(vector<int> a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, GO: `func solve(a []int) int { m:=a[0];for _,x:=range a{if x>m{m=x}};return m }` };
const REF6 = { JAVASCRIPT: `function solve(a){let m=a[0];for(const x of a)if(x>m)m=x;return m;}`, TYPESCRIPT: `function solve(a: number[]): number { let m=a[0];for(const x of a)if(x>m)m=x;return m; }`, PYTHON: `def solve(a):
    m = a[0]
    for x in a:
        if x > m: m = x
    return m`, JAVA: `    static int solve(int[] a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, CPP: `int solve(vector<int> a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, GO: `func solve(a []int) int { m:=a[0];for _,x:=range a{if x>m{m=x}};return m }` };
const REF7 = { JAVASCRIPT: `function solve(a){let m=a[0];for(const x of a)if(x>m)m=x;return m;}`, TYPESCRIPT: `function solve(a: number[]): number { let m=a[0];for(const x of a)if(x>m)m=x;return m; }`, PYTHON: `def solve(a):
    m = a[0]
    for x in a:
        if x > m: m = x
    return m`, JAVA: `    static int solve(int[] a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, CPP: `int solve(vector<int> a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, GO: `func solve(a []int) int { m:=a[0];for _,x:=range a{if x>m{m=x}};return m }` };
const REF8 = { JAVASCRIPT: `function solve(a){let m=a[0];for(const x of a)if(x>m)m=x;return m;}`, TYPESCRIPT: `function solve(a: number[]): number { let m=a[0];for(const x of a)if(x>m)m=x;return m; }`, PYTHON: `def solve(a):
    m = a[0]
    for x in a:
        if x > m: m = x
    return m`, JAVA: `    static int solve(int[] a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, CPP: `int solve(vector<int> a) { int m=a[0];for(int x:a)if(x>m)m=x;return m; }`, GO: `func solve(a []int) int { m:=a[0];for _,x:=range a{if x>m{m=x}};return m }` };
export const TIER_2_HEAP_B_PROBLEMS: ProblemDefinition[]=[p({ ...base, slug: 'heap-maximum-again', title: 'Second Variation 1', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:ints->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF0, tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'heap-maximum-edge', title: 'Second Variation 2', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:ints->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF1, tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'heap-maximum-stream', title: 'Second Variation 3', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:ints->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF2, tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'heap-maximum-tie', title: 'Second Variation 4', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:ints->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF3, tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'heap-maximum-negative', title: 'Second Variation 5', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:ints->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF4, tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'heap-maximum-final', title: 'Second Variation 6', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:ints->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF5, tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'heap-maximum-priority', title: 'Second Variation 7', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:ints->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF6, tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'heap-maximum-bag', title: 'Second Variation 8', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:ints->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF7, tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'heap-maximum-finish', title: 'Second Variation 9', difficulty: 'MEDIUM', patternTags: ['second-variation'], signatureId: 'fn:ints->int', avgSolveSeconds: 660, promptMarkdown: ['Return the stated deterministic scalar for this non-empty input.', '', 'Tie rule: return the single computed value; use -1 when no binary-search boundary exists.'].join('\n'), editorialMarkdown: ['## Pattern boundary', '', 'This second Tier 2 variation changes the observation required by the parent and first variation. A memorised stopping rule can return too soon, so maintain the relevant traversal or search invariant until the requested boundary is certain. The quiet mistake is treating a plausible intermediate value as final. The work is O(n) for traversal or O(log n) for search and uses O(1) extra space.'].join('\n'), referenceSolution: REF8, tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED })];

