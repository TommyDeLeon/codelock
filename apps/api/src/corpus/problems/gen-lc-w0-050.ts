import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w0-050` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W0_050_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "robotic-frog-jump",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Robotic Frog Jump",
    patternTags: ["math","grid","simulation","geometry"],
    signatureId: "fn:string->bool",
    avgSolveSeconds: 600,
    promptMarkdown: "A robotic frog is placed on an 8x8 grid with columns labeled `'a'` to `'h'` and rows labeled `'1'` to `'8'`. Its current `position` is given as a 2-character string. The frog moves exactly like a knight in chess (2 steps in one direction, 1 in a perpendicular direction).\n\nDetermine if the total number of valid jumps the frog can make from its current position that keep it on the 8x8 grid is an even number. Return `true` if the number of valid jumps is even, and `false` otherwise.\n\n**Constraints**\n- `position` is exactly 2 characters.\n- `position[0]` is a lowercase English letter from `'a'` to `'h'`.\n- `position[1]` is a digit from `'1'` to `'8'`.\n\n**Example 1**\n```\ninput:\na1\noutput: true\n```\n*Explanation: From a1, the frog can only jump to b3 and c2. That is 2 valid jumps, which is even.*\n\n**Example 2**\n```\ninput:\na2\noutput: false\n```\n*Explanation: From a2, the valid jumps are b4, c3, and c1. There are 3 jumps, which is odd.*\n\n**Example 3**\n```\ninput:\nc4\noutput: true\n```\n*Explanation: From c4, the frog can make all 8 possible jumps safely on the grid. 8 is even.*\n\n**Follow-up**\nIs there a mathematical property of the board you could use to return the answer without simulating all 8 moves?",
    editorialMarkdown: "## Robotic Frog Jump\nWe check all 8 possible L-shaped jumps from the given start position. For each jump, we check if the new row and column fall within the 8x8 grid boundaries. We keep a count of all valid moves and finally return `true` if the count is even, `false` otherwise.\n\n**Trap**: Do not forget that the board starts at 'a' for columns and '1' for rows, and both have exactly 8 elements.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(1) as we only check a fixed 8 directions.\n- **Space Complexity:** mathcal{O}(1) for constant variables.",
    referenceSolution: {
      JAVASCRIPT: "function solve(pos) {\n    const c = pos.charCodeAt(0) - 97;\n    const r = pos.charCodeAt(1) - 49;\n    let count = 0;\n    const dx = [1, 1, -1, -1, 2, 2, -2, -2];\n    const dy = [2, -2, 2, -2, 1, -1, 1, -1];\n    for (let i = 0; i < 8; i++) {\n        const nx = c + dx[i], ny = r + dy[i];\n        if (nx >= 0 && nx < 8 && ny >= 0 && ny < 8) count++;\n    }\n    return count % 2 === 0;\n}",
      TYPESCRIPT: "function solve(pos: string): boolean {\n    const c = pos.charCodeAt(0) - 97;\n    const r = pos.charCodeAt(1) - 49;\n    let count = 0;\n    const dx = [1, 1, -1, -1, 2, 2, -2, -2];\n    const dy = [2, -2, 2, -2, 1, -1, 1, -1];\n    for (let i = 0; i < 8; i++) {\n        const nx = c + dx[i], ny = r + dy[i];\n        if (nx >= 0 && nx < 8 && ny >= 0 && ny < 8) count++;\n    }\n    return count % 2 === 0;\n}",
      PYTHON: "def solve(pos):\n    c = ord(pos[0]) - 97\n    r = ord(pos[1]) - 49\n    count = 0\n    for dx, dy in [(1,2), (1,-2), (-1,2), (-1,-2), (2,1), (2,-1), (-2,1), (-2,-1)]:\n        if 0 <= c + dx < 8 and 0 <= r + dy < 8:\n            count += 1\n    return count % 2 == 0",
      JAVA: "    static boolean solve(String pos) {\n        int c = pos.charAt(0) - 'a';\n        int r = pos.charAt(1) - '1';\n        int count = 0;\n        int[] dx = {1, 1, -1, -1, 2, 2, -2, -2};\n        int[] dy = {2, -2, 2, -2, 1, -1, 1, -1};\n        for (int i = 0; i < 8; i++) {\n            int nx = c + dx[i];\n            int ny = r + dy[i];\n            if (nx >= 0 && nx < 8 && ny >= 0 && ny < 8) count++;\n        }\n        return count % 2 == 0;\n    }",
      CPP: "#include <string>\n#include <vector>\nusing namespace std;\nbool solve(string pos) {\n    int c = pos[0] - 'a';\n    int r = pos[1] - '1';\n    int count = 0;\n    int dx[] = {1, 1, -1, -1, 2, 2, -2, -2};\n    int dy[] = {2, -2, 2, -2, 1, -1, 1, -1};\n    for (int i = 0; i < 8; i++) {\n        int nx = c + dx[i];\n        int ny = r + dy[i];\n        if (nx >= 0 && nx < 8 && ny >= 0 && ny < 8) count++;\n    }\n    return count % 2 == 0;\n}",
      GO: "func solve(pos string) bool {\n    c := int(pos[0] - 'a')\n    r := int(pos[1] - '1')\n    count := 0\n    dx := []int{1, 1, -1, -1, 2, 2, -2, -2}\n    dy := []int{2, -2, 2, -2, 1, -1, 1, -1}\n    for i := 0; i < 8; i++ {\n        nx, ny := c+dx[i], r+dy[i]\n        if nx >= 0 && nx < 8 && ny >= 0 && ny < 8 {\n            count++\n        }\n    }\n    return count % 2 == 0\n}",
    },
    tests: [
      { stdin: "a1", expectedStdout: "true", isSample: true },
      { stdin: "a2", expectedStdout: "false", isSample: true },
      { stdin: "c4", expectedStdout: "true", isSample: true },
      { stdin: "b1", expectedStdout: "false" },
      { stdin: "h8", expectedStdout: "true" },
      { stdin: "e5", expectedStdout: "true" },
      { stdin: "g8", expectedStdout: "false" },
      { stdin: "d1", expectedStdout: "true" },
    ],
  }),

  p({
    ...base,
    slug: "research-sector-labels",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Botanical Generation Codes",
    patternTags: ["math","base-conversion","string"],
    signatureId: "fn:int->string",
    avgSolveSeconds: 600,
    promptMarkdown: "You are assigning identification codes to successive generations of a rapidly reproducing plant species. The 1st generation is assigned the code `\"A\"`, the 2nd is `\"B\"`, ..., the 26th is `\"Z\"`, the 27th is `\"AA\"`, the 28th is `\"AB\"`, and so forth.\n\nGiven a positive integer `generationNumber`, return its corresponding string code.\n\n**Constraints**\n- `1 <= generationNumber <= 2^31 - 1`\n\n**Example 1**\n```\ninput:\n1\noutput: A\n```\n*Explanation: The 1st generation is simply assigned the first letter.*\n\n**Example 2**\n```\ninput:\n28\noutput: AB\n```\n*Explanation: Following Z (26) is AA (27), making the 28th generation AB.*\n\n**Example 3**\n```\ninput:\n701\noutput: ZY\n```\n*Explanation: The 701st generation corresponds to the code ZY.*\n\n**Follow-up**\nCan you write the conversion in exactly one while loop without reversing the resulting string at the end?",
    editorialMarkdown: "## Research Sector Labels\nThis problem asks you to convert a 1-indexed number into a base-26 representation using uppercase letters. We repeatedly subtract 1 from the number to handle the 1-indexing correctly, grab the modulo 26 to find the current letter, and integer divide by 26.\n\n**Trap**: Not subtracting 1 from the number before modulo operation. Base-26 here doesn't have a zero digit!\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(log_{26}(N)) where N is the input integer.\n- **Space Complexity:** mathcal{O}(log_{26}(N)) to store the resulting string.",
    referenceSolution: {
      JAVASCRIPT: "function solve(n) {\n    let res = \"\";\n    while (n > 0) {\n        n--;\n        res = String.fromCharCode((n % 26) + 65) + res;\n        n = Math.floor(n / 26);\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(n: number): string {\n    let res = \"\";\n    while (n > 0) {\n        n--;\n        res = String.fromCharCode((n % 26) + 65) + res;\n        n = Math.floor(n / 26);\n    }\n    return res;\n}",
      PYTHON: "def solve(n):\n    res = []\n    while n > 0:\n        n -= 1\n        res.append(chr(n % 26 + 65))\n        n //= 26\n    return \"\".join(res[::-1])",
      JAVA: "    static String solve(int n) {\n        StringBuilder res = new StringBuilder();\n        while (n > 0) {\n            n--;\n            res.append((char)('A' + (n % 26)));\n            n /= 26;\n        }\n        return res.reverse().toString();\n    }",
      CPP: "#include <string>\n#include <algorithm>\nusing namespace std;\nstring solve(int n) {\n    string res = \"\";\n    while (n > 0) {\n        n--;\n        res += (char)('A' + (n % 26));\n        n /= 26;\n    }\n    reverse(res.begin(), res.end());\n    return res;\n}",
      GO: "func solve(n int) string {\n    res := \"\"\n    for n > 0 {\n        n--\n        res = string(rune('A'+(n%26))) + res\n        n /= 26\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "1", expectedStdout: "A", isSample: true },
      { stdin: "28", expectedStdout: "AB", isSample: true },
      { stdin: "701", expectedStdout: "ZY", isSample: true },
      { stdin: "26", expectedStdout: "Z" },
      { stdin: "27", expectedStdout: "AA" },
      { stdin: "52", expectedStdout: "AZ" },
      { stdin: "702", expectedStdout: "ZZ" },
      { stdin: "703", expectedStdout: "AAA" },
      { stdin: "2147483647", expectedStdout: "FXSHRXW" },
    ],
  }),

  p({
    ...base,
    slug: "weather-station-data-recovery",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Weather Station Data Recovery",
    patternTags: ["array","arrays","simulation"],
    signatureId: "fn:ints->ints",
    avgSolveSeconds: 400,
    promptMarkdown: "A weather station records hourly temperature readings. Occasionally, a sensor fails and records `-1`. You need to clean the data by replacing every missing reading (`-1`) with the most recently recorded valid temperature. \n\nIf the sequence starts with one or more `-1` readings, they should be replaced with `0`.\n\nGiven an integer array `readings`, return the cleaned data as a new array.\n\n**Constraints**\n- `0 <= readings.length <= 40`\n- `readings[i]` is either `-1` or between `0` and `100`.\n\n**Example 1**\n```\ninput:\n20 -1 -1 25 -1\noutput: 20 20 20 25 25\n```\n*Explanation: The missing readings are replaced by the most recent valid temperatures (20 and 25).*\n\n**Example 2**\n```\ninput:\n-1 -1 15 16 -1\noutput: 0 0 15 16 16\n```\n*Explanation: Since the first two readings are missing, they default to 0. The final -1 takes the last valid reading of 16.*\n\n**Example 3**\n```\ninput:\n10 20 30\noutput: 10 20 30\n```\n*Explanation: There are no missing readings.*\n\n**Follow-up**\nCan you modify the array in-place instead of creating a new one?",
    editorialMarkdown: "## Weather Station Data Recovery\nWe iterate through the array and maintain the value of the `last` seen valid reading, initializing it to `0`. If we encounter `-1`, we insert the `last` reading. If we encounter a valid number, we insert it and update the `last` reading for future missing data.\n\n**Trap**: Be careful with the initial state! If the first few readings are `-1`, they should become `0`, not crash your logic.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the number of readings.\n- **Space Complexity:** mathcal{O}(N) to return the new output array.",
    referenceSolution: {
      JAVASCRIPT: "function solve(readings) {\n    let last = 0;\n    const res = [];\n    for (let i = 0; i < readings.length; i++) {\n        if (readings[i] === -1) {\n            res.push(last);\n        } else {\n            res.push(readings[i]);\n            last = readings[i];\n        }\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(readings: number[]): number[] {\n    let last = 0;\n    const res: number[] = [];\n    for (let i = 0; i < readings.length; i++) {\n        if (readings[i] === -1) {\n            res.push(last);\n        } else {\n            res.push(readings[i]);\n            last = readings[i];\n        }\n    }\n    return res;\n}",
      PYTHON: "def solve(readings):\n    last = 0\n    res = []\n    for x in readings:\n        if x == -1:\n            res.append(last)\n        else:\n            res.append(x)\n            last = x\n    return res",
      JAVA: "    static int[] solve(int[] readings) {\n        int[] res = new int[readings.length];\n        int last = 0;\n        for (int i = 0; i < readings.length; i++) {\n            if (readings[i] == -1) {\n                res[i] = last;\n            } else {\n                res[i] = readings[i];\n                last = readings[i];\n            }\n        }\n        return res;\n    }",
      CPP: "#include <vector>\nusing namespace std;\nvector<int> solve(vector<int> readings) {\n    vector<int> res(readings.size());\n    int last = 0;\n    for (int i = 0; i < readings.size(); i++) {\n        if (readings[i] == -1) {\n            res[i] = last;\n        } else {\n            res[i] = readings[i];\n            last = readings[i];\n        }\n    }\n    return res;\n}",
      GO: "func solve(readings []int) []int {\n    res := make([]int, len(readings))\n    last := 0\n    for i, x := range readings {\n        if x == -1 {\n            res[i] = last\n        } else {\n            res[i] = x\n            last = x\n        }\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "20 -1 -1 25 -1", expectedStdout: "20 20 20 25 25", isSample: true },
      { stdin: "-1 -1 15 16 -1", expectedStdout: "0 0 15 16 16", isSample: true },
      { stdin: "10 20 30", expectedStdout: "10 20 30", isSample: true },
      { stdin: "", expectedStdout: "" },
      { stdin: "-1", expectedStdout: "0" },
      { stdin: "0 -1 0", expectedStdout: "0 0 0" },
      { stdin: "-1 5 -1 5", expectedStdout: "0 5 5 5" },
      { stdin: "100 -1 99 -1", expectedStdout: "100 100 99 99" },
    ],
  }),

  p({
    ...base,
    slug: "cargo-ship-weight-filtering",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Cargo Ship Weight Filtering",
    patternTags: ["array","arrays","filtering","in-place"],
    signatureId: "fn:ints,int->ints",
    avgSolveSeconds: 300,
    promptMarkdown: "A space cargo ship can only transport containers that weigh at least a certain `threshold` weight.\n\nGiven an array of integer `weights` and the `threshold`, return a new array of weights containing only the containers that meet or exceed the threshold. The retained containers must be in their original relative order.\n\n**Constraints**\n- `0 <= weights.length <= 40`\n- `0 <= weights[i] <= 1000`\n- `0 <= threshold <= 1000`\n\n**Example 1**\n```\ninput:\n10 20 5 30 15\n15\noutput: 20 30 15\n```\n*Explanation: The elements 10 and 5 are below 15 and are removed. 15 meets the exact threshold and stays.*\n\n**Example 2**\n```\ninput:\n1 2 3\n5\noutput: \n```\n*Explanation: None of the containers meet the threshold, so the result is empty.*\n\n**Example 3**\n```\ninput:\n10 10 10\n10\noutput: 10 10 10\n```\n*Explanation: All containers meet the exact threshold.*\n\n**Follow-up**\nCan you achieve this in mathcal{O}(1) space using two pointers to overwrite the original array?",
    editorialMarkdown: "## Cargo Ship Weight Filtering\nA simple iteration and filtering condition is needed here. You scan through each weight and keep it if it is ≥ the specified threshold.\n\n**Trap**: Do not alter the order of the original elements. Make sure edge cases with empty arrays or elements exactly equal to the threshold are handled smoothly.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the length of the weights array.\n- **Space Complexity:** mathcal{O}(N) auxiliary space for returning the result.",
    referenceSolution: {
      JAVASCRIPT: "function solve(weights, threshold) {\n    return weights.filter(w => w >= threshold);\n}",
      TYPESCRIPT: "function solve(weights: number[], threshold: number): number[] {\n    return weights.filter(w => w >= threshold);\n}",
      PYTHON: "def solve(weights, threshold):\n    return [w for w in weights if w >= threshold]",
      JAVA: "    static int[] solve(int[] weights, int threshold) {\n        int count = 0;\n        for (int w : weights) {\n            if (w >= threshold) count++;\n        }\n        int[] res = new int[count];\n        int idx = 0;\n        for (int w : weights) {\n            if (w >= threshold) res[idx++] = w;\n        }\n        return res;\n    }",
      CPP: "#include <vector>\nusing namespace std;\nvector<int> solve(vector<int> weights, int threshold) {\n    vector<int> res;\n    for (int w : weights) {\n        if (w >= threshold) res.push_back(w);\n    }\n    return res;\n}",
      GO: "func solve(weights []int, threshold int) []int {\n    res := []int{}\n    for _, w := range weights {\n        if w >= threshold {\n            res = append(res, w)\n        }\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "10 20 5 30 15\n15", expectedStdout: "20 30 15", isSample: true },
      { stdin: "1 2 3\n5", expectedStdout: "", isSample: true },
      { stdin: "10 10 10\n10", expectedStdout: "10 10 10", isSample: true },
      { stdin: "\n5", expectedStdout: "" },
      { stdin: "5\n1", expectedStdout: "5" },
      { stdin: "1 2\n1", expectedStdout: "1 2" },
      { stdin: "1 2 3 4 5 6\n4", expectedStdout: "4 5 6" },
      { stdin: "100 0 100 0\n50", expectedStdout: "100 100" },
    ],
  }),

  p({
    ...base,
    slug: "cyber-market-rebates",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "STACK",
    title: "Particle Accelerator Resonance",
    patternTags: ["array","arrays","stack","monotonic-stack"],
    signatureId: "fn:ints->ints",
    avgSolveSeconds: 700,
    promptMarkdown: "You are analyzing a sequence of energy barriers in a particle accelerator, represented by an array `barriers`. A particle passing through a barrier will have its required energy reduced by the value of the **first** subsequent barrier that has an energy requirement less than or equal to the current one due to a momentum resonance effect.\n\nIf no such subsequent barrier exists, the energy requirement for that barrier remains unchanged.\n\nReturn an array containing the final energy requirements for all barriers after accounting for the resonance effects.\n\n**Constraints**\n- `0 <= barriers.length <= 40`\n- `0 <= barriers[i] <= 1000`\n\n**Example 1**\n```\ninput:\n8 4 6 2 3\noutput: 4 2 4 2 3\n```\n*Explanation: \n- Barrier 8 is reduced by 4 (final = 4).\n- Barrier 4 is reduced by 2 (final = 2).\n- Barrier 6 is reduced by 2 (final = 4).\n- Barrier 2 is not reduced as no smaller/equal barrier follows it.\n- Barrier 3 is not reduced.*\n\n**Example 2**\n```\ninput:\n1 2 3 4 5\noutput: 1 2 3 4 5\n```\n*Explanation: The energy requirements are strictly increasing, so no resonance effect occurs.*\n\n**Example 3**\n```\ninput:\n10 1 1 6\noutput: 9 0 1 6\n```\n*Explanation: The 10 is reduced by 1. The first 1 is reduced by the second 1 (final = 0). The second 1 and the 6 are not reduced.*\n\n**Follow-up**\nCan you accomplish this in strictly mathcal{O}(N) time using a monotonic stack data structure?",
    editorialMarkdown: "## Cyber Market Rebates\nBecause the array sizes are small (≤ 40), you can use two nested loops. For each item at index `i`, you simply scan through the subsequent items starting from `i+1` and find the first item `j` where `credits[j] <= credits[i]`. If you find one, subtract its value from `credits[i]` and break early. An optimal solution uses a monotonic stack in mathcal{O}(N) time.\n\n**Trap**: Do not subtract the value of an item that appears before the current one, and remember to break after finding the FIRST valid rebate.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N^2) due to the nested loop scanning approach, or mathcal{O}(N) if solved using a stack.\n- **Space Complexity:** mathcal{O}(N) to hold the copied output array.",
    referenceSolution: {
      JAVASCRIPT: "function solve(credits) {\n    const res = [...credits];\n    for (let i = 0; i < credits.length; i++) {\n        for (let j = i + 1; j < credits.length; j++) {\n            if (credits[j] <= credits[i]) {\n                res[i] -= credits[j];\n                break;\n            }\n        }\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(credits: number[]): number[] {\n    const res = [...credits];\n    for (let i = 0; i < credits.length; i++) {\n        for (let j = i + 1; j < credits.length; j++) {\n            if (credits[j] <= credits[i]) {\n                res[i] -= credits[j];\n                break;\n            }\n        }\n    }\n    return res;\n}",
      PYTHON: "def solve(credits):\n    res = list(credits)\n    for i in range(len(credits)):\n        for j in range(i + 1, len(credits)):\n            if credits[j] <= credits[i]:\n                res[i] -= credits[j]\n                break\n    return res",
      JAVA: "    static int[] solve(int[] credits) {\n        int[] res = new int[credits.length];\n        for (int i = 0; i < credits.length; i++) {\n            res[i] = credits[i];\n            for (int j = i + 1; j < credits.length; j++) {\n                if (credits[j] <= credits[i]) {\n                    res[i] -= credits[j];\n                    break;\n                }\n            }\n        }\n        return res;\n    }",
      CPP: "#include <vector>\nusing namespace std;\nvector<int> solve(vector<int> credits) {\n    vector<int> res = credits;\n    for (int i = 0; i < credits.size(); i++) {\n        for (int j = i + 1; j < credits.size(); j++) {\n            if (credits[j] <= credits[i]) {\n                res[i] -= credits[j];\n                break;\n            }\n        }\n    }\n    return res;\n}",
      GO: "func solve(credits []int) []int {\n    res := make([]int, len(credits))\n    copy(res, credits)\n    for i := 0; i < len(credits); i++ {\n        for j := i + 1; j < len(credits); j++ {\n            if credits[j] <= credits[i] {\n                res[i] -= credits[j]\n                break\n            }\n        }\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "8 4 6 2 3", expectedStdout: "4 2 4 2 3", isSample: true },
      { stdin: "1 2 3 4 5", expectedStdout: "1 2 3 4 5", isSample: true },
      { stdin: "10 1 1 6", expectedStdout: "9 0 1 6", isSample: true },
      { stdin: "", expectedStdout: "" },
      { stdin: "5", expectedStdout: "5" },
      { stdin: "7 7 7", expectedStdout: "0 0 7" },
      { stdin: "5 4 3 2 1", expectedStdout: "1 1 1 1 1" },
      { stdin: "10 9 8 1", expectedStdout: "1 1 7 1" },
    ],
  }),

  p({
    ...base,
    slug: "drone-altitude-sensors",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "TWO_POINTERS",
    title: "Faulty Manuscript Scribes",
    patternTags: ["array","arrays","two-pointers"],
    signatureId: "fn:matrix->int",
    avgSolveSeconds: 800,
    promptMarkdown: "Two medieval scribes are copying an ancient manuscript, where each page is represented as an array of integers denoting the lengths of consecutive words. You are given a 2D array `transcripts` containing exactly two rows of equal length `N`, one for each scribe's copy. Exactly one scribe made a mistake: they skipped exactly one word, causing their remaining word lengths to shift left by one position, and they added a random integer at the end to match the required word count per page. The other scribe's copy is perfectly accurate.\n\nReturn `1` if the first scribe (`transcripts[0]`) is the faulty one, `2` if the second scribe (`transcripts[1]`) is the faulty one, and `-1` if it is impossible to determine (i.e., both are valid candidates).\n\n**Constraints**\n- `transcripts.length == 2`\n- `2 <= transcripts[0].length <= 40`\n- `1 <= transcripts[i][j] <= 100`\n\n**Example 1**\n```\ninput:\n2 3 4 5;2 4 5 8\noutput: 2\n```\n*Explanation: The copies first differ at index 1 (3 vs 4). If the second scribe skipped the 3, their sequence shifts to 2, 4, 5, with an added random 8 at the end, which perfectly matches their transcript.*\n\n**Example 2**\n```\ninput:\n2 3 4 5;2 3 4 5\noutput: -1\n```\n*Explanation: Both scribes produced the exact same sequence, making it impossible to identify the faulty one.*\n\n**Example 3**\n```\ninput:\n10 20 30 40;20 30 40 15\noutput: 2\n```\n*Explanation: The second scribe shifted 20, 30, and 40 to the left, meaning they skipped the 10.*\n\n**Follow-up**\nCan you solve this with only mathcal{O}(1) additional space without creating copies of arrays?",
    editorialMarkdown: "## Drone Altitude Sensors\nWe traverse the two sensors' arrays to find the first index where they differ. This index is the point where the dropped reading occurred. Since we know exactly one is faulty, we can check if `sensors[0]` excluding the last element matches the rest of `sensors[1]` and vice versa. If both are possible (or both are impossible if the input was fully identical), we return -1.\n\n**Trap**: Make sure to check the case where the difference occurs exactly at the last element; in this situation it is always impossible to determine which sensor is faulty.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the length of the sensor readings.\n- **Space Complexity:** mathcal{O}(1) or mathcal{O}(N) depending on substring/slice creation in different languages.",
    referenceSolution: {
      JAVASCRIPT: "function solve(sensors) {\n    const s1 = sensors[0], s2 = sensors[1];\n    const n = s1.length;\n    for (let i = 0; i < n; i++) {\n        if (s1[i] !== s2[i]) {\n            if (i === n - 1) return -1;\n            let f1 = true, f2 = true;\n            for (let j = i; j < n - 1; j++) {\n                if (s1[j] !== s2[j + 1]) f1 = false;\n                if (s2[j] !== s1[j + 1]) f2 = false;\n            }\n            if (f1 && !f2) return 1;\n            if (f2 && !f1) return 2;\n            return -1;\n        }\n    }\n    return -1;\n}",
      TYPESCRIPT: "function solve(sensors: number[][]): number {\n    const s1 = sensors[0], s2 = sensors[1];\n    const n = s1.length;\n    for (let i = 0; i < n; i++) {\n        if (s1[i] !== s2[i]) {\n            if (i === n - 1) return -1;\n            let f1 = true, f2 = true;\n            for (let j = i; j < n - 1; j++) {\n                if (s1[j] !== s2[j + 1]) f1 = false;\n                if (s2[j] !== s1[j + 1]) f2 = false;\n            }\n            if (f1 && !f2) return 1;\n            if (f2 && !f1) return 2;\n            return -1;\n        }\n    }\n    return -1;\n}",
      PYTHON: "def solve(sensors):\n    s1, s2 = sensors[0], sensors[1]\n    n = len(s1)\n    for i in range(n):\n        if s1[i] != s2[i]:\n            if i == n - 1:\n                return -1\n            f1 = (s1[i:n-1] == s2[i+1:n])\n            f2 = (s2[i:n-1] == s1[i+1:n])\n            if f1 and not f2:\n                return 1\n            if f2 and not f1:\n                return 2\n            return -1\n    return -1",
      JAVA: "    static int solve(int[][] sensors) {\n        int[] s1 = sensors[0];\n        int[] s2 = sensors[1];\n        int n = s1.length;\n        for (int i = 0; i < n; i++) {\n            if (s1[i] != s2[i]) {\n                if (i == n - 1) return -1;\n                boolean f1 = true, f2 = true;\n                for (int j = i; j < n - 1; j++) {\n                    if (s1[j] != s2[j + 1]) f1 = false;\n                    if (s2[j] != s1[j + 1]) f2 = false;\n                }\n                if (f1 && !f2) return 1;\n                if (f2 && !f1) return 2;\n                return -1;\n            }\n        }\n        return -1;\n    }",
      CPP: "#include <vector>\nusing namespace std;\nint solve(vector<vector<int>> sensors) {\n    vector<int>& s1 = sensors[0];\n    vector<int>& s2 = sensors[1];\n    int n = s1.size();\n    for (int i = 0; i < n; i++) {\n        if (s1[i] != s2[i]) {\n            if (i == n - 1) return -1;\n            bool f1 = true, f2 = true;\n            for (int j = i; j < n - 1; j++) {\n                if (s1[j] != s2[j + 1]) f1 = false;\n                if (s2[j] != s1[j + 1]) f2 = false;\n            }\n            if (f1 && !f2) return 1;\n            if (f2 && !f1) return 2;\n            return -1;\n        }\n    }\n    return -1;\n}",
      GO: "func solve(sensors [][]int) int {\n    s1 := sensors[0]\n    s2 := sensors[1]\n    n := len(s1)\n    for i := 0; i < n; i++ {\n        if s1[i] != s2[i] {\n            if i == n-1 {\n                return -1\n            }\n            f1, f2 := true, true\n            for j := i; j < n-1; j++ {\n                if s1[j] != s2[j+1] {\n                    f1 = false\n                }\n                if s2[j] != s1[j+1] {\n                    f2 = false\n                }\n            }\n            if f1 && !f2 {\n                return 1\n            }\n            if f2 && !f1 {\n                return 2\n            }\n            return -1\n        }\n    }\n    return -1\n}",
    },
    tests: [
      { stdin: "2 3 4 5;2 4 5 8", expectedStdout: "2", isSample: true },
      { stdin: "2 3 4 5;2 3 4 5", expectedStdout: "-1", isSample: true },
      { stdin: "10 20 30 40;20 30 40 15", expectedStdout: "2", isSample: true },
      { stdin: "1 2;1 2", expectedStdout: "-1" },
      { stdin: "5 5 5 5;5 5 5 5", expectedStdout: "-1" },
      { stdin: "1 2 1 2;1 1 2 1", expectedStdout: "-1" },
      { stdin: "7 8 9;7 8 10", expectedStdout: "-1" },
      { stdin: "1 3 2 4 5;1 2 4 5 9", expectedStdout: "2" },
      { stdin: "2 4 5 9;3 2 4 5", expectedStdout: "1" },
    ],
  }),
];
