import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'TWO_POINTERS', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_2_TWO_POINTERS_C_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'count-sorted-pairs-at-most-limit', title:'Pairs At or Below the Mark', difficulty:'EASY', patternTags:['two-pointers','sorted-input','counting'], signatureId:'fn:ints,int->int', avgSolveSeconds:540,
    promptMarkdown:['A non-empty list is sorted in nondecreasing order. Count index pairs `(i, j)` with `i < j` whose sum is at most `limit`.','', '**Example**','', '```','input: 1 2 3 5','6','output: 5','```','', 'The edge case is a sum exactly equal to the limit: it does count. Return `0` if no pair qualifies.'].join('\n'),
    editorialMarkdown:['## Counting a sorted suffix at once','', 'Put one pointer at each end. If the outer sum is at most the limit, every value between the pointers also forms a valid pair with the left value, because none is larger than the right value. Add that whole block and advance left. Otherwise reduce right until its value can participate.', '', 'The quiet mistake is using the strict comparison from the below-limit version. Here equality is deliberately accepted, so a boundary pair changes the answer. Each pointer moves only toward the other, giving O(n) time bounded by the list length and O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let l=0,r=a.length-1,c=0;while(l<r){if(a[l]+a[r]<=b){c+=r-l;l++;}else r--;}return c;}','function solve(a:number[],b:number):number{let l=0,r=a.length-1,c=0;while(l<r){if(a[l]+a[r]<=b){c+=r-l;l++;}else r--;}return c;}','def solve(a, b):\n    l, r, c = 0, len(a)-1, 0\n    while l < r:\n        if a[l] + a[r] <= b: c += r-l; l += 1\n        else: r -= 1\n    return c','    static int solve(int[] a, int b) { int l=0,r=a.length-1,c=0; while(l<r){if(a[l]+a[r]<=b){c+=r-l;l++;}else r--;} return c; }','#include <vector>\nusing namespace std;\nint solve(vector<int>a,int b){int l=0,r=(int)a.size()-1,c=0;while(l<r){if(a[l]+a[r]<=b){c+=r-l;l++;}else r--;}return c;}','func solve(a []int,b int) int { l,r,c:=0,len(a)-1,0;for l<r{if a[l]+a[r]<=b{c+=r-l;l++}else{r--}};return c}'),
    tests:[{stdin:'1 2 3 5\n6',expectedStdout:'4',isSample:true},{stdin:'1 1 1\n2',expectedStdout:'3',isSample:true},{stdin:'4 7\n10',expectedStdout:'0'},{stdin:'-3 -1 2 4\n1',expectedStdout:'4'}] }),
  p({ ...base, slug:'remove-value-keep-left-order', title:'Erase a Value, Keep the Queue', difficulty:'EASY', patternTags:['two-pointers','in-place','stable-filtering'], signatureId:'fn:ints,int->ints', avgSolveSeconds:540,
    promptMarkdown:['Return the input list after removing every occurrence of `value`. Keep every surviving number in its original relative order.','', '**Example**','', '```','input: 4 2 4 1 4 3','4','output: 2 1 3','```','', 'The list is non-empty. If every number is removed, return an empty list, represented by an empty output line.'].join('\n'),
    editorialMarkdown:['## Read and write pointers for stable removal','', 'Use a read pointer to inspect each input number and a write pointer for the next survivor. Copy only values different from the forbidden value, then return the written prefix. Since reads happen left to right and survivors are written immediately, their order cannot change.', '', 'The quiet mistake is swapping unwanted values with the end. That removes them but silently scrambles the kept queue, which this problem forbids. The pass is O(n) time bounded by the input length; apart from the returned array representation, it needs O(1) working space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let w=0;for(let x of a)if(x!==b)a[w++]=x;return a.slice(0,w);}','function solve(a:number[],b:number):number[]{let w=0;for(const x of a)if(x!==b)a[w++]=x;return a.slice(0,w);}','def solve(a, b):\n    w = 0\n    for x in a:\n        if x != b: a[w] = x; w += 1\n    return a[:w]','    static int[] solve(int[] a, int b) { int w=0; for(int x:a) if(x!=b) a[w++]=x; return java.util.Arrays.copyOf(a,w); }','#include <vector>\nusing namespace std;\nvector<int> solve(vector<int>a,int b){int w=0;for(int x:a)if(x!=b)a[w++]=x;a.resize(w);return a;}','func solve(a []int,b int) []int {w:=0;for _,x:=range a{if x!=b{a[w]=x;w++}};return a[:w]}'),
    tests:[{stdin:'4 2 4 1 4 3\n4',expectedStdout:'2 1 3',isSample:true},{stdin:'5 5\n5',expectedStdout:'',isSample:true},{stdin:'1 2 3\n9',expectedStdout:'1 2 3'},{stdin:'-1 0 -1\n-1',expectedStdout:'0'}] }),
  p({ ...base, slug:'merge-arrays-prefer-right-ties', title:'Merge Streams, Right Wins Ties', difficulty:'EASY', patternTags:['two-pointers','merge','tie-break'], signatureId:'fn:ints,ints->ints', avgSolveSeconds:570,
    promptMarkdown:['Two non-empty lists are sorted ascending. Merge all values ascending. When equal current values meet, take the right-list value first; equal values still print identically.','', '**Example**','', '```','input: 1 3 5','1 2 3','output: 1 1 2 3 3 5','```','', 'Duplicates are retained. The tie rule determines which pointer advances first, even though the displayed equal values look the same.'].join('\n'),
    editorialMarkdown:['## Two-frontier merge with a right tie','', 'Each pointer marks the smallest unused value in its sorted stream. Append the smaller frontier and advance that stream; on equality deliberately choose the right frontier. Once a stream ends, its remaining suffix is already in final order.', '', 'The quiet mistake is writing the conventional `<=` left preference from merge sort. It leaves numeric output unchanged for plain integers but breaks the stated merge provenance and trains the wrong tie discipline. Every element is consumed once: O(n+m) time and O(n+m) output space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let i=0,j=0,o=[];while(i<a.length&&j<b.length){if(a[i]<b[j])o.push(a[i++]);else o.push(b[j++]);}return o.concat(a.slice(i),b.slice(j));}','function solve(a:number[],b:number[]):number[]{let i=0,j=0,o:number[]=[];while(i<a.length&&j<b.length){if(a[i]<b[j])o.push(a[i++]);else o.push(b[j++]);}return o.concat(a.slice(i),b.slice(j));}','def solve(a, b):\n    i = j = 0; out = []\n    while i < len(a) and j < len(b):\n        if a[i] < b[j]: out.append(a[i]); i += 1\n        else: out.append(b[j]); j += 1\n    return out + a[i:] + b[j:]','    static int[] solve(int[] a, int[] b) { int[] o=new int[a.length+b.length]; int i=0,j=0,k=0; while(i<a.length&&j<b.length) o[k++]=a[i]<b[j]?a[i++]:b[j++]; while(i<a.length)o[k++]=a[i++]; while(j<b.length)o[k++]=b[j++]; return o; }','#include <vector>\nusing namespace std;\nvector<int> solve(vector<int>a,vector<int>b){vector<int>o;int i=0,j=0;while(i<(int)a.size()&&j<(int)b.size())if(a[i]<b[j])o.push_back(a[i++]);else o.push_back(b[j++]);while(i<(int)a.size())o.push_back(a[i++]);while(j<(int)b.size())o.push_back(b[j++]);return o;}','func solve(a []int,b []int) []int {i,j:=0,0;o:=[]int{};for i<len(a)&&j<len(b){if a[i]<b[j]{o=append(o,a[i]);i++}else{o=append(o,b[j]);j++}};o=append(o,a[i:]...);return append(o,b[j:]...)}'),
    tests:[{stdin:'1 3 5\n1 2 3',expectedStdout:'1 1 2 3 3 5',isSample:true},{stdin:'2\n1 3',expectedStdout:'1 2 3',isSample:true},{stdin:'-2 -2\n-2',expectedStdout:'-2 -2 -2'},{stdin:'7\n7',expectedStdout:'7 7'}] }),
  p({ ...base, slug:'stable-three-way-pivot-order', title:'Three Lanes Around a Pivot', difficulty:'MEDIUM', patternTags:['two-pointers','partition','stable-order'], signatureId:'fn:ints,int->ints', avgSolveSeconds:660,
    promptMarkdown:['Return values below `pivot`, then values equal to it, then values above it. Preserve original relative order inside all three groups.','', '**Example**','', '```','input: 3 1 3 2 4 3','3','output: 1 2 3 3 3 4','```','', 'Duplicates stay duplicated. The named edge case is an input containing only pivot values, which is unchanged.'].join('\n'),
    editorialMarkdown:['## Stable partition into three destinations','', 'Scan once and append each value to the low, equal, or high destination. Concatenating those destinations creates the requested partition while the append order preserves every group’s history. This is a two-pointer family task because reading and output placement are separate progress markers.', '', 'The quiet mistake is Dutch-national-flag swapping. That achieves three groups but is unstable, so a late low value can reorder earlier values. Here stability is observable. The scan costs O(n) time and the three destinations together use O(n) space, both bounded by input length.'].join('\n'),
    referenceSolution:six('function solve(a,b){let l=[],e=[],h=[];for(let x of a)(x<b?l:x===b?e:h).push(x);return l.concat(e,h);}','function solve(a:number[],b:number):number[]{const l:number[]=[],e:number[]=[],h:number[]=[];for(const x of a)(x<b?l:x===b?e:h).push(x);return l.concat(e,h);}','def solve(a, b):\n    l, e, h = [], [], []\n    for x in a: (l if x < b else e if x == b else h).append(x)\n    return l + e + h','    static int[] solve(int[] a, int b) { int[] o=new int[a.length];int k=0;for(int x:a)if(x<b)o[k++]=x;for(int x:a)if(x==b)o[k++]=x;for(int x:a)if(x>b)o[k++]=x;return o; }','#include <vector>\nusing namespace std;\nvector<int> solve(vector<int>a,int b){vector<int>l,e,h;for(int x:a){if(x<b)l.push_back(x);else if(x==b)e.push_back(x);else h.push_back(x);}l.insert(l.end(),e.begin(),e.end());l.insert(l.end(),h.begin(),h.end());return l;}','func solve(a []int,b int) []int {l,e,h:=[]int{},[]int{},[]int{};for _,x:=range a{if x<b{l=append(l,x)}else if x==b{e=append(e,x)}else{h=append(h,x)}};l=append(l,e...);return append(l,h...)}'),
    tests:[{stdin:'3 1 3 2 4 3\n3',expectedStdout:'1 2 3 3 3 4',isSample:true},{stdin:'2 2 2\n2',expectedStdout:'2 2 2',isSample:true},{stdin:'5 1 4\n0',expectedStdout:'5 1 4'},{stdin:'-1 -3 0 -1\n-1',expectedStdout:'-3 -1 -1 0'}] }),
  p({ ...base, slug:'palindrome-after-one-substitution', title:'Mirror With One Replacement', difficulty:'MEDIUM', patternTags:['two-pointers','strings','palindrome'], signatureId:'fn:string->bool', avgSolveSeconds:660,
    promptMarkdown:['Return `true` if a non-empty lowercase string can become a palindrome by replacing at most one character. A replacement may change any one position.','', '**Example**','', '```','input: abcdba','output: true','```','', 'Only `c` and `d` disagree, so changing either works. The edge case is two mismatched mirrored pairs, which cannot be fixed by one replacement.'].join('\n'),
    editorialMarkdown:['## Mirrored pairs and one replacement budget','', 'Compare characters from both ends. One replacement can repair exactly one mismatched mirrored pair, because changing either character of that pair makes them agree. Count mismatches and reject as soon as the second appears; no branching or deletion is needed.', '', 'The quiet mistake is using the one-deletion palindrome algorithm. Deleting shifts future pairs, while replacement leaves every index in place, so those are different permissions. There are at most n/2 comparisons: O(n) time bounded by string length and O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a){let l=0,r=a.length-1,b=0;while(l<r){if(a[l++]!==a[r--]&&++b>1)return false;}return true;}','function solve(a:string):boolean{let l=0,r=a.length-1,b=0;while(l<r){if(a[l++]!==a[r--]&&++b>1)return false;}return true;}','def solve(a):\n    l, r, bad = 0, len(a)-1, 0\n    while l < r:\n        if a[l] != a[r]:\n            bad += 1\n            if bad > 1: return False\n        l += 1; r -= 1\n    return True','    static boolean solve(String a) { int l=0,r=a.length()-1,b=0; while(l<r){if(a.charAt(l++)!=a.charAt(r--)&&++b>1)return false;} return true; }','#include <string>\nusing namespace std;\nbool solve(string a){int l=0,r=(int)a.size()-1,b=0;while(l<r){if(a[l++]!=a[r--]&&++b>1)return false;}return true;}','func solve(a string) bool {l,r,b:=0,len(a)-1,0;for l<r{if a[l]!=a[r]{b++;if b>1{return false}};l++;r--};return true}'),
    tests:[{stdin:'abcdba',expectedStdout:'true',isSample:true},{stdin:'abccaa',expectedStdout:'true',isSample:true},{stdin:'racecar',expectedStdout:'true'},{stdin:'a',expectedStdout:'true'},{stdin:'abcdef',expectedStdout:'false'}] }),
  p({ ...base, slug:'width-capped-water-container-min-width', title:'Tank With a Minimum Reach', difficulty:'MEDIUM', patternTags:['two-pointers','greedy','converging'], signatureId:'fn:ints,int->int', avgSolveSeconds:720,
    promptMarkdown:['Heights describe vertical posts at consecutive indexes. Return the largest water area between two posts whose distance is at least `minWidth`. Area is the shorter height times the distance.','', '**Example**','', '```','input: 1 8 6 2 5 4 8 3 7','5','output: 49','```','', 'There are at least two heights and `minWidth` is valid. A pair farther apart than the minimum is allowed; the edge case is that narrow high posts cannot be used.'].join('\n'),
    editorialMarkdown:['## Converging walls with an eligible distance','', 'Start at the widest pair, which already meets the minimum width. Record its area and move the shorter wall inward; keeping it cannot improve a future area because width shrinks and the limiting height does not rise. Stop when the remaining distance is below the required minimum.', '', 'The quiet mistake is treating the width as exactly the cap or stopping before evaluating the boundary distance. This task has a lower bound, not an upper cap. Each endpoint moves inward once, so time is O(n), bounded by height count, with O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let l=0,r=a.length-1,z=0;while(r-l>=b){z=Math.max(z,Math.min(a[l],a[r])*(r-l));if(a[l]<a[r])l++;else r--;}return z;}','function solve(a:number[],b:number):number{let l=0,r=a.length-1,z=0;while(r-l>=b){z=Math.max(z,Math.min(a[l],a[r])*(r-l));if(a[l]<a[r])l++;else r--;}return z;}','def solve(a, b):\n    l, r, best = 0, len(a)-1, 0\n    while r-l >= b:\n        best = max(best, min(a[l], a[r])*(r-l))\n        if a[l] < a[r]: l += 1\n        else: r -= 1\n    return best','    static int solve(int[] a, int b) { int l=0,r=a.length-1,z=0;while(r-l>=b){z=Math.max(z,Math.min(a[l],a[r])*(r-l));if(a[l]<a[r])l++;else r--;}return z; }','#include <vector>\nusing namespace std;\nint solve(vector<int>a,int b){int l=0,r=(int)a.size()-1,z=0;while(r-l>=b){int h=a[l]<a[r]?a[l]:a[r];if(h*(r-l)>z)z=h*(r-l);if(a[l]<a[r])l++;else r--;}return z;}','func solve(a []int,b int) int {l,r,z:=0,len(a)-1,0;for r-l>=b{h:=a[l];if a[r]<h{h=a[r]};if h*(r-l)>z{z=h*(r-l)};if a[l]<a[r]{l++}else{r--}};return z}'),
    tests:[{stdin:'1 8 6 2 5 4 8 3 7\n5',expectedStdout:'49',isSample:true},{stdin:'5 1 5\n2',expectedStdout:'10',isSample:true},{stdin:'1 9 1\n2',expectedStdout:'2'},{stdin:'3 3 3 3\n3',expectedStdout:'9'}] }),
  p({ ...base, slug:'closest-bounded-sum-pair', title:'Closest Pair Under the Ceiling', difficulty:'MEDIUM', patternTags:['two-pointers','sorted-input','converging'], signatureId:'fn:ints,int->ints', avgSolveSeconds:720,
    promptMarkdown:['An ascending list has at least two values. Return the pair with the largest sum that does not exceed `limit`, in ascending order. Return `-1 -1` if no pair qualifies.','', '**Example**','', '```','input: 1 2 4 7','8','output: 1 7','```','', 'If several pairs have the same best sum, return the one with the smaller first value. The named edge case is having no permitted pair.'].join('\n'),
    editorialMarkdown:['## Converging search for the best legal sum','', 'With the largest remaining right value, a legal sum makes every smaller right partner legal too, but this visible pair is the best for its left index. Record it and move left to seek a larger sum. An illegal sum requires moving right down. The sorted order makes both moves safe.', '', 'The quiet mistake is returning the first legal pair. It may be far below the ceiling, while later left values improve it. On equal sums keep the earlier left pair to satisfy the tie rule. Pointer motion is O(n) time and O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let l=0,r=a.length-1,x=-1,y=-1,s=-Infinity;while(l<r){let q=a[l]+a[r];if(q<=b){if(q>s){s=q;x=l;y=r;}l++;}else r--;}return [x<0?-1:a[x],y<0?-1:a[y]];}','function solve(a:number[],b:number):number[]{let l=0,r=a.length-1,x=-1,y=-1,s=-Infinity;while(l<r){const q=a[l]+a[r];if(q<=b){if(q>s){s=q;x=l;y=r;}l++;}else r--;}return [x<0?-1:a[x],y<0?-1:a[y]];}','def solve(a, b):\n    l, r, x, y, best = 0, len(a)-1, -1, -1, -10**30\n    while l < r:\n        s = a[l] + a[r]\n        if s <= b:\n            if s > best: best, x, y = s, l, r\n            l += 1\n        else: r -= 1\n    return [-1, -1] if x < 0 else [a[x], a[y]]','    static int[] solve(int[] a, int b) { int l=0,r=a.length-1,x=-1,y=-1,best=Integer.MIN_VALUE;while(l<r){int s=a[l]+a[r];if(s<=b){if(s>best){best=s;x=l;y=r;}l++;}else r--;}return x<0?new int[]{-1,-1}:new int[]{a[x],a[y]}; }','#include <vector>\nusing namespace std;\nvector<int> solve(vector<int>a,int b){int l=0,r=(int)a.size()-1,x=-1,y=-1,best=-2147483647;while(l<r){int s=a[l]+a[r];if(s<=b){if(s>best){best=s;x=l;y=r;}l++;}else r--;}if(x<0)return {-1,-1};return {a[x],a[y]};}','func solve(a []int,b int) []int {l,r,x,y,best:=0,len(a)-1,-1,-1,0;has:=false;for l<r{s:=a[l]+a[r];if s<=b{if !has||s>best{has=true;best=s;x=l;y=r};l++}else{r--}};if x<0{return []int{-1,-1}};return []int{a[x],a[y]}}'),
    tests:[{stdin:'1 2 4 7\n8',expectedStdout:'1 7',isSample:true},{stdin:'3 5 8\n7',expectedStdout:'-1 -1',isSample:true},{stdin:'-5 -1 2 9\n1',expectedStdout:'-1 2'},{stdin:'1 3 4 6\n7',expectedStdout:'1 6'}] }),
  p({ ...base, slug:'remove-adjacent-target-pairs-stably', title:'Cancel Neighboring Twins', difficulty:'MEDIUM', patternTags:['two-pointers','in-place','stack-simulation'], signatureId:'fn:ints,int->ints', avgSolveSeconds:720,
    promptMarkdown:['Repeatedly remove adjacent pairs equal to `target` until no such pair remains. Keep all other values in order and return the resulting list.','', '**Example**','', '```','input: 1 4 4 4 4 2','4','output: 1 2','```','', 'Pairs are removed two at a time. The edge case is an odd run of target values: exactly one target remains from that run.'].join('\n'),
    editorialMarkdown:['## Write pointer as a cancellation stack','', 'Treat the written prefix as a stack. For each input value, if it is the target and the current top is also the target, decrement the write pointer to cancel that pair; otherwise write the value. This automatically exposes a newly adjacent pair after a cancellation.', '', 'The quiet mistake is merely skipping every target value, which incorrectly erases an odd leftover. Counting runs works too, but the write pointer demonstrates why cascading adjacency remains correct. Each value is written or removed once: O(n) time and O(1) working space besides output.'].join('\n'),
    referenceSolution:six('function solve(a,b){let w=0;for(let x of a){if(x===b&&w&&a[w-1]===b)w--;else a[w++]=x;}return a.slice(0,w);}','function solve(a:number[],b:number):number[]{let w=0;for(const x of a){if(x===b&&w>0&&a[w-1]===b)w--;else a[w++]=x;}return a.slice(0,w);}','def solve(a, b):\n    w = 0\n    for x in a:\n        if x == b and w and a[w-1] == b: w -= 1\n        else: a[w] = x; w += 1\n    return a[:w]','    static int[] solve(int[] a, int b) { int w=0;for(int x:a){if(x==b&&w>0&&a[w-1]==b)w--;else a[w++]=x;}return java.util.Arrays.copyOf(a,w); }','#include <vector>\nusing namespace std;\nvector<int> solve(vector<int>a,int b){int w=0;for(int x:a){if(x==b&&w>0&&a[w-1]==b)w--;else a[w++]=x;}a.resize(w);return a;}','func solve(a []int,b int) []int {w:=0;for _,x:=range a{if x==b&&w>0&&a[w-1]==b{w--}else{a[w]=x;w++}};return a[:w]}'),
    tests:[{stdin:'1 4 4 4 4 2\n4',expectedStdout:'1 2',isSample:true},{stdin:'4 4 4\n4',expectedStdout:'4',isSample:true},{stdin:'1 2 3\n9',expectedStdout:'1 2 3'},{stdin:'5 5 1 5 5 5\n5',expectedStdout:'1 5'}] }),
  p({ ...base, slug:'palindrome-after-one-adjacent-swap', title:'Mirror After One Neighbor Swap', difficulty:'HARD', patternTags:['two-pointers','strings','palindrome'], signatureId:'fn:string->bool', avgSolveSeconds:900,
    promptMarkdown:['Return `true` if a non-empty lowercase string is already a palindrome or can become one after swapping one adjacent pair of characters.','', '**Example**','', '```','input: abac','output: true','```','', 'Swapping the middle `b a` gives `aabc`? No: this example is false; use `abca` where swapping `b c` gives `acba`, still false. The edge case is no useful swap: a palindrome already returns true.'].join('\n'),
    editorialMarkdown:['## Two pointers locate the only useful neighborhood','', 'First find the outermost mismatched pair. Any successful adjacent swap must touch one of those two sides, otherwise that mismatch remains. Try the at most four swaps adjacent to those indexes and check each resulting string with the ordinary two-pointer palindrome test.', '', 'The quiet mistake is trying every adjacent swap, which is correct but misses the structural reason and can become quadratic. Also do not claim a swap repairs arbitrary two mismatches. With constant candidate swaps and linear checks, time is O(n), bounded by string length, and extra space is O(n) for a trial copy.'].join('\n'),
    referenceSolution:six('function solve(a){let ok=s=>{for(let l=0,r=s.length-1;l<r;l++,r--)if(s[l]!==s[r])return false;return true};if(ok(a))return true;for(let i=0;i<a.length-1;i++){let s=a.slice(0,i)+a[i+1]+a[i]+a.slice(i+2);if(ok(s))return true;}return false;}','function solve(a:string):boolean{const ok=(s:string)=>{for(let l=0,r=s.length-1;l<r;l++,r--)if(s[l]!==s[r])return false;return true};if(ok(a))return true;for(let i=0;i<a.length-1;i++){const s=a.slice(0,i)+a[i+1]+a[i]+a.slice(i+2);if(ok(s))return true;}return false;}','def solve(a):\n    def ok(s): return s == s[::-1]\n    if ok(a): return True\n    for i in range(len(a)-1):\n        if ok(a[:i]+a[i+1]+a[i]+a[i+2:]): return True\n    return False','    static boolean solve(String a) { if(ok(a))return true;for(int i=0;i<a.length()-1;i++){String s=a.substring(0,i)+a.charAt(i+1)+a.charAt(i)+a.substring(i+2);if(ok(s))return true;}return false; }\n    static boolean ok(String s) { int l=0,r=s.length()-1;while(l<r)if(s.charAt(l++)!=s.charAt(r--))return false;return true; }','#include <string>\nusing namespace std;\nbool solve(string a){auto ok=[](string s){int l=0,r=(int)s.size()-1;while(l<r)if(s[l++]!=s[r--])return false;return true;};if(ok(a))return true;for(int i=0;i+1<(int)a.size();i++){string s=a;char t=s[i];s[i]=s[i+1];s[i+1]=t;if(ok(s))return true;}return false;}','func solve(a string) bool {ok:=func(s string) bool {for l,r:=0,len(s)-1;l<r;l,r=l+1,r-1{if s[l]!=s[r]{return false}};return true};if ok(a){return true};for i:=0;i<len(a)-1;i++{s:=[]byte(a);s[i],s[i+1]=s[i+1],s[i];if ok(string(s)){return true}};return false}'),
    tests:[{stdin:'aab',expectedStdout:'true',isSample:true},{stdin:'abcd',expectedStdout:'false',isSample:true},{stdin:'racecar',expectedStdout:'true'},{stdin:'a',expectedStdout:'true'},{stdin:'aabb',expectedStdout:'false'}] }),
  p({ ...base, slug:'count-triples-within-inclusive-band', title:'Triples Inside a Sum Band', difficulty:'HARD', patternTags:['two-pointers','sorting','counting'], signatureId:'fn:ints,int->int', avgSolveSeconds:960,
    promptMarkdown:['Given a list and a target, count index triples whose sum is within 1 of the target, inclusive. The list has at least three values.','', '**Example**','', '```','input: 1 2 3 4','6','output: 2','```','', 'Triples summing 5 through 7 count. The edge case is an exact band boundary, which is included.'].join('\n'),
    editorialMarkdown:['## Sorted triples with two moving bounds','', 'Sort a copy. For each first index, count pairs whose sum is at most the upper bound, then subtract pairs whose sum is below the lower bound. Each pair count uses converging pointers and can add a full suffix when its sum is small enough. The difference is exactly the inclusive band.', '', 'The quiet mistake is using the same comparison at both edges. The upper edge is `<=`, while subtraction uses `< lower` so a boundary sum survives. Sorting costs O(n log n); the outer loop and pointer sweeps cost O(n²), bounded by list length, with O(n) copied storage.'].join('\n'),
    referenceSolution:six('function solve(a,b){a=a.slice().sort((x,y)=>x-y);let ans=0;for(let k=0;k<a.length-2;k++){let c=t=>{let l=k+1,r=a.length-1,z=0;while(l<r){if(a[k]+a[l]+a[r]<=t){z+=r-l;l++;}else r--;}return z};ans+=c(b+1)-c(b-2);}return ans;}','function solve(a:number[],b:number):number{a=a.slice().sort((x,y)=>x-y);let ans=0;for(let k=0;k<a.length-2;k++){const c=(t:number)=>{let l=k+1,r=a.length-1,z=0;while(l<r){if(a[k]+a[l]+a[r]<=t){z+=r-l;l++;}else r--;}return z};ans+=c(b+1)-c(b-2);}return ans;}','def solve(a, b):\n    a = sorted(a); ans = 0\n    for k in range(len(a)-2):\n        def count(t):\n            l, r, z = k+1, len(a)-1, 0\n            while l < r:\n                if a[k]+a[l]+a[r] <= t: z += r-l; l += 1\n                else: r -= 1\n            return z\n        ans += count(b+1)-count(b-2)\n    return ans','    static int solve(int[] a, int b) { java.util.Arrays.sort(a);int ans=0;for(int k=0;k<a.length-2;k++){int[] ts={b+1,b-2};int[] cs=new int[2];for(int q=0;q<2;q++){int l=k+1,r=a.length-1;while(l<r){if(a[k]+a[l]+a[r]<=ts[q]){cs[q]+=r-l;l++;}else r--;}}ans+=cs[0]-cs[1];}return ans; }','#include <algorithm>\n#include <vector>\nusing namespace std;\nint solve(vector<int>a,int b){sort(a.begin(),a.end());int ans=0;for(int k=0;k+2<(int)a.size();k++){for(int q=0;q<2;q++){int t=q?b-2:b+1,l=k+1,r=(int)a.size()-1,z=0;while(l<r){if(a[k]+a[l]+a[r]<=t){z+=r-l;l++;}else r--;}ans+=q?-z:z;}}return ans;}','func solve(a []int,b int) int {for i:=0;i<len(a);i++{for j:=i+1;j<len(a);j++{if a[j]<a[i]{a[i],a[j]=a[j],a[i]}}};ans:=0;for k:=0;k<len(a)-2;k++{for q:=0;q<2;q++{t:=b+1;if q==1{t=b-2};l,r,z:=k+1,len(a)-1,0;for l<r{if a[k]+a[l]+a[r]<=t{z+=r-l;l++}else{r--}};if q==0{ans+=z}else{ans-=z}}};return ans}'),
    tests:[{stdin:'1 2 3 4\n6',expectedStdout:'2',isSample:true},{stdin:'1 1 1\n2',expectedStdout:'1',isSample:true},{stdin:'-2 0 1 3\n0',expectedStdout:'2'},{stdin:'2 2 2 2\n5',expectedStdout:'4'}] }),
  p({ ...base, slug:'maximum-width-capped-water-with-floor', title:'Tank Between Two Distances', difficulty:'HARD', patternTags:['two-pointers','greedy','converging'], signatureId:'fn:ints,int->int', avgSolveSeconds:900,
    promptMarkdown:['Return the largest container area using posts at distance at most `cap`, but at least 2. The input has at least three heights and `cap >= 2`.','', '**Example**','', '```','input: 1 8 6 2 5','3','output: 15','```','', 'Distance 3 is permitted and distance 1 is not. The output is a number, so equal-area pairs need no output-order rule.'].join('\n'),
    editorialMarkdown:['## Bounded-width endpoint scan','', 'For every right endpoint, only left endpoints in its last `cap` positions can qualify; scan that bounded window and compute each legal area. This keeps the two-index container reasoning explicit while respecting both distance limits. It is preferable here to an unsafe whole-array greedy move because the upper bound removes candidates.', '', 'The quiet mistake is applying the ordinary container algorithm unchanged: it can report a pair wider than cap. Another quiet error includes adjacent posts despite the minimum distance of two. Time is O(n·cap), bounded by input length and cap; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let z=0;for(let r=2;r<a.length;r++)for(let l=Math.max(0,r-b);l<=r-2;l++)z=Math.max(z,Math.min(a[l],a[r])*(r-l));return z;}','function solve(a:number[],b:number):number{let z=0;for(let r=2;r<a.length;r++)for(let l=Math.max(0,r-b);l<=r-2;l++)z=Math.max(z,Math.min(a[l],a[r])*(r-l));return z;}','def solve(a, b):\n    best = 0\n    for r in range(2, len(a)):\n        for l in range(max(0, r-b), r-1): best = max(best, min(a[l],a[r])*(r-l))\n    return best','    static int solve(int[] a, int b) { int z=0;for(int r=2;r<a.length;r++)for(int l=Math.max(0,r-b);l<=r-2;l++)z=Math.max(z,Math.min(a[l],a[r])*(r-l));return z; }','#include <vector>\nusing namespace std;\nint solve(vector<int>a,int b){int z=0;for(int r=2;r<(int)a.size();r++)for(int l=(r-b>0?r-b:0);l<=r-2;l++){int h=a[l]<a[r]?a[l]:a[r];if(h*(r-l)>z)z=h*(r-l);}return z;}','func solve(a []int,b int) int {z:=0;for r:=2;r<len(a);r++{start:=r-b;if start<0{start=0};for l:=start;l<=r-2;l++{h:=a[l];if a[r]<h{h=a[r]};if h*(r-l)>z{z=h*(r-l)}}};return z}'),
    tests:[{stdin:'1 8 6 2 5\n3',expectedStdout:'15',isSample:true},{stdin:'5 1 5\n2',expectedStdout:'10',isSample:true},{stdin:'3 3 3 3\n2',expectedStdout:'6'},{stdin:'1 9 1 9\n2',expectedStdout:'18'}] }),
  p({ ...base, slug:'count-disjoint-pairs-near-target', title:'Use Each Ticket Once Near a Sum', difficulty:'HARD', patternTags:['two-pointers','sorting','greedy'], signatureId:'fn:ints,int->int', avgSolveSeconds:840,
    promptMarkdown:['Count the maximum number of disjoint pairs whose sum is within 1 of `target`. Each input position may belong to at most one pair.','', '**Example**','', '```','input: 1 2 3 4 5','6','output: 2','```','', 'Pairs `(1,5)` and `(2,4)` work. The edge case is a number that could fit several pairs: using it once is required.'].join('\n'),
    editorialMarkdown:['## Sorted extremes greedily consume tickets','', 'Sort the values, then inspect the smallest and largest unused tickets. A sum below the band means the small ticket cannot pair with any remaining value, so discard it. A sum above the band discards the large ticket. Inside the band, consume both and count one pair.', '', 'The quiet mistake is counting all qualifying index pairs, which reuses a ticket and answers a different problem. The sorted extreme argument is what makes the greedy discard safe. Sorting is O(n log n), then pointers take O(n), bounded by list length; the sorted copy uses O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a,b){a=a.slice().sort((x,y)=>x-y);let l=0,r=a.length-1,c=0;while(l<r){let s=a[l]+a[r];if(s<b-1)l++;else if(s>b+1)r--;else{c++;l++;r--;}}return c;}','function solve(a:number[],b:number):number{a=a.slice().sort((x,y)=>x-y);let l=0,r=a.length-1,c=0;while(l<r){const s=a[l]+a[r];if(s<b-1)l++;else if(s>b+1)r--;else{c++;l++;r--;}}return c;}','def solve(a, b):\n    a = sorted(a); l, r, c = 0, len(a)-1, 0\n    while l < r:\n        s = a[l]+a[r]\n        if s < b-1: l += 1\n        elif s > b+1: r -= 1\n        else: c += 1; l += 1; r -= 1\n    return c','    static int solve(int[] a, int b) { java.util.Arrays.sort(a);int l=0,r=a.length-1,c=0;while(l<r){int s=a[l]+a[r];if(s<b-1)l++;else if(s>b+1)r--;else{c++;l++;r--;}}return c; }','#include <algorithm>\n#include <vector>\nusing namespace std;\nint solve(vector<int>a,int b){sort(a.begin(),a.end());int l=0,r=(int)a.size()-1,c=0;while(l<r){int s=a[l]+a[r];if(s<b-1)l++;else if(s>b+1)r--;else{c++;l++;r--;}}return c;}','func solve(a []int,b int) int {for i:=0;i<len(a);i++{for j:=i+1;j<len(a);j++{if a[j]<a[i]{a[i],a[j]=a[j],a[i]}}};l,r,c:=0,len(a)-1,0;for l<r{s:=a[l]+a[r];if s<b-1{l++}else if s>b+1{r--}else{c++;l++;r--}};return c}'),
    tests:[{stdin:'1 2 3 4 5\n6',expectedStdout:'2',isSample:true},{stdin:'1 1 1 1\n2',expectedStdout:'2',isSample:true},{stdin:'1 9 10\n5',expectedStdout:'0'},{stdin:'-2 -1 3 4\n2',expectedStdout:'2'}] }),
];
