import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'GREEDY', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_2_GREEDY_B_PROBLEMS: ProblemDefinition[] = [
  p({...base,slug:'earliest-finish-shift-count',title:'Keep the Earliest Finishing Shifts',difficulty:'EASY',patternTags:['greedy','scheduling','exchange-argument'],signatureId:'fn:matrix->int',avgSolveSeconds:540,
    promptMarkdown:['Each row is a half-open shift `[start, end]`, already sorted by increasing end time. Return the greatest number of mutually compatible shifts; an end equal to the next start is compatible.','','**Example**','','```','input: 1 3;2 4;3 5;5 6','output: 3','```','','There is at least one shift. The named edge case is touching shifts: `[1,3]` and `[3,5]` may both remain.'].join('\n'),
    editorialMarkdown:['## Earliest-finish exchange scheduling','','Keep a shift exactly when its start is at least the end of the last kept shift. Among all available choices, the earliest finish leaves the most room for everything later; replacing any later-finishing first choice with it never invalidates a future compatible choice. That is the exchange argument.','','The quiet mistake is treating touching endpoints as a collision and using `start > end`. These are half-open shifts, so that loses legal work. Time is O(n), bounded by the supplied end-time order; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let e=-1e9,c=0;for(const x of a)if(x[0]>=e){c++;e=x[1]}return c}','function solve(a:number[][]):number{let e=-1e9,c=0;for(const x of a)if(x[0]>=e){c++;e=x[1]}return c}','def solve(a):\n    end=-10**9; count=0\n    for start,finish in a:\n        if start>=end: count+=1; end=finish\n    return count','    static int solve(int[][] a) { int end=-1000000000,count=0; for(int[] x:a) if(x[0]>=end){count++;end=x[1];} return count; }','int solve(vector<vector<int>> a){int e=-1000000000,c=0;for(auto x:a)if(x[0]>=e){c++;e=x[1];}return c;}','func solve(a [][]int)int{e,c:=-1000000000,0;for _,x:=range a{if x[0]>=e{c++;e=x[1]}};return c}'),
    tests:[{stdin:'1 3;2 4;3 5;5 6',expectedStdout:'3',isSample:true},{stdin:'1 2;2 3',expectedStdout:'2',isSample:true},{stdin:'4 9',expectedStdout:'1'},{stdin:'1 10;2 3;3 4;4 5',expectedStdout:'1'}]}),
  p({...base,slug:'minimum-exact-dock-hops',title:'Fewest Hops to the Exact Dock',difficulty:'MEDIUM',patternTags:['greedy','jump-game','exact-landing'],signatureId:'fn:ints->int',avgSolveSeconds:720,
    promptMarkdown:['At index `i`, the positive number gives jump lengths from 1 through that number. Return the fewest hops that land exactly on the last index, or `-1` when impossible.','','**Example**','','```','input: 2 3 1 1 4','output: 2','```','','The list is non-empty and has at most 20 values. A one-position list needs zero hops; jumping past the dock does not count.'].join('\n'),
    editorialMarkdown:['## Greedy breadth layers with an exact boundary','','All positions reached in one more hop form an interval. Scan the current interval, record its farthest legal next position, and only count a hop when the interval ends. Reaching the final index is success; the farthest frontier is clipped to it because overshooting is forbidden.','','The quiet mistake is importing the usual “reach or pass” rule and accepting a jump beyond the last index. That passes many tests until an overshoot-only route appears. Time is O(n), bounded by positions scanned once; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){if(a.length<2)return 0;let h=0,e=0,f=0;for(let i=0;i<a.length-1;i++){f=Math.max(f,Math.min(a.length-1,i+a[i]));if(i==e){h++;e=f;if(e==i)return-1}}return e==a.length-1?h:-1}','function solve(a:number[]):number{if(a.length<2)return 0;let h=0,e=0,f=0;for(let i=0;i<a.length-1;i++){f=Math.max(f,Math.min(a.length-1,i+a[i]));if(i===e){h++;e=f;if(e===i)return-1}}return e===a.length-1?h:-1}','def solve(a):\n    if len(a)<2:return 0\n    hops=end=far=0\n    for i in range(len(a)-1):\n        far=max(far,min(len(a)-1,i+a[i]))\n        if i==end:\n            hops+=1; end=far\n            if end==i:return -1\n    return hops if end==len(a)-1 else -1','    static int solve(int[] a) { if(a.length<2)return 0;int h=0,e=0,f=0;for(int i=0;i<a.length-1;i++){f=Math.max(f,Math.min(a.length-1,i+a[i]));if(i==e){h++;e=f;if(e==i)return -1;}}return e==a.length-1?h:-1; }','int solve(vector<int>a){if(a.size()<2)return 0;int h=0,e=0,f=0;for(int i=0;i<(int)a.size()-1;i++){int v=i+a[i];if(v>(int)a.size()-1)v=a.size()-1;if(v>f)f=v;if(i==e){h++;e=f;if(e==i)return -1;}}return e==(int)a.size()-1?h:-1;}','func solve(a []int)int{if len(a)<2{return 0};h,e,f:=0,0,0;for i:=0;i<len(a)-1;i++{v:=i+a[i];if v>=len(a){v=len(a)-1};if v>f{f=v};if i==e{h++;e=f;if e==i{return -1}}};if e==len(a)-1{return h};return -1}'),
    tests:[{stdin:'2 3 1 1 4',expectedStdout:'2',isSample:true},{stdin:'2 0 0',expectedStdout:'1',isSample:true},{stdin:'0',expectedStdout:'0'},{stdin:'1 0 1',expectedStdout:'-1'}]}),
  p({...base,slug:'longest-closed-letter-piece',title:'Largest Closed Letter Piece',difficulty:'EASY',patternTags:['greedy','partition-labels','strings'],signatureId:'fn:string->int',avgSolveSeconds:540,
    promptMarkdown:['Split a non-empty lowercase string wherever no letter crosses the cut. Return the length of the largest resulting piece.','','**Example**','','```','input: abaccbdeffed','output: 6','```','','The pieces are maximal under that rule. A one-letter string returns 1; repeated letters force their first and last appearances into one piece.'].join('\n'),
    editorialMarkdown:['## Last-occurrence partition frontier','','First record each letter’s last position. While scanning a piece, extend its right boundary to every seen letter’s last position; when the scan meets that boundary, no seen letter can leak right, so a cut is safe. Taking each safe cut immediately gives the required maximal pieces.','','The quiet mistake is closing at the last position of only the first letter. In `abac`, the `b` is harmless but the later `a` extends the piece. Time is O(n), bounded by string length; the fixed alphabet table is O(1) space.'].join('\n'),
    referenceSolution:six('function solve(a){let z={};for(let i=0;i<a.length;i++)z[a[i]]=i;let s=0,e=0,b=0;for(let i=0;i<a.length;i++){e=Math.max(e,z[a[i]]);if(i==e){b=Math.max(b,e-s+1);s=i+1}}return b}','function solve(a:string):number{const z=new Map<string,number>();for(let i=0;i<a.length;i++)z.set(a[i],i);let s=0,e=0,b=0;for(let i=0;i<a.length;i++){e=Math.max(e,z.get(a[i])!);if(i===e){b=Math.max(b,e-s+1);s=i+1}}return b}','def solve(a):\n    last={x:i for i,x in enumerate(a)}; start=end=best=0\n    for i,x in enumerate(a):\n        end=max(end,last[x])\n        if i==end: best=max(best,end-start+1); start=i+1\n    return best','    static int solve(String a) { int[] last=new int[26];for(int i=0;i<a.length();i++)last[a.charAt(i)-97]=i;int s=0,e=0,b=0;for(int i=0;i<a.length();i++){e=Math.max(e,last[a.charAt(i)-97]);if(i==e){b=Math.max(b,e-s+1);s=i+1;}}return b; }','int solve(string a){int z[26]={0},s=0,e=0,b=0;for(int i=0;i<(int)a.size();i++)z[a[i]-97]=i;for(int i=0;i<(int)a.size();i++){if(z[a[i]-97]>e)e=z[a[i]-97];if(i==e){if(e-s+1>b)b=e-s+1;s=i+1;}}return b;}','func solve(a string)int{z:=make([]int,26);for i:=0;i<len(a);i++{z[a[i]-97]=i};s,e,b:=0,0,0;for i:=0;i<len(a);i++{if z[a[i]-97]>e{e=z[a[i]-97]};if i==e{if e-s+1>b{b=e-s+1};s=i+1}};return b}'),
    tests:[{stdin:'abaccbdeffed',expectedStdout:'6',isSample:true},{stdin:'abc',expectedStdout:'1',isSample:true},{stdin:'aaaa',expectedStdout:'4'},{stdin:'eccbbbbdec',expectedStdout:'10'}]}),
  p({...base,slug:'capped-fuel-circuit-start',title:'Circuit Start With a Small Tank',difficulty:'MEDIUM',patternTags:['greedy','gas-station','capacity'],signatureId:'fn:ints,int->int',avgSolveSeconds:780,
    promptMarkdown:['The list alternates fuel gained and fuel spent at each station: `gain0 cost0 gain1 cost1 ...`. Start empty with tank capacity `cap`. Return the smallest start station that completes one circuit without fuel dropping below zero or exceeding cap, or `-1`.','','**Example**','','```','input: 3 2 1 2 2 1','3','output: 0','```','','There are 1 to 10 stations. Fuel gained above capacity is lost, which is the named edge case.'].join('\n'),
    editorialMarkdown:['## Greedy circuit simulation under a cap','','A capacity limit breaks the ordinary total-gas shortcut: excess fuel may be discarded before the next cost. Try each possible start and simulate the one circuit, clamping after every gain. The first successful start is the requested deterministic answer.','','The quiet mistake is adding all gains and costs first. A positive total does not rescue fuel that overflowed earlier. With at most ten stations, the direct O(n²) scan is bounded by those starts and is clearer; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let n=a.length/2;for(let s=0;s<n;s++){let f=0,ok=1;for(let k=0;k<n;k++){let i=(s+k)%n;f=Math.min(b,f+a[2*i])-a[2*i+1];if(f<0){ok=0;break}}if(ok)return s}return-1}','function solve(a:number[],b:number):number{const n=a.length/2;for(let s=0;s<n;s++){let f=0,ok=true;for(let k=0;k<n;k++){const i=(s+k)%n;f=Math.min(b,f+a[2*i])-a[2*i+1];if(f<0){ok=false;break}}if(ok)return s}return-1}','def solve(a,b):\n    n=len(a)//2\n    for s in range(n):\n        fuel=0\n        for k in range(n):\n            i=(s+k)%n; fuel=min(b,fuel+a[2*i])-a[2*i+1]\n            if fuel<0: break\n        else:return s\n    return -1','    static int solve(int[] a, int b) { int n=a.length/2;for(int s=0;s<n;s++){int f=0;boolean ok=true;for(int k=0;k<n;k++){int i=(s+k)%n;f=Math.min(b,f+a[2*i])-a[2*i+1];if(f<0){ok=false;break;}}if(ok)return s;}return -1; }','int solve(vector<int>a,int b){int n=a.size()/2;for(int s=0;s<n;s++){int f=0;bool ok=1;for(int k=0;k<n;k++){int i=(s+k)%n;f=(f+a[2*i]<b?f+a[2*i]:b)-a[2*i+1];if(f<0){ok=0;break;}}if(ok)return s;}return -1;}','func solve(a []int,b int)int{n:=len(a)/2;for s:=0;s<n;s++{f:=0;ok:=true;for k:=0;k<n;k++{i:=(s+k)%n;f+=a[2*i];if f>b{f=b};f-=a[2*i+1];if f<0{ok=false;break}};if ok{return s}};return -1}'),
    tests:[{stdin:'3 2 1 2 2 1\n3',expectedStdout:'0',isSample:true},{stdin:'4 3 1 2\n3',expectedStdout:'-1',isSample:true},{stdin:'1 2 1 2\n5',expectedStdout:'-1'},{stdin:'5 1 1 4\n3',expectedStdout:'-1'}]}),
  p({...base,slug:'limited-coin-fill-value',title:'Best Value From Limited Coin Bins',difficulty:'MEDIUM',patternTags:['greedy','coins','bounded-selection'],signatureId:'fn:ints,int->int',avgSolveSeconds:720,
    promptMarkdown:['The list alternates coin value and available count. Select at most `target` coins and return the largest possible total value. All values are non-negative.','','**Example**','','```','input: 5 1 3 4 8 1','4','output: 20','```','','There are at most 10 bins. You may leave capacity unused when only zero-value coins remain.'].join('\n'),
    editorialMarkdown:['## Exchange selection by value','','Every chosen coin costs one slot, so any lower-value chosen coin can be exchanged with an available higher-value coin and never lowers the answer. Repeatedly take the best remaining bin, one coin at a time, until slots or useful coins run out.','','The quiet mistake is treating the count as value, or taking every bin in input order. A later high-value bin must displace earlier low-value coins. With at most ten bins and target selections, time is O(target · bins), bounded by both inputs; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let r=0;for(let k=0;k<b;k++){let j=-1;for(let i=0;i<a.length;i+=2)if(a[i+1]&&a[i]>=(j<0?-1:a[j]))j=i;if(j<0||a[j]==0)break;r+=a[j];a[j+1]--}return r}','function solve(a:number[],b:number):number{let r=0;for(let k=0;k<b;k++){let j=-1;for(let i=0;i<a.length;i+=2)if(a[i+1]>0&&(j<0||a[i]>a[j]))j=i;if(j<0||a[j]===0)break;r+=a[j];a[j+1]--}return r}','def solve(a,b):\n    total=0\n    for _ in range(b):\n        choices=[i for i in range(0,len(a),2) if a[i+1]>0]\n        if not choices:break\n        j=max(choices,key=lambda i:a[i])\n        if a[j]==0:break\n        total+=a[j];a[j+1]-=1\n    return total','    static int solve(int[] a, int b) { int r=0;for(int k=0;k<b;k++){int j=-1;for(int i=0;i<a.length;i+=2)if(a[i+1]>0&&(j<0||a[i]>a[j]))j=i;if(j<0||a[j]==0)break;r+=a[j];a[j+1]--;}return r; }','int solve(vector<int>a,int b){int r=0;for(int k=0;k<b;k++){int j=-1;for(int i=0;i<(int)a.size();i+=2)if(a[i+1]>0&&(j<0||a[i]>a[j]))j=i;if(j<0||a[j]==0)break;r+=a[j];a[j+1]--;}return r;}','func solve(a []int,b int)int{r:=0;for k:=0;k<b;k++{j:=-1;for i:=0;i<len(a);i+=2{if a[i+1]>0&&(j<0||a[i]>a[j]){j=i}};if j<0||a[j]==0{break};r+=a[j];a[j+1]--};return r}'),
    tests:[{stdin:'5 1 3 4 8 1\n4',expectedStdout:'19',isSample:true},{stdin:'1 5 2 2\n3',expectedStdout:'5',isSample:true},{stdin:'0 4\n3',expectedStdout:'0'},{stdin:'9 1 8 1\n5',expectedStdout:'17'}]}),
  p({...base,slug:'minimum-deadline-interval-cover',title:'Fewest Ranges Before the Deadline',difficulty:'MEDIUM',patternTags:['greedy','interval-covering','deadline'],signatureId:'fn:matrix,int->int',avgSolveSeconds:780,
    promptMarkdown:['Rows are closed coverage ranges `[start,end]`. Starting at point 0, return the fewest ranges that cover every point through `deadline`, or `-1`. A range beginning at the current covered point is allowed.','','**Example**','','```','input: 0 2;1 5;4 6','6','output: 3','```','','Rows are non-empty and at most 10. The edge case is a gap: if no range starts at or before the current frontier, coverage is impossible.'].join('\n'),
    editorialMarkdown:['## Farthest-reaching interval cover','','At each uncovered frontier, inspect every range that has already started and take the one extending farthest right. Any valid solution must choose some such range; exchanging it for the farthest one cannot reduce future coverage. Repeat until the deadline is passed.','','The quiet mistake is choosing the first usable range, which may create needless extra selections or a false gap. The current range must maximize its end. The direct scan is O(n²), bounded by at most ten rows and chosen ranges; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let x=0,c=0;while(x<b){let y=x;for(const z of a)if(z[0]<=x&&z[1]>y)y=z[1];if(y==x)return-1;x=y;c++}return c}','function solve(a:number[][],b:number):number{let x=0,c=0;while(x<b){let y=x;for(const z of a)if(z[0]<=x&&z[1]>y)y=z[1];if(y===x)return-1;x=y;c++}return c}','def solve(a,b):\n    x=count=0\n    while x<b:\n        y=max([x]+[e for s,e in a if s<=x])\n        if y==x:return -1\n        x=y;count+=1\n    return count','    static int solve(int[][] a, int b) { int x=0,c=0;while(x<b){int y=x;for(int[] z:a)if(z[0]<=x&&z[1]>y)y=z[1];if(y==x)return -1;x=y;c++;}return c; }','int solve(vector<vector<int>>a,int b){int x=0,c=0;while(x<b){int y=x;for(auto z:a)if(z[0]<=x&&z[1]>y)y=z[1];if(y==x)return -1;x=y;c++;}return c;}','func solve(a [][]int,b int)int{x,c:=0,0;for x<b{y:=x;for _,z:=range a{if z[0]<=x&&z[1]>y{y=z[1]}};if y==x{return -1};x=y;c++};return c}'),
    tests:[{stdin:'0 2;1 5;4 6\n6',expectedStdout:'3',isSample:true},{stdin:'0 4;2 7\n6',expectedStdout:'2',isSample:true},{stdin:'1 3\n3',expectedStdout:'-1'},{stdin:'0 10\n5',expectedStdout:'1'}]}),
  p({...base,slug:'first-closed-letter-piece-length',title:'Length of the First Closed Letter Piece',difficulty:'EASY',patternTags:['greedy','partition-labels','strings'],signatureId:'fn:string->int',avgSolveSeconds:480,
    promptMarkdown:['A non-empty lowercase string is cut just after the earliest prefix whose letters never appear later. Return that first piece length.','','**Example**','','```','input: abaccbde','output: 6','```','','The named edge case is a string beginning with a repeated later letter: the cut waits for that letter’s final appearance.'].join('\n'),
    editorialMarkdown:['## First last-occurrence frontier','','Record the last index of every letter, then scan only until the running farthest last index is reached. Every letter in that prefix ends inside it, so the cut is safe; ending sooner would leave at least one seen letter outside. This is the first greedy partition boundary.','','The quiet mistake is returning the last index of the first character alone. Other letters can extend the same prefix without being obvious. Time is O(n), bounded by the string length; the alphabet table is O(1) space.'].join('\n'),
    referenceSolution:six('function solve(a){let z={};for(let i=0;i<a.length;i++)z[a[i]]=i;let e=0;for(let i=0;i<a.length;i++){e=Math.max(e,z[a[i]]);if(i==e)return i+1}return 0}','function solve(a:string):number{const z=new Map<string,number>();for(let i=0;i<a.length;i++)z.set(a[i],i);let e=0;for(let i=0;i<a.length;i++){e=Math.max(e,z.get(a[i])!);if(i===e)return i+1}return 0}','def solve(a):\n    last={x:i for i,x in enumerate(a)};end=0\n    for i,x in enumerate(a):\n        end=max(end,last[x])\n        if i==end:return i+1\n    return 0','    static int solve(String a) { int[] z=new int[26];for(int i=0;i<a.length();i++)z[a.charAt(i)-97]=i;int e=0;for(int i=0;i<a.length();i++){e=Math.max(e,z[a.charAt(i)-97]);if(i==e)return i+1;}return 0; }','int solve(string a){int z[26]={0},e=0;for(int i=0;i<(int)a.size();i++)z[a[i]-97]=i;for(int i=0;i<(int)a.size();i++){if(z[a[i]-97]>e)e=z[a[i]-97];if(i==e)return i+1;}return 0;}','func solve(a string)int{z:=make([]int,26);for i:=0;i<len(a);i++{z[a[i]-97]=i};e:=0;for i:=0;i<len(a);i++{if z[a[i]-97]>e{e=z[a[i]-97]};if i==e{return i+1}};return 0}'),
    tests:[{stdin:'abaccbde',expectedStdout:'6',isSample:true},{stdin:'abc',expectedStdout:'1',isSample:true},{stdin:'aaaa',expectedStdout:'4'},{stdin:'eccbbbbdec',expectedStdout:'10'}]}),
  p({...base,slug:'latest-deadline-task-reward',title:'Fill the Most Valuable Deadline Slots',difficulty:'MEDIUM',patternTags:['greedy','scheduling','exchange-argument'],signatureId:'fn:ints->int',avgSolveSeconds:840,
    promptMarkdown:['The list alternates a task deadline and reward. Each task takes one slot, slots are numbered from 1, and a task must finish no later than its deadline. Return the greatest reward.','','**Example**','','```','input: 1 10 2 20 2 15','output: 35','```','','There are at most 10 tasks and positive deadlines. Only one task fits in each slot; equal rewards need no tie output.'].join('\n'),
    editorialMarkdown:['## Reward-first scheduling into the latest slot','','Consider tasks from highest reward down. Put each one into the latest unused slot no later than its deadline; saving earlier slots leaves them available for tighter tasks. If no slot exists, exchanging out a higher reward for this one cannot improve the total.','','The quiet mistake is filling the earliest available slot, which blocks a later task with deadline one. The small bound permits repeatedly selecting the highest unused reward. Time is O(n²), bounded by task count; slot storage is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let n=a.length/2,s=Array(n+1).fill(0),r=0;for(let q=0;q<n;q++){let j=-1;for(let i=0;i<n;i++)if(!a[2*i]&&false){}else if(a[2*i]>0&&(j<0||a[2*i+1]>a[2*j+1]))j=i;if(j<0)break;for(let t=Math.min(n,a[2*j]);t;t--)if(!s[t]){s[t]=1;r+=a[2*j+1];break}a[2*j]=0}return r}','function solve(a:number[]):number{const n=a.length/2,s=new Array(n+1).fill(false);let r=0;for(let q=0;q<n;q++){let j=-1;for(let i=0;i<n;i++)if(a[2*i]>0&&(j<0||a[2*i+1]>a[2*j+1]))j=i;if(j<0)break;for(let t=Math.min(n,a[2*j]);t>0;t--)if(!s[t]){s[t]=true;r+=a[2*j+1];break}a[2*j]=0}return r}','def solve(a):\n    n=len(a)//2; used=[False]*(n+1); total=0\n    for _ in range(n):\n        choices=[i for i in range(n) if a[2*i]>0]\n        if not choices:break\n        j=max(choices,key=lambda i:a[2*i+1])\n        for t in range(min(n,a[2*j]),0,-1):\n            if not used[t]:used[t]=True;total+=a[2*j+1];break\n        a[2*j]=0\n    return total','    static int solve(int[] a) { int n=a.length/2,r=0;boolean[] u=new boolean[n+1];for(int q=0;q<n;q++){int j=-1;for(int i=0;i<n;i++)if(a[2*i]>0&&(j<0||a[2*i+1]>a[2*j+1]))j=i;if(j<0)break;for(int t=Math.min(n,a[2*j]);t>0;t--)if(!u[t]){u[t]=true;r+=a[2*j+1];break;}a[2*j]=0;}return r; }','int solve(vector<int>a){int n=a.size()/2,r=0;vector<int>u(n+1);for(int q=0;q<n;q++){int j=-1;for(int i=0;i<n;i++)if(a[2*i]>0&&(j<0||a[2*i+1]>a[2*j+1]))j=i;if(j<0)break;for(int t=a[2*j]<n?a[2*j]:n;t;t--)if(!u[t]){u[t]=1;r+=a[2*j+1];break;}a[2*j]=0;}return r;}','func solve(a []int)int{n:=len(a)/2;u:=make([]bool,n+1);r:=0;for q:=0;q<n;q++{j:=-1;for i:=0;i<n;i++{if a[2*i]>0&&(j<0||a[2*i+1]>a[2*j+1]){j=i}};if j<0{break};t:=a[2*j];if t>n{t=n};for ;t>0;t--{if !u[t]{u[t]=true;r+=a[2*j+1];break}};a[2*j]=0};return r}'),
    tests:[{stdin:'1 10 2 20 2 15',expectedStdout:'35',isSample:true},{stdin:'1 5 1 8',expectedStdout:'8',isSample:true},{stdin:'3 7',expectedStdout:'7'},{stdin:'1 4 2 3 1 10',expectedStdout:'13'}]}),
  p({...base,slug:'exact-step-landing-count',title:'Count Exact Landing Routes',difficulty:'MEDIUM',patternTags:['greedy','jump-game','exact-landing'],signatureId:'fn:ints->int',avgSolveSeconds:780,
    promptMarkdown:['At index `i`, you may jump exactly the positive value there or exactly one less (when positive). Return how many routes land exactly on the final index, modulo 1,000,000,007.','','**Example**','','```','input: 2 2 1','output: 2','```','','The list has at most 10 values. A jump past the final index is discarded, and the final index itself contributes one completed route.'].join('\n'),
    editorialMarkdown:['## Forward exact-landing accumulation','','Every route reaches a position before it can take one of its two allowed jumps. Accumulate the number of ways at each position and send it only to legal landing positions. The final cell is read, never expanded, so overshoots cannot be mistaken for success.','','The quiet mistake is counting paths that pass the final index. That is natural in reachability variants but violates this landing contract. With n at most ten, the forward scan is O(n), bounded by positions and two transitions; extra space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let d=Array(a.length).fill(0);d[0]=1;for(let i=0;i<a.length-1;i++)for(let x of [a[i],a[i]-1])if(x>0&&i+x<a.length)d[i+x]=(d[i+x]+d[i])%1000000007;return d[a.length-1]}','function solve(a:number[]):number{const d=new Array(a.length).fill(0);d[0]=1;for(let i=0;i<a.length-1;i++)for(const x of [a[i],a[i]-1])if(x>0&&i+x<a.length)d[i+x]=(d[i+x]+d[i])%1000000007;return d[a.length-1]}','def solve(a):\n    d=[0]*len(a);d[0]=1\n    for i in range(len(a)-1):\n        for x in (a[i],a[i]-1):\n            if x>0 and i+x<len(a):d[i+x]=(d[i+x]+d[i])%1000000007\n    return d[-1]','    static int solve(int[] a) { long[] d=new long[a.length];d[0]=1;for(int i=0;i<a.length-1;i++)for(int x:new int[]{a[i],a[i]-1})if(x>0&&i+x<a.length)d[i+x]=(d[i+x]+d[i])%1000000007;return (int)d[a.length-1]; }','int solve(vector<int>a){long long d[11]={1};for(int i=0;i<(int)a.size()-1;i++)for(int x:{a[i],a[i]-1})if(x>0&&i+x<(int)a.size())d[i+x]=(d[i+x]+d[i])%1000000007;return d[a.size()-1];}','func solve(a []int)int{d:=make([]int,len(a));d[0]=1;for i:=0;i<len(a)-1;i++{for _,x:=range []int{a[i],a[i]-1}{if x>0&&i+x<len(a){d[i+x]=(d[i+x]+d[i])%1000000007}}};return d[len(a)-1]}'),
    tests:[{stdin:'2 2 1',expectedStdout:'2',isSample:true},{stdin:'1 1',expectedStdout:'1',isSample:true},{stdin:'3 1 1',expectedStdout:'1'},{stdin:'1 0 1',expectedStdout:'0'}]}),
  p({...base,slug:'largest-safe-fuel-reserve',title:'Largest Reserve After a Capped Circuit',difficulty:'HARD',patternTags:['greedy','gas-station','capacity'],signatureId:'fn:ints,int->int',avgSolveSeconds:900,
    promptMarkdown:['The list alternates gain and cost for a fixed circuit. Starting at station 0 with empty tank and capacity `cap`, return the fuel left after one circuit, or `-1` if any cost cannot be paid.','','**Example**','','```','input: 4 1 4 3 1 2','5','output: 3','```','','Overflow is discarded after each gain. There are at most 10 stations; capacity may make a positive total-gain circuit fail.'].join('\n'),
    editorialMarkdown:['## Capped fuel-state simulation','','There is only one prescribed route, so retain exactly its current usable fuel. Add the station gain, clamp at capacity, then pay its cost. Clamping before payment models the tank rather than imaginary stored fuel, and a negative balance proves failure at that station.','','The quiet mistake is clamping only at the end. That secretly banks overflow and reports a reserve that cannot exist. Time is O(n), bounded by stations; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let f=0;for(let i=0;i<a.length;i+=2){f=Math.min(b,f+a[i])-a[i+1];if(f<0)return-1}return f}','function solve(a:number[],b:number):number{let f=0;for(let i=0;i<a.length;i+=2){f=Math.min(b,f+a[i])-a[i+1];if(f<0)return-1}return f}','def solve(a,b):\n    fuel=0\n    for i in range(0,len(a),2):\n        fuel=min(b,fuel+a[i])-a[i+1]\n        if fuel<0:return -1\n    return fuel','    static int solve(int[] a, int b) { int f=0;for(int i=0;i<a.length;i+=2){f=Math.min(b,f+a[i])-a[i+1];if(f<0)return -1;}return f; }','int solve(vector<int>a,int b){int f=0;for(int i=0;i<(int)a.size();i+=2){f=(f+a[i]<b?f+a[i]:b)-a[i+1];if(f<0)return -1;}return f;}','func solve(a []int,b int)int{f:=0;for i:=0;i<len(a);i+=2{f+=a[i];if f>b{f=b};f-=a[i+1];if f<0{return -1}};return f}'),
    tests:[{stdin:'4 1 4 3 1 2\n5',expectedStdout:'1',isSample:true},{stdin:'2 3\n5',expectedStdout:'-1',isSample:true},{stdin:'10 1\n3',expectedStdout:'2'},{stdin:'2 1 2 1\n2',expectedStdout:'1'}]}),
  p({...base,slug:'bounded-coin-target-remainder',title:'Smallest Remainder With Limited Coins',difficulty:'HARD',patternTags:['greedy','coins','bounded-selection'],signatureId:'fn:ints,int->int',avgSolveSeconds:900,
    promptMarkdown:['The list alternates a positive denomination and its available count, in descending denomination order. Choose coins with total at most `target` and return the smallest nonnegative remainder.','','**Example**','','```','input: 5 1 3 2 1 4','10','output: 0','```','','There are at most 10 bins and target is at most 100. A denomination may be used only up to its displayed limit.'].join('\n'),
    editorialMarkdown:['## Bounded descending coin greedy','','For canonical descending denominations, taking the largest coin that still fits reduces the remaining target as much as any smaller choice, while the count limits stop the loop at each bin. Process the supplied bins in descending denomination order and take as many as fit.','','The quiet mistake is spending coins above the remaining target and then trying to repair the excess. This task forbids totals above target. Time is O(n + selected coins), bounded by bins and their counts; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){for(let i=0;i<a.length;i+=2){let k=Math.min(a[i+1],Math.floor(b/a[i]));b-=k*a[i]}return b}','function solve(a:number[],b:number):number{for(let i=0;i<a.length;i+=2){const k=Math.min(a[i+1],Math.floor(b/a[i]));b-=k*a[i]}return b}','def solve(a,b):\n    for i in range(0,len(a),2):\n        k=min(a[i+1],b//a[i]);b-=k*a[i]\n    return b','    static int solve(int[] a, int b) { for(int i=0;i<a.length;i+=2){int k=Math.min(a[i+1],b/a[i]);b-=k*a[i];}return b; }','int solve(vector<int>a,int b){for(int i=0;i<(int)a.size();i+=2){int k=a[i+1]<b/a[i]?a[i+1]:b/a[i];b-=k*a[i];}return b;}','func solve(a []int,b int)int{for i:=0;i<len(a);i+=2{k:=b/a[i];if a[i+1]<k{k=a[i+1]};b-=k*a[i]};return b}'),
    tests:[{stdin:'5 1 3 2 1 4\n10',expectedStdout:'0',isSample:true},{stdin:'4 1 3 1\n6',expectedStdout:'2',isSample:true},{stdin:'7 2\n5',expectedStdout:'5'},{stdin:'10 1 6 2 1 5\n17',expectedStdout:'0'}]}),
  p({...base,slug:'deadline-cover-with-one-spare',title:'Cover a Deadline and Keep One Spare',difficulty:'HARD',patternTags:['greedy','interval-covering','deadline'],signatureId:'fn:matrix,int->int',avgSolveSeconds:960,
    promptMarkdown:['Rows are closed ranges. Cover from 0 through `deadline` using the fewest ranges, but return `-1` unless the final chosen range extends at least one point beyond the deadline.','','**Example**','','```','input: 0 3;2 6;5 8','7','output: 3','```','','At most 10 ranges are supplied. Exact ending at the deadline is insufficient: the named edge case requires one spare covered point.'].join('\n'),
    editorialMarkdown:['## Farthest cover with a reserve boundary','','Use the same farthest-reaching choice at each frontier, because replacing any usable range with one that reaches farther preserves every later option. The only changed stopping boundary is `deadline + 1`: coverage must reach that point to prove one spare point remains.','','The quiet mistake is stopping at `deadline`, which solves the ordinary cover problem but violates the reserve condition. Direct repeated scanning takes O(n²), bounded by the ten ranges; extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let x=0,c=0;while(x<=b){let y=x;for(const z of a)if(z[0]<=x&&z[1]>y)y=z[1];if(y==x)return-1;x=y;c++}return c}','function solve(a:number[][],b:number):number{let x=0,c=0;while(x<=b){let y=x;for(const z of a)if(z[0]<=x&&z[1]>y)y=z[1];if(y===x)return-1;x=y;c++}return c}','def solve(a,b):\n    x=count=0\n    while x<=b:\n        y=max([x]+[e for s,e in a if s<=x])\n        if y==x:return -1\n        x=y;count+=1\n    return count','    static int solve(int[][] a, int b) { int x=0,c=0;while(x<=b){int y=x;for(int[] z:a)if(z[0]<=x&&z[1]>y)y=z[1];if(y==x)return -1;x=y;c++;}return c; }','int solve(vector<vector<int>>a,int b){int x=0,c=0;while(x<=b){int y=x;for(auto z:a)if(z[0]<=x&&z[1]>y)y=z[1];if(y==x)return -1;x=y;c++;}return c;}','func solve(a [][]int,b int)int{x,c:=0,0;for x<=b{y:=x;for _,z:=range a{if z[0]<=x&&z[1]>y{y=z[1]}};if y==x{return -1};x=y;c++};return c}'),
    tests:[{stdin:'0 3;2 6;5 8\n7',expectedStdout:'3',isSample:true},{stdin:'0 5\n4',expectedStdout:'1',isSample:true},{stdin:'0 4\n4',expectedStdout:'-1'},{stdin:'1 9\n5',expectedStdout:'-1'}]}),
];
