import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w4-063` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-17 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W4_063_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "earliest-common-satellite-signal",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "TWO_POINTERS",
    title: "Earliest Common Signal",
    patternTags: ["two-pointers","arrays","linear-scan"],
    signatureId: "fn:matrix->int",
    avgSolveSeconds: 300,
    promptMarkdown: "Two different satellites recorded the timestamps of meteor strikes. Both lists of timestamps are strictly increasing. We need to find the earliest timestamp that was recorded by *both* satellites.\n\nYou are given a 2D array (matrix) containing exactly two rows. The first row represents the timestamps from satellite A, and the second row represents the timestamps from satellite B. Return the smallest timestamp present in both rows. If there is no common timestamp, return -1.\n\n**Constraints**\n- The input matrix will have exactly 2 rows.\n- Each row will contain between 1 and 10^4 integers.\n- Each integer will be between 1 and 10^9.\n- Each row is sorted in strictly increasing order.\n\n**Example 1**\n```\ninput:\n1 2 3;2 3 4\noutput:\n2\n```\n*Explanation: The earliest timestamp present in both rows is 2.*\n\n**Example 2**\n```\ninput:\n1 5 9;2 4 6\noutput:\n-1\n```\n*Explanation: There is no common timestamp between the two satellites.*\n\n**Example 3**\n```\ninput:\n10;10\noutput:\n10\n```\n*Explanation: Both satellites recorded a strike exactly at timestamp 10.*\n\n**Follow-up**\nCan you solve this with O(1) auxiliary space and O(N + M) time complexity?",
    editorialMarkdown: "## Earliest Common Signal\n\nYou need to find the smallest common value in two sorted arrays.\n\nThe most efficient approach is to use two pointers. Start one pointer at the beginning of the first array and another at the beginning of the second array. Compare the values at these pointers:\n- If they are equal, you have found the earliest common timestamp. Return it.\n- If the value in the first array is smaller, advance the first pointer.\n- If the value in the second array is smaller, advance the second pointer.\n\n**Trap**: Make sure to check for array bounds, as the arrays might have different lengths and you must stop as soon as one pointer goes out of bounds.\n\n**Complexity:**\n- **Time:** O(N + M), where N and M are the lengths of the two arrays.\n- **Space:** O(1), as we only use two variables for the pointers.",
    referenceSolution: {
      JAVASCRIPT: "function solve(matrix) {\n    if (matrix.length !== 2) return -1;\n    let A = matrix[0], B = matrix[1];\n    let i = 0, j = 0;\n    while (i < A.length && j < B.length) {\n        if (A[i] === B[j]) return A[i];\n        else if (A[i] < B[j]) i++;\n        else j++;\n    }\n    return -1;\n}",
      TYPESCRIPT: "function solve(matrix: number[][]): number {\n    if (matrix.length !== 2) return -1;\n    let A = matrix[0], B = matrix[1];\n    let i = 0, j = 0;\n    while (i < A.length && j < B.length) {\n        if (A[i] === B[j]) return A[i];\n        else if (A[i] < B[j]) i++;\n        else j++;\n    }\n    return -1;\n}",
      PYTHON: "def solve(matrix):\n    if len(matrix) != 2: return -1\n    A, B = matrix[0], matrix[1]\n    i, j = 0, 0\n    while i < len(A) and j < len(B):\n        if A[i] == B[j]:\n            return A[i]\n        elif A[i] < B[j]:\n            i += 1\n        else:\n            j += 1\n    return -1",
      JAVA: "    static int solve(int[][] matrix) {\n        if (matrix.length != 2) return -1;\n        int[] A = matrix[0];\n        int[] B = matrix[1];\n        int i = 0, j = 0;\n        while (i < A.length && j < B.length) {\n            if (A[i] == B[j]) return A[i];\n            else if (A[i] < B[j]) i++;\n            else j++;\n        }\n        return -1;\n    }",
      CPP: "int solve(vector<vector<int>> matrix) {\n    if (matrix.size() != 2) return -1;\n    vector<int>& A = matrix[0];\n    vector<int>& B = matrix[1];\n    int i = 0, j = 0;\n    while (i < A.size() && j < B.size()) {\n        if (A[i] == B[j]) return A[i];\n        else if (A[i] < B[j]) i++;\n        else j++;\n    }\n    return -1;\n}",
      GO: "func solve(matrix [][]int) int {\n    if len(matrix) != 2 { return -1 }\n    A := matrix[0]\n    B := matrix[1]\n    i, j := 0, 0\n    for i < len(A) && j < len(B) {\n        if A[i] == B[j] {\n            return A[i]\n        } else if A[i] < B[j] {\n            i++\n        } else {\n            j++\n        }\n    }\n    return -1\n}",
    },
    tests: [
      { stdin: "1 2 3;2 3 4", expectedStdout: "2", isSample: true },
      { stdin: "1 5 9;2 4 6", expectedStdout: "-1", isSample: true },
      { stdin: "10;10", expectedStdout: "10" },
      { stdin: "1;2", expectedStdout: "-1" },
      { stdin: "1 2 3 4 5;5 6 7", expectedStdout: "5" },
      { stdin: "3 4 5;1 2 3", expectedStdout: "3" },
      { stdin: "2 4 6 8 10;1 3 5 7 9", expectedStdout: "-1" },
      { stdin: "1 100;100 200", expectedStdout: "100" },
    ],
  }),

  p({
    ...base,
    slug: "equalize-storage-units",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Equalize Storage Units",
    patternTags: ["math","arrays","greedy"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A cargo ship has multiple identical storage bays, but they currently hold different amounts of fuel. To stabilize the ship, all bays must have exactly the same amount of fuel. The automated system can only vent exactly 1 unit of fuel at a time from a single bay. It cannot add fuel.\n\nGiven an array `nums` representing the current fuel units in each bay, return the minimum number of venting operations required to balance all the bays so they hold an equal amount of fuel.\n\n**Constraints**\n- `nums` will have between 1 and 1000 elements.\n- Each element in `nums` is between 0 and 10^4.\n\n**Example 1**\n```\ninput:\n3 2 1\noutput:\n3\n```\n*Explanation: The bays are stabilized at 1 unit each. Bay 0 vents 2 units, bay 1 vents 1 unit. Total operations = 2 + 1 = 3.*\n\n**Example 2**\n```\ninput:\n1 1 1\noutput:\n0\n```\n*Explanation: All bays already hold equal fuel.*\n\n**Example 3**\n```\ninput:\n5 5 1\noutput:\n8\n```\n*Explanation: The bays are stabilized at 1 unit. Bay 0 vents 4 units, bay 1 vents 4 units. Total operations = 4 + 4 = 8.*\n\n**Follow-up**\nCan you calculate the answer in a single pass over the array?",
    editorialMarkdown: "## Equalize Storage Units\n\nTo minimize operations when we can only decrement values by 1, we should decrement all values until they reach the smallest value in the array.\n\nThe approach is straightforward:\n1. Iterate through the array to find the minimum value.\n2. Iterate through the array again, adding the difference between each element and the minimum value to a running total.\n3. Return the total.\n\n**Trap**: Do not try to decrement step-by-step in a loop, as that will be highly inefficient (O(N * MaxValue)). Calculate the differences mathematically instead.\n\n**Complexity:**\n- **Time:** O(N), where N is the length of the array.\n- **Space:** O(1), as we only need a couple of variables to keep track of the minimum and the sum.",
    referenceSolution: {
      JAVASCRIPT: "function solve(nums) {\n    if (nums.length === 0) return 0;\n    let m = nums[0];\n    for (let x of nums) if (x < m) m = x;\n    let sum = 0;\n    for (let x of nums) sum += x - m;\n    return sum;\n}",
      TYPESCRIPT: "function solve(nums: number[]): number {\n    if (nums.length === 0) return 0;\n    let m = nums[0];\n    for (let x of nums) if (x < m) m = x;\n    let sum = 0;\n    for (let x of nums) sum += x - m;\n    return sum;\n}",
      PYTHON: "def solve(nums):\n    if not nums: return 0\n    m = min(nums)\n    return sum(x - m for x in nums)",
      JAVA: "    static int solve(int[] nums) {\n        if (nums.length == 0) return 0;\n        int m = nums[0];\n        for (int x : nums) if (x < m) m = x;\n        int sum = 0;\n        for (int x : nums) sum += x - m;\n        return sum;\n    }",
      CPP: "int solve(vector<int> nums) {\n    if (nums.empty()) return 0;\n    int m = nums[0];\n    for (int x : nums) if (x < m) m = x;\n    int sum = 0;\n    for (int x : nums) sum += x - m;\n    return sum;\n}",
      GO: "func solve(nums []int) int {\n    if len(nums) == 0 { return 0 }\n    min := nums[0]\n    for _, x := range nums {\n        if x < min { min = x }\n    }\n    sum := 0\n    for _, x := range nums {\n        sum += x - min\n    }\n    return sum\n}",
    },
    tests: [
      { stdin: "3 2 1", expectedStdout: "3", isSample: true },
      { stdin: "1 1 1", expectedStdout: "0", isSample: true },
      { stdin: "5 5 1", expectedStdout: "8" },
      { stdin: "10", expectedStdout: "0" },
      { stdin: "0 0 0", expectedStdout: "0" },
      { stdin: "10 20 30", expectedStdout: "30" },
      { stdin: "100 1 100", expectedStdout: "198" },
      { stdin: "4 2 2 4", expectedStdout: "4" },
    ],
  }),

  p({
    ...base,
    slug: "asteroid-rover-thrusters",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "GREEDY",
    title: "Asteroid Rover Thrusters",
    patternTags: ["greedy","math"],
    signatureId: "fn:int,int->int",
    avgSolveSeconds: 300,
    promptMarkdown: "You control a mining rover on an asteroid. The rover is currently at position `start` and needs to reach a destination at position `target` (where `target >= start`). The rover's forward thrusters can only propel it by exactly 60, 15, 5, or 1 meters in a single burn. It cannot go backwards.\n\nReturn the minimum number of thruster burns required to reach the target exactly.\n\n**Constraints**\n- `start` and `target` are integers between 0 and 10^5.\n- `start <= target`.\n\n**Example 1**\n```\ninput:\n0\n60\noutput:\n1\n```\n*Explanation: One burn of 60 meters gets the rover exactly to the target.*\n\n**Example 2**\n```\ninput:\n10\n85\noutput:\n2\n```\n*Explanation: The distance is 75 meters. The rover can make one burn of 60 meters and one burn of 15 meters.*\n\n**Example 3**\n```\ninput:\n0\n0\noutput:\n0\n```\n*Explanation: The rover is already at the target.*\n\n**Follow-up**\nWould this greedy approach work if the available burns were 60, 40, 5, and 1?",
    editorialMarkdown: "## Asteroid Rover Thrusters\n\nWe need to find the minimum number of jumps to cover a specific distance, choosing from the jump lengths 60, 15, 5, and 1.\n\nBecause the jump lengths are multiples of each other in a way that perfectly supports a greedy strategy (60 is a multiple of 15, 15 is a multiple of 5, 5 is a multiple of 1), we can always take the largest possible jump without worrying about getting stuck in a suboptimal path.\nFirst calculate the total distance `diff = target - start`. Then, divide the distance by 60 to find the number of 60-meter jumps. The remainder is the new distance. Repeat this for 15, 5, and 1.\n\n**Trap**: If the denominations were not well-aligned (e.g., 60, 40, 5, 1), the greedy approach would fail, and dynamic programming would be required. But with these specific values, greedy is safe and optimal.\n\n**Complexity:**\n- **Time:** O(1), as the number of operations does not depend on the input size.\n- **Space:** O(1), since we only use a few variables.",
    referenceSolution: {
      JAVASCRIPT: "function solve(start, target) {\n    let diff = target - start;\n    if (diff < 0) diff = 0;\n    let ans = 0;\n    ans += Math.floor(diff / 60);\n    diff %= 60;\n    ans += Math.floor(diff / 15);\n    diff %= 15;\n    ans += Math.floor(diff / 5);\n    diff %= 5;\n    ans += diff;\n    return ans;\n}",
      TYPESCRIPT: "function solve(start: number, target: number): number {\n    let diff = target - start;\n    if (diff < 0) diff = 0;\n    let ans = 0;\n    ans += Math.floor(diff / 60);\n    diff %= 60;\n    ans += Math.floor(diff / 15);\n    diff %= 15;\n    ans += Math.floor(diff / 5);\n    diff %= 5;\n    ans += diff;\n    return ans;\n}",
      PYTHON: "def solve(start, target):\n    diff = max(0, target - start)\n    ans = 0\n    ans += diff // 60\n    diff %= 60\n    ans += diff // 15\n    diff %= 15\n    ans += diff // 5\n    diff %= 5\n    ans += diff\n    return ans",
      JAVA: "    static int solve(int start, int target) {\n        int diff = target - start;\n        if (diff < 0) diff = 0;\n        int ans = 0;\n        ans += diff / 60;\n        diff %= 60;\n        ans += diff / 15;\n        diff %= 15;\n        ans += diff / 5;\n        diff %= 5;\n        ans += diff;\n        return ans;\n    }",
      CPP: "int solve(int start, int target) {\n    int diff = target - start;\n    if (diff < 0) diff = 0;\n    int ans = 0;\n    ans += diff / 60;\n    diff %= 60;\n    ans += diff / 15;\n    diff %= 15;\n    ans += diff / 5;\n    diff %= 5;\n    ans += diff;\n    return ans;\n}",
      GO: "func solve(start int, target int) int {\n    diff := target - start\n    if diff < 0 { diff = 0 }\n    ans := 0\n    ans += diff / 60\n    diff %= 60\n    ans += diff / 15\n    diff %= 15\n    ans += diff / 5\n    diff %= 5\n    ans += diff\n    return ans\n}",
    },
    tests: [
      { stdin: "0\n60", expectedStdout: "1", isSample: true },
      { stdin: "10\n85", expectedStdout: "2", isSample: true },
      { stdin: "0\n0", expectedStdout: "0" },
      { stdin: "5\n11", expectedStdout: "2" },
      { stdin: "100\n104", expectedStdout: "4" },
      { stdin: "0\n78", expectedStdout: "5" },
      { stdin: "30\n150", expectedStdout: "2" },
      { stdin: "0\n123", expectedStdout: "5" },
    ],
  }),

  p({
    ...base,
    slug: "alien-telegraph-presses",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Alien Telegraph Presses",
    patternTags: ["strings","greedy","math"],
    signatureId: "fn:string->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A vintage alien telegraph system has only 8 active buttons. You want to transmit a word consisting of distinct lowercase English letters. Each button can be mapped to multiple letters, but pressing a button `k` times is required to transmit the `k`-th letter mapped to it.\n\nGiven a string `word` containing only distinct lowercase letters, return the minimum number of total button presses needed to type the word optimally.\n\n**Constraints**\n- `word` has a length between 1 and 26.\n- `word` consists only of distinct lowercase English letters.\n\n**Example 1**\n```\ninput:\nabc\noutput:\n3\n```\n*Explanation: Since there are 8 buttons, we can map 'a', 'b', and 'c' as the first letter of three different buttons. Each will take 1 press, totaling 3 presses.*\n\n**Example 2**\n```\ninput:\nabcdefghi\noutput:\n10\n```\n*Explanation: The first 8 letters can be mapped as the primary letters of the 8 buttons (1 press each, total 8). The 9th letter must be mapped as the secondary letter of one of the buttons (2 presses). Total presses = 8 + 2 = 10.*\n\n**Example 3**\n```\ninput:\na\noutput:\n1\n```\n*Explanation: A single letter requires a single press.*\n\n**Follow-up**\nHow would the problem change if the letters in the word were not distinct?",
    editorialMarkdown: "## Alien Telegraph Presses\n\nThe goal is to map letters to keys such that we minimize the total number of presses. \n\nSince all characters in the given word are distinct, their frequency is exactly 1. We should assign the letters in our word to the \"cheapest\" available slots on the buttons. The first 8 letters mapped will be placed in the 1-press slots. The next 8 letters will be placed in the 2-press slots, and so on. Since the order of mapping distinct characters doesn't matter, we can simply iterate through the word, and for the `i`-th character (0-indexed), its cost will be `(i / 8) + 1`.\n\n**Trap**: Do not overcomplicate this by trying to track which specific button gets which letter. The total cost is only dependent on the length of the string.\n\n**Complexity:**\n- **Time:** O(N), where N is the length of the string, to calculate the sum.\n- **Space:** O(1), as we only need an accumulator variable.",
    referenceSolution: {
      JAVASCRIPT: "function solve(word) {\n    let ans = 0;\n    for (let i = 0; i < word.length; i++) {\n        ans += Math.floor(i / 8) + 1;\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(word: string): number {\n    let ans = 0;\n    for (let i = 0; i < word.length; i++) {\n        ans += Math.floor(i / 8) + 1;\n    }\n    return ans;\n}",
      PYTHON: "def solve(word):\n    ans = 0\n    for i in range(len(word)):\n        ans += (i // 8) + 1\n    return ans",
      JAVA: "    static int solve(String word) {\n        int ans = 0;\n        for (int i = 0; i < word.length(); i++) {\n            ans += (i / 8) + 1;\n        }\n        return ans;\n    }",
      CPP: "int solve(string word) {\n    int ans = 0;\n    for (int i = 0; i < word.length(); i++) {\n        ans += (i / 8) + 1;\n    }\n    return ans;\n}",
      GO: "func solve(word string) int {\n    ans := 0\n    for i := 0; i < len(word); i++ {\n        ans += (i / 8) + 1\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "abc", expectedStdout: "3", isSample: true },
      { stdin: "abcdefghi", expectedStdout: "10", isSample: true },
      { stdin: "a", expectedStdout: "1" },
      { stdin: "abcdefgh", expectedStdout: "8" },
      { stdin: "abcdefghij", expectedStdout: "12" },
      { stdin: "abcdefghijklmnop", expectedStdout: "24" },
      { stdin: "abcdefghijklmnopq", expectedStdout: "27" },
      { stdin: "qwertyuiopasdfghjklzxcvbnm", expectedStdout: "56" },
    ],
  }),

  p({
    ...base,
    slug: "prune-defective-orchids",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Prune Defective Orchids",
    patternTags: ["arrays","counting","linear-scan"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A botanical research station grows rare orchids. Any orchid with a stem length strictly less than `k` centimeters is deemed non-viable and is pruned. \n\nGiven an array `nums` representing the stem lengths of all orchids and an integer `k`, return the number of pruning operations required (each operation prunes one orchid) so that all remaining orchids have a stem length of at least `k`.\n\n**Constraints**\n- `nums` will have between 1 and 10^4 elements.\n- Each element in `nums` is between 0 and 10^9.\n- `k` is between 0 and 10^9.\n\n**Example 1**\n```\ninput:\n2 11 10 1 3\n10\noutput:\n3\n```\n*Explanation: Orchids with lengths 2, 1, and 3 are strictly less than the threshold 10, so they are pruned. Total operations = 3.*\n\n**Example 2**\n```\ninput:\n1 1 2 4 9\n1\noutput:\n0\n```\n*Explanation: All orchids have a length of at least 1, so no pruning is needed.*\n\n**Example 3**\n```\ninput:\n1 1 2 4 9\n10\noutput:\n5\n```\n*Explanation: All 5 orchids are strictly less than 10, so all are pruned.*\n\n**Follow-up**\nCan you modify the original array in-place to only contain the viable orchids?",
    editorialMarkdown: "## Prune Defective Orchids\n\nWe are looking to find how many elements in the array are strictly less than a given threshold `k`.\n\nThis requires a simple linear scan over the array. Initialize a counter to 0. Iterate through every element in `nums`. If the element is strictly less than `k`, increment the counter. After examining all elements, return the counter.\n\n**Trap**: Be careful with the condition: the problem specifies *strictly less than*, so an orchid with length exactly equal to `k` is acceptable and should not be pruned.\n\n**Complexity:**\n- **Time:** O(N), where N is the length of the array, since we check each element exactly once.\n- **Space:** O(1), as only a single integer variable is used for counting.",
    referenceSolution: {
      JAVASCRIPT: "function solve(nums, k) {\n    let ans = 0;\n    for (let x of nums) {\n        if (x < k) ans++;\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(nums: number[], k: number): number {\n    let ans = 0;\n    for (let x of nums) {\n        if (x < k) ans++;\n    }\n    return ans;\n}",
      PYTHON: "def solve(nums, k):\n    ans = 0\n    for x in nums:\n        if x < k:\n            ans += 1\n    return ans",
      JAVA: "    static int solve(int[] nums, int k) {\n        int ans = 0;\n        for (int x : nums) {\n            if (x < k) ans++;\n        }\n        return ans;\n    }",
      CPP: "int solve(vector<int> nums, int k) {\n    int ans = 0;\n    for (int x : nums) {\n        if (x < k) ans++;\n    }\n    return ans;\n}",
      GO: "func solve(nums []int, k int) int {\n    ans := 0\n    for _, x := range nums {\n        if x < k { ans++ }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "2 11 10 1 3\n10", expectedStdout: "3", isSample: true },
      { stdin: "1 1 2 4 9\n1", expectedStdout: "0", isSample: true },
      { stdin: "1 1 2 4 9\n10", expectedStdout: "5" },
      { stdin: "10\n5", expectedStdout: "0" },
      { stdin: "5\n10", expectedStdout: "1" },
      { stdin: "1 2 3 4 5\n3", expectedStdout: "2" },
      { stdin: "10 20 30 40 50\n25", expectedStdout: "2" },
      { stdin: "100 100 100\n101", expectedStdout: "3" },
    ],
  }),

  p({
    ...base,
    slug: "symmetric-packet-headers",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "TWO_POINTERS",
    title: "Symmetric Packet Headers",
    patternTags: ["strings","two-pointers","greedy"],
    signatureId: "fn:string->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A space communication protocol requires all packet headers to be symmetric (a palindrome) to easily detect corruption. A header is represented as a string of `'0'`s and `'1'`s. You can correct a header by flipping any `'0'` to a `'1'` or any `'1'` to a `'0'`.\n\nGiven a string `s`, return the minimum number of flips needed to make the header perfectly symmetric (meaning the string reads the same forwards and backwards).\n\n**Constraints**\n- The string length is between 1 and 10^4.\n- The string consists only of characters `'0'` and `'1'`.\n\n**Example 1**\n```\ninput:\n1001\noutput:\n0\n```\n*Explanation: The string is already a palindrome, so no flips are needed.*\n\n**Example 2**\n```\ninput:\n1000\noutput:\n1\n```\n*Explanation: You can flip the last character to '1' to make the string \"1001\", which is a palindrome.*\n\n**Example 3**\n```\ninput:\n0000\noutput:\n0\n```\n*Explanation: The string is perfectly symmetric.*\n\n**Follow-up**\nWhat is the time complexity of your solution?",
    editorialMarkdown: "## Symmetric Packet Headers\n\nTo make a string symmetric with the minimum number of flips, we need to compare characters from the beginning and the end, moving towards the center.\n\nUse two pointers, one at the start of the string (`left = 0`) and one at the end (`right = n - 1`). Check if `s[left]` matches `s[right]`. If they do not match, one flip is required to make them equal. Then increment `left` and decrement `right`. Stop when the pointers cross.\n\n**Trap**: Do not try to generate all possible palindromes. Only focus on the mismatched symmetric pairs.\n\n**Complexity:**\n- **Time:** O(N), where N is the length of the string, as we only need to inspect each character pair once.\n- **Space:** O(1), no extra memory is needed.",
    referenceSolution: {
      JAVASCRIPT: "function solve(s) {\n    let ans = 0;\n    let n = s.length;\n    for (let i = 0; i < Math.floor(n / 2); i++) {\n        if (s[i] !== s[n - 1 - i]) ans++;\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(s: string): number {\n    let ans = 0;\n    let n = s.length;\n    for (let i = 0; i < Math.floor(n / 2); i++) {\n        if (s[i] !== s[n - 1 - i]) ans++;\n    }\n    return ans;\n}",
      PYTHON: "def solve(s):\n    ans = 0\n    n = len(s)\n    for i in range(n // 2):\n        if s[i] != s[n - 1 - i]:\n            ans += 1\n    return ans",
      JAVA: "    static int solve(String s) {\n        int ans = 0;\n        int n = s.length();\n        for (int i = 0; i < n / 2; i++) {\n            if (s.charAt(i) != s.charAt(n - 1 - i)) ans++;\n        }\n        return ans;\n    }",
      CPP: "int solve(string s) {\n    int ans = 0;\n    int n = s.length();\n    for (int i = 0; i < n / 2; i++) {\n        if (s[i] != s[n - 1 - i]) ans++;\n    }\n    return ans;\n}",
      GO: "func solve(s string) int {\n    ans := 0\n    n := len(s)\n    for i := 0; i < n/2; i++ {\n        if s[i] != s[n-1-i] {\n            ans++\n        }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "1001", expectedStdout: "0", isSample: true },
      { stdin: "1000", expectedStdout: "1", isSample: true },
      { stdin: "0000", expectedStdout: "0", isSample: true },
      { stdin: "1", expectedStdout: "0" },
      { stdin: "10", expectedStdout: "1" },
      { stdin: "111000", expectedStdout: "3" },
      { stdin: "010101", expectedStdout: "3" },
      { stdin: "100101", expectedStdout: "1" },
    ],
  }),
];
