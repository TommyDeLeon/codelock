import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'BACKTRACKING', provenance: AUTHORED } as const;

export const TIER_2_BACKTRACKING_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug: 'nonempty-subsets-hitting-target', title: 'Hit the Target Without Standing Still', difficulty: 'MEDIUM', patternTags: ['backtracking', 'subsets', 'counting'], signatureId: 'fn:ints,int->int', avgSolveSeconds: 660,
    promptMarkdown: ['Count the non-empty picks whose values add to the target. Positions are distinct, even when values match.', '', '**Example**', '', '```', 'input:  1 2 3', '        3', 'output: 2', '```', '', 'The picks are `3` and `1 2`. Values may be negative or zero. The list has at most 10 values. Unlike the parent task, the empty pick never counts: an empty line with target `0` returns `0`.'].join('\n'),
    editorialMarkdown: ['## Backtracking: carry one more piece of state', '', 'The parent subset walk tests only the running sum at a leaf. Its memorised version quietly accepts the empty subset when the target is zero. This variation needs a second state: how many positions were chosen. At every number, recurse once after choosing it and once after skipping it; only count a leaf when `sum == target` and `used > 0`.', '', 'The quiet mistake is trying to subtract one from the final count whenever the target is zero. That happens to work only when the list itself is empty; a non-empty collection of zeroes has many non-empty zero-sum picks. Values may be negative, so no overshoot prune is sound. The binary tree takes O(2^n) time and O(n) stack space, with n at most 10.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a,b){let c=0;function f(i,s,k){if(i===a.length){if(k&&s===b)c++;return;}f(i+1,s+a[i],k+1);f(i+1,s,k)}f(0,0,0);return c}',
      TYPESCRIPT: 'function solve(a:number[],b:number):number{let c=0;function f(i:number,s:number,k:number):void{if(i===a.length){if(k>0&&s===b)c++;return;}f(i+1,s+a[i],k+1);f(i+1,s,k)}f(0,0,0);return c}',
      PYTHON: 'def solve(a, b):\n    def f(i, s, k):\n        if i == len(a): return 1 if k and s == b else 0\n        return f(i+1, s+a[i], k+1) + f(i+1, s, k)\n    return f(0, 0, 0)',
      JAVA: '    static int solve(int[] a,int b){return fNonempty(a,b,0,0,0);}\n    static int fNonempty(int[] a,int b,int i,int s,int k){if(i==a.length)return k>0&&s==b?1:0;return fNonempty(a,b,i+1,s+a[i],k+1)+fNonempty(a,b,i+1,s,k);}',
      CPP: 'int fNonempty(const vector<int>&a,int b,int i,int s,int k){if(i==(int)a.size())return k>0&&s==b;return fNonempty(a,b,i+1,s+a[i],k+1)+fNonempty(a,b,i+1,s,k);}\nint solve(vector<int>a,int b){return fNonempty(a,b,0,0,0);}',
      GO: 'func solve(a []int,b int) int { var f func(int,int,int)int; f=func(i,s,k int)int{if i==len(a){if k>0&&s==b{return 1};return 0};return f(i+1,s+a[i],k+1)+f(i+1,s,k)};return f(0,0,0) }',
    }, tests: [{ stdin:'1 2 3\n3',expectedStdout:'2',isSample:true},{stdin:'0 0\n0',expectedStdout:'3',isSample:true},{stdin:'\n0',expectedStdout:'0'},{stdin:'-1 1 2\n0',expectedStdout:'1'},{stdin:'5\n5',expectedStdout:'1'},{stdin:'1 2\n9',expectedStdout:'0'}] }),

  p({ ...base, slug: 'count-nonadjacent-subsets', title: 'Picks With a Seat Between', difficulty: 'MEDIUM', patternTags: ['backtracking', 'subsets', 'constraints'], signatureId: 'fn:ints->int', avgSolveSeconds: 660,
    promptMarkdown: ['Count all subsets of the list that never use two neighbouring positions. The empty subset counts.', '', '**Example**', '', '```', 'input:  4 5 6', 'output: 5', '```', '', 'They are empty, each one-value pick, and `4 6`. Equal values still belong to separate positions. The non-empty list has at most 10 values; a one-value list returns `2`.'].join('\n'),
    editorialMarkdown: ['## Backtracking with a forced skip', '', 'Ordinary subset code branches take/skip at every index and would include illegal neighbours. The twist changes the take branch: after choosing index `i`, recurse at `i + 2`; after skipping it, recurse at `i + 1`. That one shifted argument records the constraint without a shared buffer.', '', 'The quiet mistake is counting valid-looking value sets instead of position sets, which loses answers for repeated values. Another common error is advancing only one slot after a take, silently permitting adjacent picks. There are still at most two calls per level, so time is O(2^n) and stack space O(n), bounded by n <= 10.'].join('\n'),
    referenceSolution: { JAVASCRIPT:'function solve(a){function f(i){if(i>=a.length)return 1;return f(i+1)+f(i+2)}return f(0)}', TYPESCRIPT:'function solve(a:number[]):number{function f(i:number):number{if(i>=a.length)return 1;return f(i+1)+f(i+2)}return f(0)}', PYTHON:'def solve(a):\n    def f(i): return 1 if i >= len(a) else f(i+1) + f(i+2)\n    return f(0)', JAVA:'    static int solve(int[] a){return fSeats(a,0);}\n    static int fSeats(int[] a,int i){return i>=a.length?1:fSeats(a,i+1)+fSeats(a,i+2);}', CPP:'int fSeats(const vector<int>&a,int i){return i>=(int)a.size()?1:fSeats(a,i+1)+fSeats(a,i+2);}\nint solve(vector<int>a){return fSeats(a,0);}', GO:'func solve(a []int) int { var f func(int)int; f=func(i int)int{if i>=len(a){return 1};return f(i+1)+f(i+2)};return f(0) }' }, tests:[{stdin:'4 5 6',expectedStdout:'5',isSample:true},{stdin:'1 1 1 1',expectedStdout:'8',isSample:true},{stdin:'9',expectedStdout:'2'},{stdin:'1 2',expectedStdout:'3'},{stdin:'1 2 3 4 5',expectedStdout:'13'},{stdin:'0 0 0',expectedStdout:'5'}] }),

  p({ ...base, slug: 'count-deranged-orderings', title: 'Every Tile Must Move', difficulty: 'MEDIUM', patternTags: ['backtracking', 'permutations', 'constraints'], signatureId: 'fn:ints->int', avgSolveSeconds: 720,
    promptMarkdown: ['Count the orderings of a list of distinct values where no value remains in its original position. The input is sorted and holds 1 to 8 values.', '', '**Example**', '', '```', 'input:  1 2 3', 'output: 2', '```', '', 'The valid orderings are `2 3 1` and `3 1 2`. A one-value list has no valid ordering, so it returns `0`.'].join('\n'),
    editorialMarkdown: ['## Backtracking: permutations with a forbidden choice', '', 'The parent permutation search accepts every unused position. Here depth `d` is the output position, so choosing original position `d` is forbidden. Keep a shared `used` array, try every other unused index, recurse, then undo the mark. The parent’s solution would count all `n!` orders because it never checks this new per-depth constraint.', '', 'The quiet mistake is comparing values to the depth instead of comparing the chosen original index; that only accidentally works for special input values. Do not forget to clear `used[i]`, or later branches inherit a tile. The search is O(n!) time and O(n) space; n is capped at 8.'].join('\n'),
    referenceSolution: { JAVASCRIPT:'function solve(a){let u=Array(a.length).fill(false);function f(d){if(d===a.length)return 1;let z=0;for(let i=0;i<a.length;i++)if(!u[i]&&i!==d){u[i]=true;z+=f(d+1);u[i]=false}return z}return f(0)}', TYPESCRIPT:'function solve(a:number[]):number{const u:boolean[]=Array(a.length).fill(false);function f(d:number):number{if(d===a.length)return 1;let z=0;for(let i=0;i<a.length;i++)if(!u[i]&&i!==d){u[i]=true;z+=f(d+1);u[i]=false}return z}return f(0)}', PYTHON:'def solve(a):\n    u=[False]*len(a)\n    def f(d):\n        if d==len(a): return 1\n        z=0\n        for i in range(len(a)):\n            if not u[i] and i!=d:\n                u[i]=True; z+=f(d+1); u[i]=False\n        return z\n    return f(0)', JAVA:'    static int solve(int[] a){return fDerange(a,new boolean[a.length],0);}\n    static int fDerange(int[]a,boolean[]u,int d){if(d==a.length)return 1;int z=0;for(int i=0;i<a.length;i++)if(!u[i]&&i!=d){u[i]=true;z+=fDerange(a,u,d+1);u[i]=false;}return z;}', CPP:'int fDerange(const vector<int>&a,vector<char>&u,int d){if(d==(int)a.size())return 1;int z=0;for(int i=0;i<(int)a.size();i++)if(!u[i]&&i!=d){u[i]=1;z+=fDerange(a,u,d+1);u[i]=0;}return z;}\nint solve(vector<int>a){vector<char>u(a.size());return fDerange(a,u,0);}', GO:'func solve(a []int) int {u:=make([]bool,len(a));var f func(int)int;f=func(d int)int{if d==len(a){return 1};z:=0;for i:=0;i<len(a);i++{if !u[i]&&i!=d{u[i]=true;z+=f(d+1);u[i]=false}};return z};return f(0)}' }, tests:[{stdin:'1 2 3',expectedStdout:'2',isSample:true},{stdin:'1 2 3 4',expectedStdout:'9',isSample:true},{stdin:'7',expectedStdout:'0'},{stdin:'-2 4',expectedStdout:'1'},{stdin:'1 2 3 4 5',expectedStdout:'44'},{stdin:'1 2 3 4 5 6',expectedStdout:'265'}] }),

  p({ ...base, slug: 'count-one-turn-grid-routes', title: 'A Route That Bends Once', difficulty: 'MEDIUM', patternTags: ['backtracking', 'grid', 'constraints'], signatureId: 'fn:ints,int->int', avgSolveSeconds: 660,
    promptMarkdown: ['The first line is a flattened open/blocked grid and the second is its width. Count routes from top-left to bottom-right moving right or down that change direction exactly once. `1` is blocked and `0` open.', '', '**Example**', '', '```', 'input:  0 0 0 0', '        2', 'output: 2', '```', '', 'Right then down and down then right each bend once. The grid is non-empty, rectangular, and at most 4 by 4. If start or finish is blocked, return `0`; a one-cell grid returns `0` because it makes no turn.'].join('\n'),
    editorialMarkdown: ['## Backtracking with direction state', '', 'The parent grid walk counts every right/down route. This variation carries the previous move and a turn count. Taking a move different from the previous direction adds one turn; prune when it exceeds one, and count only at the finish with exactly one. Passing these small values as arguments makes undo automatic.', '', 'The quiet mistake is treating the first move as a turn. It has no previous direction, so it must leave the count at zero. Do not assume an open one-cell grid qualifies: it has no direction change. At most 4 by 4 keeps the O(2^(h+w)) route tree tiny; stack space is O(h+w).'].join('\n'),
    referenceSolution: { JAVASCRIPT:'function solve(a,w){let h=a.length/w,c=0;function f(r,x,d,t){if(r>=h||x>=w||a[r*w+x])return;if(r===h-1&&x===w-1){if(t===1)c++;return}f(r+1,x,0,t+(d===1?1:0));f(r,x+1,1,t+(d===0?1:0))}f(0,0,2,0);return c}', TYPESCRIPT:'function solve(a:number[],w:number):number{const h=a.length/w;let c=0;function f(r:number,x:number,d:number,t:number):void{if(r>=h||x>=w||a[r*w+x])return;if(r===h-1&&x===w-1){if(t===1)c++;return}f(r+1,x,0,t+(d===1?1:0));f(r,x+1,1,t+(d===0?1:0))}f(0,0,2,0);return c}', PYTHON:'def solve(a, w):\n    h=len(a)//w\n    def f(r,c,d,t):\n        if r>=h or c>=w or a[r*w+c]: return 0\n        if r==h-1 and c==w-1: return int(t==1)\n        return f(r+1,c,0,t+(d==1))+f(r,c+1,1,t+(d==0))\n    return f(0,0,2,0)', JAVA:'    static int solve(int[]a,int w){return fTurn(a,w,a.length/w,0,0,2,0);}\n    static int fTurn(int[]a,int w,int h,int r,int c,int d,int t){if(r>=h||c>=w||a[r*w+c]==1)return 0;if(r==h-1&&c==w-1)return t==1?1:0;return fTurn(a,w,h,r+1,c,0,t+(d==1?1:0))+fTurn(a,w,h,r,c+1,1,t+(d==0?1:0));}', CPP:'int fTurn(const vector<int>&a,int w,int h,int r,int c,int d,int t){if(r>=h||c>=w||a[r*w+c])return 0;if(r==h-1&&c==w-1)return t==1;return fTurn(a,w,h,r+1,c,0,t+(d==1))+fTurn(a,w,h,r,c+1,1,t+(d==0));}\nint solve(vector<int>a,int w){return fTurn(a,w,a.size()/w,0,0,2,0);}', GO:'func solve(a []int,w int)int{h:=len(a)/w;var f func(int,int,int,int)int;f=func(r,c,d,t int)int{if r>=h||c>=w||a[r*w+c]==1{return 0};if r==h-1&&c==w-1{if t==1{return 1};return 0};z:=0;if d==1{z=f(r+1,c,0,t+1)}else{z=f(r+1,c,0,t)};if d==0{z+=f(r,c+1,1,t+1)}else{z+=f(r,c+1,1,t)};return z};return f(0,0,2,0)}' }, tests:[{stdin:'0 0 0 0\n2',expectedStdout:'2',isSample:true},{stdin:'0 0 0 0 0 0 0 0 0\n3',expectedStdout:'2',isSample:true},{stdin:'0\n1',expectedStdout:'0'},{stdin:'1 0 0 0\n2',expectedStdout:'0'},{stdin:'0 0 0 1\n2',expectedStdout:'0'},{stdin:'0 0 0 0 0 0\n3',expectedStdout:'2'}] }),

  p({ ...base, slug: 'can-split-into-equal-sized-halves', title: 'Balance With Equal Headcounts', difficulty: 'MEDIUM', patternTags: ['backtracking', 'subset-sum', 'constraints'], signatureId: 'fn:ints->bool', avgSolveSeconds: 720,
    promptMarkdown: ['Return `true` if the numbers can be divided into two groups with the same sum and the same number of positions. Every value goes in one group. The list has 1 to 10 non-negative values.', '', '**Example**', '', '```', 'input:  1 2 3 4', 'output: true', '```', '', 'Choose `1 4` for one two-item group and `2 3` for the other. An odd-length list returns `false`; a single `0` is therefore `false`, unlike the parent problem.'].join('\n'),
    editorialMarkdown: ['## Backtracking: sum plus size', '', 'Equal total alone reduces to subset sum, which is the parent’s whole state. The twist requires the chosen subset to have exactly half the positions too. Reject odd lengths or odd totals, then choose/skip each value while carrying both `sum` and `count`; success is exactly `sum == halfSum && count == n/2`.', '', 'The quiet mistake is returning true the moment the sum reaches half: it can use the wrong number of values and leave an impossible remainder. Because values are non-negative, sums above half and counts above n/2 are safe prunes. The tree is O(2^n) time and O(n) space for n <= 10.'].join('\n'),
    referenceSolution:{JAVASCRIPT:'function solve(a){let s=a.reduce((x,y)=>x+y,0),n=a.length;if(n%2||s%2)return false;function f(i,x,k){if(x>s/2||k>n/2)return false;if(i===n)return x===s/2&&k===n/2;return f(i+1,x+a[i],k+1)||f(i+1,x,k)}return f(0,0,0)}',TYPESCRIPT:'function solve(a:number[]):boolean{let s=a.reduce((x,y)=>x+y,0),n=a.length;if(n%2!==0||s%2!==0)return false;function f(i:number,x:number,k:number):boolean{if(x>s/2||k>n/2)return false;if(i===n)return x===s/2&&k===n/2;return f(i+1,x+a[i],k+1)||f(i+1,x,k)}return f(0,0,0)}',PYTHON:'def solve(a):\n    s=sum(a); n=len(a)\n    if n%2 or s%2: return False\n    def f(i,x,k):\n        if x>s//2 or k>n//2: return False\n        if i==n: return x==s//2 and k==n//2\n        return f(i+1,x+a[i],k+1) or f(i+1,x,k)\n    return f(0,0,0)',JAVA:'    static boolean solve(int[]a){int s=0;for(int x:a)s+=x;if(a.length%2==1||s%2==1)return false;return fHalf(a,0,0,0,s/2,a.length/2);}\n    static boolean fHalf(int[]a,int i,int s,int k,int h,int m){if(s>h||k>m)return false;if(i==a.length)return s==h&&k==m;return fHalf(a,i+1,s+a[i],k+1,h,m)||fHalf(a,i+1,s,k,h,m);}',CPP:'bool fHalf(const vector<int>&a,int i,int s,int k,int h,int m){if(s>h||k>m)return false;if(i==(int)a.size())return s==h&&k==m;return fHalf(a,i+1,s+a[i],k+1,h,m)||fHalf(a,i+1,s,k,h,m);}\nbool solve(vector<int>a){int s=0;for(int x:a)s+=x;return a.size()%2==0&&s%2==0&&fHalf(a,0,0,0,s/2,a.size()/2);}',GO:'func solve(a []int)bool{s:=0;for _,x:=range a{s+=x};n:=len(a);if n%2==1||s%2==1{return false};var f func(int,int,int)bool;f=func(i,x,k int)bool{if x>s/2||k>n/2{return false};if i==n{return x==s/2&&k==n/2};return f(i+1,x+a[i],k+1)||f(i+1,x,k)};return f(0,0,0)}'},tests:[{stdin:'1 2 3 4',expectedStdout:'true',isSample:true},{stdin:'1 1 1 3',expectedStdout:'false',isSample:true},{stdin:'0',expectedStdout:'false'},{stdin:'0 0',expectedStdout:'true'},{stdin:'1 2 3',expectedStdout:'false'},{stdin:'2 2 2 2',expectedStdout:'true'}]}),

  p({ ...base, slug: 'count-combinations-using-values-at-most-twice', title: 'Reach the Total, Twice Each', difficulty: 'MEDIUM', patternTags: ['backtracking', 'combinations', 'bounded'], signatureId: 'fn:ints,int->int', avgSolveSeconds: 720,
    promptMarkdown:['Count unordered collections that reach the target. Each distinct positive input value may be used zero, one, or two times, never more. The list is sorted, distinct, has at most 8 values, and the target is 0 to 30.', '', '**Example**', '', '```', 'input:  2 3 5', '        10', 'output: 2', '```', '', 'The collections are `5 5` and `2 3 5`. Target `0` has one collection: use nothing; an unreachable target returns `0`.'].join('\n'),
    editorialMarkdown:['## Backtracking over multiplicities', '', 'The parent combination recursion stays at index `i`, allowing unlimited reuse. That memorised call is exactly what violates this twist. At each index, choose its multiplicity explicitly: zero, one, or two, then recurse to `i + 1`. This canonical index order means no ordering duplicates are produced.', '', 'The quiet mistake is calling the next level after one use but then accidentally allowing it again through a loop, restoring the unlimited parent problem. Values are positive, so a remaining total below zero is a safe prune. There are three choices per value: O(3^n) time and O(n) stack space, with n <= 8.'].join('\n'),
    referenceSolution:{JAVASCRIPT:'function solve(a,b){function f(i,r){if(r<0)return 0;if(i===a.length)return r===0?1:0;return f(i+1,r)+f(i+1,r-a[i])+f(i+1,r-2*a[i])}return f(0,b)}',TYPESCRIPT:'function solve(a:number[],b:number):number{function f(i:number,r:number):number{if(r<0)return 0;if(i===a.length)return r===0?1:0;return f(i+1,r)+f(i+1,r-a[i])+f(i+1,r-2*a[i])}return f(0,b)}',PYTHON:'def solve(a, b):\n    def f(i,r):\n        if r<0: return 0\n        if i==len(a): return int(r==0)\n        return f(i+1,r)+f(i+1,r-a[i])+f(i+1,r-2*a[i])\n    return f(0,b)',JAVA:'    static int solve(int[]a,int b){return fTwice(a,0,b);}\n    static int fTwice(int[]a,int i,int r){if(r<0)return 0;if(i==a.length)return r==0?1:0;return fTwice(a,i+1,r)+fTwice(a,i+1,r-a[i])+fTwice(a,i+1,r-2*a[i]);}',CPP:'int fTwice(const vector<int>&a,int i,int r){if(r<0)return 0;if(i==(int)a.size())return r==0;return fTwice(a,i+1,r)+fTwice(a,i+1,r-a[i])+fTwice(a,i+1,r-2*a[i]);}\nint solve(vector<int>a,int b){return fTwice(a,0,b);}',GO:'func solve(a []int,b int)int{var f func(int,int)int;f=func(i,r int)int{if r<0{return 0};if i==len(a){if r==0{return 1};return 0};return f(i+1,r)+f(i+1,r-a[i])+f(i+1,r-2*a[i])};return f(0,b)}'},tests:[{stdin:'2 3 5\n10',expectedStdout:'2',isSample:true},{stdin:'1 2\n3',expectedStdout:'1',isSample:true},{stdin:'1 2\n0',expectedStdout:'1'},{stdin:'2\n6',expectedStdout:'0'},{stdin:'2 4\n8',expectedStdout:'1'},{stdin:'1 3 4\n6',expectedStdout:'2'}]}),

  p({ ...base, slug: 'count-full-tile-arrangements', title: 'Use Every Tile', difficulty: 'MEDIUM', patternTags: ['backtracking', 'permutations', 'counting'], signatureId: 'fn:string->int', avgSolveSeconds: 720,
    promptMarkdown:['Count the distinct sequences made by using every lowercase letter tile exactly once. Repeated letters are indistinguishable. The line has 1 to 8 letters.', '', '**Example**', '', '```', 'input:  aab', 'output: 3', '```', '', 'The sequences are `aab`, `aba`, and `baa`. A line of all equal letters has exactly one full arrangement.'].join('\n'),
    editorialMarkdown:['## Backtracking to a fixed depth', '', 'The parent tile task adds one answer at every partial sequence, because it permits stopping early. This variation counts only when no tiles remain. Keep letter counts, choose any available letter, decrement, recurse, and restore. A full root-to-leaf path spells one distinct arrangement without needing a duplicate set.', '', 'The quiet mistake is retaining the parent’s `1 + walk()` and counting prefixes that did not use every tile. Another is iterating positions instead of letter counts, which duplicates repeated letters. The tree is O(n!) in the worst case and the stack is O(n); n <= 8.'].join('\n'),
    referenceSolution:{JAVASCRIPT:'function solve(a){let c=Array(26).fill(0);for(let x of a)c[x.charCodeAt(0)-97]++;function f(k){if(k===a.length)return 1;let z=0;for(let i=0;i<26;i++)if(c[i]){c[i]--;z+=f(k+1);c[i]++}return z}return f(0)}',TYPESCRIPT:'function solve(a:string):number{const c:number[]=Array(26).fill(0);for(const x of a)c[x.charCodeAt(0)-97]++;function f(k:number):number{if(k===a.length)return 1;let z=0;for(let i=0;i<26;i++)if(c[i]){c[i]--;z+=f(k+1);c[i]++}return z}return f(0)}',PYTHON:'def solve(a):\n    c=[0]*26\n    for x in a: c[ord(x)-97]+=1\n    def f(k):\n        if k==len(a): return 1\n        z=0\n        for i in range(26):\n            if c[i]: c[i]-=1; z+=f(k+1); c[i]+=1\n        return z\n    return f(0)',JAVA:"    static int solve(String a){int[]c=new int[26];for(int i=0;i<a.length();i++)c[a.charAt(i)-'a']++;return fFull(c,a.length(),0);}\n    static int fFull(int[]c,int n,int k){if(k==n)return 1;int z=0;for(int i=0;i<26;i++)if(c[i]>0){c[i]--;z+=fFull(c,n,k+1);c[i]++;}return z;}",CPP:"int fFull(vector<int>&c,int n,int k){if(k==n)return 1;int z=0;for(int i=0;i<26;i++)if(c[i]){c[i]--;z+=fFull(c,n,k+1);c[i]++;}return z;}\nint solve(string a){vector<int>c(26);for(char x:a)c[x-'a']++;return fFull(c,a.size(),0);}",GO:"func solve(a string)int{c:=make([]int,26);for i:=0;i<len(a);i++{c[a[i]-'a']++};var f func(int)int;f=func(k int)int{if k==len(a){return 1};z:=0;for i:=0;i<26;i++{if c[i]>0{c[i]--;z+=f(k+1);c[i]++}};return z};return f(0)}"},tests:[{stdin:'aab',expectedStdout:'3',isSample:true},{stdin:'abc',expectedStdout:'6',isSample:true},{stdin:'a',expectedStdout:'1'},{stdin:'aaaa',expectedStdout:'1'},{stdin:'aabb',expectedStdout:'6'},{stdin:'abca',expectedStdout:'12'}]}),

  p({ ...base, slug: 'count-queens-with-anchored-first-row', title: 'Queens From a Fixed Start', difficulty: 'HARD', patternTags: ['backtracking', 'queens', 'constraints'], signatureId: 'fn:ints->int', avgSolveSeconds: 840,
    promptMarkdown:['The input has two integers: board size `n`, then the required column for the queen in row 0. Count safe arrangements of `n` queens obeying that anchor. Columns are zero-based and `n` is 1 to 8.', '', '**Example**', '', '```', 'input:  4 1', 'output: 1', '```', '', 'One of the two four-queen arrangements starts with column 1. If the anchor is outside `0..n-1`, return `0`; `1 0` returns `1`.'].join('\n'),
    editorialMarkdown:['## Backtracking with a pre-chosen row', '', 'The parent queen search tries every column in row zero. The twist makes that choice mandatory: validate the supplied anchor, mark its column and diagonals, then recurse from row one. The rest is the same choose/recurse/un-choose search using three attack sets.', '', 'The quiet mistake is placing the anchored queen and then starting the recursion at row zero, which permits an extra queen or overwrites the constraint. Also undo only rows explored by recursion; the anchor stays marked for the whole search. The pruned search is O(n!) time in the worst case and O(n) space, with n <= 8.'].join('\n'),
    referenceSolution:{JAVASCRIPT:'function solve(a){let n=a[0],q=a[1];if(q<0||q>=n)return 0;let C=Array(n).fill(0),U=Array(2*n).fill(0),D=Array(2*n).fill(0);C[q]=U[n-q]=D[q]=1;function f(r){if(r===n)return 1;let z=0;for(let x=0;x<n;x++){let u=r-x+n,d=r+x;if(!C[x]&&!U[u]&&!D[d]){C[x]=U[u]=D[d]=1;z+=f(r+1);C[x]=U[u]=D[d]=0}}return z}return f(1)}',TYPESCRIPT:'function solve(a:number[]):number{const n=a[0],q=a[1];if(q<0||q>=n)return 0;const C:number[]=Array(n).fill(0),U:number[]=Array(2*n).fill(0),D:number[]=Array(2*n).fill(0);C[q]=U[n-q]=D[q]=1;function f(r:number):number{if(r===n)return 1;let z=0;for(let x=0;x<n;x++){const u=r-x+n,d=r+x;if(!C[x]&&!U[u]&&!D[d]){C[x]=U[u]=D[d]=1;z+=f(r+1);C[x]=U[u]=D[d]=0}}return z}return f(1)}',PYTHON:'def solve(a):\n    n,q=a\n    if q<0 or q>=n: return 0\n    C=[0]*n; U=[0]*(2*n); D=[0]*(2*n); C[q]=U[n-q]=D[q]=1\n    def f(r):\n        if r==n:return 1\n        z=0\n        for x in range(n):\n            u=r-x+n; d=r+x\n            if not C[x] and not U[u] and not D[d]:\n                C[x]=U[u]=D[d]=1; z+=f(r+1); C[x]=U[u]=D[d]=0\n        return z\n    return f(1)',JAVA:'    static int solve(int[]a){int n=a[0],q=a[1];if(q<0||q>=n)return 0;boolean[]c=new boolean[n],u=new boolean[2*n],d=new boolean[2*n];c[q]=u[n-q]=d[q]=true;return fAnchor(n,1,c,u,d);}\n    static int fAnchor(int n,int r,boolean[]c,boolean[]u,boolean[]d){if(r==n)return 1;int z=0;for(int x=0;x<n;x++){int A=r-x+n,B=r+x;if(!c[x]&&!u[A]&&!d[B]){c[x]=u[A]=d[B]=true;z+=fAnchor(n,r+1,c,u,d);c[x]=u[A]=d[B]=false;}}return z;}',CPP:'int fAnchor(int n,int r,vector<char>&c,vector<char>&u,vector<char>&d){if(r==n)return 1;int z=0;for(int x=0;x<n;x++){int A=r-x+n,B=r+x;if(!c[x]&&!u[A]&&!d[B]){c[x]=u[A]=d[B]=1;z+=fAnchor(n,r+1,c,u,d);c[x]=u[A]=d[B]=0;}}return z;}\nint solve(vector<int>a){int n=a[0],q=a[1];if(q<0||q>=n)return 0;vector<char>c(n),u(2*n),d(2*n);c[q]=u[n-q]=d[q]=1;return fAnchor(n,1,c,u,d);}',GO:'func solve(a []int)int{n,q:=a[0],a[1];if q<0||q>=n{return 0};c:=make([]bool,n);u:=make([]bool,2*n);d:=make([]bool,2*n);c[q],u[n-q],d[q]=true,true,true;var f func(int)int;f=func(r int)int{if r==n{return 1};z:=0;for x:=0;x<n;x++{A,B:=r-x+n,r+x;if !c[x]&&!u[A]&&!d[B]{c[x],u[A],d[B]=true,true,true;z+=f(r+1);c[x],u[A],d[B]=false,false,false}};return z};return f(1)}'},tests:[{stdin:'4 1',expectedStdout:'1',isSample:true},{stdin:'4 0',expectedStdout:'0',isSample:true},{stdin:'1 0',expectedStdout:'1'},{stdin:'2 0',expectedStdout:'0'},{stdin:'5 0',expectedStdout:'2'},{stdin:'4 4',expectedStdout:'0'}]}),

  p({ ...base, slug: 'word-from-top-left-grid-trace', title: 'Begin at the Corner', difficulty: 'HARD', patternTags: ['backtracking', 'grid', 'visited-state'], signatureId: 'fn:string,string->bool', avgSolveSeconds: 840,
    promptMarkdown:['Return `true` when the word can be traced starting specifically at the top-left cell. Rows of the lowercase grid are joined with `;`; move up, down, left, or right and never reuse a cell.', '', '**Example**', '', '```', 'input:  ab;cd', '        abd', 'output: true', '```', '', 'Start at `a`, move right to `b`, then down to `d`. The rectangular grid has at most 12 cells and the word has 1 to 8 letters. A word longer than the grid, or one whose first letter differs from the corner, returns `false`.'].join('\n'),
    editorialMarkdown:['## Backtracking from one mandated start', '', 'The parent problem loops over every cell as a possible start. That memorised outer loop defeats the twist: only `(0, 0)` is legal. From that single start, mark the current cell, recurse to its four neighbours for the next letter, then restore it. The visited mark prevents a trace from borrowing one square twice.', '', 'The quiet mistake is leaving the old start loop in place; it returns true for words that exist elsewhere but cannot begin at the corner. Restore the mark even after failed branches, otherwise an attempted neighbour poisons its siblings. The bound is O(4^L) time and O(L) stack space, where L <= 8.'].join('\n'),
    referenceSolution:{JAVASCRIPT:"function solve(a,b){let g=a.split(';').map(x=>x.split('')),h=g.length,w=g[0].length;function f(r,c,k){if(r<0||c<0||r>=h||c>=w||g[r][c]!==b[k])return false;if(k===b.length-1)return true;let x=g[r][c];g[r][c]='#';let z=f(r+1,c,k+1)||f(r-1,c,k+1)||f(r,c+1,k+1)||f(r,c-1,k+1);g[r][c]=x;return z}return b.length<=h*w&&f(0,0,0)}",TYPESCRIPT:"function solve(a:string,b:string):boolean{const g:string[][]=a.split(';').map(x=>x.split(''));const h=g.length,w=g[0].length;function f(r:number,c:number,k:number):boolean{if(r<0||c<0||r>=h||c>=w||g[r][c]!==b[k])return false;if(k===b.length-1)return true;const x=g[r][c];g[r][c]='#';const z=f(r+1,c,k+1)||f(r-1,c,k+1)||f(r,c+1,k+1)||f(r,c-1,k+1);g[r][c]=x;return z}return b.length<=h*w&&f(0,0,0)}",PYTHON:"def solve(a, b):\n    g=[list(x) for x in a.split(';')]\n    def f(r,c,k):\n        if r<0 or c<0 or r>=len(g) or c>=len(g[0]) or g[r][c]!=b[k]: return False\n        if k==len(b)-1: return True\n        x=g[r][c]; g[r][c]='#'\n        z=f(r+1,c,k+1) or f(r-1,c,k+1) or f(r,c+1,k+1) or f(r,c-1,k+1)\n        g[r][c]=x\n        return z\n    return len(b)<=len(g)*len(g[0]) and f(0,0,0)",JAVA:"    static boolean solve(String a,String b){String[]r=a.split(\";\");char[][]g=new char[r.length][];for(int i=0;i<r.length;i++)g[i]=r[i].toCharArray();return b.length()<=g.length*g[0].length&&fCorner(g,b,0,0,0);}\n    static boolean fCorner(char[][]g,String b,int r,int c,int k){if(r<0||c<0||r>=g.length||c>=g[0].length||g[r][c]!=b.charAt(k))return false;if(k==b.length()-1)return true;char x=g[r][c];g[r][c]='#';boolean z=fCorner(g,b,r+1,c,k+1)||fCorner(g,b,r-1,c,k+1)||fCorner(g,b,r,c+1,k+1)||fCorner(g,b,r,c-1,k+1);g[r][c]=x;return z;}",CPP:"bool fCorner(vector<string>&g,const string&b,int r,int c,int k){if(r<0||c<0||r>=(int)g.size()||c>=(int)g[0].size()||g[r][c]!=b[k])return false;if(k==(int)b.size()-1)return true;char x=g[r][c];g[r][c]='#';bool z=fCorner(g,b,r+1,c,k+1)||fCorner(g,b,r-1,c,k+1)||fCorner(g,b,r,c+1,k+1)||fCorner(g,b,r,c-1,k+1);g[r][c]=x;return z;}\nbool solve(string a,string b){vector<string>g;string x;for(char q:a){if(q==';'){g.push_back(x);x.clear();}else x+=q;}g.push_back(x);return b.size()<=g.size()*g[0].size()&&fCorner(g,b,0,0,0);}",GO:"func solve(a string,b string)bool{r:=strings.Split(a,\";\");g:=make([][]byte,len(r));for i:=range r{g[i]=[]byte(r[i])};var f func(int,int,int)bool;f=func(x,y,k int)bool{if x<0||y<0||x>=len(g)||y>=len(g[0])||g[x][y]!=b[k]{return false};if k==len(b)-1{return true};z:=g[x][y];g[x][y]='#';ok:=f(x+1,y,k+1)||f(x-1,y,k+1)||f(x,y+1,k+1)||f(x,y-1,k+1);g[x][y]=z;return ok};return len(b)<=len(g)*len(g[0])&&f(0,0,0)}"},tests:[{stdin:'ab;cd\nabd',expectedStdout:'true',isSample:true},{stdin:'ab;cd\ncdb',expectedStdout:'false',isSample:true},{stdin:'a\na',expectedStdout:'true'},{stdin:'ab;cd\nac',expectedStdout:'true'},{stdin:'ab;cd\nabc',expectedStdout:'false'},{stdin:'ab;cd\nabcde',expectedStdout:'false'}]}),
];
