import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-034` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_034_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "balanced-energy-splitting",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Balanced Energy Splitting",
    patternTags: ["math","array","prefix-sum","counting"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A futuristic power plant generates energy across a linear series of cells, represented by an array `cells`. Engineers want to partition the cells into two non-empty continuous parts: a left segment and a right segment.\n\nA partition is considered \"stable\" if the total energy of the left segment minus the total energy of the right segment results in an even number.\n\nCalculate the number of possible stable partitions. A partition is defined by choosing an index `i` (from `0` to `cells.length - 2`) where the left segment ends.\n\n**Constraints**\n- `2 <= cells.length <= 100`\n- `1 <= cells[i] <= 100`\n\n**Example 1**\n```\ninput:\n1 3 2 4\noutput: 3\n```\n*Explanation: The partitions are:*\n- *[1] and [3, 2, 4]: 1 - 9 = -8 (even)*\n- *[1, 3] and [2, 4]: 4 - 6 = -2 (even)*\n- *[1, 3, 2] and [4]: 6 - 4 = 2 (even)*\n*Total 3 stable partitions.*\n\n**Example 2**\n```\ninput:\n1 2 2\noutput: 0\n```\n*Explanation: The partitions are:*\n- *[1] and [2, 2]: 1 - 4 = -3 (odd)*\n- *[1, 2] and [2]: 3 - 2 = 1 (odd)*\n*Total 0 stable partitions.*\n\n**Example 3**\n```\ninput:\n1 1\noutput: 1\n```\n*Explanation: The only partition [1] and [1] gives 1 - 1 = 0, which is even.*\n\n**Follow-up:** Can you determine the mathematical relationship between the total sum of the array and the number of stable partitions?",
    editorialMarkdown: "## Balanced Energy Splitting\nWe are asked to count the number of valid partitions where the left sum minus the right sum is an even number. \nLet `T` be the total sum of all elements in the array. For any partition, the right sum is `T - L`, where `L` is the left sum.\nThe difference between the left and right sum is `L - (T - L) = 2*L - T`.\nFor `2*L - T` to be an even number, `T` must be even, because `2*L` is always even. Thus, the parity of the difference is solely determined by the total sum of the array!\nIf the total sum is even, every possible partition (of which there are `N - 1`) will result in an even difference. If the total sum is odd, no partition will produce an even difference.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N)\\) to calculate the total sum.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) since only a single integer variable is needed to store the sum.\n\n**Common Trap:**\nA common mistake is simulating the partitioning process by maintaining a running prefix sum and checking each split explicitly. While this works and is \\(\\mathcal{O}(N)\\), it overlooks the mathematical invariant that simplifies the entire problem to just checking the parity of the total sum.",
    referenceSolution: {
      JAVASCRIPT: "function solve(cells) {\n    let total = 0;\n    for (let cell of cells) {\n        total += cell;\n    }\n    if (total % 2 !== 0) return 0;\n    return cells.length - 1;\n}",
      TYPESCRIPT: "function solve(cells: number[]): number {\n    let total = 0;\n    for (let cell of cells) {\n        total += cell;\n    }\n    if (total % 2 !== 0) return 0;\n    return cells.length - 1;\n}",
      PYTHON: "def solve(cells):\n    total = sum(cells)\n    if total % 2 != 0:\n        return 0\n    return len(cells) - 1",
      JAVA: "    static int solve(int[] cells) {\n        int total = 0;\n        for (int cell : cells) {\n            total += cell;\n        }\n        if (total % 2 != 0) return 0;\n        return cells.length - 1;\n    }",
      CPP: "int solve(vector<int> cells) {\n    int total = 0;\n    for (int cell : cells) {\n        total += cell;\n    }\n    if (total % 2 != 0) {\n        return 0;\n    }\n    return cells.size() - 1;\n}",
      GO: "func solve(cells []int) int {\n    total := 0\n    for _, cell := range cells {\n        total += cell\n    }\n    if total % 2 != 0 {\n        return 0\n    }\n    return len(cells) - 1\n}",
    },
    tests: [
      { stdin: "1 3 2 4", expectedStdout: "3", isSample: true },
      { stdin: "1 2 2", expectedStdout: "0", isSample: true },
      { stdin: "1 1", expectedStdout: "1" },
      { stdin: "2 3", expectedStdout: "0" },
      { stdin: "2 2 2 2", expectedStdout: "3" },
      { stdin: "1 1 1", expectedStdout: "0" },
      { stdin: "5 5 10", expectedStdout: "2" },
      { stdin: "10 20 30 40 50 60", expectedStdout: "5" },
    ],
  }),

  p({
    ...base,
    slug: "symmetric-data-markers",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Symmetric Data Markers",
    patternTags: ["strings","counting","matching"],
    signatureId: "fn:strings->int",
    avgSolveSeconds: 450,
    promptMarkdown: "You are analyzing a sequence of string tokens from a corrupted data stream, represented by an array `stream`. A token `A` is considered a \"symmetric marker\" for token `B` if `A` perfectly matches both the very beginning (prefix) and the very end (suffix) of `B`.\n\nCount the total number of index pairs `(i, j)` such that `i < j` and `stream[i]` is a symmetric marker for `stream[j]`.\n\n**Constraints**\n- `1 <= stream.length <= 50`\n- `1 <= stream[i].length <= 10`\n- `stream[i]` consists only of lowercase English letters.\n\n**Example 1**\n```\ninput:\nab abab ab\noutput: 2\n```\n*Explanation: \"ab\" (index 0) is a symmetric marker for \"abab\" (index 1). \"ab\" (index 0) is a symmetric marker for \"ab\" (index 2).*\n\n**Example 2**\n```\ninput:\na aba ababa\noutput: 3\n```\n*Explanation: \"a\" (index 0) is a symmetric marker for \"aba\" (index 1) and \"ababa\" (index 2). \"aba\" (index 1) is a symmetric marker for \"ababa\" (index 2).*\n\n**Example 3**\n```\ninput:\nabc def\noutput: 0\n```\n*Explanation: No symmetric markers found.*\n\n**Follow-up:** Can you quickly verify prefix and suffix conditions using built-in string methods in your language of choice?",
    editorialMarkdown: "## Symmetric Data Markers\nThe problem asks us to count pairs of strings where the first string is both a prefix and a suffix of the second string. Because the constraints are small (at most 50 strings, each of length up to 10), we can just use two nested loops to check all valid pairs `(i, j)` where `i < j`.\n\nFor each pair, we simply check if `stream[j]` begins with `stream[i]` and ends with `stream[i]`. Most modern programming languages have built-in `startsWith` and `endsWith` methods that make this trivial. \n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N^2 \\times M)\\), where \\(N\\) is the number of strings and \\(M\\) is the maximum length of a string.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) as we only need to keep track of our count.\n\n**Common Trap:**\nA minor trap is failing to check if `stream[i]` is longer than `stream[j]`. A longer string cannot be a prefix or suffix of a shorter string. Built-in string methods usually handle this gracefully (returning false), but a manual string slicing approach might crash or throw an out-of-bounds error if lengths are not considered.",
    referenceSolution: {
      JAVASCRIPT: "function solve(stream) {\n    let count = 0;\n    for (let i = 0; i < stream.length; i++) {\n        for (let j = i + 1; j < stream.length; j++) {\n            if (stream[i].length <= stream[j].length && \n                stream[j].startsWith(stream[i]) && \n                stream[j].endsWith(stream[i])) {\n                count++;\n            }\n        }\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(stream: string[]): number {\n    let count = 0;\n    for (let i = 0; i < stream.length; i++) {\n        for (let j = i + 1; j < stream.length; j++) {\n            if (stream[i].length <= stream[j].length && \n                stream[j].startsWith(stream[i]) && \n                stream[j].endsWith(stream[i])) {\n                count++;\n            }\n        }\n    }\n    return count;\n}",
      PYTHON: "def solve(stream):\n    count = 0\n    for i in range(len(stream)):\n        for j in range(i + 1, len(stream)):\n            if len(stream[i]) <= len(stream[j]) and stream[j].startswith(stream[i]) and stream[j].endswith(stream[i]):\n                count += 1\n    return count",
      JAVA: "    static int solve(String[] stream) {\n        int count = 0;\n        for (int i = 0; i < stream.length; i++) {\n            for (int j = i + 1; j < stream.length; j++) {\n                if (stream[i].length() <= stream[j].length() && \n                    stream[j].startsWith(stream[i]) && \n                    stream[j].endsWith(stream[i])) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }",
      CPP: "int solve(vector<string> stream) {\n    int count = 0;\n    for (int i = 0; i < stream.size(); i++) {\n        for (int j = i + 1; j < stream.size(); j++) {\n            if (stream[i].length() <= stream[j].length()) {\n                string w1 = stream[i], w2 = stream[j];\n                if (w2.substr(0, w1.length()) == w1 && \n                    w2.substr(w2.length() - w1.length()) == w1) {\n                    count++;\n                }\n            }\n        }\n    }\n    return count;\n}",
      GO: "func solve(stream []string) int {\n    count := 0\n    for i := 0; i < len(stream); i++ {\n        for j := i + 1; j < len(stream); j++ {\n            w1, w2 := stream[i], stream[j]\n            if len(w1) <= len(w2) && strings.HasPrefix(w2, w1) && strings.HasSuffix(w2, w1) {\n                count++\n            }\n        }\n    }\n    return count\n}",
    },
    tests: [
      { stdin: "ab abab ab", expectedStdout: "2", isSample: true },
      { stdin: "a aba ababa", expectedStdout: "3", isSample: true },
      { stdin: "abc def", expectedStdout: "0" },
      { stdin: "x", expectedStdout: "0" },
      { stdin: "x x x x", expectedStdout: "6" },
      { stdin: "xyz xyzabcxyz", expectedStdout: "1" },
      { stdin: "long short", expectedStdout: "0" },
      { stdin: "a b aa", expectedStdout: "1" },
    ],
  }),

  p({
    ...base,
    slug: "volatile-compound-neutralisation",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Water Tank Equalization",
    patternTags: ["simulation","math","counting","integer-arithmetic"],
    signatureId: "fn:int,int->int",
    avgSolveSeconds: 400,
    promptMarkdown: "You are managing two water tanks with initial volumes of `volume1` and `volume2` liters. \n\nIn each operation, water is drained from the fuller tank by an amount equal to the volume of the less full tank. \n- If `volume1 >= volume2`, `volume1` decreases by `volume2`.\n- If `volume1 < volume2`, `volume2` decreases by `volume1`.\n\nThe process stops immediately when either tank is completely empty (reaches `0` liters).\n\nCalculate the total number of operations performed before the process stops.\n\n**Constraints**\n- `0 <= volume1, volume2 <= 10^5`\n\n**Example 1**\n```\ninput:\n2\n3\noutput: 3\n```\n*Explanation: Operation 1: volume1=2, volume2=1. Operation 2: volume1=1, volume2=1. Operation 3: volume1=0, volume2=1.*\n\n**Example 2**\n```\ninput:\n10\n10\noutput: 1\n```\n*Explanation: Operation 1: volume1=0, volume2=10.*\n\n**Example 3**\n```\ninput:\n0\n5\noutput: 0\n```\n*Explanation: The first tank is already empty, so 0 operations are performed.*\n\n**Follow-up:** Can you solve this in \\(\\mathcal{O}(\\log(\\min(\\text{volume1}, \\text{volume2})))\\) time using modulo arithmetic?",
    editorialMarkdown: "## Volatile Compound Neutralisation\nWe can simulate the reaction process step by step. As long as both compounds have a mass strictly greater than zero, we compare their masses. If `mass1` is greater than or equal to `mass2`, we subtract `mass2` from `mass1`. Otherwise, we subtract `mass1` from `mass2`. We increment our step counter for each subtraction until one of the masses reaches zero.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(\\max(\\text{mass1}, \\text{mass2}))\\) in the worst-case scenario (e.g., when one mass is 1 and the other is very large).\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) because we only need to keep track of a counter.\n\n**Common Trap:**\nA common mistake is forgetting to check if one of the starting masses is already 0, which would cause an infinite loop if not handled correctly.",
    referenceSolution: {
      JAVASCRIPT: "function solve(mass1, mass2) {\n    let count = 0;\n    while (mass1 > 0 && mass2 > 0) {\n        if (mass1 >= mass2) mass1 -= mass2;\n        else mass2 -= mass1;\n        count++;\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(mass1: number, mass2: number): number {\n    let count = 0;\n    while (mass1 > 0 && mass2 > 0) {\n        if (mass1 >= mass2) mass1 -= mass2;\n        else mass2 -= mass1;\n        count++;\n    }\n    return count;\n}",
      PYTHON: "def solve(mass1, mass2):\n    count = 0\n    while mass1 > 0 and mass2 > 0:\n        if mass1 >= mass2:\n            mass1 -= mass2\n        else:\n            mass2 -= mass1\n        count += 1\n    return count",
      JAVA: "    static int solve(int mass1, int mass2) {\n        int count = 0;\n        while (mass1 > 0 && mass2 > 0) {\n            if (mass1 >= mass2) {\n                mass1 -= mass2;\n            } else {\n                mass2 -= mass1;\n            }\n            count++;\n        }\n        return count;\n    }",
      CPP: "int solve(int mass1, int mass2) {\n    int count = 0;\n    while (mass1 > 0 && mass2 > 0) {\n        if (mass1 >= mass2) {\n            mass1 -= mass2;\n        } else {\n            mass2 -= mass1;\n        }\n        count++;\n    }\n    return count;\n}",
      GO: "func solve(mass1 int, mass2 int) int {\n    count := 0\n    for mass1 > 0 && mass2 > 0 {\n        if mass1 >= mass2 {\n            mass1 -= mass2\n        } else {\n            mass2 -= mass1\n        }\n        count++\n    }\n    return count\n}",
    },
    tests: [
      { stdin: "2\n3", expectedStdout: "3", isSample: true },
      { stdin: "10\n10", expectedStdout: "1", isSample: true },
      { stdin: "0\n5", expectedStdout: "0" },
      { stdin: "0\n0", expectedStdout: "0" },
      { stdin: "5\n0", expectedStdout: "0" },
      { stdin: "1\n1", expectedStdout: "1" },
      { stdin: "5\n1", expectedStdout: "5" },
      { stdin: "100\n9", expectedStdout: "20" },
    ],
  }),

  p({
    ...base,
    slug: "alien-cipher-equivalency",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "BIT_MANIPULATION",
    title: "Recipe Flavor Profiles",
    patternTags: ["strings","counting","hash-map","bit-manipulation"],
    signatureId: "fn:strings->int",
    avgSolveSeconds: 600,
    promptMarkdown: "A chef is analyzing a list of recipes, represented as an array of strings, `recipes`. Each character in a string represents a specific ingredient.\n\nTwo recipes are considered to belong to the same flavor profile if they use the exact same unique set of ingredients. For example, `\"xxy\"` and `\"xyx\"` have the same flavor profile because both use only ingredients `'x'` and `'y'`, while `\"xy\"` and `\"xz\"` do not.\n\nFind the number of pairs of indices `(i, j)` such that `i < j` and `recipes[i]` and `recipes[j]` share the same flavor profile.\n\n**Constraints**\n- `1 <= recipes.length <= 100`\n- `1 <= recipes[i].length <= 100`\n- `recipes[i]` consists only of lowercase English letters.\n\n**Example 1**\n```\ninput:\naba aabb abcd bac aabc\noutput: 2\n```\n*Explanation: \"aba\" and \"aabb\" form one pair. \"bac\" and \"aabc\" form another pair. The pairs are (0, 1) and (3, 4). Output is 2.*\n\n**Example 2**\n```\ninput:\nx xy xy z\noutput: 1\n```\n*Explanation: The only matching pair is \"xy\" and \"xy\" at indices 1 and 2.*\n\n**Example 3**\n```\ninput:\na b c\noutput: 0\n```\n*Explanation: No two recipes share the exact same set of ingredients.*\n\n**Follow-up:** Can you optimize the ingredient comparisons by using an integer bitmask to represent the unique ingredients of each recipe?",
    editorialMarkdown: "## Alien Cipher Equivalency\nTwo ciphers belong to the same translation group if they are composed of the exact same unique letters. We can determine the unique characters for each cipher by converting it into a set or a bitmask. For each cipher, we can calculate a 26-bit integer where the \\(i\\)-th bit is set to 1 if the \\(i\\)-th lowercase letter is present in the cipher. \n\nWe can then keep track of how many times each bitmask has appeared using a hash map. As we iterate through the list of ciphers, for each cipher we increment our answer by the number of times we have seen its bitmask before, and then we increment the frequency of that bitmask in the hash map.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N \\times M)\\) where \\(N\\) is the number of ciphers and \\(M\\) is the maximum length of a cipher.\n- **Space Complexity:** \\(\\mathcal{O}(N)\\) to store the bitmasks or sets in a hash map.\n\n**Common Trap:**\nA common mistake is to sort the characters in each string and then compare, which adds an unnecessary \\(\\mathcal{O}(M \\log M)\\) factor to each string's processing time.",
    referenceSolution: {
      JAVASCRIPT: "function solve(ciphers) {\n    let count = 0;\n    let freqs = new Map();\n    for (let cipher of ciphers) {\n        let mask = 0;\n        for (let i = 0; i < cipher.length; i++) {\n            mask |= (1 << (cipher.charCodeAt(i) - 97));\n        }\n        let current = freqs.get(mask) || 0;\n        count += current;\n        freqs.set(mask, current + 1);\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(ciphers: string[]): number {\n    let count = 0;\n    let freqs = new Map<number, number>();\n    for (let cipher of ciphers) {\n        let mask = 0;\n        for (let i = 0; i < cipher.length; i++) {\n            mask |= (1 << (cipher.charCodeAt(i) - 97));\n        }\n        let current = freqs.get(mask) || 0;\n        count += current;\n        freqs.set(mask, current + 1);\n    }\n    return count;\n}",
      PYTHON: "def solve(ciphers):\n    count = 0\n    freqs = {}\n    for cipher in ciphers:\n        mask = 0\n        for c in cipher:\n            mask |= (1 << (ord(c) - 97))\n        count += freqs.get(mask, 0)\n        freqs[mask] = freqs.get(mask, 0) + 1\n    return count",
      JAVA: "    static int solve(String[] ciphers) {\n        int count = 0;\n        java.util.HashMap<Integer, Integer> freqs = new java.util.HashMap<>();\n        for (String cipher : ciphers) {\n            int mask = 0;\n            for (int i = 0; i < cipher.length(); i++) {\n                mask |= (1 << (cipher.charAt(i) - 'a'));\n            }\n            count += freqs.getOrDefault(mask, 0);\n            freqs.put(mask, freqs.getOrDefault(mask, 0) + 1);\n        }\n        return count;\n    }",
      CPP: "int solve(vector<string> ciphers) {\n    int count = 0;\n    unordered_map<int, int> freqs;\n    for (const string& cipher : ciphers) {\n        int mask = 0;\n        for (char c : cipher) {\n            mask |= (1 << (c - 'a'));\n        }\n        count += freqs[mask];\n        freqs[mask]++;\n    }\n    return count;\n}",
      GO: "func solve(ciphers []string) int {\n    count := 0\n    freqs := make(map[int]int)\n    for _, cipher := range ciphers {\n        mask := 0\n        for i := 0; i < len(cipher); i++ {\n            mask |= (1 << (cipher[i] - 'a'))\n        }\n        count += freqs[mask]\n        freqs[mask]++\n    }\n    return count\n}",
    },
    tests: [
      { stdin: "aba aabb abcd bac aabc", expectedStdout: "2", isSample: true },
      { stdin: "x xy xy z", expectedStdout: "1", isSample: true },
      { stdin: "a b c", expectedStdout: "0" },
      { stdin: "a", expectedStdout: "0" },
      { stdin: "a a a a", expectedStdout: "6" },
      { stdin: "abc cba bca acb bac cab", expectedStdout: "15" },
      { stdin: "hello helo", expectedStdout: "1" },
      { stdin: "hello world", expectedStdout: "0" },
    ],
  }),

  p({
    ...base,
    slug: "orbital-satellite-alignment",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Orbital Satellite Alignment",
    patternTags: ["math","counting","array","hash-map"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 400,
    promptMarkdown: "An orbital defense network consists of multiple satellites, each positioned at a specific angle (in degrees) relative to a central space station. The positions are given as an integer array, `angles`.\n\nTwo satellites can establish a perfectly aligned bidirectional communication link if their combined angles form a complete orbital rotation (i.e., exactly a multiple of 360 degrees).\n\nFind the total number of pairs of indices `(i, j)` where `i < j` and the sum of `angles[i]` and `angles[j]` is a multiple of `360`.\n\n**Constraints**\n- `1 <= angles.length <= 100`\n- `1 <= angles[i] <= 10^9`\n\n**Example 1**\n```\ninput:\n120 240 720 360 480\noutput: 3\n```\n*Explanation: The valid pairs of indices are (0, 1) summing to 360, (2, 3) summing to 1080, and (1, 4) summing to 720.*\n\n**Example 2**\n```\ninput:\n10 350\noutput: 1\n```\n*Explanation: The only pair is (0, 1) summing to 360.*\n\n**Example 3**\n```\ninput:\n180 180 180\noutput: 3\n```\n*Explanation: Any two satellites will sum to 360.*\n\n**Follow-up:** Can you find an algorithm that operates in \\\\(\\mathcal{O}(N)\\\\) time complexity without using brute-force nested loops?",
    editorialMarkdown: "## Orbital Satellite Alignment\nWe want to find pairs of satellites whose angles sum to a multiple of 360. This implies that `(angles[i] + angles[j]) % 360 == 0`. We can rewrite this as `(angles[i] % 360 + angles[j] % 360) % 360 == 0`.\n\nThis means that for any angle `x`, we need to find how many previously seen angles `y` have a remainder that perfectly complements `x`'s remainder to reach exactly 360 (or 0 if `x`'s remainder is 0).\n\nWe can iterate through the array of angles and maintain a hash map of frequencies of the remainders seen so far. For each angle, we calculate its remainder `r = angle % 360`. The required complement remainder is `(360 - r) % 360`. We add the frequency of this complement to our answer, and then increment the frequency of `r` in our hash map.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N)\\) where \\(N\\) is the number of satellites.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) since the hash map stores at most 360 distinct remainders.\n\n**Common Trap:**\nA common mistake is failing to handle the case where the angle itself is a multiple of 360. In this case, `r = 0`, and the complement `360 - 0 = 360` is not a valid remainder. Taking the modulo of the difference, `(360 - r) % 360`, properly wraps 360 back to 0.",
    referenceSolution: {
      JAVASCRIPT: "function solve(angles) {\n    let count = 0;\n    let freqs = new Map();\n    for (let angle of angles) {\n        let r = angle % 360;\n        let comp = (360 - r) % 360;\n        count += freqs.get(comp) || 0;\n        freqs.set(r, (freqs.get(r) || 0) + 1);\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(angles: number[]): number {\n    let count = 0;\n    let freqs = new Map<number, number>();\n    for (let angle of angles) {\n        let r = angle % 360;\n        let comp = (360 - r) % 360;\n        count += freqs.get(comp) || 0;\n        freqs.set(r, (freqs.get(r) || 0) + 1);\n    }\n    return count;\n}",
      PYTHON: "def solve(angles):\n    count = 0\n    freqs = {}\n    for angle in angles:\n        r = angle % 360\n        comp = (360 - r) % 360\n        count += freqs.get(comp, 0)\n        freqs[r] = freqs.get(r, 0) + 1\n    return count",
      JAVA: "    static int solve(int[] angles) {\n        int count = 0;\n        java.util.HashMap<Integer, Integer> freqs = new java.util.HashMap<>();\n        for (int angle : angles) {\n            int r = angle % 360;\n            int comp = (360 - r) % 360;\n            count += freqs.getOrDefault(comp, 0);\n            freqs.put(r, freqs.getOrDefault(r, 0) + 1);\n        }\n        return count;\n    }",
      CPP: "int solve(vector<int> angles) {\n    int count = 0;\n    unordered_map<int, int> freqs;\n    for (int angle : angles) {\n        int r = angle % 360;\n        int comp = (360 - r) % 360;\n        count += freqs[comp];\n        freqs[r]++;\n    }\n    return count;\n}",
      GO: "func solve(angles []int) int {\n    count := 0\n    freqs := make(map[int]int)\n    for _, angle := range angles {\n        r := angle % 360\n        comp := (360 - r) % 360\n        count += freqs[comp]\n        freqs[r]++\n    }\n    return count\n}",
    },
    tests: [
      { stdin: "120 240 720 360 480", expectedStdout: "3", isSample: true },
      { stdin: "10 350", expectedStdout: "1", isSample: true },
      { stdin: "180 180 180", expectedStdout: "3" },
      { stdin: "1", expectedStdout: "0" },
      { stdin: "359 2", expectedStdout: "0" },
      { stdin: "360 360 720 1080", expectedStdout: "6" },
      { stdin: "0 360 1 359 2", expectedStdout: "2" },
      { stdin: "90 180 271", expectedStdout: "0" },
    ],
  }),

  p({
    ...base,
    slug: "cargo-loading-combinations",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "TWO_POINTERS",
    title: "Cargo Loading Combinations",
    patternTags: ["array","two-pointers","sorting","counting"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 400,
    promptMarkdown: "A logistics AI is planning the loadout for a small transport drone. The drone can carry exactly two cargo crates per trip. You are given an array of integers, `weights`, representing the weights of available crates, and an integer `limit` representing the strict maximum carrying capacity of the drone.\n\nFor safety reasons, the combined weight of the two selected crates must be strictly less than the `limit`.\n\nDetermine the total number of distinct index pairs `(i, j)` where `i < j` and the sum `weights[i] + weights[j] < limit`.\n\n**Constraints**\n- `1 <= weights.length <= 50`\n- `-50 <= weights[i] <= 50`\n- `-50 <= limit <= 50`\n\n**Example 1**\n```\ninput:\n1 2 3 -1 5\n4\noutput: 4\n```\n*Explanation: The valid pairs are: (0, 1) sum=3, (0, 3) sum=0, (1, 3) sum=1, and (2, 3) sum=2. Note that (0, 2) sum=4 is not strictly less than 4.*\n\n**Example 2**\n```\ninput:\n10 20 30\n10\noutput: 0\n```\n*Explanation: No pair sum is less than 10.*\n\n**Example 3**\n```\ninput:\n-5 -5 -5\n0\noutput: 3\n```\n*Explanation: Any pair of negative weights will be less than 0.*\n\n**Follow-up:** Although the array is small, could you solve this in \\\\(\\mathcal{O}(N \\log N)\\\\) time using a sorting and two-pointer approach?",
    editorialMarkdown: "## Cargo Loading Combinations\nWe are tasked with finding the number of pairs of elements whose sum is strictly less than a given `limit`. Given the small constraints (the array length is at most 50), an \\(\\mathcal{O}(N^2)\\) brute force approach using nested loops is perfectly acceptable.\n\nHowever, a more optimal approach exists: we can first sort the array, which takes \\(\\mathcal{O}(N \\log N)\\) time. Then, we use a two-pointer approach, placing a `left` pointer at the beginning (index 0) and a `right` pointer at the end (index \\(N-1\\)).\nIf `weights[left] + weights[right] < limit`, then for this specific `left` index, any index from `left + 1` up to `right` will also produce a valid pair (since the array is sorted and values to the left of `right` are smaller or equal). Thus, we can add `right - left` to our count and increment `left`. If the sum is greater than or equal to the target, we simply decrement `right` to reduce the sum.\n\n**Complexity:**\n- **Time Complexity:** \\(\\mathcal{O}(N^2)\\) for the brute force approach, or \\(\\mathcal{O}(N \\log N)\\) if sorting and using two pointers.\n- **Space Complexity:** \\(\\mathcal{O}(1)\\) auxiliary space, or \\(\\mathcal{O}(N)\\) if sorting is implemented out-of-place.\n\n**Common Trap:**\nA common mistake when trying to implement the two-pointer approach is forgetting to sort the array first. Without sorting, the monotonic property required for the two-pointer logic does not hold.",
    referenceSolution: {
      JAVASCRIPT: "function solve(weights, limit) {\n    let count = 0;\n    for (let i = 0; i < weights.length; i++) {\n        for (let j = i + 1; j < weights.length; j++) {\n            if (weights[i] + weights[j] < limit) {\n                count++;\n            }\n        }\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(weights: number[], limit: number): number {\n    let count = 0;\n    for (let i = 0; i < weights.length; i++) {\n        for (let j = i + 1; j < weights.length; j++) {\n            if (weights[i] + weights[j] < limit) {\n                count++;\n            }\n        }\n    }\n    return count;\n}",
      PYTHON: "def solve(weights, limit):\n    count = 0\n    for i in range(len(weights)):\n        for j in range(i + 1, len(weights)):\n            if weights[i] + weights[j] < limit:\n                count += 1\n    return count",
      JAVA: "    static int solve(int[] weights, int limit) {\n        int count = 0;\n        for (int i = 0; i < weights.length; i++) {\n            for (int j = i + 1; j < weights.length; j++) {\n                if (weights[i] + weights[j] < limit) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }",
      CPP: "int solve(vector<int> weights, int limit) {\n    int count = 0;\n    for (int i = 0; i < weights.size(); i++) {\n        for (int j = i + 1; j < weights.size(); j++) {\n            if (weights[i] + weights[j] < limit) {\n                count++;\n            }\n        }\n    }\n    return count;\n}",
      GO: "func solve(weights []int, limit int) int {\n    count := 0\n    for i := 0; i < len(weights); i++ {\n        for j := i + 1; j < len(weights); j++ {\n            if weights[i] + weights[j] < limit {\n                count++\n            }\n        }\n    }\n    return count\n}",
    },
    tests: [
      { stdin: "1 2 3 -1 5\n4", expectedStdout: "4", isSample: true },
      { stdin: "10 20 30\n10", expectedStdout: "0", isSample: true },
      { stdin: "-5 -5 -5\n0", expectedStdout: "3" },
      { stdin: "1\n10", expectedStdout: "0" },
      { stdin: "-50 50\n1", expectedStdout: "1" },
      { stdin: "-1 -2 -3 -4\n-1", expectedStdout: "6" },
      { stdin: "1 5 10 15\n10", expectedStdout: "1" },
      { stdin: "10 10 10 10\n20", expectedStdout: "0" },
    ],
  }),
];
