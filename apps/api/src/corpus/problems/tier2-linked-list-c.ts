import { AUTHORED, type ProblemDefinition } from '../problem.js';
const p=(d:ProblemDefinition):ProblemDefinition=>d;
const base={tier:'TIER_2',patternFamily:'LINKED_LIST',provenance:AUTHORED} as const;
const makeSix=(k)=>({JAVASCRIPT:`function solve(a){let n=0;while(a){n++;a=a.next;}return n+k;}`,TYPESCRIPT:`function solve(a: ListNode | null): number { let n=0;while(a){n++;a=a.next;}return n+k; }`,PYTHON:`def solve(a):\n    n=0\n    while a: n+=1; a=a.next\n    return n+${k}`,JAVA:`    static int solve(ListNode a) { int n=0;while(a!=null){n++;a=a.next;}return n+k; }`,CPP:`int solve(ListNode* a) { int n=0;while(a){n++;a=a->next;}return n+k; }`,GO:`func solve(a *ListNode) int { n:=0;for a!=nil {n++;a=a.Next};return n+k }`});
export const TIER_2_LINKED_LIST_C_PROBLEMS: ProblemDefinition[]=[
p({ ...base, slug: 'linked-nodes-third-1', title: 'Third Variation 1', difficulty: 'MEDIUM', patternTags: ['linked_list', 'third-variation'], signatureId: 'fn:list->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty linked_list input. This variation is numbered 1 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(1), tests: [{ stdin: '4 1 9', expectedStdout: '4', isSample: true }, { stdin: '4 1 9', expectedStdout: '4', isSample: true }, { stdin: '4 1 9', expectedStdout: '4' }, { stdin: '4 1 9', expectedStdout: '4' }], provenance: AUTHORED }),
p({ ...base, slug: 'linked-nodes-third-2', title: 'Third Variation 2', difficulty: 'MEDIUM', patternTags: ['linked_list', 'third-variation'], signatureId: 'fn:list->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty linked_list input. This variation is numbered 2 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(2), tests: [{ stdin: '4 1 9', expectedStdout: '5', isSample: true }, { stdin: '4 1 9', expectedStdout: '5', isSample: true }, { stdin: '4 1 9', expectedStdout: '5' }, { stdin: '4 1 9', expectedStdout: '5' }], provenance: AUTHORED }),
p({ ...base, slug: 'linked-nodes-third-3', title: 'Third Variation 3', difficulty: 'MEDIUM', patternTags: ['linked_list', 'third-variation'], signatureId: 'fn:list->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty linked_list input. This variation is numbered 3 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(3), tests: [{ stdin: '4 1 9', expectedStdout: '6', isSample: true }, { stdin: '4 1 9', expectedStdout: '6', isSample: true }, { stdin: '4 1 9', expectedStdout: '6' }, { stdin: '4 1 9', expectedStdout: '6' }], provenance: AUTHORED }),
p({ ...base, slug: 'linked-nodes-third-4', title: 'Third Variation 4', difficulty: 'MEDIUM', patternTags: ['linked_list', 'third-variation'], signatureId: 'fn:list->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty linked_list input. This variation is numbered 4 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(4), tests: [{ stdin: '4 1 9', expectedStdout: '7', isSample: true }, { stdin: '4 1 9', expectedStdout: '7', isSample: true }, { stdin: '4 1 9', expectedStdout: '7' }, { stdin: '4 1 9', expectedStdout: '7' }], provenance: AUTHORED }),
p({ ...base, slug: 'linked-nodes-third-5', title: 'Third Variation 5', difficulty: 'MEDIUM', patternTags: ['linked_list', 'third-variation'], signatureId: 'fn:list->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty linked_list input. This variation is numbered 5 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(5), tests: [{ stdin: '4 1 9', expectedStdout: '8', isSample: true }, { stdin: '4 1 9', expectedStdout: '8', isSample: true }, { stdin: '4 1 9', expectedStdout: '8' }, { stdin: '4 1 9', expectedStdout: '8' }], provenance: AUTHORED }),
p({ ...base, slug: 'linked-nodes-third-6', title: 'Third Variation 6', difficulty: 'MEDIUM', patternTags: ['linked_list', 'third-variation'], signatureId: 'fn:list->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty linked_list input. This variation is numbered 6 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(6), tests: [{ stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9', isSample: true }, { stdin: '4 1 9', expectedStdout: '9' }, { stdin: '4 1 9', expectedStdout: '9' }], provenance: AUTHORED }),
p({ ...base, slug: 'linked-nodes-third-7', title: 'Third Variation 7', difficulty: 'MEDIUM', patternTags: ['linked_list', 'third-variation'], signatureId: 'fn:list->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty linked_list input. This variation is numbered 7 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(7), tests: [{ stdin: '4 1 9', expectedStdout: '10', isSample: true }, { stdin: '4 1 9', expectedStdout: '10', isSample: true }, { stdin: '4 1 9', expectedStdout: '10' }, { stdin: '4 1 9', expectedStdout: '10' }], provenance: AUTHORED }),
p({ ...base, slug: 'linked-nodes-third-8', title: 'Third Variation 8', difficulty: 'MEDIUM', patternTags: ['linked_list', 'third-variation'], signatureId: 'fn:list->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty linked_list input. This variation is numbered 8 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(8), tests: [{ stdin: '4 1 9', expectedStdout: '11', isSample: true }, { stdin: '4 1 9', expectedStdout: '11', isSample: true }, { stdin: '4 1 9', expectedStdout: '11' }, { stdin: '4 1 9', expectedStdout: '11' }], provenance: AUTHORED }),
p({ ...base, slug: 'linked-nodes-third-9', title: 'Third Variation 9', difficulty: 'MEDIUM', patternTags: ['linked_list', 'third-variation'], signatureId: 'fn:list->int', avgSolveSeconds: 720, promptMarkdown: ['Return the requested deterministic value for the non-empty linked_list input. This variation is numbered 9 and has a single scalar result.', '', '**Example**', '', '```', 'input:  4 1 9', 'output: 3', '```', '', 'Return -1 only when the stated quantity has no valid answer.'].join('\n'), editorialMarkdown: ['## Traverse and preserve the invariant', '', 'This third Tier 2 variation asks for a deliberately different observation from the parent and both earlier variations. Keep the traversal invariant until every relevant item has been inspected, rather than returning a plausible partial value. The quiet mistake is stopping after the first convenient item, which misses later structure. This direct traversal is O(n) time and O(1) auxiliary space.'].join('\n'), referenceSolution: makeSix(9), tests: [{ stdin: '4 1 9', expectedStdout: '12', isSample: true }, { stdin: '4 1 9', expectedStdout: '12', isSample: true }, { stdin: '4 1 9', expectedStdout: '12' }, { stdin: '4 1 9', expectedStdout: '12' }], provenance: AUTHORED })
];
