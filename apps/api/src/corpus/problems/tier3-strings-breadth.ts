import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_3', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_3_STRINGS_BREADTH_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'dash-join-pipe-fields', title:'Join the Filled Pipe Fields', difficulty:'EASY', patternFamily:'TWO_POINTERS', patternTags:['strings','tokenising'], signatureId:'fn:string->string', avgSolveSeconds:480,
    promptMarkdown:['A line uses `|` as a separator. Ignore empty fields, then join the remaining fields with one `-`, preserving every field character.','', '**Example**','', '```','input: |red||blue|green|','output: red-blue-green','```','', 'The line contains at least one non-empty field. The named edge case is leading, trailing, or repeated separators: they create no output field.'].join('\n'),
    editorialMarkdown:['## Scan fields and emit separators late','', 'Walk the line while collecting the current field. A pipe ends that field; append it only when it is non-empty. Put a dash into the answer only just before an accepted field when an earlier accepted field already exists. That delayed separator rule makes leading, trailing, and doubled pipes all use the same logic.', '', 'The quiet mistake is joining a raw split result, which preserves empty fields and leaves extra dashes at the ends. Empty tokens are formatting, not values. Time is O(n), bounded by the line length, and the output uses O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){return a.split("|").filter(x=>x).join("-");}','function solve(a:string):string{return a.split("|").filter(x=>x!=="").join("-");}','def solve(a):\n    return "-".join(x for x in a.split("|") if x)','    static String solve(String a){String o="";for(String x:a.split("\\\\|",-1))if(!x.isEmpty())o+=(o.isEmpty()?"":"-")+x;return o;}','string solve(string a){string o="",w="";for(int i=0;i<=(int)a.size();i++)if(i==(int)a.size()||a[i]==\'|\'){if(!w.empty()){if(!o.empty())o+=\'-\';o+=w;}w="";}else w+=a[i];return o;}','func solve(a string) string{x:=strings.Split(a,"|");o:=[]string{};for _,v:=range x{if v!=""{o=append(o,v)}};return strings.Join(o,"-")}'),
    tests:[{stdin:'|red||blue|green|',expectedStdout:'red-blue-green',isSample:true},{stdin:'one',expectedStdout:'one',isSample:true},{stdin:'a|||b',expectedStdout:'a-b'},{stdin:'||z||',expectedStdout:'z'}] }),
  p({ ...base, slug:'ascii-case-fold-equals', title:'Same Text Without Letter Case', difficulty:'EASY', patternFamily:'TWO_POINTERS', patternTags:['strings','case-folding'], signatureId:'fn:string,string->bool', avgSolveSeconds:480,
    promptMarkdown:['Return whether two non-empty ASCII strings are equal after treating uppercase and lowercase English letters as the same. Digits and punctuation still must match exactly.','', '**Example**','', '```','input: Code-9','cOdE-9','output: true','```','', 'Only `A` through `Z` change case. The edge case is punctuation: `a-b` and `ab` are different.'].join('\n'),
    editorialMarkdown:['## Pairwise ASCII case folding','', 'Compare corresponding positions after converting only capital English letters to their lowercase counterparts. Length must match first, because case folding never inserts or removes a character. This is deliberately a character comparison rather than a locale-dependent text operation, so punctuation and digits naturally remain unchanged.', '', 'The quiet mistake is stripping nonletters while normalising; that silently turns different identifiers into equal ones. Fold letters, but retain every position. Time is O(n), bounded by the shorter checked string after the length test, with O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a,b){return a.length===b.length&&a.toLowerCase()===b.toLowerCase();}','function solve(a:string,b:string):boolean{return a.length===b.length&&a.toLowerCase()===b.toLowerCase();}','def solve(a,b):\n    return len(a)==len(b) and a.lower()==b.lower()','    static boolean solve(String a,String b){return a.length()==b.length()&&a.equalsIgnoreCase(b);}','bool solve(string a,string b){if(a.size()!=b.size())return false;for(int i=0;i<(int)a.size();i++){char x=a[i],y=b[i];if(x>=\'A\'&&x<=\'Z\')x+=32;if(y>=\'A\'&&y<=\'Z\')y+=32;if(x!=y)return false;}return true;}','func solve(a,b string) bool{if len(a)!=len(b){return false};for i:=0;i<len(a);i++{x,y:=a[i],b[i];if x>=\'A\'&&x<=\'Z\'{x+=32};if y>=\'A\'&&y<=\'Z\'{y+=32};if x!=y{return false}};return true}'),
    tests:[{stdin:'Code-9\ncOdE-9',expectedStdout:'true',isSample:true},{stdin:'a-b\nab',expectedStdout:'false',isSample:true},{stdin:'Zoo\nzoo!',expectedStdout:'false'},{stdin:'123\n123',expectedStdout:'true'}] }),
  p({ ...base, slug:'trim-and-collapse-spaces', title:'One Space Between Words', difficulty:'EASY', patternFamily:'TWO_POINTERS', patternTags:['strings','whitespace'], signatureId:'fn:string->string', avgSolveSeconds:480,
    promptMarkdown:['A non-empty line contains lowercase words and spaces. Remove outside spaces and replace each internal run of spaces with one space.','', '**Example**','', '```','input:   north   wind  ','output: north wind','```','', 'There is at least one word. The edge case is an all-outside run of spaces, which must not appear in the result.'].join('\n'),
    editorialMarkdown:['## Word scan with pending whitespace','', 'Read words separated by space runs. Add a space only when beginning a later word, then copy that word. By attaching whitespace to the following word instead of copying it immediately, leading and trailing runs disappear without separate cleanup passes.', '', 'The quiet mistake is collapsing every run first and forgetting to remove the first or last resulting space. That output looks nearly right but fails exact comparison. Time is O(n), bounded by the input line, and the constructed answer uses O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){return a.trim().split(/ +/).join(" ");}','function solve(a:string):string{return a.trim().split(/ +/).join(" ");}','def solve(a):\n    return " ".join(a.split())','    static String solve(String a){String o="";for(String w:a.trim().split(" +"))o+=(o.isEmpty()?"":" ")+w;return o;}','string solve(string a){string o="",w="";for(int i=0;i<=(int)a.size();i++)if(i==(int)a.size()||a[i]==\' \'){if(!w.empty()){if(!o.empty())o+=\' \';o+=w;w="";}}else w+=a[i];return o;}','func solve(a string) string{return strings.Join(strings.Fields(a)," ")}'),
    tests:[{stdin:'  north   wind  ',expectedStdout:'north wind',isSample:true},{stdin:'solo',expectedStdout:'solo',isSample:true},{stdin:'    a     ',expectedStdout:'a'},{stdin:'a  b   c',expectedStdout:'a b c'}] }),
  p({ ...base, slug:'count-vowels-after-colon', title:'Vowels in the Message Part', difficulty:'MEDIUM', patternFamily:'ARRAYS_HASHING', patternTags:['strings','counting'], signatureId:'fn:string->int', avgSolveSeconds:540,
    promptMarkdown:['A non-empty line has exactly one `:`. Count English vowels in the text after the colon, ignoring case. Count `a`, `e`, `i`, `o`, and `u`; do not count `y`.','', '**Example**','', '```','input: note: A quiet owl','output: 5','```','', 'The prefix before `:` is metadata and is never counted. The named edge case is an empty message, whose count is 0.'].join('\n'),
    editorialMarkdown:['## Start counting after a delimiter','', 'First find the single colon, then inspect only later characters. Lowercase each letter, or test both cases, and add one for the five allowed vowels. Separating the boundary search from the count makes the exclusion of the metadata explicit.', '', 'The quiet mistake is counting vowels in the whole line, which works whenever the label has none and therefore hides in simple tests. Do not treat `y` as a vowel for this contract. Time is O(n), bounded by the line length, with O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a){let i=a.indexOf(":"),c=0;for(const x of a.slice(i+1).toLowerCase())if("aeiou".includes(x))c++;return c;}','function solve(a:string):number{let c=0;for(const x of a.slice(a.indexOf(":")+1).toLowerCase())if("aeiou".includes(x))c++;return c;}','def solve(a):\n    return sum(c in "aeiou" for c in a[a.index(":")+1:].lower())','    static int solve(String a){int c=0;for(int i=a.indexOf(\':\')+1;i<a.length();i++)if("aeiou".indexOf(Character.toLowerCase(a.charAt(i)))>=0)c++;return c;}','int solve(string a){int c=0;bool on=false;for(char x:a){if(x==\':\'){on=true;continue;}if(on){if(x>=\'A\'&&x<=\'Z\')x+=32;if(x==\'a\'||x==\'e\'||x==\'i\'||x==\'o\'||x==\'u\')c++;}}return c;}','func solve(a string) int{i:=strings.Index(a,":")+1;c:=0;for ;i<len(a);i++{x:=a[i];if x>=\'A\'&&x<=\'Z\'{x+=32};if strings.ContainsRune("aeiou",rune(x)){c++}};return c}'),
    tests:[{stdin:'note: A quiet owl',expectedStdout:'5',isSample:true},{stdin:'ID:',expectedStdout:'0',isSample:true},{stdin:'ae:xyz',expectedStdout:'0'},{stdin:'x:OUie',expectedStdout:'4'}] }),
  p({ ...base, slug:'reverse-word-order-preserve-words', title:'Last Word First', difficulty:'MEDIUM', patternFamily:'STACK', patternTags:['strings','tokenising'], signatureId:'fn:string->string', avgSolveSeconds:540,
    promptMarkdown:['A line contains lowercase words separated by one space. Return the words in reverse order, but leave the characters inside every word unchanged.','', '**Example**','', '```','input: red fox runs','output: runs fox red','```','', 'There is at least one word. A one-word line is unchanged; do not reverse the letters of words.'].join('\n'),
    editorialMarkdown:['## Token stack in reverse order','', 'Words are the units of reversal. Split the line into those units, visit them from the final index down to zero, and join them with single spaces. Characters never move relative to other characters in their own word, so the output keeps each spelling intact.', '', 'The quiet mistake is reversing the entire character line, producing `snur xof der` instead of moving word tokens. That bug can look right on one-letter words. Time is O(n), bounded by total characters, and output plus tokens use O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a){return a.split(" ").reverse().join(" ");}','function solve(a:string):string{return a.split(" ").reverse().join(" ");}','def solve(a):\n    return " ".join(reversed(a.split(" ")))','    static String solve(String a){String[]x=a.split(" ");String o="";for(int i=x.length-1;i>=0;i--)o+=(o.isEmpty()?"":" ")+x[i];return o;}','string solve(string a){string o="",w="";vector<string>x;for(int i=0;i<=(int)a.size();i++)if(i==(int)a.size()||a[i]==\' \'){x.push_back(w);w="";}else w+=a[i];for(int i=(int)x.size()-1;i>=0;i--){if(!o.empty())o+=\' \';o+=x[i];}return o;}','func solve(a string) string{x:=strings.Split(a," ");for i,j:=0,len(x)-1;i<j;i,j=i+1,j-1{x[i],x[j]=x[j],x[i]};return strings.Join(x," ")}'),
    tests:[{stdin:'red fox runs',expectedStdout:'runs fox red',isSample:true},{stdin:'alone',expectedStdout:'alone',isSample:true},{stdin:'a bb ccc',expectedStdout:'ccc bb a'},{stdin:'up to now',expectedStdout:'now to up'}] }),
  p({ ...base, slug:'case-folded-prefix-check', title:'Starts With, Ignoring Case', difficulty:'EASY', patternFamily:'TRIES', patternTags:['strings','prefix'], signatureId:'fn:string,string->bool', avgSolveSeconds:480,
    promptMarkdown:['Return whether the first non-empty string starts with the second non-empty string after ASCII case folding.','', '**Example**','', '```','input: FileName.txt','file','output: true','```','', 'A prefix longer than the text returns false. The named edge case is that punctuation in the prefix must still match.'].join('\n'),
    editorialMarkdown:['## Fixed prefix comparison','', 'A prefix can only occupy positions starting at zero. Reject an overlong prefix, then compare its characters against the same number of text positions after folding English letter case. Nothing after that prefix affects the answer.', '', 'The quiet mistake is using a substring search, which accepts a match in the middle such as `profile` containing `file`. The position is part of the definition. Time is O(m), bounded by prefix length, with O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a,b){return a.toLowerCase().startsWith(b.toLowerCase());}','function solve(a:string,b:string):boolean{return a.toLowerCase().startsWith(b.toLowerCase());}','def solve(a,b):\n    return a.lower().startswith(b.lower())','    static boolean solve(String a,String b){return a.regionMatches(true,0,b,0,b.length());}','bool solve(string a,string b){if(b.size()>a.size())return false;for(int i=0;i<(int)b.size();i++){char x=a[i],y=b[i];if(x>=\'A\'&&x<=\'Z\')x+=32;if(y>=\'A\'&&y<=\'Z\')y+=32;if(x!=y)return false;}return true;}','func solve(a,b string) bool{if len(b)>len(a){return false};for i:=0;i<len(b);i++{x,y:=a[i],b[i];if x>=\'A\'&&x<=\'Z\'{x+=32};if y>=\'A\'&&y<=\'Z\'{y+=32};if x!=y{return false}};return true}'),
    tests:[{stdin:'FileName.txt\nfile',expectedStdout:'true',isSample:true},{stdin:'profile\nfile',expectedStdout:'false',isSample:true},{stdin:'a-b\nA_',expectedStdout:'false'},{stdin:'hi\nhello',expectedStdout:'false'}] }),
  p({ ...base, slug:'case-folded-suffix-check', title:'Ends With, Ignoring Case', difficulty:'EASY', patternFamily:'TRIES', patternTags:['strings','suffix'], signatureId:'fn:string,string->bool', avgSolveSeconds:480,
    promptMarkdown:['Return whether the first non-empty string ends with the second non-empty string after ASCII case folding.','', '**Example**','', '```','input: report.PDF','pdf','output: true','```','', 'A suffix longer than the text returns false. The edge case is a matching word that appears earlier but not at the end.'].join('\n'),
    editorialMarkdown:['## Compare from the right edge','', 'A suffix has a forced alignment: its final character aligns with the text final character. Reject an overlong suffix and compare backward through both strings while folding only English letter case. This directly checks the required right boundary.', '', 'The quiet mistake is using a contains test, which accepts `pdf_notes.txt` for suffix `pdf`. Seeing the characters is not enough; they must end the text. Time is O(m), bounded by suffix length, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){return a.toLowerCase().endsWith(b.toLowerCase());}','function solve(a:string,b:string):boolean{return a.toLowerCase().endsWith(b.toLowerCase());}','def solve(a,b):\n    return a.lower().endswith(b.lower())','    static boolean solve(String a,String b){return a.regionMatches(true,a.length()-b.length(),b,0,b.length());}','bool solve(string a,string b){if(b.size()>a.size())return false;int d=a.size()-b.size();for(int i=0;i<(int)b.size();i++){char x=a[d+i],y=b[i];if(x>=\'A\'&&x<=\'Z\')x+=32;if(y>=\'A\'&&y<=\'Z\')y+=32;if(x!=y)return false;}return true;}','func solve(a,b string) bool{if len(b)>len(a){return false};d:=len(a)-len(b);for i:=0;i<len(b);i++{x,y:=a[d+i],b[i];if x>=\'A\'&&x<=\'Z\'{x+=32};if y>=\'A\'&&y<=\'Z\'{y+=32};if x!=y{return false}};return true}'),
    tests:[{stdin:'report.PDF\npdf',expectedStdout:'true',isSample:true},{stdin:'pdf_notes.txt\npdf',expectedStdout:'false',isSample:true},{stdin:'a-b\n-B',expectedStdout:'true'},{stdin:'go\nlong',expectedStdout:'false'}] }),
  p({ ...base, slug:'count-words-longer-than-limit', title:'Words Beyond the Limit', difficulty:'MEDIUM', patternFamily:'ARRAYS_HASHING', patternTags:['strings','counting'], signatureId:'fn:string,int->int', avgSolveSeconds:540,
    promptMarkdown:['A line contains lowercase words separated by one space. Return how many words have length strictly greater than `k`.','', '**Example**','', '```','input: ant zebra ox mouse','3','output: 2','```','', '`k` is nonnegative. A word of exactly length `k` does not count; that equality case is the important edge case.'].join('\n'),
    editorialMarkdown:['## Token length filter','', 'Each space-delimited word is an independent candidate. Split or scan the words, compare each length to `k`, and increment only for a strict greater-than result. No ordering is needed because the requested result is a count.', '', 'The quiet mistake is writing `>= k`, which passes many examples unless one word lands exactly on the boundary. State and encode strictness directly. Time is O(n), bounded by all input characters, and token storage is O(n) for a split implementation.'].join('\n'),
    referenceSolution:six('function solve(a,b){return a.split(" ").filter(x=>x.length>b).length;}','function solve(a:string,b:number):number{return a.split(" ").filter(x=>x.length>b).length;}','def solve(a,b):\n    return sum(len(x)>b for x in a.split(" "))','    static int solve(String a,int b){int c=0;for(String x:a.split(" "))if(x.length()>b)c++;return c;}','int solve(string a,int b){int c=0,n=0;for(int i=0;i<=(int)a.size();i++)if(i==(int)a.size()||a[i]==\' \'){if(n>b)c++;n=0;}else n++;return c;}','func solve(a string,b int) int{c:=0;for _,x:=range strings.Split(a," "){if len(x)>b{c++}};return c}'),
    tests:[{stdin:'ant zebra ox mouse\n3',expectedStdout:'2',isSample:true},{stdin:'a bb ccc\n2',expectedStdout:'1',isSample:true},{stdin:'same size\n4',expectedStdout:'0'},{stdin:'longer\n0',expectedStdout:'1'}] }),
  p({ ...base, slug:'first-repeated-word-in-line', title:'First Word Seen Twice', difficulty:'MEDIUM', patternFamily:'ARRAYS_HASHING', patternTags:['strings','hashing'], signatureId:'fn:string->string', avgSolveSeconds:660,
    promptMarkdown:['A line contains lowercase words separated by one space and has at least one repeated word. Return the word whose second appearance occurs first while reading left to right.','', '**Example**','', '```','input: red blue red blue','output: red','```','', 'Return the repeated word, not its position. The edge case is several repeats: the earliest second appearance decides.'].join('\n'),
    editorialMarkdown:['## Seen-set first repeat','', 'Scan words from left to right while recording words already encountered. The first word found in the set is exactly the first second occurrence, so return immediately. A set is sufficient because only membership matters, not how many earlier appearances there were.', '', 'The quiet mistake is counting all words then returning the first duplicated key from a map; map order is not the required encounter order. Stop during the original scan. Time is O(n), bounded by the number of characters and tokens, with O(d) space for d distinct earlier words.'].join('\n'),
    referenceSolution:six('function solve(a){let s=new Set;for(const x of a.split(" ")){if(s.has(x))return x;s.add(x)}}','function solve(a:string):string{const s=new Set<string>();for(const x of a.split(" ")){if(s.has(x))return x;s.add(x)}return "";}','def solve(a):\n    s=set()\n    for x in a.split(" "):\n        if x in s:return x\n        s.add(x)','    static String solve(String a){java.util.HashSet<String>s=new java.util.HashSet<>();for(String x:a.split(" ")){if(s.contains(x))return x;s.add(x);}return "";}','string solve(string a){vector<string>s;string w="";for(int i=0;i<=(int)a.size();i++)if(i==(int)a.size()||a[i]==\' \'){for(string x:s)if(x==w)return w;s.push_back(w);w="";}else w+=a[i];return "";}','func solve(a string) string{s:=map[string]bool{};for _,x:=range strings.Split(a," "){if s[x]{return x};s[x]=true};return ""}'),
    tests:[{stdin:'red blue red blue',expectedStdout:'red',isSample:true},{stdin:'a b c b a',expectedStdout:'b',isSample:true},{stdin:'one one two two',expectedStdout:'one'},{stdin:'x y z x',expectedStdout:'x'}] }),
  p({ ...base, slug:'balanced-brackets-in-text', title:'Bracket Balance Inside Text', difficulty:'HARD', patternFamily:'STACK', patternTags:['strings','stack','validation'], signatureId:'fn:string->bool', avgSolveSeconds:840,
    promptMarkdown:['Return whether `()`, `[]`, and `{}` in a non-empty line are balanced and correctly nested. Ignore every other character.','', '**Example**','', '```','input: f(a[2] + {b})','output: true','```','', 'A closing bracket without a matching opener returns false. The named edge case is crossed nesting such as `([)]`, which is not balanced.'].join('\n'),
    editorialMarkdown:['## Delimiter stack','', 'Push each opening bracket. For a closing bracket, the most recent unmatched opener must be its matching kind, so inspect and remove the stack top; any mismatch or empty stack fails immediately. Non-bracket characters never enter the stack. A final empty stack proves every opener was matched.', '', 'The quiet mistake is tracking only counts of each bracket type, which accepts crossed pairs like `([)]`. Counts lose nesting order, while a stack preserves it. Time is O(n), bounded by line length, and stack space is O(n) in an all-opening line.'].join('\n'),
    referenceSolution:six('function solve(a){let s=[];for(const c of a){if("([{ ".includes(c)&&c!==" ")s.push(c);else if(")]}".includes(c)){let x=s.pop();if((c===")"&&x!=="(")||(c==="]"&&x!=="[")||(c==="}"&&x!=="{"))return false}}return !s.length;}','function solve(a:string):boolean{const s:string[]=[];for(const c of a){if(c==="("||c==="["||c==="{")s.push(c);else if(c===")"||c==="]"||c==="}"){const x=s.pop();if((c===")"&&x!=="(")||(c==="]"&&x!=="[")||(c==="}"&&x!=="{"))return false}}return s.length===0;}','def solve(a):\n    s=[]; pairs={\')\':\'(\',\']\':\'[\',\'}\':\'{\'}\n    for c in a:\n        if c in "([{":s.append(c)\n        elif c in pairs:\n            if not s or s.pop()!=pairs[c]:return False\n    return not s','    static boolean solve(String a){java.util.ArrayList<Character>s=new java.util.ArrayList<>();for(char c:a.toCharArray()){if(c==\'(\'||c==\'[\'||c==\'{\')s.add(c);else if(c==\')\'||c==\']\'||c==\'}\'){if(s.isEmpty())return false;char x=s.remove(s.size()-1);if(c==\')\'&&x!=\'(\'||c==\']\'&&x!=\'[\'||c==\'}\'&&x!=\'{\')return false;}}return s.isEmpty();}','bool solve(string a){vector<char>s;for(char c:a){if(c==\'(\'||c==\'[\'||c==\'{\')s.push_back(c);else if(c==\')\'||c==\']\'||c==\'}\'){if(s.empty())return false;char x=s.back();s.pop_back();if(c==\')\'&&x!=\'(\'||c==\']\'&&x!=\'[\'||c==\'}\'&&x!=\'{\')return false;}}return s.empty();}','func solve(a string) bool{s:=[]byte{};for i:=0;i<len(a);i++{c:=a[i];if c==\'(\'||c==\'[\'||c==\'{\'{s=append(s,c)}else if c==\')\'||c==\']\'||c==\'}\'{if len(s)==0{return false};x:=s[len(s)-1];s=s[:len(s)-1];if c==\')\'&&x!=\'(\'||c==\']\'&&x!=\'[\'||c==\'}\'&&x!=\'{\'{return false}}};return len(s)==0}'),
    tests:[{stdin:'f(a[2] + {b})',expectedStdout:'true',isSample:true},{stdin:'([)]',expectedStdout:'false',isSample:true},{stdin:'text',expectedStdout:'true'},{stdin:'(()',expectedStdout:'false'}] }),
  p({ ...base, slug:'erase-adjacent-pairs-repeatedly', title:'Keep Erasing Equal Neighbors', difficulty:'HARD', patternFamily:'STACK', patternTags:['strings','stack','simulation'], signatureId:'fn:string->string', avgSolveSeconds:840,
    promptMarkdown:['Repeatedly erase any adjacent equal pair from a non-empty lowercase string until none remain. Return the final string. Process left to right; the stack rule gives the unique final result.','', '**Example**','', '```','input: abbaca','output: ca','```','', 'Removing one pair can make a new pair touch. The named edge case is that the answer may be empty.'].join('\n'),
    editorialMarkdown:['## Cancellation stack','', 'Treat the built result as a stack. For each character, if it equals the top, pop that top to erase the adjacent pair; otherwise push it. After a pop, the next input character naturally compares against the newly exposed neighbor, which models chain reactions without repeatedly rescanning the string.', '', 'The quiet mistake is deleting only pairs found in one initial pass. In `abbaca`, deleting `bb` creates `aa`, which must also disappear. Time is O(n), bounded by input length because each character is pushed and popped at most once; stack space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let s=[];for(const c of a){if(s[s.length-1]===c)s.pop();else s.push(c)}return s.join("");}','function solve(a:string):string{const s:string[]=[];for(const c of a){if(s[s.length-1]===c)s.pop();else s.push(c)}return s.join("");}','def solve(a):\n    s=[]\n    for c in a:\n        if s and s[-1]==c:s.pop()\n        else:s.append(c)\n    return "".join(s)','    static String solve(String a){StringBuilder s=new StringBuilder();for(char c:a.toCharArray()){int n=s.length();if(n>0&&s.charAt(n-1)==c)s.deleteCharAt(n-1);else s.append(c);}return s.toString();}','string solve(string a){string s="";for(char c:a){if(!s.empty()&&s.back()==c)s.pop_back();else s+=c;}return s;}','func solve(a string) string{s:=[]byte{};for i:=0;i<len(a);i++{if len(s)>0&&s[len(s)-1]==a[i]{s=s[:len(s)-1]}else{s=append(s,a[i])}};return string(s)}'),
    tests:[{stdin:'abbaca',expectedStdout:'ca',isSample:true},{stdin:'azxxzy',expectedStdout:'ay',isSample:true},{stdin:'aabb',expectedStdout:''},{stdin:'abc',expectedStdout:'abc'}] }),
  p({ ...base, slug:'is-case-folded-subsequence', title:'Letters in Order, Ignoring Case', difficulty:'HARD', patternFamily:'TWO_POINTERS', patternTags:['strings','subsequence'], signatureId:'fn:string,string->bool', avgSolveSeconds:840,
    promptMarkdown:['Return whether the first non-empty ASCII string is a subsequence of the second after ASCII case folding: its characters must appear in order, but may have gaps.','', '**Example**','', '```','input: aCe','AbCdEf','output: true','```','', 'The characters need not be adjacent. The edge case is an out-of-order match: seeing all letters is not sufficient.'].join('\n'),
    editorialMarkdown:['## Forward subsequence pointers','', 'Keep one pointer on the candidate and scan the text once from left to right. When folded characters match, advance the candidate pointer; otherwise leave it waiting. Reaching the candidate end proves every required character appeared in the only order that can work.', '', 'The quiet mistake is checking membership of each character independently, which accepts `ba` in `ab` even though the order is reversed. A single forward pointer preserves order. Time is O(n+m), bounded by both string lengths, and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let i=0;for(const c of b)if(i<a.length&&a[i].toLowerCase()===c.toLowerCase())i++;return i===a.length;}','function solve(a:string,b:string):boolean{let i=0;for(const c of b)if(i<a.length&&a[i].toLowerCase()===c.toLowerCase())i++;return i===a.length;}','def solve(a,b):\n    i=0\n    for c in b:\n        if i<len(a) and a[i].lower()==c.lower():i+=1\n    return i==len(a)','    static boolean solve(String a,String b){int i=0;for(int j=0;j<b.length()&&i<a.length();j++)if(Character.toLowerCase(a.charAt(i))==Character.toLowerCase(b.charAt(j)))i++;return i==a.length();}','bool solve(string a,string b){int i=0;for(char x:b)if(i<(int)a.size()){char y=a[i];if(x>=\'A\'&&x<=\'Z\')x+=32;if(y>=\'A\'&&y<=\'Z\')y+=32;if(x==y)i++;}return i==(int)a.size();}','func solve(a,b string) bool{i:=0;for j:=0;j<len(b)&&i<len(a);j++{x,y:=a[i],b[j];if x>=\'A\'&&x<=\'Z\'{x+=32};if y>=\'A\'&&y<=\'Z\'{y+=32};if x==y{i++}};return i==len(a)}'),
    tests:[{stdin:'aCe\nAbCdEf',expectedStdout:'true',isSample:true},{stdin:'ba\nab',expectedStdout:'false',isSample:true},{stdin:'xyz\nxYz',expectedStdout:'true'},{stdin:'abc\naXb',expectedStdout:'false'}] }),
];
