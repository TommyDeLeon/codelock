import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w4-059` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-17 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W4_059_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "largest-prime-sector-suffix",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Valid Barcode Prefix",
    patternTags: ["string","math","greedy"],
    signatureId: "fn:string->string",
    avgSolveSeconds: 480,
    promptMarkdown: "You are developing software for a barcode scanner. The scanner reads a string of digits, but sometimes captures extra trailing digits due to sensor noise.\n\nA valid product code is defined as the longest prefix of the scanned string that ends with an odd digit (odd digits represent valid checksums). If the scanned string does not contain any odd digits, then no valid product code can be extracted, and the result should be an empty string.\n\nGiven a string `serial`, return the extracted valid product code.\n\n**Constraints**\n- `1 <= serial.length <= 40`\n- `serial` consists only of digits ('0'-'9').\n\n**Example 1**\n```\ninput:\n52\noutput:\n5\n```\n*Explanation: The odd digits in the string are '5'. The longest prefix ending in an odd digit is \"5\".*\n\n**Example 2**\n```\ninput:\n4206\noutput:\n\n```\n*Explanation: The string contains only even digits, so there is no prefix ending in an odd digit.*\n\n**Example 3**\n```\ninput:\n35427\noutput:\n35427\n```\n*Explanation: The final digit '7' is odd, so the entire scanned string is the valid product code.*\n\n**Follow-up**\nCan you determine the product code in O(N) time without allocating extra space for intermediate strings?",
    editorialMarkdown: "## Largest Prime Sector Suffix\n\nThe intended approach is to scan the string from right to left to find the first odd digit. Because we want the longest prefix ending in an odd digit, the rightmost odd digit defines the optimal boundary. This uses the **Greedy** pattern. \n\n**Trap**: Searching from the beginning requires checking the entire string for the last odd digit, which is less efficient. Ensure you handle the case where no odd digits exist at all by returning an empty string.\n\n**Complexity:**\n- **Time:** O(N) — We scan the string at most once.\n- **Space:** O(1) or O(N) — Returning the substring takes O(N) space.",
    referenceSolution: {
      JAVASCRIPT: "function solve(serial) {\n    for (let i = serial.length - 1; i >= 0; i--) {\n        if (parseInt(serial[i]) % 2 !== 0) {\n            return serial.substring(0, i + 1);\n        }\n    }\n    return \"\";\n}",
      TYPESCRIPT: "function solve(serial: string): string {\n    for (let i = serial.length - 1; i >= 0; i--) {\n        if (parseInt(serial[i]) % 2 !== 0) {\n            return serial.substring(0, i + 1);\n        }\n    }\n    return \"\";\n}",
      PYTHON: "def solve(serial):\n    for i in range(len(serial) - 1, -1, -1):\n        if int(serial[i]) % 2 != 0:\n            return serial[:i + 1]\n    return \"\"",
      JAVA: "    static String solve(String serial) {\n        for (int i = serial.length() - 1; i >= 0; i--) {\n            if ((serial.charAt(i) - '0') % 2 != 0) {\n                return serial.substring(0, i + 1);\n            }\n        }\n        return \"\";\n    }",
      CPP: "string solve(string serial) {\n    for (int i = serial.length() - 1; i >= 0; i--) {\n        if ((serial[i] - '0') % 2 != 0) {\n            return serial.substr(0, i + 1);\n        }\n    }\n    return \"\";\n}",
      GO: "func solve(serial string) string {\n    for i := len(serial) - 1; i >= 0; i-- {\n        if (serial[i]-'0')%2 != 0 {\n            return serial[:i+1]\n        }\n    }\n    return \"\"\n}",
    },
    tests: [
      { stdin: "52", expectedStdout: "5", isSample: true },
      { stdin: "4206", expectedStdout: "", isSample: true },
      { stdin: "35427", expectedStdout: "35427" },
      { stdin: "2", expectedStdout: "" },
      { stdin: "1", expectedStdout: "1" },
      { stdin: "24681", expectedStdout: "24681" },
      { stdin: "13579", expectedStdout: "13579" },
      { stdin: "2460", expectedStdout: "" },
    ],
  }),

  p({
    ...base,
    slug: "highest-reversed-trade",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Highest Reversed Trade",
    patternTags: ["array","hash-set","two-pointers"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 420,
    promptMarkdown: "You are reconciling trading accounts where credit and debit logs of exact amounts are recorded. A positive number represents a credit, and a negative number represents a debit.\n\nA trade is considered \"reversed\" if both a credit of `k` and a debit of `-k` exist in the logs.\n\nFind the highest value `k` such that the trade was reversed. If no such trade exists, return `-1`.\n\n**Constraints**\n- `1 <= logs.length <= 40`\n- `-1000 <= logs[i] <= 1000`\n- `logs[i] != 0`\n\n**Example 1**\n```\ninput:\n3 2 -2 5 -3\noutput:\n3\n```\n*Explanation: Both 2 and -2, and 3 and -3 exist. The maximum reversed trade is 3.*\n\n**Example 2**\n```\ninput:\n-1 2 3\noutput:\n-1\n```\n*Explanation: There are no positive numbers that have a corresponding negative number in the logs.*\n\n**Example 3**\n```\ninput:\n-5 -5 5 5\noutput:\n5\n```\n*Explanation: The positive trade 5 has a corresponding -5 debit, making 5 the highest reversed trade.*\n\n**Follow-up**\nCan you optimize this process to run in O(N) time using a hash set?",
    editorialMarkdown: "## Highest Reversed Trade\n\nThe intended approach is to use a Hash Set to track all available trades. We can iterate over the items and for each positive item, check if its negative counterpart exists in the set. This heavily employs the **Hash Map** / Set pattern. Given the constraints, a Brute Force loop is also valid.\n\n**Trap**: Ensure you only update the max when checking positive values to avoid double-counting or incorrectly returning a negative magnitude.\n\n**Complexity:**\n- **Time:** O(N^2) or O(N) — Brute force takes O(N^2) while a HashSet achieves O(N).\n- **Space:** O(1) or O(N) — Brute force uses O(1) extra space.",
    referenceSolution: {
      JAVASCRIPT: "function solve(logs) {\n    let maxK = -1;\n    let set = new Set(logs);\n    for (let val of logs) {\n        if (val > 0 && set.has(-val) && val > maxK) {\n            maxK = val;\n        }\n    }\n    return maxK;\n}",
      TYPESCRIPT: "function solve(logs: number[]): number {\n    let maxK = -1;\n    let set = new Set(logs);\n    for (let val of logs) {\n        if (val > 0 && set.has(-val) && val > maxK) {\n            maxK = val;\n        }\n    }\n    return maxK;\n}",
      PYTHON: "def solve(logs):\n    max_k = -1\n    log_set = set(logs)\n    for val in logs:\n        if val > 0 and -val in log_set and val > max_k:\n            max_k = val\n    return max_k",
      JAVA: "    static int solve(int[] logs) {\n        int maxK = -1;\n        for (int i = 0; i < logs.length; i++) {\n            if (logs[i] > 0) {\n                for (int j = 0; j < logs.length; j++) {\n                    if (logs[j] == -logs[i]) {\n                        if (logs[i] > maxK) {\n                            maxK = logs[i];\n                        }\n                    }\n                }\n            }\n        }\n        return maxK;\n    }",
      CPP: "int solve(vector<int> logs) {\n    int maxK = -1;\n    for (int i = 0; i < logs.size(); i++) {\n        if (logs[i] > 0) {\n            for (int j = 0; j < logs.size(); j++) {\n                if (logs[j] == -logs[i]) {\n                    if (logs[i] > maxK) {\n                        maxK = logs[i];\n                    }\n                }\n            }\n        }\n    }\n    return maxK;\n}",
      GO: "func solve(logs []int) int {\n    maxK := -1\n    for i := 0; i < len(logs); i++ {\n        if logs[i] > 0 {\n            for j := 0; j < len(logs); j++ {\n                if logs[j] == -logs[i] {\n                    if logs[i] > maxK {\n                        maxK = logs[i]\n                    }\n                }\n            }\n        }\n    }\n    return maxK\n}",
    },
    tests: [
      { stdin: "3 2 -2 5 -3", expectedStdout: "3", isSample: true },
      { stdin: "-1 2 3", expectedStdout: "-1", isSample: true },
      { stdin: "-5 -5 5 5", expectedStdout: "5" },
      { stdin: "-10 8 6 7 -2 -3", expectedStdout: "-1" },
      { stdin: "-1000 1000", expectedStdout: "1000" },
      { stdin: "1", expectedStdout: "-1" },
      { stdin: "-1", expectedStdout: "-1" },
      { stdin: "7 -7 8 -8 9", expectedStdout: "8" },
    ],
  }),

  p({
    ...base,
    slug: "maximum-sensor-event-gap",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Maximum Sensor Event Gap",
    patternTags: ["string","hash-map","counting"],
    signatureId: "fn:string->int",
    avgSolveSeconds: 450,
    promptMarkdown: "A sensor records categorical event IDs represented by lowercase English letters. You need to find the maximum time gap between two identical event IDs.\n\nThe time gap between two events is defined as the number of events recorded strictly *between* them.\n\nGiven a string `events`, return the maximum gap between any two identical characters. If no two identical characters exist, return `-1`.\n\n**Constraints**\n- `1 <= events.length <= 40`\n- `events` consists only of lowercase English letters.\n\n**Example 1**\n```\ninput:\naba\noutput:\n1\n```\n*Explanation: The events 'a' occur at indices 0 and 2. There is 1 event ('b') between them.*\n\n**Example 2**\n```\ninput:\nabca\noutput:\n2\n```\n*Explanation: The 'a' events occur at indices 0 and 3, with 2 events between them.*\n\n**Example 3**\n```\ninput:\ncbzxy\noutput:\n-1\n```\n*Explanation: All events are unique, so there are no two identical characters.*\n\n**Follow-up**\nCan you solve this in a single pass of O(N) time by keeping track of the first occurrence of each character?",
    editorialMarkdown: "## Maximum Sensor Event Gap\n\nThis problem asks us to find the maximum distance between two identical characters. Using a **Hash Map** or array of size 26, we record the very first index each character appears. On subsequent appearances, we calculate the gap and update our maximum.\n\n**Trap**: The gap is defined as strictly *between* the elements (i.e. index difference minus 1). Don't forget this '- 1' in your calculation.\n\n**Complexity:**\n- **Time:** O(N) — We only traverse the string once.\n- **Space:** O(1) — The array to store 26 letters is constant in size.",
    referenceSolution: {
      JAVASCRIPT: "function solve(events) {\n    let maxGap = -1;\n    let firstSeen = {};\n    for (let i = 0; i < events.length; i++) {\n        let char = events[i];\n        if (firstSeen[char] !== undefined) {\n            let gap = i - firstSeen[char] - 1;\n            if (gap > maxGap) maxGap = gap;\n        } else {\n            firstSeen[char] = i;\n        }\n    }\n    return maxGap;\n}",
      TYPESCRIPT: "function solve(events: string): number {\n    let maxGap = -1;\n    let firstSeen: { [key: string]: number } = {};\n    for (let i = 0; i < events.length; i++) {\n        let char = events[i];\n        if (firstSeen[char] !== undefined) {\n            let gap = i - firstSeen[char] - 1;\n            if (gap > maxGap) maxGap = gap;\n        } else {\n            firstSeen[char] = i;\n        }\n    }\n    return maxGap;\n}",
      PYTHON: "def solve(events):\n    max_gap = -1\n    first_seen = {}\n    for i, char in enumerate(events):\n        if char in first_seen:\n            gap = i - first_seen[char] - 1\n            if gap > max_gap:\n                max_gap = gap\n        else:\n            first_seen[char] = i\n    return max_gap",
      JAVA: "    static int solve(String events) {\n        int maxGap = -1;\n        int[] firstSeen = new int[26];\n        for (int i = 0; i < 26; i++) firstSeen[i] = -1;\n        for (int i = 0; i < events.length(); i++) {\n            int idx = events.charAt(i) - 'a';\n            if (firstSeen[idx] != -1) {\n                int gap = i - firstSeen[idx] - 1;\n                if (gap > maxGap) maxGap = gap;\n            } else {\n                firstSeen[idx] = i;\n            }\n        }\n        return maxGap;\n    }",
      CPP: "int solve(string events) {\n    int maxGap = -1;\n    vector<int> firstSeen(26, -1);\n    for (int i = 0; i < events.length(); i++) {\n        int idx = events[i] - 'a';\n        if (firstSeen[idx] != -1) {\n            int gap = i - firstSeen[idx] - 1;\n            if (gap > maxGap) maxGap = gap;\n        } else {\n            firstSeen[idx] = i;\n        }\n    }\n    return maxGap;\n}",
      GO: "func solve(events string) int {\n    maxGap := -1\n    firstSeen := make([]int, 26)\n    for i := 0; i < 26; i++ {\n        firstSeen[i] = -1\n    }\n    for i := 0; i < len(events); i++ {\n        idx := events[i] - 'a'\n        if firstSeen[idx] != -1 {\n            gap := i - firstSeen[idx] - 1\n            if gap > maxGap {\n                maxGap = gap\n            }\n        } else {\n            firstSeen[idx] = i\n        }\n    }\n    return maxGap\n}",
    },
    tests: [
      { stdin: "aba", expectedStdout: "1", isSample: true },
      { stdin: "abca", expectedStdout: "2", isSample: true },
      { stdin: "cbzxy", expectedStdout: "-1" },
      { stdin: "c", expectedStdout: "-1" },
      { stdin: "aaaa", expectedStdout: "2" },
      { stdin: "abccba", expectedStdout: "4" },
      { stdin: "zz", expectedStdout: "0" },
      { stdin: "abcdefghijklmnopqrstuvwxyza", expectedStdout: "25" },
    ],
  }),

  p({
    ...base,
    slug: "particle-annihilation-energy",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "HEAP_PRIORITY_QUEUE",
    title: "Gladiator Tournament",
    patternTags: ["array","simulation"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You are organizing a tournament for a group of gladiators. You are given a list of their initial `energies`.\n\nIn each round, the two gladiators with the highest energies face each other in the arena. Let their energies be `x` and `y` where `x <= y`.\n- If `x == y`, both gladiators exhaust each other completely and are eliminated from the tournament.\n- If `x < y`, the gladiator with energy `x` is eliminated, and the gladiator with energy `y` remains in the tournament, but their energy is reduced to `y - x` due to fatigue.\n\nThis process is repeated until at most one gladiator remains. Return the energy of the final gladiator, or `0` if all gladiators are eliminated.\n\n**Constraints**\n- `1 <= energies.length <= 40`\n- `1 <= energies[i] <= 1000`\n\n**Example 1**\n```\ninput:\n2 7 4 1 8 1\noutput:\n1\n```\n*Explanation: The gladiators with energies 8 and 7 fight, leaving one with 1. Then 4 and 2 fight, leaving 2. Next, 2 and 1 fight, leaving 1. Finally, the two gladiators with 1 and 1 fight and eliminate each other, leaving the very first 1.*\n\n**Example 2**\n```\ninput:\n2 2\noutput:\n0\n```\n*Explanation: Both gladiators have the same energy, so they eliminate each other.*\n\n**Example 3**\n```\ninput:\n5\noutput:\n5\n```\n*Explanation: There is only one gladiator, so their energy is the final remaining energy.*\n\n**Follow-up**\nCan you efficiently track the strongest gladiators using a Max Heap instead of sorting the list repeatedly?",
    editorialMarkdown: "## Particle Annihilation Energy\n\nWe need to repeatedly find and remove the two largest elements. The ideal approach uses a **Priority Queue** (Max Heap) to quickly extract the heaviest particles. For small constraints, repeatedly sorting or a direct O(N^2) simulation works perfectly too.\n\n**Trap**: Failing to push back the difference correctly when the two particles are not equal. Also, don't forget to handle the edge case where no particles remain.\n\n**Complexity:**\n- **Time:** O(N log N) using a Heap, or O(N^2) using sorting/scanning per iteration.\n- **Space:** O(N) — To store the priority queue or a copy of the array.",
    referenceSolution: {
      JAVASCRIPT: "function solve(energies) {\n    let arr = [...energies];\n    while (arr.length > 1) {\n        arr.sort((a, b) => a - b);\n        let y = arr.pop();\n        let x = arr.pop();\n        if (y !== x) {\n            arr.push(y - x);\n        }\n    }\n    return arr.length === 0 ? 0 : arr[0];\n}",
      TYPESCRIPT: "function solve(energies: number[]): number {\n    let arr = [...energies];\n    while (arr.length > 1) {\n        arr.sort((a, b) => a - b);\n        let y = arr.pop()!;\n        let x = arr.pop()!;\n        if (y !== x) {\n            arr.push(y - x);\n        }\n    }\n    return arr.length === 0 ? 0 : arr[0];\n}",
      PYTHON: "def solve(energies):\n    arr = list(energies)\n    while len(arr) > 1:\n        arr.sort()\n        y = arr.pop()\n        x = arr.pop()\n        if x != y:\n            arr.append(y - x)\n    return arr[0] if arr else 0",
      JAVA: "    static int solve(int[] energies) {\n        int n = energies.length;\n        int[] arr = new int[n];\n        for (int i = 0; i < n; i++) arr[i] = energies[i];\n        int count = n;\n        while (count > 1) {\n            int max1 = -1, max2 = -1;\n            int idx1 = -1, idx2 = -1;\n            for (int i = 0; i < n; i++) {\n                if (arr[i] > max1) {\n                    max2 = max1;\n                    idx2 = idx1;\n                    max1 = arr[i];\n                    idx1 = i;\n                } else if (arr[i] > max2) {\n                    max2 = arr[i];\n                    idx2 = i;\n                }\n            }\n            arr[idx1] = max1 - max2;\n            arr[idx2] = -1;\n            count--;\n            if (max1 == max2) count--;\n        }\n        for (int i = 0; i < n; i++) {\n            if (arr[i] > 0) return arr[i];\n        }\n        return 0;\n    }",
      CPP: "int solve(vector<int> energies) {\n    int n = energies.size();\n    vector<int> arr = energies;\n    int count = n;\n    while (count > 1) {\n        int max1 = -1, max2 = -1;\n        int idx1 = -1, idx2 = -1;\n        for (int i = 0; i < n; i++) {\n            if (arr[i] > max1) {\n                max2 = max1;\n                idx2 = idx1;\n                max1 = arr[i];\n                idx1 = i;\n            } else if (arr[i] > max2) {\n                max2 = arr[i];\n                idx2 = i;\n            }\n        }\n        arr[idx1] = max1 - max2;\n        arr[idx2] = -1;\n        count--;\n        if (max1 == max2) count--;\n    }\n    for (int i = 0; i < n; i++) {\n        if (arr[i] > 0) return arr[i];\n    }\n    return 0;\n}",
      GO: "func solve(energies []int) int {\n    n := len(energies)\n    arr := make([]int, n)\n    for i := 0; i < n; i++ {\n        arr[i] = energies[i]\n    }\n    count := n\n    for count > 1 {\n        max1, max2 := -1, -1\n        idx1, idx2 := -1, -1\n        for i := 0; i < n; i++ {\n            if arr[i] > max1 {\n                max2 = max1\n                idx2 = idx1\n                max1 = arr[i]\n                idx1 = i\n            } else if arr[i] > max2 {\n                max2 = arr[i]\n                idx2 = i\n            }\n        }\n        arr[idx1] = max1 - max2\n        arr[idx2] = -1\n        count--\n        if max1 == max2 {\n            count--\n        }\n    }\n    for i := 0; i < n; i++ {\n        if arr[i] > 0 {\n            return arr[i]\n        }\n    }\n    return 0\n}",
    },
    tests: [
      { stdin: "2 7 4 1 8 1", expectedStdout: "1", isSample: true },
      { stdin: "2 2", expectedStdout: "0", isSample: true },
      { stdin: "5", expectedStdout: "5" },
      { stdin: "10 4 2 10", expectedStdout: "2" },
      { stdin: "1000 1000 1000", expectedStdout: "1000" },
      { stdin: "1 1 1 1 1", expectedStdout: "1" },
      { stdin: "3 7 2", expectedStdout: "2" },
      { stdin: "100 50 25 12 6 3 1", expectedStdout: "3" },
    ],
  }),

  p({
    ...base,
    slug: "optimal-engine-firing-sequence",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "GREEDY",
    title: "Shelf Arrangement",
    patternTags: ["string","greedy"],
    signatureId: "fn:string->string",
    avgSolveSeconds: 400,
    promptMarkdown: "You are organizing a row of books on a shelf, where each book has a single-digit ID. The current arrangement is represented by a string of digits `sequence`.\n\nYou want to adjust the arrangement to make the string as small as possible lexicographically. To achieve this, you are allowed to perform at most one swap of adjacent books. However, you can only swap two adjacent books if their IDs have the same parity (both are even numbers, or both are odd numbers).\n\nReturn the lexicographically smallest arrangement you can create under these rules.\n\n**Constraints**\n- `1 <= sequence.length <= 40`\n- `sequence` consists only of digits ('0'-'9').\n\n**Example 1**\n```\ninput:\n45320\noutput:\n43520\n```\n*Explanation: We could swap the adjacent odd digits 5 and 3 to get \"43520\". We could also swap the adjacent even digits 2 and 0 to get \"45302\". Since \"43520\" is the lexicographically smallest possible result after at most one valid swap, we return it.*\n\n**Example 2**\n```\ninput:\n3925\noutput:\n3925\n```\n*Explanation: We could swap 3 and 9 to get \"9325\", but that would result in a lexicographically larger string. Therefore, it is optimal to perform no swaps.*\n\n**Example 3**\n```\ninput:\n4325\noutput:\n4325\n```\n*Explanation: There are no adjacent books with the same parity, so no swaps can be performed.*\n\n**Follow-up**\nSince you want to minimize the string lexicographically, how can you determine the single best swap to make in a single pass?",
    editorialMarkdown: "## Optimal Engine Firing Sequence\n\nThe goal is to make the string lexicographically smaller with one swap. The **Greedy** approach works here: we iterate from left to right and swap the very first adjacent pair that have the same parity and where the left digit is strictly greater than the right digit.\n\n**Trap**: Over-complicating by looking for the absolute smallest possible global swap. The first valid swap from the left that decreases the string is guaranteed to be the optimal choice.\n\n**Complexity:**\n- **Time:** O(N) — We do a single pass over the string.\n- **Space:** O(N) — To modify the string characters, depending on the language's string immutability.",
    referenceSolution: {
      JAVASCRIPT: "function solve(sequence) {\n    let arr = sequence.split('');\n    for (let i = 0; i < arr.length - 1; i++) {\n        let d1 = parseInt(arr[i]);\n        let d2 = parseInt(arr[i+1]);\n        if (d1 % 2 === d2 % 2 && d1 > d2) {\n            let temp = arr[i];\n            arr[i] = arr[i+1];\n            arr[i+1] = temp;\n            break;\n        }\n    }\n    return arr.join('');\n}",
      TYPESCRIPT: "function solve(sequence: string): string {\n    let arr = sequence.split('');\n    for (let i = 0; i < arr.length - 1; i++) {\n        let d1 = parseInt(arr[i]);\n        let d2 = parseInt(arr[i+1]);\n        if (d1 % 2 === d2 % 2 && d1 > d2) {\n            let temp = arr[i];\n            arr[i] = arr[i+1];\n            arr[i+1] = temp;\n            break;\n        }\n    }\n    return arr.join('');\n}",
      PYTHON: "def solve(sequence):\n    arr = list(sequence)\n    for i in range(len(arr) - 1):\n        d1 = int(arr[i])\n        d2 = int(arr[i+1])\n        if d1 % 2 == d2 % 2 and d1 > d2:\n            arr[i], arr[i+1] = arr[i+1], arr[i]\n            break\n    return \"\".join(arr)",
      JAVA: "    static String solve(String sequence) {\n        char[] arr = sequence.toCharArray();\n        for (int i = 0; i < arr.length - 1; i++) {\n            int d1 = arr[i] - '0';\n            int d2 = arr[i+1] - '0';\n            if (d1 % 2 == d2 % 2 && d1 > d2) {\n                char temp = arr[i];\n                arr[i] = arr[i+1];\n                arr[i+1] = temp;\n                break;\n            }\n        }\n        return new String(arr);\n    }",
      CPP: "string solve(string sequence) {\n    for (int i = 0; i < sequence.length() - 1; i++) {\n        int d1 = sequence[i] - '0';\n        int d2 = sequence[i+1] - '0';\n        if (d1 % 2 == d2 % 2 && d1 > d2) {\n            swap(sequence[i], sequence[i+1]);\n            break;\n        }\n    }\n    return sequence;\n}",
      GO: "func solve(sequence string) string {\n    arr := []byte(sequence)\n    for i := 0; i < len(arr)-1; i++ {\n        d1 := arr[i] - '0'\n        d2 := arr[i+1] - '0'\n        if d1%2 == d2%2 && d1 > d2 {\n            arr[i], arr[i+1] = arr[i+1], arr[i]\n            break\n        }\n    }\n    return string(arr)\n}",
    },
    tests: [
      { stdin: "45320", expectedStdout: "43520", isSample: true },
      { stdin: "3925", expectedStdout: "3925", isSample: true },
      { stdin: "4325", expectedStdout: "4325" },
      { stdin: "132", expectedStdout: "132" },
      { stdin: "53", expectedStdout: "35" },
      { stdin: "8642", expectedStdout: "6842" },
      { stdin: "1", expectedStdout: "1" },
      { stdin: "2468", expectedStdout: "2468" },
    ],
  }),

  p({
    ...base,
    slug: "valid-encrypted-transmission-segment",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "SLIDING_WINDOW",
    title: "Clean Audio Snippet",
    patternTags: ["array","sliding-window","two-pointers"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You are analyzing an audio track represented by an array of integers `signals`.\n\nA \"clean\" audio snippet is defined as a contiguous subarray of `signals` that meets all of the following criteria:\n1. The snippet must start with an even value.\n2. The values within the snippet must strictly alternate between even and odd.\n3. Every value in the snippet must be less than or equal to a given threshold `maxAmp`.\n\nReturn the length of the longest clean audio snippet. If no snippet satisfies these conditions, return 0.\n\n**Constraints**\n- `1 <= signals.length <= 40`\n- `1 <= signals[i] <= 100`\n- `1 <= maxAmp <= 100`\n\n**Example 1**\n```\ninput:\n3 2 5 4\n5\noutput:\n3\n```\n*Explanation: The valid snippet is [2, 5, 4] because it starts with an even number, alternates between even and odd, and all values are <= 5. The length is 3.*\n\n**Example 2**\n```\ninput:\n1 2\n2\noutput:\n1\n```\n*Explanation: The longest valid snippet is [2]. It starts with an even number and is <= 2.*\n\n**Example 3**\n```\ninput:\n2 3 4 5\n4\noutput:\n3\n```\n*Explanation: The snippet [2, 3, 4] is valid. We cannot extend it to include 5 because 5 exceeds the `maxAmp` threshold of 4.*\n\n**Follow-up**\nCan you find the longest clean snippet using a two-pointer sliding window approach in O(N) time?",
    editorialMarkdown: "## Valid Encrypted Transmission Segment\n\nThis is a subarray problem where we want the longest segment satisfying three conditions. The optimal strategy uses a **Sliding Window** or **Two Pointers**. We can scan the array and whenever an element meets the starting conditions (even and <= maxAmp), we expand our window as long as elements alternate and stay within the limit.\n\n**Trap**: Resetting the window pointer incorrectly. A loop finding the bounds of valid alternating sequences ensures you don't overcount or mis-start.\n\n**Complexity:**\n- **Time:** O(N^2) for brute-force or O(N) with a proper two-pointer pass.\n- **Space:** O(1) — Only a few integers are needed to keep track of the max length.",
    referenceSolution: {
      JAVASCRIPT: "function solve(signals, maxAmp) {\n    let maxLen = 0;\n    for (let i = 0; i < signals.length; i++) {\n        if (signals[i] % 2 === 0 && signals[i] <= maxAmp) {\n            let len = 1;\n            for (let j = i + 1; j < signals.length; j++) {\n                if (signals[j] <= maxAmp && signals[j] % 2 !== signals[j-1] % 2) {\n                    len++;\n                } else {\n                    break;\n                }\n            }\n            if (len > maxLen) maxLen = len;\n        }\n    }\n    return maxLen;\n}",
      TYPESCRIPT: "function solve(signals: number[], maxAmp: number): number {\n    let maxLen = 0;\n    for (let i = 0; i < signals.length; i++) {\n        if (signals[i] % 2 === 0 && signals[i] <= maxAmp) {\n            let len = 1;\n            for (let j = i + 1; j < signals.length; j++) {\n                if (signals[j] <= maxAmp && signals[j] % 2 !== signals[j-1] % 2) {\n                    len++;\n                } else {\n                    break;\n                }\n            }\n            if (len > maxLen) maxLen = len;\n        }\n    }\n    return maxLen;\n}",
      PYTHON: "def solve(signals, max_amp):\n    max_len = 0\n    for i in range(len(signals)):\n        if signals[i] % 2 == 0 and signals[i] <= max_amp:\n            curr_len = 1\n            for j in range(i + 1, len(signals)):\n                if signals[j] <= max_amp and signals[j] % 2 != signals[j-1] % 2:\n                    curr_len += 1\n                else:\n                    break\n            if curr_len > max_len:\n                max_len = curr_len\n    return max_len",
      JAVA: "    static int solve(int[] signals, int maxAmp) {\n        int maxLen = 0;\n        for (int i = 0; i < signals.length; i++) {\n            if (signals[i] % 2 == 0 && signals[i] <= maxAmp) {\n                int len = 1;\n                for (int j = i + 1; j < signals.length; j++) {\n                    if (signals[j] <= maxAmp && signals[j] % 2 != signals[j-1] % 2) {\n                        len++;\n                    } else {\n                        break;\n                    }\n                }\n                if (len > maxLen) maxLen = len;\n            }\n        }\n        return maxLen;\n    }",
      CPP: "int solve(vector<int> signals, int maxAmp) {\n    int maxLen = 0;\n    for (int i = 0; i < signals.size(); i++) {\n        if (signals[i] % 2 == 0 && signals[i] <= maxAmp) {\n            int len = 1;\n            for (int j = i + 1; j < signals.size(); j++) {\n                if (signals[j] <= maxAmp && signals[j] % 2 != signals[j-1] % 2) {\n                    len++;\n                } else {\n                    break;\n                }\n            }\n            if (len > maxLen) maxLen = len;\n        }\n    }\n    return maxLen;\n}",
      GO: "func solve(signals []int, maxAmp int) int {\n    maxLen := 0\n    for i := 0; i < len(signals); i++ {\n        if signals[i]%2 == 0 && signals[i] <= maxAmp {\n            length := 1\n            for j := i + 1; j < len(signals); j++ {\n                if signals[j] <= maxAmp && signals[j]%2 != signals[j-1]%2 {\n                    length++\n                } else {\n                    break\n                }\n            }\n            if length > maxLen {\n                maxLen = length\n            }\n        }\n    }\n    return maxLen\n}",
    },
    tests: [
      { stdin: "3 2 5 4\n5", expectedStdout: "3", isSample: true },
      { stdin: "1 2\n2", expectedStdout: "1", isSample: true },
      { stdin: "2 3 4 5\n4", expectedStdout: "3" },
      { stdin: "4 1 3\n5", expectedStdout: "2" },
      { stdin: "2 2\n2", expectedStdout: "1" },
      { stdin: "10\n5", expectedStdout: "0" },
      { stdin: "4\n5", expectedStdout: "1" },
      { stdin: "2 5 8 11\n100", expectedStdout: "4" },
    ],
  }),
];
