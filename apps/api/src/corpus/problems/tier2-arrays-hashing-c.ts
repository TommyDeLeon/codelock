import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'ARRAYS_HASHING', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_2_ARRAYS_HASHING_C_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'largest-most-frequent-value', title:'Largest Most-Frequent Value', difficulty:'EASY', patternTags:['hash-map','frequency-count','tie-breaking'], signatureId:'fn:ints->int', avgSolveSeconds:480,
    promptMarkdown:['Return the value occurring most often. When frequencies tie, return the larger value.','','**Example**','','```','input: 4 1 4 1 2','output: 4','```','','The list has at least one number. The named edge case is a tie: input order never chooses the winner.'].join('\n'),
    editorialMarkdown:['## Frequency table with a descending tie','', 'Count every value, then scan the table for the greatest count. On equal counts, explicitly replace the answer only when the candidate value is larger. The map supplies frequencies; the comparison supplies the deterministic rule.', '', 'The quiet mistake is returning the first maximum seen. That accidentally follows insertion order, so ordinary examples pass while a reordered tie fails. Time is O(n), bounded by the list length, and the frequency table uses O(d) space for d distinct values.'].join('\n'),
    referenceSolution:six('function solve(a){let m=new Map;for(let x of a)m.set(x,(m.get(x)||0)+1);let v=a[0],c=0;for(let [x,y]of m)if(y>c||y===c&&x>v){v=x;c=y}return v}','function solve(a:number[]):number{const m=new Map<number,number>();for(const x of a)m.set(x,(m.get(x)||0)+1);let v=a[0],c=0;for(const [x,y]of m)if(y>c||(y===c&&x>v)){v=x;c=y}return v}','def solve(a):\n m={}\n for x in a:m[x]=m.get(x,0)+1\n return max(m,key=lambda x:(m[x],x))','    static int solve(int[] a){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();for(int x:a)m.put(x,m.getOrDefault(x,0)+1);int v=a[0],c=0;for(int x:m.keySet())if(m.get(x)>c||m.get(x)==c&&x>v){v=x;c=m.get(x);}return v;}','#include <unordered_map>\nint solve(vector<int>a){unordered_map<int,int>m;for(int x:a)m[x]++;int v=a[0],c=0;for(auto e:m)if(e.second>c||(e.second==c&&e.first>v)){v=e.first;c=e.second;}return v;}','func solve(a []int) int {m:=map[int]int{};for _,x:=range a{m[x]++};v,c:=a[0],0;for x,y:=range m{if y>c||(y==c&&x>v){v,c=x,y}};return v}'),
    tests:[{stdin:'4 1 4 1 2',expectedStdout:'4',isSample:true},{stdin:'3 3 2',expectedStdout:'3',isSample:true},{stdin:'-2 -1 -1 -2',expectedStdout:'-1'},{stdin:'9',expectedStdout:'9'},{stdin:'5 4 3',expectedStdout:'5'}] }),
  p({ ...base, slug:'count-values-meeting-frequency-cap', title:'How Many Values Meet the Count?', difficulty:'EASY', patternTags:['hash-map','frequency-count','threshold'], signatureId:'fn:ints,int->int', avgSolveSeconds:480,
    promptMarkdown:['Return how many distinct values occur at least `k` times.','','**Example**','','```','input: 1 1 2 2 2 3','2','output: 2','```','','Values `1` and `2` qualify. The list has at least one number and `k` is positive; an unreachable threshold returns `0`.'].join('\n'),
    editorialMarkdown:['## Frequency threshold counting','', 'A frequency map collapses all copies of a value into one count. After the scan, count only map entries whose count reaches k, so a value contributes once regardless of how far it exceeds the threshold.', '', 'The quiet mistake is incrementing the answer for every occurrence after the k-th. That counts extra copies instead of qualifying values. Time is O(n), bounded by the input length, with O(d) space for distinct values.'].join('\n'),
    referenceSolution:six('function solve(a,b){let m={};for(let x of a)m[x]=(m[x]||0)+1;return Object.values(m).filter(x=>x>=b).length}','function solve(a:number[],b:number):number{const m=new Map<number,number>();for(const x of a)m.set(x,(m.get(x)||0)+1);let r=0;for(const c of m.values())if(c>=b)r++;return r}','def solve(a,b):\n m={}\n for x in a:m[x]=m.get(x,0)+1\n return sum(x>=b for x in m.values())','    static int solve(int[]a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();for(int x:a)m.put(x,m.getOrDefault(x,0)+1);int r=0;for(int c:m.values())if(c>=b)r++;return r;}','int solve(vector<int>a,int b){unordered_map<int,int>m;for(int x:a)m[x]++;int r=0;for(auto e:m)if(e.second>=b)r++;return r;}','func solve(a []int,b int) int {m:=map[int]int{};for _,x:=range a{m[x]++};r:=0;for _,c:=range m{if c>=b{r++}};return r}'),
    tests:[{stdin:'1 1 2 2 2 3\n2',expectedStdout:'2',isSample:true},{stdin:'7\n1',expectedStdout:'1',isSample:true},{stdin:'1 2 3\n2',expectedStdout:'0'},{stdin:'-1 -1 -1 0 0\n3',expectedStdout:'1'},{stdin:'5 5 5\n4',expectedStdout:'0'}] }),
  p({ ...base, slug:'first-unique-within-prefix-cap', title:'First Unique Before the Cap', difficulty:'EASY', patternTags:['hash-map','frequency-count','prefix'], signatureId:'fn:ints,int->int', avgSolveSeconds:540,
    promptMarkdown:['Inspect only the first `k` numbers. Return the earliest among them whose frequency within those first `k` numbers is exactly one, or `-1` if none exists.','','**Example**','','```','input: 2 3 2 4 3 5','5','output: 4','```','','Only `2 3 2 4 3` is inspected. The list has at least `k` values and `k` is positive; later copies do not affect the answer.'].join('\n'),
    editorialMarkdown:['## Two passes over a capped prefix','', 'Count exactly the inspected prefix, then scan that same prefix in original order for the first count of one. Separating count from selection matters because an item that looks unique early can have a twin later within the cap.', '', 'The quiet mistake is counting the entire list. A copy after position k is deliberately irrelevant, so that bug is quiet unless a test places a late duplicate. Time is O(k), bounded by the cap, and the map uses O(k) space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let m={};for(let i=0;i<b;i++)m[a[i]]=(m[a[i]]||0)+1;for(let i=0;i<b;i++)if(m[a[i]]===1)return a[i];return -1}','function solve(a:number[],b:number):number{const m=new Map<number,number>();for(let i=0;i<b;i++)m.set(a[i],(m.get(a[i])||0)+1);for(let i=0;i<b;i++)if(m.get(a[i])===1)return a[i];return -1}','def solve(a,b):\n m={}\n for x in a[:b]:m[x]=m.get(x,0)+1\n for x in a[:b]:\n  if m[x]==1:return x\n return -1','    static int solve(int[]a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();for(int i=0;i<b;i++)m.put(a[i],m.getOrDefault(a[i],0)+1);for(int i=0;i<b;i++)if(m.get(a[i])==1)return a[i];return -1;}','int solve(vector<int>a,int b){unordered_map<int,int>m;for(int i=0;i<b;i++)m[a[i]]++;for(int i=0;i<b;i++)if(m[a[i]]==1)return a[i];return -1;}','func solve(a []int,b int) int {m:=map[int]int{};for i:=0;i<b;i++{m[a[i]]++};for i:=0;i<b;i++{if m[a[i]]==1{return a[i]}};return -1}'),
    tests:[{stdin:'2 3 2 4 3 5\n5',expectedStdout:'4',isSample:true},{stdin:'1 2 1 2 3\n4',expectedStdout:'-1',isSample:true},{stdin:'7 7 8\n2',expectedStdout:'-1'},{stdin:'9 9 9\n1',expectedStdout:'9'},{stdin:'1 2 1 2\n3',expectedStdout:'2'}] }),
  p({ ...base, slug:'second-frequency-tier-smallest-value', title:'Smallest Value on the Second Frequency Tier', difficulty:'MEDIUM', patternTags:['hash-map','frequency-count','ranking'], signatureId:'fn:ints->int', avgSolveSeconds:660,
    promptMarkdown:['Find the second-highest distinct frequency among the values, then return the smallest value with that frequency. Return `-1` if every distinct value has the same frequency.','','**Example**','','```','input: 4 4 4 2 2 1','output: 2','```','','Frequencies are 3, 2, and 1, so the second tier is 2. The list is non-empty; ties within the selected tier use the smaller value.'].join('\n'),
    editorialMarkdown:['## Frequency ranking without sorting keys','', 'First count values. Track the largest and second-largest distinct frequency values, then select the smallest key whose count equals the second tier. Keeping tiers distinct is what separates frequency 3, 3, 2 from a second copy of the top tier.', '', 'The quiet mistake is sorting values rather than their counts, or treating another maximum as second place. Both often pass inputs with all different counts. Time is O(n), bounded by list length, and O(d) map space is used.'].join('\n'),
    referenceSolution:six('function solve(a){let m={};for(let x of a)m[x]=(m[x]||0)+1;let u=[...new Set(Object.values(m))].sort((x,y)=>y-x);if(u.length<2)return -1;return Math.min(...Object.keys(m).map(Number).filter(x=>m[x]===u[1]))}','function solve(a:number[]):number{const m=new Map<number,number>();for(const x of a)m.set(x,(m.get(x)||0)+1);const q=[...new Set(m.values())].sort((x,y)=>y-x);if(q.length<2)return -1;let r=Infinity;for(const [x,c]of m)if(c===q[1]&&x<r)r=x;return r}','def solve(a):\n m={}\n for x in a:m[x]=m.get(x,0)+1\n q=sorted(set(m.values()),reverse=True)\n return min(x for x in m if m[x]==q[1]) if len(q)>1 else -1','    static int solve(int[]a){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();for(int x:a)m.put(x,m.getOrDefault(x,0)+1);int hi=0,lo=0;for(int c:m.values())if(c>hi){lo=hi;hi=c;}else if(c<hi&&c>lo)lo=c;if(lo==0)return -1;int r=Integer.MAX_VALUE;for(int x:m.keySet())if(m.get(x)==lo&&x<r)r=x;return r;}','int solve(vector<int>a){unordered_map<int,int>m;for(int x:a)m[x]++;int hi=0,lo=0;for(auto e:m)if(e.second>hi){lo=hi;hi=e.second;}else if(e.second<hi&&e.second>lo)lo=e.second;if(!lo)return -1;int r=2147483647;for(auto e:m)if(e.second==lo&&e.first<r)r=e.first;return r;}','func solve(a []int) int {m:=map[int]int{};for _,x:=range a{m[x]++};hi,lo:=0,0;for _,c:=range m{if c>hi{lo,hi=hi,c}else if c<hi&&c>lo{lo=c}};if lo==0{return -1};r:=int(^uint(0)>>1);for x,c:=range m{if c==lo&&x<r{r=x}};return r}'),
    tests:[{stdin:'4 4 4 2 2 1',expectedStdout:'2',isSample:true},{stdin:'5 5 4 4 3',expectedStdout:'3',isSample:true},{stdin:'1 2 3',expectedStdout:'-1'},{stdin:'-1 -1 -2 -2 -2 -3',expectedStdout:'-1'},{stdin:'7 7 7 1 2 2',expectedStdout:'2'}] }),
  p({ ...base, slug:'count-target-suffixes', title:'How Many Tails Hit the Target?', difficulty:'MEDIUM', patternTags:['prefix-sum','suffix-sum','counting'], signatureId:'fn:ints,int->int', avgSolveSeconds:600,
    promptMarkdown:['Count non-empty suffixes whose sum equals the target. A suffix ends at the last position and starts somewhere in the list.','','**Example**','','```','input: 1 2 -1 2','2','output: 1','```','','Only the suffix `2` qualifies. The list is non-empty, numbers may be negative, and no matching tail returns `0`.'].join('\n'),
    editorialMarkdown:['## Reverse running sum','', 'A suffix is a prefix viewed from the other end. Walk right to left, add each value to a running total, and count each time it equals the target. No hash history is needed because every valid segment must touch the final position.', '', 'The quiet mistake is scanning left to right and counting ordinary prefixes, which answers a different question but looks similar on symmetric tests. Negative values mean passing the target is not a reason to stop. Time is O(n), bounded by list length, with O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let s=0,r=0;for(let i=a.length-1;i>=0;i--){s+=a[i];if(s===b)r++}return r}','function solve(a:number[],b:number):number{let s=0,r=0;for(let i=a.length-1;i>=0;i--){s+=a[i];if(s===b)r++}return r}','def solve(a,b):\n s=r=0\n for x in a[::-1]:\n  s+=x\n  if s==b:r+=1\n return r','    static int solve(int[]a,int b){int s=0,r=0;for(int i=a.length-1;i>=0;i--){s+=a[i];if(s==b)r++;}return r;}','int solve(vector<int>a,int b){int s=0,r=0;for(int i=(int)a.size()-1;i>=0;i--){s+=a[i];if(s==b)r++;}return r;}','func solve(a []int,b int) int {s,r:=0,0;for i:=len(a)-1;i>=0;i--{s+=a[i];if s==b{r++}};return r}'),
    tests:[{stdin:'1 2 -1 2\n2',expectedStdout:'1',isSample:true},{stdin:'1 1 1\n2',expectedStdout:'1',isSample:true},{stdin:'3\n3',expectedStdout:'1'},{stdin:'-1 1 -1\n0',expectedStdout:'1'},{stdin:'1 2 3\n7',expectedStdout:'0'}] }),
  p({ ...base, slug:'count-anagram-clusters', title:'How Many Anagram Clusters?', difficulty:'MEDIUM', patternTags:['hash-map','canonical-key','grouping'], signatureId:'fn:strings->int', avgSolveSeconds:720,
    promptMarkdown:['Count canonical word groups containing at least two input words. Two lowercase words share a group when they use the same letters with the same multiplicities.','','**Example**','','```','input: ate tea tan nat bat','output: 2','```','','`ate, tea` and `tan, nat` are the two groups. Every word contains only lowercase English letters; a lone word does not count as a cluster.'].join('\n'),
    editorialMarkdown:['## Letter-count canonical keys','', 'Words that are anagrams have equal counts in each of the 26 letter slots. Build a fixed count key for every word, increment its group count, then count keys whose group size is at least two. The key makes differently ordered spellings meet at one map entry.', '', 'The quiet mistake is grouping by the set of letters: `aab` and `ab` would then collide even though multiplicity matters. Time is O(total characters), bounded by all input text, and the map uses O(g) space for g groups.'].join('\n'),
    referenceSolution:six('function solve(a){let m={};for(let w of a){let c=Array(26).fill(0);for(let x of w)c[x.charCodeAt()-97]++;let k=c.join(",");m[k]=(m[k]||0)+1}return Object.values(m).filter(x=>x>=2).length}','function solve(a:string[]):number{const m=new Map<string,number>();for(const w of a){const c=Array(26).fill(0);for(const x of w)c[x.charCodeAt(0)-97]++;const k=c.join(",");m.set(k,(m.get(k)||0)+1)}let r=0;for(const x of m.values())if(x>=2)r++;return r}','def solve(a):\n m={}\n for w in a:\n  c=[0]*26\n  for x in w:c[ord(x)-97]+=1\n  k=tuple(c);m[k]=m.get(k,0)+1\n return sum(x>=2 for x in m.values())','    static int solve(String[]a){java.util.HashMap<String,Integer>m=new java.util.HashMap<>();for(String w:a){int[]c=new int[26];for(char x:w.toCharArray())c[x-\'a\']++;String k=java.util.Arrays.toString(c);m.put(k,m.getOrDefault(k,0)+1);}int r=0;for(int x:m.values())if(x>=2)r++;return r;}','int solve(vector<string>a){unordered_map<string,int>m;for(string w:a){int c[26]={};for(char x:w)c[x-\'a\']++;string k="";for(int i=0;i<26;i++)k+=to_string(c[i])+",";m[k]++;}int r=0;for(auto e:m)if(e.second>=2)r++;return r;}','func solve(a []string) int {m:=map[string]int{};for _,w:=range a{c:=make([]int,26);for _,x:=range w{c[int(x-\'a\')]++};k:="";for _,v:=range c{k+=strconv.Itoa(v)+","};m[k]++};r:=0;for _,v:=range m{if v>=2{r++}};return r}'),
    tests:[{stdin:'ate tea tan nat bat',expectedStdout:'2',isSample:true},{stdin:'abc bca cab xyz',expectedStdout:'1',isSample:true},{stdin:'a b c',expectedStdout:'0'},{stdin:'aab aba baa ab',expectedStdout:'1'},{stdin:'listen silent enlist inlets',expectedStdout:'1'}] }),
  p({ ...base, slug:'largest-anagram-group-size', title:'Largest Anagram Group', difficulty:'MEDIUM', patternTags:['hash-map','canonical-key','frequency-count'], signatureId:'fn:strings->int', avgSolveSeconds:720,
    promptMarkdown:['Return the size of the largest anagram group among lowercase words.','','**Example**','','```','input: arc car rat tar art','output: 3','```','','`rat, tar, art` form the largest group. At least one word is supplied; when every word stands alone, return `1`.'].join('\n'),
    editorialMarkdown:['## Canonical count keys and a running maximum','', 'Give every word its 26-count key, then increment that key in a map. Each new count is the size of one anagram group, so a running maximum avoids a separate grouping structure.', '', 'The quiet mistake is using word length as the key. Equal length is necessary but not sufficient: `arc` and `rat` must remain separate. Time is O(total characters), bounded by the input text, and O(g) space holds the group counts.'].join('\n'),
    referenceSolution:six('function solve(a){let m={},r=0;for(let w of a){let c=Array(26).fill(0);for(let x of w)c[x.charCodeAt()-97]++;let k=c.join();m[k]=(m[k]||0)+1;r=Math.max(r,m[k])}return r}','function solve(a:string[]):number{const m=new Map<string,number>();let r=0;for(const w of a){const c=Array(26).fill(0);for(const x of w)c[x.charCodeAt(0)-97]++;const k=c.join();const v=(m.get(k)||0)+1;m.set(k,v);if(v>r)r=v}return r}','def solve(a):\n m={};r=0\n for w in a:\n  c=[0]*26\n  for x in w:c[ord(x)-97]+=1\n  k=tuple(c);m[k]=m.get(k,0)+1;r=max(r,m[k])\n return r','    static int solve(String[]a){java.util.HashMap<String,Integer>m=new java.util.HashMap<>();int r=0;for(String w:a){int[]c=new int[26];for(char x:w.toCharArray())c[x-\'a\']++;String k=java.util.Arrays.toString(c);int v=m.getOrDefault(k,0)+1;m.put(k,v);if(v>r)r=v;}return r;}','int solve(vector<string>a){unordered_map<string,int>m;int r=0;for(string w:a){int c[26]={};for(char x:w)c[x-\'a\']++;string k="";for(int i=0;i<26;i++)k+=to_string(c[i])+",";m[k]++;if(m[k]>r)r=m[k];}return r;}','func solve(a []string) int {m:=map[string]int{};r:=0;for _,w:=range a{c:=make([]int,26);for _,x:=range w{c[int(x-\'a\')]++};k:="";for _,v:=range c{k+=strconv.Itoa(v)+","};m[k]++;if m[k]>r{r=m[k]}};return r}'),
    tests:[{stdin:'arc car rat tar art',expectedStdout:'3',isSample:true},{stdin:'ab ba cd',expectedStdout:'2',isSample:true},{stdin:'solo',expectedStdout:'1'},{stdin:'a b c',expectedStdout:'1'},{stdin:'aab aba baa aba',expectedStdout:'4'}] }),
  p({ ...base, slug:'first-word-from-each-anagram-group', title:'First Word From Each Anagram Group', difficulty:'MEDIUM', patternTags:['hash-map','canonical-key','stable-order'], signatureId:'fn:strings->strings', avgSolveSeconds:780,
    promptMarkdown:['Return the first input word from each anagram group, preserving the order in which those groups first appear.','','**Example**','','```','input: tea ate dog god eat','output: tea dog','```','','`tea` represents its group and `dog` represents the next one. Words are lowercase; anagrams after the first representative are omitted.'].join('\n'),
    editorialMarkdown:['## Stable grouping by a canonical key','', 'Compute a letter-count key for each word. A set of previously seen keys answers whether this group already has a representative; append the word only on its first key occurrence, which preserves the required input order.', '', 'The quiet mistake is iterating the hash map after grouping. Maps do not promise the first-seen order required by exact stdout. Time is O(total characters), bounded by all words, and O(g) space stores seen keys and output.'].join('\n'),
    referenceSolution:six('function solve(a){let s=new Set,o=[];for(let w of a){let c=Array(26).fill(0);for(let x of w)c[x.charCodeAt()-97]++;let k=c.join();if(!s.has(k)){s.add(k);o.push(w)}}return o}','function solve(a:string[]):string[]{const s=new Set<string>(),o:string[]=[];for(const w of a){const c=Array(26).fill(0);for(const x of w)c[x.charCodeAt(0)-97]++;const k=c.join();if(!s.has(k)){s.add(k);o.push(w)}}return o}','def solve(a):\n s=set();o=[]\n for w in a:\n  c=[0]*26\n  for x in w:c[ord(x)-97]+=1\n  k=tuple(c)\n  if k not in s:s.add(k);o.append(w)\n return o','    static String[] solve(String[]a){java.util.HashSet<String>s=new java.util.HashSet<>();java.util.ArrayList<String>o=new java.util.ArrayList<>();for(String w:a){int[]c=new int[26];for(char x:w.toCharArray())c[x-\'a\']++;String k=java.util.Arrays.toString(c);if(s.add(k))o.add(w);}return o.toArray(new String[0]);}','vector<string> solve(vector<string>a){unordered_set<string>s;vector<string>o;for(string w:a){int c[26]={};for(char x:w)c[x-\'a\']++;string k="";for(int i=0;i<26;i++)k+=to_string(c[i])+",";if(s.insert(k).second)o.push_back(w);}return o;}','func solve(a []string) []string {s:=map[string]bool{};o:=[]string{};for _,w:=range a{c:=make([]int,26);for _,x:=range w{c[int(x-\'a\')]++};k:="";for _,v:=range c{k+=strconv.Itoa(v)+","};if !s[k]{s[k]=true;o=append(o,w)}};return o}'),
    tests:[{stdin:'tea ate dog god eat',expectedStdout:'tea dog',isSample:true},{stdin:'abc bca xyz zyx def',expectedStdout:'abc xyz def',isSample:true},{stdin:'solo',expectedStdout:'solo'},{stdin:'a a a',expectedStdout:'a'},{stdin:'ab ba abc cab bca',expectedStdout:'ab abc'}] }),
  p({ ...base, slug:'product-except-self-modulo-thousand', title:'Other Products Modulo 1000', difficulty:'MEDIUM', patternTags:['prefix-product','suffix-product','modulo'], signatureId:'fn:ints->ints', avgSolveSeconds:780,
    promptMarkdown:['For every position, return the product of all values at the other positions, reduced to a number from `0` through `999`. Do not use division.','','**Example**','','```','input: 2 3 4','output: 12 8 6','```','','The list has at least one number and values may be negative or zero. The one-number edge case returns `1`, the product of no other values.'].join('\n'),
    editorialMarkdown:['## Prefix and suffix products under a modulus','', 'Write each position’s product of earlier values, then multiply it by a running product from the right. Reducing after every multiplication preserves the final remainder while keeping the intermediate value bounded. This handles zeros naturally without division.', '', 'The quiet mistake is leaving negative remainders negative in languages where `%` keeps the sign. Normalize after every multiplication so stdout always uses 0 through 999. Time is O(n), bounded by list length, and output aside uses O(1) space.'].join('\n'),
    referenceSolution:six('function solve(a){let r=[],p=1;for(let x of a){r.push(p);p=((p*x)%1000+1000)%1000}p=1;for(let i=a.length-1;i>=0;i--){r[i]=r[i]*p%1000;p=((p*a[i])%1000+1000)%1000}return r}','function solve(a:number[]):number[]{const r:number[]=[];let p=1;for(const x of a){r.push(p);p=((p*x)%1000+1000)%1000}p=1;for(let i=a.length-1;i>=0;i--){r[i]=r[i]*p%1000;p=((p*a[i])%1000+1000)%1000}return r}','def solve(a):\n r=[];p=1\n for x in a:r.append(p);p=p*x%1000\n p=1\n for i in range(len(a)-1,-1,-1):r[i]=r[i]*p%1000;p=p*a[i]%1000\n return r','    static int[] solve(int[]a){int[]r=new int[a.length];int p=1;for(int i=0;i<a.length;i++){r[i]=p;p=((p*a[i])%1000+1000)%1000;}p=1;for(int i=a.length-1;i>=0;i--){r[i]=r[i]*p%1000;p=((p*a[i])%1000+1000)%1000;}return r;}','vector<int> solve(vector<int>a){vector<int>r(a.size());int p=1;for(int i=0;i<(int)a.size();i++){r[i]=p;p=((p*a[i])%1000+1000)%1000;}p=1;for(int i=(int)a.size()-1;i>=0;i--){r[i]=r[i]*p%1000;p=((p*a[i])%1000+1000)%1000;}return r;}','func solve(a []int) []int {r:=make([]int,len(a));p:=1;for i,x:=range a{r[i]=p;p=((p*x)%1000+1000)%1000};p=1;for i:=len(a)-1;i>=0;i--{r[i]=r[i]*p%1000;p=((p*a[i])%1000+1000)%1000};return r}'),
    tests:[{stdin:'2 3 4',expectedStdout:'12 8 6',isSample:true},{stdin:'5',expectedStdout:'1',isSample:true},{stdin:'1 0 3',expectedStdout:'0 3 0'},{stdin:'-2 3 -4',expectedStdout:'988 8 994'},{stdin:'10 10 10 10',expectedStdout:'0 0 0 0'}] }),
  p({ ...base, slug:'count-even-length-target-slices', title:'Even-Length Slices at Target', difficulty:'HARD', patternTags:['prefix-sum','hash-map','parity'], signatureId:'fn:ints,int->int', avgSolveSeconds:960,
    promptMarkdown:['Count contiguous non-empty slices whose sum is the target and whose length is even. Numbers may be negative.','','**Example**','','```','input: 1 1 1 1','2','output: 3','```','','The length-two slices at positions 0-1, 1-2, and 2-3 qualify. Return `0` when no qualifying slice exists.'].join('\n'),
    editorialMarkdown:['## Prefix sums split by index parity','', 'A slice ending at index i has target sum when an earlier prefix is `running - target`. Its length is even exactly when that earlier prefix position has the same parity as the current prefix position. Store counts by both prefix sum and prefix-length parity.', '', 'The quiet mistake is using one count per prefix sum, which also counts odd-length target slices. Seed sum zero at even prefix length before scanning. Time is O(n), bounded by input length, and the parity-split map uses O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let m=new Map([["0,0",1]]),s=0,r=0;for(let i=0;i<a.length;i++){s+=a[i];let q=(i+1)%2;r+=m.get((s-b)+","+q)||0;let k=s+","+q;m.set(k,(m.get(k)||0)+1)}return r}','function solve(a:number[],b:number):number{const m=new Map<string,number>([["0,0",1]]);let s=0,r=0;for(let i=0;i<a.length;i++){s+=a[i];const q=(i+1)%2;r+=m.get((s-b)+","+q)||0;const k=s+","+q;m.set(k,(m.get(k)||0)+1)}return r}','def solve(a,b):\n m={(0,0):1};s=r=0\n for i,x in enumerate(a):\n  s+=x;q=(i+1)%2;r+=m.get((s-b,q),0);m[(s,q)]=m.get((s,q),0)+1\n return r','    static int solve(int[]a,int b){java.util.HashMap<String,Integer>m=new java.util.HashMap<>();m.put("0,0",1);int s=0,r=0;for(int i=0;i<a.length;i++){s+=a[i];int q=(i+1)%2;String z=(s-b)+","+q;r+=m.getOrDefault(z,0);String k=s+","+q;m.put(k,m.getOrDefault(k,0)+1);}return r;}','int solve(vector<int>a,int b){map<pair<int,int>,int>m;m[{0,0}]=1;int s=0,r=0;for(int i=0;i<(int)a.size();i++){s+=a[i];int q=(i+1)%2;r+=m[{s-b,q}];m[{s,q}]++;}return r;}','func solve(a []int,b int) int {m:=map[string]int{"0,0":1};s,r:=0,0;for i,x:=range a{s+=x;q:=(i+1)%2;k:=strconv.Itoa(s-b)+","+strconv.Itoa(q);r+=m[k];z:=strconv.Itoa(s)+","+strconv.Itoa(q);m[z]++};return r}'),
    tests:[{stdin:'1 1 1 1\n2',expectedStdout:'3',isSample:true},{stdin:'1 -1 1 -1\n0',expectedStdout:'4',isSample:true},{stdin:'2\n2',expectedStdout:'0'},{stdin:'2 0 0\n2',expectedStdout:'1'},{stdin:'1 2 3\n3',expectedStdout:'1'}] }),
  p({ ...base, slug:'longest-target-sum-slice', title:'Longest Slice at Target', difficulty:'HARD', patternTags:['prefix-sum','hash-map','longest-subarray'], signatureId:'fn:ints,int->int', avgSolveSeconds:960,
    promptMarkdown:['Return the greatest length of a contiguous slice whose sum equals the target. Return `0` when none exists. Numbers may be negative.','','**Example**','','```','input: 1 -1 5 -2 3','3','output: 4','```','','The first four values sum to `3`. The list has at least one number; negative values prevent a sliding-window shortcut.'].join('\n'),
    editorialMarkdown:['## Earliest prefix occurrence wins','', 'If the running prefix is S, a target slice starts after an earlier prefix S-target. To maximize length, remember only the first index at which each prefix occurred. Every later occurrence of that same prefix would make a shorter candidate.', '', 'The quiet mistake is overwriting the first index in the map. That still finds some target slice, so it quietly fails only maximum-length tests. Seed prefix zero at index -1. Time is O(n), bounded by list length, and the map uses O(n) space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let m=new Map([[0,-1]]),s=0,r=0;for(let i=0;i<a.length;i++){s+=a[i];if(m.has(s-b))r=Math.max(r,i-m.get(s-b));if(!m.has(s))m.set(s,i)}return r}','function solve(a:number[],b:number):number{const m=new Map<number,number>([[0,-1]]);let s=0,r=0;for(let i=0;i<a.length;i++){s+=a[i];if(m.has(s-b))r=Math.max(r,i-m.get(s-b)!);if(!m.has(s))m.set(s,i)}return r}','def solve(a,b):\n m={0:-1};s=r=0\n for i,x in enumerate(a):\n  s+=x\n  if s-b in m:r=max(r,i-m[s-b])\n  if s not in m:m[s]=i\n return r','    static int solve(int[]a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();m.put(0,-1);int s=0,r=0;for(int i=0;i<a.length;i++){s+=a[i];if(m.containsKey(s-b))r=Math.max(r,i-m.get(s-b));if(!m.containsKey(s))m.put(s,i);}return r;}','int solve(vector<int>a,int b){unordered_map<int,int>m;m[0]=-1;int s=0,r=0;for(int i=0;i<(int)a.size();i++){s+=a[i];if(m.count(s-b))r=max(r,i-m[s-b]);if(!m.count(s))m[s]=i;}return r;}','func solve(a []int,b int) int {m:=map[int]int{0:-1};s,r:=0,0;for i,x:=range a{s+=x;if j,ok:=m[s-b];ok&&i-j>r{r=i-j};if _,ok:=m[s];!ok{m[s]=i}};return r}'),
    tests:[{stdin:'1 -1 5 -2 3\n3',expectedStdout:'4',isSample:true},{stdin:'1 2 3\n3',expectedStdout:'2',isSample:true},{stdin:'1 2 3\n7',expectedStdout:'0'},{stdin:'-1 1 -1 1\n0',expectedStdout:'4'},{stdin:'5\n5',expectedStdout:'1'}] }),
  p({ ...base, slug:'count-values-over-fraction-threshold', title:'How Many Values Beat One Over K?', difficulty:'HARD', patternTags:['hash-map','majority','threshold'], signatureId:'fn:ints,int->int', avgSolveSeconds:840,
    promptMarkdown:['Return how many distinct values occur strictly more than `n / k` times, where `n` is the list length and integer division is used.','','**Example**','','```','input: 1 1 1 2 2 3 4','3','output: 1','```','','Here `n / k` is `2`, so only `1` beats the threshold with three copies. The list is non-empty and `k` is at least 2; equality with the threshold does not qualify.'].join('\n'),
    editorialMarkdown:['## Frequency map against a moving majority threshold','', 'The threshold depends on the whole list, so count all values first, compute n divided by k, then count keys strictly above it. Returning a count avoids any unstable ordering among several qualifying values.', '', 'The quiet mistake is using greater-than-or-equal. A value occurring exactly n/k times is not over the stated fraction, and that off-by-one only appears on divisible lengths. Time is O(n), bounded by input length, with O(d) frequency-map space.'].join('\n'),
    referenceSolution:six('function solve(a,b){let m={};for(let x of a)m[x]=(m[x]||0)+1;let r=0;for(let x in m)if(m[x]>Math.floor(a.length/b))r++;return r}','function solve(a:number[],b:number):number{const m=new Map<number,number>();for(const x of a)m.set(x,(m.get(x)||0)+1);let r=0;for(const c of m.values())if(c>Math.floor(a.length/b))r++;return r}','def solve(a,b):\n m={}\n for x in a:m[x]=m.get(x,0)+1\n return sum(x>len(a)//b for x in m.values())','    static int solve(int[]a,int b){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();for(int x:a)m.put(x,m.getOrDefault(x,0)+1);int r=0;for(int c:m.values())if(c>a.length/b)r++;return r;}','int solve(vector<int>a,int b){unordered_map<int,int>m;for(int x:a)m[x]++;int r=0;for(auto e:m)if(e.second>(int)a.size()/b)r++;return r;}','func solve(a []int,b int) int {m:=map[int]int{};for _,x:=range a{m[x]++};r:=0;for _,c:=range m{if c>len(a)/b{r++}};return r}'),
    tests:[{stdin:'1 1 1 2 2 3 4\n3',expectedStdout:'1',isSample:true},{stdin:'1 1 2 2 3 3\n3',expectedStdout:'0',isSample:true},{stdin:'7\n2',expectedStdout:'1'},{stdin:'1 1 2 2 2 3\n2',expectedStdout:'0'},{stdin:'-1 -1 0 0 1\n4',expectedStdout:'2'}] }),
];
