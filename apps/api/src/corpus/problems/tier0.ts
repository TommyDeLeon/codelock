import { AUTHORED, type ProblemDefinition } from '../problem.js';

/**
 * Tier 0 — foundations.
 *
 * Loops, strings, arrays, hashmaps, and reading input. No published interview
 * list covers this material, because every list starts at "you can already
 * program". Our audience frequently cannot yet write a for-loop with
 * confidence, and handing that person a Tier 1 problem under a lock screen
 * teaches them only that they are not welcome.
 *
 * Every problem here is written from scratch and released CC0. That is not
 * incidental: this is the one tier we could not have imported our way out of,
 * and it is the tier the product most depends on.
 *
 * House style for these statements:
 *   - Say exactly what goes in and what comes out, with an example.
 *   - Name the edge case rather than hiding it as a gotcha. A beginner under a
 *     lock does not need a trick; they need a fair problem.
 *   - Keep the prose short. This is read at 11pm with the screen gone.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

export const TIER_0_PROBLEMS: ProblemDefinition[] = [
  p({
    slug: 'sum-of-array',
    title: 'Sum of an Array',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['loops', 'arrays', 'accumulator'],
    signatureId: 'fn:ints->int',
    avgSolveSeconds: 180,
    promptMarkdown: [
      'Add up every number in a list.',
      '',
      'Given a list of whole numbers, return their total.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 3 4',
      'output: 10',
      '```',
      '',
      'If the list is empty, the total is `0`.',
    ].join('\n'),
    editorialMarkdown: [
      '## The accumulator',
      '',
      'This is the first pattern worth having a name for. Keep one variable — the',
      '*accumulator* — outside the loop, and add each element to it as you pass.',
      '',
      'The two mistakes to know about:',
      '',
      '1. **Declaring the total inside the loop.** Then it resets on every pass and',
      '   you end up with the last element instead of the sum.',
      '2. **Starting at something other than zero.** Zero is the identity for',
      '   addition: adding it changes nothing, which is exactly what you want',
      '   before you have added anything. It is also why the empty list returns `0`',
      '   without a special case — the loop simply never runs.',
      '',
      'O(n) time and O(1) space, and you cannot do better: you have to look at',
      'every number at least once to add it.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a) {\n  let total = 0;\n  for (const n of a) total += n;\n  return total;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): number {\n  let total = 0;\n  for (const n of a) total += n;\n  return total;\n}',
      PYTHON: 'def solve(a):\n    total = 0\n    for n in a:\n        total += n\n    return total',
      JAVA: '    static int solve(int[] a) {\n        int total = 0;\n        for (int n : a) total += n;\n        return total;\n    }',
      CPP: 'int solve(vector<int> a) {\n    int total = 0;\n    for (int n : a) total += n;\n    return total;\n}',
      GO: 'func solve(a []int) int {\n\ttotal := 0\n\tfor _, n := range a {\n\t\ttotal += n\n\t}\n\treturn total\n}',
    },
    tests: [
      { stdin: '1 2 3 4', expectedStdout: '10', isSample: true },
      { stdin: '', expectedStdout: '0', isSample: true },
      { stdin: '-5 5', expectedStdout: '0' },
      { stdin: '7', expectedStdout: '7' },
      { stdin: '100 200 300 400 500', expectedStdout: '1500' },
      { stdin: '-1 -2 -3', expectedStdout: '-6' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'count-vowels',
    title: 'Count the Vowels',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['strings', 'loops', 'membership'],
    signatureId: 'fn:string->int',
    avgSolveSeconds: 200,
    promptMarkdown: [
      'Count how many vowels a line of text contains.',
      '',
      'The vowels are `a`, `e`, `i`, `o`, `u`. The input is always lower case.',
      '',
      '**Example**',
      '',
      '```',
      'input:  hello world',
      'output: 3',
      '```',
      '',
      'Spaces and punctuation are not vowels. An empty line has `0`.',
    ].join('\n'),
    editorialMarkdown: [
      '## Membership, not five comparisons',
      '',
      'The obvious version works:',
      '',
      '```',
      "if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u') count++;",
      '```',
      '',
      'The better version asks a set instead: *is this character in the vowels?*',
      'It reads like the sentence you would say out loud, and it does not grow a',
      'new clause every time the definition changes.',
      '',
      'This is the same idea you will meet again as "use a hash set for O(1)',
      'membership". Here the set has five elements, so the speed is irrelevant —',
      'but the shape is the thing to take away, and you will use it constantly.',
      '',
      'O(n) in the length of the string.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        "function solve(a) {\n  const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n  let count = 0;\n  for (const c of a) if (vowels.has(c)) count++;\n  return count;\n}",
      TYPESCRIPT:
        "function solve(a: string): number {\n  const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n  let count = 0;\n  for (const c of a) if (vowels.has(c)) count++;\n  return count;\n}",
      PYTHON: "def solve(a):\n    vowels = set('aeiou')\n    return sum(1 for c in a if c in vowels)",
      JAVA: '    static int solve(String a) {\n        String vowels = "aeiou";\n        int count = 0;\n        for (char c : a.toCharArray()) if (vowels.indexOf(c) >= 0) count++;\n        return count;\n    }',
      CPP: 'int solve(string a) {\n    string vowels = "aeiou";\n    int count = 0;\n    for (char c : a) if (vowels.find(c) != string::npos) count++;\n    return count;\n}',
      GO: 'func solve(a string) int {\n\tcount := 0\n\tfor _, c := range a {\n\t\tif strings.ContainsRune("aeiou", c) {\n\t\t\tcount++\n\t\t}\n\t}\n\treturn count\n}',
    },
    tests: [
      { stdin: 'hello world', expectedStdout: '3', isSample: true },
      { stdin: 'xyz', expectedStdout: '0', isSample: true },
      { stdin: '', expectedStdout: '0' },
      { stdin: 'aeiou', expectedStdout: '5' },
      { stdin: 'the quick brown fox jumps over the lazy dog', expectedStdout: '11' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'fizzbuzz-list',
    title: 'FizzBuzz',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['loops', 'conditionals', 'modulo'],
    signatureId: 'fn:int->strings',
    avgSolveSeconds: 240,
    promptMarkdown: [
      'The classic.',
      '',
      'Given a number `n`, return the list of words for `1` through `n`:',
      '',
      '- a multiple of both 3 and 5 becomes `FizzBuzz`',
      '- a multiple of 3 becomes `Fizz`',
      '- a multiple of 5 becomes `Buzz`',
      '- anything else becomes the number itself',
      '',
      '**Example**',
      '',
      '```',
      'input:  5',
      'output: 1 2 Fizz 4 Buzz',
      '```',
    ].join('\n'),
    editorialMarkdown: [
      '## Order the conditions from most specific to least',
      '',
      'The entire difficulty of FizzBuzz is the overlap at 15. Check',
      '`divisible by 3 and 5` **first**: if you check `divisible by 3` first, then',
      '15 matches it, you append `Fizz`, and you never reach the case you wanted.',
      '',
      'That is a general rule worth keeping — when conditions overlap, the most',
      'specific one goes first, or it is unreachable.',
      '',
      'The other common way to write it builds the word by concatenation:',
      '',
      '```',
      'word = (n % 3 == 0 ? "Fizz" : "") + (n % 5 == 0 ? "Buzz" : "")',
      'if word is empty, use the number',
      '```',
      '',
      'That version has no overlap problem at all, because 15 simply matches both',
      'halves. Either is fine; the second generalises if a third rule arrives.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        "function solve(a) {\n  const out = [];\n  for (let i = 1; i <= a; i++) {\n    if (i % 15 === 0) out.push('FizzBuzz');\n    else if (i % 3 === 0) out.push('Fizz');\n    else if (i % 5 === 0) out.push('Buzz');\n    else out.push(String(i));\n  }\n  return out;\n}",
      TYPESCRIPT:
        "function solve(a: number): string[] {\n  const out: string[] = [];\n  for (let i = 1; i <= a; i++) {\n    if (i % 15 === 0) out.push('FizzBuzz');\n    else if (i % 3 === 0) out.push('Fizz');\n    else if (i % 5 === 0) out.push('Buzz');\n    else out.push(String(i));\n  }\n  return out;\n}",
      PYTHON:
        "def solve(a):\n    out = []\n    for i in range(1, a + 1):\n        if i % 15 == 0:\n            out.append('FizzBuzz')\n        elif i % 3 == 0:\n            out.append('Fizz')\n        elif i % 5 == 0:\n            out.append('Buzz')\n        else:\n            out.append(str(i))\n    return out",
      JAVA: '    static String[] solve(int a) {\n        String[] out = new String[a];\n        for (int i = 1; i <= a; i++) {\n            if (i % 15 == 0) out[i - 1] = "FizzBuzz";\n            else if (i % 3 == 0) out[i - 1] = "Fizz";\n            else if (i % 5 == 0) out[i - 1] = "Buzz";\n            else out[i - 1] = String.valueOf(i);\n        }\n        return out;\n    }',
      CPP: 'vector<string> solve(int a) {\n    vector<string> out;\n    for (int i = 1; i <= a; i++) {\n        if (i % 15 == 0) out.push_back("FizzBuzz");\n        else if (i % 3 == 0) out.push_back("Fizz");\n        else if (i % 5 == 0) out.push_back("Buzz");\n        else out.push_back(to_string(i));\n    }\n    return out;\n}',
      GO: 'func solve(a int) []string {\n\tout := []string{}\n\tfor i := 1; i <= a; i++ {\n\t\tswitch {\n\t\tcase i%15 == 0:\n\t\t\tout = append(out, "FizzBuzz")\n\t\tcase i%3 == 0:\n\t\t\tout = append(out, "Fizz")\n\t\tcase i%5 == 0:\n\t\t\tout = append(out, "Buzz")\n\t\tdefault:\n\t\t\tout = append(out, strconv.Itoa(i))\n\t\t}\n\t}\n\treturn out\n}',
    },
    tests: [
      { stdin: '5', expectedStdout: '1 2 Fizz 4 Buzz', isSample: true },
      {
        stdin: '15',
        expectedStdout: '1 2 Fizz 4 Buzz Fizz 7 8 Fizz Buzz 11 Fizz 13 14 FizzBuzz',
        isSample: true,
      },
      { stdin: '1', expectedStdout: '1' },
      { stdin: '3', expectedStdout: '1 2 Fizz' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'reverse-a-string',
    title: 'Reverse a String',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['strings', 'loops', 'indexing'],
    signatureId: 'fn:string->string',
    avgSolveSeconds: 180,
    promptMarkdown: [
      'Return the input line with its characters in the opposite order.',
      '',
      '**Example**',
      '',
      '```',
      'input:  hello',
      'output: olleh',
      '```',
      '',
      'Spaces count as characters. An empty line reverses to an empty line.',
    ].join('\n'),
    editorialMarkdown: [
      '## Walking an index backwards',
      '',
      'Most languages will reverse a string for you in one call, and in real code',
      'you should use it. Write the loop once anyway, because the off-by-one here',
      'is the one you will meet in every two-pointer problem later:',
      '',
      '```',
      'for (i = length - 1; i >= 0; i--)',
      '```',
      '',
      '`length - 1` because the last valid index is one less than the count, and',
      '`>= 0` because index zero is a real character. Writing `> 0` silently drops',
      'the first letter, and that bug is quiet — the output still looks reversed.',
      '',
      'O(n) time, O(n) space for the result. You cannot do it in O(1) space here,',
      'because strings are immutable in most of these languages.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        "function solve(a) {\n  let out = '';\n  for (let i = a.length - 1; i >= 0; i--) out += a[i];\n  return out;\n}",
      TYPESCRIPT:
        "function solve(a: string): string {\n  let out = '';\n  for (let i = a.length - 1; i >= 0; i--) out += a[i];\n  return out;\n}",
      PYTHON: 'def solve(a):\n    return a[::-1]',
      JAVA: '    static String solve(String a) {\n        StringBuilder sb = new StringBuilder();\n        for (int i = a.length() - 1; i >= 0; i--) sb.append(a.charAt(i));\n        return sb.toString();\n    }',
      CPP: 'string solve(string a) {\n    string out;\n    for (int i = (int)a.size() - 1; i >= 0; i--) out += a[i];\n    return out;\n}',
      GO: 'func solve(a string) string {\n\tr := []rune(a)\n\tfor i, j := 0, len(r)-1; i < j; i, j = i+1, j-1 {\n\t\tr[i], r[j] = r[j], r[i]\n\t}\n\treturn string(r)\n}',
    },
    tests: [
      { stdin: 'hello', expectedStdout: 'olleh', isSample: true },
      { stdin: 'a', expectedStdout: 'a', isSample: true },
      { stdin: '', expectedStdout: '' },
      { stdin: 'ab cd', expectedStdout: 'dc ba' },
      { stdin: 'racecar', expectedStdout: 'racecar' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'largest-number',
    title: 'Largest Number in a List',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['loops', 'arrays', 'running-best'],
    signatureId: 'fn:ints->int',
    avgSolveSeconds: 200,
    promptMarkdown: [
      'Find the biggest number in a list.',
      '',
      '**Example**',
      '',
      '```',
      'input:  3 9 2 7',
      'output: 9',
      '```',
      '',
      'The list always has at least one number. Numbers can be negative.',
    ].join('\n'),
    editorialMarkdown: [
      '## Start from the data, not from zero',
      '',
      'The pattern is a *running best*: hold the best value seen so far, compare',
      'each new element against it, replace when the new one wins.',
      '',
      'The trap is what you start with. Starting at `0` looks harmless and is wrong',
      'the moment every number is negative — `[-5, -2]` would answer `0`, a value',
      'that was never in the list. Start with the **first element** instead. It is',
      'guaranteed to be a real answer, and it is why the statement promises the',
      'list is not empty.',
      '',
      'The same reasoning gives the alternative: start at negative infinity, which',
      'is the identity for `max` the way `0` is the identity for `+`.',
      '',
      'O(n) time, O(1) space.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  let best = a[0];\n  for (const n of a) if (n > best) best = n;\n  return best;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): number {\n  let best = a[0];\n  for (const n of a) if (n > best) best = n;\n  return best;\n}',
      PYTHON: 'def solve(a):\n    best = a[0]\n    for n in a:\n        if n > best:\n            best = n\n    return best',
      JAVA: '    static int solve(int[] a) {\n        int best = a[0];\n        for (int n : a) if (n > best) best = n;\n        return best;\n    }',
      CPP: 'int solve(vector<int> a) {\n    int best = a[0];\n    for (int n : a) if (n > best) best = n;\n    return best;\n}',
      GO: 'func solve(a []int) int {\n\tbest := a[0]\n\tfor _, n := range a {\n\t\tif n > best {\n\t\t\tbest = n\n\t\t}\n\t}\n\treturn best\n}',
    },
    tests: [
      { stdin: '3 9 2 7', expectedStdout: '9', isSample: true },
      { stdin: '-5 -2 -9', expectedStdout: '-2', isSample: true },
      { stdin: '4', expectedStdout: '4' },
      { stdin: '1 1 1', expectedStdout: '1' },
      { stdin: '-1 0 1', expectedStdout: '1' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'count-occurrences',
    title: 'Count Occurrences',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['loops', 'arrays', 'counting'],
    signatureId: 'fn:ints,int->int',
    avgSolveSeconds: 200,
    promptMarkdown: [
      'Count how many times a number appears in a list.',
      '',
      'The first line is the list. The second line is the number to look for.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 2 3',
      '        2',
      'output: 2',
      '```',
      '',
      'If the number never appears, the answer is `0`.',
    ].join('\n'),
    editorialMarkdown: [
      '## A counter is an accumulator that adds one',
      '',
      'Same shape as summing a list — one variable outside the loop — except you',
      'add `1` when the element matches instead of adding the element.',
      '',
      'Two things worth noticing:',
      '',
      '- **Do not return early.** A `return` inside the loop on the first match',
      '  gives you "does it appear", not "how many times". Let the loop finish.',
      '- **The not-found case is free.** The counter is already `0`, so there is no',
      '  special case to write. That is a recurring benefit of starting from the',
      '  identity value.',
      '',
      'O(n) time. If you had to answer this for many different targets, you would',
      'count everything once into a hash map instead — which is the next thing you',
      'will learn, and why this problem is worth doing by hand first.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  let count = 0;\n  for (const n of a) if (n === b) count++;\n  return count;\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number): number {\n  let count = 0;\n  for (const n of a) if (n === b) count++;\n  return count;\n}',
      PYTHON: 'def solve(a, b):\n    count = 0\n    for n in a:\n        if n == b:\n            count += 1\n    return count',
      JAVA: '    static int solve(int[] a, int b) {\n        int count = 0;\n        for (int n : a) if (n == b) count++;\n        return count;\n    }',
      CPP: 'int solve(vector<int> a, int b) {\n    int count = 0;\n    for (int n : a) if (n == b) count++;\n    return count;\n}',
      GO: 'func solve(a []int, b int) int {\n\tcount := 0\n\tfor _, n := range a {\n\t\tif n == b {\n\t\t\tcount++\n\t\t}\n\t}\n\treturn count\n}',
    },
    tests: [
      { stdin: '1 2 2 3\n2', expectedStdout: '2', isSample: true },
      { stdin: '1 2 3\n9', expectedStdout: '0', isSample: true },
      { stdin: '5 5 5 5\n5', expectedStdout: '4' },
      { stdin: '\n1', expectedStdout: '0' },
      { stdin: '-3 -3 4\n-3', expectedStdout: '2' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'palindrome-check',
    title: 'Is It a Palindrome?',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['strings', 'two-pointers', 'comparison'],
    signatureId: 'fn:string->bool',
    avgSolveSeconds: 240,
    promptMarkdown: [
      'Decide whether a line reads the same forwards and backwards.',
      '',
      'Return `true` if it does, `false` otherwise. Compare the characters exactly',
      'as given — do not remove spaces. The input is always lower case.',
      '',
      '**Example**',
      '',
      '```',
      'input:  racecar',
      'output: true',
      '```',
      '',
      'An empty line and a single character are both palindromes.',
    ].join('\n'),
    editorialMarkdown: [
      '## Two pointers, walking toward each other',
      '',
      'You can reverse the string and compare, and that is a perfectly good answer.',
      'The one worth learning puts a pointer at each end and walks them inward:',
      '',
      '```',
      'left = 0, right = length - 1',
      'while left < right:',
      '    if s[left] != s[right]: return false',
      '    left++, right--',
      'return true',
      '```',
      '',
      'Why it is better: it stops at the *first* mismatch instead of building a',
      'whole second string, and it uses O(1) extra space.',
      '',
      'The loop condition is `left < right`, not `<=`. When they meet on the middle',
      'character of an odd-length string, that character is trivially equal to',
      'itself — checking it costs a comparison and buys nothing.',
      '',
      'This is your first two-pointer problem. The family it belongs to is one of',
      'the largest in interviews, and it all looks like this.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  let left = 0;\n  let right = a.length - 1;\n  while (left < right) {\n    if (a[left] !== a[right]) return false;\n    left++;\n    right--;\n  }\n  return true;\n}',
      TYPESCRIPT:
        'function solve(a: string): boolean {\n  let left = 0;\n  let right = a.length - 1;\n  while (left < right) {\n    if (a[left] !== a[right]) return false;\n    left++;\n    right--;\n  }\n  return true;\n}',
      PYTHON:
        'def solve(a):\n    left, right = 0, len(a) - 1\n    while left < right:\n        if a[left] != a[right]:\n            return False\n        left += 1\n        right -= 1\n    return True',
      JAVA: '    static boolean solve(String a) {\n        int left = 0, right = a.length() - 1;\n        while (left < right) {\n            if (a.charAt(left) != a.charAt(right)) return false;\n            left++;\n            right--;\n        }\n        return true;\n    }',
      CPP: 'bool solve(string a) {\n    int left = 0, right = (int)a.size() - 1;\n    while (left < right) {\n        if (a[left] != a[right]) return false;\n        left++;\n        right--;\n    }\n    return true;\n}',
      GO: 'func solve(a string) bool {\n\tr := []rune(a)\n\tfor i, j := 0, len(r)-1; i < j; i, j = i+1, j-1 {\n\t\tif r[i] != r[j] {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}',
    },
    tests: [
      { stdin: 'racecar', expectedStdout: 'true', isSample: true },
      { stdin: 'hello', expectedStdout: 'false', isSample: true },
      { stdin: '', expectedStdout: 'true' },
      { stdin: 'a', expectedStdout: 'true' },
      { stdin: 'abba', expectedStdout: 'true' },
      // Spaces are characters like any other, so this one *is* a palindrome:
      // a, b, space, b, a reads the same either way.
      { stdin: 'ab ba', expectedStdout: 'true' },
      { stdin: 'a bc', expectedStdout: 'false' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'first-repeated-number',
    title: 'First Repeated Number',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['hash-set', 'arrays', 'seen-before'],
    signatureId: 'fn:ints->int',
    avgSolveSeconds: 300,
    promptMarkdown: [
      'Find the first number that appears twice.',
      '',
      'Scanning from left to right, return the first number you reach that you',
      'have already seen earlier in the list.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 3 2 1',
      'output: 2',
      '```',
      '',
      '`2` wins because its *second* appearance comes before the second `1`.',
      '',
      'If every number is unique, return `-1`.',
    ].join('\n'),
    editorialMarkdown: [
      '## "Have I seen this before?" is a hash set',
      '',
      'The straightforward version compares every element against every earlier one',
      '— two nested loops, O(n^2). It is correct, and on a long list it is slow.',
      '',
      'The pattern that replaces it is the single most useful one in this corpus:',
      '**remember what you have seen in a hash set**. Then "have I seen this?" is',
      'one lookup instead of a scan.',
      '',
      '```',
      'seen = empty set',
      'for n in list:',
      '    if n in seen: return n',
      '    add n to seen',
      'return -1',
      '```',
      '',
      'O(n) time and O(n) space. You traded memory for speed, deliberately — that',
      'trade is most of what algorithm design consists of.',
      '',
      'Why a set lookup is O(1) is worth knowing rather than memorising, and it is',
      'exactly what you build in the Tier 0.5 hash-table problem. An interviewer',
      'who asks "why is that constant time?" is asking whether you know or whether',
      'you are repeating something.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const seen = new Set();\n  for (const n of a) {\n    if (seen.has(n)) return n;\n    seen.add(n);\n  }\n  return -1;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): number {\n  const seen = new Set<number>();\n  for (const n of a) {\n    if (seen.has(n)) return n;\n    seen.add(n);\n  }\n  return -1;\n}',
      PYTHON:
        'def solve(a):\n    seen = set()\n    for n in a:\n        if n in seen:\n            return n\n        seen.add(n)\n    return -1',
      JAVA: '    static int solve(int[] a) {\n        Set<Integer> seen = new HashSet<>();\n        for (int n : a) {\n            if (seen.contains(n)) return n;\n            seen.add(n);\n        }\n        return -1;\n    }',
      CPP: 'int solve(vector<int> a) {\n    unordered_set<int> seen;\n    for (int n : a) {\n        if (seen.count(n)) return n;\n        seen.insert(n);\n    }\n    return -1;\n}',
      GO: 'func solve(a []int) int {\n\tseen := map[int]bool{}\n\tfor _, n := range a {\n\t\tif seen[n] {\n\t\t\treturn n\n\t\t}\n\t\tseen[n] = true\n\t}\n\treturn -1\n}',
    },
    tests: [
      { stdin: '1 2 3 2 1', expectedStdout: '2', isSample: true },
      { stdin: '1 2 3', expectedStdout: '-1', isSample: true },
      { stdin: '', expectedStdout: '-1' },
      { stdin: '5 5', expectedStdout: '5' },
      { stdin: '9 8 7 6 9', expectedStdout: '9' },
      { stdin: '-1 2 -1', expectedStdout: '-1' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'most-frequent-character',
    title: 'Most Frequent Character',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['hash-map', 'strings', 'frequency-count'],
    signatureId: 'fn:string->string',
    avgSolveSeconds: 330,
    promptMarkdown: [
      'Find the character that appears most often.',
      '',
      'Return it as a one-character string. The input is lower-case letters only,',
      'with no spaces, and is never empty.',
      '',
      '**Example**',
      '',
      '```',
      'input:  aabbbcc',
      'output: b',
      '```',
      '',
      'If two characters tie, return whichever appears **first** in the string.',
    ].join('\n'),
    editorialMarkdown: [
      '## Count first, then choose',
      '',
      'Two passes, and trying to do it in one is where this goes wrong.',
      '',
      '**Pass one** builds a frequency map: character to count. **Pass two** walks',
      'the map — or better, walks the *original string* — and keeps the best.',
      '',
      'The tie-break is the interesting part. The statement says "whichever appears',
      'first in the string", and a hash map has no order, so iterating the map can',
      'give you either winner depending on the language and the day. Iterating the',
      '**string** instead, and only replacing the best on a strictly greater count,',
      'gives the first-appearing winner every time.',
      '',
      'That is a real lesson and not a technicality: when a problem specifies a',
      'tie-break, your iteration order has to be one you actually control.',
      '',
      'O(n) time, O(k) space where k is the size of the alphabet.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const counts = new Map();\n  for (const c of a) counts.set(c, (counts.get(c) || 0) + 1);\n  let best = a[0];\n  for (const c of a) if (counts.get(c) > counts.get(best)) best = c;\n  return best;\n}',
      TYPESCRIPT:
        'function solve(a: string): string {\n  const counts = new Map<string, number>();\n  for (const c of a) counts.set(c, (counts.get(c) || 0) + 1);\n  let best = a[0];\n  for (const c of a) if ((counts.get(c) as number) > (counts.get(best) as number)) best = c;\n  return best;\n}',
      PYTHON:
        'def solve(a):\n    counts = {}\n    for c in a:\n        counts[c] = counts.get(c, 0) + 1\n    best = a[0]\n    for c in a:\n        if counts[c] > counts[best]:\n            best = c\n    return best',
      JAVA: '    static String solve(String a) {\n        Map<Character, Integer> counts = new HashMap<>();\n        for (char c : a.toCharArray()) counts.merge(c, 1, Integer::sum);\n        char best = a.charAt(0);\n        for (char c : a.toCharArray()) if (counts.get(c) > counts.get(best)) best = c;\n        return String.valueOf(best);\n    }',
      CPP: 'string solve(string a) {\n    unordered_map<char, int> counts;\n    for (char c : a) counts[c]++;\n    char best = a[0];\n    for (char c : a) if (counts[c] > counts[best]) best = c;\n    return string(1, best);\n}',
      GO: 'func solve(a string) string {\n\tcounts := map[rune]int{}\n\tfor _, c := range a {\n\t\tcounts[c]++\n\t}\n\tr := []rune(a)\n\tbest := r[0]\n\tfor _, c := range r {\n\t\tif counts[c] > counts[best] {\n\t\t\tbest = c\n\t\t}\n\t}\n\treturn string(best)\n}',
    },
    tests: [
      { stdin: 'aabbbcc', expectedStdout: 'b', isSample: true },
      { stdin: 'abc', expectedStdout: 'a', isSample: true },
      { stdin: 'z', expectedStdout: 'z' },
      { stdin: 'aabb', expectedStdout: 'a' },
      { stdin: 'xxyyyzz', expectedStdout: 'y' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'add-two-lists',
    title: 'Add Two Lists Position by Position',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['arrays', 'loops', 'parallel-iteration'],
    signatureId: 'fn:ints,ints->ints',
    avgSolveSeconds: 240,
    promptMarkdown: [
      'Add two lists together, position by position.',
      '',
      'The two lists always have the same length. Return a list where each entry',
      'is the sum of the entries at that position.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 3',
      '        4 5 6',
      'output: 5 7 9',
      '```',
      '',
      'Two empty lists give an empty list.',
    ].join('\n'),
    editorialMarkdown: [
      '## One index, two lists',
      '',
      'The shape to reach for is a single loop over the *positions*, using the',
      'index to read from both lists:',
      '',
      '```',
      'for i in 0 .. length - 1:',
      '    out[i] = first[i] + second[i]',
      '```',
      '',
      'Not two nested loops — that would pair every element with every other and',
      'give you n^2 sums, which is a different problem entirely.',
      '',
      'The statement promises equal lengths, which is what makes a single bound',
      'safe. Notice how much that promise is doing: without it you would have to',
      'decide what happens when one list runs out, and "the shorter one wins" and',
      '"treat the missing entries as zero" are both defensible. When a statement',
      'guarantees something, it is usually removing exactly one decision like that',
      '— worth spotting, because in an interview the guarantee is often left out on',
      'purpose and the real question is whether you ask.',
      '',
      'O(n) time, O(n) space for the output.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  const out = [];\n  for (let i = 0; i < a.length; i++) out.push(a[i] + b[i]);\n  return out;\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number[]): number[] {\n  const out: number[] = [];\n  for (let i = 0; i < a.length; i++) out.push(a[i] + b[i]);\n  return out;\n}',
      PYTHON: 'def solve(a, b):\n    return [a[i] + b[i] for i in range(len(a))]',
      JAVA: '    static int[] solve(int[] a, int[] b) {\n        int[] out = new int[a.length];\n        for (int i = 0; i < a.length; i++) out[i] = a[i] + b[i];\n        return out;\n    }',
      CPP: 'vector<int> solve(vector<int> a, vector<int> b) {\n    vector<int> out;\n    for (size_t i = 0; i < a.size(); i++) out.push_back(a[i] + b[i]);\n    return out;\n}',
      GO: 'func solve(a []int, b []int) []int {\n\tout := []int{}\n\tfor i := range a {\n\t\tout = append(out, a[i]+b[i])\n\t}\n\treturn out\n}',
    },
    tests: [
      { stdin: '1 2 3\n4 5 6', expectedStdout: '5 7 9', isSample: true },
      { stdin: '\n', expectedStdout: '', isSample: true },
      { stdin: '0\n0', expectedStdout: '0' },
      { stdin: '-1 -2\n1 2', expectedStdout: '0 0' },
      { stdin: '10 20 30 40\n1 2 3 4', expectedStdout: '11 22 33 44' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'average-of-list',
    title: 'Average of a List',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['loops', 'arrays', 'integer-division'],
    signatureId: 'fn:ints->double',
    avgSolveSeconds: 260,
    promptMarkdown: [
      'Return the average of a list of whole numbers.',
      '',
      'The answer is a decimal, printed to six decimal places. The list always has',
      'at least one number.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 3 4',
      'output: 2.500000',
      '```',
      '',
      'You do not need to format the number yourself — return the value and the',
      'grader prints it.',
    ].join('\n'),
    editorialMarkdown: [
      '## The integer-division trap',
      '',
      'The arithmetic is sum divided by count. The bug is that in most of these',
      'languages, dividing an integer by an integer gives you an **integer**:',
      '`10 / 4` is `2`, not `2.5`, and the `.5` is discarded silently. No error, no',
      'warning, just a quietly wrong answer — which is the worst kind.',
      '',
      'The fix is to make at least one side a decimal before dividing:',
      '',
      '- C++/Java: `(double) sum / count`',
      '- Go: `float64(sum) / float64(count)`',
      '- Python: `/` is already float division (`//` is the integer one)',
      '- JavaScript: numbers are floats already, so this one is free',
      '',
      'Worth meeting early, because it will bite you in every language that has',
      'separate integer and floating-point types, and it never announces itself.',
      '',
      'O(n) time, O(1) space.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  let total = 0;\n  for (const n of a) total += n;\n  return total / a.length;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): number {\n  let total = 0;\n  for (const n of a) total += n;\n  return total / a.length;\n}',
      PYTHON: 'def solve(a):\n    return sum(a) / len(a)',
      JAVA: '    static double solve(int[] a) {\n        int total = 0;\n        for (int n : a) total += n;\n        return (double) total / a.length;\n    }',
      CPP: 'double solve(vector<int> a) {\n    int total = 0;\n    for (int n : a) total += n;\n    return (double) total / a.size();\n}',
      GO: 'func solve(a []int) float64 {\n\ttotal := 0\n\tfor _, n := range a {\n\t\ttotal += n\n\t}\n\treturn float64(total) / float64(len(a))\n}',
    },
    tests: [
      { stdin: '1 2 3 4', expectedStdout: '2.500000', isSample: true },
      { stdin: '5', expectedStdout: '5.000000', isSample: true },
      { stdin: '1 2', expectedStdout: '1.500000' },
      { stdin: '-2 2', expectedStdout: '0.000000' },
      { stdin: '10 4', expectedStdout: '7.000000' },
    ],
    provenance: AUTHORED,
  }),

  p({
    slug: 'sum-comma-separated',
    title: 'Sum a Comma-Separated Line',
    difficulty: 'EASY',
    tier: 'TIER_0',
    patternFamily: 'FOUNDATIONS',
    patternTags: ['parsing', 'strings', 'split'],
    signatureId: 'fn:string->int',
    avgSolveSeconds: 300,
    promptMarkdown: [
      'Add up the numbers in a comma-separated line.',
      '',
      'You are given the line as raw text, not as a list. Split it yourself.',
      '',
      '**Example**',
      '',
      '```',
      'input:  3,4,5',
      'output: 12',
      '```',
      '',
      'There are no spaces. Numbers may be negative. An empty line sums to `0`.',
    ].join('\n'),
    editorialMarkdown: [
      '## Parsing is a skill, not a chore',
      '',
      'Most of the problems here hand you a ready-made list. Real input arrives as',
      'text, and turning text into data is its own step: **split**, then **convert**.',
      '',
      '```',
      'parts = line.split(",")',
      'for part in parts: total += toInteger(part)',
      '```',
      '',
      'The edge case worth naming is the empty line. Splitting `""` on `","` does',
      'not give you an empty list in most languages — it gives you a list',
      'containing one empty string, and converting `""` to a number then throws.',
      'Check for the empty line before you split, or skip empty pieces as you go.',
      '',
      'That mismatch between "no items" and "one empty item" is behind a large',
      'share of real parsing bugs, and it is why this problem exists.',
      '',
      'O(n) in the length of the line.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        "function solve(a) {\n  if (a.trim() === '') return 0;\n  let total = 0;\n  for (const part of a.split(',')) total += parseInt(part, 10);\n  return total;\n}",
      TYPESCRIPT:
        "function solve(a: string): number {\n  if (a.trim() === '') return 0;\n  let total = 0;\n  for (const part of a.split(',')) total += parseInt(part, 10);\n  return total;\n}",
      PYTHON:
        "def solve(a):\n    if a.strip() == '':\n        return 0\n    return sum(int(part) for part in a.split(','))",
      JAVA: '    static int solve(String a) {\n        if (a.trim().isEmpty()) return 0;\n        int total = 0;\n        for (String part : a.split(",")) total += Integer.parseInt(part.trim());\n        return total;\n    }',
      CPP: "int solve(string a) {\n    int total = 0;\n    string part;\n    stringstream ss(a);\n    while (getline(ss, part, ',')) {\n        if (!part.empty()) total += stoi(part);\n    }\n    return total;\n}",
      GO: 'func solve(a string) int {\n\ttotal := 0\n\tif strings.TrimSpace(a) == "" {\n\t\treturn 0\n\t}\n\tfor _, part := range strings.Split(a, ",") {\n\t\tn, _ := strconv.Atoi(strings.TrimSpace(part))\n\t\ttotal += n\n\t}\n\treturn total\n}',
    },
    tests: [
      { stdin: '3,4,5', expectedStdout: '12', isSample: true },
      { stdin: '', expectedStdout: '0', isSample: true },
      { stdin: '42', expectedStdout: '42' },
      { stdin: '-1,1', expectedStdout: '0' },
      { stdin: '10,-20,30', expectedStdout: '20' },
    ],
    provenance: AUTHORED,
  }),
];
