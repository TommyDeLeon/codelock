import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'TREES', provenance: AUTHORED } as const;

export const TIER_2_TREES_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base, slug: 'tree-minimum-leaf-depth', title: 'Nearest Exit From the Root', difficulty: 'MEDIUM', patternTags: ['trees', 'dfs', 'recursion'], signatureId: 'fn:tree->int', avgSolveSeconds: 660,
    promptMarkdown: ['Return the number of nodes on the shortest path from the root to a leaf. A leaf has no children. Do not stop at a missing child: a node with one child is not a leaf.', '', '**Example**', '', '```', 'input:  1 2 null 3 null 4', 'output: 4', '```', '', 'Every node on this one-sided tree has one child until `4`, so the nearest leaf is four nodes away. The tree may be empty; its answer is `0`. A single node answers `1`.'].join('\n'),
    editorialMarkdown: ['## Depth-first recursion with a missing-side rule', '', 'The familiar maximum-depth recurrence is a trap here. A node with two children can take one plus the smaller child answer, but a missing child does not offer a short route to a leaf. Give a missing subtree an infinite distance, so the existing child is chosen; a leaf is handled directly as one. This makes each recursive answer exactly the nearest leaf distance below that node.', '', 'The quiet mistake is returning zero for a null child and applying `min`. It makes every one-sided node look like it reaches a leaf immediately, yet it passes on perfect trees and single nodes. Check whether the node itself is a leaf before recursing. Time is O(n), visiting each node once, and space is O(h) for the recursion stack, bounded by tree height.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){function d(n){if(!n)return Infinity;if(!n.left&&!n.right)return 1;return 1+Math.min(d(n.left),d(n.right));}return a?d(a):0;}',
      TYPESCRIPT: 'function solve(a: TreeNode | null): number { function d(n: TreeNode | null): number { if(!n)return Infinity; if(!n.left&&!n.right)return 1; return 1+Math.min(d(n.left),d(n.right)); } return a?d(a):0; }',
      PYTHON: 'def solve(a):\n    def depth(node):\n        if node is None: return float("inf")\n        if node.left is None and node.right is None: return 1\n        return 1 + min(depth(node.left), depth(node.right))\n    return 0 if a is None else depth(a)',
      JAVA: '    static int minLeaf(TreeNode n) { if(n==null)return 1000000; if(n.left==null&&n.right==null)return 1; int l=minLeaf(n.left),r=minLeaf(n.right);return 1+(l<r?l:r); }\n    static int solve(TreeNode a) { return a==null?0:minLeaf(a); }',
      CPP: 'int minLeaf(TreeNode* n){if(n==nullptr)return 1000000;if(n->left==nullptr&&n->right==nullptr)return 1;int l=minLeaf(n->left),r=minLeaf(n->right);return 1+(l<r?l:r);}\nint solve(TreeNode* a){return a==nullptr?0:minLeaf(a);}',
      GO: 'func minLeaf(n *TreeNode) int { if n==nil{return 1000000};if n.Left==nil&&n.Right==nil{return 1};l,r:=minLeaf(n.Left),minLeaf(n.Right);if l<r{return 1+l};return 1+r }\nfunc solve(a *TreeNode) int {if a==nil{return 0};return minLeaf(a)}',
    }, tests: [
      { stdin: '1 2 null 3 null 4', expectedStdout: '4', isSample: true }, { stdin: '1 2 3 4 null null null', expectedStdout: '2', isSample: true }, { stdin: '', expectedStdout: '0' }, { stdin: '7', expectedStdout: '1' }, { stdin: '1 null 2 null 3', expectedStdout: '3' },
    ],
  }),
  p({
    ...base, slug: 'tree-identical-root-branches', title: 'Two Branches, Same Way Round', difficulty: 'MEDIUM', patternTags: ['trees', 'dfs', 'recursion'], signatureId: 'fn:tree->bool', avgSolveSeconds: 660,
    promptMarkdown: ['Decide whether the root’s left and right subtrees are identical in both shape and values. Unlike a mirror test, corresponding children must stay on the same side. Print `true` or `false`.', '', '**Example**', '', '```', 'input:  1 2 2 3 4 3 4', 'output: true', '```', '', 'Both branches are `2` with left child `3` and right child `4`. An empty tree and a single-node tree answer `true`, because their two root branches are both absent.'].join('\n'),
    editorialMarkdown: ['## Paired depth-first traversal', '', 'This is a relation between two nodes, so recurse on a pair beginning with the root children. Two absent nodes agree, exactly one absent node disagrees, and two present nodes agree only when their values agree and their left children agree with each other while their right children agree with each other. That same-side pairing is what makes this task differ from symmetry.', '', 'The quiet mistake is crossing the pairs as `left.left` against `right.right`; that silently solves the mirror problem instead. It passes when a branch happens to be both identical and mirrored, including many all-equal tests. Time is O(n), as corresponding nodes are inspected once, and space is O(h) for the recursion stack, bounded by height.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){function same(x,y){if(!x||!y)return x===y;return x.val===y.val&&same(x.left,y.left)&&same(x.right,y.right);}return !a||same(a.left,a.right);}',
      TYPESCRIPT: 'function solve(a: TreeNode | null): boolean { function same(x: TreeNode | null,y: TreeNode | null): boolean {if(!x||!y)return x===y;return x.val===y.val&&same(x.left,y.left)&&same(x.right,y.right);} return !a||same(a.left,a.right); }',
      PYTHON: 'def solve(a):\n    def same(x, y):\n        if x is None or y is None: return x is y\n        return x.val == y.val and same(x.left, y.left) and same(x.right, y.right)\n    return a is None or same(a.left, a.right)',
      JAVA: '    static boolean same(TreeNode x,TreeNode y){if(x==null||y==null)return x==y;return x.val==y.val&&same(x.left,y.left)&&same(x.right,y.right);}\n    static boolean solve(TreeNode a){return a==null||same(a.left,a.right);}',
      CPP: 'bool same(TreeNode* x,TreeNode* y){if(x==nullptr||y==nullptr)return x==y;return x->val==y->val&&same(x->left,y->left)&&same(x->right,y->right);}\nbool solve(TreeNode* a){return a==nullptr||same(a->left,a->right);}',
      GO: 'func sameBranches(x *TreeNode,y *TreeNode) bool {if x==nil||y==nil{return x==y};return x.Val==y.Val&&sameBranches(x.Left,y.Left)&&sameBranches(x.Right,y.Right)}\nfunc solve(a *TreeNode) bool {return a==nil||sameBranches(a.Left,a.Right)}',
    }, tests: [
      { stdin: '1 2 2 3 4 3 4', expectedStdout: 'true', isSample: true }, { stdin: '1 2 2 3 4 4 3', expectedStdout: 'false', isSample: true }, { stdin: '', expectedStdout: 'true' }, { stdin: '1', expectedStdout: 'true' }, { stdin: '1 2 2 null 3 null 3', expectedStdout: 'true' },
    ],
  }),
  p({
    ...base, slug: 'tree-invert-odd-depths', title: 'Turn Over Alternate Levels', difficulty: 'MEDIUM', patternTags: ['trees', 'dfs', 'depth', 'in-place'], signatureId: 'fn:tree->tree', avgSolveSeconds: 720,
    promptMarkdown: ['Return the tree after swapping every node’s left and right child only at odd depth, where the root has depth 0. Nodes at even depth keep their own child order. Output uses level order with trailing `null`s trimmed.', '', '**Example**', '', '```', 'input:  1 2 3 4 5 6 7', 'output: 1 2 3 5 4 7 6', '```', '', 'The root is at even depth and stays put. Nodes `2` and `3` are at odd depth, so each swaps its children. An empty tree stays empty; a single node is unchanged.'].join('\n'),
    editorialMarkdown: ['## Depth-carrying depth-first traversal', '', 'Ordinary inversion swaps at every visit. Carry the current depth with the recursive call and perform the swap only when that depth is odd. Whether a parent swapped has no effect on the depth of its children: both remain one level below it, so the same simple depth promise works throughout the tree.', '', 'The quiet mistake is using one-based depth without changing the condition, which swaps the root and every other wrong level. It can look correct on a two-level tree where no visible grandchildren exist. Also recurse only after checking for null. Time is O(n), with one visit per node; recursion uses O(h) space, where h is the tree height.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){function go(n,d){if(!n)return;if(d%2===1){const t=n.left;n.left=n.right;n.right=t;}go(n.left,d+1);go(n.right,d+1);}go(a,0);return a;}',
      TYPESCRIPT: 'function solve(a: TreeNode | null): TreeNode | null { function go(n: TreeNode | null,d: number): void {if(!n)return;if(d%2===1){const t=n.left;n.left=n.right;n.right=t;}go(n.left,d+1);go(n.right,d+1);}go(a,0);return a; }',
      PYTHON: 'def solve(a):\n    def go(node, depth):\n        if node is None: return\n        if depth % 2 == 1: node.left, node.right = node.right, node.left\n        go(node.left, depth + 1); go(node.right, depth + 1)\n    go(a, 0)\n    return a',
      JAVA: '    static void oddSwap(TreeNode n,int d){if(n==null)return;if(d%2==1){TreeNode t=n.left;n.left=n.right;n.right=t;}oddSwap(n.left,d+1);oddSwap(n.right,d+1);}\n    static TreeNode solve(TreeNode a){oddSwap(a,0);return a;}',
      CPP: 'void oddSwap(TreeNode* n,int d){if(n==nullptr)return;if(d%2==1){TreeNode* t=n->left;n->left=n->right;n->right=t;}oddSwap(n->left,d+1);oddSwap(n->right,d+1);}\nTreeNode* solve(TreeNode* a){oddSwap(a,0);return a;}',
      GO: 'func oddSwap(n *TreeNode,d int){if n==nil{return};if d%2==1{n.Left,n.Right=n.Right,n.Left};oddSwap(n.Left,d+1);oddSwap(n.Right,d+1)}\nfunc solve(a *TreeNode) *TreeNode {oddSwap(a,0);return a}',
    }, tests: [
      { stdin: '1 2 3 4 5 6 7', expectedStdout: '1 2 3 5 4 7 6', isSample: true }, { stdin: '1 2 3 null 4 5 null', expectedStdout: '1 2 3 4 null null 5', isSample: true }, { stdin: '', expectedStdout: '' }, { stdin: '8', expectedStdout: '8' }, { stdin: '1 2 null 3 4', expectedStdout: '1 2 null 4 3' },
    ],
  }),
  p({
    ...base, slug: 'tree-zigzag-level-values', title: 'Levels That Change Direction', difficulty: 'MEDIUM', patternTags: ['trees', 'bfs', 'queue', 'level-order'], signatureId: 'fn:tree->ints', avgSolveSeconds: 720,
    promptMarkdown: ['Return all tree values level by level, but alternate the direction of each level: root level left-to-right, next level right-to-left, then left-to-right again. Return one flat list in that exact order.', '', '**Example**', '', '```', 'input:  1 2 3 4 5 6 7', 'output: 1 3 2 4 5 6 7', '```', '', 'The middle level is read right-to-left as `3 2`. The tree may be empty, which returns an empty output line. A single node returns its own value.'].join('\n'),
    editorialMarkdown: ['## Breadth-first traversal with indexed placement', '', 'A queue still discovers every level from left to right, which is valuable because it preserves the real tree structure for the next level. For each level, allocate its output slots and place the j-th dequeued value at j on forward levels or at `size - 1 - j` on reverse levels. Appending that completed level gives the specified flat order.', '', 'The quiet mistake is enqueueing children in reverse on alternate levels. That changes the discovery order of the following level too, so later values become wrong even if the current line looks right. Reverse only the output placement. Time is O(n), every node enters and leaves the queue once; space is O(w) for the widest level plus the returned list.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){if(!a)return [];let q=[a],out=[],rev=false;while(q.length){let n=q.length,row=Array(n);for(let i=0;i<n;i++){let x=q.shift();row[rev?n-1-i:i]=x.val;if(x.left)q.push(x.left);if(x.right)q.push(x.right);}out.push(...row);rev=!rev;}return out;}',
      TYPESCRIPT: 'function solve(a: TreeNode | null): number[] {if(!a)return [];let q: TreeNode[]=[a],out: number[]=[],rev=false;while(q.length){const n=q.length,row:number[]=Array(n);for(let i=0;i<n;i++){const x=q.shift() as TreeNode;row[rev?n-1-i:i]=x.val;if(x.left)q.push(x.left);if(x.right)q.push(x.right);}out.push(...row);rev=!rev;}return out;}',
      PYTHON: 'def solve(a):\n    if a is None: return []\n    queue, out, reverse = [a], [], False\n    while queue:\n        size = len(queue); row = [0] * size\n        for i in range(size):\n            node = queue.pop(0); row[size - 1 - i if reverse else i] = node.val\n            if node.left is not None: queue.append(node.left)\n            if node.right is not None: queue.append(node.right)\n        out.extend(row); reverse = not reverse\n    return out',
      JAVA: '    static int[] solve(TreeNode a){if(a==null)return new int[0];ArrayDeque<TreeNode> q=new ArrayDeque<>();java.util.ArrayList<Integer> out=new java.util.ArrayList<>();q.add(a);boolean rev=false;while(!q.isEmpty()){int n=q.size();int[] row=new int[n];for(int i=0;i<n;i++){TreeNode x=q.poll();row[rev?n-1-i:i]=x.val;if(x.left!=null)q.add(x.left);if(x.right!=null)q.add(x.right);}for(int x:row)out.add(x);rev=!rev;}int[] r=new int[out.size()];for(int i=0;i<r.length;i++)r[i]=out.get(i);return r;}',
      CPP: '#include <queue>\n#include <vector>\nvector<int> solve(TreeNode* a){if(a==nullptr)return {};queue<TreeNode*> q;vector<int> out;q.push(a);bool rev=false;while(!q.empty()){int n=(int)q.size();vector<int> row(n);for(int i=0;i<n;i++){TreeNode* x=q.front();q.pop();row[rev?n-1-i:i]=x->val;if(x->left)q.push(x->left);if(x->right)q.push(x->right);}out.insert(out.end(),row.begin(),row.end());rev=!rev;}return out;}',
      GO: 'func solve(a *TreeNode) []int {if a==nil{return []int{}};q:=[]*TreeNode{a};out:=[]int{};rev:=false;for len(q)>0{n:=len(q);row:=make([]int,n);for i:=0;i<n;i++{x:=q[0];q=q[1:];at:=i;if rev{at=n-1-i};row[at]=x.Val;if x.Left!=nil{q=append(q,x.Left)};if x.Right!=nil{q=append(q,x.Right)}};out=append(out,row...);rev=!rev};return out}',
    }, tests: [
      { stdin: '1 2 3 4 5 6 7', expectedStdout: '1 3 2 4 5 6 7', isSample: true }, { stdin: '3 9 20 null null 15 7', expectedStdout: '3 20 9 15 7', isSample: true }, { stdin: '', expectedStdout: '' }, { stdin: '4', expectedStdout: '4' }, { stdin: '1 2 3 null 4 5', expectedStdout: '1 3 2 4 5' },
    ],
  }),
  p({
    ...base, slug: 'tree-largest-child-height-gap', title: 'The Widest Branching Gap', difficulty: 'MEDIUM', patternTags: ['trees', 'dfs', 'post-order', 'height'], signatureId: 'fn:tree->int', avgSolveSeconds: 720,
    promptMarkdown: ['For every node, take the absolute difference between the heights of its left and right subtrees. Return the largest such difference anywhere in the tree. A missing subtree has height 0. The empty tree answers 0.', '', '**Example**', '', '```', 'input:  1 2 3 4 null null null', 'output: 2', '```', '', 'At root `1`, the left subtree has height 2 and the right has height 1, a gap of 1. At node `2`, its left height is 1 and missing right height is 0. The largest gap is actually 1.'].join('\n'),
    editorialMarkdown: ['## Post-order height aggregation', '', 'Each node needs the finished heights of both children before it can compute its own gap, so post-order is the natural traversal. Let the recursive call return height; after receiving the two heights, update one shared maximum with their absolute difference and return one plus the larger height. The maximum records the requested node statistic without needing a second traversal.', '', 'The quiet mistake is returning a boolean as in the parent balance check, which loses the size of a gap greater than one. Another is updating before the child calls, when their heights are unknown. Time is O(n), because every node supplies one height, and space is O(h) for the recursion stack, bounded by height.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){let best=0;function h(n){if(!n)return 0;let l=h(n.left),r=h(n.right);best=Math.max(best,Math.abs(l-r));return 1+Math.max(l,r);}h(a);return best;}',
      TYPESCRIPT: 'function solve(a: TreeNode | null): number {let best=0;function h(n: TreeNode | null): number {if(!n)return 0;const l=h(n.left),r=h(n.right);best=Math.max(best,Math.abs(l-r));return 1+Math.max(l,r);}h(a);return best;}',
      PYTHON: 'def solve(a):\n    best = 0\n    def height(node):\n        nonlocal best\n        if node is None: return 0\n        left, right = height(node.left), height(node.right)\n        best = max(best, abs(left - right))\n        return 1 + max(left, right)\n    height(a)\n    return best',
      JAVA: '    static int gapBest; static int gapHeight(TreeNode n){if(n==null)return 0;int l=gapHeight(n.left),r=gapHeight(n.right),d=l-r;if(d<0)d=-d;if(d>gapBest)gapBest=d;return 1+(l>r?l:r);}\n    static int solve(TreeNode a){gapBest=0;gapHeight(a);return gapBest;}',
      CPP: 'static int gapBest;int gapHeight(TreeNode* n){if(n==nullptr)return 0;int l=gapHeight(n->left),r=gapHeight(n->right),d=l-r;if(d<0)d=-d;if(d>gapBest)gapBest=d;return 1+(l>r?l:r);}\nint solve(TreeNode* a){gapBest=0;gapHeight(a);return gapBest;}',
      GO: 'func solve(a *TreeNode) int {best:=0;var h func(*TreeNode) int;h=func(n *TreeNode) int {if n==nil{return 0};l,r:=h(n.Left),h(n.Right);d:=l-r;if d<0{d=-d};if d>best{best=d};if l>r{return 1+l};return 1+r};h(a);return best}',
    }, tests: [
      { stdin: '1 2 3 4 null null null', expectedStdout: '1', isSample: true }, { stdin: '1 2 null 3 null 4', expectedStdout: '3', isSample: true }, { stdin: '', expectedStdout: '0' }, { stdin: '1', expectedStdout: '0' }, { stdin: '1 2 3 4 5 6 7', expectedStdout: '0' },
    ],
  }),
  p({
    ...base, slug: 'tree-count-single-child-nodes', title: 'Exactly One Way Down', difficulty: 'EASY', patternTags: ['trees', 'dfs', 'counting'], signatureId: 'fn:tree->int', avgSolveSeconds: 540,
    promptMarkdown: ['Count nodes that have exactly one child. Leaves have zero children and do not count; nodes with both children do not count. The tree may be empty and then answers 0.', '', '**Example**', '', '```', 'input:  1 2 3 4 null null 5', 'output: 2', '```', '', 'Node `2` has only child `4`, and node `3` has only child `5`. The root has two children, while `4` and `5` are leaves.'].join('\n'),
    editorialMarkdown: ['## Depth-first traversal with an exclusive test', '', 'Visit every node and add one precisely when one child exists and the other does not. XOR expresses that condition cleanly, though two comparisons work too. The count from the current node combines with independent counts from left and right subtrees, so simple depth-first recursion covers the whole tree.', '', 'The quiet mistake is using OR instead of exclusive OR, which also counts nodes with two children. It hides on one-sided chains because every non-leaf there really should count. Test a full root with two leaves. Time is O(n), with each node visited once, and space is O(h) for recursion, where h is the tree height.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){function f(n){if(!n)return 0;return ((!!n.left)!=(!!n.right)?1:0)+f(n.left)+f(n.right);}return f(a);}',
      TYPESCRIPT: 'function solve(a: TreeNode | null): number {function f(n: TreeNode | null): number {if(!n)return 0;return ((!!n.left)!=(!!n.right)?1:0)+f(n.left)+f(n.right);}return f(a);}',
      PYTHON: 'def solve(a):\n    def count(node):\n        if node is None: return 0\n        return (1 if (node.left is None) != (node.right is None) else 0) + count(node.left) + count(node.right)\n    return count(a)',
      JAVA: '    static int solve(TreeNode a){if(a==null)return 0;return ((a.left==null)!=(a.right==null)?1:0)+solve(a.left)+solve(a.right);}',
      CPP: 'int solve(TreeNode* a){if(a==nullptr)return 0;return ((a->left==nullptr)!=(a->right==nullptr)?1:0)+solve(a->left)+solve(a->right);}',
      GO: 'func solve(a *TreeNode) int {if a==nil{return 0};v:=0;if (a.Left==nil)!=(a.Right==nil){v=1};return v+solve(a.Left)+solve(a.Right)}',
    }, tests: [
      { stdin: '1 2 3 4 null null 5', expectedStdout: '2', isSample: true }, { stdin: '1 2 3', expectedStdout: '0', isSample: true }, { stdin: '', expectedStdout: '0' }, { stdin: '7', expectedStdout: '0' }, { stdin: '1 null 2 null 3', expectedStdout: '2' },
    ],
  }),
  p({
    ...base, slug: 'tree-count-valid-search-subtrees', title: 'Search Trees Hidden Inside', difficulty: 'HARD', patternTags: ['trees', 'dfs', 'post-order', 'bst'], signatureId: 'fn:tree->int', avgSolveSeconds: 900,
    promptMarkdown: ['Count how many non-empty subtrees are valid binary search trees. A subtree starts at any node and includes every descendant of that node. A valid search tree has all left values strictly smaller and all right values strictly larger. The empty tree answers 0.', '', '**Example**', '', '```', 'input:  5 2 4 1 3', 'output: 4', '```', '', 'The subtree rooted at `5` is invalid because `4` is on its right but smaller. The subtrees rooted at `2`, `1`, `3`, and `4` are valid.'].join('\n'),
    editorialMarkdown: ['## Post-order range summaries', '', 'A parent subtree can be judged only after both children report whether they are search trees and what values bound them. Post-order returns a summary: valid flag, smallest value, and largest value. A node is valid when both child summaries are valid and the left maximum is smaller than its value while its value is smaller than the right minimum. Count every valid node-rooted summary.', '', 'The quiet mistake is comparing only direct child values. A bad value can be buried deeper, as the `4` under the right side of `5` demonstrates. Another is treating an empty child as min or max zero, which breaks negative values; use absent bounds. Time is O(n), one summary per node, and space is O(h) recursion stack space, bounded by height.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){let c=0;function f(n){if(!n)return [true,Infinity,-Infinity];let l=f(n.left),r=f(n.right),ok=l[0]&&r[0]&&l[2]<n.val&&n.val<r[1];if(ok)c++;return [ok,Math.min(l[1],n.val,r[1]),Math.max(l[2],n.val,r[2])];}f(a);return c;}',
      TYPESCRIPT: 'function solve(a: TreeNode | null): number {let c=0;function f(n: TreeNode | null): [boolean,number,number]{if(!n)return [true,Infinity,-Infinity];const l=f(n.left),r=f(n.right),ok=l[0]&&r[0]&&l[2]<n.val&&n.val<r[1];if(ok)c++;return [ok,Math.min(l[1],n.val,r[1]),Math.max(l[2],n.val,r[2])];}f(a);return c;}',
      PYTHON: 'def solve(a):\n    count = 0\n    def visit(node):\n        nonlocal count\n        if node is None: return True, float("inf"), float("-inf")\n        lv, lo, hi = visit(node.left); rv, ro, rh = visit(node.right)\n        ok = lv and rv and hi < node.val < ro\n        if ok: count += 1\n        return ok, min(lo, node.val, ro), max(hi, node.val, rh)\n    visit(a)\n    return count',
      JAVA: '    static int validCount;static int[] valid(TreeNode n){if(n==null)return new int[]{1,1000000000,-1000000000};int[] l=valid(n.left),r=valid(n.right);boolean ok=l[0]==1&&r[0]==1&&l[2]<n.val&&n.val<r[1];if(ok)validCount++;int lo=l[1]<n.val?l[1]:n.val;if(r[1]<lo)lo=r[1];int hi=l[2]>n.val?l[2]:n.val;if(r[2]>hi)hi=r[2];return new int[]{ok?1:0,lo,hi};}\n    static int solve(TreeNode a){validCount=0;valid(a);return validCount;}',
      CPP: '#include <vector>\nstatic int validCount;vector<int> valid(TreeNode* n){if(n==nullptr)return {1,1000000000,-1000000000};vector<int> l=valid(n->left),r=valid(n->right);bool ok=l[0]&&r[0]&&l[2]<n->val&&n->val<r[1];if(ok)validCount++;int lo=l[1]<n->val?l[1]:n->val;if(r[1]<lo)lo=r[1];int hi=l[2]>n->val?l[2]:n->val;if(r[2]>hi)hi=r[2];return {ok?1:0,lo,hi};}\nint solve(TreeNode* a){validCount=0;valid(a);return validCount;}',
      GO: 'func solve(a *TreeNode) int {count:=0;var f func(*TreeNode)(bool,int,int);f=func(n *TreeNode)(bool,int,int){if n==nil{return true,1000000000,-1000000000};lv,lo,hi:=f(n.Left);rv,ro,rh:=f(n.Right);ok:=lv&&rv&&hi<n.Val&&n.Val<ro;if ok{count++};if n.Val<lo{lo=n.Val};if ro<lo{lo=ro};if n.Val>hi{hi=n.Val};if rh>hi{hi=rh};return ok,lo,hi};f(a);return count}',
    }, tests: [
      { stdin: '5 2 4 1 3', expectedStdout: '4', isSample: true }, { stdin: '2 1 3', expectedStdout: '3', isSample: true }, { stdin: '', expectedStdout: '0' }, { stdin: '7', expectedStdout: '1' }, { stdin: '5 1 4 null null 3 6', expectedStdout: '4' },
    ],
  }),
  p({
    ...base, slug: 'tree-kth-largest-value', title: 'The K-th Largest in a Search Tree', difficulty: 'MEDIUM', patternTags: ['trees', 'bst', 'reverse-in-order'], signatureId: 'fn:tree,int->int', avgSolveSeconds: 660,
    promptMarkdown: ['The first line is a valid binary search tree and the second is `k`. Return its k-th largest value, counting from 1: `k = 1` asks for the largest. All values are distinct, the tree is non-empty, and k is within its node count.', '', '**Example**', '', '```', 'input:  5 3 8 1 4 7 9', '3', 'output: 7', '```', '', 'Descending order is `9 8 7 5 4 3 1`, so the third value is `7`.'].join('\n'),
    editorialMarkdown: ['## Reverse in-order traversal', '', 'In-order emits a search tree from small to large. This variation needs the reverse: right subtree, node, then left subtree. An explicit stack walks down the right edge first, then repeatedly visits a node and turns into its left subtree. The k-th emitted value is exactly the k-th largest, so it can stop immediately.', '', 'The quiet mistake is reusing ordinary in-order unchanged; that returns the k-th smallest, a plausible but reversed answer. Another is decrementing k after testing it and shifting every result by one. Descend right before popping. Time is O(h + k) and at worst O(n); stack space is O(h), where h is tree height.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a,b){let s=[],n=a,k=b;while(n||s.length){while(n){s.push(n);n=n.right;}n=s.pop();if(--k===0)return n.val;n=n.left;}return -1;}',
      TYPESCRIPT: 'function solve(a: TreeNode | null,b: number): number {let s:TreeNode[]=[],n:TreeNode | null=a,k=b;while(n||s.length){while(n){s.push(n);n=n.right;}n=s.pop() as TreeNode;if(--k===0)return n.val;n=n.left;}return -1;}',
      PYTHON: 'def solve(a, b):\n    stack = []; node = a\n    while node is not None or stack:\n        while node is not None: stack.append(node); node = node.right\n        node = stack.pop(); b -= 1\n        if b == 0: return node.val\n        node = node.left\n    return -1',
      JAVA: '    static int solve(TreeNode a,int b){java.util.ArrayDeque<TreeNode>s=new java.util.ArrayDeque<>();TreeNode n=a;while(n!=null||!s.isEmpty()){while(n!=null){s.push(n);n=n.right;}n=s.pop();if(--b==0)return n.val;n=n.left;}return -1;}',
      CPP: '#include <vector>\nint solve(TreeNode* a,int b){vector<TreeNode*> s;TreeNode* n=a;while(n!=nullptr||!s.empty()){while(n!=nullptr){s.push_back(n);n=n->right;}n=s.back();s.pop_back();if(--b==0)return n->val;n=n->left;}return -1;}',
      GO: 'func solve(a *TreeNode,b int) int {s:=[]*TreeNode{};n:=a;for n!=nil||len(s)>0{for n!=nil{s=append(s,n);n=n.Right};n=s[len(s)-1];s=s[:len(s)-1];b--;if b==0{return n.Val};n=n.Left};return -1}',
    }, tests: [
      { stdin: '5 3 8 1 4 7 9\n3', expectedStdout: '7', isSample: true }, { stdin: '5 3 8 1 4 7 9\n1', expectedStdout: '9', isSample: true }, { stdin: '5 3 8 1 4 7 9\n7', expectedStdout: '1' }, { stdin: '2 1 3\n2', expectedStdout: '2' }, { stdin: '6\n1', expectedStdout: '6' },
    ],
  }),
  p({
    ...base, slug: 'tree-deepest-ancestor-of-leaves', title: 'Where Every Leaf Meets', difficulty: 'HARD', patternTags: ['trees', 'dfs', 'post-order', 'ancestors'], signatureId: 'fn:tree->int', avgSolveSeconds: 900,
    promptMarkdown: ['Return the value of the deepest node that is an ancestor of every leaf in the tree. A node is an ancestor of itself, so when the tree has one leaf, that leaf is the answer. The tree has at least one node.', '', '**Example**', '', '```', 'input:  1 2 3 4 5 null null', 'output: 1', '```', '', 'The leaves are `4`, `5`, and `3`; their deepest common ancestor is root `1`. A single-node tree returns that node.'].join('\n'),
    editorialMarkdown: ['## Post-order lowest-common-ancestor recursion', '', 'Each subtree can report its deepest node that covers all leaves within it. A leaf reports itself. If a node has only one non-empty child, every leaf below it is in that child, so pass that answer upward. If both children exist, leaves occur on both sides and the current node is the first, deepest node covering them all. This produces the global answer at the root.', '', 'The quiet mistake is treating a missing child like a leaf. That makes every one-sided node appear to join two leaf groups and returns too high an ancestor. Check for a real leaf before recursing, then distinguish one child from two. Time is O(n), visiting each node once; space is O(h) recursion stack space, bounded by tree height.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){function f(n){if(!n)return null;if(!n.left&&!n.right)return n;if(!n.left)return f(n.right);if(!n.right)return f(n.left);f(n.left);f(n.right);return n;}return f(a).val;}',
      TYPESCRIPT: 'function solve(a: TreeNode | null): number {function f(n: TreeNode | null): TreeNode | null {if(!n)return null;if(!n.left&&!n.right)return n;if(!n.left)return f(n.right);if(!n.right)return f(n.left);f(n.left);f(n.right);return n;}return (f(a) as TreeNode).val;}',
      PYTHON: 'def solve(a):\n    def find(node):\n        if node.left is None and node.right is None: return node\n        if node.left is None: return find(node.right)\n        if node.right is None: return find(node.left)\n        find(node.left); find(node.right)\n        return node\n    return find(a).val',
      JAVA: '    static TreeNode leafMeet(TreeNode n){if(n.left==null&&n.right==null)return n;if(n.left==null)return leafMeet(n.right);if(n.right==null)return leafMeet(n.left);leafMeet(n.left);leafMeet(n.right);return n;}\n    static int solve(TreeNode a){return leafMeet(a).val;}',
      CPP: 'TreeNode* leafMeet(TreeNode* n){if(n->left==nullptr&&n->right==nullptr)return n;if(n->left==nullptr)return leafMeet(n->right);if(n->right==nullptr)return leafMeet(n->left);leafMeet(n->left);leafMeet(n->right);return n;}\nint solve(TreeNode* a){return leafMeet(a)->val;}',
      GO: 'func leafMeet(n *TreeNode) *TreeNode {if n.Left==nil&&n.Right==nil{return n};if n.Left==nil{return leafMeet(n.Right)};if n.Right==nil{return leafMeet(n.Left)};leafMeet(n.Left);leafMeet(n.Right);return n}\nfunc solve(a *TreeNode) int{return leafMeet(a).Val}',
    }, tests: [
      { stdin: '1 2 3 4 5', expectedStdout: '1', isSample: true }, { stdin: '1 2 null 3 4', expectedStdout: '2', isSample: true }, { stdin: '7', expectedStdout: '7' }, { stdin: '1 null 2 null 3', expectedStdout: '3' }, { stdin: '1 2 3 4 null 5 null', expectedStdout: '1' },
    ],
  }),
];
