import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'BIT_MANIPULATION', provenance: AUTHORED } as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_2_BIT_MANIPULATION_B_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'even-position-lit-count', title:'Lights on Even Positions', difficulty:'EASY', patternTags:['bit-manipulation','counting'], signatureId:'fn:int->int', avgSolveSeconds:540,
    promptMarkdown:['Number bit positions from 0 at the rightmost bit. Return how many lit bits of non-negative `n` sit at an even position.','', '**Example**','', '```','input: 45','output: 2','```','', '`45` is `101101`, with lit bits at positions 0, 2, 3, and 5. The named edge case is `0`, which has no lit positions.'].join('\n'),
    editorialMarkdown:['## Alternating-position bit scan','', 'Peel one bit at a time while keeping its position counter. The low bit is the current position, and a right shift exposes the next one, so adding only when both the bit and parity are one counts exactly the requested lights. This uses the binary representation directly rather than converting it to text.', '', 'The quiet mistake is numbering from the left. Binary width changes with the value, while this task fixes position zero at the right, so left-based parity silently changes answers. The loop is O(log n) time for the number of binary digits and O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(n){let c=0,p=0;while(n){if(p%2===0&&(n&1))c++;n>>=1;p++}return c}','function solve(n:number):number{let c=0,p=0;while(n){if(p%2===0&&(n&1))c++;n>>=1;p++}return c}','def solve(n):\n c=p=0\n while n:\n  if p%2==0 and n&1:c+=1\n  n//=2;p+=1\n return c','    static int solve(int n){int c=0,p=0;while(n>0){if(p%2==0&&(n&1)==1)c++;n>>=1;p++;}return c;}','#include <vector>\nusing namespace std; int solve(int n){int c=0,p=0;while(n){if(p%2==0&&(n&1))c++;n>>=1;p++;}return c;}','func solve(n int) int {c,p:=0,0;for n>0{if p%2==0&&n&1==1{c++};n>>=1;p++};return c}'),
    tests:[{stdin:'45',expectedStdout:'2',isSample:true},{stdin:'0',expectedStdout:'0',isSample:true},{stdin:'8',expectedStdout:'0'},{stdin:'5',expectedStdout:'2'}] }),
  p({ ...base, slug:'power-of-four-lights', title:'Every Other Light', difficulty:'EASY', patternTags:['bit-manipulation','power-check'], signatureId:'fn:int->bool', avgSolveSeconds:540,
    promptMarkdown:['Return whether positive integer `n` is a power of four. Zero and negative values are not powers of four.','', '**Example**','', '```','input: 64','output: true','```','', 'A power of four has one lit bit, and that bit is at an even position; `8` is the named near miss.'].join('\n'),
    editorialMarkdown:['## One bit plus an alternating mask','', 'First require exactly one lit bit with `n & (n - 1)`. Powers of four are the subset of those powers of two whose single bit is in positions 0, 2, 4, and so on; the hexadecimal mask `0x55555555` has ones precisely there. Both tests together express the definition.', '', 'The quiet mistake is checking only for a power of two, which accepts `2`, `8`, and `32`. Those values look equally sparse but their one bit has odd position. The fixed 32-bit mask gives O(1) time and O(1) space.'].join('\n'),
    referenceSolution:six('function solve(n){return n>0&&(n&(n-1))===0&&(n&0x55555555)!==0}','function solve(n:number):boolean{return n>0&&(n&(n-1))===0&&(n&0x55555555)!==0}','def solve(n):\n return n>0 and n&(n-1)==0 and n&0x55555555!=0','    static boolean solve(int n){return n>0&&(n&(n-1))==0&&(n&0x55555555)!=0;}','#include <vector>\nusing namespace std; bool solve(int n){return n>0&&(n&(n-1))==0&&(n&0x55555555)!=0;}','func solve(n int) bool{return n>0&&n&(n-1)==0&&n&0x55555555!=0}'),
    tests:[{stdin:'64',expectedStdout:'true',isSample:true},{stdin:'8',expectedStdout:'false',isSample:true},{stdin:'1',expectedStdout:'true'},{stdin:'0',expectedStdout:'false'}] }),
  p({ ...base, slug:'reverse-within-chosen-width', title:'Mirror a Chosen Width', difficulty:'EASY', patternTags:['bit-manipulation','reversal'], signatureId:'fn:int,int->int', avgSolveSeconds:600,
    promptMarkdown:['Treat non-negative `n` as exactly `width` bits and return those bits reversed. `1 <= width <= 30`, and `n < 2^width`.','', '**Example**','', '```','input: 6','4','output: 6','```','', '`6` is `0110` at width 4 and stays `0110`; leading zeroes are meaningful, so `1` at width 4 becomes 8.'].join('\n'),
    editorialMarkdown:['## Fixed-width peel and rebuild','', 'For exactly `width` turns, append n’s current low bit to the answer and shift n right. Shifting the answer left before OR-ing the bit moves every earlier bit toward its mirrored place. Running the stated width, rather than stopping at zero, preserves leading zero positions.', '', 'The quiet mistake is using a while loop that ends once n is zero. It passes values with a high bit set yet fails the leading-zero cases that make a chosen width different from ordinary binary text. Time is O(width), bounded by 30, and space is O(1).'].join('\n'),
    referenceSolution:six('function solve(n,w){let r=0;for(let i=0;i<w;i++){r=(r<<1)|(n&1);n>>=1}return r}','function solve(n:number,w:number):number{let r=0;for(let i=0;i<w;i++){r=(r<<1)|(n&1);n>>=1}return r}','def solve(n,w):\n r=0\n for _ in range(w):r=(r<<1)|(n&1);n//=2\n return r','    static int solve(int n,int w){int r=0;for(int i=0;i<w;i++){r=(r<<1)|(n&1);n>>=1;}return r;}','#include <vector>\nusing namespace std; int solve(int n,int w){int r=0;for(int i=0;i<w;i++){r=(r<<1)|(n&1);n>>=1;}return r;}','func solve(n,w int) int{r:=0;for i:=0;i<w;i++{r=r<<1|n&1;n>>=1};return r}'),
    tests:[{stdin:'6\n4',expectedStdout:'6',isSample:true},{stdin:'1\n4',expectedStdout:'8',isSample:true},{stdin:'0\n5',expectedStdout:'0'},{stdin:'13\n4',expectedStdout:'11'}] }),
  p({ ...base, slug:'single-badge-among-triplicates', title:'One Badge, Triple Noise', difficulty:'MEDIUM', patternTags:['bit-manipulation','state-machine'], signatureId:'fn:ints->int', avgSolveSeconds:720,
    promptMarkdown:['One non-negative value appears once; every other value appears exactly three times. Return the value that appears once.','', '**Example**','', '```','input: 6 2 6 2 6 9 2','output: 9','```','', 'The list is non-empty and values fit in signed 32-bit range. The edge case is when the single value is 0.'].join('\n'),
    editorialMarkdown:['## Per-bit counting modulo three','', 'At every bit position, count how many inputs have that bit set. Triple copies contribute multiples of three, leaving a remainder of one only for a bit belonging to the unique value. Rebuild the answer from those remainders, avoiding a frequency map.', '', 'The quiet mistake is applying plain XOR cancellation, which removes pairs rather than triples and leaves misleading residue. Each of 31 safe non-negative bit positions scans n values, so time is O(31n), equivalently O(n), and extra space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let r=0;for(let b=0;b<31;b++){let c=0;for(let x of a)c+=(x>>b)&1;if(c%3)r|=1<<b}return r}','function solve(a:number[]):number{let r=0;for(let b=0;b<31;b++){let c=0;for(const x of a)c+=(x>>b)&1;if(c%3)r|=1<<b}return r}','def solve(a):\n r=0\n for b in range(31):\n  if sum((x>>b)&1 for x in a)%3:r|=1<<b\n return r','    static int solve(int[]a){int r=0;for(int b=0;b<31;b++){int c=0;for(int x:a)c+=(x>>b)&1;if(c%3!=0)r|=1<<b;}return r;}','#include <vector>\nusing namespace std; int solve(vector<int>a){int r=0;for(int b=0;b<31;b++){int c=0;for(int x:a)c+=(x>>b)&1;if(c%3)r|=1<<b;}return r;}','func solve(a []int) int{r:=0;for b:=0;b<31;b++{c:=0;for _,x:=range a{c+=(x>>b)&1};if c%3!=0{r|=1<<b}};return r}'),
    tests:[{stdin:'6 2 6 2 6 9 2',expectedStdout:'9',isSample:true},{stdin:'0 5 5 5',expectedStdout:'0',isSample:true},{stdin:'7',expectedStdout:'7'},{stdin:'1 1 1 12 4 4 4',expectedStdout:'12'}] }),
  p({ ...base, slug:'subset-xor-target-count', title:'Subset Masks Hit a Target', difficulty:'MEDIUM', patternTags:['bit-manipulation','subsets','xor'], signatureId:'fn:ints,int->int', avgSolveSeconds:780,
    promptMarkdown:['Count subsets whose XOR is exactly `target`. The input list has at most 10 values; equal values at different indices form different subsets.','', '**Example**','', '```','input: 1 2 3','3','output: 2','```','', 'The qualifying subsets are `[3]` and `[1,2]`. The empty subset is included and has XOR 0.'].join('\n'),
    editorialMarkdown:['## Enumerate subset masks','', 'A mask from 0 through 2^n - 1 chooses each index by one bit. XOR every chosen value and increment the answer when it matches target; every subset has exactly one mask, so this neither misses nor duplicates choices. The small n bound makes direct enumeration the clearest method.', '', 'The quiet mistake is treating equal values as one choice. Masks select positions, so two equal numbers can produce several distinct subsets. There are 2^n masks and each scans n positions, giving O(n 2^n) time bounded by n <= 10 and O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a,t){let c=0;for(let m=0;m<(1<<a.length);m++){let x=0;for(let i=0;i<a.length;i++)if(m>>i&1)x^=a[i];if(x===t)c++}return c}','function solve(a:number[],t:number):number{let c=0;for(let m=0;m<(1<<a.length);m++){let x=0;for(let i=0;i<a.length;i++)if(m>>i&1)x^=a[i];if(x===t)c++}return c}','def solve(a,t):\n c=0\n for m in range(1<<len(a)):\n  x=0\n  for i in range(len(a)):\n   if m>>i&1:x^=a[i]\n  c+=x==t\n return c','    static int solve(int[]a,int t){int c=0;for(int m=0;m<(1<<a.length);m++){int x=0;for(int i=0;i<a.length;i++)if((m>>i&1)==1)x^=a[i];if(x==t)c++;}return c;}','#include <vector>\nusing namespace std; int solve(vector<int>a,int t){int c=0;for(int m=0;m<(1<<(int)a.size());m++){int x=0;for(int i=0;i<(int)a.size();i++)if(m>>i&1)x^=a[i];if(x==t)c++;}return c;}','func solve(a []int,t int) int{c:=0;for m:=0;m<1<<len(a);m++{x:=0;for i:=0;i<len(a);i++{if m>>i&1==1{x^=a[i]}};if x==t{c++}};return c}'),
    tests:[{stdin:'1 2 3\n3',expectedStdout:'2',isSample:true},{stdin:'5\n0',expectedStdout:'1',isSample:true},{stdin:'1 1\n0',expectedStdout:'2'},{stdin:'0 0\n0',expectedStdout:'4'}] }),
  p({ ...base, slug:'range-xor-ledger', title:'XOR for Every Ledger Range', difficulty:'MEDIUM', patternTags:['bit-manipulation','xor','range-query'], signatureId:'fn:matrix->ints', avgSolveSeconds:780,
    promptMarkdown:['Each matrix row contains two non-negative integers `[left, right]` with `left <= right`. Return one XOR per row: the XOR of every integer from left through right, in the same row order.','', '**Example**','', '```','input: 3 6;0 4;7 7','output: 4 4 7','```','', 'A one-number range returns that number. Output order is exactly the input row order.'].join('\n'),
    editorialMarkdown:['## Four-value prefix XOR cycle','', 'Let F(n) be XOR of 0 through n. Its value repeats by n modulo four: n, 1, n+1, 0. A range XOR is F(right) XOR F(left-1), because the shared prefix values cancel in pairs. Compute each row independently, preserving its order.', '', 'The quiet mistake is using F(left) instead of F(left-1), which accidentally removes the left endpoint. That error is especially quiet for ranges whose left value is zero. Each row takes O(1) time, so total time is O(r) for r rows and extra space is O(r) for the returned array.'].join('\n'),
    referenceSolution:six('function solve(a){let f=n=>n<0?0:[n,1,n+1,0][n%4];return a.map(q=>f(q[1])^f(q[0]-1))}','function solve(a:number[][]):number[]{const f=(n:number)=>n<0?0:[n,1,n+1,0][n%4];return a.map(q=>f(q[1])^f(q[0]-1))}','def solve(a):\n def f(n):return 0 if n<0 else [n,1,n+1,0][n%4]\n return [f(x[1])^f(x[0]-1) for x in a]','    static int[] solve(int[][]a){int[]r=new int[a.length];for(int i=0;i<a.length;i++){int x=a[i][1],y=a[i][0]-1;int fx=x<0?0:(x%4==0?x:x%4==1?1:x%4==2?x+1:0),fy=y<0?0:(y%4==0?y:y%4==1?1:y%4==2?y+1:0);r[i]=fx^fy;}return r;}','#include <vector>\nusing namespace std; vector<int> solve(vector<vector<int>>a){vector<int>r;for(auto q:a){int x=q[1],y=q[0]-1;int fx=x<0?0:(x%4==0?x:x%4==1?1:x%4==2?x+1:0),fy=y<0?0:(y%4==0?y:y%4==1?1:y%4==2?y+1:0);r.push_back(fx^fy);}return r;}','func solve(a [][]int) []int{r:=[]int{};for _,q:=range a{x,y:=q[1],q[0]-1;fx:=0;fy:=0;if x>=0{switch x%4{case 0:fx=x;case 1:fx=1;case 2:fx=x+1}};if y>=0{switch y%4{case 0:fy=y;case 1:fy=1;case 2:fy=y+1}};r=append(r,fx^fy)};return r}'),
    tests:[{stdin:'3 6;0 4;7 7',expectedStdout:'4 4 7',isSample:true},{stdin:'0 0',expectedStdout:'0',isSample:true},{stdin:'1 1;2 3',expectedStdout:'1 1'},{stdin:'8 10',expectedStdout:'11'}] }),
  p({ ...base, slug:'maximum-xor-subset', title:'Strongest XOR Subset', difficulty:'HARD', patternTags:['bit-manipulation','subsets','xor'], signatureId:'fn:ints->int', avgSolveSeconds:900,
    promptMarkdown:['Return the largest numeric XOR obtainable from any subset of the non-negative input values. The list has at most 10 values, and the empty subset is allowed.','', '**Example**','', '```','input: 2 4 5','output: 7','```','', '`2 XOR 5` is 7. A one-value list is valid; choosing the empty subset yields 0.'].join('\n'),
    editorialMarkdown:['## Exhaustive XOR basis search','', 'With at most ten positions, enumerate every subset mask and XOR its selected values. Comparing each computed XOR to the running maximum finds the optimum because every allowed subset appears exactly once. This is a deliberately small-input version of maximum-subset-XOR reasoning.', '', 'The quiet mistake is greedily taking the largest values. Numeric magnitude does not predict which high bits cancel under XOR, so a smaller value can improve the result. The method is O(n 2^n) time bounded by n <= 10 and O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a){let z=0;for(let m=0;m<(1<<a.length);m++){let x=0;for(let i=0;i<a.length;i++)if(m>>i&1)x^=a[i];if(x>z)z=x}return z}','function solve(a:number[]):number{let z=0;for(let m=0;m<(1<<a.length);m++){let x=0;for(let i=0;i<a.length;i++)if(m>>i&1)x^=a[i];if(x>z)z=x}return z}','def solve(a):\n z=0\n for m in range(1<<len(a)):\n  x=0\n  for i in range(len(a)):\n   if m>>i&1:x^=a[i]\n  z=max(z,x)\n return z','    static int solve(int[]a){int z=0;for(int m=0;m<(1<<a.length);m++){int x=0;for(int i=0;i<a.length;i++)if((m>>i&1)==1)x^=a[i];if(x>z)z=x;}return z;}','#include <vector>\nusing namespace std; int solve(vector<int>a){int z=0;for(int m=0;m<(1<<(int)a.size());m++){int x=0;for(int i=0;i<(int)a.size();i++)if(m>>i&1)x^=a[i];if(x>z)z=x;}return z;}','func solve(a []int) int{z:=0;for m:=0;m<1<<len(a);m++{x:=0;for i:=0;i<len(a);i++{if m>>i&1==1{x^=a[i]}};if x>z{z=x}};return z}'),
    tests:[{stdin:'2 4 5',expectedStdout:'7',isSample:true},{stdin:'8',expectedStdout:'8',isSample:true},{stdin:'1 2 4',expectedStdout:'7'},{stdin:'0 0',expectedStdout:'0'}] }),
  p({ ...base, slug:'subarrays-with-xor-target', title:'XOR Windows at Target', difficulty:'HARD', patternTags:['bit-manipulation','prefix-xor','counting'], signatureId:'fn:ints,int->int', avgSolveSeconds:960,
    promptMarkdown:['Count contiguous subarrays whose XOR equals `target`. The non-empty list has at most 200 values, all non-negative.','', '**Example**','', '```','input: 4 2 2 6 4','6','output: 4','```','', 'Overlapping subarrays count separately. The named edge case is target 0, where equal prefix XOR values form answers.'].join('\n'),
    editorialMarkdown:['## Prefix XOR frequency table','', 'Let p be XOR through the current index. A prior prefix q starts a valid subarray exactly when q XOR p equals target, or q equals p XOR target. Count how many such q values were seen before adding p to the table. This converts every possible left endpoint into a lookup.', '', 'The quiet mistake is updating the table before counting. That wrongly permits an empty subarray when target is zero. Seed the table with prefix XOR zero once, then count before inserting each new prefix. Time is O(n) and space O(n), both bounded by the list length.'].join('\n'),
    referenceSolution:six('function solve(a,t){let m=new Map([[0,1]]),p=0,c=0;for(let x of a){p^=x;c+=m.get(p^t)||0;m.set(p,(m.get(p)||0)+1)}return c}','function solve(a:number[],t:number):number{const m=new Map<number,number>([[0,1]]);let p=0,c=0;for(const x of a){p^=x;c+=m.get(p^t)||0;m.set(p,(m.get(p)||0)+1)}return c}','def solve(a,t):\n m={0:1};p=c=0\n for x in a:\n  p^=x;c+=m.get(p^t,0);m[p]=m.get(p,0)+1\n return c','    static int solve(int[]a,int t){java.util.HashMap<Integer,Integer>m=new java.util.HashMap<>();m.put(0,1);int p=0,c=0;for(int x:a){p^=x;c+=m.getOrDefault(p^t,0);m.put(p,m.getOrDefault(p,0)+1);}return c;}','#include <vector>\n#include <unordered_map>\nusing namespace std; int solve(vector<int>a,int t){unordered_map<int,int>m;m[0]=1;int p=0,c=0;for(int x:a){p^=x;c+=m[p^t];m[p]++;}return c;}','func solve(a []int,t int) int{m:=map[int]int{0:1};p,c:=0,0;for _,x:=range a{p^=x;c+=m[p^t];m[p]++};return c}'),
    tests:[{stdin:'4 2 2 6 4\n6',expectedStdout:'4',isSample:true},{stdin:'1 1 1\n0',expectedStdout:'2',isSample:true},{stdin:'5\n5',expectedStdout:'1'},{stdin:'0 0 0\n0',expectedStdout:'6'}] }),
  p({ ...base, slug:'gray-step-sequence-check', title:'One Switch Per Gray Step', difficulty:'MEDIUM', patternTags:['bit-manipulation','gray-code','validation'], signatureId:'fn:ints->bool', avgSolveSeconds:720,
    promptMarkdown:['Return true when each adjacent pair in the non-empty non-negative list differs in exactly one bit.','', '**Example**','', '```','input: 0 1 3 2','output: true','```','', 'A one-value list is true because it has no adjacent pair to violate the rule. Repeated neighbors differ in zero bits and are false.'].join('\n'),
    editorialMarkdown:['## XOR difference has one bit','', 'XOR marks precisely the positions where two values differ. Therefore adjacent values are a Gray step exactly when their XOR is a positive power of two, tested by `d & (d - 1) == 0`. Scan every neighboring pair and reject the first failure.', '', 'The quiet mistake is allowing d equal to zero because its bit-clearing expression is also zero. The positivity guard is what rejects repeated neighbors. The scan is O(n) time for n values and O(1) extra space.'].join('\n'),
    referenceSolution:six('function solve(a){for(let i=1;i<a.length;i++){let d=a[i]^a[i-1];if(d===0||(d&(d-1))!==0)return false}return true}','function solve(a:number[]):boolean{for(let i=1;i<a.length;i++){const d=a[i]^a[i-1];if(d===0||(d&(d-1))!==0)return false}return true}','def solve(a):\n for i in range(1,len(a)):\n  d=a[i]^a[i-1]\n  if d==0 or d&(d-1):return False\n return True','    static boolean solve(int[]a){for(int i=1;i<a.length;i++){int d=a[i]^a[i-1];if(d==0||(d&(d-1))!=0)return false;}return true;}','#include <vector>\nusing namespace std; bool solve(vector<int>a){for(int i=1;i<(int)a.size();i++){int d=a[i]^a[i-1];if(d==0||(d&(d-1)))return false;}return true;}','func solve(a []int) bool{for i:=1;i<len(a);i++{d:=a[i]^a[i-1];if d==0||d&(d-1)!=0{return false}};return true}'),
    tests:[{stdin:'0 1 3 2',expectedStdout:'true',isSample:true},{stdin:'0 3',expectedStdout:'false',isSample:true},{stdin:'7',expectedStdout:'true'},{stdin:'1 1',expectedStdout:'false'}] }),
  p({ ...base, slug:'gray-code-list-for-width', title:'Gray List for a Width', difficulty:'MEDIUM', patternTags:['bit-manipulation','gray-code','construction'], signatureId:'fn:int->ints', avgSolveSeconds:780,
    promptMarkdown:['Return all 2^n Gray-code values for `n` bits, in increasing ordinary index order: value i is `i XOR (i >> 1)`. `0 <= n <= 16`.','', '**Example**','', '```','input: 2','output: 0 1 3 2','```','', 'For n = 0, return the one-element list `0`; the order is part of the required output.'].join('\n'),
    editorialMarkdown:['## Formula-driven Gray sequence','', 'The reflected Gray value for index i is `i XOR (i >> 1)`. Consecutive indices change in a structured way, and this formula produces the standard sequence without building binary strings or searching for neighbors. Emit it for every index from zero to 2^n minus one.', '', 'The quiet mistake is returning the right set in an arbitrary order. The judge and the mathematical sequence both require increasing index order, which starts 0, 1, 3, 2 for width two. Time is O(2^n), bounded by n <= 16, and output space is O(2^n).'].join('\n'),
    referenceSolution:six('function solve(n){let r=[];for(let i=0;i<(1<<n);i++)r.push(i^(i>>1));return r}','function solve(n:number):number[]{const r:number[]=[];for(let i=0;i<(1<<n);i++)r.push(i^(i>>1));return r}','def solve(n):\n return [i^(i>>1) for i in range(1<<n)]','    static int[] solve(int n){int[]r=new int[1<<n];for(int i=0;i<r.length;i++)r[i]=i^(i>>1);return r;}','#include <vector>\nusing namespace std; vector<int> solve(int n){vector<int>r;for(int i=0;i<(1<<n);i++)r.push_back(i^(i>>1));return r;}','func solve(n int) []int{r:=[]int{};for i:=0;i<1<<n;i++{r=append(r,i^i>>1)};return r}'),
    tests:[{stdin:'2',expectedStdout:'0 1 3 2',isSample:true},{stdin:'0',expectedStdout:'0',isSample:true},{stdin:'1',expectedStdout:'0 1'},{stdin:'3',expectedStdout:'0 1 3 2 6 7 5 4'}] }),
  p({ ...base, slug:'three-lit-numbers-through-limit', title:'Exactly Three Lights Through a Limit', difficulty:'HARD', patternTags:['bit-manipulation','counting','combinatorics'], signatureId:'fn:int->int', avgSolveSeconds:900,
    promptMarkdown:['Return how many integers from 0 through non-negative `n`, inclusive, have exactly three lit bits in binary. `n <= 1,000,000`.','', '**Example**','', '```','input: 10','output: 1','```','', 'Only 7 (`111`) qualifies through 10. Values below 7 are the named edge case and return 0.'].join('\n'),
    editorialMarkdown:['## Direct bounded popcount scan','', 'The stated upper bound permits scanning every value through n. For each one, repeatedly clear its lowest lit bit and stop counting after the fourth; increment the answer only when exactly three clears occur. Clearing bits avoids formatting binary strings and makes the rule explicit.', '', 'The quiet mistake is counting values with at least three lights, which incorrectly includes 15 and every denser number. The equality test must happen after the full popcount, not when the third bit is found. Time is O(n log n) in the worst case, bounded by one million values, and space is O(1).'].join('\n'),
    referenceSolution:six('function solve(n){let a=0;for(let x=0;x<=n;x++){let y=x,c=0;while(y){c++;y&=y-1}if(c===3)a++}return a}','function solve(n:number):number{let a=0;for(let x=0;x<=n;x++){let y=x,c=0;while(y){c++;y&=y-1}if(c===3)a++}return a}','def solve(n):\n a=0\n for x in range(n+1):\n  y=x;c=0\n  while y:c+=1;y&=y-1\n  if c==3:a+=1\n return a','    static int solve(int n){int a=0;for(int x=0;x<=n;x++){int y=x,c=0;while(y>0){c++;y&=y-1;}if(c==3)a++;}return a;}','#include <vector>\nusing namespace std; int solve(int n){int a=0;for(int x=0;x<=n;x++){int y=x,c=0;while(y){c++;y&=y-1;}if(c==3)a++;}return a;}','func solve(n int) int{a:=0;for x:=0;x<=n;x++{y,c:=x,0;for y>0{c++;y&=y-1};if c==3{a++}};return a}'),
    tests:[{stdin:'10',expectedStdout:'1',isSample:true},{stdin:'6',expectedStdout:'0',isSample:true},{stdin:'7',expectedStdout:'1'},{stdin:'15',expectedStdout:'4'}] }),
  p({ ...base, slug:'bit-flips-between-keys', title:'Switches Needed Between Keys', difficulty:'EASY', patternTags:['bit-manipulation','xor','counting'], signatureId:'fn:int,int->int', avgSolveSeconds:540,
    promptMarkdown:['Return the number of bit positions that differ between non-negative integers `a` and `b`. Both fit in signed 32-bit range.','', '**Example**','', '```','input: 29','15','output: 2','```','', '`29` is `11101` and `15` is `01111`; a zero difference is the named edge case.'].join('\n'),
    editorialMarkdown:['## XOR then clear differences','', 'XOR turns equal positions into zero and different positions into one. The requested count is therefore the popcount of `a XOR b`; repeatedly clearing its lowest one counts one required flip each time. The arithmetic never needs to inspect positions where the keys already agree.', '', 'The quiet mistake is counting one bits in a and b separately, which measures brightness rather than disagreement. XOR is the comparison operation that preserves only mismatches. The loop runs once per differing bit, at most 31 times, so time and space are O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let x=a^b,c=0;while(x){c++;x&=x-1}return c}','function solve(a:number,b:number):number{let x=a^b,c=0;while(x){c++;x&=x-1}return c}','def solve(a,b):\n x=a^b;c=0\n while x:c+=1;x&=x-1\n return c','    static int solve(int a,int b){int x=a^b,c=0;while(x>0){c++;x&=x-1;}return c;}','#include <vector>\nusing namespace std; int solve(int a,int b){int x=a^b,c=0;while(x){c++;x&=x-1;}return c;}','func solve(a,b int) int{x,c:=a^b,0;for x>0{c++;x&=x-1};return c}'),
    tests:[{stdin:'29\n15',expectedStdout:'2',isSample:true},{stdin:'0\n0',expectedStdout:'0',isSample:true},{stdin:'1\n8',expectedStdout:'2'},{stdin:'7\n0',expectedStdout:'3'}] }),
  /* Removed draft entry: lowest-set-bit-value.
    promptMarkdown:['Return the value of the lowest lit bit of positive integer `n`.','', '**Example**','', '```','input: 40','output: 8','```','', '`40` is `101000`, so its lowest lit position has value 8. The input is always positive, avoiding the undefined zero case.'].join('\n'),
    editorialMarkdown:['## Isolate the low bit','', 'Subtracting one flips the lowest lit bit of n to zero and changes lower zeroes to ones. The expression `n & -n` keeps only the position where n and its two’s-complement negation overlap, which is exactly the lowest lit bit. It returns the bit’s value, not its position.', '', 'The quiet mistake is returning the count of trailing zeroes instead. For 40 that position is 3, while this task asks for 8. The identity uses a fixed signed integer operation, so it is O(1) time and O(1) space.'].join('\n'),
    oldReferenceSolution:six('function solve(n){return n&-n}','function solve(n:number):number{return n&-n}','def solve(n):\n return n&-n','    static int solve(int n){return n&-n;}','#include <vector>\nusing namespace std; int solve(int n){return n&-n;}','func solve(n int) int{return n&-n}'),
    oldTests:[{stdin:'40',expectedStdout:'8',oldSample:true},{stdin:'1',expectedStdout:'1',oldSample:true},{stdin:'12',expectedStdout:'4'},{stdin:'1024',expectedStdout:'1024'}] }), */
];
