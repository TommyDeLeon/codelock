import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w2-055` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W2_055_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "network-node-connections",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "GRAPHS",
    title: "Network Node Connections",
    patternTags: ["graphs","counting","array"],
    signatureId: "fn:matrix,int->ints",
    avgSolveSeconds: 300,
    promptMarkdown: "You are mapping a local network of computers. You are given a 2D integer array `links` where `links[i] = [u, v]` indicates a bidirectional network cable connecting computer `u` and computer `v`. You are also given an integer `n`, meaning the computers are labeled from `0` to `n - 1`.\n\nReturn an integer array of size `n` where the `i`-th element is the number of direct connections computer `i` has.\n\n**Constraints**\n- `1 <= n <= 1000`\n- `0 <= links.length <= 1000`\n- `links[i].length == 2`\n- `0 <= links[i][0], links[i][1] < n`\n- There are no self-loops, and each pair is connected by at most one cable.\n\n**Example 1**\n```\ninput:\n0 1;0 2;1 2\n3\noutput: 2 2 2\n```\n*Explanation: Node 0 is connected to 1 and 2 (2 links). Node 1 to 0 and 2. Node 2 to 0 and 1.*\n\n**Example 2**\n```\ninput:\n0 3;1 3;2 3\n4\noutput: 1 1 1 3\n```\n*Explanation: Computers 0, 1, and 2 connect only to 3. Computer 3 connects to 0, 1, and 2.*\n\n**Example 3**\n```\ninput:\n\n5\noutput: 0 0 0 0 0\n```\n*Explanation: No links. The output should be five zeros.*\n\n**Follow-up**\nHow does the solution change if the graph is directed?",
    editorialMarkdown: "## Network Node Connections\nThis problem is a foundational graph concept: finding the degree of each vertex. The degree is simply the number of edges connected to it. We can solve this by initializing an array of size `n` with zeros. As we iterate through each edge (link) `[u, v]` in the input matrix, we increment the count for node `u` and node `v`.\n\n**Trap**: Assuming all node IDs appear in the connections array. Isolated nodes won't appear, which is why an array of size `n` initialized to 0 is necessary.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(M) where M is the number of links.\n- **Space Complexity:** mathcal{O}(N) to store the result array.",
    referenceSolution: {
      JAVASCRIPT: "function solve(links, n) {\n    let res = new Array(n).fill(0);\n    for (let link of links) {\n        if(link.length === 2) {\n            res[link[0]]++;\n            res[link[1]]++;\n        }\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(links: number[][], n: number): number[] {\n    let res = new Array(n).fill(0);\n    for (let link of links) {\n        if(link.length === 2) {\n            res[link[0]]++;\n            res[link[1]]++;\n        }\n    }\n    return res;\n}",
      PYTHON: "def solve(links, n):\n    res = [0] * n\n    for link in links:\n        if len(link) == 2:\n            res[link[0]] += 1\n            res[link[1]] += 1\n    return res",
      JAVA: "    static int[] solve(int[][] links, int n) {\n        int[] res = new int[n];\n        for (int[] link : links) {\n            if (link.length == 2) {\n                res[link[0]]++;\n                res[link[1]]++;\n            }\n        }\n        return res;\n    }",
      CPP: "vector<int> solve(vector<vector<int>> links, int n) {\n    vector<int> res(n, 0);\n    for (auto& link : links) {\n        if (link.size() == 2) {\n            res[link[0]]++;\n            res[link[1]]++;\n        }\n    }\n    return res;\n}",
      GO: "func solve(links [][]int, n int) []int {\n    res := make([]int, n)\n    for _, link := range links {\n        if len(link) == 2 {\n            res[link[0]]++\n            res[link[1]]++\n        }\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "0 1;0 2;1 2\n3", expectedStdout: "2 2 2", isSample: true },
      { stdin: "0 3;1 3;2 3\n4", expectedStdout: "1 1 1 3", isSample: true },
      { stdin: "\n5", expectedStdout: "0 0 0 0 0", isSample: true },
      { stdin: "\n1", expectedStdout: "0" },
      { stdin: "0 1\n2", expectedStdout: "1 1" },
      { stdin: "0 1;1 2\n3", expectedStdout: "1 2 1" },
      { stdin: "0 1;0 2\n4", expectedStdout: "2 1 1 0" },
      { stdin: "0 1;0 2;0 3\n4", expectedStdout: "3 1 1 1" },
    ],
  }),

  p({
    ...base,
    slug: "successful-apprentices",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Successful Apprentices",
    patternTags: ["hash-map","array","counting"],
    signatureId: "fn:matrix,int->int",
    avgSolveSeconds: 450,
    promptMarkdown: "In a wizarding academy, `n` apprentices (numbered `0` to `n - 1`) are crafting potions from various magical categories.\n\nYou are given a 2D integer array `tasks` where each `tasks[i] = [apprentice_id, category_id]` means the given apprentice successfully crafted a potion of the given category.\n\nAn apprentice is deemed \"successful\" if they craft at least `i + 1` potions in *any single category* (where `i` is their apprentice ID).\n\nReturn the total number of successful apprentices.\n\n**Constraints**\n- `1 <= n <= 1000`\n- `0 <= tasks.length <= 1000`\n- `tasks[i].length == 2`\n- `0 <= tasks[i][0] < n`\n- `0 <= tasks[i][1] <= 1000`\n\n**Example 1**\n```\ninput:\n0 1;0 1;1 2\n2\noutput: 1\n```\n*Explanation: Apprentice 0 needs 1 potion in any one category (has 2 in category 1). Apprentice 1 needs 2 potions (has 1 in category 2). Only apprentice 0 is successful.*\n\n**Example 2**\n```\ninput:\n1 5;1 5;2 3\n3\noutput: 1\n```\n*Explanation: Apprentice 1 needs 2 potions in one category (has 2 in category 5). Apprentice 2 needs 3 potions (has 1). Apprentice 0 needs 1 potion (has 0). Total successful: 1.*\n\n**Example 3**\n```\ninput:\n0 0;1 1;2 2\n3\noutput: 1\n```\n*Explanation: Only apprentice 0 reaches their target of 1.*\n\n**Follow-up**\nCan you do this efficiently with only a single pass over the tasks array if we don't care about memory limits?",
    editorialMarkdown: "## Successful Apprentices\nWe want to check if any apprentice has completed at least `i + 1` tasks of a single category, where `i` is their ID. We can map each apprentice to a hashmap or a frequency array counting how many times they completed each category.\nFirst, initialize an array of dictionaries (or a 2D matrix if the max category ID is small). Populate this structure by iterating through the `tasks` array. Next, check each apprentice's dictionary values: if any category frequency is greater than their apprentice ID, they are considered successful. Count and return the number of successful apprentices.\n\n**Trap**: Initializing an array of objects in Javascript via `new Array(n).fill({})` will cause all elements to reference the exact same object. A map or `.from()` factory function should be used instead.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(T + N) where T is the number of tasks and N is the number of apprentices.\n- **Space Complexity:** mathcal{O}(T + N) since we only store records for categories that an apprentice has actually completed.",
    referenceSolution: {
      JAVASCRIPT: "function solve(tasks, n) {\n    let counts = Array.from({length: n}, () => ({}));\n    for (let task of tasks) {\n        if (!task || task.length < 2) continue;\n        let id = task[0], cat = task[1];\n        counts[id][cat] = (counts[id][cat] || 0) + 1;\n    }\n    let success = 0;\n    for (let i = 0; i < n; i++) {\n        for (let key in counts[i]) {\n            if (counts[i][key] > i) {\n                success++;\n                break;\n            }\n        }\n    }\n    return success;\n}",
      TYPESCRIPT: "function solve(tasks: number[][], n: number): number {\n    let counts: Record<number, number>[] = Array.from({length: n}, () => ({}));\n    for (let task of tasks) {\n        if (task.length === 2) {\n            let id = task[0], cat = task[1];\n            counts[id][cat] = (counts[id][cat] || 0) + 1;\n        }\n    }\n    let success = 0;\n    for (let i = 0; i < n; i++) {\n        for (let key in counts[i]) {\n            if (counts[i][key] > i) {\n                success++;\n                break;\n            }\n        }\n    }\n    return success;\n}",
      PYTHON: "def solve(tasks, n):\n    from collections import defaultdict\n    counts = [defaultdict(int) for _ in range(n)]\n    for task in tasks:\n        if len(task) == 2:\n            counts[task[0]][task[1]] += 1\n    success = 0\n    for i in range(n):\n        for val in counts[i].values():\n            if val > i:\n                success += 1\n                break\n    return success",
      JAVA: "    static int solve(int[][] tasks, int n) {\n        java.util.Map<Integer, Integer>[] counts = new java.util.HashMap[n];\n        for (int i = 0; i < n; i++) {\n            counts[i] = new java.util.HashMap<>();\n        }\n        for (int[] task : tasks) {\n            if (task.length == 2) {\n                counts[task[0]].put(task[1], counts[task[0]].getOrDefault(task[1], 0) + 1);\n            }\n        }\n        int success = 0;\n        for (int i = 0; i < n; i++) {\n            for (int count : counts[i].values()) {\n                if (count > i) {\n                    success++;\n                    break;\n                }\n            }\n        }\n        return success;\n    }",
      CPP: "int solve(vector<vector<int>> tasks, int n) {\n    vector<unordered_map<int, int>> counts(n);\n    for (auto& task : tasks) {\n        if (task.size() == 2) {\n            counts[task[0]][task[1]]++;\n        }\n    }\n    int success = 0;\n    for (int i = 0; i < n; i++) {\n        for (auto& pair : counts[i]) {\n            if (pair.second > i) {\n                success++;\n                break;\n            }\n        }\n    }\n    return success;\n}",
      GO: "func solve(tasks [][]int, n int) int {\n    counts := make([]map[int]int, n)\n    for i := range counts {\n        counts[i] = make(map[int]int)\n    }\n    for _, task := range tasks {\n        if len(task) == 2 {\n            counts[task[0]][task[1]]++\n        }\n    }\n    success := 0\n    for i, cats := range counts {\n        for _, count := range cats {\n            if count > i {\n                success++\n                break\n            }\n        }\n    }\n    return success\n}",
    },
    tests: [
      { stdin: "0 1;0 1;1 2\n2", expectedStdout: "1", isSample: true },
      { stdin: "1 5;1 5;2 3\n3", expectedStdout: "1", isSample: true },
      { stdin: "0 0;1 1;2 2\n3", expectedStdout: "1", isSample: true },
      { stdin: "\n5", expectedStdout: "0" },
      { stdin: "0 999\n10", expectedStdout: "1" },
      { stdin: "0 10;1 10;1 10\n2", expectedStdout: "2" },
      { stdin: "1 5;2 5;2 5\n3", expectedStdout: "0" },
      { stdin: "0 1;1 2;1 2;2 3;2 3\n3", expectedStdout: "2" },
    ],
  }),

  p({
    ...base,
    slug: "cargo-container-compaction",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "TWO_POINTERS",
    title: "Cargo Container Compaction",
    patternTags: ["two-pointers","string","array"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 400,
    promptMarkdown: "You are operating a cargo crane that compacts containers. You are given a 0-indexed integer array `weights` representing the weights of a row of containers.\n\nThe crane repeatedly performs the following operation until the row is empty:\n- If there are at least two containers remaining, the crane removes the first and the last container. It fuses them into a single container whose weight is the string concatenation of the first and last weights, converted back to an integer. This new weight is added to the `total_compacted_weight`.\n- If there is exactly one container left, the crane removes it, adds its weight to the `total_compacted_weight`, and the process ends.\n\nReturn the `total_compacted_weight`.\n\n**Constraints**\n- `1 <= weights.length <= 1000`\n- `1 <= weights[i] <= 10000`\n\n**Example 1**\n```\ninput:\n7 52 2 4\noutput: 596\n```\n*Explanation: First pairs 7 and 4 to make 74. Then pairs 52 and 2 to make 522. Total is 74 + 522 = 596.*\n\n**Example 2**\n```\ninput:\n5 14 13 8 12\noutput: 673\n```\n*Explanation: Pairs 5 and 12 (512), then 14 and 8 (148), leaving 13. Total = 512 + 148 + 13 = 673.*\n\n**Example 3**\n```\ninput:\n10\noutput: 10\n```\n*Explanation: Only one container, its weight is added to total directly.*\n\n**Follow-up**\nCan you solve this with mathcal{O}(1) auxiliary space?",
    editorialMarkdown: "## Cargo Container Compaction\nThis problem asks us to pair elements from the ends of an array inward, treating them as strings during combination. We can use a **Two Pointers** approach. Initialize one pointer at the start of the array and another at the end. At each step, convert the two numbers to strings, concatenate them, convert back to an integer, and add to the total. Then move both pointers inward. If the pointers meet at the exact same index (which happens if the array length is odd), simply add that single remaining element to the total.\n\n**Trap**: Forgetting to handle the middle element when the array length is odd, or adding it twice.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the number of elements in the array.\n- **Space Complexity:** mathcal{O}(1) beyond the space used for string conversion.",
    referenceSolution: {
      JAVASCRIPT: "function solve(weights) {\n    let total = 0;\n    let i = 0, j = weights.length - 1;\n    while (i < j) {\n        total += parseInt(weights[i].toString() + weights[j].toString());\n        i++;\n        j--;\n    }\n    if (i === j) {\n        total += weights[i];\n    }\n    return total;\n}",
      TYPESCRIPT: "function solve(weights: number[]): number {\n    let total = 0;\n    let i = 0, j = weights.length - 1;\n    while (i < j) {\n        total += parseInt(weights[i].toString() + weights[j].toString());\n        i++;\n        j--;\n    }\n    if (i === j) {\n        total += weights[i];\n    }\n    return total;\n}",
      PYTHON: "def solve(weights):\n    total = 0\n    i, j = 0, len(weights) - 1\n    while i < j:\n        total += int(str(weights[i]) + str(weights[j]))\n        i += 1\n        j -= 1\n    if i == j:\n        total += weights[i]\n    return total",
      JAVA: "    static int solve(int[] weights) {\n        int total = 0;\n        int i = 0, j = weights.length - 1;\n        while (i < j) {\n            String s = String.valueOf(weights[i]) + String.valueOf(weights[j]);\n            total += Integer.parseInt(s);\n            i++;\n            j--;\n        }\n        if (i == j) {\n            total += weights[i];\n        }\n        return total;\n    }",
      CPP: "int solve(vector<int> weights) {\n    int total = 0;\n    int i = 0, j = weights.size() - 1;\n    while (i < j) {\n        string s = to_string(weights[i]) + to_string(weights[j]);\n        total += stoi(s);\n        i++;\n        j--;\n    }\n    if (i == j) {\n        total += weights[i];\n    }\n    return total;\n}",
      GO: "func solve(weights []int) int {\n    total := 0\n    i, j := 0, len(weights)-1\n    for i < j {\n        s := strconv.Itoa(weights[i]) + strconv.Itoa(weights[j])\n        val, _ := strconv.Atoi(s)\n        total += val\n        i++\n        j--\n    }\n    if i == j {\n        total += weights[i]\n    }\n    return total\n}",
    },
    tests: [
      { stdin: "7 52 2 4", expectedStdout: "596", isSample: true },
      { stdin: "5 14 13 8 12", expectedStdout: "673", isSample: true },
      { stdin: "10", expectedStdout: "10", isSample: true },
      { stdin: "1 1", expectedStdout: "11" },
      { stdin: "100 100", expectedStdout: "100100" },
      { stdin: "1 2 3 4 5 6 7 8 9", expectedStdout: "135" },
      { stdin: "5 10000", expectedStdout: "510000" },
      { stdin: "9999 888 7 8 10 9999", expectedStdout: "100088887" },
    ],
  }),

  p({
    ...base,
    slug: "encrypted-signal-strength",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "SLIDING_WINDOW",
    title: "Encrypted Signal Strength",
    patternTags: ["sliding-window","math","string"],
    signatureId: "fn:int,int->int",
    avgSolveSeconds: 400,
    promptMarkdown: "An encrypted signal is represented by a positive integer `signal`. The decryption algorithm requires analyzing sub-sequences of digits of length `k`.\n\nThe \"strength\" of the signal is the number of contiguous substrings of length `k` (read as integers) that perfectly divide the original `signal`.\n\nNote:\n- Substrings may contain leading zeros and should be parsed as normal integers.\n- A value of 0 cannot be a divisor.\n\nReturn the strength of the signal.\n\n**Constraints**\n- `1 <= signal <= 10^9`\n- `1 <= k <= the number of digits in signal`\n\n**Example 1**\n```\ninput:\n240\n2\noutput: 2\n```\n*Explanation: Substrings of length 2 are \"24\" and \"40\". 24 divides 240. 40 divides 240. Strength is 2.*\n\n**Example 2**\n```\ninput:\n430043\n2\noutput: 2\n```\n*Explanation: Substrings of length 2 are \"43\", \"30\", \"00\", \"04\", \"43\". \"43\" divides 430043 (twice). \"30\", \"00\" (0), and \"04\" (4) do not. Total strength: 2.*\n\n**Example 3**\n```\ninput:\n10\n1\noutput: 1\n```\n*Explanation: Substrings of length 1 are \"1\" and \"0\". 1 divides 10, 0 does not.*\n\n**Follow-up**\nCan you optimize this by doing mathematically sound rolling window operations without string allocations?",
    editorialMarkdown: "## Encrypted Signal Strength\nTo find the strength of the signal, we must check every contiguous substring of length `k`. Converting the number to a string allows us to cleanly extract each sliding window of characters. After extracting, we convert it back to an integer and check whether it perfectly divides the original number.\n\n**Trap**: Substrings evaluated to zero cannot be used as divisors. Evaluating `signal % 0` will crash in many languages, so a `val != 0` check is essential.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(L) where L is the number of digits. The string slicing and parsing operations are mathcal{O}(k), making it strictly mathcal{O}(L · k), but since k ≤ 10, this operates efficiently like mathcal{O}(L).\n- **Space Complexity:** mathcal{O}(L) for the string representation of the number.",
    referenceSolution: {
      JAVASCRIPT: "function solve(signal, k) {\n    let str = signal.toString();\n    let count = 0;\n    for (let i = 0; i <= str.length - k; i++) {\n        let val = parseInt(str.substring(i, i + k));\n        if (val !== 0 && signal % val === 0) count++;\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(signal: number, k: number): number {\n    let str = signal.toString();\n    let count = 0;\n    for (let i = 0; i <= str.length - k; i++) {\n        let val = parseInt(str.substring(i, i + k));\n        if (val !== 0 && signal % val === 0) count++;\n    }\n    return count;\n}",
      PYTHON: "def solve(signal, k):\n    s = str(signal)\n    count = 0\n    for i in range(len(s) - k + 1):\n        val = int(s[i:i+k])\n        if val != 0 and signal % val == 0:\n            count += 1\n    return count",
      JAVA: "    static int solve(int signal, int k) {\n        String str = String.valueOf(signal);\n        int count = 0;\n        for (int i = 0; i <= str.length() - k; i++) {\n            int val = Integer.parseInt(str.substring(i, i + k));\n            if (val != 0 && signal % val == 0) {\n                count++;\n            }\n        }\n        return count;\n    }",
      CPP: "int solve(int signal, int k) {\n    string s = to_string(signal);\n    int count = 0;\n    for (int i = 0; i <= (int)s.length() - k; i++) {\n        int val = stoi(s.substr(i, k));\n        if (val != 0 && signal % val == 0) {\n            count++;\n        }\n    }\n    return count;\n}",
      GO: "func solve(signal int, k int) int {\n    str := strconv.Itoa(signal)\n    count := 0\n    for i := 0; i <= len(str) - k; i++ {\n        val, _ := strconv.Atoi(str[i:i+k])\n        if val != 0 && signal % val == 0 {\n            count++\n        }\n    }\n    return count\n}",
    },
    tests: [
      { stdin: "240\n2", expectedStdout: "2", isSample: true },
      { stdin: "430043\n2", expectedStdout: "2", isSample: true },
      { stdin: "10\n1", expectedStdout: "1", isSample: true },
      { stdin: "5\n1", expectedStdout: "1" },
      { stdin: "52\n1", expectedStdout: "1" },
      { stdin: "111\n1", expectedStdout: "3" },
      { stdin: "1000000000\n10", expectedStdout: "1" },
      { stdin: "2048\n2", expectedStdout: "1" },
    ],
  }),

  p({
    ...base,
    slug: "biometric-security-code",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Biometric Security Code",
    patternTags: ["math","string","digits"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 350,
    promptMarkdown: "A biometric vault is secured by three distinct access codes, given as an array of 3 integers `codes`. To generate the final unlock code, each of the three access codes is padded with leading zeros until it is exactly 4 digits long.\n\nThe final unlock code is formed by examining each of the 4 digit positions one by one, and selecting the smallest digit at that position across all three codes.\n\nReturn the final unlock code as an integer (which automatically drops any leading zeros).\n\n**Constraints**\n- `codes.length == 3`\n- `1 <= codes[i] <= 9999`\n\n**Example 1**\n```\ninput:\n987 879 798\noutput: 777\n```\n*Explanation: Padded to 4 digits: 0987, 0879, 0798.\nPos 1: min(0,0,0) = 0. Pos 2: min(9,8,7) = 7. Pos 3: min(8,7,9) = 7. Pos 4: min(7,9,8) = 7. Result: 0777 -> 777.*\n\n**Example 2**\n```\ninput:\n1 1 1\noutput: 1\n```\n*Explanation: Codes are 0001, 0001, 0001. Min at each position gives 0001 -> 1.*\n\n**Example 3**\n```\ninput:\n9999 9999 9999\noutput: 9999\n```\n*Explanation: Minimum digits remain 9999.*\n\n**Follow-up**\nCan you do this purely mathematically without converting to strings?",
    editorialMarkdown: "## Biometric Security Code\nTo form the final unlock code, we need to extract and compare characters at the exact same digit positions across three numbers. The easiest way is to stringify all three integers, pad them with leading zeros using a standard library function (like `padStart` or `zfill`) so they are 4 characters long, and then iterate through each character position. Take the minimum character, build the final string, and finally parse it back to an integer.\n\n**Trap**: Forgetting to pad with leading zeros if one of the access codes has fewer than 4 digits. The padding must happen before alignment.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(1) since the number of digits is constant (4).\n- **Space Complexity:** mathcal{O}(1) because we only allocate small fixed-length strings.",
    referenceSolution: {
      JAVASCRIPT: "function solve(codes) {\n    let s0 = codes[0].toString().padStart(4, '0');\n    let s1 = codes[1].toString().padStart(4, '0');\n    let s2 = codes[2].toString().padStart(4, '0');\n    let res = \"\";\n    for (let i = 0; i < 4; i++) {\n        res += Math.min(parseInt(s0[i]), parseInt(s1[i]), parseInt(s2[i]));\n    }\n    return parseInt(res);\n}",
      TYPESCRIPT: "function solve(codes: number[]): number {\n    let s0 = codes[0].toString().padStart(4, '0');\n    let s1 = codes[1].toString().padStart(4, '0');\n    let s2 = codes[2].toString().padStart(4, '0');\n    let res = \"\";\n    for (let i = 0; i < 4; i++) {\n        res += Math.min(parseInt(s0[i]), parseInt(s1[i]), parseInt(s2[i]));\n    }\n    return parseInt(res);\n}",
      PYTHON: "def solve(codes):\n    s0 = str(codes[0]).zfill(4)\n    s1 = str(codes[1]).zfill(4)\n    s2 = str(codes[2]).zfill(4)\n    res = \"\"\n    for i in range(4):\n        res += min(s0[i], s1[i], s2[i])\n    return int(res)",
      JAVA: "    static int solve(int[] codes) {\n        String s0 = String.format(\"%04d\", codes[0]);\n        String s1 = String.format(\"%04d\", codes[1]);\n        String s2 = String.format(\"%04d\", codes[2]);\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < 4; i++) {\n            char m = (char) Math.min(s0.charAt(i), Math.min(s1.charAt(i), s2.charAt(i)));\n            sb.append(m);\n        }\n        return Integer.parseInt(sb.toString());\n    }",
      CPP: "int solve(vector<int> codes) {\n    auto pad = [](int n) {\n        string s = to_string(n);\n        while (s.length() < 4) s = \"0\" + s;\n        return s;\n    };\n    string s0 = pad(codes[0]);\n    string s1 = pad(codes[1]);\n    string s2 = pad(codes[2]);\n    string res = \"\";\n    for (int i = 0; i < 4; i++) {\n        res += min({s0[i], s1[i], s2[i]});\n    }\n    return stoi(res);\n}",
      GO: "func solve(codes []int) int {\n    s0 := fmt.Sprintf(\"%04d\", codes[0])\n    s1 := fmt.Sprintf(\"%04d\", codes[1])\n    s2 := fmt.Sprintf(\"%04d\", codes[2])\n    resStr := \"\"\n    for i := 0; i < 4; i++ {\n        minD := s0[i]\n        if s1[i] < minD { minD = s1[i] }\n        if s2[i] < minD { minD = s2[i] }\n        resStr += string(minD)\n    }\n    res, _ := strconv.Atoi(resStr)\n    return res\n}",
    },
    tests: [
      { stdin: "987 879 798", expectedStdout: "777", isSample: true },
      { stdin: "1 1 1", expectedStdout: "1", isSample: true },
      { stdin: "9999 9999 9999", expectedStdout: "9999", isSample: true },
      { stdin: "5 10 100", expectedStdout: "0" },
      { stdin: "2222 222 2222", expectedStdout: "222" },
      { stdin: "1 2 3", expectedStdout: "1" },
      { stdin: "8000 8123 9000", expectedStdout: "8000" },
      { stdin: "1234 1234 1234", expectedStdout: "1234" },
    ],
  }),

  p({
    ...base,
    slug: "optimal-power-grid-voltage",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Optimal Power Grid Voltage",
    patternTags: ["array","counting"],
    signatureId: "fn:matrix->int",
    avgSolveSeconds: 400,
    promptMarkdown: "You are an engineer designing a power grid. You are given a 2D integer array `data` of size `2 x n`.\n\nThe first row `data[0]` contains the power capacities of `n` industrial machines.\nThe second row `data[1]` contains `n` candidate voltage levels.\n\nA voltage level is considered compatible with a machine if it divides the machine's power capacity perfectly (with no remainder).\n\nYour task is to find the candidate voltage from `data[1]` that is compatible with the maximum number of machines in `data[0]`.\nIf there is a tie for the maximum compatibility score, return the lowest voltage among those tied.\n\n**Constraints**\n- `data.length == 2`\n- `1 <= data[0].length == data[1].length <= 100`\n- `1 <= data[0][i], data[1][i] <= 10^5`\n\n**Example 1**\n```\ninput:\n10 20 30;5 3 2\noutput: 2\n```\n*Explanation:\nVoltage 5 divides 10, 20 (score 2).\nVoltage 3 divides 30 (score 1).\nVoltage 2 divides 10, 20, 30 (score 3).\nMax score is 3, achieved by voltage 2.*\n\n**Example 2**\n```\ninput:\n15 15;3 5\noutput: 3\n```\n*Explanation: Both 3 and 5 divide 15 and have a score of 2. The minimum voltage among them is 3.*\n\n**Example 3**\n```\ninput:\n7 11 13;2 4 6\noutput: 2\n```\n*Explanation: None divide perfectly (score 0). Lowest voltage is 2.*\n\n**Follow-up**\nCan you optimize this if both the machine capacities and the voltage candidates are pre-sorted?",
    editorialMarkdown: "## Optimal Power Grid Voltage\nThe goal is to test a set of candidates against a set of constraints to find the candidate with the highest \"score\", with a specific tie-breaking rule. Since the sizes are small, we can afford a brute-force double loop. Iterate through each candidate voltage. For each voltage, iterate over every machine and increment a score counter if the voltage perfectly divides the machine's capacity. Finally, keep a running track of the maximum score and the best voltage so far.\n\n**Trap**: When a tie occurs (the current voltage score matches the `maxScore`), we must update the `bestVoltage` if the current voltage is strictly less than the `bestVoltage`. Make sure to initialize `bestVoltage` gracefully or check for defaults correctly.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N^2) because we check N voltages against N machines.\n- **Space Complexity:** mathcal{O}(1) beyond the input structures.",
    referenceSolution: {
      JAVASCRIPT: "function solve(data) {\n    let machines = data[0];\n    let voltages = data[1];\n    let maxScore = -1;\n    let bestVoltage = -1;\n    for (let v of voltages) {\n        let score = 0;\n        for (let m of machines) {\n            if (m % v === 0) score++;\n        }\n        if (score > maxScore) {\n            maxScore = score;\n            bestVoltage = v;\n        } else if (score === maxScore && (bestVoltage === -1 || v < bestVoltage)) {\n            bestVoltage = v;\n        }\n    }\n    return bestVoltage;\n}",
      TYPESCRIPT: "function solve(data: number[][]): number {\n    let machines = data[0];\n    let voltages = data[1];\n    let maxScore = -1;\n    let bestVoltage = -1;\n    for (let v of voltages) {\n        let score = 0;\n        for (let m of machines) {\n            if (m % v === 0) score++;\n        }\n        if (score > maxScore) {\n            maxScore = score;\n            bestVoltage = v;\n        } else if (score === maxScore && (bestVoltage === -1 || v < bestVoltage)) {\n            bestVoltage = v;\n        }\n    }\n    return bestVoltage;\n}",
      PYTHON: "def solve(data):\n    max_score = -1\n    best_voltage = -1\n    for v in data[1]:\n        score = sum(1 for m in data[0] if m % v == 0)\n        if score > max_score:\n            max_score = score\n            best_voltage = v\n        elif score == max_score:\n            if best_voltage == -1 or v < best_voltage:\n                best_voltage = v\n    return best_voltage",
      JAVA: "    static int solve(int[][] data) {\n        int maxScore = -1;\n        int bestVoltage = -1;\n        for (int v : data[1]) {\n            int score = 0;\n            for (int m : data[0]) {\n                if (m % v == 0) score++;\n            }\n            if (score > maxScore) {\n                maxScore = score;\n                bestVoltage = v;\n            } else if (score == maxScore) {\n                if (bestVoltage == -1 || v < bestVoltage) bestVoltage = v;\n            }\n        }\n        return bestVoltage;\n    }",
      CPP: "int solve(vector<vector<int>> data) {\n    int maxScore = -1;\n    int bestVoltage = -1;\n    for (int v : data[1]) {\n        int score = 0;\n        for (int m : data[0]) {\n            if (m % v == 0) score++;\n        }\n        if (score > maxScore) {\n            maxScore = score;\n            bestVoltage = v;\n        } else if (score == maxScore) {\n            if (bestVoltage == -1 || v < bestVoltage) bestVoltage = v;\n        }\n    }\n    return bestVoltage;\n}",
      GO: "func solve(data [][]int) int {\n    maxScore := -1\n    bestVoltage := -1\n    for _, v := range data[1] {\n        score := 0\n        for _, m := range data[0] {\n            if m % v == 0 {\n                score++\n            }\n        }\n        if score > maxScore {\n            maxScore = score\n            bestVoltage = v\n        } else if score == maxScore {\n            if bestVoltage == -1 || v < bestVoltage {\n                bestVoltage = v\n            }\n        }\n    }\n    return bestVoltage\n}",
    },
    tests: [
      { stdin: "10 20 30;5 3 2", expectedStdout: "2", isSample: true },
      { stdin: "15 15;3 5", expectedStdout: "3", isSample: true },
      { stdin: "7 11 13;2 4 6", expectedStdout: "2", isSample: true },
      { stdin: "100;1", expectedStdout: "1" },
      { stdin: "2 4 8;8 4 2", expectedStdout: "2" },
      { stdin: "100 200 300;10 10 10", expectedStdout: "10" },
      { stdin: "10 10 10;10 11 12", expectedStdout: "10" },
      { stdin: "10 20 30;10 5 2", expectedStdout: "2" },
    ],
  }),
];
