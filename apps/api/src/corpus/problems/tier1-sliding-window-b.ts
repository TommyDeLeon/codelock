import { AUTHORED, type ProblemDefinition } from '../problem.js';

/**
 * Tier 1 — Sliding Window, part B.
 *
 * `tier1-sliding-window.ts` holds a single problem, which leaves the family
 * short of the three solves the progression gate wants before it will open the
 * next node. These eight finish it.
 *
 * They are ordered as a ramp through the four window shapes a solver actually
 * meets: a window of one number remembered (best buy/sell), a window of fixed
 * width, a window that grows and shrinks under a constraint, and a window that
 * carries an auxiliary structure — a count map, then a monotonic deque. Every
 * editorial answers the same question, because it is the only question the
 * pattern really has: why does the left edge never need to move backwards?
 * That monotonicity is what collapses O(n^2) into O(n); the loop shape is just
 * the shadow it casts.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = {
  tier: 'TIER_1',
  patternFamily: 'SLIDING_WINDOW',
  provenance: AUTHORED,
} as const;

export const TIER_1_SLIDING_WINDOW_B_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: 'best-single-buy-and-sell',
    title: 'Buy Once, Sell Later',
    difficulty: 'EASY',
    patternTags: ['sliding-window', 'running-minimum', 'greedy'],
    signatureId: 'fn:ints->int',
    avgSolveSeconds: 420,
    promptMarkdown: [
      'You are given the price of one item on each of a run of days, in order.',
      '',
      'Choose one day to buy and one **later** day to sell. Return the largest',
      'profit that is available. If every possible sale loses money, return `0` —',
      'that is the "do nothing" case, and it is a real answer, not an error.',
      '',
      '**Example**',
      '',
      '```',
      'input:  7 1 5 3 6 4',
      'output: 5',
      '```',
      '',
      'Buy on day 2 at `1`, sell on day 5 at `6`. Buying at `7` and selling at any',
      'later price would lose money; selling before you buy is not allowed.',
      '',
      '```',
      'input:  9 7 5 3',
      'output: 0',
      '```',
      '',
      'Prices only fall, so no buy-then-sell pair profits. The answer is `0`.',
      '',
      'The list always has at least one price. A single price gives `0`, because',
      'there is no later day to sell on, and a list where every price is equal also',
      'gives `0`. Prices may be any integers.',
    ].join('\n'),
    editorialMarkdown: [
      '## A window with only its cheapest day remembered',
      '',
      'The brute force tries every pair of days: for each buy day, scan every later',
      'sell day. That is O(n^2), and it is correct. It also asks the same question',
      'over and over — "what is the cheapest day at or before here?" — and recomputes',
      'the answer from scratch each time.',
      '',
      'Think of it as a window that starts on the cheapest day seen so far and ends',
      'on today. The only fact the window needs to carry is that one price:',
      '',
      '```',
      'low = first price, best = 0',
      'for price in prices:',
      '    if price < low: low = price',
      '    best = max(best, price - low)',
      '```',
      '',
      '**Why the left edge never moves backwards.** The left edge is the buy day. If',
      'you are standing on day `i` and the cheapest price at or before `i` sits on',
      'day `j`, then for every day after `i` the cheapest price is still at `j` or at',
      'something even cheaper found later — never at something before `j`, because a',
      'prefix minimum can only fall. A day whose price is above the running minimum',
      'is dead forever: any sale it could serve, the cheaper earlier day serves at',
      'least as well. So the buy day only ever slides forward, and each price is',
      'looked at once.',
      '',
      'The quiet mistake here is updating the answer before the window is valid:',
      'writing `low = min(low, price)` and `best = max(best, price - low)` in the',
      'wrong order relative to each other is fine — `price - price` is `0` — but',
      'reaching for `max(prices) - min(prices)` instead is not. That ignores the',
      'ordering constraint entirely and reports a profit for a sale that happens',
      'before its own purchase. On `7 1 5 3 6 4` both give `5`, so the bug hides;',
      'on `9 7 5 3` the correct answer is `0` and the broken one says `6`.',
      '',
      'The second quiet mistake is seeding `best` with the first difference rather',
      'than with `0`, which makes a strictly falling list report a negative profit.',
      'Read the statement: the floor is `0`.',
      '',
      'O(n) time — one pass, one comparison per price — and O(1) space, since the',
      'whole window is summarised by a single number. The time is bounded by the',
      'single scan; nothing is revisited.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  let best = 0;\n  let low = 0;\n  let started = false;\n  for (let i = 0; i < a.length; i++) {\n    const price = a[i];\n    if (!started || price < low) {\n      low = price;\n      started = true;\n    }\n    const gain = price - low;\n    if (gain > best) best = gain;\n  }\n  return best;\n}',
      TYPESCRIPT:
        'function solve(a: number[]): number {\n  let best = 0;\n  let low = 0;\n  let started = false;\n  for (let i = 0; i < a.length; i++) {\n    const price = a[i] as number;\n    if (!started || price < low) {\n      low = price;\n      started = true;\n    }\n    const gain = price - low;\n    if (gain > best) best = gain;\n  }\n  return best;\n}',
      PYTHON:
        'def solve(a):\n    best = 0\n    low = 0\n    started = False\n    for price in a:\n        if not started or price < low:\n            low = price\n            started = True\n        gain = price - low\n        if gain > best:\n            best = gain\n    return best',
      JAVA: '    static int solve(int[] a) {\n        int best = 0;\n        int low = 0;\n        boolean started = false;\n        for (int i = 0; i < a.length; i++) {\n            int price = a[i];\n            if (!started || price < low) {\n                low = price;\n                started = true;\n            }\n            int gain = price - low;\n            if (gain > best) best = gain;\n        }\n        return best;\n    }',
      CPP: 'int solve(vector<int> a) {\n    int best = 0;\n    int low = 0;\n    bool started = false;\n    for (int i = 0; i < (int) a.size(); i++) {\n        int price = a[i];\n        if (!started || price < low) {\n            low = price;\n            started = true;\n        }\n        int gain = price - low;\n        if (gain > best) best = gain;\n    }\n    return best;\n}',
      GO: 'func solve(a []int) int {\n\tbest := 0\n\tlow := 0\n\tstarted := false\n\tfor _, price := range a {\n\t\tif !started || price < low {\n\t\t\tlow = price\n\t\t\tstarted = true\n\t\t}\n\t\tgain := price - low\n\t\tif gain > best {\n\t\t\tbest = gain\n\t\t}\n\t}\n\treturn best\n}',
    },
    tests: [
      { stdin: '7 1 5 3 6 4', expectedStdout: '5', isSample: true },
      { stdin: '9 7 5 3', expectedStdout: '0', isSample: true },
      { stdin: '4 4 4 4', expectedStdout: '0' },
      { stdin: '2', expectedStdout: '0' },
      { stdin: '3 2 6 5 0 3', expectedStdout: '4' },
      { stdin: '1 2 3 4 5', expectedStdout: '4' },
    ],
  }),

  p({
    ...base,
    slug: 'longest-run-of-one-value-after-k-flips',
    title: 'Flip At Most k Zeros',
    difficulty: 'MEDIUM',
    patternTags: ['sliding-window', 'constraint-window', 'counting'],
    signatureId: 'fn:ints,int->int',
    avgSolveSeconds: 600,
    promptMarkdown: [
      'The first line is a list made only of `0`s and `1`s. The second line is a',
      'number `k`.',
      '',
      'You may change at most `k` of the zeros into ones. Return the length of the',
      'longest run of neighbouring `1`s you can end up with.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 1 0 1 1 0 1',
      '        1',
      'output: 5',
      '```',
      '',
      'Flip the zero at position 3 and the first five values become `1 1 1 1 1`.',
      'Flipping the other zero instead only reaches a run of 4, and with one flip',
      'you cannot bridge both zeros.',
      '',
      '```',
      'input:  0 0 0',
      '        0',
      'output: 0',
      '```',
      '',
      'With `k` of `0` you may not flip anything, so the answer is the longest run',
      'of `1`s already present — here there are none, and the answer is `0`.',
      '',
      'Guarantees: the list has at least one value, every value is `0` or `1`, and',
      '`k` is zero or more. If `k` is at least the number of zeros, the whole list',
      'can be turned into `1`s and the answer is its length.',
    ].join('\n'),
    editorialMarkdown: [
      '## A window that grows greedily and shrinks only when it must',
      '',
      'Rephrase the task and it stops being about flipping: you want the longest',
      'contiguous stretch that contains **at most `k` zeros**. Every zero inside such',
      'a stretch gets flipped; every one is already fine. Counting zeros is much',
      'easier than reasoning about which zeros to spend flips on.',
      '',
      'So keep a window `[left, right]` and a count of the zeros inside it. Push',
      '`right` forward one step at a time. If the window now holds more than `k`',
      'zeros it is illegal, so pull `left` forward until it is legal again.',
      '',
      '```',
      'left = 0, zeros = 0, best = 0',
      'for right in 0..n-1:',
      '    if a[right] == 0: zeros += 1',
      '    while zeros > k:',
      '        if a[left] == 0: zeros -= 1',
      '        left += 1',
      '    best = max(best, right - left + 1)',
      '```',
      '',
      '**Why the left edge never moves backwards.** Suppose for a given `right` the',
      'smallest legal `left` is `L`. Now advance to `right + 1`. Adding a value can',
      'only increase the zero count, never decrease it, so every start position that',
      'was already illegal for `right` is still illegal for `right + 1`. Nothing to',
      'the left of `L` can ever become legal again. The best window ending at each',
      'position therefore starts at a non-decreasing index, and `left` may march',
      'forward monotonically. Each index is entered once by `right` and left once by',
      '`left`, which is why two nested loops still add up to O(n) rather than O(n^2).',
      '',
      'The quiet mistake this problem invites is shrinking with `if` instead of',
      '`while`. One `if` removes at most one element, which is enough whenever the',
      'window overshoots by exactly one — and pushing `right` one step can only add',
      'one zero, so on this problem `if` looks right and passes most inputs. It',
      'breaks the moment you reuse the shape on a problem where the right edge can',
      'add more than one unit of badness at a time, and it breaks here too if you',
      'ever let `right` jump. Write `while`; it is correct in both worlds and costs',
      'nothing.',
      '',
      'The other quiet mistake is measuring `best` before restoring validity —',
      'reading `right - left + 1` inside the growth step rather than after the shrink',
      'loop. That records the width of an illegal window, and it only shows up on',
      'inputs where the window actually overflows, so a test made of all `1`s will',
      'never catch it.',
      '',
      'O(n) time, since both pointers only move right, and O(1) space — the window is',
      'summarised by one integer count.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  let left = 0;\n  let zeros = 0;\n  let best = 0;\n  for (let right = 0; right < a.length; right++) {\n    if (a[right] === 0) zeros++;\n    while (zeros > b) {\n      if (a[left] === 0) zeros--;\n      left++;\n    }\n    const width = right - left + 1;\n    if (width > best) best = width;\n  }\n  return best;\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number): number {\n  let left = 0;\n  let zeros = 0;\n  let best = 0;\n  for (let right = 0; right < a.length; right++) {\n    if (a[right] === 0) zeros++;\n    while (zeros > b) {\n      if (a[left] === 0) zeros--;\n      left++;\n    }\n    const width = right - left + 1;\n    if (width > best) best = width;\n  }\n  return best;\n}',
      PYTHON:
        'def solve(a, b):\n    left = 0\n    zeros = 0\n    best = 0\n    for right in range(len(a)):\n        if a[right] == 0:\n            zeros += 1\n        while zeros > b:\n            if a[left] == 0:\n                zeros -= 1\n            left += 1\n        width = right - left + 1\n        if width > best:\n            best = width\n    return best',
      JAVA: '    static int solve(int[] a, int b) {\n        int left = 0;\n        int zeros = 0;\n        int best = 0;\n        for (int right = 0; right < a.length; right++) {\n            if (a[right] == 0) zeros++;\n            while (zeros > b) {\n                if (a[left] == 0) zeros--;\n                left++;\n            }\n            int width = right - left + 1;\n            if (width > best) best = width;\n        }\n        return best;\n    }',
      CPP: 'int solve(vector<int> a, int b) {\n    int left = 0;\n    int zeros = 0;\n    int best = 0;\n    for (int right = 0; right < (int) a.size(); right++) {\n        if (a[right] == 0) zeros++;\n        while (zeros > b) {\n            if (a[left] == 0) zeros--;\n            left++;\n        }\n        int width = right - left + 1;\n        if (width > best) best = width;\n    }\n    return best;\n}',
      GO: 'func solve(a []int, b int) int {\n\tleft := 0\n\tzeros := 0\n\tbest := 0\n\tfor right := 0; right < len(a); right++ {\n\t\tif a[right] == 0 {\n\t\t\tzeros++\n\t\t}\n\t\tfor zeros > b {\n\t\t\tif a[left] == 0 {\n\t\t\t\tzeros--\n\t\t\t}\n\t\t\tleft++\n\t\t}\n\t\twidth := right - left + 1\n\t\tif width > best {\n\t\t\tbest = width\n\t\t}\n\t}\n\treturn best\n}',
    },
    tests: [
      { stdin: '1 1 0 1 1 0 1\n1', expectedStdout: '5', isSample: true },
      { stdin: '0 0 0\n0', expectedStdout: '0', isSample: true },
      { stdin: '0 0 0\n3', expectedStdout: '3' },
      { stdin: '1 1 1 1\n0', expectedStdout: '4' },
      { stdin: '1 0 1 1 0 1 1 0 1\n2', expectedStdout: '7' },
      { stdin: '0 1 0 1 0\n1', expectedStdout: '3' },
    ],
  }),

  p({
    ...base,
    slug: 'largest-sum-of-k-consecutive',
    title: 'Best Block of k',
    difficulty: 'MEDIUM',
    patternTags: ['sliding-window', 'fixed-window', 'running-sum'],
    signatureId: 'fn:ints,int->int',
    avgSolveSeconds: 600,
    promptMarkdown: [
      'The first line is a list of numbers. The second line is a number `k`.',
      '',
      'Return the largest sum you can get by adding up `k` neighbouring values.',
      'The `k` values must be contiguous — a block, not a selection.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 3 4 5',
      '        2',
      'output: 9',
      '```',
      '',
      'The blocks of two are `1+2`, `2+3`, `3+4`, `4+5`; the last is the biggest',
      'at `9`.',
      '',
      '```',
      'input:  -3 -1 -4 -2',
      '        2',
      'output: -4',
      '```',
      '',
      'Values may be negative, and when they all are the answer is negative too.',
      'There is no option to take fewer than `k` values, so `-4` — the block',
      '`-3 -1` — is the best available, not `0`.',
      '',
      'Guarantees: `k` is at least 1 and never larger than the length of the list,',
      'so at least one block always exists. When `k` equals the length there is',
      'exactly one block and the answer is the sum of everything.',
    ].join('\n'),
    editorialMarkdown: [
      '## A fixed-width window: add one, drop one',
      '',
      'There are `n - k + 1` blocks. Summing each from scratch costs O(k) apiece, so',
      'the obvious version is O(n*k) — and it is doing almost all of that work twice,',
      'because two neighbouring blocks share `k - 1` of their values.',
      '',
      'Sum the first block once. Then to move the window one step right, add the',
      'value entering on the right and subtract the value leaving on the left:',
      '',
      '```',
      'sum = a[0] + ... + a[k-1]',
      'best = sum',
      'for right in k..n-1:',
      '    sum += a[right] - a[right - k]',
      '    best = max(best, sum)',
      '```',
      '',
      '**Why the left edge never moves backwards.** Here the width is pinned, so the',
      'left edge is not a choice at all — it is `right - k + 1`, and it advances in',
      'lockstep. The monotonicity that matters is the same one: once a value has',
      'fallen off the left it is never needed again, because every remaining block',
      'starts at or after the current left. That is exactly what licenses the',
      'subtraction; you never have to reconstruct a window you have already left.',
      '',
      'The quiet mistake is forgetting to remove the outgoing element — writing',
      '`sum += a[right]` and no subtraction. That silently computes prefix sums',
      'instead of window sums, and on an all-positive list the largest prefix sum is',
      'the whole list, which is often close enough to the right answer to look',
      'plausible. On `1 2 3 4 5` with `k = 2` it reports `15` instead of `9`. The',
      'sibling mistake is subtracting the wrong index: `a[right - k]` is the value',
      'that just left, while `a[left]` after `left` has already been incremented is',
      'one that is still inside.',
      '',
      'Watch the seeding too. Starting `best` at `0` rather than at the first block',
      'sum breaks the all-negative case, reporting `0` for a problem whose answer is',
      'negative — and every test made of positive numbers passes anyway.',
      '',
      'O(n) time: one pass to build the first window and one pass to slide it, with',
      'O(1) work per step. O(1) space, since only the running sum crosses iterations.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  let sum = 0;\n  for (let i = 0; i < b; i++) sum += a[i];\n  let best = sum;\n  for (let right = b; right < a.length; right++) {\n    sum += a[right] - a[right - b];\n    if (sum > best) best = sum;\n  }\n  return best;\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number): number {\n  let sum = 0;\n  for (let i = 0; i < b; i++) sum += a[i] as number;\n  let best = sum;\n  for (let right = b; right < a.length; right++) {\n    sum += (a[right] as number) - (a[right - b] as number);\n    if (sum > best) best = sum;\n  }\n  return best;\n}',
      PYTHON:
        'def solve(a, b):\n    total = 0\n    for i in range(b):\n        total += a[i]\n    best = total\n    for right in range(b, len(a)):\n        total += a[right] - a[right - b]\n        if total > best:\n            best = total\n    return best',
      JAVA: '    static int solve(int[] a, int b) {\n        int sum = 0;\n        for (int i = 0; i < b; i++) sum += a[i];\n        int best = sum;\n        for (int right = b; right < a.length; right++) {\n            sum += a[right] - a[right - b];\n            if (sum > best) best = sum;\n        }\n        return best;\n    }',
      CPP: 'int solve(vector<int> a, int b) {\n    int sum = 0;\n    for (int i = 0; i < b; i++) sum += a[i];\n    int best = sum;\n    for (int right = b; right < (int) a.size(); right++) {\n        sum += a[right] - a[right - b];\n        if (sum > best) best = sum;\n    }\n    return best;\n}',
      GO: 'func solve(a []int, b int) int {\n\tsum := 0\n\tfor i := 0; i < b; i++ {\n\t\tsum += a[i]\n\t}\n\tbest := sum\n\tfor right := b; right < len(a); right++ {\n\t\tsum += a[right] - a[right-b]\n\t\tif sum > best {\n\t\t\tbest = sum\n\t\t}\n\t}\n\treturn best\n}',
    },
    tests: [
      { stdin: '1 2 3 4 5\n2', expectedStdout: '9', isSample: true },
      { stdin: '-3 -1 -4 -2\n2', expectedStdout: '-4', isSample: true },
      { stdin: '5 5 5 5\n4', expectedStdout: '20' },
      { stdin: '2 -1 3 -4 5 1\n3', expectedStdout: '4' },
      { stdin: '7\n1', expectedStdout: '7' },
      { stdin: '1 -2 3 -4 5\n1', expectedStdout: '5' },
    ],
  }),

  p({
    ...base,
    slug: 'shortest-run-reaching-target',
    title: 'Reach the Target in as Few as Possible',
    difficulty: 'MEDIUM',
    patternTags: ['sliding-window', 'shrinking-window', 'running-sum'],
    signatureId: 'fn:ints,int->int',
    avgSolveSeconds: 600,
    promptMarkdown: [
      'The first line is a list of positive numbers. The second line is a target.',
      '',
      'Return the length of the **shortest** contiguous run whose sum is at least',
      'the target. If no run reaches the target — not even the whole list — return',
      '`0`.',
      '',
      '**Example**',
      '',
      '```',
      'input:  2 3 1 2 4 3',
      '        7',
      'output: 2',
      '```',
      '',
      'The run `4 3` sums to `7` using two values. Every one-value run is smaller',
      'than `7`, so `2` is the shortest possible.',
      '',
      '```',
      'input:  1 1 1 1',
      '        11',
      'output: 0',
      '```',
      '',
      'The entire list only sums to `4`, so nothing reaches `11`. That is the',
      '"no valid window" case, and the answer for it is `0`.',
      '',
      'Guarantees: the list has at least one value, every value is strictly greater',
      'than zero, and the target is at least 1. Sums are compared with "at least",',
      'so a run that overshoots the target still counts.',
    ].join('\n'),
    editorialMarkdown: [
      '## Grow until valid, then shrink while still valid',
      '',
      'The brute force fixes a start, walks forward until the sum reaches the target,',
      'records the length, and repeats from the next start: O(n^2). It works, and it',
      're-adds the same numbers over and over.',
      '',
      'Instead run one window. Push `right` forward, adding to a running sum. The',
      'moment the sum reaches the target the window is valid, so record its width —',
      'then try to make it narrower by pulling `left` forward while it *stays* valid:',
      '',
      '```',
      'left = 0, sum = 0, best = none',
      'for right in 0..n-1:',
      '    sum += a[right]',
      '    while sum >= target:',
      '        best = min(best, right - left + 1)',
      '        sum -= a[left]',
      '        left += 1',
      'return best or 0',
      '```',
      '',
      '**Why the left edge never moves backwards.** All values are positive, so the',
      'running sum of a window is strictly decreasing in `left` and strictly',
      'increasing in `right`. Fix a `right`: there is a threshold `L(right)` such that',
      'a window is valid exactly when `left <= L(right)`, and because extending',
      '`right` only adds a positive number, `L` never decreases. So a start position',
      'discarded at one step could only ever produce longer windows later — never a',
      'shorter one, and shorter is all we are after. The left edge is safe to advance',
      'and never revisit. Both pointers cross the list once, giving O(n).',
      '',
      'This is also the place to notice that positivity is a load-bearing guarantee,',
      'not decoration. With a negative value in the list, shrinking could *raise* the',
      'sum, `L` would no longer be monotone, and the whole argument collapses — that',
      'problem needs prefix sums and a monotonic deque instead.',
      '',
      'The quiet mistake is shrinking with `if` instead of `while`. A single step',
      'right can push the sum far past the target, and one removal may leave it still',
      'above — the window is then valid but not minimal, and the recorded answer is',
      'too large. On `2 3 1 2 4 3` with target `7` the `if` version happens to reach',
      '`2` anyway; on `1 4 4` with target `8` it reports `3` instead of `2`. The',
      'related mistake is updating `best` after the shrink loop rather than inside it,',
      'which measures a window that has already been broken.',
      '',
      'Finally, the not-found case: seed `best` with something larger than `n` and',
      'translate it to `0` at the end. Seeding it with `0` makes every answer `0`.',
      '',
      'O(n) time, bounded by the two forward-only pointers, and O(1) space.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  const n = a.length;\n  let left = 0;\n  let sum = 0;\n  let best = n + 1;\n  for (let right = 0; right < n; right++) {\n    sum += a[right];\n    while (sum >= b) {\n      const width = right - left + 1;\n      if (width < best) best = width;\n      sum -= a[left];\n      left++;\n    }\n  }\n  return best === n + 1 ? 0 : best;\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number): number {\n  const n = a.length;\n  let left = 0;\n  let sum = 0;\n  let best = n + 1;\n  for (let right = 0; right < n; right++) {\n    sum += a[right] as number;\n    while (sum >= b) {\n      const width = right - left + 1;\n      if (width < best) best = width;\n      sum -= a[left] as number;\n      left++;\n    }\n  }\n  return best === n + 1 ? 0 : best;\n}',
      PYTHON:
        'def solve(a, b):\n    n = len(a)\n    left = 0\n    total = 0\n    best = n + 1\n    for right in range(n):\n        total += a[right]\n        while total >= b:\n            width = right - left + 1\n            if width < best:\n                best = width\n            total -= a[left]\n            left += 1\n    return 0 if best == n + 1 else best',
      JAVA: '    static int solve(int[] a, int b) {\n        int n = a.length;\n        int left = 0;\n        int sum = 0;\n        int best = n + 1;\n        for (int right = 0; right < n; right++) {\n            sum += a[right];\n            while (sum >= b) {\n                int width = right - left + 1;\n                if (width < best) best = width;\n                sum -= a[left];\n                left++;\n            }\n        }\n        return best == n + 1 ? 0 : best;\n    }',
      CPP: 'int solve(vector<int> a, int b) {\n    int n = (int) a.size();\n    int left = 0;\n    int sum = 0;\n    int best = n + 1;\n    for (int right = 0; right < n; right++) {\n        sum += a[right];\n        while (sum >= b) {\n            int width = right - left + 1;\n            if (width < best) best = width;\n            sum -= a[left];\n            left++;\n        }\n    }\n    return best == n + 1 ? 0 : best;\n}',
      GO: 'func solve(a []int, b int) int {\n\tn := len(a)\n\tleft := 0\n\tsum := 0\n\tbest := n + 1\n\tfor right := 0; right < n; right++ {\n\t\tsum += a[right]\n\t\tfor sum >= b {\n\t\t\twidth := right - left + 1\n\t\t\tif width < best {\n\t\t\t\tbest = width\n\t\t\t}\n\t\t\tsum -= a[left]\n\t\t\tleft++\n\t\t}\n\t}\n\tif best == n+1 {\n\t\treturn 0\n\t}\n\treturn best\n}',
    },
    tests: [
      { stdin: '2 3 1 2 4 3\n7', expectedStdout: '2', isSample: true },
      { stdin: '1 1 1 1\n11', expectedStdout: '0', isSample: true },
      { stdin: '5\n5', expectedStdout: '1' },
      { stdin: '1 2 3 4 5\n15', expectedStdout: '5' },
      { stdin: '1 4 4\n8', expectedStdout: '2' },
      { stdin: '2 1 6 5 4\n9', expectedStdout: '2' },
    ],
  }),

  p({
    ...base,
    slug: 'longest-run-with-two-distinct',
    title: 'At Most Two Different Letters',
    difficulty: 'MEDIUM',
    patternTags: ['sliding-window', 'hash-map', 'distinct-count'],
    signatureId: 'fn:string->int',
    avgSolveSeconds: 600,
    promptMarkdown: [
      'Return the length of the longest contiguous stretch of a string that uses at',
      'most two different characters.',
      '',
      'The stretch must be a run of neighbouring characters. Return only its length.',
      '',
      '**Example**',
      '',
      '```',
      'input:  eceba',
      'output: 3',
      '```',
      '',
      'The stretch `ece` uses only `e` and `c`. Extending it to `eceb` would bring',
      'in a third character.',
      '',
      '```',
      'input:  aaaa',
      'output: 4',
      '```',
      '',
      'When every character is the same the whole string qualifies — one distinct',
      'character is still "at most two".',
      '',
      'Guarantees: the string has at least one character, so the answer is never',
      '`0`. A one-character string gives `1`. Characters are compared exactly, so',
      'upper and lower case count as different.',
    ].join('\n'),
    editorialMarkdown: [
      '## A window carrying a count of what is inside it',
      '',
      'Checking every substring for its distinct count is O(n^2) substrings times the',
      'cost of counting, and most of that counting repeats work from the substring',
      'one character shorter.',
      '',
      'One window does it in a single pass. Keep a map from character to how many',
      'times it occurs inside the window; the map\'s size is the distinct count. Push',
      '`right` forward, incrementing. While the map holds three or more keys, pull',
      '`left` forward, decrementing — and **delete the key when its count hits zero**,',
      'because a key sitting at zero still counts toward the map size.',
      '',
      '```',
      'left = 0, counts = {}, best = 0',
      'for right in 0..n-1:',
      '    counts[s[right]] += 1',
      '    while len(counts) > 2:',
      '        counts[s[left]] -= 1',
      '        if counts[s[left]] == 0: delete counts[s[left]]',
      '        left += 1',
      '    best = max(best, right - left + 1)',
      '```',
      '',
      '**Why the left edge never moves backwards.** Adding a character can only keep',
      'the distinct count the same or raise it; it can never lower it. So if starting',
      'at index `i` already gives three distinct characters for some `right`, it gives',
      'at least three for every larger `right` too — that start is permanently dead.',
      'The smallest legal start is a non-decreasing function of `right`, so `left`',
      'sweeps forward once. Each index is added once and removed at most once: O(n)',
      'total, not O(n^2), even though the code has a loop inside a loop.',
      '',
      'The quiet mistake is exactly that deletion: decrementing the count but leaving',
      'the key in the map. The map then reports characters that have already slid out',
      'of the window, `len(counts)` never comes back down, and after the first shrink',
      'the window can never grow again. It is quiet because on a string like `aaaa`,',
      'where nothing is ever removed, the answer is still right — and on `eceba` it',
      'is still right, because the first shrink happens late. Test it on something',
      'like `abaccc`, where the window has to abandon `b` and then expand over the',
      'run of `c`s; the buggy version reports `3` instead of `4`.',
      '',
      'The second quiet mistake is reading the width before the shrink loop, which',
      'measures a window that currently holds three distinct characters.',
      '',
      'O(n) time — both pointers only move right — and O(1) space in practice, since',
      'the map never holds more than three entries.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const counts = new Map();\n  let left = 0;\n  let best = 0;\n  for (let right = 0; right < a.length; right++) {\n    const ch = a[right];\n    counts.set(ch, (counts.get(ch) || 0) + 1);\n    while (counts.size > 2) {\n      const out = a[left];\n      const next = counts.get(out) - 1;\n      if (next === 0) counts.delete(out);\n      else counts.set(out, next);\n      left++;\n    }\n    const width = right - left + 1;\n    if (width > best) best = width;\n  }\n  return best;\n}',
      TYPESCRIPT:
        'function solve(a: string): number {\n  const counts = new Map<string, number>();\n  let left = 0;\n  let best = 0;\n  for (let right = 0; right < a.length; right++) {\n    const ch = a[right] as string;\n    counts.set(ch, (counts.get(ch) ?? 0) + 1);\n    while (counts.size > 2) {\n      const out = a[left] as string;\n      const next = (counts.get(out) ?? 0) - 1;\n      if (next === 0) counts.delete(out);\n      else counts.set(out, next);\n      left++;\n    }\n    const width = right - left + 1;\n    if (width > best) best = width;\n  }\n  return best;\n}',
      PYTHON:
        'def solve(a):\n    counts = {}\n    left = 0\n    best = 0\n    for right in range(len(a)):\n        ch = a[right]\n        counts[ch] = counts.get(ch, 0) + 1\n        while len(counts) > 2:\n            out = a[left]\n            counts[out] -= 1\n            if counts[out] == 0:\n                del counts[out]\n            left += 1\n        width = right - left + 1\n        if width > best:\n            best = width\n    return best',
      JAVA: '    static int solve(String a) {\n        Map<Character, Integer> counts = new HashMap<>();\n        int left = 0;\n        int best = 0;\n        for (int right = 0; right < a.length(); right++) {\n            char ch = a.charAt(right);\n            counts.put(ch, counts.getOrDefault(ch, 0) + 1);\n            while (counts.size() > 2) {\n                char out = a.charAt(left);\n                int next = counts.get(out) - 1;\n                if (next == 0) counts.remove(out);\n                else counts.put(out, next);\n                left++;\n            }\n            int width = right - left + 1;\n            if (width > best) best = width;\n        }\n        return best;\n    }',
      CPP: 'int solve(string a) {\n    unordered_map<char, int> counts;\n    int left = 0;\n    int best = 0;\n    for (int right = 0; right < (int) a.size(); right++) {\n        counts[a[right]]++;\n        while ((int) counts.size() > 2) {\n            char out = a[left];\n            counts[out]--;\n            if (counts[out] == 0) counts.erase(out);\n            left++;\n        }\n        int width = right - left + 1;\n        if (width > best) best = width;\n    }\n    return best;\n}',
      GO: 'func solve(a string) int {\n\tcounts := map[byte]int{}\n\tleft := 0\n\tbest := 0\n\tfor right := 0; right < len(a); right++ {\n\t\tcounts[a[right]]++\n\t\tfor len(counts) > 2 {\n\t\t\tout := a[left]\n\t\t\tcounts[out]--\n\t\t\tif counts[out] == 0 {\n\t\t\t\tdelete(counts, out)\n\t\t\t}\n\t\t\tleft++\n\t\t}\n\t\twidth := right - left + 1\n\t\tif width > best {\n\t\t\tbest = width\n\t\t}\n\t}\n\treturn best\n}',
    },
    tests: [
      { stdin: 'eceba', expectedStdout: '3', isSample: true },
      { stdin: 'aaaa', expectedStdout: '4', isSample: true },
      { stdin: 'abcdef', expectedStdout: '2' },
      { stdin: 'ab', expectedStdout: '2' },
      { stdin: 'a', expectedStdout: '1' },
      { stdin: 'abaccc', expectedStdout: '4' },
    ],
  }),

  p({
    ...base,
    slug: 'count-runs-with-exactly-k-distinct',
    title: 'Exactly k Different Letters',
    difficulty: 'HARD',
    patternTags: ['sliding-window', 'hash-map', 'counting', 'at-most-trick'],
    signatureId: 'fn:string,int->int',
    avgSolveSeconds: 900,
    promptMarkdown: [
      'The first line is a string. The second line is a number `k`.',
      '',
      'Count the contiguous stretches of the string that use **exactly** `k`',
      'different characters. Two stretches at different positions count separately',
      'even if they read the same.',
      '',
      '**Example**',
      '',
      '```',
      'input:  pqpr',
      '        2',
      'output: 4',
      '```',
      '',
      'The four are `pq`, `qp`, `pqp` and `pr`. `qpr` and `pqpr` use three',
      'different characters, and every single character uses one.',
      '',
      '```',
      'input:  aaaa',
      '        1',
      'output: 10',
      '```',
      '',
      'Every one of the 10 stretches of `aaaa` uses exactly one character.',
      '',
      'Guarantees: the string has at least one character and `k` is zero or more.',
      'When `k` is `0` the answer is `0`, because a stretch has at least one',
      'character and so at least one distinct character. When `k` is larger than the',
      'number of distinct characters in the whole string the answer is also `0`.',
    ].join('\n'),
    editorialMarkdown: [
      '## Exactly k = atMost(k) - atMost(k-1)',
      '',
      'A window naturally answers "at most `k` distinct", because "at most" is a',
      'condition that only gets harder as the window grows — which is precisely what',
      'lets the left edge move forward and never back. "Exactly `k`" is not like',
      'that: a window can stop being exactly-`k` by growing *or* by shrinking, so',
      'there is no single legal region for one window to track.',
      '',
      'So do not track it. Write a helper `atMost(k)` that counts every stretch using',
      '`k` or fewer distinct characters, then answer:',
      '',
      '```',
      'exactly(k) = atMost(k) - atMost(k - 1)',
      '```',
      '',
      '**Why the subtraction is valid.** Every stretch has some distinct count `d`,',
      'and each stretch is counted by `atMost(m)` if and only if `d <= m`. Sort the',
      'stretches into buckets by `d`. Then `atMost(k)` is the total of buckets',
      '`1..k`, and `atMost(k-1)` is the total of buckets `1..k-1`. Subtracting leaves',
      'bucket `k` alone — exactly the stretches with `d = k`. Nothing is double',
      'counted and nothing is missed, because the buckets partition the stretches:',
      'each stretch has one distinct count, not a range of them. This works for any',
      'monotone predicate, which is the reusable idea here — `atMost` is monotone in',
      '`k`, and a difference of two monotone counts isolates one level.',
      '',
      'The `atMost` helper itself is the ordinary window:',
      '',
      '```',
      'atMost(m):',
      '    if m < 0: return 0',
      '    left = 0, counts = {}, total = 0',
      '    for right in 0..n-1:',
      '        counts[s[right]] += 1',
      '        while len(counts) > m:',
      '            counts[s[left]] -= 1',
      '            if counts[s[left]] == 0: delete counts[s[left]]',
      '            left += 1',
      '        total += right - left + 1',
      '    return total',
      '```',
      '',
      '**Why the left edge never moves backwards, and why `right - left + 1` counts',
      'correctly.** Adding a character can only raise the distinct count, so a start',
      'that is already illegal for some `right` stays illegal for every later',
      '`right`; the smallest legal start only advances. And once the window',
      '`[left, right]` is legal, so is every stretch ending at `right` that starts at',
      '`left` or later — a suffix of a legal window uses a subset of its characters.',
      'That is `right - left + 1` stretches, all new because they all end at `right`.',
      'Adding that per step counts every legal stretch exactly once.',
      '',
      'The quiet mistake is forgetting to remove the outgoing character from the',
      'count map — decrementing but never deleting the zero entry. The map size then',
      'only ever grows, the window never reopens after its first shrink, and the',
      'totals come out low. On `aaaa` with `k = 1` nothing is ever removed, so the',
      'answer is still `10` and the bug looks fine. The `m < 0` guard matters too:',
      'without it, `k = 0` calls `atMost(-1)`, whose `while` loop runs off the end of',
      'the string.',
      '',
      'Two passes of an O(n) window, so O(n) time; space is O(k) for the count map,',
      'bounded by the alphabet.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  const atMost = (m) => {\n    if (m < 0) return 0;\n    const counts = new Map();\n    let left = 0;\n    let total = 0;\n    for (let right = 0; right < a.length; right++) {\n      const ch = a[right];\n      counts.set(ch, (counts.get(ch) || 0) + 1);\n      while (counts.size > m) {\n        const out = a[left];\n        const next = counts.get(out) - 1;\n        if (next === 0) counts.delete(out);\n        else counts.set(out, next);\n        left++;\n      }\n      total += right - left + 1;\n    }\n    return total;\n  };\n  return atMost(b) - atMost(b - 1);\n}',
      TYPESCRIPT:
        'function solve(a: string, b: number): number {\n  const atMost = (m: number): number => {\n    if (m < 0) return 0;\n    const counts = new Map<string, number>();\n    let left = 0;\n    let total = 0;\n    for (let right = 0; right < a.length; right++) {\n      const ch = a[right] as string;\n      counts.set(ch, (counts.get(ch) ?? 0) + 1);\n      while (counts.size > m) {\n        const out = a[left] as string;\n        const next = (counts.get(out) ?? 0) - 1;\n        if (next === 0) counts.delete(out);\n        else counts.set(out, next);\n        left++;\n      }\n      total += right - left + 1;\n    }\n    return total;\n  };\n  return atMost(b) - atMost(b - 1);\n}',
      PYTHON:
        'def solve(a, b):\n    def at_most(m):\n        if m < 0:\n            return 0\n        counts = {}\n        left = 0\n        total = 0\n        for right in range(len(a)):\n            ch = a[right]\n            counts[ch] = counts.get(ch, 0) + 1\n            while len(counts) > m:\n                out = a[left]\n                counts[out] -= 1\n                if counts[out] == 0:\n                    del counts[out]\n                left += 1\n            total += right - left + 1\n        return total\n\n    return at_most(b) - at_most(b - 1)',
      JAVA: '    static int atMostDistinct(String a, int m) {\n        if (m < 0) return 0;\n        Map<Character, Integer> counts = new HashMap<>();\n        int left = 0;\n        int total = 0;\n        for (int right = 0; right < a.length(); right++) {\n            char ch = a.charAt(right);\n            counts.put(ch, counts.getOrDefault(ch, 0) + 1);\n            while (counts.size() > m) {\n                char out = a.charAt(left);\n                int next = counts.get(out) - 1;\n                if (next == 0) counts.remove(out);\n                else counts.put(out, next);\n                left++;\n            }\n            total += right - left + 1;\n        }\n        return total;\n    }\n\n    static int solve(String a, int b) {\n        return atMostDistinct(a, b) - atMostDistinct(a, b - 1);\n    }',
      CPP: 'int atMostDistinct(const string &a, int m) {\n    if (m < 0) return 0;\n    unordered_map<char, int> counts;\n    int left = 0;\n    int total = 0;\n    for (int right = 0; right < (int) a.size(); right++) {\n        counts[a[right]]++;\n        while ((int) counts.size() > m) {\n            char out = a[left];\n            counts[out]--;\n            if (counts[out] == 0) counts.erase(out);\n            left++;\n        }\n        total += right - left + 1;\n    }\n    return total;\n}\n\nint solve(string a, int b) {\n    return atMostDistinct(a, b) - atMostDistinct(a, b - 1);\n}',
      GO: 'func atMostDistinct(a string, m int) int {\n\tif m < 0 {\n\t\treturn 0\n\t}\n\tcounts := map[byte]int{}\n\tleft := 0\n\ttotal := 0\n\tfor right := 0; right < len(a); right++ {\n\t\tcounts[a[right]]++\n\t\tfor len(counts) > m {\n\t\t\tout := a[left]\n\t\t\tcounts[out]--\n\t\t\tif counts[out] == 0 {\n\t\t\t\tdelete(counts, out)\n\t\t\t}\n\t\t\tleft++\n\t\t}\n\t\ttotal += right - left + 1\n\t}\n\treturn total\n}\n\nfunc solve(a string, b int) int {\n\treturn atMostDistinct(a, b) - atMostDistinct(a, b-1)\n}',
    },
    tests: [
      { stdin: 'pqpr\n2', expectedStdout: '4', isSample: true },
      { stdin: 'aaaa\n1', expectedStdout: '10', isSample: true },
      { stdin: 'aaaa\n0', expectedStdout: '0' },
      { stdin: 'abc\n3', expectedStdout: '1' },
      { stdin: 'abc\n4', expectedStdout: '0' },
      { stdin: 'aabab\n2', expectedStdout: '9' },
    ],
  }),

  p({
    ...base,
    slug: 'smallest-window-containing-all',
    title: 'Smallest Stretch That Covers Everything',
    difficulty: 'HARD',
    patternTags: ['sliding-window', 'hash-map', 'shrinking-window'],
    signatureId: 'fn:string,string->string',
    avgSolveSeconds: 900,
    promptMarkdown: [
      'The first line is a text. The second line is a set of required characters,',
      'given as a string.',
      '',
      'Return the shortest contiguous stretch of the text that contains every',
      'required character, **counting duplicates**: if the second line has three',
      '`a`s, the stretch must contain at least three `a`s. The stretch may contain',
      'extra characters as well.',
      '',
      'If no stretch works, return an empty line.',
      '',
      '**Example**',
      '',
      '```',
      'input:  ADOBECODEBANC',
      '        ABC',
      'output: BANC',
      '```',
      '',
      '`BANC` is four characters long and holds an `A`, a `B` and a `C`. `ADOBEC`',
      'also qualifies but is six long.',
      '',
      '```',
      'input:  a',
      '        aa',
      'output:',
      '```',
      '',
      'Two `a`s are required and the text has only one, so no stretch works and the',
      'output is an empty line. That is the "no valid window" case.',
      '',
      'Guarantees: both lines have at least one character, and characters are',
      'compared exactly, so upper and lower case differ. If two stretches tie for',
      'shortest, return the one that starts earlier.',
    ].join('\n'),
    editorialMarkdown: [
      '## Grow to cover, then shrink while still covered',
      '',
      'Checking every stretch is O(n^2) stretches, each needing its own tally: far',
      'too slow, and it recounts characters it has already counted.',
      '',
      'Build a `need` map from the requirement string: character to how many copies',
      'are required. Keep a `have` map for the current window, and — this is the part',
      'that makes it O(n) — a single integer `matched` counting how many *characters*',
      'are fully satisfied, so checking coverage is one comparison instead of walking',
      'the map.',
      '',
      '```',
      'left = 0, matched = 0, best = none',
      'for right in 0..n-1:',
      '    ch = text[right]',
      '    have[ch] += 1',
      '    if ch in need and have[ch] == need[ch]: matched += 1',
      '    while matched == len(need):',
      '        if right - left + 1 < best width: record (left, right)',
      '        out = text[left]',
      '        have[out] -= 1',
      '        if out in need and have[out] < need[out]: matched -= 1',
      '        left += 1',
      'return recorded stretch, or empty',
      '```',
      '',
      'Note `have[ch] == need[ch]` rather than `>=`: the equality fires once, on the',
      'step where that character crosses from unsatisfied to satisfied, so `matched`',
      'is incremented exactly once per character. Using `>=` counts every extra copy',
      'and `matched` runs away past `len(need)`, which is the first thing to check if',
      'the answer comes out too long.',
      '',
      '**Why the left edge never moves backwards.** Suppose the window ending at',
      '`right` is minimal — it covers the requirement and dropping `text[left]` would',
      'break it. Now extend to `right + 1`. Coverage is a monotone property in the',
      'window: a wider window contains everything a narrower one does, so any start',
      'that failed to cover for `right` also fails for `right + 1`. The earliest',
      'usable start never retreats. And we do not lose the answer by advancing:',
      'anything shorter that starts before the current `left` would have to end at or',
      'before `right`, and we already examined every covering window ending at each',
      'earlier position when we were standing there. So each index is added once and',
      'removed once — O(n) rather than O(n^2).',
      '',
      'The quiet mistake is shrinking with `if` instead of `while`. When the window',
      'is padded with characters that are not required at all, one removal leaves it',
      'still covering, and the recorded stretch is longer than the true answer. On',
      '`ADOBECODEBANC`/`ABC` the `if` version still lands on `BANC`, which is why it',
      'survives a casual test; on `bba`/`ab` it reports `bba` instead of `ba`. The',
      'second mistake is recording the answer after the shrink loop rather than',
      'inside it — by then the window has just been broken, and the recorded width is',
      'one short of any real stretch.',
      '',
      'Finally, name the empty case in code and not only in the statement: seed the',
      'best width with something larger than the text, and return the empty string if',
      'it was never beaten. Returning `text[0:0]` by accident and returning the empty',
      'string on purpose look identical in the output but not in the reasoning.',
      '',
      'O(n + m) time, bounded by the two forward-only pointers over the text plus one',
      'pass to build `need`. O(k) space for the two maps, where k is the number of',
      'distinct required characters.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  const need = new Map();\n  for (const ch of b) need.set(ch, (need.get(ch) || 0) + 1);\n  const have = new Map();\n  let matched = 0;\n  let left = 0;\n  let bestLen = a.length + 1;\n  let bestStart = 0;\n  for (let right = 0; right < a.length; right++) {\n    const ch = a[right];\n    have.set(ch, (have.get(ch) || 0) + 1);\n    if (need.has(ch) && have.get(ch) === need.get(ch)) matched++;\n    while (matched === need.size) {\n      const width = right - left + 1;\n      if (width < bestLen) {\n        bestLen = width;\n        bestStart = left;\n      }\n      const out = a[left];\n      have.set(out, have.get(out) - 1);\n      if (need.has(out) && have.get(out) < need.get(out)) matched--;\n      left++;\n    }\n  }\n  return bestLen === a.length + 1 ? \'\' : a.slice(bestStart, bestStart + bestLen);\n}',
      TYPESCRIPT:
        'function solve(a: string, b: string): string {\n  const need = new Map<string, number>();\n  for (const ch of b) need.set(ch, (need.get(ch) ?? 0) + 1);\n  const have = new Map<string, number>();\n  let matched = 0;\n  let left = 0;\n  let bestLen = a.length + 1;\n  let bestStart = 0;\n  for (let right = 0; right < a.length; right++) {\n    const ch = a[right] as string;\n    have.set(ch, (have.get(ch) ?? 0) + 1);\n    if (need.has(ch) && have.get(ch) === need.get(ch)) matched++;\n    while (matched === need.size) {\n      const width = right - left + 1;\n      if (width < bestLen) {\n        bestLen = width;\n        bestStart = left;\n      }\n      const out = a[left] as string;\n      have.set(out, (have.get(out) ?? 0) - 1);\n      if (need.has(out) && (have.get(out) ?? 0) < (need.get(out) ?? 0)) matched--;\n      left++;\n    }\n  }\n  return bestLen === a.length + 1 ? \'\' : a.slice(bestStart, bestStart + bestLen);\n}',
      PYTHON:
        'def solve(a, b):\n    need = {}\n    for ch in b:\n        need[ch] = need.get(ch, 0) + 1\n    have = {}\n    matched = 0\n    left = 0\n    best_len = len(a) + 1\n    best_start = 0\n    for right in range(len(a)):\n        ch = a[right]\n        have[ch] = have.get(ch, 0) + 1\n        if ch in need and have[ch] == need[ch]:\n            matched += 1\n        while matched == len(need):\n            width = right - left + 1\n            if width < best_len:\n                best_len = width\n                best_start = left\n            out = a[left]\n            have[out] -= 1\n            if out in need and have[out] < need[out]:\n                matched -= 1\n            left += 1\n    if best_len == len(a) + 1:\n        return \'\'\n    return a[best_start:best_start + best_len]',
      JAVA: '    static String solve(String a, String b) {\n        Map<Character, Integer> need = new HashMap<>();\n        for (int i = 0; i < b.length(); i++) {\n            char ch = b.charAt(i);\n            need.put(ch, need.getOrDefault(ch, 0) + 1);\n        }\n        Map<Character, Integer> have = new HashMap<>();\n        int matched = 0;\n        int left = 0;\n        int bestLen = a.length() + 1;\n        int bestStart = 0;\n        for (int right = 0; right < a.length(); right++) {\n            char ch = a.charAt(right);\n            have.put(ch, have.getOrDefault(ch, 0) + 1);\n            if (need.containsKey(ch) && have.get(ch).equals(need.get(ch))) matched++;\n            while (matched == need.size()) {\n                int width = right - left + 1;\n                if (width < bestLen) {\n                    bestLen = width;\n                    bestStart = left;\n                }\n                char out = a.charAt(left);\n                have.put(out, have.get(out) - 1);\n                if (need.containsKey(out) && have.get(out) < need.get(out)) matched--;\n                left++;\n            }\n        }\n        if (bestLen == a.length() + 1) return "";\n        return a.substring(bestStart, bestStart + bestLen);\n    }',
      CPP: 'string solve(string a, string b) {\n    unordered_map<char, int> need;\n    for (size_t i = 0; i < b.size(); i++) need[b[i]]++;\n    unordered_map<char, int> have;\n    int matched = 0;\n    int left = 0;\n    int bestLen = (int) a.size() + 1;\n    int bestStart = 0;\n    for (int right = 0; right < (int) a.size(); right++) {\n        char ch = a[right];\n        have[ch]++;\n        if (need.count(ch) && have[ch] == need[ch]) matched++;\n        while (matched == (int) need.size()) {\n            int width = right - left + 1;\n            if (width < bestLen) {\n                bestLen = width;\n                bestStart = left;\n            }\n            char out = a[left];\n            have[out]--;\n            if (need.count(out) && have[out] < need[out]) matched--;\n            left++;\n        }\n    }\n    if (bestLen == (int) a.size() + 1) return string("");\n    return a.substr(bestStart, bestLen);\n}',
      GO: 'func solve(a string, b string) string {\n\tneed := map[byte]int{}\n\tfor i := 0; i < len(b); i++ {\n\t\tneed[b[i]]++\n\t}\n\thave := map[byte]int{}\n\tmatched := 0\n\tleft := 0\n\tbestLen := len(a) + 1\n\tbestStart := 0\n\tfor right := 0; right < len(a); right++ {\n\t\tch := a[right]\n\t\thave[ch]++\n\t\tif want, ok := need[ch]; ok && have[ch] == want {\n\t\t\tmatched++\n\t\t}\n\t\tfor matched == len(need) {\n\t\t\twidth := right - left + 1\n\t\t\tif width < bestLen {\n\t\t\t\tbestLen = width\n\t\t\t\tbestStart = left\n\t\t\t}\n\t\t\tout := a[left]\n\t\t\thave[out]--\n\t\t\tif want, ok := need[out]; ok && have[out] < want {\n\t\t\t\tmatched--\n\t\t\t}\n\t\t\tleft++\n\t\t}\n\t}\n\tif bestLen == len(a)+1 {\n\t\treturn ""\n\t}\n\treturn a[bestStart : bestStart+bestLen]\n}',
    },
    tests: [
      { stdin: 'ADOBECODEBANC\nABC', expectedStdout: 'BANC', isSample: true },
      { stdin: 'a\naa', expectedStdout: '', isSample: true },
      { stdin: 'a\na', expectedStdout: 'a' },
      { stdin: 'ab\nb', expectedStdout: 'b' },
      { stdin: 'aabbccab\nabc', expectedStdout: 'cab' },
      { stdin: 'bba\nab', expectedStdout: 'ba' },
    ],
  }),

  p({
    ...base,
    slug: 'max-of-each-window',
    title: 'Biggest in Every Window',
    difficulty: 'HARD',
    patternTags: ['sliding-window', 'monotonic-deque', 'fixed-window'],
    signatureId: 'fn:ints,int->ints',
    avgSolveSeconds: 900,
    promptMarkdown: [
      'The first line is a list of numbers. The second line is a window width `k`.',
      '',
      'Slide a window of width `k` across the list from left to right, one step at a',
      'time, and report the largest value inside it at each position. Return those',
      'maxima in order, as a list.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 3 -1 -3 5 3 6 7',
      '        3',
      'output: 3 3 5 5 6 7',
      '```',
      '',
      'The windows are `1 3 -1`, `3 -1 -3`, `-1 -3 5`, `-3 5 3`, `5 3 6`, `3 6 7`,',
      'and their maxima are `3 3 5 5 6 7`. A list of length `n` produces exactly',
      '`n - k + 1` answers.',
      '',
      '```',
      'input:  4 4 4 4',
      '        4',
      'output: 4',
      '```',
      '',
      'When `k` equals the length there is a single window, so the output is one',
      'number. All values being equal changes nothing.',
      '',
      'Guarantees: `k` is at least 1 and never larger than the length of the list,',
      'so the output is never empty. Values may be negative. With `k` of `1` the',
      'output is the list itself.',
    ].join('\n'),
    editorialMarkdown: [
      '## A monotonic deque: the window remembers only its possible futures',
      '',
      'Recomputing the maximum for each of the `n - k + 1` windows costs O(k) each,',
      'so O(n*k). A heap gets it to O(n log k) but has to cope with stale entries.',
      'The O(n) answer is a deque of **indices** whose values are strictly',
      'decreasing from front to back.',
      '',
      '```',
      'deque = []            # indices, values decreasing front -> back',
      'out = []',
      'for right in 0..n-1:',
      '    while deque and a[deque.back] <= a[right]: deque.pop_back()',
      '    deque.push_back(right)',
      '    if deque.front <= right - k: deque.pop_front()   # slid out of the window',
      '    if right >= k - 1: out.append(a[deque.front])',
      '```',
      '',
      '**Why a smaller element can be discarded forever.** Take two positions `i < j`',
      'with `a[i] <= a[j]`. Any window that still contains `i` must start at or before',
      '`i` and end at or after the current right edge, so it contains `j` as well —',
      '`j` is between them. In every such window `a[j]` is at least as large as',
      '`a[i]`, so `a[i]` can never be the unique maximum, and reporting `a[j]` is',
      'always at least as correct. Once a bigger-or-equal value arrives to its right,',
      '`i` has no possible future in which it matters. That is why the pop is',
      'permanent and not a temporary reordering, and it is what keeps the total work',
      'linear: every index is pushed once and popped at most once.',
      '',
      '**Why the left edge never moves backwards.** The window width is fixed, so the',
      'left edge is `right - k + 1` and advances in lockstep. The deque front is the',
      'answer for the current window precisely because everything ahead of it was',
      'either dominated (popped from the back) or has slid out of range (popped from',
      'the front). Both kinds of removal are forward-only, which is what turns the',
      'nested `while` into amortised O(1) per step.',
      '',
      'The quiet mistake is forgetting to remove the outgoing element — dropping the',
      'front-expiry check because the back-popping already keeps the deque short.',
      'The front index then refers to a value that left the window steps ago, and the',
      'output holds a maximum that is no longer inside. It is quiet because it only',
      'shows on inputs where the running maximum sits near the left edge and expires:',
      'on `1 2 3 4 5` with `k = 2` the deque never holds a stale front and the answer',
      'is right; on `9 8 7 6` with `k = 2` the buggy version reports `9 9 9` instead',
      'of `9 8 7`.',
      '',
      'The second is popping with `<` instead of `<=` when the entering value ties.',
      'Keeping equal values is still correct — they expire in order — but only if the',
      'front-expiry check is by index, not by value. Comparing values to decide what',
      'has left the window is how ties turn into wrong answers.',
      '',
      'O(n) time, amortised, bounded by each index entering and leaving the deque once.',
      'O(k) space, since the deque never holds more than one window\'s worth of indices.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  const n = a.length;\n  const deque = [];\n  let head = 0;\n  const out = [];\n  for (let right = 0; right < n; right++) {\n    while (deque.length > head && a[deque[deque.length - 1]] <= a[right]) deque.pop();\n    deque.push(right);\n    if (deque[head] <= right - b) head++;\n    if (right >= b - 1) out.push(a[deque[head]]);\n  }\n  return out;\n}',
      TYPESCRIPT:
        'function solve(a: number[], b: number): number[] {\n  const n = a.length;\n  const deque: number[] = [];\n  let head = 0;\n  const out: number[] = [];\n  for (let right = 0; right < n; right++) {\n    while (deque.length > head && (a[deque[deque.length - 1] as number] as number) <= (a[right] as number)) {\n      deque.pop();\n    }\n    deque.push(right);\n    if ((deque[head] as number) <= right - b) head++;\n    if (right >= b - 1) out.push(a[deque[head] as number] as number);\n  }\n  return out;\n}',
      PYTHON:
        'def solve(a, b):\n    n = len(a)\n    deque = []\n    head = 0\n    out = []\n    for right in range(n):\n        while len(deque) > head and a[deque[-1]] <= a[right]:\n            deque.pop()\n        deque.append(right)\n        if deque[head] <= right - b:\n            head += 1\n        if right >= b - 1:\n            out.append(a[deque[head]])\n    return out',
      JAVA: '    static int[] solve(int[] a, int b) {\n        int n = a.length;\n        int[] deque = new int[n];\n        int head = 0;\n        int tail = 0;\n        int[] out = new int[n - b + 1];\n        int written = 0;\n        for (int right = 0; right < n; right++) {\n            while (tail > head && a[deque[tail - 1]] <= a[right]) tail--;\n            deque[tail++] = right;\n            if (deque[head] <= right - b) head++;\n            if (right >= b - 1) out[written++] = a[deque[head]];\n        }\n        return out;\n    }',
      CPP: 'vector<int> solve(vector<int> a, int b) {\n    int n = (int) a.size();\n    vector<int> dq;\n    int head = 0;\n    vector<int> out;\n    for (int right = 0; right < n; right++) {\n        while ((int) dq.size() > head && a[dq.back()] <= a[right]) dq.pop_back();\n        dq.push_back(right);\n        if (dq[head] <= right - b) head++;\n        if (right >= b - 1) out.push_back(a[dq[head]]);\n    }\n    return out;\n}',
      GO: 'func solve(a []int, b int) []int {\n\tn := len(a)\n\tdq := []int{}\n\thead := 0\n\tout := []int{}\n\tfor right := 0; right < n; right++ {\n\t\tfor len(dq) > head && a[dq[len(dq)-1]] <= a[right] {\n\t\t\tdq = dq[:len(dq)-1]\n\t\t}\n\t\tdq = append(dq, right)\n\t\tif dq[head] <= right-b {\n\t\t\thead++\n\t\t}\n\t\tif right >= b-1 {\n\t\t\tout = append(out, a[dq[head]])\n\t\t}\n\t}\n\treturn out\n}',
    },
    tests: [
      { stdin: '1 3 -1 -3 5 3 6 7\n3', expectedStdout: '3 3 5 5 6 7', isSample: true },
      { stdin: '4 4 4 4\n4', expectedStdout: '4', isSample: true },
      { stdin: '9 8 7 6\n1', expectedStdout: '9 8 7 6' },
      { stdin: '1 2 3 4 5\n2', expectedStdout: '2 3 4 5' },
      { stdin: '-5 -2 -8 -1\n2', expectedStdout: '-2 -2 -1' },
      { stdin: '7 2 4\n2', expectedStdout: '7 4' },
    ],
  }),
];
