import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w0-140` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-17 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W0_140_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "space-relay-diagnostic-tap",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "TWO_POINTERS",
    title: "Train Conductor Positioning",
    patternTags: ["linked-list","fast-slow-pointers","pointers"],
    signatureId: "fn:list->list",
    avgSolveSeconds: 600,
    promptMarkdown: "A freight train consists of several connected railcars arranged in a single sequence. Each railcar is linked to the following one.\n\nTo ensure optimal visibility, the train conductor must be stationed in the middle railcar of the sequence. In the event that there are an even number of railcars, the conductor should be placed in the second middle railcar.\n\nGiven the `head` of the train sequence (a linked list), return the railcar where the conductor should be positioned (which includes all subsequent railcars).\n\n**Constraints**\n- The number of railcars in the sequence is between `1` and `100`.\n- Each railcar's value is between `1` and `100`.\n\n**Example 1**\n```\ninput:\n  1 2 3 4 5\noutput:\n3 4 5\n```\n*Explanation: The middle railcar is 3.*\n\n**Example 2**\n```\ninput:\n  1 2 3 4 5 6\noutput:\n4 5 6\n```\n*Explanation: There is an even number of railcars, so we choose the second middle railcar, which is 4.*\n\n**Example 3**\n```\ninput:\n  1\noutput:\n1\n```\n*Explanation: With only one railcar, it is the middle one.*\n\n**Follow-up**\nCan you find the middle railcar in a single pass through the list using O(1) extra space?",
    editorialMarkdown: "## Space Relay Diagnostic Tap\n\nThe problem asks us to find the middle of a singly linked list. If the length is even, we return the second middle node.\n\nThe classic approach is the \"fast and slow pointers\" technique. We initialize two pointers, `slow` and `fast`, both starting at the `head` of the list. We then move `slow` by one step and `fast` by two steps in each iteration. When `fast` reaches the end of the list (or `fast.next` is null), `slow` will be pointing precisely at the middle node.\n\n**Trap**: A common pitfall is mishandling the loop condition for the fast pointer, leading to a null pointer dereference. Ensure you check both `fast != null` and `fast.next != null` before advancing.\n\n**Complexity:**\n- **Time:** O(N), where N is the number of nodes in the list. We traverse the list at most once.\n- **Space:** O(1), as we only use two pointers regardless of the list size.",
    referenceSolution: {
      JAVASCRIPT: "function solve(head) {\n    let slow = head;\n    let fast = head;\n    while (fast !== null && fast.next !== null) {\n        slow = slow.next;\n        fast = fast.next.next;\n    }\n    return slow;\n}",
      TYPESCRIPT: "function solve(head: any): any {\n    let slow = head;\n    let fast = head;\n    while (fast !== null && fast.next !== null) {\n        slow = slow.next;\n        fast = fast.next.next;\n    }\n    return slow;\n}",
      PYTHON: "def solve(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n    return slow",
      JAVA: "    static ListNode solve(ListNode head) {\n        ListNode slow = head, fast = head;\n        while (fast != null && fast.next != null) {\n            slow = slow.next;\n            fast = fast.next.next;\n        }\n        return slow;\n    }",
      CPP: "ListNode* solve(ListNode* head) {\n    ListNode* slow = head;\n    ListNode* fast = head;\n    while (fast != nullptr && fast->next != nullptr) {\n        slow = slow->next;\n        fast = fast->next->next;\n    }\n    return slow;\n}",
      GO: "func solve(head *ListNode) *ListNode {\n    slow, fast := head, head\n    for fast != nil && fast.Next != nil {\n        slow = slow.Next\n        fast = fast.Next.Next\n    }\n    return slow\n}",
    },
    tests: [
      { stdin: "1 2 3 4 5", expectedStdout: "3 4 5", isSample: true },
      { stdin: "1 2 3 4 5 6", expectedStdout: "4 5 6", isSample: true },
      { stdin: "1", expectedStdout: "1" },
      { stdin: "1 2", expectedStdout: "2" },
      { stdin: "1 2 3", expectedStdout: "2 3" },
      { stdin: "1 1 1 1", expectedStdout: "1 1" },
      { stdin: "10 20 30 40 50 60 70 80", expectedStdout: "50 60 70 80" },
      { stdin: "5 4 3 2 1 0 -1 -2 -3", expectedStdout: "1 0 -1 -2 -3" },
    ],
  }),

  p({
    ...base,
    slug: "alien-beverage-brew-time",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "GREEDY",
    title: "Cupcake Baking Schedule",
    patternTags: ["greedy","math","array"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 600,
    promptMarkdown: "A local bakery has received a catering order for three distinct flavors of cupcakes. You are provided with an array `orders` of length 3, where `orders[0]`, `orders[1]`, and `orders[2]` indicate the quantity needed for each of the three flavors.\n\nThe bakery is equipped with a dual-tray oven system. Every minute, you can choose to either bake two cupcakes of **different** flavors at the same time, or bake a single cupcake of any flavor.\n\nReturn the minimum number of minutes needed to complete the entire order.\n\n**Constraints**\n- `orders.length == 3`\n- `0 <= orders[i] <= 100`\n\n**Example 1**\n```\ninput:\n  1 4 2\noutput:\n4\n```\n*Explanation: You can bake (flavors 1 and 2), then (flavors 1 and 2), then (flavors 1 and 0), then (flavor 1). Total 4 minutes.*\n\n**Example 2**\n```\ninput:\n  5 4 4\noutput:\n7\n```\n*Explanation: Bake two different flavors each time until 13 cupcakes are completed, taking 7 minutes total.*\n\n**Example 3**\n```\ninput:\n  0 0 0\noutput:\n0\n```\n*Explanation: No orders to fulfill.*\n\n**Follow-up**\nCan you solve this mathematically in O(1) time without simulating the process?",
    editorialMarkdown: "## Alien Beverage Brew Time\n\nThe problem asks for the minimum time to process three quantities, where in one step we can reduce any two non-zero quantities by 1, or one quantity by 1.\n\nThis can be solved optimally without simulation. If the largest order is greater than the sum of the other two, we must pair the other two entirely with the largest one, and then finish the remaining cups of the largest one. In this case, the answer is just the largest order. Otherwise, we can always pair cups so that at most one cup is left unpaired. In this case, the answer is the ceiling of the sum of all orders divided by 2.\n\n**Trap**: Simulating the process using a priority queue works, but is overly complex. The math approach is simpler but requires careful handling of odd sums when dividing by 2.\n\n**Complexity:**\n- **Time:** O(1), as there are only 3 elements.\n- **Space:** O(1).",
    referenceSolution: {
      JAVASCRIPT: "function solve(orders) {\n    let max = Math.max(...orders);\n    let sum = orders[0] + orders[1] + orders[2];\n    return Math.max(max, Math.ceil(sum / 2));\n}",
      TYPESCRIPT: "function solve(orders: number[]): number {\n    let max = Math.max(...orders);\n    let sum = orders[0] + orders[1] + orders[2];\n    return Math.max(max, Math.ceil(sum / 2));\n}",
      PYTHON: "def solve(orders):\n    return max(max(orders), (sum(orders) + 1) // 2)",
      JAVA: "    static int solve(int[] orders) {\n        int sum = 0, max = 0;\n        for (int x : orders) { sum += x; max = Math.max(max, x); }\n        return Math.max(max, (sum + 1) / 2);\n    }",
      CPP: "int solve(vector<int> orders) {\n    int sum = 0, max_val = 0;\n    for (int x : orders) { sum += x; max_val = max(max_val, x); }\n    return max(max_val, (sum + 1) / 2);\n}",
      GO: "func solve(orders []int) int {\n    sum, maxVal := 0, 0\n    for _, x := range orders {\n        sum += x\n        if x > maxVal {\n            maxVal = x\n        }\n    }\n    ans := (sum + 1) / 2\n    if maxVal > ans {\n        ans = maxVal\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "1 4 2", expectedStdout: "4", isSample: true },
      { stdin: "5 4 4", expectedStdout: "7", isSample: true },
      { stdin: "0 0 0", expectedStdout: "0" },
      { stdin: "100 100 100", expectedStdout: "150" },
      { stdin: "10 0 0", expectedStdout: "10" },
      { stdin: "0 5 0", expectedStdout: "5" },
      { stdin: "2 2 2", expectedStdout: "3" },
      { stdin: "10 20 30", expectedStdout: "30" },
    ],
  }),

  p({
    ...base,
    slug: "solar-panel-alignment-energy",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Library Book Consolidation",
    patternTags: ["math","parity","array"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You have a collection of antique books scattered across a long row of numbered display cases. You are given an integer array `positions`, where `positions[i]` represents the case number of the `i`-th book.\n\nYou wish to gather all the books into a single display case. Moving a book by exactly `2` cases to the left or right requires `0` effort. Moving a book by exactly `1` case to the left or right requires `1` effort.\n\nReturn the minimum total effort needed to gather all the books into one display case.\n\n**Constraints**\n- `1 <= positions.length <= 100`\n- `1 <= positions[i] <= 10^9`\n\n**Example 1**\n```\ninput:\n  1 2 3\noutput:\n1\n```\n*Explanation: Move the book at 1 to 3 (requires 0 effort). Now books are at 2, 3, 3. Move the book at 2 to 3 (requires 1 effort). Total effort is 1.*\n\n**Example 2**\n```\ninput:\n  2 2 2 3 3\noutput:\n2\n```\n*Explanation: Move the two books at 3 to 2 (requires 1 effort each, total 2). All books are at 2.*\n\n**Example 3**\n```\ninput:\n  1 1000000000\noutput:\n1\n```\n*Explanation: Move one of the books to the other's parity, requiring 1 effort.*\n\n**Follow-up**\nCan you solve this with only a single pass and O(1) extra space?",
    editorialMarkdown: "## Solar Panel Alignment Energy\n\nSince moving a panel by 2 units costs 0 energy, we can freely move any panel to any other position that has the same parity (even or odd). This means all panels at even positions can be clustered together at no cost, and all panels at odd positions can be clustered together at no cost.\n\nTo merge the two clusters, we must pay 1 energy per panel for the smaller cluster to change its parity and join the larger one. Therefore, the problem reduces to counting how many panels are at even positions and how many are at odd positions, and returning the minimum of these two counts.\n\n**Trap**: A naive simulation of moving panels one by one is complex and inefficient. Recognizing the parity invariant simplifies the problem to a basic counting task.\n\n**Complexity:**\n- **Time:** O(N), where N is the number of panels, to count the parities.\n- **Space:** O(1).",
    referenceSolution: {
      JAVASCRIPT: "function solve(positions) {\n    let odd = 0, even = 0;\n    for (let x of positions) {\n        if (x % 2 === 0) even++; else odd++;\n    }\n    return Math.min(odd, even);\n}",
      TYPESCRIPT: "function solve(positions: number[]): number {\n    let odd = 0, even = 0;\n    for (let x of positions) {\n        if (x % 2 === 0) even++; else odd++;\n    }\n    return Math.min(odd, even);\n}",
      PYTHON: "def solve(positions):\n    odd = sum(1 for x in positions if x % 2 != 0)\n    return min(odd, len(positions) - odd)",
      JAVA: "    static int solve(int[] positions) {\n        int odd = 0, even = 0;\n        for (int x : positions) {\n            if (x % 2 == 0) even++; else odd++;\n        }\n        return Math.min(odd, even);\n    }",
      CPP: "int solve(vector<int> positions) {\n    int odd = 0, even = 0;\n    for (int x : positions) {\n        if (x % 2 == 0) even++; else odd++;\n    }\n    return min(odd, even);\n}",
      GO: "func solve(positions []int) int {\n    odd, even := 0, 0\n    for _, x := range positions {\n        if x % 2 == 0 { even++ } else { odd++ }\n    }\n    if odd < even { return odd }\n    return even\n}",
    },
    tests: [
      { stdin: "1 2 3", expectedStdout: "1", isSample: true },
      { stdin: "2 2 2 3 3", expectedStdout: "2", isSample: true },
      { stdin: "1 1000000000", expectedStdout: "1" },
      { stdin: "1 1 1 1 1", expectedStdout: "0" },
      { stdin: "2 4 6 8 10", expectedStdout: "0" },
      { stdin: "1 2 1 2 1 2 1 2", expectedStdout: "4" },
      { stdin: "5", expectedStdout: "0" },
      { stdin: "100 200 101", expectedStdout: "1" },
    ],
  }),

  p({
    ...base,
    slug: "gladiator-power-deficit",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "GREEDY",
    title: "Boss Rush Leveling",
    patternTags: ["greedy","array","simulation"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You are playing a role-playing game where your character must clear a gauntlet of boss monsters. Your character starts with a base stat of `initialPower`. You are given an array `opponents` where `opponents[i]` denotes the strength of the `i`-th boss.\n\nTo successfully defeat a boss, your character's current power must be **strictly greater** than the boss's strength. Upon winning, your character absorbs the boss's life force, adding its strength to your current power. If your power is not sufficient, you must spend time grinding for experience before starting the gauntlet. Each hour of grinding permanently boosts your `initialPower` by 1.\n\nReturn the minimum number of hours you need to grind to guarantee victory against all bosses in the exact order they appear.\n\n**Constraints**\n- `1 <= opponents.length <= 100`\n- `1 <= opponents[i] <= 100`\n- `1 <= initialPower <= 100`\n\n**Example 1**\n```\ninput:\n  1 4 3 2\n  5\noutput:\n0\n```\n*Explanation: Initial power is 5. \n- vs 1: 5 > 1, win. Power becomes 5 + 1 = 6.\n- vs 4: 6 > 4, win. Power becomes 6 + 4 = 10.\n- vs 3: 10 > 3, win. Power becomes 10 + 3 = 13.\n- vs 2: 13 > 2, win. Power becomes 13 + 2 = 15.\nNo grinding needed, return 0.*\n\n**Example 2**\n```\ninput:\n  5 3 4\n  2\noutput:\n4\n```\n*Explanation: With initial power 2, you cannot defeat the first boss (5). You need strictly more, so you must have 6 power. Grind for 4 hours to reach 6 power. \n- vs 5: win, power becomes 6 + 5 = 11.\n- vs 3: win, power becomes 11 + 3 = 14.\n- vs 4: win, power becomes 14 + 4 = 18.\nTotal grinding hours: 4.*\n\n**Example 3**\n```\ninput:\n  10 20\n  11\noutput:\n0\n```\n*Explanation: - vs 10: 11 > 10, win. Power = 21. \n- vs 20: 21 > 20, win. Power = 41. Total grinding hours: 0.*\n\n**Follow-up**\nCan you solve this efficiently in a single pass?",
    editorialMarkdown: "## Gladiator Power Deficit\n\nTo determine the minimum training required, we simulate the gauntlet. We maintain a running total of the robot's current power and the number of training hours accumulated.\n\nFor each opponent, we check if our current power is strictly greater than theirs. If it is not, we are forced to train just enough to beat them. The required training hours for that specific opponent is `opponentPower - currentPower + 1`. We add this amount to our total training hours and also to our current power. After ensuring we can beat the opponent, we then absorb their power by adding `opponentPower` to our current power. We repeat this for all opponents.\n\n**Trap**: Make sure to update the current power by both the training hours added *and* the opponent's absorbed power. The required condition is strictly greater, so do not forget the `+ 1`.\n\n**Complexity:**\n- **Time:** O(N), where N is the number of opponents, as we process each opponent exactly once.\n- **Space:** O(1), since we only use a few variables for counting.",
    referenceSolution: {
      JAVASCRIPT: "function solve(opponents, initialPower) {\n    let training = 0;\n    let current = initialPower;\n    for (let p of opponents) {\n        if (current <= p) {\n            let deficit = p - current + 1;\n            training += deficit;\n            current += deficit;\n        }\n        current += p;\n    }\n    return training;\n}",
      TYPESCRIPT: "function solve(opponents: number[], initialPower: number): number {\n    let training = 0;\n    let current = initialPower;\n    for (let p of opponents) {\n        if (current <= p) {\n            let deficit = p - current + 1;\n            training += deficit;\n            current += deficit;\n        }\n        current += p;\n    }\n    return training;\n}",
      PYTHON: "def solve(opponents, initialPower):\n    training = 0\n    current = initialPower\n    for p in opponents:\n        if current <= p:\n            deficit = p - current + 1\n            training += deficit\n            current += deficit\n        current += p\n    return training",
      JAVA: "    static int solve(int[] opponents, int initialPower) {\n        int training = 0;\n        int current = initialPower;\n        for (int p : opponents) {\n            if (current <= p) {\n                int deficit = p - current + 1;\n                training += deficit;\n                current += deficit;\n            }\n            current += p;\n        }\n        return training;\n    }",
      CPP: "int solve(vector<int> opponents, int initialPower) {\n    int training = 0;\n    int current = initialPower;\n    for (int p : opponents) {\n        if (current <= p) {\n            int deficit = p - current + 1;\n            training += deficit;\n            current += deficit;\n        }\n        current += p;\n    }\n    return training;\n}",
      GO: "func solve(opponents []int, initialPower int) int {\n    training := 0\n    current := initialPower\n    for _, p := range opponents {\n        if current <= p {\n            deficit := p - current + 1\n            training += deficit\n            current += deficit\n        }\n        current += p\n    }\n    return training\n}",
    },
    tests: [
      { stdin: "1 4 3 2\n5", expectedStdout: "0", isSample: true },
      { stdin: "5 3 4\n2", expectedStdout: "4", isSample: true },
      { stdin: "10 20\n11", expectedStdout: "0" },
      { stdin: "10 10 10\n10", expectedStdout: "1" },
      { stdin: "1\n1", expectedStdout: "1" },
      { stdin: "1 2 3\n100", expectedStdout: "0" },
      { stdin: "100 100 100\n1", expectedStdout: "100" },
      { stdin: "50 40 30\n10", expectedStdout: "41" },
    ],
  }),

  p({
    ...base,
    slug: "drone-charging-assignment",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "GREEDY",
    title: "Drone Charging Assignment",
    patternTags: ["greedy","sorting","matrix","absolute-value"],
    signatureId: "fn:matrix->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You oversee a fleet of delivery drones and a set of charging pads. You are given a 2D integer array `data` containing exactly two rows. The first row `data[0]` contains the starting positions of all your drones. The second row `data[1]` contains the positions of all available charging pads. There are exactly as many drones as there are pads.\n\nYou must assign each drone to exactly one unique charging pad. The energy required to move a drone is equal to the absolute difference between its starting position and the pad's position.\n\nReturn the minimum total energy required to move all drones to their assigned charging pads.\n\n**Constraints**\n- `data.length == 2`\n- `1 <= data[0].length == data[1].length <= 100`\n- `1 <= data[0][i], data[1][i] <= 100`\n\n**Example 1**\n```\ninput:\n  1 5 3;2 6 4\noutput:\n3\n```\n*Explanation: Drones are at [1, 5, 3] and pads at [2, 6, 4]. Assign drone 1 to pad 2 (cost 1). Assign drone 5 to pad 6 (cost 1). Assign drone 3 to pad 4 (cost 1). Total cost: 1 + 1 + 1 = 3.*\n\n**Example 2**\n```\ninput:\n  4 1 5 9;1 3 2 6\noutput:\n7\n```\n*Explanation: Drones at [4, 1, 5, 9], pads at [1, 3, 2, 6]. Sorted drones: [1, 4, 5, 9]. Sorted pads: [1, 2, 3, 6]. Costs: |1-1|=0, |4-2|=2, |5-3|=2, |9-6|=3. Total: 0 + 2 + 2 + 3 = 7.*\n\n**Example 3**\n```\ninput:\n  10;10\noutput:\n0\n```\n*Explanation: One drone already at the pad.*\n\n**Follow-up**\nCan you prove why sorting both arrays and pairing them sequentially yields the optimal assignment?",
    editorialMarkdown: "## Drone Charging Assignment\n\nTo minimize the total distance between a set of starting positions and destination positions, we should avoid \"crossing\" paths. If drone A is to the left of drone B, then drone A should go to a pad that is to the left of drone B's pad.\n\nTherefore, the optimal strategy is to sort both the drone positions and the pad positions. Then, we pair the i-th drone in the sorted list with the i-th pad in the sorted list. The total cost is simply the sum of the absolute differences between these paired positions.\n\n**Trap**: A greedy approach that tries to pair the closest available pad to each drone without sorting first can lead to sub-optimal configurations where paths cross and waste energy.\n\n**Complexity:**\n- **Time:** O(N log N), where N is the number of drones, dominated by the sorting step.\n- **Space:** O(1) or O(N) depending on the in-place sorting capabilities of the language.",
    referenceSolution: {
      JAVASCRIPT: "function solve(data) {\n    let drones = data[0].slice().sort((a,b)=>a-b);\n    let pads = data[1].slice().sort((a,b)=>a-b);\n    let total = 0;\n    for (let i = 0; i < drones.length; i++) {\n        total += Math.abs(drones[i] - pads[i]);\n    }\n    return total;\n}",
      TYPESCRIPT: "function solve(data: number[][]): number {\n    let drones = data[0].slice().sort((a,b)=>a-b);\n    let pads = data[1].slice().sort((a,b)=>a-b);\n    let total = 0;\n    for (let i = 0; i < drones.length; i++) {\n        total += Math.abs(drones[i] - pads[i]);\n    }\n    return total;\n}",
      PYTHON: "def solve(data):\n    drones = sorted(data[0])\n    pads = sorted(data[1])\n    return sum(abs(d - p) for d, p in zip(drones, pads))",
      JAVA: "    static int solve(int[][] data) {\n        java.util.Arrays.sort(data[0]);\n        java.util.Arrays.sort(data[1]);\n        int total = 0;\n        for (int i = 0; i < data[0].length; i++) {\n            total += Math.abs(data[0][i] - data[1][i]);\n        }\n        return total;\n    }",
      CPP: "#include <algorithm>\n#include <cmath>\nint solve(vector<vector<int>> data) {\n    sort(data[0].begin(), data[0].end());\n    sort(data[1].begin(), data[1].end());\n    int total = 0;\n    for (size_t i = 0; i < data[0].size(); i++) {\n        total += abs(data[0][i] - data[1][i]);\n    }\n    return total;\n}",
      GO: "func solve(data [][]int) int {\n    for i := 0; i < len(data[0]); i++ {\n        for j := i + 1; j < len(data[0]); j++ {\n            if data[0][i] > data[0][j] {\n                data[0][i], data[0][j] = data[0][j], data[0][i]\n            }\n            if data[1][i] > data[1][j] {\n                data[1][i], data[1][j] = data[1][j], data[1][i]\n            }\n        }\n    }\n    total := 0\n    for i := 0; i < len(data[0]); i++ {\n        diff := data[0][i] - data[1][i]\n        if diff < 0 { diff = -diff }\n        total += diff\n    }\n    return total\n}",
    },
    tests: [
      { stdin: "1 5 3;2 6 4", expectedStdout: "3", isSample: true },
      { stdin: "4 1 5 9;1 3 2 6", expectedStdout: "7", isSample: true },
      { stdin: "10;10", expectedStdout: "0" },
      { stdin: "10 20;1 2", expectedStdout: "27" },
      { stdin: "1 1 1;9 9 9", expectedStdout: "24" },
      { stdin: "5 4 3 2 1;1 2 3 4 5", expectedStdout: "0" },
      { stdin: "2 2 2 2;2 2 2 2", expectedStdout: "0" },
      { stdin: "1 100 2 99;3 4 5 6", expectedStdout: "192" },
    ],
  }),

  p({
    ...base,
    slug: "discounted-spacesuit-components",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "GREEDY",
    title: "Plant Nursery Promotion",
    patternTags: ["greedy","sorting","array"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You are decorating your garden with various potted plants. A local nursery is hosting a special sale: for every two plants you purchase, you may take a third plant for free, provided that the free plant's price is less than or equal to the minimum price of the two purchased plants.\n\nYou are given an integer array `costs` representing the price of each plant you intend to take home.\n\nReturn the minimum total cost to acquire all the desired plants.\n\n**Constraints**\n- `1 <= costs.length <= 100`\n- `1 <= costs[i] <= 100`\n\n**Example 1**\n```\ninput:\n  1 2 3\noutput:\n5\n```\n*Explanation: Buy the plants priced at 3 and 2. You get the plant priced at 1 for free. Total cost: 3 + 2 = 5.*\n\n**Example 2**\n```\ninput:\n  6 5 7 9 2 2\noutput:\n23\n```\n*Explanation: Buy 9 and 7 to get 6 for free. Buy 5 and 2 to get 2 for free. Total: 9 + 7 + 5 + 2 = 23.*\n\n**Example 3**\n```\ninput:\n  5 5\noutput:\n10\n```\n*Explanation: You only purchase 2 plants, so you don't receive any for free.*\n\n**Follow-up**\nCan you solve this efficiently by sorting the array first?",
    editorialMarkdown: "## Discounted Spacesuit Components\n\nTo minimize the total cost, we want to maximize the value of the free items we receive. Since a free item's cost cannot exceed the cheaper of the two items bought to earn it, the optimal strategy is to always bundle the most expensive available items together.\n\nWe sort the array in descending order. Then, we can iterate through the items and pay for the first two items of every group of three, skipping the third item as it will be free.\n\n**Trap**: If you sort in ascending order and take the free item from the end, you won't maximize your savings. Always pair the most expensive remaining items to get the next most expensive item for free.\n\n**Complexity:**\n- **Time:** O(N log N) to sort the array, where N is the number of components.\n- **Space:** O(1) or O(N) depending on the sorting algorithm used.",
    referenceSolution: {
      JAVASCRIPT: "function solve(costs) {\n    costs.sort((a, b) => b - a);\n    let total = 0;\n    for (let i = 0; i < costs.length; i++) {\n        if (i % 3 !== 2) total += costs[i];\n    }\n    return total;\n}",
      TYPESCRIPT: "function solve(costs: number[]): number {\n    costs.sort((a, b) => b - a);\n    let total = 0;\n    for (let i = 0; i < costs.length; i++) {\n        if (i % 3 !== 2) total += costs[i];\n    }\n    return total;\n}",
      PYTHON: "def solve(costs):\n    costs.sort(reverse=True)\n    return sum(costs[i] for i in range(len(costs)) if i % 3 != 2)",
      JAVA: "    static int solve(int[] costs) {\n        java.util.Arrays.sort(costs);\n        int total = 0;\n        for (int i = costs.length - 1; i >= 0; i--) {\n            int pos = costs.length - 1 - i;\n            if (pos % 3 != 2) total += costs[i];\n        }\n        return total;\n    }",
      CPP: "#include <algorithm>\nint solve(vector<int> costs) {\n    sort(costs.rbegin(), costs.rend());\n    int total = 0;\n    for (size_t i = 0; i < costs.size(); i++) {\n        if (i % 3 != 2) total += costs[i];\n    }\n    return total;\n}",
      GO: "func solve(costs []int) int {\n    for i := 0; i < len(costs); i++ {\n        for j := i + 1; j < len(costs); j++ {\n            if costs[i] < costs[j] {\n                costs[i], costs[j] = costs[j], costs[i]\n            }\n        }\n    }\n    total := 0\n    for i := 0; i < len(costs); i++ {\n        if i % 3 != 2 { total += costs[i] }\n    }\n    return total\n}",
    },
    tests: [
      { stdin: "1 2 3", expectedStdout: "5", isSample: true },
      { stdin: "6 5 7 9 2 2", expectedStdout: "23", isSample: true },
      { stdin: "5 5", expectedStdout: "10" },
      { stdin: "10", expectedStdout: "10" },
      { stdin: "1 1 1 1", expectedStdout: "3" },
      { stdin: "100 100 100 100 100 100 100", expectedStdout: "500" },
      { stdin: "2 2 2 2 2 2 2 2", expectedStdout: "12" },
      { stdin: "1 2 3 4 5 6 7 8 9", expectedStdout: "33" },
    ],
  }),
];
