import { AUTHORED, type ProblemDefinition } from '../problem.js';

/**
 * Tier 1 — Trees.
 *
 * The hinge of the roadmap: Tries, Backtracking and Heap / Priority Queue all
 * hang off this family, so nothing in that third of the graph is reachable
 * until these exist. Every statement here is written from the task rather than
 * from anyone's prose — see the non-negotiable at the top of docs/AUTHORING.md.
 *
 * The through-line across the nine: a recursive call on a subtree is a
 * *promise* — "give me a node, I will hand back the answer for everything
 * beneath it" — and the whole job is deciding what that promise has to carry so
 * that two subtree answers can be combined into the parent's. Depth combines
 * with a max, balance has to smuggle a height out alongside a yes/no, and the
 * search-tree check has to carry information *downward* as well, which is the
 * one case where a purely bottom-up promise is not enough.
 *
 * Wire format reminder: a tree arrives as level order with `null` for absent
 * children (`1 2 3 null 4`) and leaves in the same form with trailing nulls
 * trimmed.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = {
  tier: 'TIER_1',
  patternFamily: 'TREES',
  provenance: AUTHORED,
} as const;

export const TIER_1_TREES_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: 'tree-maximum-depth',
    title: 'How Many Levels Deep',
    difficulty: 'EASY',
    patternTags: ['trees', 'dfs', 'bfs', 'recursion'],
    signatureId: 'fn:tree->int',
    avgSolveSeconds: 420,
    promptMarkdown: [
      'You are given a binary tree. Return the number of levels in it — the count',
      'of nodes on the longest path from the root down to any leaf, counting both',
      'ends.',
      '',
      'The tree arrives on one line in level order, with `null` where a child is',
      'missing.',
      '',
      '**Example**',
      '',
      '```',
      'input:  3 9 20 null null 15 7',
      'output: 3',
      '```',
      '',
      'That is the root `3`, then `9` and `20`, then `15` and `7` under the `20`.',
      'Three levels.',
      '',
      'The tree may be empty — the input line is then blank, and the answer is `0`.',
      'A single node with no children is `1`. A one-sided chain of `k` nodes is `k`,',
      'so the deepest tree of `n` nodes is `n` levels.',
    ].join('\n'),
    editorialMarkdown: [
      '## Depth-first recursion, combined with a max',
      '',
      'The recursive call is a promise: hand it a node, and it returns the number of',
      'levels in the tree rooted at that node. Take that promise at face value for',
      'the two children and the parent is immediate — the deepest path through the',
      'parent goes down one side or the other, so the parent contributes one level on',
      'top of whichever child subtree is taller.',
      '',
      '```',
      'depth(node):',
      '    if node is missing: return 0',
      '    return 1 + max(depth(node.left), depth(node.right))',
      '```',
      '',
      'The base case is what makes the promise true at the bottom: a missing subtree',
      'has zero levels, so a leaf gets `1 + max(0, 0) = 1`, which is right, and every',
      'node above it inherits a correct answer from correct children. That is the',
      'whole induction.',
      '',
      'The quiet mistake is using `min` instead of `max`, or adding the two sides',
      'together. Both are quiet because they agree with the correct answer on a',
      'perfectly balanced tree and on a single node, which are exactly the two shapes',
      'people test by hand. Feed either one a lopsided tree and the answer collapses.',
      'The other quiet one is returning `1` for a missing child instead of `0`, which',
      'inflates every answer by one only on trees that have a missing child at all.',
      '',
      'A level-by-level sweep with a queue computes the same number and is worth',
      'knowing, because it never recurses.',
      '',
      'O(n) time — every node is visited once — and O(h) space for the recursion',
      'stack, where `h` is the height. For a balanced tree `h` is about log n; for a',
      'degenerate one-sided chain `h` is `n`, and that is the case that overflows a',
      'stack. The queue version costs O(w) for the widest level instead.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  if (!a) return 0;\n  let depth = 0;\n  let level = [a];\n  while (level.length > 0) {\n    depth++;\n    const next = [];\n    for (const node of level) {\n      if (node.left) next.push(node.left);\n      if (node.right) next.push(node.right);\n    }\n    level = next;\n  }\n  return depth;\n}',
      TYPESCRIPT:
        'function solve(a: TreeNode | null): number {\n  if (!a) return 0;\n  let depth = 0;\n  let level: TreeNode[] = [a];\n  while (level.length > 0) {\n    depth++;\n    const next: TreeNode[] = [];\n    for (const node of level) {\n      if (node.left) next.push(node.left);\n      if (node.right) next.push(node.right);\n    }\n    level = next;\n  }\n  return depth;\n}',
      PYTHON:
        'def solve(a):\n    if a is None:\n        return 0\n    depth = 0\n    level = [a]\n    while level:\n        depth += 1\n        nxt = []\n        for node in level:\n            if node.left is not None:\n                nxt.append(node.left)\n            if node.right is not None:\n                nxt.append(node.right)\n        level = nxt\n    return depth',
      JAVA:
        '    static int solve(TreeNode a) {\n        if (a == null) return 0;\n        int depth = 0;\n        ArrayDeque<TreeNode> q = new ArrayDeque<>();\n        q.add(a);\n        while (!q.isEmpty()) {\n            depth++;\n            int size = q.size();\n            for (int i = 0; i < size; i++) {\n                TreeNode node = q.poll();\n                if (node.left != null) q.add(node.left);\n                if (node.right != null) q.add(node.right);\n            }\n        }\n        return depth;\n    }',
      CPP:
        'int solve(TreeNode* a) {\n    if (a == nullptr) return 0;\n    int depth = 0;\n    queue<TreeNode*> q;\n    q.push(a);\n    while (!q.empty()) {\n        depth++;\n        int size = (int) q.size();\n        for (int i = 0; i < size; i++) {\n            TreeNode* node = q.front();\n            q.pop();\n            if (node->left != nullptr) q.push(node->left);\n            if (node->right != nullptr) q.push(node->right);\n        }\n    }\n    return depth;\n}',
      GO: 'func solve(a *TreeNode) int {\n\tif a == nil {\n\t\treturn 0\n\t}\n\tdepth := 0\n\tlevel := []*TreeNode{a}\n\tfor len(level) > 0 {\n\t\tdepth++\n\t\tnext := []*TreeNode{}\n\t\tfor _, node := range level {\n\t\t\tif node.Left != nil {\n\t\t\t\tnext = append(next, node.Left)\n\t\t\t}\n\t\t\tif node.Right != nil {\n\t\t\t\tnext = append(next, node.Right)\n\t\t\t}\n\t\t}\n\t\tlevel = next\n\t}\n\treturn depth\n}',
    },
    tests: [
      { stdin: '3 9 20 null null 15 7', expectedStdout: '3', isSample: true },
      { stdin: '', expectedStdout: '0', isSample: true },
      { stdin: '1', expectedStdout: '1' },
      { stdin: '1 2 null 3 null 4', expectedStdout: '4' },
      { stdin: '1 2 3 4 null null 5', expectedStdout: '3' },
      { stdin: '1 2 3', expectedStdout: '2' },
    ],
  }),

  p({
    ...base,
    slug: 'tree-mirror-of-itself',
    title: 'A Mirror Down the Middle',
    difficulty: 'MEDIUM',
    patternTags: ['trees', 'dfs', 'recursion', 'symmetry'],
    signatureId: 'fn:tree->bool',
    avgSolveSeconds: 600,
    promptMarkdown: [
      'Decide whether a binary tree is symmetric about its centre line — whether the',
      'left half is a mirror image of the right half, in both shape and values.',
      '',
      'Print `true` or `false`.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 2 3 4 4 3',
      'output: true',
      '```',
      '',
      'Under the root sit two `2`s. The left one has children `3 4`; the right one',
      'has `4 3` — the same pair reflected. Fold the page down the middle and every',
      'node lands on its twin.',
      '',
      'Mirroring is about position, not just about the multiset of values: two nodes',
      'match only if their values are equal **and** the left one\'s left child mirrors',
      'the right one\'s right child.',
      '',
      'The tree may be empty — the input line is then blank, and the answer is `true`.',
      'A single node is `true`. Values may repeat.',
    ].join('\n'),
    editorialMarkdown: [
      '## Recursion on pairs of nodes, not on single nodes',
      '',
      'The trick is to change what the recursion is about. Asking "is this subtree',
      'symmetric?" of one node does not compose, because symmetry is a relation',
      'between two different subtrees. So define the promise over a **pair**: given',
      'two nodes, return whether the tree under the first is the mirror image of the',
      'tree under the second.',
      '',
      '```',
      'mirror(p, q):',
      '    if both missing: return true',
      '    if exactly one missing: return false',
      '    return p.val == q.val',
      '           and mirror(p.left,  q.right)',
      '           and mirror(p.right, q.left)',
      '',
      'answer = tree is empty or mirror(root.left, root.right)',
      '```',
      '',
      'Why combining works: a reflection maps the outer edge of one side onto the',
      'outer edge of the other, and the inner edge onto the inner edge. So the pair',
      '`(p.left, q.right)` is exactly the outer pair and `(p.right, q.left)` exactly',
      'the inner pair. If both of those subproblems are mirrors and the two roots',
      'carry the same value, then every node under `p` has a partner under `q` at the',
      'reflected position — which is the definition. The base cases pin it down: two',
      'absences mirror each other, one absence against a present node cannot.',
      '',
      'The quiet mistake is crossing the recursion the same way on both calls —',
      'writing `mirror(p.left, q.left)` and `mirror(p.right, q.right)`. That asks',
      'whether the two halves are *identical* rather than reflected, and it is quiet',
      'because it returns the right answer on every tree whose two halves happen to',
      'be palindromic at each level, including `1 2 2` and any tree of all-equal',
      'values. It only diverges once a level is asymmetric in a way that a straight',
      'copy would not catch. The other quiet one is comparing only values and',
      'forgetting the one-missing-child case, which passes until a shape difference',
      'appears without a value difference.',
      '',
      'O(n) time — each node participates in exactly one pair comparison — and O(h)',
      'space for the recursion stack, where `h` is the height. `h` is n for a',
      'degenerate one-sided chain, though such a tree fails at the first pair.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  if (!a) return true;\n  const stack = [[a.left, a.right]];\n  while (stack.length > 0) {\n    const [p, q] = stack.pop();\n    if (!p && !q) continue;\n    if (!p || !q) return false;\n    if (p.val !== q.val) return false;\n    stack.push([p.left, q.right]);\n    stack.push([p.right, q.left]);\n  }\n  return true;\n}',
      TYPESCRIPT:
        'function solve(a: TreeNode | null): boolean {\n  if (!a) return true;\n  const stack: Array<[TreeNode | null, TreeNode | null]> = [[a.left, a.right]];\n  while (stack.length > 0) {\n    const pair = stack.pop() as [TreeNode | null, TreeNode | null];\n    const p = pair[0];\n    const q = pair[1];\n    if (!p && !q) continue;\n    if (!p || !q) return false;\n    if (p.val !== q.val) return false;\n    stack.push([p.left, q.right]);\n    stack.push([p.right, q.left]);\n  }\n  return true;\n}',
      PYTHON:
        'def solve(a):\n    if a is None:\n        return True\n    stack = [(a.left, a.right)]\n    while stack:\n        p, q = stack.pop()\n        if p is None and q is None:\n            continue\n        if p is None or q is None:\n            return False\n        if p.val != q.val:\n            return False\n        stack.append((p.left, q.right))\n        stack.append((p.right, q.left))\n    return True',
      JAVA:
        '    static boolean solve(TreeNode a) {\n        if (a == null) return true;\n        ArrayDeque<TreeNode[]> stack = new ArrayDeque<>();\n        stack.push(new TreeNode[] { a.left, a.right });\n        while (!stack.isEmpty()) {\n            TreeNode[] pair = stack.pop();\n            TreeNode p = pair[0];\n            TreeNode q = pair[1];\n            if (p == null && q == null) continue;\n            if (p == null || q == null) return false;\n            if (p.val != q.val) return false;\n            stack.push(new TreeNode[] { p.left, q.right });\n            stack.push(new TreeNode[] { p.right, q.left });\n        }\n        return true;\n    }',
      CPP:
        'bool solve(TreeNode* a) {\n    if (a == nullptr) return true;\n    vector<pair<TreeNode*, TreeNode*>> st;\n    st.push_back(make_pair(a->left, a->right));\n    while (!st.empty()) {\n        pair<TreeNode*, TreeNode*> cur = st.back();\n        st.pop_back();\n        TreeNode* p = cur.first;\n        TreeNode* q = cur.second;\n        if (p == nullptr && q == nullptr) continue;\n        if (p == nullptr || q == nullptr) return false;\n        if (p->val != q->val) return false;\n        st.push_back(make_pair(p->left, q->right));\n        st.push_back(make_pair(p->right, q->left));\n    }\n    return true;\n}',
      GO: 'func solve(a *TreeNode) bool {\n\tif a == nil {\n\t\treturn true\n\t}\n\ttype nodePair struct {\n\t\tp *TreeNode\n\t\tq *TreeNode\n\t}\n\tstack := []nodePair{{a.Left, a.Right}}\n\tfor len(stack) > 0 {\n\t\tcur := stack[len(stack)-1]\n\t\tstack = stack[:len(stack)-1]\n\t\tif cur.p == nil && cur.q == nil {\n\t\t\tcontinue\n\t\t}\n\t\tif cur.p == nil || cur.q == nil {\n\t\t\treturn false\n\t\t}\n\t\tif cur.p.Val != cur.q.Val {\n\t\t\treturn false\n\t\t}\n\t\tstack = append(stack, nodePair{cur.p.Left, cur.q.Right})\n\t\tstack = append(stack, nodePair{cur.p.Right, cur.q.Left})\n\t}\n\treturn true\n}',
    },
    tests: [
      { stdin: '1 2 2 3 4 4 3', expectedStdout: 'true', isSample: true },
      { stdin: '1 2 2 null 3 null 3', expectedStdout: 'false', isSample: true },
      { stdin: '', expectedStdout: 'true' },
      { stdin: '1', expectedStdout: 'true' },
      { stdin: '1 2 2 null 3 3', expectedStdout: 'true' },
      { stdin: '1 2 3', expectedStdout: 'false' },
    ],
  }),

  p({
    ...base,
    slug: 'tree-invert-left-right',
    title: 'Swap Every Left and Right',
    difficulty: 'EASY',
    patternTags: ['trees', 'dfs', 'recursion', 'in-place'],
    signatureId: 'fn:tree->tree',
    avgSolveSeconds: 420,
    promptMarkdown: [
      'Mirror a binary tree: at every node, exchange its left child with its right',
      'child. Return the tree.',
      '',
      'Input and output are both level order with `null` for a missing child;',
      'trailing `null`s are trimmed from the output.',
      '',
      '**Example**',
      '',
      '```',
      'input:  4 2 7 1 3 6 9',
      'output: 4 7 2 9 6 3 1',
      '```',
      '',
      'The root keeps its value; `2` and `7` trade places, and so does every pair',
      'below them, all the way down.',
      '',
      'The swap is recursive — it is not enough to swap only the root\'s two children.',
      '',
      'The tree may be empty — the input line is then blank, and the output is a blank',
      'line. A single node comes back unchanged. A one-sided chain flips to lean the',
      'other way.',
    ].join('\n'),
    editorialMarkdown: [
      '## Post-order (or pre-order) recursion that rebuilds nothing',
      '',
      'The recursive call promises: hand it a node, and it returns that node with the',
      'entire tree beneath it already mirrored. Given that promise for both children,',
      'the parent has one thing left to do — hang the mirrored right subtree on its',
      'left and the mirrored left subtree on its right.',
      '',
      '```',
      'invert(node):',
      '    if node is missing: return nothing',
      '    left  = invert(node.left)',
      '    right = invert(node.right)',
      '    node.left  = right',
      '    node.right = left',
      '    return node',
      '```',
      '',
      'Why combining is enough: mirroring a tree means reflecting it about the',
      'vertical line through the root. A reflection of the whole is a reflection of',
      'each half, moved to the opposite side. So there is nothing to merge and',
      'nothing to compare — the two subtree answers are already correct in isolation,',
      'and the parent only has to place them. Pre-order works equally well: swap the',
      'two child pointers first, then recurse into both. The order does not matter',
      'because the two subtrees never interact.',
      '',
      'The quiet mistake is swapping into a variable you then overwrite —',
      '`node.left = node.right; node.right = node.left`. Both children end up as the',
      'original right child and the left subtree silently vanishes. It is quiet',
      'because the output still has a plausible shape and passes on a single node, on',
      'an empty tree, and on any tree whose two children are equal. Hold one side in',
      'a temporary, or assign both at once. The second quiet one is recursing on the',
      'children *after* swapping the pointers while still using the old variable',
      'names, which mirrors the top two levels and leaves the rest untouched.',
      '',
      'O(n) time — one visit and one pointer swap per node — and O(h) space for the',
      'recursion stack, where `h` is the height. `h` is about log n when balanced and',
      '`n` for a degenerate one-sided chain, which is the shape that overflows.',
      'Nothing is allocated: the returned tree is the input tree, rewired.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const stack = [a];\n  while (stack.length > 0) {\n    const node = stack.pop();\n    if (!node) continue;\n    const tmp = node.left;\n    node.left = node.right;\n    node.right = tmp;\n    stack.push(node.left);\n    stack.push(node.right);\n  }\n  return a;\n}',
      TYPESCRIPT:
        'function solve(a: TreeNode | null): TreeNode | null {\n  const stack: Array<TreeNode | null> = [a];\n  while (stack.length > 0) {\n    const node = stack.pop() as TreeNode | null;\n    if (!node) continue;\n    const tmp = node.left;\n    node.left = node.right;\n    node.right = tmp;\n    stack.push(node.left);\n    stack.push(node.right);\n  }\n  return a;\n}',
      PYTHON:
        'def solve(a):\n    stack = [a]\n    while stack:\n        node = stack.pop()\n        if node is None:\n            continue\n        node.left, node.right = node.right, node.left\n        stack.append(node.left)\n        stack.append(node.right)\n    return a',
      JAVA:
        '    static TreeNode solve(TreeNode a) {\n        ArrayDeque<TreeNode> stack = new ArrayDeque<>();\n        if (a != null) stack.push(a);\n        while (!stack.isEmpty()) {\n            TreeNode node = stack.pop();\n            TreeNode tmp = node.left;\n            node.left = node.right;\n            node.right = tmp;\n            if (node.left != null) stack.push(node.left);\n            if (node.right != null) stack.push(node.right);\n        }\n        return a;\n    }',
      CPP:
        'TreeNode* solve(TreeNode* a) {\n    vector<TreeNode*> st;\n    if (a != nullptr) st.push_back(a);\n    while (!st.empty()) {\n        TreeNode* node = st.back();\n        st.pop_back();\n        TreeNode* tmp = node->left;\n        node->left = node->right;\n        node->right = tmp;\n        if (node->left != nullptr) st.push_back(node->left);\n        if (node->right != nullptr) st.push_back(node->right);\n    }\n    return a;\n}',
      GO: 'func solve(a *TreeNode) *TreeNode {\n\tstack := []*TreeNode{}\n\tif a != nil {\n\t\tstack = append(stack, a)\n\t}\n\tfor len(stack) > 0 {\n\t\tnode := stack[len(stack)-1]\n\t\tstack = stack[:len(stack)-1]\n\t\tnode.Left, node.Right = node.Right, node.Left\n\t\tif node.Left != nil {\n\t\t\tstack = append(stack, node.Left)\n\t\t}\n\t\tif node.Right != nil {\n\t\t\tstack = append(stack, node.Right)\n\t\t}\n\t}\n\treturn a\n}',
    },
    tests: [
      { stdin: '4 2 7 1 3 6 9', expectedStdout: '4 7 2 9 6 3 1', isSample: true },
      { stdin: '', expectedStdout: '', isSample: true },
      { stdin: '1', expectedStdout: '1' },
      { stdin: '1 2', expectedStdout: '1 null 2' },
      { stdin: '2 1 3', expectedStdout: '2 3 1' },
      { stdin: '1 2 3 4', expectedStdout: '1 3 2 null null null 4' },
    ],
  }),

  p({
    ...base,
    slug: 'tree-level-order-values',
    title: 'Read the Tree Row by Row',
    difficulty: 'MEDIUM',
    patternTags: ['trees', 'bfs', 'queue', 'traversal'],
    signatureId: 'fn:tree->ints',
    avgSolveSeconds: 600,
    promptMarkdown: [
      'Return the values of a binary tree read one level at a time, top to bottom,',
      'and within each level from left to right. Flatten the whole thing into a',
      'single list — no level boundaries in the output.',
      '',
      '**Example**',
      '',
      '```',
      'input:  3 9 20 null null 15 7',
      'output: 3 9 20 15 7',
      '```',
      '',
      'Level one is `3`, level two is `9 20`, level three is `15 7`. Missing children',
      'contribute nothing.',
      '',
      'The tree may be empty — the input line is then blank, and the output is a',
      'blank line. A single node prints just its own value. A one-sided chain prints',
      'one value per level, top to bottom. Values may repeat, and every occurrence is',
      'printed.',
    ].join('\n'),
    editorialMarkdown: [
      '## Breadth-first search with a queue',
      '',
      'This is the one traversal that recursion does not hand you for free. Depth-first',
      'recursion goes all the way down one side before touching the other, so it visits',
      'a level-three node before a level-two node on the far side. Level order needs',
      'the opposite discipline: finish a level before starting the next.',
      '',
      'A queue enforces exactly that. Push the root. Then repeatedly pop the front',
      'node, record its value, and push its present children on the back.',
      '',
      '```',
      'queue = [root]',
      'while queue is not empty:',
      '    node = pop front',
      '    output node.val',
      '    if node.left:  push node.left',
      '    if node.right: push node.right',
      '```',
      '',
      'Why this is correct: the queue is first-in-first-out, and a node at depth `d`',
      'can only be pushed by its parent at depth `d-1`. Every depth-`d-1` node is',
      'therefore enqueued before every depth-`d` node, and children are pushed left',
      'before right — so the queue holds the levels in order, concatenated, and',
      'popping it yields exactly the reading order asked for. There is no separate',
      '"combining two subtree answers" step here, which is the point: the parent\'s',
      'answer is not built from its subtrees, it is interleaved with theirs.',
      '',
      'The quiet mistake is reaching for a stack — popping from the back instead of',
      'the front. That gives a depth-first order, and it is quiet because on a tree of',
      'two levels the two orders are identical, and on a one-sided chain they agree',
      'as well. It only diverges once there are three levels with branching on both',
      'sides. The other quiet one is pushing missing children onto the queue and then',
      'printing something for them; guard with a null check on the way in, or on the',
      'way out, but not neither.',
      '',
      'O(n) time — each node is enqueued and dequeued once — and O(w) space, where `w`',
      'is the widest level; for a complete tree that is about n/2, so this is the',
      'traversal that costs memory in width rather than in depth. Recursion depth is',
      'not a factor here at all, which makes BFS the safe choice on a degenerate',
      'chain where a depth-first recursion of depth `n` would overflow.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const out = [];\n  if (!a) return out;\n  const queue = [a];\n  let head = 0;\n  while (head < queue.length) {\n    const node = queue[head++];\n    out.push(node.val);\n    if (node.left) queue.push(node.left);\n    if (node.right) queue.push(node.right);\n  }\n  return out;\n}',
      TYPESCRIPT:
        'function solve(a: TreeNode | null): number[] {\n  const out: number[] = [];\n  if (!a) return out;\n  const queue: TreeNode[] = [a];\n  let head = 0;\n  while (head < queue.length) {\n    const node = queue[head++];\n    out.push(node.val);\n    if (node.left) queue.push(node.left);\n    if (node.right) queue.push(node.right);\n  }\n  return out;\n}',
      PYTHON:
        'def solve(a):\n    out = []\n    if a is None:\n        return out\n    queue = [a]\n    head = 0\n    while head < len(queue):\n        node = queue[head]\n        head += 1\n        out.append(node.val)\n        if node.left is not None:\n            queue.append(node.left)\n        if node.right is not None:\n            queue.append(node.right)\n    return out',
      JAVA:
        '    static int[] solve(TreeNode a) {\n        ArrayList<Integer> out = new ArrayList<>();\n        if (a != null) {\n            ArrayDeque<TreeNode> queue = new ArrayDeque<>();\n            queue.add(a);\n            while (!queue.isEmpty()) {\n                TreeNode node = queue.poll();\n                out.add(node.val);\n                if (node.left != null) queue.add(node.left);\n                if (node.right != null) queue.add(node.right);\n            }\n        }\n        int[] res = new int[out.size()];\n        for (int i = 0; i < res.length; i++) res[i] = out.get(i);\n        return res;\n    }',
      CPP:
        'vector<int> solve(TreeNode* a) {\n    vector<int> out;\n    if (a == nullptr) return out;\n    queue<TreeNode*> q;\n    q.push(a);\n    while (!q.empty()) {\n        TreeNode* node = q.front();\n        q.pop();\n        out.push_back(node->val);\n        if (node->left != nullptr) q.push(node->left);\n        if (node->right != nullptr) q.push(node->right);\n    }\n    return out;\n}',
      GO: 'func solve(a *TreeNode) []int {\n\tout := []int{}\n\tif a == nil {\n\t\treturn out\n\t}\n\tqueue := []*TreeNode{a}\n\tfor len(queue) > 0 {\n\t\tnode := queue[0]\n\t\tqueue = queue[1:]\n\t\tout = append(out, node.Val)\n\t\tif node.Left != nil {\n\t\t\tqueue = append(queue, node.Left)\n\t\t}\n\t\tif node.Right != nil {\n\t\t\tqueue = append(queue, node.Right)\n\t\t}\n\t}\n\treturn out\n}',
    },
    tests: [
      { stdin: '3 9 20 null null 15 7', expectedStdout: '3 9 20 15 7', isSample: true },
      { stdin: '', expectedStdout: '', isSample: true },
      { stdin: '1', expectedStdout: '1' },
      { stdin: '1 2 3 4 5 6 7', expectedStdout: '1 2 3 4 5 6 7' },
      { stdin: '1 2 null 3', expectedStdout: '1 2 3' },
      { stdin: '5 null 4 null 3', expectedStdout: '5 4 3' },
    ],
  }),

  p({
    ...base,
    slug: 'tree-is-balanced',
    title: 'Balanced Everywhere, Not Just at the Top',
    difficulty: 'MEDIUM',
    patternTags: ['trees', 'dfs', 'recursion', 'height'],
    signatureId: 'fn:tree->bool',
    avgSolveSeconds: 600,
    promptMarkdown: [
      'Decide whether a binary tree is height-balanced: at **every** node, the height',
      'of its left subtree and the height of its right subtree differ by at most one.',
      '',
      'Height here is the number of levels, so a missing subtree has height `0` and a',
      'leaf has height `1`.',
      '',
      'Print `true` or `false`.',
      '',
      '**Example**',
      '',
      '```',
      'input:  3 9 20 null null 15 7',
      'output: true',
      '```',
      '',
      'The root has a left subtree of height `1` and a right subtree of height `2` —',
      'a difference of one, which is allowed — and the same holds at every node below.',
      '',
      'The condition must hold at every node, not only at the root. A tree can have',
      'two same-height halves and still be unbalanced deeper down.',
      '',
      'The tree may be empty — the input line is then blank, and the answer is `true`.',
      'A single node is `true`. A one-sided chain of three or more nodes is `false`.',
    ].join('\n'),
    editorialMarkdown: [
      '## Bottom-up recursion that returns a height and a verdict at once',
      '',
      'The obvious version asks, at each node, "what is my left height, what is my',
      'right height, do they differ by at most one?" — and then recurses into both',
      'children to ask the same. That is correct but it recomputes every height once',
      'per ancestor, which is O(n log n) on a balanced tree and O(n^2) on a chain.',
      '',
      'The fix is to widen the promise. Instead of returning just a height, the',
      'recursive call returns the height of the subtree **or** a sentinel meaning',
      '"something below me is already unbalanced". One traversal then carries both',
      'facts up.',
      '',
      '```',
      'height(node):',
      '    if node is missing: return 0',
      '    l = height(node.left)',
      '    r = height(node.right)',
      '    if l == BAD or r == BAD or abs(l - r) > 1: return BAD',
      '    return 1 + max(l, r)',
      '',
      'answer = height(root) != BAD',
      '```',
      '',
      'Why combining two subtree answers gives the parent\'s: "balanced everywhere in',
      'my subtree" is exactly "balanced everywhere in my left subtree" **and**',
      '"balanced everywhere in my right subtree" **and** "the two heights differ by at',
      'most one at me". The first two are what the children promise; the third needs',
      'only the two heights, which the children also hand back. Nothing else about the',
      'subtrees matters, so a single number per node is enough state. `-1` is the',
      'usual sentinel, since a real height is never negative.',
      '',
      'The quiet mistake is checking the difference only at the root — or, subtler,',
      'writing the recursion so that the failure never propagates: computing heights',
      'correctly, comparing them at each node, but returning `1 + max(l, r)` even',
      'after a violation was found. The tree then reports `true` because the bad news',
      'was thrown away one level down. It is quiet because a violation at the root is',
      'still caught, and the root is the case everyone tests. Feed it a tree that is',
      'even at the top and lopsided in one grandchild and the bug appears.',
      '',
      'O(n) time with the sentinel version — each node yields its height exactly once',
      '— and O(h) space for the recursion stack, where `h` is the height. `h` is `n`',
      'for a degenerate one-sided chain, which is precisely the input that both blows',
      'the stack and answers `false`.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const height = (node) => {\n    if (!node) return 0;\n    const l = height(node.left);\n    if (l < 0) return -1;\n    const r = height(node.right);\n    if (r < 0) return -1;\n    if (Math.abs(l - r) > 1) return -1;\n    return 1 + (l > r ? l : r);\n  };\n  return height(a) >= 0;\n}',
      TYPESCRIPT:
        'function solve(a: TreeNode | null): boolean {\n  const height = (node: TreeNode | null): number => {\n    if (!node) return 0;\n    const l = height(node.left);\n    if (l < 0) return -1;\n    const r = height(node.right);\n    if (r < 0) return -1;\n    if (Math.abs(l - r) > 1) return -1;\n    return 1 + (l > r ? l : r);\n  };\n  return height(a) >= 0;\n}',
      PYTHON:
        'def solve(a):\n    if a is None:\n        return True\n    heights = {}\n    stack = [(a, False)]\n    while stack:\n        node, expanded = stack.pop()\n        if expanded:\n            l = heights[id(node.left)] if node.left is not None else 0\n            r = heights[id(node.right)] if node.right is not None else 0\n            if l < 0 or r < 0 or abs(l - r) > 1:\n                heights[id(node)] = -1\n            else:\n                heights[id(node)] = 1 + (l if l > r else r)\n        else:\n            stack.append((node, True))\n            if node.left is not None:\n                stack.append((node.left, False))\n            if node.right is not None:\n                stack.append((node.right, False))\n    return heights[id(a)] >= 0',
      JAVA:
        '    static int balancedHeight(TreeNode node) {\n        if (node == null) return 0;\n        int l = balancedHeight(node.left);\n        if (l < 0) return -1;\n        int r = balancedHeight(node.right);\n        if (r < 0) return -1;\n        if (Math.abs(l - r) > 1) return -1;\n        return 1 + (l > r ? l : r);\n    }\n\n    static boolean solve(TreeNode a) {\n        return balancedHeight(a) >= 0;\n    }',
      CPP:
        'static int balancedHeight(TreeNode* node) {\n    if (node == nullptr) return 0;\n    int l = balancedHeight(node->left);\n    if (l < 0) return -1;\n    int r = balancedHeight(node->right);\n    if (r < 0) return -1;\n    int diff = l - r;\n    if (diff < 0) diff = -diff;\n    if (diff > 1) return -1;\n    return 1 + (l > r ? l : r);\n}\n\nbool solve(TreeNode* a) {\n    return balancedHeight(a) >= 0;\n}',
      GO: 'func balancedHeight(node *TreeNode) int {\n\tif node == nil {\n\t\treturn 0\n\t}\n\tl := balancedHeight(node.Left)\n\tif l < 0 {\n\t\treturn -1\n\t}\n\tr := balancedHeight(node.Right)\n\tif r < 0 {\n\t\treturn -1\n\t}\n\tdiff := l - r\n\tif diff < 0 {\n\t\tdiff = -diff\n\t}\n\tif diff > 1 {\n\t\treturn -1\n\t}\n\tif l > r {\n\t\treturn 1 + l\n\t}\n\treturn 1 + r\n}\n\nfunc solve(a *TreeNode) bool {\n\treturn balancedHeight(a) >= 0\n}',
    },
    tests: [
      { stdin: '3 9 20 null null 15 7', expectedStdout: 'true', isSample: true },
      { stdin: '1 2 2 3 3 null null 4 4', expectedStdout: 'false', isSample: true },
      { stdin: '', expectedStdout: 'true' },
      { stdin: '1', expectedStdout: 'true' },
      { stdin: '1 2 null 3', expectedStdout: 'false' },
      { stdin: '1 2 3 4 5 6 7', expectedStdout: 'true' },
    ],
  }),

  p({
    ...base,
    slug: 'tree-count-leaves',
    title: 'Count the Dead Ends',
    difficulty: 'EASY',
    patternTags: ['trees', 'dfs', 'recursion', 'counting'],
    signatureId: 'fn:tree->int',
    avgSolveSeconds: 420,
    promptMarkdown: [
      'Count the leaves of a binary tree — the nodes that have no left child and no',
      'right child.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 3 4 5',
      'output: 3',
      '```',
      '',
      'Under the root `1` sit `2` and `3`; under `2` sit `4` and `5`. The leaves are',
      '`4`, `5` and `3` — three of them.',
      '',
      'A node with exactly one child is **not** a leaf. Values may repeat and are',
      'irrelevant to the count.',
      '',
      'The tree may be empty — the input line is then blank, and the answer is `0`.',
      'A single node is `1`. A one-sided chain of any length has exactly one leaf.',
    ].join('\n'),
    editorialMarkdown: [
      '## Depth-first recursion, combined with a sum',
      '',
      'The recursive call promises: hand it a node, and it returns how many leaves',
      'are in the tree rooted there. The parent then has an easy job — every leaf',
      'beneath a node lies in exactly one of its two subtrees, and the two subtrees',
      'share no nodes, so the counts simply add.',
      '',
      '```',
      'leaves(node):',
      '    if node is missing: return 0',
      '    if node.left is missing and node.right is missing: return 1',
      '    return leaves(node.left) + leaves(node.right)',
      '```',
      '',
      'The two base cases are doing different work and both are needed. "Missing node',
      'contributes 0" is what lets a one-child node be handled by the general case at',
      'all: the absent side contributes nothing and the present side contributes its',
      'own count. "A node with neither child contributes 1" is the actual definition.',
      'Because the disjointness of the subtrees is what makes addition legal, no',
      'node is ever counted twice.',
      '',
      'The quiet mistake is dropping the leaf test and returning `1` for every',
      'missing child instead — `if node is missing: return 1`. On a perfect tree,',
      'where every node has either two children or none, that counts the missing',
      'slots under the leaves, which is twice the number of leaves... except it also',
      'happens to give the right answer for a single node and for an empty tree read',
      'the wrong way. Worse, on a tree with one-child nodes it counts each of those',
      'empty slots as a leaf, silently inflating the total. The dead-end test belongs',
      'on the node, not on its absence.',
      '',
      'O(n) time — one visit per node — and O(h) space for the recursion stack, where',
      '`h` is the height. `h` is about log n for a balanced tree and `n` for a',
      'degenerate one-sided chain, which is the shape that risks a stack overflow',
      'even though its answer is always `1`.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  let count = 0;\n  const stack = [a];\n  while (stack.length > 0) {\n    const node = stack.pop();\n    if (!node) continue;\n    if (!node.left && !node.right) {\n      count++;\n      continue;\n    }\n    stack.push(node.left);\n    stack.push(node.right);\n  }\n  return count;\n}',
      TYPESCRIPT:
        'function solve(a: TreeNode | null): number {\n  let count = 0;\n  const stack: Array<TreeNode | null> = [a];\n  while (stack.length > 0) {\n    const node = stack.pop() as TreeNode | null;\n    if (!node) continue;\n    if (!node.left && !node.right) {\n      count++;\n      continue;\n    }\n    stack.push(node.left);\n    stack.push(node.right);\n  }\n  return count;\n}',
      PYTHON:
        'def solve(a):\n    count = 0\n    stack = [a]\n    while stack:\n        node = stack.pop()\n        if node is None:\n            continue\n        if node.left is None and node.right is None:\n            count += 1\n            continue\n        stack.append(node.left)\n        stack.append(node.right)\n    return count',
      JAVA:
        '    static int solve(TreeNode a) {\n        int count = 0;\n        ArrayDeque<TreeNode> stack = new ArrayDeque<>();\n        if (a != null) stack.push(a);\n        while (!stack.isEmpty()) {\n            TreeNode node = stack.pop();\n            if (node.left == null && node.right == null) {\n                count++;\n                continue;\n            }\n            if (node.left != null) stack.push(node.left);\n            if (node.right != null) stack.push(node.right);\n        }\n        return count;\n    }',
      CPP:
        'int solve(TreeNode* a) {\n    int count = 0;\n    vector<TreeNode*> st;\n    if (a != nullptr) st.push_back(a);\n    while (!st.empty()) {\n        TreeNode* node = st.back();\n        st.pop_back();\n        if (node->left == nullptr && node->right == nullptr) {\n            count++;\n            continue;\n        }\n        if (node->left != nullptr) st.push_back(node->left);\n        if (node->right != nullptr) st.push_back(node->right);\n    }\n    return count;\n}',
      GO: 'func solve(a *TreeNode) int {\n\tcount := 0\n\tstack := []*TreeNode{}\n\tif a != nil {\n\t\tstack = append(stack, a)\n\t}\n\tfor len(stack) > 0 {\n\t\tnode := stack[len(stack)-1]\n\t\tstack = stack[:len(stack)-1]\n\t\tif node.Left == nil && node.Right == nil {\n\t\t\tcount++\n\t\t\tcontinue\n\t\t}\n\t\tif node.Left != nil {\n\t\t\tstack = append(stack, node.Left)\n\t\t}\n\t\tif node.Right != nil {\n\t\t\tstack = append(stack, node.Right)\n\t\t}\n\t}\n\treturn count\n}',
    },
    tests: [
      { stdin: '1 2 3 4 5', expectedStdout: '3', isSample: true },
      { stdin: '', expectedStdout: '0', isSample: true },
      { stdin: '1', expectedStdout: '1' },
      { stdin: '1 2 null 3 null 4', expectedStdout: '1' },
      { stdin: '1 2 3 4 5 6 7', expectedStdout: '4' },
      { stdin: '1 null 2 null 3', expectedStdout: '1' },
    ],
  }),

  p({
    ...base,
    slug: 'tree-is-valid-search-tree',
    title: 'Every Value in Its Allowed Range',
    difficulty: 'HARD',
    patternTags: ['trees', 'dfs', 'bst', 'invariants'],
    signatureId: 'fn:tree->bool',
    avgSolveSeconds: 900,
    promptMarkdown: [
      'Decide whether a binary tree is a valid binary **search** tree.',
      '',
      'The rule applies to whole subtrees, not just to immediate children: for every',
      'node, **every** value in its left subtree must be strictly smaller than the',
      'node\'s value, and **every** value in its right subtree strictly larger.',
      '',
      'Print `true` or `false`.',
      '',
      '**Example**',
      '',
      '```',
      'input:  5 1 4 null null 3 6',
      'output: false',
      '```',
      '',
      'Read it carefully: the root is `5`, its right child is `4` — already smaller',
      'than the root, so this fails immediately. And `3`, which sits under the `4`,',
      'is in the root\'s right subtree while being smaller than `5`, so it breaks the',
      'rule a second time even though `3 < 4` looks locally fine.',
      '',
      '"Strictly" means duplicates are not allowed: a node whose child carries the',
      'same value is invalid.',
      '',
      'The tree may be empty — the input line is then blank, and the answer is `true`.',
      'A single node is `true`. A one-sided chain is valid exactly when it is sorted',
      'in the direction it leans.',
    ].join('\n'),
    editorialMarkdown: [
      '## Carrying a permitted range down the tree',
      '',
      'This problem exists to punish one specific shortcut, so start there. The',
      'tempting check is local: at each node, confirm `left.val < node.val < right.val`',
      'and recurse. **That is not the search-tree property.** It only constrains a node',
      'against its two children, and the property constrains a node against every',
      'descendant. The example above is the whole point — a `3` hanging under a `4`',
      'satisfies its parent perfectly while sitting in the root `5`\'s right subtree,',
      'where nothing smaller than `5` is allowed to be. The parent-child check says',
      'yes; the tree is not a search tree.',
      '',
      'That mistake is exceptionally quiet. It accepts every valid tree, so it never',
      'produces a false negative; it only ever fails to reject. And the trees people',
      'sketch by hand to test it — three nodes, or a small perfect tree that really is',
      'a BST — all come back correct. The bug needs a violation that is more than one',
      'level away from the ancestor it violates before it shows itself.',
      '',
      'The fix is to send information **downward**, which is the part depth-first',
      'recursion on trees usually does not need. Every node inherits a permitted open',
      'range `(low, high)` from its ancestors. The root may be anything. Descending',
      'left tightens the upper bound to the parent\'s value; descending right tightens',
      'the lower bound to it. So the promise changes shape: "given a node and a range,',
      'is everything beneath it a valid search tree whose values all fall inside that',
      'range?"',
      '',
      '```',
      'ok(node, low, high):',
      '    if node is missing: return true',
      '    if low  exists and node.val <= low:  return false',
      '    if high exists and node.val >= high: return false',
      '    return ok(node.left,  low,      node.val)',
      '       and ok(node.right, node.val, high)',
      '```',
      '',
      'Why combining the two subtree answers is enough: the range that reaches a node',
      'is the intersection of every constraint its ancestors impose, because each step',
      'down only ever tightens one side. So checking `node.val` against its own range',
      'checks it against *all* of its ancestors at once, and if every node passes its',
      'own range then no ancestor is violated anywhere. The left and right calls cover',
      'disjoint sets of nodes and together cover all of them, so an `and` is the right',
      'combination.',
      '',
      'The bounds must be absent rather than merely extreme at the root. Using a',
      'minimum and maximum integer as the initial range is fine in principle but',
      'breaks the moment a node actually holds that value, which is a second very',
      'quiet bug — carry a "no bound yet" flag, or use a nullable bound, as the',
      'reference solutions here do.',
      '',
      'An in-order walk is the elegant alternative: a tree is a valid search tree',
      'exactly when its in-order traversal is strictly increasing, so tracking only',
      'the previously visited value suffices.',
      '',
      'O(n) time — one visit and two comparisons per node — and O(h) space, where `h`',
      'is the height: about log n balanced, and `n` for a degenerate one-sided chain,',
      'which is both the deepest recursion and a perfectly legal search tree when',
      'sorted.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a) {\n  const stack = [[a, false, 0, false, 0]];\n  while (stack.length > 0) {\n    const [node, hasLow, low, hasHigh, high] = stack.pop();\n    if (!node) continue;\n    if (hasLow && node.val <= low) return false;\n    if (hasHigh && node.val >= high) return false;\n    stack.push([node.left, hasLow, low, true, node.val]);\n    stack.push([node.right, true, node.val, hasHigh, high]);\n  }\n  return true;\n}',
      TYPESCRIPT:
        'type Bound = [TreeNode | null, boolean, number, boolean, number];\n\nfunction solve(a: TreeNode | null): boolean {\n  const stack: Bound[] = [[a, false, 0, false, 0]];\n  while (stack.length > 0) {\n    const frame = stack.pop() as Bound;\n    const node = frame[0];\n    if (!node) continue;\n    if (frame[1] && node.val <= frame[2]) return false;\n    if (frame[3] && node.val >= frame[4]) return false;\n    stack.push([node.left, frame[1], frame[2], true, node.val]);\n    stack.push([node.right, true, node.val, frame[3], frame[4]]);\n  }\n  return true;\n}',
      PYTHON:
        'def solve(a):\n    stack = [(a, False, 0, False, 0)]\n    while stack:\n        node, has_low, low, has_high, high = stack.pop()\n        if node is None:\n            continue\n        if has_low and node.val <= low:\n            return False\n        if has_high and node.val >= high:\n            return False\n        stack.append((node.left, has_low, low, True, node.val))\n        stack.append((node.right, True, node.val, has_high, high))\n    return True',
      JAVA:
        '    static boolean inRange(TreeNode node, boolean hasLow, int low, boolean hasHigh, int high) {\n        if (node == null) return true;\n        if (hasLow && node.val <= low) return false;\n        if (hasHigh && node.val >= high) return false;\n        return inRange(node.left, hasLow, low, true, node.val)\n            && inRange(node.right, true, node.val, hasHigh, high);\n    }\n\n    static boolean solve(TreeNode a) {\n        return inRange(a, false, 0, false, 0);\n    }',
      CPP:
        'static bool inRange(TreeNode* node, bool hasLow, int low, bool hasHigh, int high) {\n    if (node == nullptr) return true;\n    if (hasLow && node->val <= low) return false;\n    if (hasHigh && node->val >= high) return false;\n    if (!inRange(node->left, hasLow, low, true, node->val)) return false;\n    return inRange(node->right, true, node->val, hasHigh, high);\n}\n\nbool solve(TreeNode* a) {\n    return inRange(a, false, 0, false, 0);\n}',
      GO: 'func inRange(node *TreeNode, hasLow bool, low int, hasHigh bool, high int) bool {\n\tif node == nil {\n\t\treturn true\n\t}\n\tif hasLow && node.Val <= low {\n\t\treturn false\n\t}\n\tif hasHigh && node.Val >= high {\n\t\treturn false\n\t}\n\tif !inRange(node.Left, hasLow, low, true, node.Val) {\n\t\treturn false\n\t}\n\treturn inRange(node.Right, true, node.Val, hasHigh, high)\n}\n\nfunc solve(a *TreeNode) bool {\n\treturn inRange(a, false, 0, false, 0)\n}',
    },
    tests: [
      { stdin: '2 1 3', expectedStdout: 'true', isSample: true },
      { stdin: '5 1 4 null null 3 6', expectedStdout: 'false', isSample: true },
      { stdin: '', expectedStdout: 'true' },
      { stdin: '2 2', expectedStdout: 'false' },
      { stdin: '10 5 15 null null 6 20', expectedStdout: 'false' },
      { stdin: '5 3 8 1 4 7 9', expectedStdout: 'true' },
    ],
  }),

  p({
    ...base,
    slug: 'tree-kth-smallest-value',
    title: 'The K-th Smallest in a Search Tree',
    difficulty: 'MEDIUM',
    patternTags: ['trees', 'bst', 'in-order', 'traversal'],
    signatureId: 'fn:tree,int->int',
    avgSolveSeconds: 600,
    promptMarkdown: [
      'The first line is a binary **search** tree in level order. The second line is',
      'an integer `k`.',
      '',
      'Return the `k`-th smallest value in the tree, counting from `1` — so `k = 1` is',
      'the smallest value present.',
      '',
      '**Example**',
      '',
      '```',
      'input:  5 3 8 1 4 7 9',
      '        4',
      'output: 5',
      '```',
      '',
      'Sorted, the values are `1 3 4 5 7 8 9`; the fourth is `5`.',
      '',
      'Guarantees you may rely on: the tree is a valid search tree, it has at least',
      'one node, all values are distinct, and `1 <= k <= number of nodes`. `k` equal',
      'to the node count is allowed and asks for the largest value.',
      '',
      'You do not need to sort anything — the tree already holds its values in order,',
      'if you walk it the right way.',
    ].join('\n'),
    editorialMarkdown: [
      '## In-order traversal, stopped early',
      '',
      'A search tree is a sorted list wearing a disguise. Visit the left subtree, then',
      'the node, then the right subtree, and the values come out in increasing order.',
      'That is exactly the search-tree property restated: everything to the left is',
      'smaller, everything to the right is larger, and the same holds recursively, so',
      'the concatenation `[in-order of left] + [node] + [in-order of right]` is sorted',
      'whenever both pieces are. That is the combination step — the parent\'s ordered',
      'sequence is the two children\'s ordered sequences with the parent wedged',
      'between them, and it needs no merging because the ranges cannot overlap.',
      '',
      'So the answer is the `k`-th value that walk emits. There is no need to collect',
      'them all: count as you go and stop at `k`.',
      '',
      '```',
      'stack = empty, cur = root',
      'while cur exists or stack is not empty:',
      '    while cur exists: push cur; cur = cur.left',
      '    cur = pop',
      '    k = k - 1',
      '    if k == 0: return cur.val',
      '    cur = cur.right',
      '```',
      '',
      'The explicit stack is worth learning here, because it stops the moment the',
      'answer is known — a recursive version has to unwind through every remaining',
      'frame, or thread a flag through the return value, to do the same.',
      '',
      'The quiet mistake is off-by-one on `k`: decrementing after the comparison',
      'rather than before, which returns the `(k+1)`-th value. It is quiet because it',
      'is only ever wrong by one position, so on a small hand-checked tree the answer',
      'still looks plausible, and it is *correct* for a single-node tree with `k = 1`',
      'if the mis-ordering happens to short-circuit. The other quiet one is visiting',
      'node-then-left-then-right, a pre-order, which emits the root first; on a tree',
      'whose root happens to be the smallest — a right-leaning chain — pre-order and',
      'in-order agree, and the bug hides.',
      '',
      'O(h + k) time: the walk descends to the smallest value, costing O(h), then',
      'emits `k` values. In the worst case that is O(n). Space is O(h) for the stack,',
      'where `h` is the height — about log n balanced, and `n` for a degenerate',
      'one-sided chain, which is exactly the shape a naive recursion overflows on.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  let k = b;\n  const stack = [];\n  let cur = a;\n  while (cur || stack.length > 0) {\n    while (cur) {\n      stack.push(cur);\n      cur = cur.left;\n    }\n    cur = stack.pop();\n    k--;\n    if (k === 0) return cur.val;\n    cur = cur.right;\n  }\n  return -1;\n}',
      TYPESCRIPT:
        'function solve(a: TreeNode | null, b: number): number {\n  let k = b;\n  const stack: TreeNode[] = [];\n  let cur: TreeNode | null = a;\n  while (cur !== null || stack.length > 0) {\n    while (cur !== null) {\n      stack.push(cur);\n      cur = cur.left;\n    }\n    const node = stack.pop() as TreeNode;\n    k--;\n    if (k === 0) return node.val;\n    cur = node.right;\n  }\n  return -1;\n}',
      PYTHON:
        'def solve(a, b):\n    k = b\n    stack = []\n    cur = a\n    while cur is not None or stack:\n        while cur is not None:\n            stack.append(cur)\n            cur = cur.left\n        cur = stack.pop()\n        k -= 1\n        if k == 0:\n            return cur.val\n        cur = cur.right\n    return -1',
      JAVA:
        '    static int solve(TreeNode a, int b) {\n        int k = b;\n        ArrayDeque<TreeNode> stack = new ArrayDeque<>();\n        TreeNode cur = a;\n        while (cur != null || !stack.isEmpty()) {\n            while (cur != null) {\n                stack.push(cur);\n                cur = cur.left;\n            }\n            cur = stack.pop();\n            k--;\n            if (k == 0) return cur.val;\n            cur = cur.right;\n        }\n        return -1;\n    }',
      CPP:
        'int solve(TreeNode* a, int b) {\n    int k = b;\n    vector<TreeNode*> st;\n    TreeNode* cur = a;\n    while (cur != nullptr || !st.empty()) {\n        while (cur != nullptr) {\n            st.push_back(cur);\n            cur = cur->left;\n        }\n        cur = st.back();\n        st.pop_back();\n        k--;\n        if (k == 0) return cur->val;\n        cur = cur->right;\n    }\n    return -1;\n}',
      GO: 'func solve(a *TreeNode, b int) int {\n\tk := b\n\tstack := []*TreeNode{}\n\tcur := a\n\tfor cur != nil || len(stack) > 0 {\n\t\tfor cur != nil {\n\t\t\tstack = append(stack, cur)\n\t\t\tcur = cur.Left\n\t\t}\n\t\tcur = stack[len(stack)-1]\n\t\tstack = stack[:len(stack)-1]\n\t\tk--\n\t\tif k == 0 {\n\t\t\treturn cur.Val\n\t\t}\n\t\tcur = cur.Right\n\t}\n\treturn -1\n}',
    },
    tests: [
      { stdin: '5 3 8 1 4 7 9\n4', expectedStdout: '5', isSample: true },
      { stdin: '5 3 8 1 4 7 9\n7', expectedStdout: '9', isSample: true },
      { stdin: '5 3 8 1 4 7 9\n1', expectedStdout: '1' },
      { stdin: '2 1 3\n2', expectedStdout: '2' },
      { stdin: '1\n1', expectedStdout: '1' },
      { stdin: '20 10 30 null 15\n3', expectedStdout: '20' },
    ],
  }),

  p({
    ...base,
    slug: 'tree-lowest-shared-ancestor',
    title: 'The Deepest Node That Covers Them All',
    difficulty: 'HARD',
    patternTags: ['trees', 'dfs', 'post-order', 'ancestors'],
    signatureId: 'fn:tree,int->int',
    avgSolveSeconds: 900,
    promptMarkdown: [
      'The first line is a binary tree in level order. The second line is a value `x`.',
      '',
      'The value `x` may appear at more than one node. Return the value of the',
      '**deepest** node whose subtree contains every node carrying `x` — the lowest',
      'shared ancestor of all of them.',
      '',
      'A node counts as being in its own subtree, so if `x` appears exactly once the',
      'answer is `x` itself.',
      '',
      '**Example**',
      '',
      '```',
      'input:  1 2 3 4 null null 4',
      '        4',
      'output: 1',
      '```',
      '',
      'One `4` sits under the `2` on the left, the other under the `3` on the right.',
      'The only node holding both is the root, so the answer is `1`.',
      '',
      'If instead both `4`s sat under the `2`, the answer would be `2` — the root also',
      'contains them, but it is not the deepest node that does.',
      '',
      'Guarantees: the tree has at least one node, and `x` appears at least once. Other',
      'values may repeat too; only `x` matters.',
    ].join('\n'),
    editorialMarkdown: [
      '## Post-order recursion that returns a count',
      '',
      'Ask what the recursive call has to promise. "Return the answer for my subtree"',
      'is not enough, because a subtree that holds only *some* of the occurrences has',
      'no answer to give — the real answer lives above it. What a subtree can always',
      'report is a **count**: how many nodes carrying `x` are beneath it, itself',
      'included.',
      '',
      'That count composes trivially — a node\'s count is its two children\'s counts',
      'plus one if the node itself carries `x`, since the two subtrees are disjoint',
      'and together with the node they are the whole subtree. And the count is exactly',
      'the fact needed to recognise the answer: a node contains every occurrence',
      'precisely when its count equals the total number of occurrences in the tree.',
      '',
      'Many nodes satisfy that — every ancestor of the answer does too. The nodes that',
      'do form a path from the root downward, because containment only shrinks as you',
      'descend. So the answer is the deepest of them, and post-order gives it for',
      'free: children are finished before their parent, so the first node that reaches',
      'the full count is the deepest one. Record it once and ignore later matches.',
      '',
      '```',
      'total = number of nodes whose value is x',
      '',
      'count(node):',
      '    if node is missing: return 0',
      '    c = count(node.left) + count(node.right) + (1 if node.val == x else 0)',
      '    if c == total and no answer recorded yet: record node.val',
      '    return c',
      '```',
      '',
      'The quiet mistake is recording the answer on the way *down* instead of on the',
      'way up — checking a node before its children have reported. Then the root, which',
      'always has the full count, is recorded first and returned every time. It is',
      'quiet because the root is genuinely the correct answer whenever the occurrences',
      'straddle both halves, which is the case people draw when testing, and it is',
      'also correct on a tree with a single occurrence at the root. It only shows up',
      'when the true answer is deeper than the root. The second quiet one is stopping',
      'the traversal as soon as a node reaches the total: the *first* such node in a',
      'pre-order walk is the shallowest, not the deepest.',
      '',
      'O(n) time — one pass to total the occurrences and one post-order pass to find',
      'the deepest full-count node, or one pass if you compute the total first — and',
      'O(h) space for the recursion stack, where `h` is the height: about log n for a',
      'balanced tree and `n` for a degenerate one-sided chain, which is the case that',
      'bounds the stack.',
    ].join('\n'),
    referenceSolution: {
      JAVASCRIPT:
        'function solve(a, b) {\n  let total = 0;\n  const seen = [a];\n  while (seen.length > 0) {\n    const node = seen.pop();\n    if (!node) continue;\n    if (node.val === b) total++;\n    seen.push(node.left);\n    seen.push(node.right);\n  }\n  let answer = null;\n  const count = (node) => {\n    if (!node) return 0;\n    const c = count(node.left) + count(node.right) + (node.val === b ? 1 : 0);\n    if (c === total && answer === null) answer = node.val;\n    return c;\n  };\n  count(a);\n  return answer;\n}',
      TYPESCRIPT:
        'function solve(a: TreeNode | null, b: number): number {\n  let total = 0;\n  const seen: Array<TreeNode | null> = [a];\n  while (seen.length > 0) {\n    const node = seen.pop() as TreeNode | null;\n    if (!node) continue;\n    if (node.val === b) total++;\n    seen.push(node.left);\n    seen.push(node.right);\n  }\n  let answer: number | null = null;\n  const count = (node: TreeNode | null): number => {\n    if (!node) return 0;\n    const c = count(node.left) + count(node.right) + (node.val === b ? 1 : 0);\n    if (c === total && answer === null) answer = node.val;\n    return c;\n  };\n  count(a);\n  return answer as unknown as number;\n}',
      PYTHON:
        'def solve(a, b):\n    total = 0\n    seen = [a]\n    while seen:\n        node = seen.pop()\n        if node is None:\n            continue\n        if node.val == b:\n            total += 1\n        seen.append(node.left)\n        seen.append(node.right)\n    counts = {}\n    answer = None\n    stack = [(a, False)]\n    while stack:\n        node, expanded = stack.pop()\n        if node is None:\n            continue\n        if expanded:\n            c = counts.get(id(node.left), 0) + counts.get(id(node.right), 0)\n            if node.val == b:\n                c += 1\n            counts[id(node)] = c\n            if c == total and answer is None:\n                answer = node.val\n        else:\n            stack.append((node, True))\n            stack.append((node.left, False))\n            stack.append((node.right, False))\n    return answer',
      JAVA:
        '    static int lcaTarget;\n    static int lcaTotal;\n    static boolean lcaFound;\n    static int lcaAnswer;\n\n    static int lcaCount(TreeNode node) {\n        if (node == null) return 0;\n        int c = lcaCount(node.left) + lcaCount(node.right);\n        if (node.val == lcaTarget) c++;\n        if (c == lcaTotal && !lcaFound) {\n            lcaFound = true;\n            lcaAnswer = node.val;\n        }\n        return c;\n    }\n\n    static int lcaOccurrences(TreeNode node) {\n        if (node == null) return 0;\n        int c = lcaOccurrences(node.left) + lcaOccurrences(node.right);\n        if (node.val == lcaTarget) c++;\n        return c;\n    }\n\n    static int solve(TreeNode a, int b) {\n        lcaTarget = b;\n        lcaFound = false;\n        lcaAnswer = 0;\n        lcaTotal = lcaOccurrences(a);\n        lcaCount(a);\n        return lcaAnswer;\n    }',
      CPP:
        'static int lcaTarget;\nstatic int lcaTotal;\nstatic bool lcaFound;\nstatic int lcaAnswer;\n\nstatic int lcaOccurrences(TreeNode* node) {\n    if (node == nullptr) return 0;\n    int c = lcaOccurrences(node->left) + lcaOccurrences(node->right);\n    if (node->val == lcaTarget) c++;\n    return c;\n}\n\nstatic int lcaCount(TreeNode* node) {\n    if (node == nullptr) return 0;\n    int c = lcaCount(node->left) + lcaCount(node->right);\n    if (node->val == lcaTarget) c++;\n    if (c == lcaTotal && !lcaFound) {\n        lcaFound = true;\n        lcaAnswer = node->val;\n    }\n    return c;\n}\n\nint solve(TreeNode* a, int b) {\n    lcaTarget = b;\n    lcaFound = false;\n    lcaAnswer = 0;\n    lcaTotal = lcaOccurrences(a);\n    lcaCount(a);\n    return lcaAnswer;\n}',
      GO: 'func lcaOccurrences(node *TreeNode, target int) int {\n\tif node == nil {\n\t\treturn 0\n\t}\n\tc := lcaOccurrences(node.Left, target) + lcaOccurrences(node.Right, target)\n\tif node.Val == target {\n\t\tc++\n\t}\n\treturn c\n}\n\nfunc solve(a *TreeNode, b int) int {\n\ttotal := lcaOccurrences(a, b)\n\tfound := false\n\tanswer := 0\n\tvar count func(node *TreeNode) int\n\tcount = func(node *TreeNode) int {\n\t\tif node == nil {\n\t\t\treturn 0\n\t\t}\n\t\tc := count(node.Left) + count(node.Right)\n\t\tif node.Val == b {\n\t\t\tc++\n\t\t}\n\t\tif c == total && !found {\n\t\t\tfound = true\n\t\t\tanswer = node.Val\n\t\t}\n\t\treturn c\n\t}\n\tcount(a)\n\treturn answer\n}',
    },
    tests: [
      { stdin: '1 2 3 4 null null 4\n4', expectedStdout: '1', isSample: true },
      { stdin: '1 2 3 4 4\n4', expectedStdout: '2', isSample: true },
      { stdin: '1 2 3 4 5 6 7\n5', expectedStdout: '5' },
      { stdin: '7\n7', expectedStdout: '7' },
      { stdin: '1 2 2 3 3 3 3\n3', expectedStdout: '1' },
      { stdin: '5 5 9\n5', expectedStdout: '5' },
    ],
  }),
];
