import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'ARRAYS_HASHING', provenance: AUTHORED } as const;
const six = (JAVASCRIPT:string, TYPESCRIPT:string, PYTHON:string, JAVA:string, CPP:string, GO:string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_2_STRINGS_PROBLEMS: ProblemDefinition[] = [
  p({ ...base, slug:'casefold-anagram-with-wildcards', title:'Anagram With Blank Tiles', difficulty:'EASY', patternTags:['strings','hash-map','anagram'], signatureId:'fn:string,string->bool', avgSolveSeconds:540,
    promptMarkdown:['Return whether the second lowercase string can be rearranged to match the first, where each `?` in the second string may stand for any one letter.','', '**Example**', '', '```', 'input:  listen', 's?l?nt', 'output: true', '```', '', 'Both strings have the same non-zero length. The first string has letters only; every `?` must fill exactly one missing letter.'].join('\n'), editorialMarkdown:['## Frequency deficits with held blanks','','Count first-word letters, spend ordinary letters from the second, then use its blanks for exactly the remaining deficits. Position-by-position matching is wrong because anagrams ignore positions. The quiet mistake is spending a blank too early. Time is O(n), bounded by the word length; space is O(1) for 26 counts.'].join('\n'),
    referenceSolution:six('function solve(a,b){let c={};for(let x of a)c[x]=(c[x]||0)+1;let q=0;for(let x of b){if(x==="?")q++;else if(!c[x])return false;else c[x]--;}return Object.values(c).reduce((s,x)=>s+x,0)===q;}','function solve(a:string,b:string):boolean{const c:Record<string,number>={};for(const x of a)c[x]=(c[x]||0)+1;let q=0;for(const x of b){if(x===\'?\')q++;else if(!c[x])return false;else c[x]--;}return Object.values(c).reduce((s,x)=>s+x,0)===q;}','def solve(a, b):\n    c = {}\n    for x in a: c[x] = c.get(x, 0) + 1\n    q = 0\n    for x in b:\n        if x == "?": q += 1\n        elif c.get(x, 0) == 0: return False\n        else: c[x] -= 1\n    return sum(c.values()) == q','    static boolean solve(String a, String b) { int[] c=new int[26];for(char x:a.toCharArray())c[x-\'a\']++;int q=0;for(char x:b.toCharArray()){if(x==\'?\')q++;else if(c[x-\'a\']==0)return false;else c[x-\'a\']--;}int s=0;for(int x:c)s+=x;return s==q; }','bool solve(string a, string b) { int c[26]={};for(char x:a)c[x-\'a\']++;int q=0,s=0;for(char x:b){if(x==\'?\')q++;else if(c[x-\'a\']==0)return false;else c[x-\'a\']--;}for(int x:c)s+=x;return s==q; }','func solve(a string, b string) bool { c:=[26]int{};for _,x:=range a{c[x-\'a\']++};q:=0;for _,x:=range b{if x==\'?\'{q++}else if c[x-\'a\']==0{return false}else{c[x-\'a\']--}};s:=0;for _,x:=range c{s+=x};return s==q }'),
    tests:[{stdin:'listen\ns?l?nt',expectedStdout:'true',isSample:true},{stdin:'apple\na?p?e',expectedStdout:'true',isSample:true},{stdin:'cat\nd?g',expectedStdout:'false'},{stdin:'a\n?',expectedStdout:'true'},{stdin:'aab\na??',expectedStdout:'true'}] }),
  p({ ...base, slug:'folded-phrase-palindrome', title:'Mirror Phrase, Ignoring Marks', difficulty:'EASY', patternTags:['strings','two-pointers','palindrome'], signatureId:'fn:string->bool', avgSolveSeconds:540,
    promptMarkdown:['Return whether a non-empty line is a palindrome after ignoring every non-letter and treating uppercase and lowercase letters as equal.','', '**Example**', '', '```', 'input:  Never odd, or even!', 'output: true', '```', '', 'Letters are English A-Z or a-z. A line containing no letters returns true because its filtered form is empty.'].join('\n'), editorialMarkdown:['## Meaningful-character two pointers','','Walk inward while skipping punctuation, then compare folded letters. Each pointer advances only once, making this linear. The quiet mistake is comparing before skipping a mark. Time is O(n), bounded by line length; space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a){let l=0,r=a.length-1;while(l<r){while(l<r&&!/[a-z]/i.test(a[l]))l++;while(l<r&&!/[a-z]/i.test(a[r]))r--;if(a[l].toLowerCase()!=a[r].toLowerCase())return false;l++;r--;}return true;}','function solve(a:string):boolean{let l=0,r=a.length-1;while(l<r){while(l<r&&!/[a-z]/i.test(a[l]))l++;while(l<r&&!/[a-z]/i.test(a[r]))r--;if(a[l].toLowerCase()!==a[r].toLowerCase())return false;l++;r--;}return true;}','def solve(a):\n    x = \"\".join(ch.lower() for ch in a if ch.isalpha())\n    return x == x[::-1]','    static boolean solve(String a) { int l=0,r=a.length()-1;while(l<r){while(l<r&&!Character.isLetter(a.charAt(l)))l++;while(l<r&&!Character.isLetter(a.charAt(r)))r--;if(Character.toLowerCase(a.charAt(l))!=Character.toLowerCase(a.charAt(r)))return false;l++;r--;}return true; }','bool solve(string a) { string x;for(char c:a)if((c>=\'A\'&&c<=\'Z\')||(c>=\'a\'&&c<=\'z\'))x+=tolower(c);for(int l=0,r=x.size()-1;l<r;l++,r--)if(x[l]!=x[r])return false;return true; }','func solve(a string) bool { x:=[]byte{};for _,c:=range a{if c>=\'A\'&&c<=\'Z\'{c+=32};if c>=\'a\'&&c<=\'z\'{x=append(x,byte(c))}};for l,r:=0,len(x)-1;l<r;l,r=l+1,r-1{if x[l]!=x[r]{return false}};return true }'),
    tests:[{stdin:'Never odd, or even!',expectedStdout:'true',isSample:true},{stdin:'A man, a plan, a canal: Panama',expectedStdout:'true',isSample:true},{stdin:'race a car',expectedStdout:'false'},{stdin:'!!!',expectedStdout:'true'},{stdin:'a',expectedStdout:'true'}] }),
  p({ ...base, slug:'reverse-token-order-preserve-runs', title:'Reverse Words, Keep Separators', difficulty:'MEDIUM', patternTags:['strings','tokenising','parsing'], signatureId:'fn:string->string', avgSolveSeconds:660,
    promptMarkdown:['A line contains lowercase words separated by one or more `_` characters. Reverse the word order but leave each separator run in its original position.','', '**Example**', '', '```', 'input:  red__blue_green', 'output: green__blue_red', '```', '', 'The line starts and ends with a word. Separator runs are not moved or resized.'].join('\n'), editorialMarkdown:['## Alternating token refill','','Collect words, reverse that collection, and refill only word slots. Separator runs remain where they were. The quiet mistake is joining words with one underscore, which loses doubled runs. Time is O(n), bounded by the line; space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let w=a.match(/[a-z]+/g).reverse(),i=0;return a.replace(/[a-z]+/g,()=>w[i++]);}','function solve(a:string):string{const w=a.match(/[a-z]+/g)!.reverse();let i=0;return a.replace(/[a-z]+/g,()=>w[i++]);}','def solve(a):\n    import re\n    w = re.findall("[a-z]+", a)[::-1]\n    return re.sub("[a-z]+", lambda _: w.pop(0), a)','    static String solve(String a) { String[] w=a.split("_+");StringBuilder z=new StringBuilder();int k=w.length-1;for(int i=0;i<a.length();){if(a.charAt(i)==\'_\'){z.append(\'_\');i++;}else{while(i<a.length()&&a.charAt(i)!=\'_\')i++;z.append(w[k--]);}}return z.toString(); }','string solve(string a) { vector<string>w;for(int i=0;i<(int)a.size();){if(a[i]==\'_\'){i++;continue;}int j=i;while(j<(int)a.size()&&a[j]!=\'_\')j++;w.push_back(a.substr(i,j-i));i=j;}int k=w.size()-1;string z;for(char c:a){if(c==\'_\')z+=c;else {if(z.empty()||z.back()==\'_\')z+=w[k--];}}return z; }','func solve(a string) string { w:=[]string{};for i:=0;i<len(a);{if a[i]==\'_\'{i++;continue};j:=i;for j<len(a)&&a[j]!=\'_\'{j++};w=append(w,a[i:j]);i=j};k:=len(w)-1;z:=\"\";for i:=0;i<len(a);{if a[i]==\'_\'{z+=\"_\";i++;continue};for i<len(a)&&a[i]!=\'_\'{i++};z+=w[k];k--};return z }'),
    tests:[{stdin:'red__blue_green',expectedStdout:'green__blue_red',isSample:true},{stdin:'a_b',expectedStdout:'b_a',isSample:true},{stdin:'one___two',expectedStdout:'two___one'},{stdin:'solo',expectedStdout:'solo'},{stdin:'a__b___c',expectedStdout:'c__b___a'}] }),
  p({ ...base, slug:'encode-runs-with-digit-escape', title:'Escape Digits in Runs', difficulty:'MEDIUM', patternTags:['strings','run-length','encoding'], signatureId:'fn:string->string', avgSolveSeconds:720,
    promptMarkdown:['Encode each run as its character followed by its decimal count. A digit character is written with a preceding `#` so the result is unambiguous.','', '**Example**', '', '```', 'input:  aa111b', 'output: a2#13b1', '```', '', 'The non-empty input contains lowercase letters and digits. A one-character run still includes count `1`.'].join('\n'), editorialMarkdown:['## Escaped run scan','','Scan maximal equal runs and emit a literal plus its count. Escaping only digit literals makes the encoding unambiguous. The quiet mistake is escaping counts themselves. Time is O(n), bounded by input length; space is O(n) for output.'].join('\n'),
    referenceSolution:six('function solve(a){let o="";for(let i=0;i<a.length;){let j=i;while(j<a.length&&a[j]==a[i])j++;o+=(/\\d/.test(a[i])?"#":"")+a[i]+(j-i);i=j;}return o;}','function solve(a:string):string{let o="";for(let i=0;i<a.length;){let j=i;while(j<a.length&&a[j]===a[i])j++;o+=(/\\d/.test(a[i])?"#":"")+a[i]+(j-i);i=j;}return o;}','def solve(a):\n    out=[]; i=0\n    while i < len(a):\n        j=i\n        while j<len(a) and a[j]==a[i]: j+=1\n        out.append(("#" if a[i].isdigit() else "") + a[i] + str(j-i)); i=j\n    return "".join(out)','    static String solve(String a) { StringBuilder o=new StringBuilder();for(int i=0;i<a.length();){int j=i;while(j<a.length()&&a.charAt(j)==a.charAt(i))j++;if(Character.isDigit(a.charAt(i)))o.append(\'#\');o.append(a.charAt(i)).append(j-i);i=j;}return o.toString(); }','string solve(string a) { string o;for(int i=0;i<(int)a.size();){int j=i;while(j<(int)a.size()&&a[j]==a[i])j++;if(a[i]>=\'0\'&&a[i]<=\'9\')o+=\'#\';o+=a[i]+to_string(j-i);i=j;}return o; }','func solve(a string) string { o:=\"\";for i:=0;i<len(a);{j:=i;for j<len(a)&&a[j]==a[i]{j++};if a[i]>=\'0\'&&a[i]<=\'9\'{o+=\"#\"};o+=string(a[i])+strconv.Itoa(j-i);i=j};return o }'),
    tests:[{stdin:'aa111b',expectedStdout:'a2#13b1',isSample:true},{stdin:'x',expectedStdout:'x1',isSample:true},{stdin:'22',expectedStdout:'#22'},{stdin:'a12',expectedStdout:'a1#11#21'},{stdin:'zzz9',expectedStdout:'z3#91'}] }),
  p({ ...base, slug:'caesar-shift-letters-only', title:'Rotate Letters, Leave Digits', difficulty:'EASY', patternTags:['strings','case-folding','encoding'], signatureId:'fn:string,int->string', avgSolveSeconds:540,
    promptMarkdown:['Shift every English letter forward by `k`, wrapping after Z or z. Preserve letter case and leave every non-letter unchanged.','', '**Example**', '', '```', 'input:  Az-9', '2', 'output: Cb-9', '```', '', 'The line is non-empty and `k` is nonnegative. A shift of 26 changes nothing.'].join('\n'), editorialMarkdown:['## Separate alphabet rings','','Map each letter into its uppercase or lowercase 26-character ring, then rotate its offset. Nonletters bypass the mapping. The quiet mistake is shifting punctuation too. Time is O(n), bounded by line length; space is O(n) for output.'].join('\n'),
    referenceSolution:six('function solve(a,b){return [...a].map(x=>/[a-z]/i.test(x)?String.fromCharCode((x<="Z"?65:97)+(x.toLowerCase().charCodeAt(0)-97+b)%26):x).join("");}','function solve(a:string,b:number):string{return [...a].map(x=>/[a-z]/i.test(x)?String.fromCharCode((x<=\'Z\'?65:97)+(x.toLowerCase().charCodeAt(0)-97+b)%26):x).join(\'\');}','def solve(a, b):\n    out=[]\n    for x in a:\n        if x.isalpha():\n            base=ord("A") if x.isupper() else ord("a"); out.append(chr(base+(ord(x)-base+b)%26))\n        else: out.append(x)\n    return "".join(out)','    static String solve(String a, int b) { StringBuilder o=new StringBuilder();for(char x:a.toCharArray()){if(x>=\'A\'&&x<=\'Z\')o.append((char)(\'A\'+(x-\'A\'+b)%26));else if(x>=\'a\'&&x<=\'z\')o.append((char)(\'a\'+(x-\'a\'+b)%26));else o.append(x);}return o.toString(); }','string solve(string a, int b) { for(char &x:a){if(x>=\'A\'&&x<=\'Z\')x=\'A\'+(x-\'A\'+b)%26;else if(x>=\'a\'&&x<=\'z\')x=\'a\'+(x-\'a\'+b)%26;}return a; }','func solve(a string, b int) string { z:=[]byte(a);for i,x:=range z{if x>=\'A\'&&x<=\'Z\'{z[i]=\'A\'+(x-\'A\'+byte(b%26))%26}else if x>=\'a\'&&x<=\'z\'{z[i]=\'a\'+(x-\'a\'+byte(b%26))%26}};return string(z) }'),
    tests:[{stdin:'Az-9\n2',expectedStdout:'Cb-9',isSample:true},{stdin:'xyz\n3',expectedStdout:'abc',isSample:true},{stdin:'Hello, Z!\n26',expectedStdout:'Hello, Z!'},{stdin:'a\n25',expectedStdout:'z'},{stdin:'A1z\n1',expectedStdout:'B1a'}] }),
  p({ ...base, slug:'minimum-anagram-replacements', title:'Replace Until the Bags Match', difficulty:'MEDIUM', patternTags:['strings','frequency','anagram'], signatureId:'fn:string,string->int', avgSolveSeconds:660,
    promptMarkdown:['Two lowercase strings have equal non-zero length. Return the fewest character replacements needed in the second so its letters can be rearranged into the first.','', '**Example**', '', '```', 'input:  aabc', 'zzab', 'output: 2', '```', '', 'Changing both `z` characters supplies the missing `a` and `c`. Positions do not matter; only letter counts do.'].join('\n'), editorialMarkdown:['## Positive letter deficits','','Subtract second-string counts from first-string counts, then sum only positive deficits. Each replacement repairs one deficit. The quiet mistake is summing absolute differences, which doubles the answer. Time is O(n), bounded by string length; space is O(1).'].join('\n'),
    referenceSolution:six('function solve(a,b){let c={};for(let x of a)c[x]=(c[x]||0)+1;for(let x of b)c[x]=(c[x]||0)-1;return Object.values(c).reduce((s,x)=>s+(x>0?x:0),0);}','function solve(a:string,b:string):number{const c:Record<string,number>={};for(const x of a)c[x]=(c[x]||0)+1;for(const x of b)c[x]=(c[x]||0)-1;return Object.values(c).reduce((s,x)=>s+(x>0?x:0),0);}','def solve(a, b):\n    c=[0]*26\n    for x in a: c[ord(x)-97]+=1\n    for x in b: c[ord(x)-97]-=1\n    return sum(x for x in c if x>0)','    static int solve(String a, String b) { int[] c=new int[26];for(char x:a.toCharArray())c[x-\'a\']++;for(char x:b.toCharArray())c[x-\'a\']--;int s=0;for(int x:c)if(x>0)s+=x;return s; }','int solve(string a, string b) { int c[26]={};for(char x:a)c[x-\'a\']++;for(char x:b)c[x-\'a\']--;int s=0;for(int x:c)if(x>0)s+=x;return s; }','func solve(a string, b string) int { c:=[26]int{};for _,x:=range a{c[x-\'a\']++};for _,x:=range b{c[x-\'a\']--};s:=0;for _,x:=range c{if x>0{s+=x}};return s }'),
    tests:[{stdin:'aabc\nzzab',expectedStdout:'2',isSample:true},{stdin:'anagram\nmangaar',expectedStdout:'0',isSample:true},{stdin:'a\nb',expectedStdout:'1'},{stdin:'abc\ndef',expectedStdout:'3'},{stdin:'aabb\nbbbb',expectedStdout:'2'}] }),
  p({ ...base, slug:'stable-casefold-deduplicate', title:'First Spelling Wins', difficulty:'EASY', patternTags:['strings','hash-set','case-folding'], signatureId:'fn:strings->strings', avgSolveSeconds:540,
    promptMarkdown:['Return the first occurrence of each word from a non-empty list, treating words equal without case but preserving the first word’s original spelling and list order.','', '**Example**', '', '```', 'input:  Go go Rust GO rust', 'output: Go Rust', '```', '', 'Words contain English letters only. The output order is the first-seen order.'].join('\n'), editorialMarkdown:['## Stable canonical-key set','','Use lowercase keys in a set but append untouched originals only on first sight. This separates identity from presentation. The quiet mistake is returning lowercase keys. Time is O(total characters), bounded by list size; space is O(u) for unique words.'].join('\n'),
    referenceSolution:six('function solve(a){let s=new Set(),o=[];for(let x of a){let k=x.toLowerCase();if(!s.has(k)){s.add(k);o.push(x)}}return o;}','function solve(a:string[]):string[]{const s=new Set<string>(),o:string[]=[];for(const x of a){const k=x.toLowerCase();if(!s.has(k)){s.add(k);o.push(x);}}return o;}','def solve(a):\n    seen=set(); out=[]\n    for x in a:\n        if x.lower() not in seen: seen.add(x.lower()); out.append(x)\n    return out','    static String[] solve(String[] a) { java.util.ArrayList<String> o=new java.util.ArrayList<>();java.util.HashSet<String>s=new java.util.HashSet<>();for(String x:a)if(s.add(x.toLowerCase()))o.add(x);return o.toArray(new String[0]); }','vector<string> solve(vector<string> a) { vector<string>o;unordered_set<string>s;for(string x:a){string k=x;for(char &c:k)if(c>=\'A\'&&c<=\'Z\')c+=32;if(s.insert(k).second)o.push_back(x);}return o; }','func solve(a []string) []string { s:=map[string]bool{};o:=[]string{};for _,x:=range a{k:=strings.ToLower(x);if !s[k]{s[k]=true;o=append(o,x)}};return o }'),
    tests:[{stdin:'Go go Rust GO rust',expectedStdout:'Go Rust',isSample:true},{stdin:'A a B',expectedStdout:'A B',isSample:true},{stdin:'solo',expectedStdout:'solo'},{stdin:'x X x',expectedStdout:'x'},{stdin:'One TWO one two Three',expectedStdout:'One TWO Three'}] }),
  p({ ...base, slug:'count-overlapping-folded-needle', title:'Count Folded Overlaps', difficulty:'MEDIUM', patternTags:['strings','matching','case-folding'], signatureId:'fn:string,string->int', avgSolveSeconds:660,
    promptMarkdown:['Count every occurrence of the second non-empty string in the first, ignoring letter case and allowing overlaps.','', '**Example**', '', '```', 'input:  aBaBa', 'aba', 'output: 2', '```', '', 'The matches begin at positions 0 and 2. If the needle is longer than the text, return 0.'].join('\n'), editorialMarkdown:['## Overlap-aware start scan','','Case-fold both strings and inspect every legal start, including the one after a match. This preserves overlaps. The quiet mistake is skipping by needle length. Time is O(nm), bounded by text and needle lengths; space is O(n+m).'].join('\n'),
    referenceSolution:six('function solve(a,b){a=a.toLowerCase();b=b.toLowerCase();let n=0;for(let i=0;i+b.length<=a.length;i++)if(a.slice(i,i+b.length)==b)n++;return n;}','function solve(a:string,b:string):number{a=a.toLowerCase();b=b.toLowerCase();let n=0;for(let i=0;i+b.length<=a.length;i++)if(a.slice(i,i+b.length)===b)n++;return n;}','def solve(a, b):\n    a,b=a.lower(),b.lower()\n    return sum(a[i:i+len(b)]==b for i in range(len(a)-len(b)+1))','    static int solve(String a, String b) { a=a.toLowerCase();b=b.toLowerCase();int n=0;for(int i=0;i+b.length()<=a.length();i++)if(a.substring(i,i+b.length()).equals(b))n++;return n; }','int solve(string a, string b) { for(char &x:a)x=tolower(x);for(char &x:b)x=tolower(x);int n=0;for(int i=0;i+(int)b.size()<=(int)a.size();i++)if(a.substr(i,b.size())==b)n++;return n; }','func solve(a string, b string) int { a=strings.ToLower(a);b=strings.ToLower(b);n:=0;for i:=0;i+len(b)<=len(a);i++{if a[i:i+len(b)]==b{n++}};return n }'),
    tests:[{stdin:'aBaBa\naba',expectedStdout:'2',isSample:true},{stdin:'aaaa\naa',expectedStdout:'3',isSample:true},{stdin:'abc\nD',expectedStdout:'0'},{stdin:'X\nx',expectedStdout:'1'},{stdin:'AbABab\nAB',expectedStdout:'3'}] }),
  p({ ...base, slug:'token-length-signature', title:'Word Shape Signature', difficulty:'MEDIUM', patternTags:['strings','tokenising','encoding'], signatureId:'fn:string->string', avgSolveSeconds:660,
    promptMarkdown:['A line contains lowercase words separated by one or more spaces. Return their lengths joined by `-`, in word order.','', '**Example**', '', '```', 'input:  red  blue green', 'output: 3-4-5', '```', '', 'The line contains at least one word. Multiple spaces do not create empty tokens.'].join('\n'), editorialMarkdown:['## Maximal word tokenisation','','Scan maximal non-space tokens and join their lengths. A raw single-space split creates empty tokens under repeated spaces. The quiet mistake is counting those fake words. Time is O(n), bounded by line length; space is O(n) for the signature.'].join('\n'),
    referenceSolution:six('function solve(a){return a.trim().split(/ +/).map(x=>x.length).join("-");}','function solve(a:string):string{return a.trim().split(/ +/).map(x=>x.length).join(\'-\');}','def solve(a):\n    return "-".join(str(len(x)) for x in a.split())','    static String solve(String a) { String[] w=a.trim().split(" +");StringBuilder o=new StringBuilder();for(int i=0;i<w.length;i++){if(i>0)o.append(\'-\');o.append(w[i].length());}return o.toString(); }','string solve(string a) { string o;for(int i=0;i<(int)a.size();){while(i<(int)a.size()&&a[i]==\' \')i++;int j=i;while(j<(int)a.size()&&a[j]!=\' \')j++;if(i<j){if(!o.empty())o+=\'-\';o+=to_string(j-i);}i=j;}return o; }','func solve(a string) string { w:=strings.Fields(a);o:=\"\";for i,x:=range w{if i>0{o+=\"-\"};o+=strconv.Itoa(len(x))};return o }'),
    tests:[{stdin:'red  blue green',expectedStdout:'3-4-5',isSample:true},{stdin:'a b',expectedStdout:'1-1',isSample:true},{stdin:'solo',expectedStdout:'4'},{stdin:'one   two',expectedStdout:'3-3'},{stdin:'x    yy zzz',expectedStdout:'1-2-3'}] }),
  p({ ...base, slug:'remove-mirrored-pairs-repeatedly', title:'Erase Adjacent Mirror Pairs', difficulty:'HARD', patternTags:['strings','stack','reduction'], signatureId:'fn:string->string', avgSolveSeconds:900,
    promptMarkdown:['Repeatedly erase adjacent equal letters, ignoring case, until no such pair remains. Preserve the original spelling of letters that survive.','', '**Example**', '', '```', 'input:  aBbAc', 'output: c', '```', '', 'Removing `Bb` exposes `aA`, which also disappears. The non-empty line contains English letters only.'].join('\n'), editorialMarkdown:['## Case-folded reduction stack','','A stack cancels a matching folded top and models newly exposed neighbours immediately. One deletion pass misses later pairs. The quiet mistake is not revisiting an exposed boundary. Time is O(n), bounded by line length; space is O(n).'].join('\n'),
    referenceSolution:six('function solve(a){let s=[];for(let x of a){if(s.length&&s[s.length-1].toLowerCase()==x.toLowerCase())s.pop();else s.push(x)}return s.join("");}','function solve(a:string):string{const s:string[]=[];for(const x of a){if(s.length&&s[s.length-1].toLowerCase()===x.toLowerCase())s.pop();else s.push(x);}return s.join(\'\');}','def solve(a):\n    s=[]\n    for x in a:\n        if s and s[-1].lower()==x.lower(): s.pop()\n        else: s.append(x)\n    return "".join(s)','    static String solve(String a) { StringBuilder s=new StringBuilder();for(char x:a.toCharArray()){int n=s.length();if(n>0&&Character.toLowerCase(s.charAt(n-1))==Character.toLowerCase(x))s.deleteCharAt(n-1);else s.append(x);}return s.toString(); }','string solve(string a) { string s;for(char x:a){char y=(x>=\'A\'&&x<=\'Z\')?x+32:x;if(!s.empty()){char z=s.back();z=(z>=\'A\'&&z<=\'Z\')?z+32:z;if(y==z){s.pop_back();continue;}}s+=x;}return s; }','func solve(a string) string { s:=[]byte{};for i:=0;i<len(a);i++{x:=a[i];y:=x;if y>=\'A\'&&y<=\'Z\'{y+=32};if len(s)>0{z:=s[len(s)-1];if z>=\'A\'&&z<=\'Z\'{z+=32};if y==z{s=s[:len(s)-1];continue}};s=append(s,x)};return string(s) }'),
    tests:[{stdin:'aBbAc',expectedStdout:'c',isSample:true},{stdin:'aA',expectedStdout:'',isSample:true},{stdin:'abBA',expectedStdout:''},{stdin:'abc',expectedStdout:'abc'},{stdin:'aabCCbA',expectedStdout:'A'}] }),
  p({ ...base, slug:'longest-palindrome-token-tiebreak', title:'Longest Mirror Word, Earliest Tie', difficulty:'MEDIUM', patternTags:['strings','palindrome','tokenising'], signatureId:'fn:strings->string', avgSolveSeconds:720,
    promptMarkdown:['From a non-empty lowercase word list, return the longest palindrome. If several have that length, return the earliest input word.','', '**Example**', '', '```', 'input:  cat level noon dog', 'output: level', '```', '', 'A one-letter word is a palindrome. At least one palindrome is guaranteed.'].join('\n'), editorialMarkdown:['## Stable best palindrome scan','','Compare each word with its reverse and replace the best only on a strict length gain. Strictness retains the earliest tie. The quiet mistake is using `>=`. Time is O(total characters), bounded by token lengths; space is O(1) beyond reversal work.'].join('\n'),
    referenceSolution:six('function solve(a){let b="";for(let x of a)if(x==[...x].reverse().join("")&&x.length>b.length)b=x;return b;}','function solve(a:string[]):string{let b="";for(const x of a)if(x===x.split(\'\').reverse().join(\'\')&&x.length>b.length)b=x;return b;}','def solve(a):\n    best=""\n    for x in a:\n        if x==x[::-1] and len(x)>len(best): best=x\n    return best','    static String solve(String[] a) { String b="";for(String x:a){String r=new StringBuilder(x).reverse().toString();if(x.equals(r)&&x.length()>b.length())b=x;}return b; }','string solve(vector<string> a) { string b;for(string x:a){bool ok=true;for(int l=0,r=(int)x.size()-1;l<r;l++,r--)if(x[l]!=x[r])ok=false;if(ok&&x.size()>b.size())b=x;}return b; }','func solve(a []string) string { b:=\"\";for _,x:=range a{ok:=true;for l,r:=0,len(x)-1;l<r;l,r=l+1,r-1{if x[l]!=x[r]{ok=false}};if ok&&len(x)>len(b){b=x}};return b }'),
    tests:[{stdin:'cat level noon dog',expectedStdout:'level',isSample:true},{stdin:'a bb c',expectedStdout:'bb',isSample:true},{stdin:'aba xyz cdc',expectedStdout:'aba'},{stdin:'x',expectedStdout:'x'},{stdin:'noon racecar level',expectedStdout:'racecar'}] }),
  p({ ...base, slug:'bracket-repeat-decoder', title:'Decode Nested Repeat Notes', difficulty:'HARD', patternTags:['strings','stack','parsing'], signatureId:'fn:string->string', avgSolveSeconds:1020,
    promptMarkdown:['Decode a non-empty expression of lowercase letters and `k[part]`, where `part` may contain nested expressions and `k` is a positive decimal number.','', '**Example**', '', '```', 'input:  2[a3[b]]', 'output: abbbabbb', '```', '', 'Every bracket is matched. Literal letters outside brackets remain once; decoded output is at most 1,000 characters.'].join('\n'), editorialMarkdown:['## Nested repeat frames','','Push the unfinished prefix and multiplier at each opening bracket; closing a frame expands its current segment into that prefix. A global multiplier cannot represent nesting. The quiet mistake is not resetting digits after `[`. Time is O(output length), bounded by 1,000; space is O(depth plus output).'].join('\n'),
    referenceSolution:six('function solve(a){let s=[],cur="",n=0;for(let x of a){if(/\\d/.test(x))n=n*10+ +x;else if(x==="["){s.push([cur,n]);cur="";n=0}else if(x==="]"){let [p,k]=s.pop();cur=p+cur.repeat(k)}else cur+=x}return cur;}','function solve(a:string):string{const s:[string,number][]=[];let cur="",n=0;for(const x of a){if(/\\d/.test(x))n=n*10+Number(x);else if(x===\'[\'){s.push([cur,n]);cur="";n=0;}else if(x===\']\'){const [p,k]=s.pop()!;cur=p+cur.repeat(k);}else cur+=x;}return cur;}','def solve(a):\n    s=[]; cur=\"\"; n=0\n    for x in a:\n        if x.isdigit(): n=n*10+int(x)\n        elif x=="[": s.append((cur,n));cur=\"\";n=0\n        elif x=="]": p,k=s.pop();cur=p+cur*k\n        else: cur+=x\n    return cur','    static String solve(String a) { java.util.Stack<String> p=new java.util.Stack<>();java.util.Stack<Integer> k=new java.util.Stack<>();String cur="";int n=0;for(char x:a.toCharArray()){if(Character.isDigit(x))n=n*10+x-\'0\';else if(x==\'[\'){p.push(cur);k.push(n);cur="";n=0;}else if(x==\']\')cur=p.pop()+cur.repeat(k.pop());else cur+=x;}return cur; }','string solve(string a) { vector<string>p;vector<int>k;string cur;int n=0;for(char x:a){if(x>=\'0\'&&x<=\'9\')n=n*10+x-\'0\';else if(x==\'[\'){p.push_back(cur);k.push_back(n);cur="";n=0;}else if(x==\']\'){string z;for(int i=0;i<k.back();i++)z+=cur;cur=p.back()+z;p.pop_back();k.pop_back();}else cur+=x;}return cur; }','func solve(a string) string { p:=[]string{};k:=[]int{};cur:=\"\";n:=0;for _,x:=range a{if x>=\'0\'&&x<=\'9\'{n=n*10+int(x-\'0\')}else if x==\'[\'{p=append(p,cur);k=append(k,n);cur=\"\";n=0}else if x==\']\'{z:=\"\";for i:=0;i<k[len(k)-1];i++{z+=cur};cur=p[len(p)-1]+z;p=p[:len(p)-1];k=k[:len(k)-1]}else{cur+=string(x)}};return cur }'),
    tests:[{stdin:'2[a3[b]]',expectedStdout:'abbbabbb',isSample:true},{stdin:'3[ab]c',expectedStdout:'abababc',isSample:true},{stdin:'10[x]',expectedStdout:'xxxxxxxxxx'},{stdin:'a2[b]c',expectedStdout:'abbc'},{stdin:'2[a2[b]c]',expectedStdout:'abbcabbc'}] }),
];
