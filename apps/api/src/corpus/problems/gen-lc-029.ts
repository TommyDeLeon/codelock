import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-029` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_029_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "in-order-server-ids",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "TREES",
    title: "Sequential Task Execution",
    patternTags: ["tree","trees","in-order","recursion"],
    signatureId: "fn:tree->ints",
    avgSolveSeconds: 600,
    promptMarkdown: "You are managing a complex project where tasks are structured as a binary tree. Each task is assigned an integer `val`. To execute the project successfully, the tasks must be resolved following an in-order traversal (resolving the left subtree, then the current task, and finally the right subtree).\n\nGiven the `root` of the binary tree, return an array containing the task `val`s in the order they are resolved.\n\n**Constraints**\n- The number of nodes in the tree is in the range `[0, 100]`.\n- `-10^4 <= val <= 10^4`\n\n**Example 1**\n```\ninput:\n1 null 2 3\noutput: 1 3 2\n```\nExplanation: The root task is 1, with no left child, and a right child of 2. The left child of 2 is 3. The in-order resolution processes task 1, followed by the left child of 2 (which is 3), and then task 2.\n\n**Example 2**\n```\ninput:\n42\noutput: 42\n```\nExplanation: The project consists of a single task.\n\n**Example 3**\n```\ninput:\n1 2 null 3 null 4\noutput: 4 3 2 1\n```\nExplanation: The project structure is entirely left-heavy. The deepest left task is 4, leading to the resolution sequence 4, 3, 2, 1.\n\n**Follow-up:** Can you solve it iteratively instead of recursively?",
    editorialMarkdown: "## In-Order Server IDs\n\nTo collect the server IDs in-order, we can perform a standard Depth-First Search (DFS) on the binary tree. We visit the left subtree, record the current node's value, and then visit the right subtree.\n\nThis can be implemented either recursively or iteratively using a stack. The recursive approach is generally simpler to write. We simply pass a list or array along with our recursive calls (or append to a globally scoped list), and append the current node's value between the left and right recursive calls.\n\n**Complexity:**\n- **Time Complexity:** \\\\(\\\\mathcal{O}(N)\\\\) where \\\\(N\\\\) is the number of nodes in the tree, because we visit each node exactly once.\n- **Space Complexity:** \\\\(\\\\mathcal{O}(N)\\\\) to store the result array, plus \\\\(\\\\mathcal{O}(H)\\\\) for the recursion stack where \\\\(H\\\\) is the height of the tree.\n\n**Common Trap:**\nA common mistake is not properly checking for `null` nodes before making recursive calls, which can lead to null pointer exceptions. Always ensure your base case handles empty subtrees correctly.",
    referenceSolution: {
      JAVASCRIPT: "function solve(root) {\n    let res = [];\n    function dfs(node) {\n        if (!node) return;\n        dfs(node.left);\n        res.push(node.val);\n        dfs(node.right);\n    }\n    dfs(root);\n    return res;\n}",
      TYPESCRIPT: "function solve(root: any): number[] {\n    let res: number[] = [];\n    function dfs(node: any) {\n        if (!node) return;\n        dfs(node.left);\n        res.push(node.val);\n        dfs(node.right);\n    }\n    dfs(root);\n    return res;\n}",
      PYTHON: "def solve(root):\n    res = []\n    def dfs(node):\n        if not node:\n            return\n        dfs(node.left)\n        res.append(node.val)\n        dfs(node.right)\n    dfs(root)\n    return res",
      JAVA: "    static int[] solve(TreeNode root) {\n        List<Integer> list = new ArrayList<>();\n        dfs(root, list);\n        int[] res = new int[list.size()];\n        for(int i = 0; i < list.size(); i++) res[i] = list.get(i);\n        return res;\n    }\n    static void dfs(TreeNode node, List<Integer> list) {\n        if (node == null) return;\n        dfs(node.left, list);\n        list.add(node.val);\n        dfs(node.right, list);\n    }",
      CPP: "vector<int> solve(TreeNode* root) {\n    vector<int> res;\n    auto dfs = [&](auto& self, TreeNode* node) -> void {\n        if (!node) return;\n        self(self, node->left);\n        res.push_back(node->val);\n        self(self, node->right);\n    };\n    dfs(dfs, root);\n    return res;\n}",
      GO: "func solve(root *TreeNode) []int {\n    var res []int\n    var dfs func(*TreeNode)\n    dfs = func(node *TreeNode) {\n        if node == nil {\n            return\n        }\n        dfs(node.Left)\n        res = append(res, node.Val)\n        dfs(node.Right)\n    }\n    dfs(root)\n    return res\n}",
    },
    tests: [
      { stdin: "1 null 2 3", expectedStdout: "1 3 2", isSample: true },
      { stdin: "42", expectedStdout: "42", isSample: true },
      { stdin: "", expectedStdout: "" },
      { stdin: "1 2 3", expectedStdout: "2 1 3" },
      { stdin: "1 2 3 4 5", expectedStdout: "4 2 5 1 3" },
      { stdin: "-10 -5 15 2 -7", expectedStdout: "2 -5 -7 -10 15" },
      { stdin: "1 2 null 3 null 4", expectedStdout: "4 3 2 1" },
      { stdin: "1 null 2 null 3 null 4", expectedStdout: "1 2 3 4" },
      { stdin: "2 2 2", expectedStdout: "2 2 2" },
    ],
  }),

  p({
    ...base,
    slug: "split-power-cell",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Distribute Cargo Weight",
    patternTags: ["math","brute-force","digits"],
    signatureId: "fn:int->ints",
    avgSolveSeconds: 400,
    promptMarkdown: "You are organizing a logistics operation where a main cargo shipment has a total `capacity`. This weight must be divided entirely between two transport vehicles such that their combined weight equals `capacity`. Due to a software limitation in the weighing system, neither vehicle's assigned weight can contain the digit `0` in its base-10 representation.\n\nGiven an integer `capacity`, return an array of two integers `[A, B]` where `A + B == capacity` and neither `A` nor `B` contains the digit `0`. Return the pair `[A, B]` where `A` is the smallest possible valid integer greater than or equal to 1.\n\n**Constraints**\n- `2 <= capacity <= 10^4`\n\n**Example 1**\n```\ninput:\n2\noutput: 1 1\n```\nExplanation: The only possible division for 2 is 1 + 1. Neither part includes a 0.\n\n**Example 2**\n```\ninput:\n11\noutput: 2 9\n```\nExplanation: 1 + 10 is invalid because 10 has a 0. The smallest valid division is 2 + 9.\n\n**Example 3**\n```\ninput:\n10000\noutput: 1 9999\n```\nExplanation: The weights 1 and 9999 sum to 10000 and neither features the digit 0.\n\n**Follow-up:** Could you solve this without converting the integers to strings?",
    editorialMarkdown: "## Split Power Cell\n\nThe problem requires us to find two positive integers `A` and `B` such that their sum equals the `capacity`, and neither contains the digit `0`.\n\nSince `capacity` is relatively small (up to 10,000), a brute-force approach is perfectly viable. We can iterate `i` from `1` to `capacity - 1`. For each `i`, we calculate the complement `j = capacity - i`. We then convert both `i` and `j` to strings and check if either contains the character `'0'`. The first pair that satisfies this condition can be returned immediately.\n\n**Complexity:**\n- **Time Complexity:** \\\\(\\\\mathcal{O}(N \\\\log N)\\\\) in the worst case, as string conversion takes logarithmic time with respect to the number's magnitude, and we may test up to \\\\(N\\\\) pairs.\n- **Space Complexity:** \\\\(\\\\mathcal{O}(\\\\log N)\\\\) auxiliary space for the string representations of the numbers.\n\n**Common Trap:**\nForgetting to check the condition on both numbers, or mistakenly using integer division/modulo incorrectly instead of robust string conversion, which can miss zeroes in the middle of a number.",
    referenceSolution: {
      JAVASCRIPT: "function solve(capacity) {\n    for (let i = 1; i < capacity; i++) {\n        let j = capacity - i;\n        if (!String(i).includes('0') && !String(j).includes('0')) {\n            return [i, j];\n        }\n    }\n    return [];\n}",
      TYPESCRIPT: "function solve(capacity: number): number[] {\n    for (let i = 1; i < capacity; i++) {\n        let j = capacity - i;\n        if (!String(i).includes('0') && !String(j).includes('0')) {\n            return [i, j];\n        }\n    }\n    return [];\n}",
      PYTHON: "def solve(capacity):\n    for i in range(1, capacity):\n        j = capacity - i\n        if '0' not in str(i) and '0' not in str(j):\n            return [i, j]\n    return []",
      JAVA: "    static int[] solve(int capacity) {\n        for (int i = 1; i < capacity; i++) {\n            int j = capacity - i;\n            if (!String.valueOf(i).contains(\"0\") && !String.valueOf(j).contains(\"0\")) {\n                return new int[]{i, j};\n            }\n        }\n        return new int[]{};\n    }",
      CPP: "vector<int> solve(int capacity) {\n    for (int i = 1; i < capacity; i++) {\n        int j = capacity - i;\n        if (to_string(i).find('0') == string::npos && to_string(j).find('0') == string::npos) {\n            return {i, j};\n        }\n    }\n    return {};\n}",
      GO: "func solve(capacity int) []int {\n    for i := 1; i < capacity; i++ {\n        j := capacity - i\n        if !strings.Contains(strconv.Itoa(i), \"0\") && !strings.Contains(strconv.Itoa(j), \"0\") {\n            return []int{i, j}\n        }\n    }\n    return []int{}\n}",
    },
    tests: [
      { stdin: "2", expectedStdout: "1 1", isSample: true },
      { stdin: "11", expectedStdout: "2 9", isSample: true },
      { stdin: "10000", expectedStdout: "1 9999" },
      { stdin: "10", expectedStdout: "1 9" },
      { stdin: "100", expectedStdout: "1 99" },
      { stdin: "1010", expectedStdout: "11 999" },
      { stdin: "3", expectedStdout: "1 2" },
      { stdin: "99", expectedStdout: "1 98" },
      { stdin: "1000", expectedStdout: "1 999" },
    ],
  }),

  p({
    ...base,
    slug: "balance-the-communication-tree",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "TREES",
    title: "Reconstruct Database Index",
    patternTags: ["tree","binary-search-tree","balancing","recursion"],
    signatureId: "fn:tree->tree",
    avgSolveSeconds: 800,
    promptMarkdown: "A database system uses a Binary Search Tree (BST) for its indexing, but sequential writes have caused the tree to lose its efficiency and become skewed.\n\nYou are given the `root` of this degraded BST. Your objective is to return the root of a **height-balanced** BST that contains exactly the same node values. A BST is considered height-balanced if the depth of the two subtrees for any node never differs by more than 1.\n\nSince there can be multiple valid height-balanced trees, you must construct it using the exact same strategy as picking the middle element of the sorted values for each subtree to act as the root.\n\n**Constraints**\n- The number of nodes in the tree is in the range `[1, 10^4]`.\n- `-10^5 <= val <= 10^5`\n\n**Example 1**\n```\ninput:\n1 null 2 null 3 null 4\noutput: 2 1 3 null null null 4\n```\nExplanation: The input is a highly unbalanced right-skewed tree. The sorted values are [1, 2, 3, 4]. The middle element for the whole array is 2 (index 1). The left half is [1], the right half is [3, 4]. The right half's middle is 3. The resulting tree is balanced.\n\n**Example 2**\n```\ninput:\n2 1 3\noutput: 2 1 3\n```\nExplanation: The input tree is already perfectly height-balanced.\n\n**Example 3**\n```\ninput:\n4 3 null 2 null 1\noutput: 2 1 3 null null null 4\n```\nExplanation: The input is a left-skewed tree. We rebuild it using the exact same middle-picking strategy, resulting in a balanced configuration.\n\n**Follow-up:** Can you balance the tree by reusing the existing nodes instead of creating new ones?",
    editorialMarkdown: "## Balance the Communication Tree\n\nThe task is to transform a potentially unbalanced Binary Search Tree (BST) into a perfectly height-balanced BST. Since the input is already a BST, its in-order traversal will naturally give us the nodes in sorted order.\n\nFirst, we perform an in-order traversal of the input tree and store the nodes in an array. Once we have the sorted array of nodes, we can build a height-balanced BST using a divide-and-conquer approach: we pick the middle element of the array to be the root, recursively build the left subtree from the left half of the array, and recursively build the right subtree from the right half of the array. By mutating and reusing the existing nodes, we avoid the overhead of allocating new objects.\n\n**Complexity:**\n- **Time Complexity:** \\\\(\\\\mathcal{O}(N)\\\\), where \\\\(N\\\\) is the number of nodes in the tree. We visit each node once during the in-order traversal and once during the tree construction.\n- **Space Complexity:** \\\\(\\\\mathcal{O}(N)\\\\) to store the nodes in an array, plus \\\\(\\\\mathcal{O}(\\\\log N)\\\\) implicit stack space for the recursive construction.\n\n**Common Trap:**\nAttempting to perform a complex rotation-based balancing algorithm (like AVL or Red-Black trees) is overkill here. Extracting the sorted nodes and rebuilding the tree from scratch is simpler and achieves optimal bounds.",
    referenceSolution: {
      JAVASCRIPT: "function solve(root) {\n    let nodes = [];\n    function inorder(node) {\n        if (!node) return;\n        inorder(node.left);\n        nodes.push(node);\n        inorder(node.right);\n    }\n    inorder(root);\n    function build(l, r) {\n        if (l > r) return null;\n        let mid = Math.floor((l + r) / 2);\n        let node = nodes[mid];\n        node.left = build(l, mid - 1);\n        node.right = build(mid + 1, r);\n        return node;\n    }\n    return build(0, nodes.length - 1);\n}",
      TYPESCRIPT: "function solve(root: any): any {\n    let nodes: any[] = [];\n    function inorder(node: any) {\n        if (!node) return;\n        inorder(node.left);\n        nodes.push(node);\n        inorder(node.right);\n    }\n    inorder(root);\n    function build(l: number, r: number): any {\n        if (l > r) return null;\n        let mid = Math.floor((l + r) / 2);\n        let node = nodes[mid];\n        node.left = build(l, mid - 1);\n        node.right = build(mid + 1, r);\n        return node;\n    }\n    return build(0, nodes.length - 1);\n}",
      PYTHON: "def solve(root):\n    nodes = []\n    def inorder(node):\n        if not node:\n            return\n        inorder(node.left)\n        nodes.append(node)\n        inorder(node.right)\n    inorder(root)\n    def build(l, r):\n        if l > r:\n            return None\n        mid = (l + r) // 2\n        node = nodes[mid]\n        node.left = build(l, mid - 1)\n        node.right = build(mid + 1, r)\n        return node\n    return build(0, len(nodes) - 1)",
      JAVA: "    static TreeNode solve(TreeNode root) {\n        List<TreeNode> nodes = new ArrayList<>();\n        inorder(root, nodes);\n        return build(nodes, 0, nodes.size() - 1);\n    }\n    static void inorder(TreeNode node, List<TreeNode> nodes) {\n        if (node == null) return;\n        inorder(node.left, nodes);\n        nodes.add(node);\n        inorder(node.right, nodes);\n    }\n    static TreeNode build(List<TreeNode> nodes, int l, int r) {\n        if (l > r) return null;\n        int mid = l + (r - l) / 2;\n        TreeNode node = nodes.get(mid);\n        node.left = build(nodes, l, mid - 1);\n        node.right = build(nodes, mid + 1, r);\n        return node;\n    }",
      CPP: "TreeNode* solve(TreeNode* root) {\n    vector<TreeNode*> nodes;\n    auto inorder = [&](auto& self, TreeNode* node) -> void {\n        if (!node) return;\n        self(self, node->left);\n        nodes.push_back(node);\n        self(self, node->right);\n    };\n    inorder(inorder, root);\n    auto build = [&](auto& self, int l, int r) -> TreeNode* {\n        if (l > r) return nullptr;\n        int mid = l + (r - l) / 2;\n        TreeNode* node = nodes[mid];\n        node->left = self(self, l, mid - 1);\n        node->right = self(self, mid + 1, r);\n        return node;\n    };\n    return build(build, 0, (int)nodes.size() - 1);\n}",
      GO: "func solve(root *TreeNode) *TreeNode {\n    var nodes []*TreeNode\n    var inorder func(*TreeNode)\n    inorder = func(node *TreeNode) {\n        if node == nil {\n            return\n        }\n        inorder(node.Left)\n        nodes = append(nodes, node)\n        inorder(node.Right)\n    }\n    inorder(root)\n    var build func(int, int) *TreeNode\n    build = func(l, r int) *TreeNode {\n        if l > r {\n            return nil\n        }\n        mid := l + (r - l) / 2\n        node := nodes[mid]\n        node.Left = build(l, mid - 1)\n        node.Right = build(mid + 1, r)\n        return node\n    }\n    return build(0, len(nodes)-1)\n}",
    },
    tests: [
      { stdin: "1 null 2 null 3 null 4", expectedStdout: "2 1 3 null null null 4", isSample: true },
      { stdin: "2 1 3", expectedStdout: "2 1 3", isSample: true },
      { stdin: "10 5 15", expectedStdout: "10 5 15" },
      { stdin: "1", expectedStdout: "1" },
      { stdin: "1 null 2", expectedStdout: "1 null 2" },
      { stdin: "-10 -20 null -30", expectedStdout: "-20 -30 -10" },
      { stdin: "4 3 null 2 null 1", expectedStdout: "2 1 3 null null null 4" },
      { stdin: "5 4 6 3 null null 7", expectedStdout: "5 3 6 null 4 null 7" },
    ],
  }),

  p({
    ...base,
    slug: "convert-time-units",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Convert Time Units",
    patternTags: ["math","integer-arithmetic"],
    signatureId: "fn:int->ints",
    avgSolveSeconds: 300,
    promptMarkdown: "You are building a timekeeping module for a deep-space station. The crew operates on a schedule measured in Earth `weeks`, but different subsystems require this duration in smaller units.\n\nGiven an integer `weeks`, return an array of two integers: the first being the equivalent number of `days`, and the second being the equivalent number of `hours`.\n\n**Constraints**\n- `0 <= weeks <= 1000`\n\n**Example 1**\n```\ninput:\n1\noutput: 7 168\n```\nExplanation: 1 week is 7 days, and 7 days is 7 * 24 = 168 hours.\n\n**Example 2**\n```\ninput:\n0\noutput: 0 0\n```\nExplanation: 0 weeks is 0 days and 0 hours.\n\n**Example 3**\n```\ninput:\n2\noutput: 14 336\n```\nExplanation: 2 weeks is 14 days and 336 hours.\n\n**Follow-up:** Could you store the conversion factors as named constants for better readability?",
    editorialMarkdown: "## Convert Time Units\n\nThis is a simple arithmetic problem. To convert weeks to days, you multiply by 7 (since there are 7 days in a week). To find the total hours, you multiply the days by 24 (since there are 24 hours in a day).\n\nBoth results are returned in an array. This checks basic understanding of arithmetic operators and array instantiation.\n\n**Complexity:**\n- **Time Complexity:** \\\\(\\\\mathcal{O}(1)\\\\) since we are performing only two arithmetic multiplications.\n- **Space Complexity:** \\\\(\\\\mathcal{O}(1)\\\\) as we only allocate a small array of constant size to hold the result.\n\n**Common Trap:**\nOvercomplicating the math or miscalculating the constants (e.g., multiplying by wrong conversion factors).",
    referenceSolution: {
      JAVASCRIPT: "function solve(weeks) {\n    return [weeks * 7, weeks * 7 * 24];\n}",
      TYPESCRIPT: "function solve(weeks: number): number[] {\n    return [weeks * 7, weeks * 7 * 24];\n}",
      PYTHON: "def solve(weeks):\n    return [weeks * 7, weeks * 7 * 24]",
      JAVA: "    static int[] solve(int weeks) {\n        return new int[]{weeks * 7, weeks * 7 * 24};\n    }",
      CPP: "vector<int> solve(int weeks) {\n    return {weeks * 7, weeks * 7 * 24};\n}",
      GO: "func solve(weeks int) []int {\n    return []int{weeks * 7, weeks * 7 * 24}\n}",
    },
    tests: [
      { stdin: "1", expectedStdout: "7 168", isSample: true },
      { stdin: "0", expectedStdout: "0 0", isSample: true },
      { stdin: "2", expectedStdout: "14 336" },
      { stdin: "10", expectedStdout: "70 1680" },
      { stdin: "52", expectedStdout: "364 8736" },
      { stdin: "100", expectedStdout: "700 16800" },
      { stdin: "5", expectedStdout: "35 840" },
      { stdin: "1000", expectedStdout: "7000 168000" },
    ],
  }),

  p({
    ...base,
    slug: "count-error-codes",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Tally Failed Transactions",
    patternTags: ["strings","array","counting"],
    signatureId: "fn:strings->int",
    avgSolveSeconds: 400,
    promptMarkdown: "You are reviewing a list of transaction `logs` generated by a payment gateway. You need to determine how many times a specific critical failure was recorded.\n\nGiven an array of strings `logs`, return the total number of times the exact string `ERROR` appears in the array.\n\n**Constraints**\n- `0 <= logs.length <= 100`\n- `1 <= logs[i].length <= 20`\n- The strings consist only of uppercase and lowercase English letters.\n\n**Example 1**\n```\ninput:\nINFO ERROR WARN ERROR OK\noutput: 2\n```\nExplanation: The exact string ERROR appears exactly twice in the sequence.\n\n**Example 2**\n```\ninput:\nERROR\noutput: 1\n```\nExplanation: The array contains a single ERROR status.\n\n**Example 3**\n```\ninput:\nOK OK OK\noutput: 0\n```\nExplanation: There are no ERROR strings present in the array.\n\n**Follow-up:** Can you solve this using a one-liner built-in function in your language of choice?",
    editorialMarkdown: "## Count Error Codes\n\nThis is a classic counting problem. We need to find how many times the specific string `\\\"ERROR\\\"` appears in a given array of strings.\n\nWe can solve this by iterating through the `logs` array, comparing each string against our target string `\\\"ERROR\\\"`. If they match, we increment a counter. At the end of the loop, we return the counter.\n\n**Complexity:**\n- **Time Complexity:** \\\\(\\\\mathcal{O}(N \\\times L)\\\\) where \\\\(N\\\\) is the number of elements in the array and \\\\(L\\\\) is the maximum length of a string, as we compare the strings directly.\n- **Space Complexity:** \\\\(\\\\mathcal{O}(1)\\\\) because we only need a single counter variable.\n\n**Common Trap:**\nIn languages like Java, using the `==` operator instead of `.equals()` when comparing strings. The `==` operator checks if they point to the same memory reference, not if their contents match.",
    referenceSolution: {
      JAVASCRIPT: "function solve(logs) {\n    let count = 0;\n    for (let i = 0; i < logs.length; i++) {\n        if (logs[i] === \"ERROR\") count++;\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(logs: string[]): number {\n    let count = 0;\n    for (let i = 0; i < logs.length; i++) {\n        if (logs[i] === \"ERROR\") count++;\n    }\n    return count;\n}",
      PYTHON: "def solve(logs):\n    return logs.count(\"ERROR\")",
      JAVA: "    static int solve(String[] logs) {\n        int count = 0;\n        for (String log : logs) {\n            if (log.equals(\"ERROR\")) count++;\n        }\n        return count;\n    }",
      CPP: "int solve(vector<string> logs) {\n    int count = 0;\n    for (const auto& log : logs) {\n        if (log == \"ERROR\") count++;\n    }\n    return count;\n}",
      GO: "func solve(logs []string) int {\n    count := 0\n    for _, log := range logs {\n        if log == \"ERROR\" {\n            count++\n        }\n    }\n    return count\n}",
    },
    tests: [
      { stdin: "INFO ERROR WARN ERROR OK", expectedStdout: "2", isSample: true },
      { stdin: "ERROR", expectedStdout: "1", isSample: true },
      { stdin: "OK OK OK", expectedStdout: "0" },
      { stdin: "ERROR ERROR ERROR ERROR", expectedStdout: "4" },
      { stdin: "", expectedStdout: "0" },
      { stdin: "ERR ERRORS ERROR", expectedStdout: "1" },
      { stdin: "a b c d e f", expectedStdout: "0" },
      { stdin: "ERROR error Error ERROR", expectedStdout: "2" },
      { stdin: "OK ERROR OK ERROR OK ERROR", expectedStdout: "3" },
    ],
  }),

  p({
    ...base,
    slug: "count-exposed-flags",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Find Unformatted Headings",
    patternTags: ["string","counting","simulation"],
    signatureId: "fn:string->int",
    avgSolveSeconds: 450,
    promptMarkdown: "You are building a custom markup parser that processes a string `packet`. In this format, the `#` symbol is used to denote a heading. However, any text enclosed within a pair of `|` characters is treated as raw unformatted text, and any `#` symbols within these pairs must be ignored.\n\nGiven a string `packet`, return the number of `#` characters that are **not** enclosed between any pair of `|` characters. You can assume that the `|` characters always appear in valid pairs (the first pairs with the second, the third with the fourth, and so on).\n\n**Constraints**\n- `1 <= packet.length <= 1000`\n- `packet` consists of English letters, `#`, and `|`.\n- The number of `|` characters in `packet` is even.\n\n**Example 1**\n```\ninput:\nlog#error|encrypted#data|more#logs\noutput: 2\n```\nExplanation: The `#` in `log#error` and `more#logs` are counted. The `#` inside `|encrypted#data|` is ignored.\n\n**Example 2**\n```\ninput:\nsafe|#|safe\noutput: 0\n```\nExplanation: The only `#` is enclosed in `|`, so it is ignored.\n\n**Example 3**\n```\ninput:\n###\noutput: 3\n```\nExplanation: There are no `|` characters, so all three `#` are counted.\n\n**Follow-up:** Can you solve this in a single pass without using built-in splitting functions?",
    editorialMarkdown: "## Count Exposed Flags\n\nThe problem requires us to iterate through a string and count a specific character (`#`), while maintaining a state that determines whether we should ignore occurrences based on being enclosed in `|`.\n\nWe can solve this by iterating character by character. We keep a boolean flag `inside` initialized to `false`. Whenever we encounter a `|` character, we toggle this boolean flag (`inside = !inside`). If we encounter a `#` character, we only increment our counter if `inside` is `false`. Since the constraints guarantee that `|` characters appear in pairs, the state logic will correctly cover all encryptions.\n\n**Complexity:**\n- **Time Complexity:** \\\\(\\\\mathcal{O}(N)\\\\), where \\\\(N\\\\) is the length of the string, since we make a single pass through the characters.\n- **Space Complexity:** \\\\(\\\\mathcal{O}(1)\\\\) as we only use a counter and a boolean flag.\n\n**Common Trap:**\nForgetting to toggle the boolean state correctly, or improperly handling consecutive `|` characters without any text in between.",
    referenceSolution: {
      JAVASCRIPT: "function solve(packet) {\n    let count = 0;\n    let inside = false;\n    for (let i = 0; i < packet.length; i++) {\n        if (packet[i] === '|') {\n            inside = !inside;\n        } else if (packet[i] === '#' && !inside) {\n            count++;\n        }\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(packet: string): number {\n    let count = 0;\n    let inside = false;\n    for (let i = 0; i < packet.length; i++) {\n        if (packet[i] === '|') {\n            inside = !inside;\n        } else if (packet[i] === '#' && !inside) {\n            count++;\n        }\n    }\n    return count;\n}",
      PYTHON: "def solve(packet):\n    count = 0\n    inside = False\n    for char in packet:\n        if char == '|':\n            inside = not inside\n        elif char == '#' and not inside:\n            count += 1\n    return count",
      JAVA: "    static int solve(String packet) {\n        int count = 0;\n        boolean inside = false;\n        for (int i = 0; i < packet.length(); i++) {\n            char c = packet.charAt(i);\n            if (c == '|') {\n                inside = !inside;\n            } else if (c == '#' && !inside) {\n                count++;\n            }\n        }\n        return count;\n    }",
      CPP: "int solve(string packet) {\n    int count = 0;\n    bool inside = false;\n    for (char c : packet) {\n        if (c == '|') {\n            inside = !inside;\n        } else if (c == '#' && !inside) {\n            count++;\n        }\n    }\n    return count;\n}",
      GO: "func solve(packet string) int {\n    count := 0\n    inside := false\n    for _, c := range packet {\n        if c == '|' {\n            inside = !inside\n        } else if c == '#' && !inside {\n            count++\n        }\n    }\n    return count\n}",
    },
    tests: [
      { stdin: "log#error|encrypted#data|more#logs", expectedStdout: "2", isSample: true },
      { stdin: "safe|#|safe", expectedStdout: "0", isSample: true },
      { stdin: "###", expectedStdout: "3" },
      { stdin: "|###|", expectedStdout: "0" },
      { stdin: "#|#|#|#|#", expectedStdout: "3" },
      { stdin: "no_flags_here", expectedStdout: "0" },
      { stdin: "||||||", expectedStdout: "0" },
      { stdin: "#", expectedStdout: "1" },
      { stdin: " ", expectedStdout: "0" },
    ],
  }),
];
