import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w1-050` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W1_050_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "detect-mirrored-sequence",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "DNA Pairing Capability",
    patternTags: ["string","substring","search"],
    signatureId: "fn:string->bool",
    avgSolveSeconds: 420,
    promptMarkdown: "A biologist is analyzing a DNA string, `signal`. A DNA string is \"pairing-capable\" if there exists any contiguous 2-nucleotide sequence in `signal` whose exact reverse also appears somewhere in `signal`. For example, if \"ab\" appears, then \"ba\" must also appear somewhere in the sequence. Return `true` if it contains such a sequence, otherwise `false`.\n\n**Constraints**\n- `2 <= signal.length <= 100`\n- `signal` consists of lowercase English letters.\n\n**Example 1**\n```\ninput:\nabccba\noutput: true\n```\n*Explanation: The sequence \"ab\" is present, and its reversed form \"ba\" is also present.*\n\n**Example 2**\n```\ninput:\nabcd\noutput: false\n```\n*Explanation: No 2-character sequence in the string has its exact reverse present.*\n\n**Example 3**\n```\ninput:\naba\noutput: true\n```\n*Explanation: Both \"ab\" and \"ba\" appear in the sequence.*\n\n**Follow-up**\nCan you solve this in mathcal{O}(N) time using a hash set?",
    editorialMarkdown: "## Detect Mirrored Sequence\nIterate through all 2-character substrings of the input string. For each substring, reverse it and check if it exists in the original string. You can use built-in string search functions or store all 2-character substrings in a hash set for faster lookup.\n\n**Trap**: Make sure to reverse the 2-character substring before checking for its existence, and don't assume the reversed substring has to be adjacent to the original.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N^2) with string search, or mathcal{O}(N) if using a hash set to store seen substrings.\n- **Space Complexity:** mathcal{O}(N) to store substrings in a set, or mathcal{O}(1) if using in-place string search.",
    referenceSolution: {
      JAVASCRIPT: "function solve(signal) {\n    for (let i = 0; i < signal.length - 1; i++) {\n        let sub = signal.substring(i, i + 2);\n        let rev = sub[1] + sub[0];\n        if (signal.includes(rev)) return true;\n    }\n    return false;\n}",
      TYPESCRIPT: "function solve(signal: string): boolean {\n    for (let i = 0; i < signal.length - 1; i++) {\n        let sub = signal.substring(i, i + 2);\n        let rev = sub[1] + sub[0];\n        if (signal.includes(rev)) return true;\n    }\n    return false;\n}",
      PYTHON: "def solve(signal):\n    for i in range(len(signal) - 1):\n        sub = signal[i:i+2]\n        if sub[::-1] in signal:\n            return True\n    return False",
      JAVA: "    static boolean solve(String signal) {\n        for (int i = 0; i < signal.length() - 1; i++) {\n            String sub = signal.substring(i, i + 2);\n            String rev = \"\" + sub.charAt(1) + sub.charAt(0);\n            if (signal.contains(rev)) return true;\n        }\n        return false;\n    }",
      CPP: "#include <vector>\n#include <string>\n#include <algorithm>\nusing namespace std;\n\nbool solve(string signal) {\n    for (int i = 0; i < signal.length() - 1; i++) {\n        string sub = signal.substr(i, 2);\n        string rev = sub;\n        reverse(rev.begin(), rev.end());\n        if (signal.find(rev) != string::npos) return true;\n    }\n    return false;\n}",
      GO: "func solve(signal string) bool {\n    for i := 0; i < len(signal)-1; i++ {\n        sub := signal[i : i+2]\n        rev := string(sub[1]) + string(sub[0])\n        if strings.Contains(signal, rev) {\n            return true\n        }\n    }\n    return false\n}",
    },
    tests: [
      { stdin: "abccba", expectedStdout: "true", isSample: true },
      { stdin: "abcd", expectedStdout: "false", isSample: true },
      { stdin: "aba", expectedStdout: "true", isSample: true },
      { stdin: "aa", expectedStdout: "true" },
      { stdin: "abcdefg", expectedStdout: "false" },
      { stdin: "xyyx", expectedStdout: "true" },
      { stdin: "xyzzyx", expectedStdout: "true" },
      { stdin: "ab", expectedStdout: "false" },
      { stdin: "zz", expectedStdout: "true" },
    ],
  }),

  p({
    ...base,
    slug: "thrust-multiplier-sequence",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Thrust Multiplier Sequence",
    patternTags: ["math","simulation","array"],
    signatureId: "fn:int->ints",
    avgSolveSeconds: 300,
    promptMarkdown: "A starship requires a sequence of power thrust multipliers to enter hyperspace. The i-th multiplier (1-indexed) is the factorial of i (i!). Given an integer `n`, return an array containing the first `n` thrust multipliers.\n\n**Constraints**\n- `1 <= n <= 10`\n\n**Example 1**\n```\ninput:\n3\noutput: 1 2 6\n```\n*Explanation: The first three factorials are 1! = 1, 2! = 2, and 3! = 6.*\n\n**Example 2**\n```\ninput:\n1\noutput: 1\n```\n*Explanation: The first factorial is 1! = 1.*\n\n**Example 3**\n```\ninput:\n5\noutput: 1 2 6 24 120\n```\n*Explanation: The first five factorials are 1, 2, 6, 24, and 120.*\n\n**Follow-up**\nCan you generate the sequence in mathcal{O}(N) time without recalculating the factorial from scratch for each element?",
    editorialMarkdown: "## Thrust Multiplier Sequence\nTo efficiently generate the sequence, maintain a running product. Loop from 1 to n, multiply the running product by the current loop index, and append it to the result array.\n\n**Trap**: Recalculating the factorial from scratch for each number leads to mathcal{O}(N^2) time complexity, which is inefficient. Use the previously computed factorial to compute the next one.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the given integer, since each multiplier takes mathcal{O}(1) to compute from the previous.\n- **Space Complexity:** mathcal{O}(N) to store the output array.",
    referenceSolution: {
      JAVASCRIPT: "function solve(n) {\n    let ans = [];\n    let p = 1;\n    for (let i = 1; i <= n; i++) {\n        p *= i;\n        ans.push(p);\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(n: number): number[] {\n    let ans: number[] = [];\n    let p = 1;\n    for (let i = 1; i <= n; i++) {\n        p *= i;\n        ans.push(p);\n    }\n    return ans;\n}",
      PYTHON: "def solve(n):\n    ans = []\n    p = 1\n    for i in range(1, n + 1):\n        p *= i\n        ans.append(p)\n    return ans",
      JAVA: "    static int[] solve(int n) {\n        int[] ans = new int[n];\n        int p = 1;\n        for (int i = 1; i <= n; i++) {\n            p *= i;\n            ans[i - 1] = p;\n        }\n        return ans;\n    }",
      CPP: "#include <vector>\nusing namespace std;\n\nvector<int> solve(int n) {\n    vector<int> ans;\n    int p = 1;\n    for (int i = 1; i <= n; i++) {\n        p *= i;\n        ans.push_back(p);\n    }\n    return ans;\n}",
      GO: "func solve(n int) []int {\n    var ans []int\n    p := 1\n    for i := 1; i <= n; i++ {\n        p *= i\n        ans = append(ans, p)\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "3", expectedStdout: "1 2 6", isSample: true },
      { stdin: "1", expectedStdout: "1", isSample: true },
      { stdin: "5", expectedStdout: "1 2 6 24 120", isSample: true },
      { stdin: "2", expectedStdout: "1 2" },
      { stdin: "4", expectedStdout: "1 2 6 24" },
      { stdin: "6", expectedStdout: "1 2 6 24 120 720" },
      { stdin: "7", expectedStdout: "1 2 6 24 120 720 5040" },
      { stdin: "10", expectedStdout: "1 2 6 24 120 720 5040 40320 362880 3628800" },
    ],
  }),

  p({
    ...base,
    slug: "drone-altitude-tracker",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Drone Altitude Tracker",
    patternTags: ["simulation","array","counting"],
    signatureId: "fn:strings->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A drone starts its flight at an altitude of 0. It receives a sequence of `commands`. The command `\"UP\"` increases the drone's altitude by 1, and the command `\"DOWN\"` decreases it by 1.\n\nReturn the final altitude of the drone after executing all commands sequentially.\n\n**Constraints**\n- `1 <= commands.length <= 100`\n- `commands[i]` is either `\"UP\"` or `\"DOWN\"`.\n\n**Example 1**\n```\ninput:\nUP UP DOWN\noutput: 1\n```\n*Explanation: Start at 0, go UP to 1, UP to 2, and DOWN to 1.*\n\n**Example 2**\n```\ninput:\nDOWN DOWN\noutput: -2\n```\n*Explanation: Start at 0, go DOWN to -1, DOWN to -2.*\n\n**Example 3**\n```\ninput:\nUP DOWN UP DOWN\noutput: 0\n```\n*Explanation: The UPS and DOWNS cancel each other out, ending at 0.*\n\n**Follow-up**\nCan you solve this with a single line of code inside the function?",
    editorialMarkdown: "## Drone Altitude Tracker\nInitialize a counter at 0. Loop through the array of commands. If the command is `\"UP\"`, increment the counter by 1. If the command is `\"DOWN\"`, decrement the counter by 1. Return the counter after the loop finishes.\n\n**Trap**: Do not reset the counter during the loop; it must persist across all commands.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the number of commands, since we iterate through the list exactly once.\n- **Space Complexity:** mathcal{O}(1) because we only use a single integer to track the altitude.",
    referenceSolution: {
      JAVASCRIPT: "function solve(commands) {\n    let ans = 0;\n    for (let c of commands) {\n        if (c === \"UP\") ans++;\n        else if (c === \"DOWN\") ans--;\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(commands: string[]): number {\n    let ans = 0;\n    for (let c of commands) {\n        if (c === \"UP\") ans++;\n        else if (c === \"DOWN\") ans--;\n    }\n    return ans;\n}",
      PYTHON: "def solve(commands):\n    ans = 0\n    for c in commands:\n        if c == \"UP\": ans += 1\n        elif c == \"DOWN\": ans -= 1\n    return ans",
      JAVA: "    static int solve(String[] commands) {\n        int ans = 0;\n        for (String c : commands) {\n            if (c.equals(\"UP\")) ans++;\n            else if (c.equals(\"DOWN\")) ans--;\n        }\n        return ans;\n    }",
      CPP: "#include <vector>\n#include <string>\nusing namespace std;\n\nint solve(vector<string> commands) {\n    int ans = 0;\n    for (string c : commands) {\n        if (c == \"UP\") ans++;\n        else if (c == \"DOWN\") ans--;\n    }\n    return ans;\n}",
      GO: "func solve(commands []string) int {\n    ans := 0\n    for _, c := range commands {\n        if c == \"UP\" {\n            ans++\n        } else if c == \"DOWN\" {\n            ans--\n        }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "UP UP DOWN", expectedStdout: "1", isSample: true },
      { stdin: "DOWN DOWN", expectedStdout: "-2", isSample: true },
      { stdin: "UP DOWN UP DOWN", expectedStdout: "0", isSample: true },
      { stdin: "UP", expectedStdout: "1" },
      { stdin: "DOWN", expectedStdout: "-1" },
      { stdin: "UP UP UP UP UP", expectedStdout: "5" },
      { stdin: "DOWN DOWN DOWN UP", expectedStdout: "-2" },
      { stdin: "UP DOWN", expectedStdout: "0" },
    ],
  }),

  p({
    ...base,
    slug: "detect-radiation-leaks",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "TWO_POINTERS",
    title: "Detect Radiation Leaks",
    patternTags: ["array","two-pointers","sliding-window"],
    signatureId: "fn:ints,int->ints",
    avgSolveSeconds: 600,
    promptMarkdown: "A line of sensors records radiation levels in an array `levels`. A reading of `0` indicates a radiation leak at that sensor's index. A sensor is considered \"at risk\" if it is located within a distance of `k` from any radiation leak (i.e., its index `i` and a leak's index `j` satisfy `abs(i - j) <= k`).\n\nReturn an array of all \"at risk\" sensor indices in strictly ascending order.\n\n**Constraints**\n- `1 <= levels.length <= 100`\n- `0 <= levels[i] <= 1000`\n- `1 <= k <= 100`\n\n**Example 1**\n```\ninput:\n1 0 1 1 0\n1\noutput: 0 1 2 3 4\n```\n*Explanation: Leaks are at indices 1 and 4. Indices within distance 1 of index 1 are 0, 1, and 2. Indices within distance 1 of index 4 are 3 and 4. All indices are at risk.*\n\n**Example 2**\n```\ninput:\n5 5 5 0 5\n2\noutput: 1 2 3 4\n```\n*Explanation: The only leak is at index 3. Indices within distance 2 of index 3 are 1, 2, 3, and 4.*\n\n**Example 3**\n```\ninput:\n1 2 3\n1\noutput: \n```\n*Explanation: There are no leaks, so no sensors are at risk.*\n\n**Follow-up**\nCan you solve this in mathcal{O}(N) time without using nested loops?",
    editorialMarkdown: "## Detect Radiation Leaks\nYou can iterate through the array to find every index. For each index, iterate through the array again to check if any leak (a `0` value) is within distance `k`. Because we check the array linearly, the indices are naturally added in ascending order.\n\n**Trap**: Be careful not to include duplicate indices if a sensor is within distance `k` of multiple different leaks.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N^2) using the nested loop approach. mathcal{O}(N) is possible by finding all 0s and sweeping intervals.\n- **Space Complexity:** mathcal{O}(N) to store the result array.",
    referenceSolution: {
      JAVASCRIPT: "function solve(levels, k) {\n    let res = [];\n    for (let i = 0; i < levels.length; i++) {\n        for (let j = 0; j < levels.length; j++) {\n            if (levels[j] === 0 && Math.abs(i - j) <= k) {\n                res.push(i);\n                break;\n            }\n        }\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(levels: number[], k: number): number[] {\n    let res: number[] = [];\n    for (let i = 0; i < levels.length; i++) {\n        for (let j = 0; j < levels.length; j++) {\n            if (levels[j] === 0 && Math.abs(i - j) <= k) {\n                res.push(i);\n                break;\n            }\n        }\n    }\n    return res;\n}",
      PYTHON: "def solve(levels, k):\n    res = []\n    n = len(levels)\n    for i in range(n):\n        for j in range(n):\n            if levels[j] == 0 and abs(i - j) <= k:\n                res.append(i)\n                break\n    return res",
      JAVA: "    static int[] solve(int[] levels, int k) {\n        java.util.List<Integer> list = new java.util.ArrayList<>();\n        for (int i = 0; i < levels.length; i++) {\n            for (int j = 0; j < levels.length; j++) {\n                if (levels[j] == 0 && Math.abs(i - j) <= k) {\n                    list.add(i);\n                    break;\n                }\n            }\n        }\n        int[] res = new int[list.size()];\n        for (int i = 0; i < list.size(); i++) res[i] = list.get(i);\n        return res;\n    }",
      CPP: "#include <vector>\n#include <cmath>\nusing namespace std;\n\nvector<int> solve(vector<int> levels, int k) {\n    vector<int> res;\n    for (int i = 0; i < levels.size(); i++) {\n        for (int j = 0; j < levels.size(); j++) {\n            if (levels[j] == 0 && abs(i - j) <= k) {\n                res.push_back(i);\n                break;\n            }\n        }\n    }\n    return res;\n}",
      GO: "func solve(levels []int, k int) []int {\n    var res []int\n    n := len(levels)\n    for i := 0; i < n; i++ {\n        for j := 0; j < n; j++ {\n            if levels[j] == 0 {\n                diff := i - j\n                if diff < 0 { diff = -diff }\n                if diff <= k {\n                    res = append(res, i)\n                    break\n                }\n            }\n        }\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "1 0 1 1 0\n1", expectedStdout: "0 1 2 3 4", isSample: true },
      { stdin: "5 5 5 0 5\n2", expectedStdout: "1 2 3 4", isSample: true },
      { stdin: "1 2 3\n1", expectedStdout: "", isSample: true },
      { stdin: "0\n1", expectedStdout: "0" },
      { stdin: "0 0 0\n2", expectedStdout: "0 1 2" },
      { stdin: "1 1 1 1 1\n5", expectedStdout: "" },
      { stdin: "1 0 1 1 1 1 0 1\n2", expectedStdout: "0 1 2 3 4 5 6 7" },
      { stdin: "0 1 2 3 4 5\n1", expectedStdout: "0 1" },
    ],
  }),

  p({
    ...base,
    slug: "balance-cargo-ships",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Balance Server Loads",
    patternTags: ["math","arrays","search"],
    signatureId: "fn:ints,ints->ints",
    avgSolveSeconds: 600,
    promptMarkdown: "Two data centers have different total processing loads, given by integer arrays `shipA` and `shipB`, representing the loads of individual servers. The centers need to swap exactly one server between them so that both data centers end up with the exact same total processing load. \n\nReturn an array of two integers `[load_from_A, load_from_B]` representing the server loads to swap. If there are multiple valid swaps, return the one that appears first when iterating through `shipA`, then `shipB`. It is guaranteed that at least one valid swap exists.\n\n**Constraints**\n- `1 <= shipA.length, shipB.length <= 100`\n- `1 <= shipA[i], shipB[i] <= 1000`\n- A valid swap is guaranteed to exist.\n\n**Example 1**\n```\ninput:\n1 1\n2 2\noutput: 1 2\n```\n*Explanation: Center A has a total load of 2, and Center B has 4. Swapping a 1 from A and a 2 from B makes both totals 3.*\n\n**Example 2**\n```\ninput:\n1 2\n2 3\noutput: 1 2\n```\n*Explanation: Center A has a total load of 3, and Center B has 5. Swapping a 1 from A and a 2 from B makes both totals 4.*\n\n**Example 3**\n```\ninput:\n2\n1 3\noutput: 2 3\n```\n*Explanation: Center A has a total load of 2, and Center B has 4. Swapping a 2 from A and a 3 from B makes both totals 3.*\n\n**Follow-up**\nCan you find the answer in mathcal{O}(N + M) time, where N and M are the lengths of the arrays?",
    editorialMarkdown: "## Balance Cargo Ships\nLet S_A and S_B be the sums of the two arrays. If we swap a from A and b from B, the new sums must be equal: S_A - a + b = S_B - b + a. Solving for a - b, we get a - b = (S_A - S_B) / 2. You can iterate through all pairs (a, b) to find the match, or use a hash set for mathcal{O}(N+M) lookup.\n\n**Trap**: Be careful with the sign of the difference between the sums. Ensure you add or subtract the difference in the correct direction.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N \\\\times M) with nested loops, or mathcal{O}(N + M) using a Hash Set.\n- **Space Complexity:** mathcal{O}(1) for nested loops, or mathcal{O}(N) if storing one of the arrays in a Hash Set.",
    referenceSolution: {
      JAVASCRIPT: "function solve(shipA, shipB) {\n    let sumA = 0;\n    for (let x of shipA) sumA += x;\n    let sumB = 0;\n    for (let x of shipB) sumB += x;\n    let diff = Math.floor((sumA - sumB) / 2);\n    for (let a of shipA) {\n        for (let b of shipB) {\n            if (a - b === diff) return [a, b];\n        }\n    }\n    return [];\n}",
      TYPESCRIPT: "function solve(shipA: number[], shipB: number[]): number[] {\n    let sumA = 0;\n    for (let x of shipA) sumA += x;\n    let sumB = 0;\n    for (let x of shipB) sumB += x;\n    let diff = Math.floor((sumA - sumB) / 2);\n    for (let a of shipA) {\n        for (let b of shipB) {\n            if (a - b === diff) return [a, b];\n        }\n    }\n    return [];\n}",
      PYTHON: "def solve(shipA, shipB):\n    diff = (sum(shipA) - sum(shipB)) // 2\n    for a in shipA:\n        for b in shipB:\n            if a - b == diff:\n                return [a, b]\n    return []",
      JAVA: "    static int[] solve(int[] shipA, int[] shipB) {\n        int sumA = 0;\n        for (int x : shipA) sumA += x;\n        int sumB = 0;\n        for (int x : shipB) sumB += x;\n        int diff = (sumA - sumB) / 2;\n        for (int a : shipA) {\n            for (int b : shipB) {\n                if (a - b == diff) {\n                    return new int[]{a, b};\n                }\n            }\n        }\n        return new int[0];\n    }",
      CPP: "#include <vector>\nusing namespace std;\n\nvector<int> solve(vector<int> shipA, vector<int> shipB) {\n    int sumA = 0;\n    for (int x : shipA) sumA += x;\n    int sumB = 0;\n    for (int x : shipB) sumB += x;\n    int diff = (sumA - sumB) / 2;\n    for (int a : shipA) {\n        for (int b : shipB) {\n            if (a - b == diff) {\n                return {a, b};\n            }\n        }\n    }\n    return {};\n}",
      GO: "func solve(shipA []int, shipB []int) []int {\n    sumA := 0\n    for _, x := range shipA { sumA += x }\n    sumB := 0\n    for _, x := range shipB { sumB += x }\n    diff := (sumA - sumB) / 2\n    for _, a := range shipA {\n        for _, b := range shipB {\n            if a - b == diff {\n                return []int{a, b}\n            }\n        }\n    }\n    return []int{}\n}",
    },
    tests: [
      { stdin: "1 1\n2 2", expectedStdout: "1 2", isSample: true },
      { stdin: "1 2\n2 3", expectedStdout: "1 2", isSample: true },
      { stdin: "2\n1 3", expectedStdout: "2 3", isSample: true },
      { stdin: "1 2 5\n2 4", expectedStdout: "5 4" },
      { stdin: "5 10\n10 15", expectedStdout: "5 10" },
      { stdin: "4 4\n6 6", expectedStdout: "4 6" },
      { stdin: "10 40\n50 20", expectedStdout: "10 20" },
      { stdin: "1 2 3\n4 6", expectedStdout: "2 4" },
    ],
  }),

  p({
    ...base,
    slug: "filter-stable-signals",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Filter Stable Signals",
    patternTags: ["string","hash-map","counting"],
    signatureId: "fn:string,int->string",
    avgSolveSeconds: 480,
    promptMarkdown: "A space probe sends a `stream` of characters back to Earth. Some characters are random noise. We consider a character \"stable\" if it appears at least `k` times in the entire `stream`.\n\nReturn a new string containing only the stable characters, maintaining their original relative order from the `stream`.\n\n**Constraints**\n- `1 <= stream.length <= 100`\n- `1 <= k <= 100`\n- `stream` consists of lowercase English letters.\n\n**Example 1**\n```\ninput:\nabacaba\n3\noutput: aaaa\n```\n*Explanation: 'a' appears 4 times, 'b' 2 times, and 'c' 1 time. Only 'a' appears at least 3 times.*\n\n**Example 2**\n```\ninput:\nxyz\n2\noutput: \n```\n*Explanation: Every character appears only once, which is less than 2, so the output is empty.*\n\n**Example 3**\n```\ninput:\naabbcc\n2\noutput: aabbcc\n```\n*Explanation: All characters appear exactly 2 times, so they are all kept in their original order.*\n\n**Follow-up**\nCan you process the string using exactly two passes?",
    editorialMarkdown: "## Filter Stable Signals\nFirst, iterate through the string and use a hash map or an array of size 26 to count the frequency of each character. Then, iterate through the string a second time, appending a character to the result only if its recorded frequency is at least `k`.\n\n**Trap**: Make sure to iterate over the original string for the second pass to maintain the original relative order, rather than iterating over the keys of the frequency map.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the length of the string, as we do two passes over it.\n- **Space Complexity:** mathcal{O}(N) to store the result string, and mathcal{O}(1) auxiliary space since there are at most 26 unique characters.",
    referenceSolution: {
      JAVASCRIPT: "function solve(stream, k) {\n    let counts = {};\n    for (let c of stream) counts[c] = (counts[c] || 0) + 1;\n    let res = \"\";\n    for (let c of stream) {\n        if (counts[c] >= k) res += c;\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(stream: string, k: number): string {\n    let counts: Record<string, number> = {};\n    for (let c of stream) counts[c] = (counts[c] || 0) + 1;\n    let res = \"\";\n    for (let c of stream) {\n        if (counts[c] >= k) res += c;\n    }\n    return res;\n}",
      PYTHON: "def solve(stream, k):\n    counts = {}\n    for c in stream:\n        counts[c] = counts.get(c, 0) + 1\n    return \"\".join(c for c in stream if counts[c] >= k)",
      JAVA: "    static String solve(String stream, int k) {\n        int[] counts = new int[256];\n        for (char c : stream.toCharArray()) {\n            counts[c]++;\n        }\n        StringBuilder res = new StringBuilder();\n        for (char c : stream.toCharArray()) {\n            if (counts[c] >= k) {\n                res.append(c);\n            }\n        }\n        return res.toString();\n    }",
      CPP: "#include <string>\nusing namespace std;\n\nstring solve(string stream, int k) {\n    int counts[256] = {0};\n    for (char c : stream) {\n        counts[c]++;\n    }\n    string res = \"\";\n    for (char c : stream) {\n        if (counts[c] >= k) {\n            res += c;\n        }\n    }\n    return res;\n}",
      GO: "func solve(stream string, k int) string {\n    counts := make(map[rune]int)\n    for _, c := range stream {\n        counts[c]++\n    }\n    var res []rune\n    for _, c := range stream {\n        if counts[c] >= k {\n            res = append(res, c)\n        }\n    }\n    return string(res)\n}",
    },
    tests: [
      { stdin: "abacaba\n3", expectedStdout: "aaaa", isSample: true },
      { stdin: "xyz\n2", expectedStdout: "", isSample: true },
      { stdin: "aabbcc\n2", expectedStdout: "aabbcc", isSample: true },
      { stdin: "a\n1", expectedStdout: "a" },
      { stdin: "abcdefg\n1", expectedStdout: "abcdefg" },
      { stdin: "mississippi\n3", expectedStdout: "ississii" },
      { stdin: "mississippi\n2", expectedStdout: "ississippi" },
      { stdin: "abacaba\n4", expectedStdout: "aaaa" },
      { stdin: "aaaaa\n5", expectedStdout: "aaaaa" },
    ],
  }),
];
