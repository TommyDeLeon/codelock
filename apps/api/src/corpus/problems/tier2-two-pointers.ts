import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p = (d: ProblemDefinition): ProblemDefinition => d;
const base = { tier: 'TIER_2', patternFamily: 'TWO_POINTERS', provenance: AUTHORED } as const;

export const TIER_2_TWO_POINTERS_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base, slug: 'reverse-each-fixed-group', title: 'Turn Around Each Full Group', difficulty: 'MEDIUM',
    patternTags: ['two-pointers', 'in-place', 'groups'], signatureId: 'fn:ints,int->ints', avgSolveSeconds: 660,
    promptMarkdown: ['Given a list and a positive group size `k`, reverse every consecutive group of exactly `k` numbers in place. Leave a final shorter group in its original order.', '', '**Example**', '', '```', 'input:  1 2 3 4 5 6 7', '3', 'output: 3 2 1 6 5 4 7', '```', '', 'The list has at least one number and `k` is at least 1. If `k` is 1, or fewer than `k` numbers remain, those numbers stay unchanged. The returned list is in its new list order.'].join('\n'),
    editorialMarkdown: ['## Two pointers inside fixed windows', '', 'The parent reversal has one pair of pointers for the whole list. Here that remembered loop is wrong because the pointers must restart at each group boundary. For each full window `[start, start + k - 1]`, swap its outside values inward, then advance `start` by `k`. A partial tail never opens a window, so it is deliberately untouched.', '', 'The quiet mistake is setting the right pointer to `min(start + k - 1, n - 1)`: that reverses the short tail even though the rule excludes it. It looks plausible because all full groups still work. Check that a whole group remains before swapping. Time is O(n), since each value is swapped at most once; extra space is O(1).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a, b) {\n  for (let s = 0; s + b <= a.length; s += b) {\n    let l = s, r = s + b - 1;\n    while (l < r) { [a[l], a[r]] = [a[r], a[l]]; l++; r--; }\n  }\n  return a;\n}',
      TYPESCRIPT: 'function solve(a: number[], b: number): number[] {\n  for (let s = 0; s + b <= a.length; s += b) {\n    let l = s, r = s + b - 1;\n    while (l < r) { const t = a[l]; a[l] = a[r]; a[r] = t; l++; r--; }\n  }\n  return a;\n}',
      PYTHON: 'def solve(a, b):\n    for s in range(0, len(a) - b + 1, b):\n        left, right = s, s + b - 1\n        while left < right:\n            a[left], a[right] = a[right], a[left]\n            left += 1\n            right -= 1\n    return a',
      JAVA: '    static int[] solve(int[] a, int b) {\n        for (int s = 0; s + b <= a.length; s += b) {\n            int l = s, r = s + b - 1;\n            while (l < r) { int t = a[l]; a[l++] = a[r]; a[r--] = t; }\n        }\n        return a;\n    }',
      CPP: 'vector<int> solve(vector<int> a, int b) {\n    for (int s = 0; s + b <= (int)a.size(); s += b) {\n        int l = s, r = s + b - 1;\n        while (l < r) { int t = a[l]; a[l++] = a[r]; a[r--] = t; }\n    }\n    return a;\n}',
      GO: 'func solve(a []int, b int) []int {\n\tfor s := 0; s+b <= len(a); s += b {\n\t\tl, r := s, s+b-1\n\t\tfor l < r { a[l], a[r] = a[r], a[l]; l++; r-- }\n\t}\n\treturn a\n}',
    }, tests: [
      { stdin: '1 2 3 4 5 6 7\n3', expectedStdout: '3 2 1 6 5 4 7', isSample: true }, { stdin: '1 2 3 4 5\n2', expectedStdout: '2 1 4 3 5', isSample: true }, { stdin: '9\n3', expectedStdout: '9' }, { stdin: '1 2 3\n1', expectedStdout: '1 2 3' }, { stdin: '-1 -2 -3 -4\n4', expectedStdout: '-4 -3 -2 -1' },
    ],
  }),
  p({
    ...base, slug: 'distinct-squares-from-sorted-list', title: 'Square Values, Once Each', difficulty: 'MEDIUM', patternTags: ['two-pointers', 'sorted-input', 'deduplication'], signatureId: 'fn:ints->ints', avgSolveSeconds: 660,
    promptMarkdown: ['Given a nondecreasing list, square every number and return the distinct results in ascending order. State the output in ascending numeric order.', '', '**Example**', '', '```', 'input:  -4 -2 -2 0 2 3', 'output: 0 4 9 16', '```', '', 'The list has at least one number. Equal squares made by opposite signs count once, and repeated input values also produce only one output value.'].join('\n'),
    editorialMarkdown: ['## Two ends choose the next largest distinct square', '', 'The parent fills an output array with every square. The twist is that equal magnitudes can arrive from opposite ends and duplicates can repeat on either side. Compare endpoint squares, append the larger unseen square to a temporary descending result, and advance every endpoint holding that chosen square. Reverse the result at the end to state its ascending output order.', '', 'The quiet mistake is advancing only one side on equality. For `-2 2`, that emits 4 twice after the other side is processed. Skip all equal chosen squares from both ends. Time is O(n), each input position is discarded once; extra space is O(d) for d distinct answers.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){let l=0,r=a.length-1,o=[];while(l<=r){let x=a[l]*a[l],y=a[r]*a[r],v=x>y?x:y;if(!o.length||o[o.length-1]!==v)o.push(v);while(l<=r&&a[l]*a[l]===v)l++;while(l<=r&&a[r]*a[r]===v)r--;}return o.reverse();}',
      TYPESCRIPT: 'function solve(a: number[]): number[] { let l=0,r=a.length-1,o:number[]=[]; while(l<=r){const x=a[l]*a[l],y=a[r]*a[r],v=x>y?x:y; if(!o.length||o[o.length-1]!==v)o.push(v); while(l<=r&&a[l]*a[l]===v)l++; while(l<=r&&a[r]*a[r]===v)r--;} return o.reverse(); }',
      PYTHON: 'def solve(a):\n    left, right, out = 0, len(a) - 1, []\n    while left <= right:\n        value = max(a[left] * a[left], a[right] * a[right])\n        if not out or out[-1] != value: out.append(value)\n        while left <= right and a[left] * a[left] == value: left += 1\n        while left <= right and a[right] * a[right] == value: right -= 1\n    return out[::-1]',
      JAVA: '    static int[] solve(int[] a) { java.util.ArrayList<Integer> o=new java.util.ArrayList<>(); int l=0,r=a.length-1; while(l<=r){int x=a[l]*a[l],y=a[r]*a[r],v=x>y?x:y;if(o.isEmpty()||o.get(o.size()-1)!=v)o.add(v);while(l<=r&&a[l]*a[l]==v)l++;while(l<=r&&a[r]*a[r]==v)r--;}int[] z=new int[o.size()];for(int i=0;i<z.length;i++)z[i]=o.get(z.length-1-i);return z; }',
      CPP: 'vector<int> solve(vector<int> a) { vector<int> o; int l=0,r=(int)a.size()-1; while(l<=r){int x=a[l]*a[l],y=a[r]*a[r],v=x>y?x:y;if(o.empty()||o.back()!=v)o.push_back(v);while(l<=r&&a[l]*a[l]==v)l++;while(l<=r&&a[r]*a[r]==v)r--;} reverse(o.begin(),o.end());return o; }',
      GO: 'func solve(a []int) []int { l,r:=0,len(a)-1; o:=[]int{}; for l<=r { x,y:=a[l]*a[l],a[r]*a[r]; v:=x;if y>v{v=y};if len(o)==0||o[len(o)-1]!=v{o=append(o,v)};for l<=r&&a[l]*a[l]==v{l++};for l<=r&&a[r]*a[r]==v{r--} };for i,j:=0,len(o)-1;i<j;i,j=i+1,j-1{o[i],o[j]=o[j],o[i]};return o }',
    }, tests: [
      { stdin: '-4 -2 -2 0 2 3', expectedStdout: '0 4 9 16', isSample: true }, { stdin: '-2 2', expectedStdout: '4', isSample: true }, { stdin: '5', expectedStdout: '25' }, { stdin: '0 0 0', expectedStdout: '0' }, { stdin: '-3 -3 -1', expectedStdout: '1 9' },
    ],
  }),
  p({
    ...base, slug: 'width-capped-water-container', title: 'A Tank With a Reach Limit', difficulty: 'MEDIUM', patternTags: ['two-pointers', 'greedy', 'converging'], signatureId: 'fn:ints,int->int', avgSolveSeconds: 780,
    promptMarkdown: ['Vertical line heights are one unit apart. Return the largest water area formed by two lines whose distance is at most `k`. Area is the shorter height times their distance.', '', '**Example**', '', '```', 'input:  1 8 6 2 5 4 8 3 7', '4', 'output: 24', '```', '', 'The original pair at positions 1 and 6 is too far apart here. Positions 2 and 6 give `min(6, 8) x 4 = 24`, the best legal area. The list has at least two nonnegative heights and `1 <= k < n`.'].join('\n'),
    editorialMarkdown: ['## Two pointers in a width-bounded window', '', 'Starting at the full list width, as in the parent, wastes comparisons that violate the limit. Start with `left = 0` and `right = k`, so every examined pair is legal. For each left position, move right as far right as its cap allows and use the usual shorter-side rule within that range; a simpler robust implementation checks the k-wide band with an inner pointer scan.', '', 'The quiet mistake is applying the parent’s full-width greedy proof unchanged: its first pair may be illegal, and moving the shorter wall cannot recover every legal candidate. The width cap changes the search space. With the stated small bound-free input, scan each legal pair: O(nk) time, bounded by the width limit, and O(1) space.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a,b){let best=0;for(let l=0;l<a.length;l++)for(let r=l+1;r<a.length&&r-l<=b;r++){let area=(a[l]<a[r]?a[l]:a[r])*(r-l);if(area>best)best=area;}return best;}',
      TYPESCRIPT: 'function solve(a: number[], b: number): number { let best=0; for(let l=0;l<a.length;l++)for(let r=l+1;r<a.length&&r-l<=b;r++){const area=(a[l]<a[r]?a[l]:a[r])*(r-l);if(area>best)best=area;}return best; }',
      PYTHON: 'def solve(a, b):\n    best = 0\n    for left in range(len(a)):\n        for right in range(left + 1, min(len(a), left + b + 1)):\n            best = max(best, min(a[left], a[right]) * (right - left))\n    return best',
      JAVA: '    static int solve(int[] a, int b) { int best=0; for(int l=0;l<a.length;l++) for(int r=l+1;r<a.length&&r-l<=b;r++){int area=Math.min(a[l],a[r])*(r-l);if(area>best)best=area;} return best; }',
      CPP: 'int solve(vector<int> a, int b) { int best=0; for(int l=0;l<(int)a.size();l++)for(int r=l+1;r<(int)a.size()&&r-l<=b;r++){int h=a[l]<a[r]?a[l]:a[r];int area=h*(r-l);if(area>best)best=area;}return best; }',
      GO: 'func solve(a []int, b int) int { best:=0; for l:=0;l<len(a);l++ { for r:=l+1;r<len(a)&&r-l<=b;r++ { h:=a[l];if a[r]<h{h=a[r]};area:=h*(r-l);if area>best{best=area} } };return best }',
    }, tests: [
      { stdin: '1 8 6 2 5 4 8 3 7\n4', expectedStdout: '24', isSample: true }, { stdin: '1 1\n1', expectedStdout: '1', isSample: true }, { stdin: '4 3 2 1 4\n2', expectedStdout: '4' }, { stdin: '1 2 1\n1', expectedStdout: '1' }, { stdin: '2 3 4 5\n3', expectedStdout: '6' },
    ],
  }),
  p({
    ...base, slug: 'count-triplets-under-target', title: 'Three Below the Mark', difficulty: 'HARD', patternTags: ['two-pointers', 'sorting', 'counting'], signatureId: 'fn:ints,int->int', avgSolveSeconds: 900,
    promptMarkdown: ['Count index triplets `(i, j, k)` with `i < j < k` whose three values sum to less than the target. Different positions count separately, even when their values match.', '', '**Example**', '', '```', 'input:  -2 0 1 3', '2', 'output: 2', '```', '', 'The qualifying triplets are `(-2, 0, 1)` and `(-2, 0, 3)`. The list has at least three numbers and may contain duplicates or negatives. If no triplet is below the target, return 0.'].join('\n'),
    editorialMarkdown: ['## Sort, fix an anchor, then count a whole pointer range', '', 'The zero-sum parent searches for exact distinct value triplets and skips duplicates. This variation counts positions and accepts an inequality. After sorting, fix the first index and use two pointers. When the sum with `right` is below target, every index from left through `right - 1` also works with that anchor, because sorted values can only be smaller. Add `right - left` at once, then advance left.', '', 'The quiet mistake is moving both pointers after finding one valid sum, which counts one triplet but loses the rest of the valid range. Do not skip duplicates: repeated positions are supposed to count. Time is O(n^2) after O(n log n) sorting; extra space is O(1) if sorting in place.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a,b){a.sort((x,y)=>x-y);let c=0;for(let i=0;i+2<a.length;i++){let l=i+1,r=a.length-1;while(l<r){if(a[i]+a[l]+a[r]<b){c+=r-l;l++;}else r--;}}return c;}',
      TYPESCRIPT: 'function solve(a: number[], b: number): number { a.sort((x,y)=>x-y);let c=0;for(let i=0;i+2<a.length;i++){let l=i+1,r=a.length-1;while(l<r){if(a[i]+a[l]+a[r]<b){c+=r-l;l++;}else r--;}}return c; }',
      PYTHON: 'def solve(a, b):\n    a.sort(); count = 0\n    for i in range(len(a) - 2):\n        left, right = i + 1, len(a) - 1\n        while left < right:\n            if a[i] + a[left] + a[right] < b:\n                count += right - left; left += 1\n            else: right -= 1\n    return count',
      JAVA: '    static int solve(int[] a, int b) { java.util.Arrays.sort(a);int c=0;for(int i=0;i+2<a.length;i++){int l=i+1,r=a.length-1;while(l<r){if(a[i]+a[l]+a[r]<b){c+=r-l;l++;}else r--;}}return c; }',
      CPP: 'int solve(vector<int> a, int b) { sort(a.begin(),a.end());int c=0;for(int i=0;i+2<(int)a.size();i++){int l=i+1,r=(int)a.size()-1;while(l<r){if(a[i]+a[l]+a[r]<b){c+=r-l;l++;}else r--;}}return c; }',
      GO: 'func solve(a []int, b int) int { for i:=1;i<len(a);i++ {v:=a[i];j:=i-1;for j>=0&&a[j]>v{a[j+1]=a[j];j--};a[j+1]=v};c:=0;for i:=0;i+2<len(a);i++ {l,r:=i+1,len(a)-1;for l<r {if a[i]+a[l]+a[r]<b {c+=r-l;l++} else {r--}}};return c }',
    }, tests: [
      { stdin: '-2 0 1 3\n2', expectedStdout: '2', isSample: true }, { stdin: '3 1 0 -2\n4', expectedStdout: '3', isSample: true }, { stdin: '1 1 1\n3', expectedStdout: '0' }, { stdin: '0 0 0 0\n1', expectedStdout: '4' }, { stdin: '-1 -1 2 2\n1', expectedStdout: '2' },
    ],
  }),
  p({
    ...base, slug: 'move-chosen-value-to-end', title: 'Send One Value Back', difficulty: 'EASY', patternTags: ['two-pointers', 'stable-filtering', 'in-place'], signatureId: 'fn:ints,int->ints', avgSolveSeconds: 540,
    promptMarkdown: ['Given a list and a chosen value, move every occurrence of that value to the end. Keep the relative order of every other number, and return the resulting list order.', '', '**Example**', '', '```', 'input:  2 1 2 3 2 4', '2', 'output: 1 3 4 2 2 2', '```', '', 'The list has at least one number. If the chosen value is absent, return the unchanged list; if every number is chosen, it also remains unchanged.'].join('\n'),
    editorialMarkdown: ['## Read and write pointers for stable compaction', '', 'The zero-moving parent can tempt you to swap a chosen value with the right end. That gathers values but scrambles the survivors, violating this variation’s order requirement. Scan with a read pointer and copy every non-chosen value to the next write slot. Once all survivors are compacted, fill the remaining slots with the chosen value.', '', 'The quiet mistake is using swaps: `2 1 2 3` becomes `3 1 2 2`, which has the right multiset but the wrong survivor order. Keep read and write moving left to right. Time is O(n), because each position is read and written a constant number of times; extra space is O(1).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a,b){let w=0;for(const x of a)if(x!==b)a[w++]=x;while(w<a.length)a[w++]=b;return a;}',
      TYPESCRIPT: 'function solve(a: number[], b: number): number[] { let w=0; for(const x of a) if(x!==b) a[w++]=x; while(w<a.length) a[w++]=b; return a; }',
      PYTHON: 'def solve(a, b):\n    write = 0\n    for x in a:\n        if x != b:\n            a[write] = x\n            write += 1\n    while write < len(a):\n        a[write] = b\n        write += 1\n    return a',
      JAVA: '    static int[] solve(int[] a, int b) { int w=0; for(int x:a) if(x!=b) a[w++]=x; while(w<a.length) a[w++]=b; return a; }',
      CPP: 'vector<int> solve(vector<int> a, int b) { int w=0; for(int x:a) if(x!=b) a[w++]=x; while(w<(int)a.size()) a[w++]=b; return a; }',
      GO: 'func solve(a []int, b int) []int { w:=0; for _,x:=range a { if x!=b { a[w]=x; w++ } }; for w<len(a) { a[w]=b; w++ }; return a }',
    }, tests: [
      { stdin: '2 1 2 3 2 4\n2', expectedStdout: '1 3 4 2 2 2', isSample: true }, { stdin: '0 1 0 2\n0', expectedStdout: '1 2 0 0', isSample: true }, { stdin: '5 6 7\n9', expectedStdout: '5 6 7' }, { stdin: '3 3 3\n3', expectedStdout: '3 3 3' }, { stdin: '-1 2 -1 4\n-1', expectedStdout: '2 4 -1 -1' },
    ],
  }),
  p({
    ...base, slug: 'keep-at-most-two-sorted', title: 'Let a Value Appear Twice', difficulty: 'MEDIUM', patternTags: ['two-pointers', 'sorted-input', 'in-place'], signatureId: 'fn:ints->ints', avgSolveSeconds: 600,
    promptMarkdown: ['Given a nondecreasing list, return its values in sorted order after keeping at most two copies of each value.', '', '**Example**', '', '```', 'input:  1 1 1 2 2 3', 'output: 1 1 2 2 3', '```', '', 'The list has at least one number. A value appearing once or twice survives unchanged; an all-equal list keeps exactly its first two numbers.'].join('\n'),
    editorialMarkdown: ['## Two pointers compare against the kept prefix', '', 'Removing every duplicate remembers only one copy. The twist changes the admission test: a candidate may be written when it differs from the value two output positions earlier. Because the input is sorted, equal values are consecutive, so that comparison precisely distinguishes the first two copies from later ones. The write pointer builds the answer prefix in output order.', '', 'The quiet mistake is comparing with the immediately previous output value, which recreates the parent and discards every second copy. Another subtle bug starts reading at index zero and addresses a negative output index. Seed the first two values, then scan the rest. Time is O(n), extra space is O(1) beyond the returned list.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a){let w=0;for(const x of a){if(w<2||x!==a[w-2])a[w++]=x;}return a.slice(0,w);}',
      TYPESCRIPT: 'function solve(a: number[]): number[] { let w=0; for(const x of a){ if(w<2||x!==a[w-2]) a[w++]=x; } return a.slice(0,w); }',
      PYTHON: 'def solve(a):\n    write = 0\n    for x in a:\n        if write < 2 or x != a[write - 2]:\n            a[write] = x\n            write += 1\n    return a[:write]',
      JAVA: '    static int[] solve(int[] a) { int w=0; for(int x:a) if(w<2||x!=a[w-2]) a[w++]=x; return java.util.Arrays.copyOf(a,w); }',
      CPP: 'vector<int> solve(vector<int> a) { int w=0; for(int x:a) if(w<2||x!=a[w-2]) a[w++]=x; a.resize(w); return a; }',
      GO: 'func solve(a []int) []int { w:=0; for _,x:=range a { if w<2 || x!=a[w-2] { a[w]=x; w++ } }; return a[:w] }',
    }, tests: [
      { stdin: '1 1 1 2 2 3', expectedStdout: '1 1 2 2 3', isSample: true }, { stdin: '0 0 0 0', expectedStdout: '0 0', isSample: true }, { stdin: '5', expectedStdout: '5' }, { stdin: '-2 -2 -1 -1 -1 0', expectedStdout: '-2 -2 -1 -1 0' }, { stdin: '1 2 3', expectedStdout: '1 2 3' },
    ],
  }),
  p({
    ...base, slug: 'merge-sorted-without-shared-values', title: 'Merge Only the Unshared', difficulty: 'MEDIUM', patternTags: ['two-pointers', 'merge', 'sorted-input'], signatureId: 'fn:ints,ints->ints', avgSolveSeconds: 720,
    promptMarkdown: ['Two nondecreasing lists are given. Return one sorted list containing values that occur in exactly one input list. If a value appears in both lists, remove every copy of it from the answer. State the output in ascending order.', '', '**Example**', '', '```', 'input:  1 2 2 5', '2 3 5 5', 'output: 1 3', '```', '', 'Both lists have at least one number. Duplicates may occur within either list. If all values are shared, return an empty output line.'].join('\n'),
    editorialMarkdown: ['## Merge pointers with duplicate runs', '', 'A standard merge outputs the smaller current value and outputs both on equality. That parent instinct fails here: equality means the entire value is forbidden. Compare the heads; when one is smaller, copy its whole run, and when they are equal, advance past that value in both lists without writing it. Remaining runs are safe because the other list is exhausted.', '', 'The quiet mistake is skipping only one equal pair. With `2 2` and `2`, the second 2 then leaks into the answer even though the value is shared. Skip complete runs, not individual positions. Time is O(n + m), as each pointer advances through its list once; extra space is O(n + m) for the returned list.'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a,b){let i=0,j=0,o=[];while(i<a.length&&j<b.length){if(a[i]<b[j]){let v=a[i];while(i<a.length&&a[i]===v)o.push(a[i++]);}else if(a[i]>b[j]){let v=b[j];while(j<b.length&&b[j]===v)o.push(b[j++]);}else{let v=a[i];while(i<a.length&&a[i]===v)i++;while(j<b.length&&b[j]===v)j++;}}while(i<a.length)o.push(a[i++]);while(j<b.length)o.push(b[j++]);return o;}',
      TYPESCRIPT: 'function solve(a: number[], b: number[]): number[] { let i=0,j=0,o:number[]=[]; while(i<a.length&&j<b.length){if(a[i]<b[j]){const v=a[i];while(i<a.length&&a[i]===v)o.push(a[i++]);}else if(a[i]>b[j]){const v=b[j];while(j<b.length&&b[j]===v)o.push(b[j++]);}else{const v=a[i];while(i<a.length&&a[i]===v)i++;while(j<b.length&&b[j]===v)j++;}} while(i<a.length)o.push(a[i++]);while(j<b.length)o.push(b[j++]);return o; }',
      PYTHON: 'def solve(a, b):\n    i = j = 0; out = []\n    while i < len(a) and j < len(b):\n        if a[i] < b[j]:\n            v = a[i]\n            while i < len(a) and a[i] == v: out.append(a[i]); i += 1\n        elif a[i] > b[j]:\n            v = b[j]\n            while j < len(b) and b[j] == v: out.append(b[j]); j += 1\n        else:\n            v = a[i]\n            while i < len(a) and a[i] == v: i += 1\n            while j < len(b) and b[j] == v: j += 1\n    return out + a[i:] + b[j:]',
      JAVA: '    static int[] solve(int[] a, int[] b) { java.util.ArrayList<Integer> o=new java.util.ArrayList<>(); int i=0,j=0; while(i<a.length&&j<b.length){if(a[i]<b[j]){int v=a[i];while(i<a.length&&a[i]==v)o.add(a[i++]);}else if(a[i]>b[j]){int v=b[j];while(j<b.length&&b[j]==v)o.add(b[j++]);}else{int v=a[i];while(i<a.length&&a[i]==v)i++;while(j<b.length&&b[j]==v)j++;}}while(i<a.length)o.add(a[i++]);while(j<b.length)o.add(b[j++]);int[] r=new int[o.size()];for(i=0;i<r.length;i++)r[i]=o.get(i);return r; }',
      CPP: 'vector<int> solve(vector<int> a, vector<int> b) { vector<int> o; int i=0,j=0; while(i<(int)a.size()&&j<(int)b.size()){if(a[i]<b[j]){int v=a[i];while(i<(int)a.size()&&a[i]==v)o.push_back(a[i++]);}else if(a[i]>b[j]){int v=b[j];while(j<(int)b.size()&&b[j]==v)o.push_back(b[j++]);}else{int v=a[i];while(i<(int)a.size()&&a[i]==v)i++;while(j<(int)b.size()&&b[j]==v)j++;}}while(i<(int)a.size())o.push_back(a[i++]);while(j<(int)b.size())o.push_back(b[j++]);return o; }',
      GO: 'func solve(a []int, b []int) []int { i,j:=0,0; o:=[]int{}; for i<len(a)&&j<len(b) { if a[i]<b[j] { v:=a[i]; for i<len(a)&&a[i]==v {o=append(o,a[i]);i++} } else if a[i]>b[j] {v:=b[j];for j<len(b)&&b[j]==v{o=append(o,b[j]);j++}} else {v:=a[i];for i<len(a)&&a[i]==v{i++};for j<len(b)&&b[j]==v{j++}} }; return append(o,append(a[i:],b[j:]...)...) }',
    }, tests: [
      { stdin: '1 2 2 5\n2 3 5 5', expectedStdout: '1 3', isSample: true }, { stdin: '1 1 4\n2 3 4', expectedStdout: '1 1 2 3', isSample: true }, { stdin: '2 2\n2', expectedStdout: '' }, { stdin: '-3 -1\n0 2', expectedStdout: '-3 -1 0 2' }, { stdin: '1 3\n1 3', expectedStdout: '' },
    ],
  }),
  p({
    ...base, slug: 'palindrome-after-one-erasure', title: 'Almost a Mirror', difficulty: 'MEDIUM', patternTags: ['two-pointers', 'strings', 'branching'], signatureId: 'fn:string->bool', avgSolveSeconds: 720,
    promptMarkdown: ['Return `true` when a line is already a palindrome, or can become one after deleting at most one character. Comparisons are case-sensitive and every character, including spaces and punctuation, counts.', '', '**Example**', '', '```', 'input:  abca', 'output: true', '```', '', 'Deleting `b` makes `aca`. The line has at least one character. A one-character line is a palindrome; a line requiring two deletions returns `false`.'].join('\n'),
    editorialMarkdown: ['## Two pointers with one controlled branch', '', 'Ordinary palindrome code returns false at its first mismatch. That memorised answer misses the twist: at that mismatch, either the left character is the one deletion or the right character is. Test the remaining interval once with left advanced and once with right reduced; either success is enough. Before a mismatch, matching ends can be discarded exactly as in the parent problem.', '', 'The quiet mistake is deleting greedily from only one side. `abca` accepts either useful branch, but inputs such as `abcbea` need the other branch and silently produce false. Do not recursively spend more deletions. Time is O(n): the main scan plus at most two linear interval checks; extra space is O(1).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a) {\n  const ok = (l, r) => { while (l < r) { if (a[l++] !== a[r--]) return false; } return true; };\n  let l = 0, r = a.length - 1;\n  while (l < r) { if (a[l] !== a[r]) return ok(l + 1, r) || ok(l, r - 1); l++; r--; }\n  return true;\n}',
      TYPESCRIPT: 'function solve(a: string): boolean {\n  const ok = (l: number, r: number): boolean => { while (l < r) { if (a[l++] !== a[r--]) return false; } return true; };\n  let l = 0, r = a.length - 1;\n  while (l < r) { if (a[l] !== a[r]) return ok(l + 1, r) || ok(l, r - 1); l++; r--; }\n  return true;\n}',
      PYTHON: 'def solve(a):\n    def ok(left, right):\n        while left < right:\n            if a[left] != a[right]: return False\n            left += 1; right -= 1\n        return True\n    left, right = 0, len(a) - 1\n    while left < right:\n        if a[left] != a[right]: return ok(left + 1, right) or ok(left, right - 1)\n        left += 1; right -= 1\n    return True',
      JAVA: '    static boolean solve(String a) {\n        int l = 0, r = a.length() - 1;\n        while (l < r) {\n            if (a.charAt(l) != a.charAt(r)) return ok(a, l + 1, r) || ok(a, l, r - 1);\n            l++; r--;\n        }\n        return true;\n    }\n    static boolean ok(String a, int l, int r) { while (l < r) { if (a.charAt(l++) != a.charAt(r--)) return false; } return true; }',
      CPP: 'bool solve(string a) {\n    auto ok = [&](int l, int r) { while (l < r) if (a[l++] != a[r--]) return false; return true; };\n    int l = 0, r = (int)a.size() - 1;\n    while (l < r) { if (a[l] != a[r]) return ok(l + 1, r) || ok(l, r - 1); l++; r--; }\n    return true;\n}',
      GO: 'func solve(a string) bool {\n\tok := func(l, r int) bool { for l < r { if a[l] != a[r] { return false }; l++; r-- }; return true }\n\tl, r := 0, len(a)-1\n\tfor l < r { if a[l] != a[r] { return ok(l+1, r) || ok(l, r-1) }; l++; r-- }\n\treturn true\n}',
    }, tests: [
      { stdin: 'abca', expectedStdout: 'true', isSample: true }, { stdin: 'abc', expectedStdout: 'false', isSample: true }, { stdin: 'a', expectedStdout: 'true' }, { stdin: 'deeee', expectedStdout: 'true' }, { stdin: 'abcdef', expectedStdout: 'false' },
    ],
  }),
  p({
    ...base, slug: 'closest-pair-in-sorted-list', title: 'Nearest Sum Pair', difficulty: 'MEDIUM', patternTags: ['two-pointers', 'sorted-input', 'closest'], signatureId: 'fn:ints,int->ints', avgSolveSeconds: 660,
    promptMarkdown: ['Given an ascending list and a target, return the two values whose sum is closest to the target. Return them in ascending order. If two pairs are equally close, return the pair with the smaller first value.', '', '**Example**', '', '```', 'input:  1 4 6 8', '10', 'output: 4 6', '```', '', '`4 + 6` hits the target exactly, so nothing is closer. When two pairs are equally far off — say `1 3 5 7` with target `8`, where `1 + 7` and `3 + 5` both land on it — the tie rule picks the pair with the smaller first value. The list has at least two numbers.'].join('\n'),
    editorialMarkdown: ['## Converging pointers preserve the nearest candidate', '', 'An exact pair-sum loop can stop on equality. This variation cannot: it must remember the best pair seen and apply a tie rule. With a sorted list, a sum below target needs a larger left value, and a sum above target needs a smaller right value. Record the current pair before making that safe move. Equality is best possible, but continue only if needed for a tie rule; the first equality has the smallest left value.', '', 'The quiet mistake is returning the first pair merely because it is close, or updating on `<=` and replacing the required earlier tie. Compare absolute differences, then update only for a smaller difference. Time is O(n), one pointer move per iteration; extra space is O(1).'].join('\n'),
    referenceSolution: {
      JAVASCRIPT: 'function solve(a, b) { let l=0,r=a.length-1, bl=0,br=1, best=Infinity; while(l<r){const d=Math.abs(a[l]+a[r]-b); if(d<best||(d===best&&a[l]<a[bl])){best=d;bl=l;br=r;} if(a[l]+a[r]>=b)r--;else l++;} return [a[bl],a[br]]; }',
      TYPESCRIPT: 'function solve(a: number[], b: number): number[] { let l=0,r=a.length-1,bl=0,br=1,best=Infinity; while(l<r){const d=Math.abs(a[l]+a[r]-b); if(d<best||(d===best&&a[l]<a[bl])){best=d;bl=l;br=r;} if(a[l]+a[r]>=b)r--;else l++;} return [a[bl],a[br]]; }',
      PYTHON: 'def solve(a, b):\n    left, right, bl, br, best = 0, len(a)-1, 0, 1, float("inf")\n    while left < right:\n        diff = abs(a[left] + a[right] - b)\n        if diff < best or (diff == best and a[left] < a[bl]): best, bl, br = diff, left, right\n        if a[left] + a[right] >= b: right -= 1\n        else: left += 1\n    return [a[bl], a[br]]',
      JAVA: '    static int[] solve(int[] a, int b) { int l=0,r=a.length-1,bl=0,br=1,best=Integer.MAX_VALUE; while(l<r){int d=Math.abs(a[l]+a[r]-b); if(d<best||(d==best&&a[l]<a[bl])){best=d;bl=l;br=r;} if(a[l]+a[r]>=b)r--; else l++;} return new int[]{a[bl],a[br]}; }',
      CPP: 'vector<int> solve(vector<int> a, int b) { int l=0,r=(int)a.size()-1,bl=0,br=1,best=2147483647; while(l<r){int d=a[l]+a[r]-b; if(d<0)d=-d; if(d<best||(d==best&&a[l]<a[bl])){best=d;bl=l;br=r;} if(a[l]+a[r]>=b)r--;else l++;} return {a[bl],a[br]}; }',
      GO: 'func solve(a []int, b int) []int { l,r,bl,br,best:=0,len(a)-1,0,1,-1; for l<r { d:=a[l]+a[r]-b; if d<0 {d=-d}; if best<0 || d<best || (d==best && a[l]<a[bl]) {best=d;bl=l;br=r}; if a[l]+a[r]>=b {r--} else {l++} }; return []int{a[bl],a[br]} }',
    }, tests: [
      { stdin: '1 4 6 8\n10', expectedStdout: '4 6', isSample: true }, { stdin: '1 3 5 7\n8', expectedStdout: '1 7', isSample: true }, { stdin: '-5 -2 4 10\n1', expectedStdout: '-2 4' }, { stdin: '2 7\n20', expectedStdout: '2 7' }, { stdin: '1 2 8 9\n11', expectedStdout: '2 9' },
    ],
  }),
];
