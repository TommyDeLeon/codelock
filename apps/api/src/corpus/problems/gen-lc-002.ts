import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-002` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_002_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "team-wage-gap",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "SLIDING_WINDOW",
    title: "Team Wage Gap",
    patternTags: ["array","sorting","sliding-window"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 600,
    promptMarkdown: "You are assembling a team of `k` workers from a pool, whose hourly rates are given in an array `rates`. To ensure fairness and avoid resentment among team members, you want to minimize the wage disparity within the team.\n\nSpecifically, you want to choose exactly `k` workers such that the absolute difference between the highest wage and the lowest wage in the team is as small as possible. Return this minimum possible difference.\n\n**Constraints**\n- `1 <= k <= rates.length <= 1000`\n- `-1000 <= rates[i] <= 1000`\n\n**Example 1**\n```\ninput: 9 4 1 7\n2\noutput: 2\n```\nBy choosing the workers with rates 9 and 7, the difference is 9 - 7 = 2.\n\n**Example 2**\n```\ninput: 10 100 10 10\n3\noutput: 0\n```\nBy choosing the three workers with rate 10, the difference is 10 - 10 = 0.\n\n**Example 3**\n```\ninput: 5\n1\noutput: 0\n```\nWith only 1 worker chosen, the highest and lowest wages are the same, so the difference is 0.\n\n**Follow-up:** Can you solve it efficiently without checking all combinations?",
    editorialMarkdown: "## Sorting and Sliding Window\n\nTo minimize the difference between the highest and lowest wage in our chosen team of `k` workers, we should choose workers whose wages are as close to each other as possible.\n\nIf we sort the array of wages, any subset of `k` elements that minimizes this difference will simply be a contiguous subarray of length `k`. Therefore, we can sort the array and then use a sliding window of size `k` to find the minimum difference between the last and first elements of the window.\n\nWe iterate from index `i = 0` to `n - k`, calculating the difference `rates[i+k-1] - rates[i]` and keeping track of the minimum difference found.\n\nTime complexity is O(N log N) due to sorting, where N is the length of the rates array. Space complexity is O(1) or O(N) depending on the sorting algorithm used.\n\nThe one trap most solvers hit is trying to check all possible combinations of size `k`, which would result in an exponential time complexity and a Time Limit Exceeded error. Sorting makes the problem trivial.",
    referenceSolution: {
      JAVASCRIPT: "function solve(rates, k) {\n    rates.sort((a, b) => a - b);\n    let minDiff = Infinity;\n    for (let i = 0; i <= rates.length - k; i++) {\n        const diff = rates[i + k - 1] - rates[i];\n        if (diff < minDiff) {\n            minDiff = diff;\n        }\n    }\n    return minDiff;\n}",
      TYPESCRIPT: "function solve(rates: number[], k: number): number {\n    rates.sort((a, b) => a - b);\n    let minDiff = Infinity;\n    for (let i = 0; i <= rates.length - k; i++) {\n        const diff = rates[i + k - 1] - rates[i];\n        if (diff < minDiff) {\n            minDiff = diff;\n        }\n    }\n    return minDiff;\n}",
      PYTHON: "def solve(rates, k):\n    rates.sort()\n    min_diff = float('inf')\n    for i in range(len(rates) - k + 1):\n        diff = rates[i + k - 1] - rates[i]\n        if diff < min_diff:\n            min_diff = diff\n    return min_diff",
      JAVA: "    static int solve(int[] rates, int k) {\n        java.util.Arrays.sort(rates);\n        int minDiff = -1;\n        for (int i = 0; i <= rates.length - k; i++) {\n            int diff = rates[i + k - 1] - rates[i];\n            if (minDiff == -1 || diff < minDiff) {\n                minDiff = diff;\n            }\n        }\n        return minDiff;\n    }",
      CPP: "int solve(vector<int> rates, int k) {\n    sort(rates.begin(), rates.end());\n    int minDiff = -1;\n    for (int i = 0; i <= (int)rates.size() - k; i++) {\n        int diff = rates[i + k - 1] - rates[i];\n        if (minDiff == -1 || diff < minDiff) {\n            minDiff = diff;\n        }\n    }\n    return minDiff;\n}",
      GO: "func solve(rates []int, k int) int {\n    for i := 1; i < len(rates); i++ {\n        key := rates[i]\n        j := i - 1\n        for j >= 0 && rates[j] > key {\n            rates[j+1] = rates[j]\n            j--\n        }\n        rates[j+1] = key\n    }\n    minDiff := -1\n    for i := 0; i <= len(rates)-k; i++ {\n        diff := rates[i+k-1] - rates[i]\n        if minDiff == -1 || diff < minDiff {\n            minDiff = diff\n        }\n    }\n    return minDiff\n}",
    },
    tests: [
      { stdin: "9 4 1 7\n2", expectedStdout: "2", isSample: true },
      { stdin: "10 100 10 10\n3", expectedStdout: "0", isSample: true },
      { stdin: "5\n1", expectedStdout: "0" },
      { stdin: "1 2 3 4 5\n5", expectedStdout: "4" },
      { stdin: "-5 -1 3 8\n2", expectedStdout: "4" },
      { stdin: "1 1 1 1 1\n2", expectedStdout: "0" },
      { stdin: "7 3\n2", expectedStdout: "4" },
      { stdin: "10 2 8 4 6\n3", expectedStdout: "4" },
    ],
  }),

  p({
    ...base,
    slug: "combine-access-codes",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Combine Access Codes",
    patternTags: ["string","math","two-pointers"],
    signatureId: "fn:string,string->string",
    avgSolveSeconds: 600,
    promptMarkdown: "You are managing a legacy security system that stores access codes as binary strings. To generate a master key that can open two zones simultaneously, you need to mathematically add their access codes together using binary addition.\n\nGiven two binary strings `zone1` and `zone2`, return their sum as a binary string.\n\n**Constraints**\n- `1 <= zone1.length, zone2.length <= 1000`\n- `zone1` and `zone2` consist only of `'0'` or `'1'` characters.\n- Each string does not contain leading zeros except for the zero itself.\n\n**Example 1**\n```\ninput: 1010\n1011\noutput: 10101\n```\nThe binary sum of 1010 and 1011 is 10101.\n\n**Example 2**\n```\ninput: 11\n1\noutput: 100\n```\nAdding 1 to 11 results in 100 in binary.\n\n**Example 3**\n```\ninput: 0\n0\noutput: 0\n```\nThe sum of 0 and 0 is 0.\n\n**Follow-up:** Can you perform the addition in a single pass from right to left?",
    editorialMarkdown: "## Bitwise Addition\n\nTo add two binary numbers represented as strings, we can simulate the standard addition process from right to left, just as we do by hand.\n\nWe maintain two pointers, one for the end of each string, and a `carry` variable initialized to 0. At each step, we sum the digits at the current pointers and the carry. The new digit in the result will be `sum % 2`, and the new carry will be `sum / 2` (using integer division). We prepend the resulting digit to our answer string, decrement the pointers, and continue until both strings are exhausted and the carry is zero.\n\nTime complexity is O(max(N, M)) where N and M are the lengths of the two strings. Space complexity is O(max(N, M)) to store the resulting string.\n\nThe one trap most solvers hit is forgetting to process the final carry after both strings have been completely traversed, leading to missing a leading '1' in cases like '11' + '1' = '100'.",
    referenceSolution: {
      JAVASCRIPT: "function solve(zone1, zone2) {\n    let res = \"\";\n    let carry = 0;\n    let i = zone1.length - 1;\n    let j = zone2.length - 1;\n    while (i >= 0 || j >= 0 || carry > 0) {\n        let sum = carry;\n        if (i >= 0) sum += parseInt(zone1[i--]);\n        if (j >= 0) sum += parseInt(zone2[j--]);\n        res = (sum % 2).toString() + res;\n        carry = Math.floor(sum / 2);\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(zone1: string, zone2: string): string {\n    let res = \"\";\n    let carry = 0;\n    let i = zone1.length - 1;\n    let j = zone2.length - 1;\n    while (i >= 0 || j >= 0 || carry > 0) {\n        let sum = carry;\n        if (i >= 0) sum += parseInt(zone1[i--]);\n        if (j >= 0) sum += parseInt(zone2[j--]);\n        res = (sum % 2).toString() + res;\n        carry = Math.floor(sum / 2);\n    }\n    return res;\n}",
      PYTHON: "def solve(zone1, zone2):\n    res = []\n    carry = 0\n    i = len(zone1) - 1\n    j = len(zone2) - 1\n    while i >= 0 or j >= 0 or carry > 0:\n        s = carry\n        if i >= 0:\n            s += int(zone1[i])\n            i -= 1\n        if j >= 0:\n            s += int(zone2[j])\n            j -= 1\n        res.append(str(s % 2))\n        carry = s // 2\n    return \"\".join(reversed(res))",
      JAVA: "    static String solve(String zone1, String zone2) {\n        StringBuilder res = new StringBuilder();\n        int carry = 0;\n        int i = zone1.length() - 1;\n        int j = zone2.length() - 1;\n        while (i >= 0 || j >= 0 || carry > 0) {\n            int sum = carry;\n            if (i >= 0) sum += zone1.charAt(i--) - '0';\n            if (j >= 0) sum += zone2.charAt(j--) - '0';\n            res.append(sum % 2);\n            carry = sum / 2;\n        }\n        return res.reverse().toString();\n    }",
      CPP: "string solve(string zone1, string zone2) {\n    string res = \"\";\n    int carry = 0;\n    int i = zone1.length() - 1;\n    int j = zone2.length() - 1;\n    while (i >= 0 || j >= 0 || carry > 0) {\n        int sum = carry;\n        if (i >= 0) sum += zone1[i--] - '0';\n        if (j >= 0) sum += zone2[j--] - '0';\n        res = to_string(sum % 2) + res;\n        carry = sum / 2;\n    }\n    return res;\n}",
      GO: "func solve(zone1 string, zone2 string) string {\n    res := \"\"\n    carry := 0\n    i := len(zone1) - 1\n    j := len(zone2) - 1\n    for i >= 0 || j >= 0 || carry > 0 {\n        sum := carry\n        if i >= 0 {\n            sum += int(zone1[i] - '0')\n            i--\n        }\n        if j >= 0 {\n            sum += int(zone2[j] - '0')\n            j--\n        }\n        res = string(rune('0' + sum%2)) + res\n        carry = sum / 2\n    }\n    if res == \"\" {\n        return \"0\"\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "1010\n1011", expectedStdout: "10101", isSample: true },
      { stdin: "11\n1", expectedStdout: "100", isSample: true },
      { stdin: "0\n0", expectedStdout: "0", isSample: true },
      { stdin: "111\n111", expectedStdout: "1110" },
      { stdin: "10\n10", expectedStdout: "100" },
      { stdin: "1\n0", expectedStdout: "1" },
      { stdin: "0\n1", expectedStdout: "1" },
      { stdin: "1111\n1111", expectedStdout: "11110" },
    ],
  }),

  p({
    ...base,
    slug: "lucky-number-reduction",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Lucky Number Reduction",
    patternTags: ["math","simulation"],
    signatureId: "fn:int->int",
    avgSolveSeconds: 400,
    promptMarkdown: "A numerologist calculates a person's lucky number by repeatedly summing the digits of their birth number until a single-digit number is reached.\n\nGiven an integer `n` representing a birth number, perform this process and return the resulting single-digit lucky number. If the number is negative, use its absolute value.\n\n**Constraints**\n- `-2^31 <= n <= 2^31 - 1`\n\n**Example 1**\n```\ninput: 38\noutput: 2\n```\nThe process is:\n1. 3 + 8 = 11\n2. 1 + 1 = 2\nSince 2 is a single digit, return it.\n\n**Example 2**\n```\ninput: 0\noutput: 0\n```\n0 is already a single digit.\n\n**Example 3**\n```\ninput: -18\noutput: 9\n```\nThe absolute value is 18. The sum of 1 + 8 is 9, which is a single digit.\n\n**Follow-up:** Can you solve this in O(1) time without any loops or recursion?",
    editorialMarkdown: "## Digital Root Simulation\n\nThe problem asks us to repeatedly sum the digits of a number until we arrive at a single digit. This is mathematically known as the digital root.\n\nThere are two approaches. The first is straight simulation: while the number is greater than 9, convert it to a string (or use arithmetic modulo 10) to sum its digits, then set the number to this sum. This takes logarithmic time.\n\nThe more elegant approach relies on the mathematical property that the digital root of a positive number `n` is simply `n % 9`. If `n % 9 == 0`, the digital root is 9 (unless `n` is 0, in which case the answer is 0).\n\nTime complexity for the mathematical approach is O(1). Space complexity is O(1).\n\nThe one trap most solvers hit is forgetting the edge cases when `n = 0` or when `n` is a multiple of 9, which requires special-cased returns. Negative numbers must also be converted to their absolute value.",
    referenceSolution: {
      JAVASCRIPT: "function solve(n) {\n    let num = Math.abs(n);\n    if (num === 0) return 0;\n    let res = num % 9;\n    return res === 0 ? 9 : res;\n}",
      TYPESCRIPT: "function solve(n: number): number {\n    let num = Math.abs(n);\n    if (num === 0) return 0;\n    let res = num % 9;\n    return res === 0 ? 9 : res;\n}",
      PYTHON: "def solve(n):\n    num = abs(n)\n    if num == 0:\n        return 0\n    res = num % 9\n    return 9 if res == 0 else res",
      JAVA: "    static int solve(int n) {\n        long num = (long) n;\n        if (num == 0) return 0;\n        if (num < 0) num = -num;\n        int res = (int)(num % 9);\n        return res == 0 ? 9 : res;\n    }",
      CPP: "int solve(int n) {\n    if (n == 0) return 0;\n    if (n < 0) {\n        long long num = n;\n        num = num < 0 ? -num : num;\n        if (num % 9 == 0) return 9;\n        return num % 9;\n    }\n    if (n % 9 == 0) return 9;\n    return n % 9;\n}",
      GO: "func solve(n int) int {\n    num := int64(n)\n    if num == 0 {\n        return 0\n    }\n    if num < 0 {\n        num = -num\n    }\n    res := int(num % 9)\n    if res == 0 {\n        return 9\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "38", expectedStdout: "2", isSample: true },
      { stdin: "0", expectedStdout: "0", isSample: true },
      { stdin: "-18", expectedStdout: "9", isSample: true },
      { stdin: "9", expectedStdout: "9" },
      { stdin: "10", expectedStdout: "1" },
      { stdin: "12345", expectedStdout: "6" },
      { stdin: "2147483647", expectedStdout: "1" },
      { stdin: "-2147483648", expectedStdout: "2" },
    ],
  }),

  p({
    ...base,
    slug: "large-payload-sum",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Large Payload Sum",
    patternTags: ["string","math","two-pointers"],
    signatureId: "fn:string,string->string",
    avgSolveSeconds: 600,
    promptMarkdown: "You are processing financial transactions where the monetary amounts are too large to fit into standard integer types. The amounts are provided as strings containing only digits.\n\nYou need to calculate the exact sum of two such amounts and return it as a string.\n\n**Constraints**\n- `1 <= val1.length, val2.length <= 1000`\n- `val1` and `val2` consist of only digits.\n- `val1` and `val2` do not have any leading zeros except for the zero itself.\n\n**Example 1**\n```\ninput: 123\n456\noutput: 579\n```\n123 + 456 = 579.\n\n**Example 2**\n```\ninput: 999\n1\noutput: 1000\n```\n999 + 1 = 1000. Notice the additional digit caused by the carry.\n\n**Example 3**\n```\ninput: 0\n0\noutput: 0\n```\n0 + 0 = 0.\n\n**Follow-up:** Can you solve this without using any built-in big integer libraries?",
    editorialMarkdown: "## Two-Pointer Addition\n\nThe problem asks us to add two large numbers represented as strings. Since the numbers can exceed the limits of standard integer types, we must perform the addition digit by digit, much like we do on paper.\n\nWe can initialize two pointers at the ends of both strings and a `carry` variable set to zero. At each iteration, we parse the digits at the current pointers, add them along with the carry, and append `sum % 10` to our result. The new carry will be `sum / 10`. We then move both pointers leftward. This continues until both pointers reach the beginning of their strings and the carry becomes zero. \n\nTime complexity is O(max(N, M)) where N and M are the lengths of the two strings. Space complexity is O(max(N, M)) for storing the constructed result string.\n\nThe one trap most solvers hit is trying to convert the strings to integers natively. While Python handles arbitrarily large integers automatically, languages like Java, C++, and Go will overflow, resulting in a failure or runtime exception. You must construct the result strictly via string manipulations.",
    referenceSolution: {
      JAVASCRIPT: "function solve(val1, val2) {\n    let res = \"\";\n    let carry = 0;\n    let i = val1.length - 1;\n    let j = val2.length - 1;\n    while (i >= 0 || j >= 0 || carry > 0) {\n        let sum = carry;\n        if (i >= 0) sum += parseInt(val1[i--]);\n        if (j >= 0) sum += parseInt(val2[j--]);\n        res = (sum % 10).toString() + res;\n        carry = Math.floor(sum / 10);\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(val1: string, val2: string): string {\n    let res = \"\";\n    let carry = 0;\n    let i = val1.length - 1;\n    let j = val2.length - 1;\n    while (i >= 0 || j >= 0 || carry > 0) {\n        let sum = carry;\n        if (i >= 0) sum += parseInt(val1[i--]);\n        if (j >= 0) sum += parseInt(val2[j--]);\n        res = (sum % 10).toString() + res;\n        carry = Math.floor(sum / 10);\n    }\n    return res;\n}",
      PYTHON: "def solve(val1, val2):\n    res = []\n    carry = 0\n    i = len(val1) - 1\n    j = len(val2) - 1\n    while i >= 0 or j >= 0 or carry > 0:\n        s = carry\n        if i >= 0:\n            s += int(val1[i])\n            i -= 1\n        if j >= 0:\n            s += int(val2[j])\n            j -= 1\n        res.append(str(s % 10))\n        carry = s // 10\n    return \"\".join(reversed(res))",
      JAVA: "    static String solve(String val1, String val2) {\n        StringBuilder res = new StringBuilder();\n        int carry = 0;\n        int i = val1.length() - 1;\n        int j = val2.length() - 1;\n        while (i >= 0 || j >= 0 || carry > 0) {\n            int sum = carry;\n            if (i >= 0) sum += val1.charAt(i--) - '0';\n            if (j >= 0) sum += val2.charAt(j--) - '0';\n            res.append(sum % 10);\n            carry = sum / 10;\n        }\n        return res.reverse().toString();\n    }",
      CPP: "string solve(string val1, string val2) {\n    string res = \"\";\n    int carry = 0;\n    int i = val1.length() - 1;\n    int j = val2.length() - 1;\n    while (i >= 0 || j >= 0 || carry > 0) {\n        int sum = carry;\n        if (i >= 0) sum += val1[i--] - '0';\n        if (j >= 0) sum += val2[j--] - '0';\n        res = to_string(sum % 10) + res;\n        carry = sum / 10;\n    }\n    return res;\n}",
      GO: "func solve(val1 string, val2 string) string {\n    res := \"\"\n    carry := 0\n    i := len(val1) - 1\n    j := len(val2) - 1\n    for i >= 0 || j >= 0 || carry > 0 {\n        sum := carry\n        if i >= 0 {\n            sum += int(val1[i] - '0')\n            i--\n        }\n        if j >= 0 {\n            sum += int(val2[j] - '0')\n            j--\n        }\n        res = string(rune('0' + sum%10)) + res\n        carry = sum / 10\n    }\n    if res == \"\" {\n        return \"0\"\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "123\n456", expectedStdout: "579", isSample: true },
      { stdin: "999\n1", expectedStdout: "1000", isSample: true },
      { stdin: "0\n0", expectedStdout: "0", isSample: true },
      { stdin: "123456789\n987654321", expectedStdout: "1111111110" },
      { stdin: "1\n999", expectedStdout: "1000" },
      { stdin: "50\n50", expectedStdout: "100" },
      { stdin: "0\n123", expectedStdout: "123" },
      { stdin: "888\n222", expectedStdout: "1110" },
    ],
  }),

  p({
    ...base,
    slug: "boost-sensor-readings",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Boost Sensor Readings",
    patternTags: ["array","math"],
    signatureId: "fn:ints,int->ints",
    avgSolveSeconds: 500,
    promptMarkdown: "You receive a high-precision measurement represented as an array of single digits, where the most significant digit is first. Due to calibration adjustments, you need to add a positive integer `boost` to this measurement.\n\nReturn the new measurement as an array of digits representing the sum.\n\n**Constraints**\n- `1 <= digits.length <= 1000`\n- `0 <= digits[i] <= 9`\n- `0 <= boost <= 10^9`\n- The `digits` array does not contain leading zeros except for the zero itself.\n\n**Example 1**\n```\ninput: 1 2 0 0\n34\noutput: 1 2 3 4\n```\n1200 + 34 = 1234.\n\n**Example 2**\n```\ninput: 2 7 4\n181\noutput: 4 5 5\n```\n274 + 181 = 455.\n\n**Example 3**\n```\ninput: 9 9 9\n1\noutput: 1 0 0 0\n```\n999 + 1 = 1000. Notice how the array length increases by 1.\n\n**Follow-up:** Can you perform the addition directly in-place without converting the array of digits into an integer?",
    editorialMarkdown: "## Array Arithmetic\n\nAdding an integer to an array-form integer requires us to mutate the array in place or construct a new array from right to left. We can treat the `boost` value entirely as our initial \"carry\". \n\nStarting from the rightmost digit of the array, we add the carry to the digit. The new value at that position becomes `(digit + carry) % 10`, and the new carry becomes `(digit + carry) / 10`. We proceed leftwards until the array is exhausted. If a carry still remains, we must prepend its digits to the front of the array (e.g., adding 100 to `[0]` yields `[1, 0, 0]`).\n\nTime complexity is O(max(N, log(K))) where N is the length of the array and K is the boost value. Space complexity is O(max(N, log(K))) for storing the new array-form digits.\n\nThe one trap most solvers hit is repeatedly inserting at the front of an array or string during the carry-over phase, resulting in O(N^2) complexity. You should append digits to an intermediate collection, then reverse it, or allocate an array of the precise required size upfront.",
    referenceSolution: {
      JAVASCRIPT: "function solve(digits, boost) {\n    let carry = boost;\n    let res = [];\n    for (let i = digits.length - 1; i >= 0; i--) {\n        let sum = digits[i] + carry;\n        res.push(sum % 10);\n        carry = Math.floor(sum / 10);\n    }\n    while (carry > 0) {\n        res.push(carry % 10);\n        carry = Math.floor(carry / 10);\n    }\n    return res.reverse();\n}",
      TYPESCRIPT: "function solve(digits: number[], boost: number): number[] {\n    let carry = boost;\n    let res: number[] = [];\n    for (let i = digits.length - 1; i >= 0; i--) {\n        let sum = digits[i] + carry;\n        res.push(sum % 10);\n        carry = Math.floor(sum / 10);\n    }\n    while (carry > 0) {\n        res.push(carry % 10);\n        carry = Math.floor(carry / 10);\n    }\n    return res.reverse();\n}",
      PYTHON: "def solve(digits, boost):\n    carry = boost\n    res = []\n    for d in reversed(digits):\n        s = d + carry\n        res.append(s % 10)\n        carry = s // 10\n    while carry > 0:\n        res.append(carry % 10)\n        carry //= 10\n    return list(reversed(res))",
      JAVA: "    static int[] solve(int[] digits, int boost) {\n        int carry = boost;\n        java.util.List<Integer> list = new java.util.ArrayList<>();\n        for (int i = digits.length - 1; i >= 0; i--) {\n            int sum = digits[i] + carry;\n            list.add(sum % 10);\n            carry = sum / 10;\n        }\n        while (carry > 0) {\n            list.add(carry % 10);\n            carry /= 10;\n        }\n        int[] res = new int[list.size()];\n        for (int i = 0; i < list.size(); i++) {\n            res[i] = list.get(list.size() - 1 - i);\n        }\n        return res;\n    }",
      CPP: "vector<int> solve(vector<int> digits, int boost) {\n    int carry = boost;\n    for (int i = (int)digits.size() - 1; i >= 0; i--) {\n        int sum = digits[i] + carry;\n        digits[i] = sum % 10;\n        carry = sum / 10;\n    }\n    vector<int> res;\n    while (carry > 0) {\n        res.push_back(carry % 10);\n        carry /= 10;\n    }\n    reverse(res.begin(), res.end());\n    res.insert(res.end(), digits.begin(), digits.end());\n    return res;\n}",
      GO: "func solve(digits []int, boost int) []int {\n    carry := boost\n    for i := len(digits) - 1; i >= 0; i-- {\n        sum := digits[i] + carry\n        digits[i] = sum % 10\n        carry = sum / 10\n    }\n    var res []int\n    for carry > 0 {\n        res = append(res, carry%10)\n        carry /= 10\n    }\n    for i := 0; i < len(res)/2; i++ {\n        res[i], res[len(res)-1-i] = res[len(res)-1-i], res[i]\n    }\n    return append(res, digits...)\n}",
    },
    tests: [
      { stdin: "1 2 0 0\n34", expectedStdout: "1 2 3 4", isSample: true },
      { stdin: "2 7 4\n181", expectedStdout: "4 5 5", isSample: true },
      { stdin: "9 9 9\n1", expectedStdout: "1 0 0 0", isSample: true },
      { stdin: "0\n0", expectedStdout: "0" },
      { stdin: "0\n100", expectedStdout: "1 0 0" },
      { stdin: "1 2 3\n0", expectedStdout: "1 2 3" },
      { stdin: "9\n9", expectedStdout: "1 8" },
      { stdin: "8 9 9\n1", expectedStdout: "9 0 0" },
    ],
  }),

  p({
    ...base,
    slug: "net-balance-change",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Net Balance Change",
    patternTags: ["math"],
    signatureId: "fn:int,int->int",
    avgSolveSeconds: 300,
    promptMarkdown: "You are maintaining a simple bank account application. Over a single period, there are two financial transactions, `a` and `b`. These can be positive (deposits) or negative (withdrawals). \n\nCalculate and return the net change to the account balance.\n\n**Constraints**\n- `-100 <= a, b <= 100`\n\n**Example 1**\n```\ninput: 10\n20\noutput: 30\n```\nTwo deposits of 10 and 20 result in a net change of 30.\n\n**Example 2**\n```\ninput: -10\n-20\noutput: -30\n```\nTwo withdrawals of 10 and 20 result in a net change of -30.\n\n**Example 3**\n```\ninput: 100\n-50\noutput: 50\n```\nA deposit of 100 and a withdrawal of 50 result in a net change of 50.\n\n**Follow-up:** Can you solve this in exactly one line of code?",
    editorialMarkdown: "## Basic Arithmetic\n\nThis is a fundamental problem testing language fluency. The solution requires taking two arguments and returning their sum using the standard addition operator `+`.\n\nThere are no edge cases or complexities because the constraints fit well within standard integer bounds for any modern programming language, making the time complexity and space complexity strictly O(1).\n\nThe one trap most solvers hit is overcomplicating the solution. A direct `return a + b` is sufficient.",
    referenceSolution: {
      JAVASCRIPT: "function solve(a, b) {\n    return a + b;\n}",
      TYPESCRIPT: "function solve(a: number, b: number): number {\n    return a + b;\n}",
      PYTHON: "def solve(a, b):\n    return a + b",
      JAVA: "    static int solve(int a, int b) {\n        return a + b;\n    }",
      CPP: "int solve(int a, int b) {\n    return a + b;\n}",
      GO: "func solve(a int, b int) int {\n    return a + b\n}",
    },
    tests: [
      { stdin: "10\n20", expectedStdout: "30", isSample: true },
      { stdin: "-10\n-20", expectedStdout: "-30", isSample: true },
      { stdin: "100\n-50", expectedStdout: "50", isSample: true },
      { stdin: "0\n0", expectedStdout: "0" },
      { stdin: "-50\n100", expectedStdout: "50" },
      { stdin: "1\n-1", expectedStdout: "0" },
      { stdin: "999\n1", expectedStdout: "1000" },
      { stdin: "0\n5", expectedStdout: "5" },
    ],
  }),
];
