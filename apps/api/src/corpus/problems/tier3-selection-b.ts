import { AUTHORED, type ProblemDefinition } from '../problem.js';

const p=(d:ProblemDefinition):ProblemDefinition=>d;
const base={tier:'TIER_3',provenance:AUTHORED} as const;
const six = (JAVASCRIPT: string, TYPESCRIPT: string, PYTHON: string, JAVA: string, CPP: string, GO: string) => ({ JAVASCRIPT, TYPESCRIPT, PYTHON, JAVA, CPP, GO });

export const TIER_3_SELECTION_B_PROBLEMS: ProblemDefinition[]=[
  p({...base,slug:'kth-largest-counting-copies',title:'K-th Largest, Copies Included',difficulty:'EASY',patternFamily:'HEAP_PRIORITY_QUEUE',patternTags:['selection','sorting','duplicates'],signatureId:'fn:ints,int->int',avgSolveSeconds:480,promptMarkdown:['Return the k-th largest number, counting every copy as its own rank.','','**Example**','','```','input: 5 2 5 1','2','output: 5','```','','The list has at least k numbers. The named edge case is duplicates: the two 5 values occupy first and second place.'].join('\n'),editorialMarkdown:['## Rank after descending order','','Sorting the complete list descending puts each occurrence at the index matching its rank, so index `k - 1` is the answer. Keeping copies is essential because a rank describes positions, not distinct values. A heap can also remove the largest value k times, but sorting is especially direct for this bounded list.','','The quiet mistake is placing values in a set before sorting. That silently turns the question into distinct selection and fails only when a duplicate crosses the requested rank. Time is O(n log n), bounded by the number of input values, and copied sorting storage is O(n).'].join('\n'),referenceSolution:six('function solve(a,b){return a.slice().sort((x,y)=>y-x)[b-1];}','function solve(a:number[],b:number):number{return [...a].sort((x,y)=>y-x)[b-1];}','def solve(a,b):\n return sorted(a,reverse=True)[b-1]','    static int solve(int[]a,int b){java.util.Arrays.sort(a);return a[a.length-b];}','int solve(vector<int>a,int b){for(int i=0;i<(int)a.size();i++)for(int j=i+1;j<(int)a.size();j++)if(a[j]>a[i]){int t=a[i];a[i]=a[j];a[j]=t;}return a[b-1];}','func solve(a []int,b int)int{for i:=1;i<len(a);i++{for j:=i;j>0&&a[j]>a[j-1];j--{a[j],a[j-1]=a[j-1],a[j]}};return a[b-1]}'),tests:[{stdin:'5 2 5 1\n2',expectedStdout:'5',isSample:true},{stdin:'-1 -3 -2\n1',expectedStdout:'-1',isSample:true},{stdin:'7\n1',expectedStdout:'7'},{stdin:'4 4 4\n3',expectedStdout:'4'}]}),
  p({...base,slug:'odd-list-middle-value',title:'Middle Value of an Odd List',difficulty:'EASY',patternFamily:'ARRAYS_HASHING',patternTags:['sorting','median','selection'],signatureId:'fn:ints->int',avgSolveSeconds:420,promptMarkdown:['The list has odd length. Sort it and return its single middle value.','','**Example**','','```','input: 9 1 5','output: 5','```','','There is always at least one number. Repeated values stay in the list, so they can be the middle value.'].join('\n'),editorialMarkdown:['## Odd-rank median','','An odd-sized sorted list has one unambiguous center: index `length / 2` using integer division. Sorting exposes that rank without needing a special median formula, and duplicate values naturally remain at their positions.','','The quiet mistake is averaging the two neighbors, a rule that belongs to even-sized data and can produce a value not present in the list. Sorting takes O(n log n), bounded by list length, and the copied sorted list uses O(n) extra space.'].join('\n'),referenceSolution:six('function solve(a){return a.slice().sort((x,y)=>x-y)[a.length>>1];}','function solve(a:number[]):number{return [...a].sort((x,y)=>x-y)[a.length>>1];}','def solve(a):\n b=sorted(a);return b[len(b)//2]','    static int solve(int[]a){java.util.Arrays.sort(a);return a[a.length/2];}','int solve(vector<int>a){for(int i=0;i<(int)a.size();i++)for(int j=i+1;j<(int)a.size();j++)if(a[j]<a[i]){int t=a[i];a[i]=a[j];a[j]=t;}return a[a.size()/2];}','func solve(a []int)int{for i:=1;i<len(a);i++{for j:=i;j>0&&a[j]<a[j-1];j--{a[j],a[j-1]=a[j-1],a[j]}};return a[len(a)/2]}'),tests:[{stdin:'9 1 5',expectedStdout:'5',isSample:true},{stdin:'4 4 1',expectedStdout:'4',isSample:true},{stdin:'-2',expectedStdout:'-2'},{stdin:'8 2 6 4 0',expectedStdout:'4'}]}),
  p({...base,slug:'inversion-pair-total',title:'Total Out-of-Order Pairs',difficulty:'MEDIUM',patternFamily:'ARRAYS_HASHING',patternTags:['sorting','inversions','counting'],signatureId:'fn:ints->int',avgSolveSeconds:660,promptMarkdown:['Count index pairs `(i, j)` with `i < j` and the earlier value larger than the later value.','','**Example**','','```','input: 3 1 2','output: 2','```','','The list has at most 200 numbers. Equal values are not out of order and contribute zero.'].join('\n'),editorialMarkdown:['## Direct inversion scan','','For each left index, compare it with every later index and add one exactly when the pair descends. This directly implements the definition, which is clear and fast enough for the stated short-list bound. Each inversion is visited once because the outer index is always the earlier position.','','The quiet mistake is treating equal values as inversions by using `>=`; that error hides in lists with all distinct values. The nested scan is O(n²), bounded by the at-most-200 input length, and uses O(1) extra space.'].join('\n'),referenceSolution:six('function solve(a){let c=0;for(let i=0;i<a.length;i++)for(let j=i+1;j<a.length;j++)if(a[i]>a[j])c++;return c;}','function solve(a:number[]):number{let c=0;for(let i=0;i<a.length;i++)for(let j=i+1;j<a.length;j++)if(a[i]>a[j])c++;return c;}','def solve(a):\n c=0\n for i in range(len(a)):\n  for j in range(i+1,len(a)):\n   if a[i]>a[j]:c+=1\n return c','    static int solve(int[]a){int c=0;for(int i=0;i<a.length;i++)for(int j=i+1;j<a.length;j++)if(a[i]>a[j])c++;return c;}','int solve(vector<int>a){int c=0;for(int i=0;i<(int)a.size();i++)for(int j=i+1;j<(int)a.size();j++)if(a[i]>a[j])c++;return c;}','func solve(a []int)int{c:=0;for i:=0;i<len(a);i++{for j:=i+1;j<len(a);j++{if a[i]>a[j]{c++}}};return c}'),tests:[{stdin:'3 1 2',expectedStdout:'2',isSample:true},{stdin:'1 2 3',expectedStdout:'0',isSample:true},{stdin:'2 2 1',expectedStdout:'2'},{stdin:'4',expectedStdout:'0'}]}),
  p({...base,slug:'single-reversal-sorted-check',title:'Can One Reversal Sort It?',difficulty:'MEDIUM',patternFamily:'TWO_POINTERS',patternTags:['sorting','reversal','validation'],signatureId:'fn:ints->bool',avgSolveSeconds:720,promptMarkdown:['Return whether reversing one contiguous section, possibly a section of length one, can make the list ascending.','','**Example**','','```','input: 1 4 3 2 5','output: true','```','','The list is non-empty. An already sorted list returns true because a one-element reversal changes nothing.'].join('\n'),editorialMarkdown:['## Sorted-target interval','','Make a sorted copy and locate the first and last positions where it differs from the original. Reversing precisely that interval is the only candidate needed; compare its result with the sorted target. Matching outside the interval is already fixed, so no successful reversal needs to touch it.','','The quiet mistake is returning false for an already sorted list because no mismatch was found. A length-one reversal is allowed. Sorting and the final comparison cost O(n log n), bounded by list length, with O(n) copied storage.'].join('\n'),referenceSolution:six('function solve(a){let b=a.slice().sort((x,y)=>x-y),l=0,r=a.length-1;while(l<a.length&&a[l]===b[l])l++;while(r>=0&&a[r]===b[r])r--;if(l>r)return true;let c=a.slice();while(l<r){[c[l],c[r]]=[c[r],c[l]];l++;r--;}return c.every((x,i)=>x===b[i]);}','function solve(a:number[]):boolean{const b=[...a].sort((x,y)=>x-y);let l=0,r=a.length-1;while(l<a.length&&a[l]===b[l])l++;while(r>=0&&a[r]===b[r])r--;if(l>r)return true;const c=[...a];for(;l<r;l++,r--)[c[l],c[r]]=[c[r],c[l]];return c.every((x,i)=>x===b[i]);}','def solve(a):\n b=sorted(a);l=0;r=len(a)-1\n while l<len(a) and a[l]==b[l]:l+=1\n while r>=0 and a[r]==b[r]:r-=1\n if l>r:return True\n c=a[:];c[l:r+1]=reversed(c[l:r+1]);return c==b','    static boolean solve(int[]a){int[]b=a.clone();java.util.Arrays.sort(b);int l=0,r=a.length-1;while(l<a.length&&a[l]==b[l])l++;while(r>=0&&a[r]==b[r])r--;if(l>r)return true;int[]c=a.clone();while(l<r){int t=c[l];c[l++]=c[r];c[r--]=t;}return java.util.Arrays.equals(c,b);}','bool solve(vector<int>a){vector<int>b=a;for(int i=0;i<(int)b.size();i++)for(int j=i+1;j<(int)b.size();j++)if(b[j]<b[i]){int t=b[i];b[i]=b[j];b[j]=t;}int l=0,r=a.size()-1;while(l<(int)a.size()&&a[l]==b[l])l++;while(r>=0&&a[r]==b[r])r--;while(l<r){int t=a[l];a[l++]=a[r];a[r--]=t;}return a==b;}','func solve(a []int)bool{b:=append([]int{},a...);for i:=1;i<len(b);i++{for j:=i;j>0&&b[j]<b[j-1];j--{b[j],b[j-1]=b[j-1],b[j]}};l,r:=0,len(a)-1;for l<len(a)&&a[l]==b[l]{l++};for r>=0&&a[r]==b[r]{r--};for l<r{a[l],a[r]=a[r],a[l];l++;r--};for i:=range a{if a[i]!=b[i]{return false}};return true}'),tests:[{stdin:'1 4 3 2 5',expectedStdout:'true',isSample:true},{stdin:'1 2 3',expectedStdout:'true',isSample:true},{stdin:'3 1 2',expectedStdout:'false'},{stdin:'2 1',expectedStdout:'true'}]}),
  p({...base,slug:'stable-pivot-value-order',title:'Stable Pivot Value Order',difficulty:'MEDIUM',patternFamily:'TWO_POINTERS',patternTags:['partition','stable-order','sorting'],signatureId:'fn:ints,int->ints',avgSolveSeconds:660,promptMarkdown:['Return the numbers less than pivot, then equal to pivot, then greater than pivot. Preserve their original left-to-right order within each group.','','**Example**','','```','input: 5 2 5 1 7','5','output: 2 1 5 5 7','```','','The list is non-empty. The named edge case is a missing pivot value: the middle group is then empty.'].join('\n'),editorialMarkdown:['## Stable three-way partition','','Scan once and append each number to the less, equal, or greater bucket. Concatenating those buckets gives the required group order, while append order preserves the promised order inside every group. Unlike an in-place partition, this specification intentionally values stability.','','The quiet mistake is using swaps from ordinary quicksort partitioning: the groups are right but their internal order changes quietly. The scan is O(n), bounded by list length, and the three output buckets together use O(n) space.'].join('\n'),referenceSolution:six('function solve(a,b){let l=[],e=[],g=[];for(let x of a)(x<b?l:x===b?e:g).push(x);return l.concat(e,g);}','function solve(a:number[],b:number):number[]{const l:number[]=[],e:number[]=[],g:number[]=[];for(const x of a)(x<b?l:x===b?e:g).push(x);return [...l,...e,...g];}','def solve(a,b):\n l=[];e=[];g=[]\n for x in a:(l if x<b else e if x==b else g).append(x)\n return l+e+g','    static int[] solve(int[]a,int b){java.util.ArrayList<Integer>l=new java.util.ArrayList<>(),e=new java.util.ArrayList<>(),g=new java.util.ArrayList<>();for(int x:a){if(x<b)l.add(x);else if(x==b)e.add(x);else g.add(x);}l.addAll(e);l.addAll(g);int[]r=new int[l.size()];for(int i=0;i<r.length;i++)r[i]=l.get(i);return r;}','vector<int> solve(vector<int>a,int b){vector<int>l,e,g;for(int x:a){if(x<b)l.push_back(x);else if(x==b)e.push_back(x);else g.push_back(x);}l.insert(l.end(),e.begin(),e.end());l.insert(l.end(),g.begin(),g.end());return l;}','func solve(a []int,b int)[]int{l:=[]int{};e:=[]int{};g:=[]int{};for _,x:=range a{if x<b{l=append(l,x)}else if x==b{e=append(e,x)}else{g=append(g,x)}};return append(append(l,e...),g...)}'),tests:[{stdin:'5 2 5 1 7\n5',expectedStdout:'2 1 5 5 7',isSample:true},{stdin:'3 1 2\n9',expectedStdout:'3 1 2',isSample:true},{stdin:'2 2 1 2\n2',expectedStdout:'1 2 2 2'},{stdin:'-1 4 0\n0',expectedStdout:'-1 0 4'}]}),
  p({...base,slug:'smallest-pair-sum-rank',title:'K-th Smallest Pair Sum',difficulty:'HARD',patternFamily:'HEAP_PRIORITY_QUEUE',patternTags:['selection','pairs','sorting'],signatureId:'fn:ints,int->int',avgSolveSeconds:960,promptMarkdown:['Consider every pair of different positions `i < j`. Return the k-th smallest pair sum, counting equal sums separately.','','**Example**','','```','input: 1 3 2','2','output: 4','```','','The list has 2 through 60 numbers and k is valid among its pairs. Equal sums occupy separate ranks.'].join('\n'),editorialMarkdown:['## Enumerate then select','','With at most 60 numbers there are only 1,770 position pairs, so enumerate every sum, sort those sums, and take rank `k - 1`. Position pairs—not distinct values or distinct totals—are the objects being ranked, which is why repeated sums stay repeated in the temporary list.','','The quiet mistake is using a set of sums, which makes ties vanish and shifts every later rank. There are p = n(n-1)/2 pairs; enumeration is O(p), sorting is O(p log p), bounded by that pair count, and storage is O(p).'].join('\n'),referenceSolution:six('function solve(a,b){let s=[];for(let i=0;i<a.length;i++)for(let j=i+1;j<a.length;j++)s.push(a[i]+a[j]);return s.sort((x,y)=>x-y)[b-1];}','function solve(a:number[],b:number):number{const s:number[]=[];for(let i=0;i<a.length;i++)for(let j=i+1;j<a.length;j++)s.push(a[i]+a[j]);return s.sort((x,y)=>x-y)[b-1];}','def solve(a,b):\n s=[]\n for i in range(len(a)):\n  for j in range(i+1,len(a)):s.append(a[i]+a[j])\n return sorted(s)[b-1]','    static int solve(int[]a,int b){java.util.ArrayList<Integer>s=new java.util.ArrayList<>();for(int i=0;i<a.length;i++)for(int j=i+1;j<a.length;j++)s.add(a[i]+a[j]);s.sort(null);return s.get(b-1);}','int solve(vector<int>a,int b){vector<int>s;for(int i=0;i<(int)a.size();i++)for(int j=i+1;j<(int)a.size();j++)s.push_back(a[i]+a[j]);for(int i=0;i<(int)s.size();i++)for(int j=i+1;j<(int)s.size();j++)if(s[j]<s[i]){int t=s[i];s[i]=s[j];s[j]=t;}return s[b-1];}','func solve(a []int,b int)int{s:=[]int{};for i:=0;i<len(a);i++{for j:=i+1;j<len(a);j++{s=append(s,a[i]+a[j])}};for i:=1;i<len(s);i++{for j:=i;j>0&&s[j]<s[j-1];j--{s[j],s[j-1]=s[j-1],s[j]}};return s[b-1]}'),tests:[{stdin:'1 3 2\n2',expectedStdout:'4',isSample:true},{stdin:'1 1 1\n3',expectedStdout:'2',isSample:true},{stdin:'-2 4 0\n1',expectedStdout:'-2'},{stdin:'5 1\n1',expectedStdout:'6'}]}),
];
