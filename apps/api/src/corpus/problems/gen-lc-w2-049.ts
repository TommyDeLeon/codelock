import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w2-049` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W2_049_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "allocate-vm-memory",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Allocate VM Memory",
    patternTags: ["math","greedy"],
    signatureId: "fn:int,int->int",
    avgSolveSeconds: 900,
    promptMarkdown: "You are an admin allocating `totalMemory` gigabytes to `vmCount` virtual machines.\nEach VM must receive at least 1 GB of memory.\nTo optimize server performance, you want to allocate exactly 8 GB to as many VMs as possible.\nHowever, due to a bug in the hypervisor, no VM can be allocated exactly 4 GB of memory. If any VM would receive exactly 4 GB, you must distribute the memory differently.\n\nReturn the maximum number of VMs that can receive exactly 8 GB of memory. If it's impossible to give at least 1 GB to every VM, return `-1`. All memory must be distributed.\n\n**Constraints**\n- `1 <= totalMemory <= 200`\n- `1 <= vmCount <= 50`\n\n**Example 1**\n```\ninput:\n20\n3\noutput: 1\n```\n*Explanation: We have 3 VMs. We can give them 8, 10, and 2 GB. (One VM gets 8 GB).*\n\n**Example 2**\n```\ninput:\n16\n2\noutput: 2\n```\n*Explanation: We have 2 VMs. We can give exactly 8 GB to both.*\n\n**Example 3**\n```\ninput:\n2\n3\noutput: -1\n```\n*Explanation: We cannot even satisfy the constraint of 1 GB per VM.*\n\n**Follow-up**\nCan you compute the answer in constant mathcal{O}(1) time?",
    editorialMarkdown: "## Allocate VM Memory\nThis problem asks us to distribute `totalMemory` across `vmCount` instances such that each has at least 1 GB and we maximize the number of instances with exactly 8 GB. Since each VM must have at least 1 GB, we can first subtract `vmCount` from `totalMemory`. We then try to give chunks of 7 GB (since 1+7 = 8) to as many VMs as possible.\n\n**Trap**: Be careful with the condition that no VM can have exactly 4 GB. If giving out the remaining memory leaves one VM with 4 GB, we may have to 'break' one of our 8 GB VMs (reducing the count by 1) to redistribute the remainder differently.\n\n**Complexity:**\n- **Time Complexity:** \\mathcal{O}(1) since the solution uses a direct formula with division and modulo.\n- **Space Complexity:** \\mathcal{O}(1) as only a few variables are used.",
    referenceSolution: {
      JAVASCRIPT: "function solve(totalMemory, vmCount) {\n    if (totalMemory < vmCount) return -1;\n    totalMemory -= vmCount;\n    if (totalMemory === 0) return 0;\n    const eights = Math.floor(totalMemory / 7);\n    const rem = totalMemory % 7;\n    if (eights > vmCount) return vmCount - 1;\n    if (eights === vmCount && rem === 0) return vmCount;\n    if (eights === vmCount && rem !== 0) return vmCount - 1;\n    if (eights === vmCount - 1 && rem === 3) return vmCount - 2;\n    return eights;\n}",
      TYPESCRIPT: "function solve(totalMemory: number, vmCount: number): number {\n    if (totalMemory < vmCount) return -1;\n    totalMemory -= vmCount;\n    if (totalMemory === 0) return 0;\n    const eights = Math.floor(totalMemory / 7);\n    const rem = totalMemory % 7;\n    if (eights > vmCount) return vmCount - 1;\n    if (eights === vmCount && rem === 0) return vmCount;\n    if (eights === vmCount && rem !== 0) return vmCount - 1;\n    if (eights === vmCount - 1 && rem === 3) return vmCount - 2;\n    return eights;\n}",
      PYTHON: "def solve(totalMemory, vmCount):\n    if totalMemory < vmCount:\n        return -1\n    totalMemory -= vmCount\n    if totalMemory == 0:\n        return 0\n    eights = totalMemory // 7\n    rem = totalMemory % 7\n    if eights > vmCount:\n        return vmCount - 1\n    if eights == vmCount and rem == 0:\n        return vmCount\n    if eights == vmCount and rem != 0:\n        return vmCount - 1\n    if eights == vmCount - 1 and rem == 3:\n        return vmCount - 2\n    return eights",
      JAVA: "    static int solve(int totalMemory, int vmCount) {\n        if (totalMemory < vmCount) return -1;\n        totalMemory -= vmCount;\n        if (totalMemory == 0) return 0;\n        int eights = totalMemory / 7;\n        int rem = totalMemory % 7;\n        if (eights > vmCount) return vmCount - 1;\n        if (eights == vmCount && rem == 0) return vmCount;\n        if (eights == vmCount && rem != 0) return vmCount - 1;\n        if (eights == vmCount - 1 && rem == 3) return vmCount - 2;\n        return eights;\n    }",
      CPP: "#include <vector>\n\nusing namespace std;\n\nint solve(int totalMemory, int vmCount) {\n    if (totalMemory < vmCount) return -1;\n    totalMemory -= vmCount;\n    if (totalMemory == 0) return 0;\n    int eights = totalMemory / 7;\n    int rem = totalMemory % 7;\n    if (eights > vmCount) return vmCount - 1;\n    if (eights == vmCount && rem == 0) return vmCount;\n    if (eights == vmCount && rem != 0) return vmCount - 1;\n    if (eights == vmCount - 1 && rem == 3) return vmCount - 2;\n    return eights;\n}",
      GO: "func solve(totalMemory int, vmCount int) int {\n    if totalMemory < vmCount {\n        return -1\n    }\n    totalMemory -= vmCount\n    if totalMemory == 0 {\n        return 0\n    }\n    eights := totalMemory / 7\n    rem := totalMemory % 7\n    if eights > vmCount {\n        return vmCount - 1\n    }\n    if eights == vmCount && rem == 0 {\n        return vmCount\n    }\n    if eights == vmCount && rem != 0 {\n        return vmCount - 1\n    }\n    if eights == vmCount - 1 && rem == 3 {\n        return vmCount - 2\n    }\n    return eights\n}",
    },
    tests: [
      { stdin: "20\n3", expectedStdout: "1", isSample: true },
      { stdin: "16\n2", expectedStdout: "2", isSample: true },
      { stdin: "2\n3", expectedStdout: "-1" },
      { stdin: "1\n1", expectedStdout: "0" },
      { stdin: "15\n2", expectedStdout: "1" },
      { stdin: "12\n2", expectedStdout: "0" },
      { stdin: "24\n3", expectedStdout: "3" },
      { stdin: "100\n10", expectedStdout: "9" },
    ],
  }),

  p({
    ...base,
    slug: "split-journey-cost",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Split Journey Cost",
    patternTags: ["arrays","greedy","sorting"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 600,
    promptMarkdown: "A merchant must split their long journey into exactly 3 consecutive phases.\nThe array `tolls` represents the toll fees at each city along the route. A phase's cost is simply the toll fee of the city where that phase begins.\nThe first phase must always start at the first city (index `0`). You must choose two other distinct cities to start the second and third phases.\nReturn the minimum possible total cost of the three phases.\n\n**Constraints**\n- `3 <= tolls.length <= 50`\n- `1 <= tolls[i] <= 1000`\n\n**Example 1**\n```\ninput:\n10 3 1 1 5\noutput: 12\n```\n*Explanation: The phases start at indices 0, 2, and 3. Cost is 10 + 1 + 1 = 12.*\n\n**Example 2**\n```\ninput:\n5 2 3\noutput: 10\n```\n*Explanation: With only 3 cities, you must start the phases at 0, 1, and 2. Cost is 5 + 2 + 3 = 10.*\n\n**Example 3**\n```\ninput:\n50 40 30 20 10\noutput: 80\n```\n*Explanation: The phases start at indices 0, 3, and 4. Cost is 50 + 20 + 10 = 80.*\n\n**Follow-up**\nCan you find the two minimums without fully sorting the array?",
    editorialMarkdown: "## Split Journey Cost\nTo split the journey into 3 phases, we must pick exactly 2 split points (where phases 2 and 3 start) after the initial starting point at index 0. This means we are essentially asked to find the sum of the first element plus the two smallest elements in the rest of the array.\n\n**Trap**: Make sure to track both the minimum and second minimum elements properly while iterating. If you sort the subarray instead, be aware it raises the time complexity.\n\n**Complexity:**\n- **Time Complexity:** \\mathcal{O}(N) as we only need to pass through the array once to find the two smallest values.\n- **Space Complexity:** \\mathcal{O}(1) as we only need two variables to keep track of the minimum values.",
    referenceSolution: {
      JAVASCRIPT: "function solve(tolls) {\n    let min1 = Infinity, min2 = Infinity;\n    for (let i = 1; i < tolls.length; i++) {\n        if (tolls[i] < min1) {\n            min2 = min1;\n            min1 = tolls[i];\n        } else if (tolls[i] < min2) {\n            min2 = tolls[i];\n        }\n    }\n    return tolls[0] + min1 + min2;\n}",
      TYPESCRIPT: "function solve(tolls: number[]): number {\n    let m1 = Infinity, m2 = Infinity;\n    for (let i = 1; i < tolls.length; i++) {\n        if (tolls[i] < m1) {\n            m2 = m1;\n            m1 = tolls[i];\n        } else if (tolls[i] < m2) {\n            m2 = tolls[i];\n        }\n    }\n    return tolls[0] + m1 + m2;\n}",
      PYTHON: "def solve(tolls):\n    m1 = float('inf')\n    m2 = float('inf')\n    for x in tolls[1:]:\n        if x < m1:\n            m2 = m1\n            m1 = x\n        elif x < m2:\n            m2 = x\n    return tolls[0] + m1 + m2",
      JAVA: "    static int solve(int[] tolls) {\n        int m1 = Integer.MAX_VALUE;\n        int m2 = Integer.MAX_VALUE;\n        for (int i = 1; i < tolls.length; i++) {\n            if (tolls[i] < m1) {\n                m2 = m1;\n                m1 = tolls[i];\n            } else if (tolls[i] < m2) {\n                m2 = tolls[i];\n            }\n        }\n        return tolls[0] + m1 + m2;\n    }",
      CPP: "#include <vector>\n\nusing namespace std;\n\nint solve(vector<int> tolls) {\n    int m1 = 1e9, m2 = 1e9;\n    for (size_t i = 1; i < tolls.size(); i++) {\n        if (tolls[i] < m1) {\n            m2 = m1;\n            m1 = tolls[i];\n        } else if (tolls[i] < m2) {\n            m2 = tolls[i];\n        }\n    }\n    return tolls[0] + m1 + m2;\n}",
      GO: "func solve(tolls []int) int {\n    m1 := int(1e9)\n    m2 := int(1e9)\n    for i := 1; i < len(tolls); i++ {\n        if tolls[i] < m1 {\n            m2 = m1\n            m1 = tolls[i]\n        } else if tolls[i] < m2 {\n            m2 = tolls[i]\n        }\n    }\n    return tolls[0] + m1 + m2\n}",
    },
    tests: [
      { stdin: "10 3 1 1 5", expectedStdout: "12", isSample: true },
      { stdin: "5 2 3", expectedStdout: "10", isSample: true },
      { stdin: "1 1 1 1 1", expectedStdout: "3" },
      { stdin: "100 100 1 100 2", expectedStdout: "103" },
      { stdin: "50 40 30 20 10", expectedStdout: "80" },
      { stdin: "10 5 10 5", expectedStdout: "20" },
      { stdin: "3 1 2 3", expectedStdout: "6" },
      { stdin: "1000 1000 1000", expectedStdout: "3000" },
    ],
  }),

  p({
    ...base,
    slug: "identical-shoe-pairs",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Matching Brake Shoes",
    patternTags: ["hash-map","counting"],
    signatureId: "fn:ints->bool",
    avgSolveSeconds: 480,
    promptMarkdown: "You are a mechanic organizing a shipment of brake shoes. Each number in the array `shoes` represents the part number of a specific brake shoe.\nTo properly service vehicles, you need to pair up the brake shoes such that every pair consists of two identical part numbers.\nReturn `true` if it is possible to perfectly divide the entire shipment into pairs of identical brake shoes, or `false` otherwise.\n\n**Constraints**\n- `2 <= shoes.length <= 1000`\n- `shoes.length` is an even number.\n- `1 <= shoes[i] <= 500`\n\n**Example 1**\n```\ninput:\n3 2 3 2 2 2\noutput: true\n```\n*Explanation: We can form three matching pairs: (3,3), (2,2), and (2,2).*\n\n**Example 2**\n```\ninput:\n1 2 3 4\noutput: false\n```\n*Explanation: None of the brake shoes share the same part number to form a pair.*\n\n**Example 3**\n```\ninput:\n1 1\noutput: true\n```\n*Explanation: The two brake shoes have the same part number and form a valid pair.*\n\n**Follow-up**\nIs it possible to count the frequencies using an array instead of a hash map to improve performance?",
    editorialMarkdown: "## Identical Shoe Pairs\nTo determine if all shoes can form pairs of identical styles, we can count the frequency of each shoe style using a hash map or frequency array. Once we have the frequencies, we verify that every style appears an even number of times.\n\n**Trap**: Overcomplicating the state by trying to explicitly construct the pairs. You only need the counts. Also, check all frequencies, not just the first one you encounter.\n\n**Complexity:**\n- **Time Complexity:** \\mathcal{O}(N) where N is the number of shoes, requiring one pass to populate the map and another pass to check frequencies.\n- **Space Complexity:** \\mathcal{O}(U) where U is the number of unique shoe styles, to store in the hash map.",
    referenceSolution: {
      JAVASCRIPT: "function solve(shoes) {\n    let map = new Map();\n    for (let i = 0; i < shoes.length; i++) {\n        map.set(shoes[i], (map.get(shoes[i]) || 0) + 1);\n    }\n    for (let [k, v] of map) {\n        if (v % 2 !== 0) return false;\n    }\n    return true;\n}",
      TYPESCRIPT: "function solve(shoes: number[]): boolean {\n    let map = new Map<number, number>();\n    for (let i = 0; i < shoes.length; i++) {\n        map.set(shoes[i], (map.get(shoes[i]) || 0) + 1);\n    }\n    for (let [k, v] of map) {\n        if (v % 2 !== 0) return false;\n    }\n    return true;\n}",
      PYTHON: "def solve(shoes):\n    counts = {}\n    for s in shoes:\n        counts[s] = counts.get(s, 0) + 1\n    for v in counts.values():\n        if v % 2 != 0:\n            return False\n    return True",
      JAVA: "    static boolean solve(int[] shoes) {\n        java.util.HashMap<Integer, Integer> map = new java.util.HashMap<>();\n        for (int s : shoes) {\n            map.put(s, map.getOrDefault(s, 0) + 1);\n        }\n        for (int v : map.values()) {\n            if (v % 2 != 0) return false;\n        }\n        return true;\n    }",
      CPP: "#include <vector>\n#include <unordered_map>\n\nusing namespace std;\n\nbool solve(vector<int> shoes) {\n    unordered_map<int, int> counts;\n    for (int s : shoes) {\n        counts[s]++;\n    }\n    for (auto const& [key, val] : counts) {\n        if (val % 2 != 0) return false;\n    }\n    return true;\n}",
      GO: "func solve(shoes []int) bool {\n    counts := make(map[int]int)\n    for _, s := range shoes {\n        counts[s]++\n    }\n    for _, v := range counts {\n        if v % 2 != 0 {\n            return false\n        }\n    }\n    return true\n}",
    },
    tests: [
      { stdin: "3 2 3 2 2 2", expectedStdout: "true", isSample: true },
      { stdin: "1 2 3 4", expectedStdout: "false", isSample: true },
      { stdin: "1 1", expectedStdout: "true" },
      { stdin: "10 10 10 10 20 20", expectedStdout: "true" },
      { stdin: "5 5 5 5 5 6", expectedStdout: "false" },
      { stdin: "100 100 200 200 300 300", expectedStdout: "true" },
      { stdin: "1 1 1 1 1 1 1 1", expectedStdout: "true" },
      { stdin: "7 8 7 8 7 8", expectedStdout: "false" },
    ],
  }),

  p({
    ...base,
    slug: "alien-game-score",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Alien Game Score",
    patternTags: ["math","simulation"],
    signatureId: "fn:int,int->int",
    avgSolveSeconds: 420,
    promptMarkdown: "You are playing an alien board game with `N` rounds, numbered 1 to `N`.\nIn each round `i`, you gain `i` points, unless `i` is a multiple of `M`. If `i` is a multiple of `M`, you are penalized and instead *lose* `i` points.\nGiven integers `N` and `M`, return your total score after all `N` rounds.\n\n**Constraints**\n- `1 <= N <= 1000`\n- `1 <= M <= 1000`\n\n**Example 1**\n```\ninput:\n10\n3\noutput: 19\n```\n*Explanation: You gain points for 1, 2, 4, 5, 7, 8, 10 (sum = 37). You lose points for 3, 6, 9 (sum = 18). Score is 37 - 18 = 19.*\n\n**Example 2**\n```\ninput:\n5\n6\noutput: 15\n```\n*Explanation: You gain points for 1, 2, 3, 4, 5. You never lose points since none of these are multiples of 6. Score is 15.*\n\n**Example 3**\n```\ninput:\n5\n1\noutput: -15\n```\n*Explanation: Every number is a multiple of 1, so you lose points every round. Score is -15.*\n\n**Follow-up**\nCan you compute the answer in mathcal{O}(1) time using mathematics?",
    editorialMarkdown: "## Alien Game Score\nWe simply simulate the `N` rounds of the game by iterating `i` from 1 to `N`. At each step, we use the modulo operator to determine if `i` is a multiple of `M`. If it is, we subtract `i` from our score; otherwise, we add `i`.\n\n**Trap**: You can simulate this, but there is an O(1) math formula. If solving with math, be careful of the exact sum bounds and integer division.\n\n**Complexity:**\n- **Time Complexity:** \\mathcal{O}(N) to loop through all numbers. This easily passes for N \\le 1000.\n- **Space Complexity:** \\mathcal{O}(1) because we only need a single integer variable to accumulate the score.",
    referenceSolution: {
      JAVASCRIPT: "function solve(N, M) {\n    let score = 0;\n    for (let i = 1; i <= N; i++) {\n        if (i % M === 0) {\n            score -= i;\n        } else {\n            score += i;\n        }\n    }\n    return score;\n}",
      TYPESCRIPT: "function solve(N: number, M: number): number {\n    let score = 0;\n    for (let i = 1; i <= N; i++) {\n        if (i % M === 0) {\n            score -= i;\n        } else {\n            score += i;\n        }\n    }\n    return score;\n}",
      PYTHON: "def solve(N, M):\n    score = 0\n    for i in range(1, N + 1):\n        if i % M == 0:\n            score -= i\n        else:\n            score += i\n    return score",
      JAVA: "    static int solve(int N, int M) {\n        int score = 0;\n        for (int i = 1; i <= N; i++) {\n            if (i % M == 0) {\n                score -= i;\n            } else {\n                score += i;\n            }\n        }\n        return score;\n    }",
      CPP: "int solve(int N, int M) {\n    int score = 0;\n    for (int i = 1; i <= N; i++) {\n        if (i % M == 0) {\n            score -= i;\n        } else {\n            score += i;\n        }\n    }\n    return score;\n}",
      GO: "func solve(N int, M int) int {\n    score := 0\n    for i := 1; i <= N; i++ {\n        if i % M == 0 {\n            score -= i\n        } else {\n            score += i\n        }\n    }\n    return score\n}",
    },
    tests: [
      { stdin: "10\n3", expectedStdout: "19", isSample: true },
      { stdin: "5\n6", expectedStdout: "15", isSample: true },
      { stdin: "5\n1", expectedStdout: "-15" },
      { stdin: "10\n5", expectedStdout: "25" },
      { stdin: "100\n10", expectedStdout: "3950" },
      { stdin: "1\n1", expectedStdout: "-1" },
      { stdin: "1\n2", expectedStdout: "1" },
      { stdin: "20\n4", expectedStdout: "90" },
    ],
  }),

  p({
    ...base,
    slug: "alchemist-transmutation",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Asteroid Extraction",
    patternTags: ["math","dynamic-programming"],
    signatureId: "fn:int->bool",
    avgSolveSeconds: 600,
    promptMarkdown: "Two space miners are competing to dismantle an asteroid with an initial mass of `M` tons.\nThey take turns making a move. On a miner's turn, they must extract a fragment of mass `x` tons such that `0 < x < M` and `M % x == 0`. The remaining mass of the asteroid is then reduced to `M - x`.\nA miner loses the competition if they are unable to make a valid extraction.\nAssuming both miners strategize flawlessly and play optimally, return `true` if the first miner to act wins the competition, and `false` otherwise.\n\n**Constraints**\n- `1 <= M <= 1000`\n\n**Example 1**\n```\ninput:\n2\noutput: true\n```\n*Explanation: The first miner chooses x = 1 (since 2 % 1 == 0), leaving a mass of 1. The second miner has no valid moves because there is no positive integer x < 1.*\n\n**Example 2**\n```\ninput:\n3\noutput: false\n```\n*Explanation: The first miner is forced to choose x = 1. The remaining mass becomes 2. The second miner then chooses x = 1, leaving a mass of 1 for the first miner, who has no valid moves and loses.*\n\n**Example 3**\n```\ninput:\n10\noutput: true\n```\n*Explanation: With flawless strategy, the first miner can secure a win.*\n\n**Follow-up**\nCan you determine the winner in constant time without resorting to dynamic programming or step-by-step simulation?",
    editorialMarkdown: "## Alchemist Transmutation\nThis game essentially depends on the parity of the starting mass. If `M` is even, the first player can always choose `x = 1` to give the second player an odd mass. If a player receives an odd mass, any divisor they choose must be odd, which means they will always leave an even mass for the other player. This forces the second player to eventually face `M = 1`, having no valid moves.\n\n**Trap**: Trying to solve this with a deep recursive minimax search or dynamic programming without memoization, which might time out if constraints were larger.\n\n**Complexity:**\n- **Time Complexity:** \\mathcal{O}(1) by simply returning whether `M` is even.\n- **Space Complexity:** \\mathcal{O}(1) since no additional memory is required.",
    referenceSolution: {
      JAVASCRIPT: "function solve(M) {\n    return M % 2 === 0;\n}",
      TYPESCRIPT: "function solve(M: number): boolean {\n    return M % 2 === 0;\n}",
      PYTHON: "def solve(M):\n    return M % 2 == 0",
      JAVA: "    static boolean solve(int M) {\n        return M % 2 == 0;\n    }",
      CPP: "bool solve(int M) {\n    return M % 2 == 0;\n}",
      GO: "func solve(M int) bool {\n    return M % 2 == 0\n}",
    },
    tests: [
      { stdin: "2", expectedStdout: "true", isSample: true },
      { stdin: "3", expectedStdout: "false", isSample: true },
      { stdin: "1", expectedStdout: "false" },
      { stdin: "4", expectedStdout: "true" },
      { stdin: "10", expectedStdout: "true" },
      { stdin: "15", expectedStdout: "false" },
      { stdin: "100", expectedStdout: "true" },
      { stdin: "999", expectedStdout: "false" },
    ],
  }),

  p({
    ...base,
    slug: "format-identification-code",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Format Identification Code",
    patternTags: ["strings","simulation"],
    signatureId: "fn:string,int->string",
    avgSolveSeconds: 600,
    promptMarkdown: "You are tasked with formatting a raw alphanumeric string `code` into standardized segments.\nYou need to divide `code` into segments of length `k`. If the string's length is not a multiple of `k`, the very last segment must be padded with `'*'` characters until its length is exactly `k`.\nReturn the segments joined together by a single hyphen `'-'`.\n\n**Constraints**\n- `1 <= code.length <= 100`\n- `1 <= k <= 100`\n- `code` consists of English letters and digits.\n\n**Example 1**\n```\ninput:\nabcdefgh\n3\noutput: abc-def-gh*\n```\n*Explanation: The string is split into \"abc\", \"def\", and \"gh\". The last segment is padded with one '*' to reach length 3.*\n\n**Example 2**\n```\ninput:\nabcd\n2\noutput: ab-cd\n```\n*Explanation: The string divides perfectly into segments of length 2.*\n\n**Example 3**\n```\ninput:\na\n5\noutput: a****\n```\n*Explanation: The string only has 1 character, so we pad it with 4 '*'s.*\n\n**Follow-up**\nCan you do this with a single pass through the string?",
    editorialMarkdown: "## Format Identification Code\nWe iterate through the input string in steps of size `k`, slicing substrings of length `k`. For the final substring, if its length is less than `k`, we pad it with the `'*'` character to ensure it has length `k`. Finally, we join these substrings with a hyphen.\n\n**Trap**: Make sure to check if the extracted slice is shorter than `k` on the final iteration instead of running out of bounds. Also ensure the output string handles edge cases where the string length exactly divides `k` without adding an empty group.\n\n**Complexity:**\n- **Time Complexity:** \\mathcal{O}(N) where N is the length of the string, as we do a single pass to build chunks.\n- **Space Complexity:** \\mathcal{O}(N) for the array used to collect the segments before joining.",
    referenceSolution: {
      JAVASCRIPT: "function solve(code, k) {\n    let res = [];\n    for (let i = 0; i < code.length; i += k) {\n        let chunk = code.substring(i, i + k);\n        while (chunk.length < k) {\n            chunk += '*';\n        }\n        res.push(chunk);\n    }\n    return res.join('-');\n}",
      TYPESCRIPT: "function solve(code: string, k: number): string {\n    let res: string[] = [];\n    for (let i = 0; i < code.length; i += k) {\n        let chunk = code.substring(i, i + k);\n        while (chunk.length < k) {\n            chunk += '*';\n        }\n        res.push(chunk);\n    }\n    return res.join('-');\n}",
      PYTHON: "def solve(code, k):\n    res = []\n    for i in range(0, len(code), k):\n        chunk = code[i:i+k]\n        if len(chunk) < k:\n            chunk += '*' * (k - len(chunk))\n        res.append(chunk)\n    return '-'.join(res)",
      JAVA: "    static String solve(String code, int k) {\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < code.length(); i += k) {\n            if (i > 0) sb.append(\"-\");\n            String chunk = code.substring(i, Math.min(i + k, code.length()));\n            sb.append(chunk);\n            for (int j = chunk.length(); j < k; j++) {\n                sb.append(\"*\");\n            }\n        }\n        return sb.toString();\n    }",
      CPP: "string solve(string code, int k) {\n    string res = \"\";\n    int n = code.length();\n    for (int i = 0; i < n; i += k) {\n        if (i > 0) res += \"-\";\n        string chunk = code.substr(i, k);\n        res += chunk;\n        int m = chunk.length();\n        for (int j = m; j < k; j++) {\n            res += \"*\";\n        }\n    }\n    return res;\n}",
      GO: "func solve(code string, k int) string {\n    var res []string\n    for i := 0; i < len(code); i += k {\n        end := i + k\n        if end > len(code) {\n            end = len(code)\n        }\n        chunk := code[i:end]\n        for len(chunk) < k {\n            chunk += \"*\"\n        }\n        res = append(res, chunk)\n    }\n    return strings.Join(res, \"-\")\n}",
    },
    tests: [
      { stdin: "abcdefgh\n3", expectedStdout: "abc-def-gh*", isSample: true },
      { stdin: "abcd\n2", expectedStdout: "ab-cd", isSample: true },
      { stdin: "a\n5", expectedStdout: "a****" },
      { stdin: "helloworld\n10", expectedStdout: "helloworld" },
      { stdin: "hello\n1", expectedStdout: "h-e-l-l-o" },
      { stdin: "ab\n3", expectedStdout: "ab*" },
      { stdin: "xyz\n2", expectedStdout: "xy-z*" },
      { stdin: "A1B2C3D4\n3", expectedStdout: "A1B-2C3-D4*" },
    ],
  }),
];
