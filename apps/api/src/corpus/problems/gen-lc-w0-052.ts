import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w0-052` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W0_052_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "peak-server-loads",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Peak Server Loads",
    patternTags: ["array","hash-map","sorting"],
    signatureId: "fn:matrix->ints",
    avgSolveSeconds: 400,
    promptMarkdown: "You are analyzing performance data for a network of servers. You are given a 2D integer array `logs` where each `logs[i] = [server_id, load]` indicates that the server with the given `server_id` experienced a CPU load of `load` at some point in time.\n\nReturn a list of the maximum load recorded for each server. The output list should be ordered by `server_id` in ascending order.\n\n**Constraints**\n- The number of rows in `logs` is in the range `[1, 1000]`.\n- `logs[i].length == 2`\n- `1 <= server_id <= 1000`\n- `0 <= load <= 10^5`\n\n**Example 1**\n```\ninput:\n1 50;2 20;1 80;3 10\noutput: 80 20 10\n```\n*Explanation: Server 1 has loads 50 and 80 (max 80). Server 2 has load 20. Server 3 has load 10. Sorted by ID (1, 2, 3) gives [80, 20, 10].*\n\n**Example 2**\n```\ninput:\n10 5;10 10;10 15\noutput: 15\n```\n*Explanation: There is only one server (ID 10) and its maximum load is 15.*\n\n**Example 3**\n```\ninput:\n42 100\noutput: 100\n```\n*Explanation: A single log entry results in a single max load of 100.*\n\n**Follow-up**\nCan you optimize the extraction if the IDs are dense and fit within a small fixed array?",
    editorialMarkdown: "## Peak Server Loads\nWe use a hash map to keep track of the maximum load seen so far for each `server_id`. As we iterate through the logs, we update the map if the current log's load is strictly greater than the one we have stored for that ID. Finally, we extract the keys, sort them in ascending order, and return a list of the corresponding max loads.\n\n**Trap**: Make sure to group by the server ID and sort by the server ID at the end, returning the max loads in that order.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N log N) where N is the number of unique server IDs (due to sorting at the end). The iteration itself is mathcal{O}(L) where L is the number of logs.\n- **Space Complexity:** mathcal{O}(N) to store the max load for each unique server ID in the map.",
    referenceSolution: {
      JAVASCRIPT: "function solve(logs) {\n    const m = new Map();\n    for(const r of logs) {\n        const id = r[0], load = r[1];\n        if(!m.has(id) || load > m.get(id)) m.set(id, load);\n    }\n    const keys = Array.from(m.keys()).sort((a,b)=>a-b);\n    return keys.map(k => m.get(k));\n}",
      TYPESCRIPT: "function solve(logs: number[][]): number[] {\n    const m = new Map<number, number>();\n    for(const r of logs) {\n        const id = r[0], load = r[1];\n        if(!m.has(id) || load > m.get(id)) m.set(id, load);\n    }\n    const keys = Array.from(m.keys()).sort((a,b)=>a-b);\n    return keys.map(k => m.get(k) as number);\n}",
      PYTHON: "def solve(logs):\n    m = {}\n    for r in logs:\n        id_, load = r[0], r[1]\n        if id_ not in m or load > m[id_]:\n            m[id_] = load\n    return [m[k] for k in sorted(m.keys())]",
      JAVA: "    static int[] solve(int[][] logs) {\n        java.util.TreeMap<Integer, Integer> m = new java.util.TreeMap<>();\n        for(int[] r : logs) {\n            int id = r[0], load = r[1];\n            if(!m.containsKey(id) || load > m.get(id)) {\n                m.put(id, load);\n            }\n        }\n        int[] res = new int[m.size()];\n        int idx = 0;\n        for(int val : m.values()) {\n            res[idx++] = val;\n        }\n        return res;\n    }",
      CPP: "vector<int> solve(vector<vector<int>> logs) {\n    map<int, int> m;\n    for(auto& r : logs) {\n        if(m.find(r[0]) == m.end() || r[1] > m[r[0]]) {\n            m[r[0]] = r[1];\n        }\n    }\n    vector<int> res;\n    for(auto& p : m) res.push_back(p.second);\n    return res;\n}",
      GO: "func solve(logs [][]int) []int {\n    m := make(map[int]int)\n    var keys []int\n    for _, r := range logs {\n        id, load := r[0], r[1]\n        if val, exists := m[id]; !exists || load > val {\n            m[id] = load\n            if !exists {\n                keys = append(keys, id)\n            }\n        }\n    }\n    for i := 0; i < len(keys); i++ {\n        for j := i + 1; j < len(keys); j++ {\n            if keys[i] > keys[j] {\n                keys[i], keys[j] = keys[j], keys[i]\n            }\n        }\n    }\n    var res []int\n    for _, k := range keys {\n        res = append(res, m[k])\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "1 50;2 20;1 80;3 10", expectedStdout: "80 20 10", isSample: true },
      { stdin: "10 5;10 10;10 15", expectedStdout: "15", isSample: true },
      { stdin: "42 100", expectedStdout: "100" },
      { stdin: "5 10;4 20;3 30;2 40;1 50", expectedStdout: "50 40 30 20 10" },
      { stdin: "1 1;2 2;3 3;1 4;2 5;3 6", expectedStdout: "4 5 6" },
      { stdin: "100 1000;200 2000;100 3000;300 500;200 1500", expectedStdout: "3000 2000 500" },
      { stdin: "9 99", expectedStdout: "99" },
      { stdin: "1 0;1 0;2 0", expectedStdout: "0 0" },
    ],
  }),

  p({
    ...base,
    slug: "drone-platoon-alignments",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Bakery Box Formatting",
    patternTags: ["array","math","modulo"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A bakery is preparing boxes of assorted pastries. Each box must contain a number of pastries that is evenly divisible by 3. You are provided with an array `boxes`, where `boxes[i]` is the initial number of pastries in the `i`-th box. (Note: A box with 0 pastries is considered valid).\n\nYou can modify a box by either adding one pastry or removing one pastry. Each addition or removal counts as a single adjustment.\n\nDetermine the minimum total number of adjustments needed so that the pastry count in every box is a multiple of 3.\n\n**Constraints**\n- `1 <= boxes.length <= 1000`\n- `1 <= boxes[i] <= 1000`\n\n**Example 1**\n```\ninput:\n1 2 3 4\noutput: 3\n```\n*Explanation: Box 0 starts with 1 pastry; removing 1 makes it 0 (1 adjustment). Box 1 starts with 2; adding 1 makes it 3 (1 adjustment). Box 2 is already a multiple of 3 (0 adjustments). Box 3 starts with 4; removing 1 makes it 3 (1 adjustment). Total adjustments = 3.*\n\n**Example 2**\n```\ninput:\n3 6 9 12\noutput: 0\n```\n*Explanation: All boxes already contain a number of pastries that is evenly divisible by 3.*\n\n**Example 3**\n```\ninput:\n10\noutput: 1\n```\n*Explanation: Removing 1 pastry from the only box leaves 9 pastries, which takes 1 adjustment.*\n\n**Follow-up**\nIf the requirement was to make each box's pastry count a multiple of 5, would the maximum adjustments per box still be bounded to 1?",
    editorialMarkdown: "## Drone Platoon Alignments\nSince we can either add or remove a drone in a single operation, any squad size that is not divisible by 3 can be made divisible by 3 in exactly 1 operation. This is because any number modulo 3 is either 0, 1, or 2. If it's 1, we subtract 1 (1 operation). If it's 2, we add 1 (1 operation). Thus, we simply need to count how many squad sizes are not evenly divisible by 3.\n\n**Trap**: Do not simulate the addition or subtraction, as it unnecessarily complicates the logic. Just check `s % 3 != 0`.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) to iterate over the array once.\n- **Space Complexity:** mathcal{O}(1) as we only maintain a counter.",
    referenceSolution: {
      JAVASCRIPT: "function solve(squads) {\n    let ops = 0;\n    for(const s of squads) if(s % 3 !== 0) ops++;\n    return ops;\n}",
      TYPESCRIPT: "function solve(squads: number[]): number {\n    let ops = 0;\n    for(const s of squads) if(s % 3 !== 0) ops++;\n    return ops;\n}",
      PYTHON: "def solve(squads):\n    return sum(1 for s in squads if s % 3 != 0)",
      JAVA: "    static int solve(int[] squads) {\n        int ops = 0;\n        for(int s : squads) if(s % 3 != 0) ops++;\n        return ops;\n    }",
      CPP: "int solve(vector<int> squads) {\n    int ops = 0;\n    for(int s : squads) if(s % 3 != 0) ops++;\n    return ops;\n}",
      GO: "func solve(squads []int) int {\n    ops := 0\n    for _, s := range squads {\n        if s % 3 != 0 {\n            ops++\n        }\n    }\n    return ops\n}",
    },
    tests: [
      { stdin: "1 2 3 4", expectedStdout: "3", isSample: true },
      { stdin: "3 6 9 12", expectedStdout: "0", isSample: true },
      { stdin: "10", expectedStdout: "1", isSample: true },
      { stdin: "3", expectedStdout: "0" },
      { stdin: "5 5 5 5 5", expectedStdout: "5" },
      { stdin: "1 4 7 10", expectedStdout: "4" },
      { stdin: "2 5 8 11", expectedStdout: "4" },
      { stdin: "100 99 98 97", expectedStdout: "3" },
    ],
  }),

  p({
    ...base,
    slug: "blueprint-page-recovery",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Sensor Network Diagnostics",
    patternTags: ["array","hash-map","counting"],
    signatureId: "fn:ints->ints",
    avgSolveSeconds: 400,
    promptMarkdown: "A factory relies on a network of `n` temperature sensors, which are assigned unique identifiers from `1` to `n`. Due to a calibration error, one sensor's data packet was transmitted twice, and as a result, another sensor's data packet was dropped.\n\nYou are given a 1D integer array `logs` containing `n` sensor IDs that successfully reported their data.\n\nFind the sensor ID that reported twice and the sensor ID that failed to report. Return an array of two integers `[repeated, dropped]`, where `repeated` is the duplicated sensor ID and `dropped` is the missing sensor ID.\n\n**Constraints**\n- `n == logs.length`\n- `2 <= n <= 1000`\n- `1 <= logs[i] <= n`\n- Exactly one sensor ID appears twice and exactly one is missing.\n\n**Example 1**\n```\ninput:\n2 2\noutput: 2 1\n```\n*Explanation: Sensor 2 transmitted twice, while sensor 1 did not transmit.*\n\n**Example 2**\n```\ninput:\n1 2 2 4\noutput: 2 3\n```\n*Explanation: Sensor 2 is recorded twice, and sensor 3 is missing.*\n\n**Example 3**\n```\ninput:\n1 3 3\noutput: 3 2\n```\n*Explanation: Sensor 3 sent two packets, and sensor 2 sent none.*\n\n**Follow-up**\nCan you solve it in mathcal{O}(1) auxiliary space without modifying the input array?",
    editorialMarkdown: "## Blueprint Page Recovery\nWe can solve this problem by iterating through the values and tallying the counts of each observed page number using an array or hash map. After counting, we do a second pass from `1` to `n` and check which number was seen exactly twice (the duplicate) and which was seen exactly zero times (the missing page).\n\n**Trap**: Make sure to return the duplicate page first and the missing page second as specified, as returning them in reverse order will fail the checker.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the number of pages, as we make a constant number of linear passes.\n- **Space Complexity:** mathcal{O}(N) to store the frequency array. It can be solved in mathcal{O}(1) space using mathematical formulas (sum and sum of squares) or by negating values at indices.",
    referenceSolution: {
      JAVASCRIPT: "function solve(pages) {\n    const n = pages.length;\n    const count = new Array(n + 1).fill(0);\n    for(const p of pages) count[p]++;\n    let dup = -1, missing = -1;\n    for(let i = 1; i <= n; i++) {\n        if(count[i] === 2) dup = i;\n        if(count[i] === 0) missing = i;\n    }\n    return [dup, missing];\n}",
      TYPESCRIPT: "function solve(pages: number[]): number[] {\n    const n = pages.length;\n    const count = new Array(n + 1).fill(0);\n    for(const p of pages) count[p]++;\n    let dup = -1, missing = -1;\n    for(let i = 1; i <= n; i++) {\n        if(count[i] === 2) dup = i;\n        if(count[i] === 0) missing = i;\n    }\n    return [dup, missing];\n}",
      PYTHON: "def solve(pages):\n    n = len(pages)\n    count = [0] * (n + 1)\n    for p in pages:\n        count[p] += 1\n    dup, missing = -1, -1\n    for i in range(1, n + 1):\n        if count[i] == 2:\n            dup = i\n        if count[i] == 0:\n            missing = i\n    return [dup, missing]",
      JAVA: "    static int[] solve(int[] pages) {\n        int n = pages.length;\n        int[] count = new int[n + 1];\n        for(int p : pages) count[p]++;\n        int dup = -1, missing = -1;\n        for(int i = 1; i <= n; i++) {\n            if(count[i] == 2) dup = i;\n            if(count[i] == 0) missing = i;\n        }\n        return new int[]{dup, missing};\n    }",
      CPP: "vector<int> solve(vector<int> pages) {\n    int n = pages.size();\n    vector<int> count(n + 1, 0);\n    for(int p : pages) count[p]++;\n    int dup = -1, missing = -1;\n    for(int i = 1; i <= n; i++) {\n        if(count[i] == 2) dup = i;\n        if(count[i] == 0) missing = i;\n    }\n    return {dup, missing};\n}",
      GO: "func solve(pages []int) []int {\n    n := len(pages)\n    count := make([]int, n+1)\n    for _, p := range pages {\n        count[p]++\n    }\n    dup, missing := -1, -1\n    for i := 1; i <= n; i++ {\n        if count[i] == 2 {\n            dup = i\n        }\n        if count[i] == 0 {\n            missing = i\n        }\n    }\n    return []int{dup, missing}\n}",
    },
    tests: [
      { stdin: "2 2", expectedStdout: "2 1", isSample: true },
      { stdin: "1 2 2 4", expectedStdout: "2 3", isSample: true },
      { stdin: "1 3 3", expectedStdout: "3 2", isSample: true },
      { stdin: "4 3 2 4", expectedStdout: "4 1" },
      { stdin: "5 5 1 2 3", expectedStdout: "5 4" },
      { stdin: "1 1 3 4 5", expectedStdout: "1 2" },
      { stdin: "2 3 4 5 5", expectedStdout: "5 1" },
      { stdin: "1 2 4 5 5 6 7", expectedStdout: "5 3" },
    ],
  }),

  p({
    ...base,
    slug: "validate-container-labels",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Validate Container Labels",
    patternTags: ["string","array","validation"],
    signatureId: "fn:strings->strings",
    avgSolveSeconds: 400,
    promptMarkdown: "You are developing a scanning system for cargo containers. A container is considered authenticated if its label follows a specific format: exactly 3 digits, followed by a hyphen `'-'`, followed by exactly 4 uppercase English letters.\n\nYou are given an array of strings `labels`. Return a new array containing only the valid labels in their original order.\n\n**Constraints**\n- `1 <= labels.length <= 1000`\n- `1 <= labels[i].length <= 20`\n- `labels[i]` consists of English letters, digits, hyphens, and underscores.\n\n**Example 1**\n```\ninput:\n123-ABCD 456-XYZa 12-ABCD 999-ZZZZ\noutput: 123-ABCD 999-ZZZZ\n```\n*Explanation: \"123-ABCD\" and \"999-ZZZZ\" match the required format. \"456-XYZa\" has a lowercase letter. \"12-ABCD\" has only 2 digits.*\n\n**Example 2**\n```\ninput:\n000-AAAA\noutput: 000-AAAA\n```\n*Explanation: The label is perfectly valid.*\n\n**Example 3**\n```\ninput:\n1234-ABC 123-ABCD\noutput: 123-ABCD\n```\n*Explanation: \"1234-ABC\" has 4 digits instead of 3.*\n\n**Follow-up**\nCould you implement this validation without using a regular expression library?",
    editorialMarkdown: "## Validate Container Labels\nTo validate the format, we simply write a helper function (or run inline checks) for each string. The constraints specify that the length must be exactly 8. If so, we verify that indices 0-2 are digits, index 3 is a hyphen, and indices 4-7 are uppercase letters. \n\n**Trap**: Make sure to include all checks (length, digit ranges, and uppercase ranges). Built-in regex might be slow or difficult to import in restricted environments, but a manual string check is very straightforward.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the number of labels, because each label is short (max length 20).\n- **Space Complexity:** mathcal{O}(1) auxiliary space, excluding the space for the output array.",
    referenceSolution: {
      JAVASCRIPT: "function solve(labels) {\n    const res = [];\n    for(const l of labels) {\n        if(l.length !== 8) continue;\n        let valid = true;\n        for(let i=0; i<3; i++) if(l[i] < '0' || l[i] > '9') valid = false;\n        if(l[3] !== '-') valid = false;\n        for(let i=4; i<8; i++) if(l[i] < 'A' || l[i] > 'Z') valid = false;\n        if(valid) res.push(l);\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(labels: string[]): string[] {\n    const res: string[] = [];\n    for(const l of labels) {\n        if(l.length !== 8) continue;\n        let valid = true;\n        for(let i=0; i<3; i++) if(l[i] < '0' || l[i] > '9') valid = false;\n        if(l[3] !== '-') valid = false;\n        for(let i=4; i<8; i++) if(l[i] < 'A' || l[i] > 'Z') valid = false;\n        if(valid) res.push(l);\n    }\n    return res;\n}",
      PYTHON: "def solve(labels):\n    res = []\n    for l in labels:\n        if len(l) != 8:\n            continue\n        valid = True\n        for i in range(3):\n            if not ('0' <= l[i] <= '9'):\n                valid = False\n        if l[3] != '-':\n            valid = False\n        for i in range(4, 8):\n            if not ('A' <= l[i] <= 'Z'):\n                valid = False\n        if valid:\n            res.append(l)\n    return res",
      JAVA: "    static String[] solve(String[] labels) {\n        java.util.List<String> res = new java.util.ArrayList<>();\n        for(String l : labels) {\n            if(l.length() != 8) continue;\n            boolean valid = true;\n            for(int i=0; i<3; i++) if(l.charAt(i) < '0' || l.charAt(i) > '9') valid = false;\n            if(l.charAt(3) != '-') valid = false;\n            for(int i=4; i<8; i++) if(l.charAt(i) < 'A' || l.charAt(i) > 'Z') valid = false;\n            if(valid) res.add(l);\n        }\n        return res.toArray(new String[0]);\n    }",
      CPP: "vector<string> solve(vector<string> labels) {\n    vector<string> res;\n    for(string l : labels) {\n        if(l.size() != 8) continue;\n        bool valid = true;\n        for(int i=0; i<3; i++) if(l[i] < '0' || l[i] > '9') valid = false;\n        if(l[3] != '-') valid = false;\n        for(int i=4; i<8; i++) if(l[i] < 'A' || l[i] > 'Z') valid = false;\n        if(valid) res.push_back(l);\n    }\n    return res;\n}",
      GO: "func solve(labels []string) []string {\n    var res []string\n    for _, l := range labels {\n        if len(l) != 8 {\n            continue\n        }\n        valid := true\n        for i := 0; i < 3; i++ {\n            if l[i] < '0' || l[i] > '9' {\n                valid = false\n                break\n            }\n        }\n        if l[3] != '-' {\n            valid = false\n        }\n        for i := 4; i < 8; i++ {\n            if l[i] < 'A' || l[i] > 'Z' {\n                valid = false\n                break\n            }\n        }\n        if valid {\n            res = append(res, l)\n        }\n    }\n    if res == nil {\n        res = make([]string, 0)\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "123-ABCD 456-XYZa 12-ABCD 999-ZZZZ", expectedStdout: "123-ABCD 999-ZZZZ", isSample: true },
      { stdin: "000-AAAA", expectedStdout: "000-AAAA", isSample: true },
      { stdin: "1234-ABC 123-ABCD", expectedStdout: "123-ABCD", isSample: true },
      { stdin: "abc-DEFG 123_ABCD", expectedStdout: "" },
      { stdin: "111-WWWW 222-XXXX 333-YYYY 444-ZZZZ", expectedStdout: "111-WWWW 222-XXXX 333-YYYY 444-ZZZZ" },
      { stdin: "123-ABCDA 1234-ABCD 12-ABCD", expectedStdout: "" },
      { stdin: "987-QWER", expectedStdout: "987-QWER" },
      { stdin: "123-ABCD 123-ABcd", expectedStdout: "123-ABCD" },
    ],
  }),

  p({
    ...base,
    slug: "select-elite-operatives",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "GREEDY",
    title: "Select Elite Operatives",
    patternTags: ["array","sorting","greedy"],
    signatureId: "fn:ints,int->ints",
    avgSolveSeconds: 400,
    promptMarkdown: "You are recruiting operatives for a special mission and need to select the `k` candidates with the highest scores from an assessment. You are given an integer array `scores` and an integer `k`.\n\nFind the `k` operatives with the highest scores and return their scores in the same order they appeared in the original `scores` array.\n\nIf there are ties in the highest scores, always prefer the candidates who appeared earlier in the array.\n\n**Constraints**\n- `1 <= k <= scores.length <= 1000`\n- `-10^4 <= scores[i] <= 10^4`\n\n**Example 1**\n```\ninput:\n2 1 3 3\n2\noutput: 3 3\n```\n*Explanation: The top 2 scores are 3 and 3. In the original array, they appear at indices 2 and 3.*\n\n**Example 2**\n```\ninput:\n-1 -2 -3\n2\noutput: -1 -2\n```\n*Explanation: The top 2 scores are -1 and -2. They appear in that order in the original array.*\n\n**Example 3**\n```\ninput:\n3 4 3 3\n2\noutput: 3 4\n```\n*Explanation: We need the 2 highest scores. The highest is 4. For the second spot, there are multiple 3s. We prefer the one that appeared earlier (index 0). Returning them in their original order gives [3, 4].*\n\n**Follow-up**\nCan you do this in mathcal{O}(N log K) using a priority queue instead of a full sort?",
    editorialMarkdown: "## Select Elite Operatives\nTo maximize the sum of a sequence of length `k`, we clearly must select the `k` largest elements. However, we also need to output them in the exact order they appeared in the original array. This implies we need a double sort.\nFirst, we store pairs of `(score, index)`. We sort this array descending by score, and break ties by picking smaller indices first. Then, we take the top `k` elements and re-sort them by their original `index` ascending.\n\n**Trap**: Pay attention to how ties in scores are handled. The requirements specify that you should prefer elements that appeared earlier, meaning if scores are equal, sort by index ascending.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N log N) where N is the number of scores, due to sorting.\n- **Space Complexity:** mathcal{O}(N) to store the pairs of values and original indices.",
    referenceSolution: {
      JAVASCRIPT: "function solve(scores, k) {\n    let indexed = scores.map((s, i) => ({s, i}));\n    indexed.sort((a, b) => {\n        if (a.s !== b.s) return b.s - a.s;\n        return a.i - b.i;\n    });\n    let topK = indexed.slice(0, k);\n    topK.sort((a, b) => a.i - b.i);\n    return topK.map(x => x.s);\n}",
      TYPESCRIPT: "function solve(scores: number[], k: number): number[] {\n    let indexed = scores.map((s, i) => ({s, i}));\n    indexed.sort((a, b) => {\n        if (a.s !== b.s) return b.s - a.s;\n        return a.i - b.i;\n    });\n    let topK = indexed.slice(0, k);\n    topK.sort((a, b) => a.i - b.i);\n    return topK.map(x => x.s);\n}",
      PYTHON: "def solve(scores, k):\n    indexed = [(s, i) for i, s in enumerate(scores)]\n    indexed.sort(key=lambda x: (-x[0], x[1]))\n    topK = indexed[:k]\n    topK.sort(key=lambda x: x[1])\n    return [x[0] for x in topK]",
      JAVA: "    static int[] solve(int[] scores, int k) {\n        int[][] indexed = new int[scores.length][2];\n        for(int i=0; i<scores.length; i++) {\n            indexed[i][0] = scores[i];\n            indexed[i][1] = i;\n        }\n        java.util.Arrays.sort(indexed, (a, b) -> {\n            if(a[0] != b[0]) return Integer.compare(b[0], a[0]);\n            return Integer.compare(a[1], b[1]);\n        });\n        int[][] topK = new int[k][2];\n        for(int i=0; i<k; i++) topK[i] = indexed[i];\n        java.util.Arrays.sort(topK, (a, b) -> Integer.compare(a[1], b[1]));\n        int[] res = new int[k];\n        for(int i=0; i<k; i++) res[i] = topK[i][0];\n        return res;\n    }",
      CPP: "vector<int> solve(vector<int> scores, int k) {\n    vector<pair<int, int>> indexed;\n    for(int i=0; i<scores.size(); i++) {\n        indexed.push_back({scores[i], i});\n    }\n    sort(indexed.begin(), indexed.end(), [](pair<int,int>& a, pair<int,int>& b) {\n        if(a.first != b.first) return a.first > b.first;\n        return a.second < b.second;\n    });\n    vector<pair<int, int>> topK(indexed.begin(), indexed.begin() + k);\n    sort(topK.begin(), topK.end(), [](pair<int,int>& a, pair<int,int>& b) {\n        return a.second < b.second;\n    });\n    vector<int> res;\n    for(auto p : topK) res.push_back(p.first);\n    return res;\n}",
      GO: "func solve(scores []int, k int) []int {\n    type pair struct { s, i int }\n    indexed := make([]pair, len(scores))\n    for i, s := range scores {\n        indexed[i] = pair{s, i}\n    }\n    for i := 0; i < len(indexed); i++ {\n        for j := i + 1; j < len(indexed); j++ {\n            if indexed[i].s < indexed[j].s || (indexed[i].s == indexed[j].s && indexed[i].i > indexed[j].i) {\n                indexed[i], indexed[j] = indexed[j], indexed[i]\n            }\n        }\n    }\n    topK := indexed[:k]\n    for i := 0; i < len(topK); i++ {\n        for j := i + 1; j < len(topK); j++ {\n            if topK[i].i > topK[j].i {\n                topK[i], topK[j] = topK[j], topK[i]\n            }\n        }\n    }\n    res := make([]int, k)\n    for i, p := range topK {\n        res[i] = p.s\n    }\n    return res\n}",
    },
    tests: [
      { stdin: "2 1 3 3\n2", expectedStdout: "3 3", isSample: true },
      { stdin: "-1 -2 -3\n2", expectedStdout: "-1 -2", isSample: true },
      { stdin: "3 4 3 3\n2", expectedStdout: "3 4", isSample: true },
      { stdin: "5\n1", expectedStdout: "5" },
      { stdin: "1 2 3 4 5\n3", expectedStdout: "3 4 5" },
      { stdin: "5 4 3 2 1\n3", expectedStdout: "5 4 3" },
      { stdin: "1 1 1 1\n2", expectedStdout: "1 1" },
      { stdin: "10 20 30 20 10\n3", expectedStdout: "20 30 20" },
    ],
  }),

  p({
    ...base,
    slug: "extraterrestrial-signal-frequencies",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Extraterrestrial Signal Frequencies",
    patternTags: ["string","hash-map","counting"],
    signatureId: "fn:string->string",
    avgSolveSeconds: 400,
    promptMarkdown: "An extraterrestrial signal is represented as a string of lowercase English letters. You want to analyze the dominant tones by finding the most frequent vowel and the most frequent consonant in the `signal`.\n\nThe vowels are considered to be `'a'`, `'e'`, `'i'`, `'o'`, and `'u'`, and all other lowercase letters are considered consonants.\n\nReturn a string of length 2, where the first character is the most frequent vowel and the second character is the most frequent consonant. \n\nIf there are multiple vowels or consonants with the same maximum frequency, choose the one that is lexicographically smaller (i.e., appears first in the alphabet).\n\n**Constraints**\n- `1 <= signal.length <= 1000`\n- `signal` consists of only lowercase English letters.\n- `signal` contains at least one vowel and at least one consonant.\n\n**Example 1**\n```\ninput:\naabccid\noutput: ac\n```\n*Explanation: The vowels are 'a' (count 2) and 'i' (count 1), so 'a' is the most frequent. The consonants are 'b' (1), 'c' (2), and 'd' (1), so 'c' is the most frequent. The answer is \"ac\".*\n\n**Example 2**\n```\ninput:\nengineering\noutput: en\n```\n*Explanation: Vowels: 'e' (3) and 'i' (2), so 'e' wins. Consonants: 'n' (3), 'g' (2), 'r' (1), so 'n' wins. The answer is \"en\".*\n\n**Example 3**\n```\ninput:\nab\noutput: ab\n```\n*Explanation: 'a' and 'b' are the only vowel and consonant respectively.*\n\n**Follow-up**\nCan you do this in a single pass using simple array indexing instead of hash maps?",
    editorialMarkdown: "## Extraterrestrial Signal Frequencies\nWe maintain two hash maps or frequency arrays: one for vowels and one for consonants. We iterate through the characters in the signal, categorize each as either a vowel or consonant, and increment its count. Finally, we find the character with the maximum count in each category. If there is a tie for the maximum count, we pick the lexicographically smaller character.\n\n**Trap**: Make sure to check if a character is a vowel strictly against the set `a, e, i, o, u`. Handle tie-breaking consistently by comparing the characters themselves when counts are equal.\n\n**Complexity:**\n- **Time Complexity:** mathcal{O}(N) where N is the length of the string, since we make a single pass through the characters.\n- **Space Complexity:** mathcal{O}(1) since the alphabet size is bounded to 26 lowercase English letters.",
    referenceSolution: {
      JAVASCRIPT: "function solve(signal) {\n    const vCount = new Map();\n    const cCount = new Map();\n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    for(const ch of signal) {\n        if(vowels.has(ch)) vCount.set(ch, (vCount.get(ch) || 0) + 1);\n        else cCount.set(ch, (cCount.get(ch) || 0) + 1);\n    }\n    let maxV = 0, bestV = 'z' + 1;\n    for(const [v, count] of vCount.entries()) {\n        if(count > maxV || (count === maxV && v < bestV)) {\n            maxV = count; bestV = v;\n        }\n    }\n    let maxC = 0, bestC = 'z' + 1;\n    for(const [c, count] of cCount.entries()) {\n        if(count > maxC || (count === maxC && c < bestC)) {\n            maxC = count; bestC = c;\n        }\n    }\n    return bestV + bestC;\n}",
      TYPESCRIPT: "function solve(signal: string): string {\n    const vCount = new Map<string, number>();\n    const cCount = new Map<string, number>();\n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    for(const ch of signal) {\n        if(vowels.has(ch)) vCount.set(ch, (vCount.get(ch) || 0) + 1);\n        else cCount.set(ch, (cCount.get(ch) || 0) + 1);\n    }\n    let maxV = 0, bestV = '{';\n    for(const [v, count] of vCount.entries()) {\n        if(count > maxV || (count === maxV && v < bestV)) {\n            maxV = count; bestV = v;\n        }\n    }\n    let maxC = 0, bestC = '{';\n    for(const [c, count] of cCount.entries()) {\n        if(count > maxC || (count === maxC && c < bestC)) {\n            maxC = count; bestC = c;\n        }\n    }\n    return bestV + bestC;\n}",
      PYTHON: "def solve(signal):\n    vCount = {}\n    cCount = {}\n    for c in signal:\n        if c in 'aeiou':\n            vCount[c] = vCount.get(c, 0) + 1\n        else:\n            cCount[c] = cCount.get(c, 0) + 1\n    bestV = min(vCount.keys(), key=lambda k: (-vCount[k], k))\n    bestC = min(cCount.keys(), key=lambda k: (-cCount[k], k))\n    return bestV + bestC",
      JAVA: "    static String solve(String signal) {\n        int[] vCount = new int[26];\n        int[] cCount = new int[26];\n        for(char c : signal.toCharArray()) {\n            if(c=='a'||c=='e'||c=='i'||c=='o'||c=='u') {\n                vCount[c - 'a']++;\n            } else {\n                cCount[c - 'a']++;\n            }\n        }\n        int maxV = 0; char bestV = 'z' + 1;\n        for(int i = 0; i < 26; i++) {\n            if(vCount[i] > maxV) {\n                maxV = vCount[i];\n                bestV = (char)(i + 'a');\n            }\n        }\n        int maxC = 0; char bestC = 'z' + 1;\n        for(int i = 0; i < 26; i++) {\n            if(cCount[i] > maxC) {\n                maxC = cCount[i];\n                bestC = (char)(i + 'a');\n            }\n        }\n        return \"\" + bestV + bestC;\n    }",
      CPP: "string solve(string signal) {\n    map<char, int> vCount, cCount;\n    for(char c : signal) {\n        if(c=='a'||c=='e'||c=='i'||c=='o'||c=='u') vCount[c]++;\n        else cCount[c]++;\n    }\n    int maxV = 0; char bestV = 'z' + 1;\n    for(auto p : vCount) {\n        if(p.second > maxV || (p.second == maxV && p.first < bestV)) {\n            maxV = p.second; bestV = p.first;\n        }\n    }\n    int maxC = 0; char bestC = 'z' + 1;\n    for(auto p : cCount) {\n        if(p.second > maxC || (p.second == maxC && p.first < bestC)) {\n            maxC = p.second; bestC = p.first;\n        }\n    }\n    string res = \"\";\n    res += bestV;\n    res += bestC;\n    return res;\n}",
      GO: "func solve(signal string) string {\n    vCount := make(map[byte]int)\n    cCount := make(map[byte]int)\n    for i := 0; i < len(signal); i++ {\n        c := signal[i]\n        if c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u' {\n            vCount[c]++\n        } else {\n            cCount[c]++\n        }\n    }\n    maxV := 0\n    var bestV byte = 'z' + 1\n    for c, count := range vCount {\n        if count > maxV || (count == maxV && c < bestV) {\n            maxV = count\n            bestV = c\n        }\n    }\n    maxC := 0\n    var bestC byte = 'z' + 1\n    for c, count := range cCount {\n        if count > maxC || (count == maxC && c < bestC) {\n            maxC = count\n            bestC = c\n        }\n    }\n    return string([]byte{bestV, bestC})\n}",
    },
    tests: [
      { stdin: "aabccid", expectedStdout: "ac", isSample: true },
      { stdin: "engineering", expectedStdout: "en", isSample: true },
      { stdin: "ab", expectedStdout: "ab", isSample: true },
      { stdin: "uuzz", expectedStdout: "uz" },
      { stdin: "ox", expectedStdout: "ox" },
      { stdin: "aaaaaab", expectedStdout: "ab" },
      { stdin: "zzyyxwvu", expectedStdout: "uy" },
      { stdin: "ebbbbaaaa", expectedStdout: "ab" },
    ],
  }),
];
