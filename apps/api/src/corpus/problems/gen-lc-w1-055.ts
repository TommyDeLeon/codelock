import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w1-055` (anchored to a public problem index; metadata only).
 *
 * Machine-drafted on 2026-09-16 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W1_055_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "filter-qualified-applicants",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Filter Qualified Applicants",
    patternTags: ["hash-set","filtering","arrays","set"],
    signatureId: "fn:ints,int->ints",
    avgSolveSeconds: 480,
    promptMarkdown: "A recruitment system stores applicant IDs in two lists. The first list contains IDs of applicants who passed the **written test**, and the second list contains IDs of applicants who passed the **interview**. Return a sorted list of IDs for applicants who passed **both** stages and whose ID is strictly greater than `minId`.\n\nIf no applicant satisfies both conditions, return an empty list.\n\n**Constraints**\n- `1 <= written.length, interview.length <= 1000`\n- `1 <= written[i], interview[i] <= 10^5`\n- `0 <= minId <= 10^5`\n- Both lists may contain duplicates; treat each ID as a single applicant.\n\n**Example 1**\n```\ninput:\n3 7 12 20 7\n7 3 20 5\n6\noutput: 7 20\n```\n*Explanation: IDs in both lists are {3, 7, 20}. Of those, 7 and 20 are > 6.*\n\n**Example 2**\n```\ninput:\n1 2 3\n4 5 6\n0\noutput: \n```\n*Explanation: No ID appears in both lists.*\n\n**Example 3**\n```\ninput:\n10 15 20\n15 20 25\n15\noutput: 20\n```\n*Explanation: IDs in both lists are {15, 20}. Only 20 is strictly greater than 15.*\n\n**Follow-up:** Can you solve this in O(n + m) time using a hash set instead of sorting?",
    editorialMarkdown: "## Filter Qualified Applicants\n\nThe core technique is **set intersection with filtering**. Build a hash set from the first list, then iterate the second list and collect IDs that appear in the set. Deduplicate with another set, filter by `> minId`, and sort the result.\n\n**Pattern:** Arrays & Hashing — membership test via hash set.\n\n**Trap:** Forgetting to deduplicate before returning. If an ID appears multiple times in either list, it should still appear only once in the output.\n\n**Complexity:**\n- **Time:** O((n + m) + k log k) where n, m are the list lengths and k is the number of qualifying IDs.\n- **Space:** O(n + k) for the hash set and result.",
    referenceSolution: {
      JAVASCRIPT: "function solve(written, minId) {\n    // The single array carries both lists, separated by one -1.\n    let sep = 0;\n    while (written[sep] !== -1) sep++;\n\n    const passedWritten = new Set();\n    for (let i = 0; i < sep; i++) passedWritten.add(written[i]);\n\n    const seen = new Set();\n    const res = [];\n    for (let i = sep + 1; i < written.length; i++) {\n        const id = written[i];\n        if (passedWritten.has(id) && id > minId && !seen.has(id)) {\n            seen.add(id);\n            res.push(id);\n        }\n    }\n\n    // Insertion sort, ascending.\n    for (let i = 1; i < res.length; i++) {\n        const value = res[i];\n        let j = i - 1;\n        while (j >= 0 && res[j] > value) {\n            res[j + 1] = res[j];\n            j--;\n        }\n        res[j + 1] = value;\n    }\n    return res;\n}",
      TYPESCRIPT: "function solve(written: number[], minId: number): number[] {\n    // The single array carries both lists, separated by one -1.\n    let sep = 0;\n    while (written[sep] !== -1) sep++;\n\n    const passedWritten = new Set<number>();\n    for (let i = 0; i < sep; i++) passedWritten.add(written[i]!);\n\n    const seen = new Set<number>();\n    const res: number[] = [];\n    for (let i = sep + 1; i < written.length; i++) {\n        const id = written[i]!;\n        if (passedWritten.has(id) && id > minId && !seen.has(id)) {\n            seen.add(id);\n            res.push(id);\n        }\n    }\n\n    // Insertion sort, ascending.\n    for (let i = 1; i < res.length; i++) {\n        const value = res[i]!;\n        let j = i - 1;\n        while (j >= 0 && res[j]! > value) {\n            res[j + 1] = res[j]!;\n            j--;\n        }\n        res[j + 1] = value;\n    }\n    return res;\n}",
      PYTHON: "def solve(written, minId):\n    # The single list carries both lists, separated by one -1.\n    sep = 0\n    while written[sep] != -1:\n        sep += 1\n\n    passed_written = set(written[:sep])\n    seen = set()\n    res = []\n    for id_ in written[sep + 1:]:\n        if id_ in passed_written and id_ > minId and id_ not in seen:\n            seen.add(id_)\n            res.append(id_)\n\n    # Insertion sort, ascending.\n    for i in range(1, len(res)):\n        value = res[i]\n        j = i - 1\n        while j >= 0 and res[j] > value:\n            res[j + 1] = res[j]\n            j -= 1\n        res[j + 1] = value\n    return res",
      JAVA: "    static int[] solve(int[] written, int minId) {\n        // The single array carries both lists, separated by one -1.\n        int sep = 0;\n        while (written[sep] != -1) sep++;\n\n        java.util.Set<Integer> passedWritten = new java.util.HashSet<>();\n        for (int i = 0; i < sep; i++) passedWritten.add(written[i]);\n\n        java.util.Set<Integer> seen = new java.util.HashSet<>();\n        java.util.List<Integer> kept = new java.util.ArrayList<>();\n        for (int i = sep + 1; i < written.length; i++) {\n            int id = written[i];\n            if (passedWritten.contains(id) && id > minId && !seen.contains(id)) {\n                seen.add(id);\n                kept.add(id);\n            }\n        }\n\n        int[] res = new int[kept.size()];\n        for (int i = 0; i < kept.size(); i++) res[i] = kept.get(i);\n\n        // Insertion sort, ascending.\n        for (int i = 1; i < res.length; i++) {\n            int value = res[i];\n            int j = i - 1;\n            while (j >= 0 && res[j] > value) {\n                res[j + 1] = res[j];\n                j--;\n            }\n            res[j + 1] = value;\n        }\n        return res;\n    }",
      CPP: "vector<int> solve(vector<int> written, int minId) {\n    // The single vector carries both lists, separated by one -1.\n    int sep = 0;\n    while (written[sep] != -1) sep++;\n\n    unordered_set<int> passedWritten;\n    for (int i = 0; i < sep; i++) passedWritten.insert(written[i]);\n\n    unordered_set<int> seen;\n    vector<int> res;\n    for (int i = sep + 1; i < (int)written.size(); i++) {\n        int id = written[i];\n        if (passedWritten.count(id) && id > minId && !seen.count(id)) {\n            seen.insert(id);\n            res.push_back(id);\n        }\n    }\n\n    // Insertion sort, ascending.\n    for (int i = 1; i < (int)res.size(); i++) {\n        int value = res[i];\n        int j = i - 1;\n        while (j >= 0 && res[j] > value) {\n            res[j + 1] = res[j];\n            j--;\n        }\n        res[j + 1] = value;\n    }\n    return res;\n}",
      GO: "func solve(written []int, minId int) []int {\n\tsep := -1\n\tfor i, v := range written {\n\t\tif v == -1 {\n\t\t\tsep = i\n\t\t\tbreak\n\t\t}\n\t}\n\twSet := make(map[int]bool)\n\tfor _, v := range written[:sep] {\n\t\twSet[v] = true\n\t}\n\tbothMap := make(map[int]bool)\n\tfor _, v := range written[sep+1:] {\n\t\tif wSet[v] && v > minId {\n\t\t\tbothMap[v] = true\n\t\t}\n\t}\n\tres := []int{}\n\tfor k := range bothMap {\n\t\tres = append(res, k)\n\t}\n\t// sort manually\n\tfor i := 0; i < len(res); i++ {\n\t\tfor j := i + 1; j < len(res); j++ {\n\t\t\tif res[j] < res[i] {\n\t\t\t\tres[i], res[j] = res[j], res[i]\n\t\t\t}\n\t\t}\n\t}\n\treturn res\n}",
    },
    tests: [
      { stdin: "3 7 12 20 7 -1 7 3 20 5\n6", expectedStdout: "7 20", isSample: true },
      { stdin: "1 2 3 -1 4 5 6\n0", expectedStdout: "", isSample: true },
      { stdin: "10 15 20 -1 15 20 25\n15", expectedStdout: "20" },
      { stdin: "5 -1 5\n0", expectedStdout: "5" },
      { stdin: "100 -1 100\n100", expectedStdout: "" },
      { stdin: "1 2 3 4 5 -1 5 4 3 2 1\n0", expectedStdout: "1 2 3 4 5" },
      { stdin: "7 7 7 -1 7 7\n0", expectedStdout: "7" },
      { stdin: "10 20 30 40 50 60 70 80 -1 80 60 40 20\n35", expectedStdout: "40 60 80" },
    ],
  }),

  p({
    ...base,
    slug: "nearest-to-baseline",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Closest Temperature Reading",
    patternTags: ["linear-scan","absolute-value","tie-breaking","array"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 420,
    promptMarkdown: "A weather station records a list of temperature readings (which may include negative values) and a target temperature `t`. Find the reading whose absolute difference from `t` is **smallest**. If two readings are equally close to `t`, return the **larger** one.\n\nYou may assume the list is non-empty.\n\n**Constraints**\n- `1 <= readings.length <= 10^4`\n- `-10^5 <= readings[i] <= 10^5`\n- `-10^5 <= t <= 10^5`\n\n**Example 1**\n```\ninput:\n-4 2 1 4 8\n3\noutput: 4\n```\n*Explanation: Distances from 3 are [7, 1, 2, 1, 5]. Both 2 and 4 are distance 1 away; return the larger one, 4.*\n\n**Example 2**\n```\ninput:\n10 -10\n0\noutput: 10\n```\n*Explanation: Both are distance 10 from 0; return the larger one, 10.*\n\n**Example 3**\n```\ninput:\n7\n5\noutput: 7\n```\n*Explanation: Only one element; it is the closest.*\n\n**Follow-up:** Can you solve this in a single pass with O(1) extra space?",
    editorialMarkdown: "## Nearest to Baseline\n\nThis is a **linear scan with running best** problem. Iterate through the array, tracking the best candidate so far. For each element, compute `|deviations[i] - b|`. Update the best if this distance is strictly smaller than the current best, or equal but the element is larger (tie-breaking rule).\n\n**Pattern:** Arrays — single-pass scan with a comparison predicate.\n\n**Trap:** Forgetting the tie-breaking rule. Many solvers return the first closest element found; the problem asks for the *larger* value on a tie.\n\n**Complexity:**\n- **Time:** O(n) — single pass.\n- **Space:** O(1) — only a constant number of variables.",
    referenceSolution: {
      JAVASCRIPT: "function solve(deviations, b) {\n    let best = deviations[0];\n    let bestDist = Math.abs(deviations[0] - b);\n    for (let i = 1; i < deviations.length; i++) {\n        const dist = Math.abs(deviations[i] - b);\n        if (dist < bestDist || (dist === bestDist && deviations[i] > best)) {\n            best = deviations[i];\n            bestDist = dist;\n        }\n    }\n    return best;\n}",
      TYPESCRIPT: "function solve(deviations: number[], b: number): number {\n    let best = deviations[0];\n    let bestDist = Math.abs(deviations[0] - b);\n    for (let i = 1; i < deviations.length; i++) {\n        const dist = Math.abs(deviations[i] - b);\n        if (dist < bestDist || (dist === bestDist && deviations[i] > best)) {\n            best = deviations[i];\n            bestDist = dist;\n        }\n    }\n    return best;\n}",
      PYTHON: "def solve(deviations, b):\n    best = deviations[0]\n    best_dist = abs(deviations[0] - b)\n    for x in deviations[1:]:\n        d = abs(x - b)\n        if d < best_dist or (d == best_dist and x > best):\n            best = x\n            best_dist = d\n    return best",
      JAVA: "    static int solve(int[] deviations, int b) {\n        int best = deviations[0];\n        int bestDist = Math.abs(deviations[0] - b);\n        for (int i = 1; i < deviations.length; i++) {\n            int dist = Math.abs(deviations[i] - b);\n            if (dist < bestDist || (dist == bestDist && deviations[i] > best)) {\n                best = deviations[i];\n                bestDist = dist;\n            }\n        }\n        return best;\n    }",
      CPP: "int solve(vector<int> deviations, int b) {\n    int best = deviations[0];\n    int bestDist = abs(deviations[0] - b);\n    for (int i = 1; i < (int)deviations.size(); i++) {\n        int dist = abs(deviations[i] - b);\n        if (dist < bestDist || (dist == bestDist && deviations[i] > best)) {\n            best = deviations[i];\n            bestDist = dist;\n        }\n    }\n    return best;\n}",
      GO: "func solve(deviations []int, b int) int {\n\tabs := func(x int) int {\n\t\tif x < 0 {\n\t\t\treturn -x\n\t\t}\n\t\treturn x\n\t}\n\tbest := deviations[0]\n\tbestDist := abs(deviations[0] - b)\n\tfor i := 1; i < len(deviations); i++ {\n\t\tdist := abs(deviations[i] - b)\n\t\tif dist < bestDist || (dist == bestDist && deviations[i] > best) {\n\t\t\tbest = deviations[i]\n\t\t\tbestDist = dist\n\t\t}\n\t}\n\treturn best\n}",
    },
    tests: [
      { stdin: "-4 2 1 4 8\n3", expectedStdout: "4", isSample: true },
      { stdin: "10 -10\n0", expectedStdout: "10", isSample: true },
      { stdin: "7\n5", expectedStdout: "7" },
      { stdin: "0\n0", expectedStdout: "0" },
      { stdin: "-5 -3 -1\n-2", expectedStdout: "-1" },
      { stdin: "100000 -100000\n0", expectedStdout: "100000" },
      { stdin: "1 1 1\n1", expectedStdout: "1" },
      { stdin: "-10 -3 3 10\n0", expectedStdout: "3" },
      { stdin: "5 5 -5\n0", expectedStdout: "5" },
    ],
  }),

  p({
    ...base,
    slug: "absent-frequency-codes",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Absent Frequency Codes",
    patternTags: ["hash-set","set-difference","arrays","filtering"],
    signatureId: "fn:ints,ints->ints",
    avgSolveSeconds: 480,
    promptMarkdown: "A warehouse uses integer **frequency codes** in the range `[lo, hi]` (inclusive) to tag products. You are given a sorted array `registered` of codes that have been assigned and an array `bounds` of exactly two integers `[lo, hi]`. Return all codes in `[lo, hi]` that do **not** appear in `registered`, in ascending order. Return an empty list if all codes are present.\n\n**Constraints**\n- `bounds.length == 2`\n- `-100 <= bounds[0] <= bounds[1] <= 100`\n- `0 <= registered.length <= 500`\n- `registered[i]` may be outside `[lo, hi]`; ignore those.\n\n**Example 1**\n```\ninput:\n1 3 5 7\n1 7\noutput: 2 4 6\n```\n*Explanation: The range is [1, 7]. Codes 2, 4, and 6 are missing from the registered list.*\n\n**Example 2**\n```\ninput:\n1 2 3 4 5\n1 5\noutput: \n```\n*Explanation: All codes in [1, 5] are registered; nothing is missing.*\n\n**Example 3**\n```\ninput:\n\n-3 2\noutput: -3 -2 -1 0 1 2\n```\n*Explanation: No codes are registered; all codes in [-3, 2] are missing.*\n\n**Follow-up:** Can you solve this in O(n + (hi - lo)) time?",
    editorialMarkdown: "## Absent Frequency Codes\n\nThis is a **set difference** problem. Build a hash set from `registered`, then iterate every integer in `[lo, hi]` and collect those not in the set.\n\n**Pattern:** Arrays & Hashing — set membership check for range coverage.\n\n**Trap:** Including registered values that fall outside `[lo, hi]`. They must be ignored; the simplest way is to only query set membership for values in the range, rather than filtering the registered array.\n\n**Complexity:**\n- **Time:** O(n + R) where n = registered.length and R = hi − lo + 1.\n- **Space:** O(n) for the hash set.",
    referenceSolution: {
      JAVASCRIPT: "function solve(registered, bounds) {\n    const lo = bounds[0], hi = bounds[1];\n    const seen = new Set(registered);\n    const result = [];\n    for (let c = lo; c <= hi; c++) {\n        if (!seen.has(c)) result.push(c);\n    }\n    return result;\n}",
      TYPESCRIPT: "function solve(registered: number[], bounds: number[]): number[] {\n    const lo = bounds[0], hi = bounds[1];\n    const seen = new Set(registered);\n    const result: number[] = [];\n    for (let c = lo; c <= hi; c++) {\n        if (!seen.has(c)) result.push(c);\n    }\n    return result;\n}",
      PYTHON: "def solve(registered, bounds):\n    lo, hi = bounds\n    seen = set(registered)\n    return [c for c in range(lo, hi + 1) if c not in seen]",
      JAVA: "    static int[] solve(int[] registered, int[] bounds) {\n        int lo = bounds[0], hi = bounds[1];\n        java.util.Set<Integer> seen = new java.util.HashSet<>();\n        for (int v : registered) seen.add(v);\n        java.util.List<Integer> result = new java.util.ArrayList<>();\n        for (int c = lo; c <= hi; c++) {\n            if (!seen.contains(c)) result.add(c);\n        }\n        return result.stream().mapToInt(Integer::intValue).toArray();\n    }",
      CPP: "vector<int> solve(vector<int> registered, vector<int> bounds) {\n    int lo = bounds[0], hi = bounds[1];\n    unordered_set<int> seen(registered.begin(), registered.end());\n    vector<int> result;\n    for (int c = lo; c <= hi; c++) {\n        if (!seen.count(c)) result.push_back(c);\n    }\n    return result;\n}",
      GO: "func solve(registered []int, bounds []int) []int {\n\tlo, hi := bounds[0], bounds[1]\n\tseen := make(map[int]bool)\n\tfor _, v := range registered {\n\t\tseen[v] = true\n\t}\n\tresult := []int{}\n\tfor c := lo; c <= hi; c++ {\n\t\tif !seen[c] {\n\t\t\tresult = append(result, c)\n\t\t}\n\t}\n\treturn result\n}",
    },
    tests: [
      { stdin: "1 3 5 7\n1 7", expectedStdout: "2 4 6", isSample: true },
      { stdin: "1 2 3 4 5\n1 5", expectedStdout: "", isSample: true },
      { stdin: "\n-3 2", expectedStdout: "-3 -2 -1 0 1 2" },
      { stdin: "5\n5 5", expectedStdout: "" },
      { stdin: "0\n-2 2", expectedStdout: "-2 -1 1 2" },
      { stdin: "-5 -3 -1 1 3 5\n-5 5", expectedStdout: "-4 -2 0 2 4" },
      { stdin: "100\n1 5", expectedStdout: "1 2 3 4 5" },
      { stdin: "1 2 3\n1 3", expectedStdout: "" },
    ],
  }),

  p({
    ...base,
    slug: "shared-axis-checkpoint",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Aligned Waypoint Finder",
    patternTags: ["linear-scan","manhattan-distance","coordinates","array"],
    signatureId: "fn:matrix,int->int",
    avgSolveSeconds: 480,
    promptMarkdown: "A rover is stationed at position `(px, py)` on a coordinate grid. You are given a list of `n` waypoints as a 2D array `waypoints` of size `n x 2`, where `waypoints[i] = [xi, yi]`. A waypoint is **aligned** with the rover if it lies on the same row or column, i.e., `xi == px` or `yi == py`.\n\nAmong all aligned waypoints, return the **index** (0-based) of the one with the **smallest Manhattan distance** to the rover. If multiple aligned waypoints share the same smallest distance, return the one with the **smallest index**. If no waypoint is aligned, return `-1`.\n\nThe Manhattan distance between `(px, py)` and `(xi, yi)` is `|px - xi| + |py - yi|`.\n\n**Constraints**\n- `1 <= n <= 10^4`\n- `waypoints.length == n`, `waypoints[i].length == 2`\n- `0 <= px, py, xi, yi <= 10^4`\n- The integer in the second parameter encodes `px * 100000 + py`.\n\n**Example 1**\n```\ninput:\n1 2;3 1;2 4\n200002\noutput: 0\n```\n*Explanation: Rover is at (2, 2). Waypoint 0 (1,2): y=2 ✓ distance 1; Waypoint 1 (3,1): x≠2, y≠2 — not aligned; Waypoint 2 (2,4): x=2 ✓ distance 2. Nearest aligned is index 0, distance 1.*\n\n**Example 2**\n```\ninput:\n1 1;2 2;3 3\n100001\noutput: 0\n```\n*Explanation: Rover is at (1,1). Waypoint 0 (1,1) shares both axes, distance 0.*\n\n**Example 3**\n```\ninput:\n5 5\n300003\noutput: -1\n```\n*Explanation: Rover is at (3,3). Waypoint (5,5) shares neither axis; no aligned waypoint exists.*\n\n**Follow-up:** Can you do this in O(n) time and O(1) extra space?",
    editorialMarkdown: "## Shared Axis Checkpoint\n\nThis is a **linear scan** problem with a filter and a minimum search. Decode `px = param / 100000` and `py = param % 100000`. Then iterate all checkpoints, skip those that share neither axis, and track the index of the minimum Manhattan distance among the reachable ones.\n\n**Pattern:** Arrays — single-pass scan with a filter predicate and a running minimum.\n\n**Trap:** Returning the checkpoint with the smallest distance among *all* checkpoints rather than only the *reachable* ones (those sharing an axis). On a tie in distance, the problem requires returning the earlier (smallest) index, so update only on strict improvement.\n\n**Complexity:**\n- **Time:** O(n) — one pass.\n- **Space:** O(1).",
    referenceSolution: {
      JAVASCRIPT: "function solve(checkpoints, encoded) {\n    const px = Math.floor(encoded / 100000);\n    const py = encoded % 100000;\n    let bestIdx = -1, bestDist = Infinity;\n    for (let i = 0; i < checkpoints.length; i++) {\n        const [xi, yi] = checkpoints[i];\n        if (xi !== px && yi !== py) continue;\n        const dist = Math.abs(xi - px) + Math.abs(yi - py);\n        if (dist < bestDist) { bestDist = dist; bestIdx = i; }\n    }\n    return bestIdx;\n}",
      TYPESCRIPT: "function solve(checkpoints: number[][], encoded: number): number {\n    const px = Math.floor(encoded / 100000);\n    const py = encoded % 100000;\n    let bestIdx = -1, bestDist = Infinity;\n    for (let i = 0; i < checkpoints.length; i++) {\n        const [xi, yi] = checkpoints[i];\n        if (xi !== px && yi !== py) continue;\n        const dist = Math.abs(xi - px) + Math.abs(yi - py);\n        if (dist < bestDist) { bestDist = dist; bestIdx = i; }\n    }\n    return bestIdx;\n}",
      PYTHON: "def solve(checkpoints, encoded):\n    px, py = divmod(encoded, 100000)\n    best_idx, best_dist = -1, float('inf')\n    for i, (xi, yi) in enumerate(checkpoints):\n        if xi != px and yi != py:\n            continue\n        dist = abs(xi - px) + abs(yi - py)\n        if dist < best_dist:\n            best_dist = dist\n            best_idx = i\n    return best_idx",
      JAVA: "    static int solve(int[][] checkpoints, int encoded) {\n        int px = encoded / 100000;\n        int py = encoded % 100000;\n        int bestIdx = -1, bestDist = Integer.MAX_VALUE;\n        for (int i = 0; i < checkpoints.length; i++) {\n            int xi = checkpoints[i][0], yi = checkpoints[i][1];\n            if (xi != px && yi != py) continue;\n            int dist = Math.abs(xi - px) + Math.abs(yi - py);\n            if (dist < bestDist) { bestDist = dist; bestIdx = i; }\n        }\n        return bestIdx;\n    }",
      CPP: "int solve(vector<vector<int>> checkpoints, int encoded) {\n    int px = encoded / 100000;\n    int py = encoded % 100000;\n    int bestIdx = -1, bestDist = INT_MAX;\n    for (int i = 0; i < (int)checkpoints.size(); i++) {\n        int xi = checkpoints[i][0], yi = checkpoints[i][1];\n        if (xi != px && yi != py) continue;\n        int dist = abs(xi - px) + abs(yi - py);\n        if (dist < bestDist) { bestDist = dist; bestIdx = i; }\n    }\n    return bestIdx;\n}",
      GO: "func solve(checkpoints [][]int, encoded int) int {\n\tpx := encoded / 100000\n\tpy := encoded % 100000\n\tbestIdx := -1\n\tbestDist := 1<<31 - 1\n\tabs := func(x int) int {\n\t\tif x < 0 {\n\t\t\treturn -x\n\t\t}\n\t\treturn x\n\t}\n\tfor i, cp := range checkpoints {\n\t\txi, yi := cp[0], cp[1]\n\t\tif xi != px && yi != py {\n\t\t\tcontinue\n\t\t}\n\t\tdist := abs(xi-px) + abs(yi-py)\n\t\tif dist < bestDist {\n\t\t\tbestDist = dist\n\t\t\tbestIdx = i\n\t\t}\n\t}\n\treturn bestIdx\n}",
    },
    tests: [
      { stdin: "1 2;3 1;2 4\n200002", expectedStdout: "0", isSample: true },
      { stdin: "1 1;2 2;3 3\n100001", expectedStdout: "0", isSample: true },
      { stdin: "5 5\n300003", expectedStdout: "-1" },
      { stdin: "0 0\n0", expectedStdout: "0" },
      { stdin: "3 5;3 8;7 5\n300005", expectedStdout: "0" },
      { stdin: "0 5;5 0;3 3\n0", expectedStdout: "0" },
      { stdin: "4 2;4 6;1 4\n400004", expectedStdout: "0" },
      { stdin: "1 0;0 1;2 2\n0", expectedStdout: "0" },
    ],
  }),

  p({
    ...base,
    slug: "extra-supply-tag",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "BIT_MANIPULATION",
    title: "Injected Inventory Character",
    patternTags: ["xor","bit-manipulation","hashing","frequency-count"],
    signatureId: "fn:string,string->string",
    avgSolveSeconds: 420,
    promptMarkdown: "An inventory system stores an **original code** string of lowercase letters. A **scanned code** is produced by taking the original code, inserting exactly one extra lowercase letter at any position (and possibly rearranging all characters). Find and return the extra letter that was inserted.\n\nBoth strings contain only lowercase English letters. The scanned code has exactly one more character than the original code.\n\n**Constraints**\n- `0 <= original.length <= 10^4`\n- `scanned.length == original.length + 1`\n\n**Example 1**\n```\ninput:\nabc\ndbca\noutput: d\n```\n*Explanation: \"dbca\" has one extra 'd' compared to \"abc\".*\n\n**Example 2**\n```\ninput:\n\ny\noutput: y\n```\n*Explanation: The original code is empty; the scanned code is \"y\", so the extra letter is 'y'.*\n\n**Example 3**\n```\ninput:\naabb\naabbc\noutput: c\n```\n*Explanation: \"aabbc\" has one additional 'c' compared to \"aabb\".*\n\n**Follow-up:** Can you find the extra letter in O(n) time using only O(1) extra space (XOR trick)?",
    editorialMarkdown: "## Extra Supply Tag\n\nThe cleanest O(1)-space solution XORs the ASCII codes of every character in both strings together. Because XOR is its own inverse (`x ^ x = 0`), all characters that appear in both strings cancel out, leaving only the extra character.\n\nAlternatively, count character frequencies from the base string, decrement for each character in the received string, and the letter whose count goes negative (or just tally received − base) is the answer.\n\n**Pattern:** Bit Manipulation — XOR cancellation.\n\n**Trap:** Thinking the strings are not shuffled and trying to compare character by character with a pointer — the problem states the received string may be shuffled, so positional comparison fails.\n\n**Complexity:**\n- **Time:** O(n) — one pass through each string.\n- **Space:** O(1) — only an integer accumulator (XOR approach).",
    referenceSolution: {
      JAVASCRIPT: "function solve(base, received) {\n    let x = 0;\n    for (const c of base) x ^= c.charCodeAt(0);\n    for (const c of received) x ^= c.charCodeAt(0);\n    return String.fromCharCode(x);\n}",
      TYPESCRIPT: "function solve(base: string, received: string): string {\n    let x = 0;\n    for (const c of base) x ^= c.charCodeAt(0);\n    for (const c of received) x ^= c.charCodeAt(0);\n    return String.fromCharCode(x);\n}",
      PYTHON: "def solve(base, received):\n    x = 0\n    for c in base:\n        x ^= ord(c)\n    for c in received:\n        x ^= ord(c)\n    return chr(x)",
      JAVA: "    static String solve(String base, String received) {\n        int x = 0;\n        for (char c : base.toCharArray()) x ^= c;\n        for (char c : received.toCharArray()) x ^= c;\n        return String.valueOf((char) x);\n    }",
      CPP: "string solve(string base, string received) {\n    int x = 0;\n    for (char c : base) x ^= c;\n    for (char c : received) x ^= c;\n    return string(1, (char)x);\n}",
      GO: "func solve(base string, received string) string {\n\tx := 0\n\tfor _, c := range base {\n\t\tx ^= int(c)\n\t}\n\tfor _, c := range received {\n\t\tx ^= int(c)\n\t}\n\treturn string(rune(x))\n}",
    },
    tests: [
      { stdin: "abc\ndbca", expectedStdout: "d", isSample: true },
      { stdin: "\ny", expectedStdout: "y", isSample: true },
      { stdin: "aabb\naabbc", expectedStdout: "c" },
      { stdin: "z\nzz", expectedStdout: "z" },
      { stdin: "abcdefghij\nabcdefghijz", expectedStdout: "z" },
      { stdin: "aaaaaa\naaaaaab", expectedStdout: "b" },
      { stdin: "xyz\nxyza", expectedStdout: "a" },
      { stdin: "m\nnm", expectedStdout: "n" },
    ],
  }),

  p({
    ...base,
    slug: "count-safely-spaced-readings",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "BINARY_SEARCH",
    title: "Count Safely Spaced Readings",
    patternTags: ["binary-search","counting","sorted-input","arrays"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 540,
    promptMarkdown: "A quality-control system has two sets of sensor readings encoded in a single array. The first `k` values (given as the second parameter) form set **A**, and the remaining values form set **B** (which is sorted in ascending order). A reading `a` from A is considered **safe** if for **every** reading `b` in B, `|a - b| > threshold`, where `threshold` is the last element of the input array.\n\nMore precisely: the input array is `readings` containing `k + m + 1` values. The first `k` values are set A, the next `m` values are set B (sorted ascending), and the last value is `threshold`. The second parameter is `k`. Return the count of safe readings from A.\n\n**Constraints**\n- `1 <= k, m <= 500`\n- The array length is `k + m + 1`\n- `-1000 <= readings[i] <= 1000`\n- `0 <= threshold <= 1000`\n- Set B is sorted in ascending order.\n\n**Example 1**\n```\ninput:\n4 5 8 1 2 3 2\n3\noutput: 1\n```\n*Explanation: A=[4,5,8], B=[1,2,3], threshold=2. a=4: |4-3|=1, not >2 → not safe. a=5: |5-3|=2, not >2 → not safe. a=8: |8-3|=5>2, |8-2|=6>2, |8-1|=7>2 → safe. Count=1.*\n\n**Example 2**\n```\ninput:\n10 20 30 1 2 3 5\n3\noutput: 3\n```\n*Explanation: A=[10,20,30], B=[1,2,3], threshold=5. For each a in A, check min distance to B. a=10: |10-3|=7>5, |10-2|=8>5, |10-1|=9>5 → safe. Similarly 20 and 30 are safe. Count=3.*\n\n**Example 3**\n```\ninput:\n5 5 0\n1\noutput: 0\n```\n*Explanation: A=[5], B=[5], threshold=0. |5-5|=0 which is NOT >0. Not safe. Count=0.*\n\n**Follow-up:** Can you use binary search on sorted B to check each element of A in O(log m) instead of O(m)?",
    editorialMarkdown: "## Count Safely Spaced Readings\n\nFor each element `a` in set A, we need to verify that *every* element `b` in (sorted) set B satisfies `|a - b| > threshold`. Equivalently, no element of B falls in the closed interval `[a - threshold, a + threshold]`.\n\nSince B is sorted, we can binary-search for the first element ≥ `a - threshold`. If that element is ≤ `a + threshold`, the element `a` is not safe. Otherwise it is safe.\n\n**Pattern:** Binary Search — searching a sorted array to evaluate a range-membership condition.\n\n**Trap:** Using a brute-force O(k·m) scan instead of binary search, or getting the boundary conditions wrong (`>` vs `>=`). A reading is safe only when *no* B element is within distance `threshold` (inclusive at the boundary counts as unsafe).\n\n**Complexity:**\n- **Time:** O(k log m) — binary search for each element of A.\n- **Space:** O(1).",
    referenceSolution: {
      JAVASCRIPT: "function solve(readings, k) {\n    const threshold = readings[readings.length - 1];\n    const A = readings.slice(0, k);\n    const B = readings.slice(k, readings.length - 1);\n    let count = 0;\n    for (const a of A) {\n        const lo = a - threshold, hi = a + threshold;\n        // binary search: find first B[mid] >= lo\n        let left = 0, right = B.length;\n        while (left < right) {\n            const mid = (left + right) >> 1;\n            if (B[mid] < lo) left = mid + 1;\n            else right = mid;\n        }\n        // if left index exists and B[left] <= hi, not safe\n        if (left === B.length || B[left] > hi) count++;\n    }\n    return count;\n}",
      TYPESCRIPT: "function solve(readings: number[], k: number): number {\n    const threshold = readings[readings.length - 1];\n    const A = readings.slice(0, k);\n    const B = readings.slice(k, readings.length - 1);\n    let count = 0;\n    for (const a of A) {\n        const lo = a - threshold, hi = a + threshold;\n        let left = 0, right = B.length;\n        while (left < right) {\n            const mid = (left + right) >> 1;\n            if (B[mid] < lo) left = mid + 1;\n            else right = mid;\n        }\n        if (left === B.length || B[left] > hi) count++;\n    }\n    return count;\n}",
      PYTHON: "def solve(readings, k):\n    threshold = readings[-1]\n    A = readings[:k]\n    B = readings[k:-1]\n    count = 0\n    for a in A:\n        lo, hi = a - threshold, a + threshold\n        # find first index in B >= lo\n        left, right = 0, len(B)\n        while left < right:\n            mid = (left + right) // 2\n            if B[mid] < lo:\n                left = mid + 1\n            else:\n                right = mid\n        if left == len(B) or B[left] > hi:\n            count += 1\n    return count",
      JAVA: "    static int solve(int[] readings, int k) {\n        int threshold = readings[readings.length - 1];\n        int m = readings.length - 1 - k;\n        int count = 0;\n        for (int i = 0; i < k; i++) {\n            int a = readings[i];\n            int lo = a - threshold, hi = a + threshold;\n            // binary search in B = readings[k..k+m-1]\n            int left = k, right = k + m;\n            while (left < right) {\n                int mid = (left + right) / 2;\n                if (readings[mid] < lo) left = mid + 1;\n                else right = mid;\n            }\n            if (left == k + m || readings[left] > hi) count++;\n        }\n        return count;\n    }",
      CPP: "int solve(vector<int> readings, int k) {\n    int threshold = readings.back();\n    int m = (int)readings.size() - 1 - k;\n    int count = 0;\n    for (int i = 0; i < k; i++) {\n        int a = readings[i];\n        int lo = a - threshold, hi = a + threshold;\n        int left = k, right = k + m;\n        while (left < right) {\n            int mid = (left + right) / 2;\n            if (readings[mid] < lo) left = mid + 1;\n            else right = mid;\n        }\n        if (left == k + m || readings[left] > hi) count++;\n    }\n    return count;\n}",
      GO: "func solve(readings []int, k int) int {\n\tthreshold := readings[len(readings)-1]\n\tm := len(readings) - 1 - k\n\tcount := 0\n\tfor i := 0; i < k; i++ {\n\t\ta := readings[i]\n\t\tlo := a - threshold\n\t\thi := a + threshold\n\t\tleft, right := k, k+m\n\t\tfor left < right {\n\t\t\tmid := (left + right) / 2\n\t\t\tif readings[mid] < lo {\n\t\t\t\tleft = mid + 1\n\t\t\t} else {\n\t\t\t\tright = mid\n\t\t\t}\n\t\t}\n\t\tif left == k+m || readings[left] > hi {\n\t\t\tcount++\n\t\t}\n\t}\n\treturn count\n}",
    },
    tests: [
      { stdin: "10 20 30 1 2 3 5\n3", expectedStdout: "3", isSample: true },
      { stdin: "5 5 0\n1", expectedStdout: "0", isSample: true },
      { stdin: "4 5 8 1 2 3 2\n3", expectedStdout: "1" },
      { stdin: "0 1 2 3 0\n1", expectedStdout: "1" },
      { stdin: "100 1 2 3 4 5 10\n1", expectedStdout: "1" },
      { stdin: "-10 0 1 2 3 4 5\n1", expectedStdout: "1" },
      { stdin: "50 1 10 20 30 40 9\n1", expectedStdout: "1" },
      { stdin: "5 1 2 3 4 5 0\n1", expectedStdout: "0" },
    ],
  }),
];
