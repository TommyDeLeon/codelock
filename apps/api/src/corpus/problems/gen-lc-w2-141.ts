import { GENERATED, type ProblemDefinition } from '../problem.js';

/**
 * Generated batch `gen-lc-w2-141` (anchored to a public problem index; metadata only).
 *
 * Drafted with gemini-3.1-pro-low on 2026-09-17 and admitted only after
 * every reference solution passed every test on the judge
 * (`scripts/author-batch.ts`). Statements are original; provenance says so.
 */

const p = (d: ProblemDefinition): ProblemDefinition => d;

const base = { provenance: GENERATED } as const;

export const GEN_LC_W2_141_PROBLEMS: ProblemDefinition[] = [
  p({
    ...base,
    slug: "drone-delivery-route-time",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "CNC Laser Cutter Travel Time",
    patternTags: ["array","geometry","math"],
    signatureId: "fn:matrix->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A CNC laser cutter must process a sequence of holes on a flat metal sheet. The locations of these holes are provided as an array `coords`, where `coords[i] = [x, y]` is the 2D coordinate of the `i`-th hole to process. The cutter head must visit these coordinates in the exact order given.\n\nThe cutter head can move 1 unit in the horizontal, vertical, or diagonal direction in exactly 1 second. Therefore, moving from `(x, y)` to `(x+1, y)`, `(x, y+1)`, or `(x+1, y+1)` takes 1 second.\n\nCalculate and return the minimum total time in seconds the cutter head needs to visit all the coordinates in the specified sequence.\n\n**Constraints**\n- `coords.length` is between `1` and `100`.\n- `coords[i].length` is exactly `2`.\n- `-100 <= coords[i][0], coords[i][1] <= 100`\n\n**Example 1**\n```\ninput:\n  1 1;3 4;-1 0\noutput:\n7\n```\n*Explanation: Moving from (1, 1) to (3, 4) requires 3 seconds. Moving from (3, 4) to (-1, 0) requires 4 seconds. The total time is 7 seconds.*\n\n**Example 2**\n```\ninput:\n  3 2;-2 2\noutput:\n5\n```\n*Explanation: Moving horizontally from x=3 to x=-2 takes 5 seconds.*\n\n**Example 3**\n```\ninput:\n  0 0;0 0\noutput:\n0\n```\n*Explanation: The start and end locations are identical, taking 0 seconds.*\n\n**Follow-up**\nCan you do this with exactly one loop and constant O(1) space?",
    editorialMarkdown: "## Drone Delivery Route Time\n\nThe problem asks us to calculate the total time required for a drone to visit a series of points in order. The key observation is that moving diagonally allows the drone to change both its x and y coordinates by 1 unit simultaneously in 1 second. \n\nTherefore, the minimum time to travel between two points `(x1, y1)` and `(x2, y2)` is governed by the maximum of the absolute differences in their x and y coordinates. Specifically, it's `max(|x2 - x1|, |y2 - y1|)`. We simply iterate through the sequence of coordinates, summing this required time for each adjacent pair of points.\n\n**Trap**: A common mistake is to calculate the Euclidean distance instead of the Chebyshev distance (maximum absolute difference). Always read the movement rules carefully!\n\n**Complexity:**\n- **Time:** O(N), where N is the number of points. We iterate through the list of coordinates exactly once.\n- **Space:** O(1), as we only need to keep track of the accumulated time and don't require any additional scaling data structures.",
    referenceSolution: {
      JAVASCRIPT: "function solve(coords) {\n    let ans = 0;\n    for (let i = 1; i < coords.length; i++) {\n        ans += Math.max(Math.abs(coords[i][0] - coords[i-1][0]), Math.abs(coords[i][1] - coords[i-1][1]));\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(coords: number[][]): number {\n    let ans = 0;\n    for (let i = 1; i < coords.length; i++) {\n        ans += Math.max(Math.abs(coords[i][0] - coords[i-1][0]), Math.abs(coords[i][1] - coords[i-1][1]));\n    }\n    return ans;\n}",
      PYTHON: "def solve(coords):\n    ans = 0\n    for i in range(1, len(coords)):\n        ans += max(abs(coords[i][0] - coords[i-1][0]), abs(coords[i][1] - coords[i-1][1]))\n    return ans",
      JAVA: "    static int solve(int[][] coords) {\n        int ans = 0;\n        for (int i = 1; i < coords.length; i++) {\n            ans += Math.max(Math.abs(coords[i][0] - coords[i - 1][0]), Math.abs(coords[i][1] - coords[i - 1][1]));\n        }\n        return ans;\n    }",
      CPP: "int solve(vector<vector<int>> coords) {\n    int ans = 0;\n    for (size_t i = 1; i < coords.size(); i++) {\n        ans += max(abs(coords[i][0] - coords[i - 1][0]), abs(coords[i][1] - coords[i - 1][1]));\n    }\n    return ans;\n}",
      GO: "func solve(coords [][]int) int {\n    ans := 0\n    for i := 1; i < len(coords); i++ {\n        dx := coords[i][0] - coords[i-1][0]\n        if dx < 0 { dx = -dx }\n        dy := coords[i][1] - coords[i-1][1]\n        if dy < 0 { dy = -dy }\n        if dx > dy {\n            ans += dx\n        } else {\n            ans += dy\n        }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "1 1;3 4;-1 0", expectedStdout: "7", isSample: true },
      { stdin: "3 2;-2 2", expectedStdout: "5", isSample: true },
      { stdin: "0 0;0 0", expectedStdout: "0" },
      { stdin: "100 -100;-100 100", expectedStdout: "200" },
      { stdin: "0 0;1 0;1 1;0 1;0 0", expectedStdout: "4" },
      { stdin: "-5 -5;-5 -5;0 0;0 0", expectedStdout: "5" },
      { stdin: "1 1", expectedStdout: "0" },
      { stdin: "0 0;5 10;15 -5", expectedStdout: "25" },
    ],
  }),

  p({
    ...base,
    slug: "missing-temperature-reading",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Missing Temperature Reading",
    patternTags: ["array","math"],
    signatureId: "fn:ints->int",
    avgSolveSeconds: 300,
    promptMarkdown: "A weather station records the ambient temperature exactly every hour. These temperatures are supposed to form an arithmetic progression (increasing or decreasing by a constant fixed amount each hour). \n\nUnfortunately, due to a transmission error, exactly one temperature reading was lost from the sequence, leaving us with an array of `readings` that is missing one value. The missing value is guaranteed not to be the first or the last reading of the original sequence.\n\nGiven the array `readings` representing the remaining temperatures, return the value of the missing temperature reading.\n\n**Constraints**\n- `3 <= readings.length <= 100`\n- `-10^5 <= readings[i] <= 10^5`\n- The answer is guaranteed to be a valid integer.\n\n**Example 1**\n```\ninput:\n  5 7 11 13\noutput:\n9\n```\n*Explanation: The true arithmetic progression is [5, 7, 9, 11, 13] with a step of 2. 9 is the missing reading.*\n\n**Example 2**\n```\ninput:\n  15 13 12\noutput:\n14\n```\n*Explanation: The progression decreases by 1 each time: [15, 14, 13, 12]. 14 is missing.*\n\n**Example 3**\n```\ninput:\n  0 0 0 0 0\noutput:\n0\n```\n*Explanation: The progression step is 0, so the missing reading must also be 0.*\n\n**Follow-up**\nCan you find the missing value in O(log N) time using binary search?",
    editorialMarkdown: "## Missing Temperature Reading\n\nThe problem provides an array that originally represented an arithmetic progression, but one element was lost. We need to find this missing element.\n\nBecause exactly one element is missing and we know the original array was an arithmetic progression, the overall difference between the first and last elements is distributed over `N` equal steps (where `N` is the length of the given array, meaning the original array had `N + 1` elements). Therefore, the step difference `d` is simply `(readings[-1] - readings[0]) / N`.\n\nBy iterating through the array, we can check if the value at index `i` matches its expected value: `readings[0] + i * d`. The first time we find a mismatch, the expected value is the missing reading!\n\n**Trap**: A common pitfall is calculating the difference `d` by just taking `readings[1] - readings[0]`. This will fail if the very first element removed was originally at index 1! Always use the first and last bounds to find the true stride.\n\n**Complexity:**\n- **Time:** O(N), where N is the number of elements in the `readings` array. We can do this in a single pass. (O(log N) is possible with binary search, but O(N) is sufficient for an Easy problem).\n- **Space:** O(1), as we only track a few variables.",
    referenceSolution: {
      JAVASCRIPT: "function solve(readings) {\n    let n = readings.length;\n    // Integer division by hand: removing the remainder leaves an exact\n    // multiple, so the division truncates toward zero without a helper.\n    let span = readings[n - 1] - readings[0];\n    let d = (span - (span % n)) / n;\n    for (let i = 0; i < n; i++) {\n        if (readings[i] !== readings[0] + i * d) {\n            return readings[0] + i * d;\n        }\n    }\n    return readings[0];\n}",
      TYPESCRIPT: "function solve(readings: number[]): number {\n    const n = readings.length;\n    // Integer division by hand: removing the remainder leaves an exact\n    // multiple, so the division truncates toward zero without a helper.\n    const span = readings[n - 1]! - readings[0]!;\n    const d = (span - (span % n)) / n;\n    for (let i = 0; i < n; i++) {\n        if (readings[i] !== readings[0]! + i * d) {\n            return readings[0]! + i * d;\n        }\n    }\n    return readings[0]!;\n}",
      PYTHON: "def solve(readings):\n    n = len(readings)\n    d = int((readings[-1] - readings[0]) / n)\n    for i in range(n):\n        if readings[i] != readings[0] + i * d:\n            return readings[0] + i * d\n    return readings[0]",
      JAVA: "    static int solve(int[] readings) {\n        int n = readings.length;\n        int d = (readings[n - 1] - readings[0]) / n;\n        for (int i = 0; i < n; i++) {\n            if (readings[i] != readings[0] + i * d) {\n                return readings[0] + i * d;\n            }\n        }\n        return readings[0];\n    }",
      CPP: "int solve(vector<int> readings) {\n    int n = readings.size();\n    int d = (readings[n - 1] - readings[0]) / n;\n    for (int i = 0; i < n; i++) {\n        if (readings[i] != readings[0] + i * d) {\n            return readings[0] + i * d;\n        }\n    }\n    return readings[0];\n}",
      GO: "func solve(readings []int) int {\n    n := len(readings)\n    d := (readings[n-1] - readings[0]) / n\n    for i := 0; i < n; i++ {\n        if readings[i] != readings[0] + i * d {\n            return readings[0] + i * d\n        }\n    }\n    return readings[0]\n}",
    },
    tests: [
      { stdin: "5 7 11 13", expectedStdout: "9", isSample: true },
      { stdin: "15 13 12", expectedStdout: "14", isSample: true },
      { stdin: "0 0 0 0 0", expectedStdout: "0", isSample: true },
      { stdin: "1 2 4 5 6", expectedStdout: "3" },
      { stdin: "-5 -1 3 11", expectedStdout: "7" },
      { stdin: "10 20 40", expectedStdout: "30" },
      { stdin: "100 80 60 20", expectedStdout: "40" },
      { stdin: "-10 -5 5 10 15", expectedStdout: "0" },
      { stdin: "2 4 6 10", expectedStdout: "8" },
      { stdin: "1 3 5 7 11", expectedStdout: "9" },
    ],
  }),

  p({
    ...base,
    slug: "reconstruct-corrupted-spreadsheet",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Impute Missing Sensor Readings",
    patternTags: ["matrix","array","maximum"],
    signatureId: "fn:matrix->matrix",
    avgSolveSeconds: 400,
    promptMarkdown: "You are processing environmental data recorded by a grid of sensors over multiple time intervals. The readings are given as a 2D integer matrix `grid`. Valid readings are non-negative integers, while missing or faulty readings are recorded as `-1`.\n\nTo impute the missing data, you must replace every faulty reading (`-1`) with the highest valid reading recorded by that specific sensor (i.e., the maximum value in that column). You can assume that every sensor has at least one valid reading.\n\nReturn a new 2D matrix representing the fully imputed sensor data.\n\n**Constraints**\n- `grid.length` (rows) is between `1` and `50`.\n- `grid[i].length` (columns) is between `1` and `50`.\n- `grid[i][j]` is either `-1` or between `0` and `100`.\n- Each column will have at least one non-negative integer.\n\n**Example 1**\n```\ninput:\n  1 2 -1;4 -1 6;7 8 9\noutput:\n1 2 9;4 8 6;7 8 9\n```\n*Explanation: The maximum value in column 1 is 8, so the -1 is replaced with 8. The maximum in column 2 is 9, so the -1 is replaced with 9.*\n\n**Example 2**\n```\ninput:\n  -1 0;0 -1\noutput:\n0 0;0 0\n```\n*Explanation: The maximum in column 0 is 0. The maximum in column 1 is 0.*\n\n**Example 3**\n```\ninput:\n  5 -1;-1 10\noutput:\n5 10;5 10\n```\n*Explanation: The missing reading in the first column is replaced by 5, and the missing reading in the second column is replaced by 10.*\n\n**Follow-up**\nCan you solve it using an auxiliary array of size O(C) and exactly two passes over the matrix data?",
    editorialMarkdown: "## Reconstruct Corrupted Spreadsheet\n\nThe problem requires us to replace specific placeholder values (`-1`) in a 2D matrix with the maximum value found in their respective columns.\n\nThe most straightforward way to solve this is using two passes. In the first pass, we iterate through every column and identify the maximum value in that column (ignoring the `-1`s or treating `-1` simply as a valid number to test since all valid inputs are `>= 0`). We can store these maximums in an array of size `cols`. \n\nIn the second pass, we construct the new grid. Whenever we encounter a `-1`, we replace it with the maximum value we stored for that specific column. Otherwise, we keep the original value.\n\n**Trap**: Ensure you iterate correctly for finding column maximums, keeping the column index on the outer loop and the row index on the inner loop, or utilizing a size-matched maximums array. Also, ensure you do not mutate the input if the environment expects a new object.\n\n**Complexity:**\n- **Time:** O(R * C), where R and C are the number of rows and columns in the grid. We scan the matrix twice.\n- **Space:** O(C) for the `max_col` array tracking the maximum values, not including the space for the output matrix.",
    referenceSolution: {
      JAVASCRIPT: "function solve(grid) {\n    let rows = grid.length;\n    let cols = grid[0].length;\n    let maxCol = new Array(cols).fill(-Infinity);\n    for (let c = 0; c < cols; c++) {\n        for (let r = 0; r < rows; r++) {\n            if (grid[r][c] > maxCol[c]) {\n                maxCol[c] = grid[r][c];\n            }\n        }\n    }\n    let ans = [];\n    for (let r = 0; r < rows; r++) {\n        let row = [];\n        for (let c = 0; c < cols; c++) {\n            row.push(grid[r][c] === -1 ? maxCol[c] : grid[r][c]);\n        }\n        ans.push(row);\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(grid: number[][]): number[][] {\n    let rows = grid.length;\n    let cols = grid[0].length;\n    let maxCol = new Array(cols).fill(-1);\n    for (let c = 0; c < cols; c++) {\n        for (let r = 0; r < rows; r++) {\n            if (grid[r][c] > maxCol[c]) {\n                maxCol[c] = grid[r][c];\n            }\n        }\n    }\n    let ans: number[][] = [];\n    for (let r = 0; r < rows; r++) {\n        let row: number[] = [];\n        for (let c = 0; c < cols; c++) {\n            row.push(grid[r][c] === -1 ? maxCol[c] : grid[r][c]);\n        }\n        ans.push(row);\n    }\n    return ans;\n}",
      PYTHON: "def solve(grid):\n    rows = len(grid)\n    cols = len(grid[0])\n    max_col = [-1] * cols\n    for c in range(cols):\n        max_col[c] = max(grid[r][c] for r in range(rows))\n    ans = []\n    for r in range(rows):\n        ans.append([grid[r][c] if grid[r][c] != -1 else max_col[c] for c in range(cols)])\n    return ans",
      JAVA: "    static int[][] solve(int[][] grid) {\n        int rows = grid.length;\n        int cols = grid[0].length;\n        int[] maxCol = new int[cols];\n        for (int c = 0; c < cols; c++) {\n            maxCol[c] = -1;\n            for (int r = 0; r < rows; r++) {\n                if (grid[r][c] > maxCol[c]) {\n                    maxCol[c] = grid[r][c];\n                }\n            }\n        }\n        int[][] ans = new int[rows][cols];\n        for (int r = 0; r < rows; r++) {\n            for (int c = 0; c < cols; c++) {\n                ans[r][c] = grid[r][c] == -1 ? maxCol[c] : grid[r][c];\n            }\n        }\n        return ans;\n    }",
      CPP: "vector<vector<int>> solve(vector<vector<int>> grid) {\n    int rows = grid.size();\n    int cols = grid[0].size();\n    vector<int> maxCol(cols, -1);\n    for (int c = 0; c < cols; c++) {\n        for (int r = 0; r < rows; r++) {\n            if (grid[r][c] > maxCol[c]) {\n                maxCol[c] = grid[r][c];\n            }\n        }\n    }\n    vector<vector<int>> ans(rows, vector<int>(cols));\n    for (int r = 0; r < rows; r++) {\n        for (int c = 0; c < cols; c++) {\n            ans[r][c] = grid[r][c] == -1 ? maxCol[c] : grid[r][c];\n        }\n    }\n    return ans;\n}",
      GO: "func solve(grid [][]int) [][]int {\n    rows := len(grid)\n    cols := len(grid[0])\n    maxCol := make([]int, cols)\n    for c := 0; c < cols; c++ {\n        maxCol[c] = -1\n        for r := 0; r < rows; r++ {\n            if grid[r][c] > maxCol[c] {\n                maxCol[c] = grid[r][c]\n            }\n        }\n    }\n    ans := make([][]int, rows)\n    for r := 0; r < rows; r++ {\n        ans[r] = make([]int, cols)\n        for c := 0; c < cols; c++ {\n            if grid[r][c] == -1 {\n                ans[r][c] = maxCol[c]\n            } else {\n                ans[r][c] = grid[r][c]\n            }\n        }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "1 2 -1;4 -1 6;7 8 9", expectedStdout: "1 2 9;4 8 6;7 8 9", isSample: true },
      { stdin: "-1 0;0 -1", expectedStdout: "0 0;0 0", isSample: true },
      { stdin: "5 -1;-1 10", expectedStdout: "5 10;5 10", isSample: true },
      { stdin: "1 2 3;4 5 6;7 8 9", expectedStdout: "1 2 3;4 5 6;7 8 9" },
      { stdin: "3 -1 2;-1 4 -1", expectedStdout: "3 4 2;3 4 2" },
      { stdin: "1", expectedStdout: "1" },
      { stdin: "-1;5", expectedStdout: "5;5" },
      { stdin: "10 20;30 40;-1 -1", expectedStdout: "10 20;30 40;30 40" },
    ],
  }),

  p({
    ...base,
    slug: "most-frequent-subsequent-page",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "ARRAYS_HASHING",
    title: "Most Frequent Subsequent Page",
    patternTags: ["array","counting","hash-map"],
    signatureId: "fn:ints,int->int",
    avgSolveSeconds: 350,
    promptMarkdown: "You are a data analyst reviewing user clickstream logs for a website. You are given an array of integers `pages` representing the chronological order of page IDs visited by a user. You are also given an integer `target` representing a specific trigger page ID.\n\nYou want to determine which page users most frequently navigate to *immediately after* visiting the `target` page. \n\nFind and return the page ID that most frequently immediately follows the `target` page ID in the array. \n\n**Constraints**\n- `2 <= pages.length <= 1000`\n- `1 <= pages[i], target <= 1000`\n- There will be at least one occurrence of `target` in the `pages` array that is not the last element.\n- The tests guarantee that exactly one page ID has the highest frequency of immediately following the `target`.\n\n**Example 1**\n```\ninput:\n  1 100 2 100 1 100 2\n  100\noutput:\n2\n```\n*Explanation: The target page is 100. It is followed by 2, then by 1, and then by 2. Page 2 follows the target most frequently (twice).*\n\n**Example 2**\n```\ninput:\n  2 2 2 2 3\n  2\noutput:\n2\n```\n*Explanation: The target page is 2. It is followed by 2, then by 2, then by 2, then by 3. Page 2 follows the target 3 times, while 3 follows it only 1 time.*\n\n**Example 3**\n```\ninput:\n  1 2 3 2 4 2 5 2 4\n  2\noutput:\n4\n```\n*Explanation: Page 2 is followed by 3, 4, 5, and 4. Page 4 appears the most.*\n\n**Follow-up**\nCan you solve this with exactly one pass and O(K) space where K is the number of distinct pages?",
    editorialMarkdown: "## Most Frequent Subsequent Page\n\nThis problem requires finding which page ID most often immediately follows a given `target` page ID in a user's chronological clickstream.\n\nThe optimal approach is to iterate through the array of pages from index `0` up to `N - 2`. Whenever we spot `pages[i] == target`, it means the user clicked the target page, and the very next click is `pages[i + 1]`. We use a hash map to keep a tally of these subsequent pages.\n\nFinally, we iterate through our hash map frequencies to determine which page ID was visited the most. The problem implies a unique maximum frequency, so we don't have to worry about complex tie-breaking logic.\n\n**Trap**: Attempting to check `pages[i + 1]` when `i` is at the very end of the array will cause an out-of-bounds error. Make sure to stop the iteration at `N - 2`.\n\n**Complexity:**\n- **Time:** O(N), where N is the length of the `pages` array. We iterate through the array once and then find the maximum frequency in the hash map.\n- **Space:** O(K), where K is the number of distinct pages that follow the `target`. In the worst-case, this is bounded by N.",
    referenceSolution: {
      JAVASCRIPT: "function solve(pages, target) {\n    let counts = new Map();\n    for (let i = 0; i < pages.length - 1; i++) {\n        if (pages[i] === target) {\n            let nxt = pages[i+1];\n            counts.set(nxt, (counts.get(nxt) || 0) + 1);\n        }\n    }\n    let maxCount = 0;\n    let ans = -1;\n    for (let [k, v] of counts.entries()) {\n        if (v > maxCount) {\n            maxCount = v;\n            ans = k;\n        }\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(pages: number[], target: number): number {\n    let counts = new Map<number, number>();\n    for (let i = 0; i < pages.length - 1; i++) {\n        if (pages[i] === target) {\n            let nxt = pages[i+1];\n            counts.set(nxt, (counts.get(nxt) || 0) + 1);\n        }\n    }\n    let maxCount = 0;\n    let ans = -1;\n    for (let [k, v] of counts.entries()) {\n        if (v > maxCount) {\n            maxCount = v;\n            ans = k;\n        }\n    }\n    return ans;\n}",
      PYTHON: "def solve(pages, target):\n    counts = {}\n    for i in range(len(pages) - 1):\n        if pages[i] == target:\n            nxt = pages[i+1]\n            counts[nxt] = counts.get(nxt, 0) + 1\n    max_count = 0\n    ans = -1\n    for k, v in counts.items():\n        if v > max_count:\n            max_count = v\n            ans = k\n    return ans",
      JAVA: "    static int solve(int[] pages, int target) {\n        java.util.HashMap<Integer, Integer> counts = new java.util.HashMap<>();\n        for (int i = 0; i < pages.length - 1; i++) {\n            if (pages[i] == target) {\n                int nxt = pages[i + 1];\n                counts.put(nxt, counts.getOrDefault(nxt, 0) + 1);\n            }\n        }\n        int maxCount = 0;\n        int ans = -1;\n        for (java.util.Map.Entry<Integer, Integer> entry : counts.entrySet()) {\n            if (entry.getValue() > maxCount) {\n                maxCount = entry.getValue();\n                ans = entry.getKey();\n            }\n        }\n        return ans;\n    }",
      CPP: "int solve(vector<int> pages, int target) {\n    unordered_map<int, int> counts;\n    for (size_t i = 0; i < pages.size() - 1; i++) {\n        if (pages[i] == target) {\n            counts[pages[i+1]]++;\n        }\n    }\n    int maxCount = 0;\n    int ans = -1;\n    for (auto& pair : counts) {\n        if (pair.second > maxCount) {\n            maxCount = pair.second;\n            ans = pair.first;\n        }\n    }\n    return ans;\n}",
      GO: "func solve(pages []int, target int) int {\n    counts := make(map[int]int)\n    for i := 0; i < len(pages)-1; i++ {\n        if pages[i] == target {\n            counts[pages[i+1]]++\n        }\n    }\n    maxCount := 0\n    ans := -1\n    for k, v := range counts {\n        if v > maxCount {\n            maxCount = v\n            ans = k\n        }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "1 100 2 100 1 100 2\n100", expectedStdout: "2", isSample: true },
      { stdin: "2 2 2 2 3\n2", expectedStdout: "2", isSample: true },
      { stdin: "1 2 3 2 4 2 5 2 4\n2", expectedStdout: "4", isSample: true },
      { stdin: "10 20 10 20 30\n10", expectedStdout: "20" },
      { stdin: "5 5 5\n5", expectedStdout: "5" },
      { stdin: "1 2 1 2 1 3\n1", expectedStdout: "2" },
      { stdin: "9 8 9 8 9 8\n9", expectedStdout: "8" },
      { stdin: "1 2 3 4 1 2 3 1 2\n1", expectedStdout: "2" },
    ],
  }),

  p({
    ...base,
    slug: "circular-perimeter-patrol",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "MATH_GEOMETRY",
    title: "Circular Assembly Line",
    patternTags: ["array","simulation","counting"],
    signatureId: "fn:ints,int->ints",
    avgSolveSeconds: 450,
    promptMarkdown: "An automated maintenance robot travels along a circular assembly line containing `n` workstations numbered `1` to `n` in sequential order.\n\nThe robot executes a sequence of jobs given as an integer array `rounds`. It starts at workstation `rounds[0]` and travels forward along the line to reach `rounds[1]`, then continues forward to `rounds[2]`, and so on until the final job is reached. Moving forward from workstation `n` takes the robot back to workstation `1`.\n\nDuring its entire journey, the robot's traversal forms a continuous path. The starting workstation is counted as visited once at the beginning, and as the robot passes through or stops at subsequent workstations, each is counted. Workstations where a job is completed and the next job begins are not double-counted.\n\nDetermine which workstations are visited the highest number of times throughout the entire sequence of jobs. Return a list of these most frequently visited workstations, sorted in ascending order.\n\n**Constraints**\n- `n` is between `2` and `100`.\n- `rounds.length` is between `2` and `100`.\n- `1 <= rounds[i] <= n`\n- `rounds[i] != rounds[i + 1]` for all valid `i`.\n\n**Example 1**\n```\ninput:\n  1 3 1 2\n  4\noutput:\n1 2\n```\n*Explanation: The continuous path from start to end goes through workstations in this sequence: 1, 2, 3, 4, 1, 2. The visit counts are: Workstation 1 (2 times), Workstation 2 (2 times), Workstation 3 (1 time), Workstation 4 (1 time). Workstations 1 and 2 are the most visited.*\n\n**Example 2**\n```\ninput:\n  2 1 2 1 2 1 2 1 2\n  2\noutput:\n2\n```\n*Explanation: The continuous path starts and ends at workstation 2, looping through 1 and 2 repeatedly. Workstation 2 is visited one more time than workstation 1.*\n\n**Example 3**\n```\ninput:\n  3 1\n  4\noutput:\n1 3 4\n```\n*Explanation: The robot starts at 3 and travels to 1, visiting workstations 3, 4, and 1. These are each visited once, while workstation 2 is never visited.*\n\n**Follow-up**\nCan you determine the most visited workstations without actually simulating the robot's path step-by-step?",
    editorialMarkdown: "## Circular Perimeter Patrol\n\nThe problem models a guard walking strictly clockwise along a circular perimeter divided into `N` zones. We are given the sequence of zones the guard passes through and must return a sorted list of the zones visited the most.\n\nSince the guard only moves clockwise, the total path taken is a continuous forward traversal around the circle. No matter how many full loops the guard makes, every single zone receives the same base number of visits. The only difference in visit counts arises from the extra partial loop starting at `rounds[0]` and ending at `rounds[rounds.length - 1]`.\n\nTherefore, the intermediary zones listed in the input array don't actually matter! We only need to consider `start = rounds[0]` and `end = rounds[-1]`:\n- If `start <= end`, the most visited zones are exactly `start` through `end`.\n- If `start > end` (meaning the guard crossed the boundary between zone `n` and `1` on the final partial lap), the most visited zones are `1` through `end`, and `start` through `n`. \n\nSince we must return them sorted in ascending order, we simply add the ranges to our result array appropriately.\n\n**Trap**: Simulating the entire walk zone by zone is unnecessary and unnecessarily complex. Rely on the mathematical shortcut dictated by the strictly clockwise movement!\n\n**Complexity:**\n- **Time:** O(N) to generate the result, where N is the total number of zones.\n- **Space:** O(N) to store the return array.",
    referenceSolution: {
      JAVASCRIPT: "function solve(rounds, n) {\n    let start = rounds[0];\n    let end = rounds[rounds.length - 1];\n    let ans = [];\n    if (start <= end) {\n        for (let i = start; i <= end; i++) ans.push(i);\n    } else {\n        for (let i = 1; i <= end; i++) ans.push(i);\n        for (let i = start; i <= n; i++) ans.push(i);\n    }\n    return ans;\n}",
      TYPESCRIPT: "function solve(rounds: number[], n: number): number[] {\n    let start = rounds[0];\n    let end = rounds[rounds.length - 1];\n    let ans: number[] = [];\n    if (start <= end) {\n        for (let i = start; i <= end; i++) ans.push(i);\n    } else {\n        for (let i = 1; i <= end; i++) ans.push(i);\n        for (let i = start; i <= n; i++) ans.push(i);\n    }\n    return ans;\n}",
      PYTHON: "def solve(rounds, n):\n    start = rounds[0]\n    end = rounds[-1]\n    ans = []\n    if start <= end:\n        for i in range(start, end + 1):\n            ans.append(i)\n    else:\n        for i in range(1, end + 1):\n            ans.append(i)\n        for i in range(start, n + 1):\n            ans.append(i)\n    return ans",
      JAVA: "    static int[] solve(int[] rounds, int n) {\n        int start = rounds[0];\n        int end = rounds[rounds.length - 1];\n        java.util.List<Integer> ansList = new java.util.ArrayList<>();\n        if (start <= end) {\n            for (int i = start; i <= end; i++) ansList.add(i);\n        } else {\n            for (int i = 1; i <= end; i++) ansList.add(i);\n            for (int i = start; i <= n; i++) ansList.add(i);\n        }\n        int[] ans = new int[ansList.size()];\n        for (int i = 0; i < ansList.size(); i++) ans[i] = ansList.get(i);\n        return ans;\n    }",
      CPP: "vector<int> solve(vector<int> rounds, int n) {\n    int start = rounds[0];\n    int end = rounds.back();\n    vector<int> ans;\n    if (start <= end) {\n        for (int i = start; i <= end; i++) ans.push_back(i);\n    } else {\n        for (int i = 1; i <= end; i++) ans.push_back(i);\n        for (int i = start; i <= n; i++) ans.push_back(i);\n    }\n    return ans;\n}",
      GO: "func solve(rounds []int, n int) []int {\n    start := rounds[0]\n    end := rounds[len(rounds)-1]\n    var ans []int\n    if start <= end {\n        for i := start; i <= end; i++ {\n            ans = append(ans, i)\n        }\n    } else {\n        for i := 1; i <= end; i++ {\n            ans = append(ans, i)\n        }\n        for i := start; i <= n; i++ {\n            ans = append(ans, i)\n        }\n    }\n    return ans\n}",
    },
    tests: [
      { stdin: "1 3 1 2\n4", expectedStdout: "1 2", isSample: true },
      { stdin: "2 1 2 1 2 1 2 1 2\n2", expectedStdout: "2", isSample: true },
      { stdin: "3 2 1 2 1 3 2 1 4 5\n7", expectedStdout: "3 4 5", isSample: true },
      { stdin: "3 1\n4", expectedStdout: "1 3 4" },
      { stdin: "1 2 3 4\n4", expectedStdout: "1 2 3 4" },
      { stdin: "4 3 2 1\n4", expectedStdout: "1 4" },
      { stdin: "2 3\n3", expectedStdout: "2 3" },
      { stdin: "5 5 5\n5", expectedStdout: "5" },
    ],
  }),

  p({
    ...base,
    slug: "microorganism-population-growth",
    difficulty: "EASY",
    tier: "TIER_1",
    patternFamily: "DP_1D",
    title: "Reactor Energy Output",
    patternTags: ["math","dynamic-programming","fibonacci"],
    signatureId: "fn:int->int",
    avgSolveSeconds: 300,
    promptMarkdown: "Engineers are modeling the energy output of an experimental fusion reactor core over multiple stages. The model predicts that the energy output at any given stage `n` is exactly the sum of the energy outputs from the previous three stages.\n\nThe initial energy outputs for the first three stages are observed as follows:\n- Stage 0: `0` units\n- Stage 1: `1` unit\n- Stage 2: `1` unit\n\nGiven an integer `n`, compute and return the projected energy output for stage `n`.\n\n**Constraints**\n- `0 <= n <= 37`\n- The answer is guaranteed to fit within a 32-bit signed integer.\n\n**Example 1**\n```\ninput:\n  4\noutput:\n4\n```\n*Explanation: The output for stage 3 is 0 + 1 + 1 = 2. The output for stage 4 is 1 + 1 + 2 = 4.*\n\n**Example 2**\n```\ninput:\n  25\noutput:\n1389537\n```\n*Explanation: Following the predictive model for 25 stages yields a total output of 1,389,537 units.*\n\n**Example 3**\n```\ninput:\n  0\noutput:\n0\n```\n*Explanation: The energy output for stage 0 is explicitly defined as 0.*\n\n**Follow-up**\nCan you compute the projected energy output for stage `n` in O(1) space complexity?",
    editorialMarkdown: "## Microorganism Population Growth\n\nThis problem asks us to find the `N`-th term of a sequence where each term is the sum of the three previous terms, with base cases established at days 0, 1, and 2. \n\nThe straightforward recursive formula `F(N) = F(N-1) + F(N-2) + F(N-3)` is extremely slow due to redundant calculations, running in exponential time. \n\nInstead, we can use an iterative dynamic programming approach. We only ever need to know the population of the last three days to compute the current day's population. By maintaining three variables (`t0`, `t1`, `t2`), we can calculate the next day's population, and then shift our window of variables forward by one day.\n\n**Trap**: Recursive solutions will face a Time Limit Exceeded (TLE) error for large `n`. Ensure you use iteration or memoization!\n\n**Complexity:**\n- **Time:** O(N). We calculate the population sequentially up to day `N`.\n- **Space:** O(1). We only need three variables regardless of how large `N` is.",
    referenceSolution: {
      JAVASCRIPT: "function solve(n) {\n    if (n === 0) return 0;\n    if (n <= 2) return 1;\n    let t0 = 0, t1 = 1, t2 = 1;\n    for (let i = 3; i <= n; i++) {\n        let nxt = t0 + t1 + t2;\n        t0 = t1;\n        t1 = t2;\n        t2 = nxt;\n    }\n    return t2;\n}",
      TYPESCRIPT: "function solve(n: number): number {\n    if (n === 0) return 0;\n    if (n <= 2) return 1;\n    let t0 = 0, t1 = 1, t2 = 1;\n    for (let i = 3; i <= n; i++) {\n        let nxt = t0 + t1 + t2;\n        t0 = t1;\n        t1 = t2;\n        t2 = nxt;\n    }\n    return t2;\n}",
      PYTHON: "def solve(n):\n    if n == 0: return 0\n    if n <= 2: return 1\n    t0, t1, t2 = 0, 1, 1\n    for i in range(3, n + 1):\n        t0, t1, t2 = t1, t2, t0 + t1 + t2\n    return t2",
      JAVA: "    static int solve(int n) {\n        if (n == 0) return 0;\n        if (n <= 2) return 1;\n        int t0 = 0, t1 = 1, t2 = 1;\n        for (int i = 3; i <= n; i++) {\n            int nxt = t0 + t1 + t2;\n            t0 = t1;\n            t1 = t2;\n            t2 = nxt;\n        }\n        return t2;\n    }",
      CPP: "int solve(int n) {\n    if (n == 0) return 0;\n    if (n <= 2) return 1;\n    int t0 = 0, t1 = 1, t2 = 1;\n    for (int i = 3; i <= n; i++) {\n        int nxt = t0 + t1 + t2;\n        t0 = t1;\n        t1 = t2;\n        t2 = nxt;\n    }\n    return t2;\n}",
      GO: "func solve(n int) int {\n    if n == 0 { return 0 }\n    if n <= 2 { return 1 }\n    t0, t1, t2 := 0, 1, 1\n    for i := 3; i <= n; i++ {\n        nxt := t0 + t1 + t2\n        t0 = t1\n        t1 = t2\n        t2 = nxt\n    }\n    return t2\n}",
    },
    tests: [
      { stdin: "4", expectedStdout: "4", isSample: true },
      { stdin: "25", expectedStdout: "1389537", isSample: true },
      { stdin: "0", expectedStdout: "0", isSample: true },
      { stdin: "1", expectedStdout: "1" },
      { stdin: "2", expectedStdout: "1" },
      { stdin: "3", expectedStdout: "2" },
      { stdin: "5", expectedStdout: "7" },
      { stdin: "37", expectedStdout: "2082876103" },
    ],
  }),
];
